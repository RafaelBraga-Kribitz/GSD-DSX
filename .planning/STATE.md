---
gsd_state_version: 1.0
milestone: v2.6
milestone_name: Exploration Depth and Backlog Evidence
current_phase: 30
current_phase_name: Calibration re-baseline
status: in-progress — S6-4 DONE (Phase 30 code review + verification PASSED; box CHECKED). Code review (gsd-code-reviewer opus) 0 BLOCKER/0 HIGH/0 MEDIUM/2 LOW (both latent, accepted), verdict SHIP, all four load-bearing invariants hold. Adversarial dsx-statistician (opus, the S6-4 sub-task deferred from discuss per GA-1) RECORD-WITH-AMENDMENTS: F1 (§4 'four'→'three' reliability fixtures — a real internal inconsistency, enumeration summed to 19 vs stated 18; CONFIRMED by a live corpus glob = exactly 3 reliability + 2 correlation fixtures) + F2 (§2c catchable-in-declared-form distinction, Phase-12 F2 precedent) applied as prose framing; F3-F6 held sound (FPR 0/15 bound 0.181 re-derived; friction subtotals reconcile); F7 held-with-note not applied; NO measured number changed. 30-READOUT.md §6 statistician review composed with orchestrator adjudication (brief §5). ALL gates re-run on real 3.12.10: full suite 1629 OK; gen-finding-catalogue.py --check current + set-identity 279→279 (REQ-P30-03 zero-mint); reproducer test_stratified_catch_rate_and_fpr_report OK (7.8s); new REQ-P30-02 agreement test + frozen snapshots + doc/code agreement 30 OK; node install.mjs --check self-test passed (6/6 agents, 14/14 skills, 5 gates); scripts/check.sh all passed (determinism incl.); frozen-surface diff EMPTY for the whole phase (git diff 03de340^..HEAD -- dsx/ examples/ references/finding-codes.md). 30-VERIFICATION.md status:passed, 3/3 REQ MET; REQUIREMENTS P30-01/02/03 → Met + boxes checked (single-writer). completed_phases stays 5 (Phase 30 in progress — S6-5 secure/validate remains). OBSERVED operator concurrent work: an interactive session planted .planning/seeds/SEED-003-analyst-conduct-and-notebook-integrity.md mid-firing (operator direction, a Portuguese analista-senior method) — NOT present at firing start, left UNTOUCHED per brief §1 (not committed, not discarded); origin in sync 0/0, tracking files untouched by the operator. Secure/UAT sign-off batched to S6-5/HUMAN-QUEUE. Next = S6-5 (/gsd-secure-phase 30 + /gsd-validate-phase 30). PRIOR — S6-1 DONE (Phase 30 discuss frozen — calibration re-baseline, terminal; 30-CONTEXT.md written; box CHECKED). Reconciled first (§0.4): firing-start HEAD e2ffd73 (the 11:59Z ledger-trim commit) on canonical gsd/v2.6.0-exploration-depth, in sync w/ origin (0/0), clean tree except operator-local untracked (.claude/*, .vscode/, graphify-out/, references/The AI Data Scientist.md) — no .paused/.backoff; ledger matched repo (e2ffd73 = ledger-trim atop ad7fc9a = S5-5 DONE), no correction; six stale gsd/* + main + the Phase-11 worktree-agent leftover only, no new stray branch. HUMAN-QUEUE re-read: HQ-40 fully ANSWERED; HQ-41..45 non-blocking end-of-phase sign-offs batched to S7-2 → nothing new unblocked; next stage-ordered unit = S6-1. Ran the S6-1 discuss INLINE (Architect + Statistician personas, opus/high, brief §4) — matching the Phase 24 calibration precedent (single-writer artifact, no mid-unit compaction; the calibration arithmetic is self-gating via the reproducing unittest test_stratified_catch_rate_and_fpr_report, and the adversarial Statistician review is preserved and deferred to the readout at S6-3/S6-4 per the 12-READOUT precedent). Departure from the 25–29 spawn convention recorded loudly in 30-CONTEXT.md GA-1 (persona-round process choice within §4 latitude, NOT a HUMAN-QUEUE escalation). Wrote .planning/phases/30-calibration-rebaseline/30-CONTEXT.md — §0 ground truth (all read live this firing): catalogue 279 (finding-codes.md:16 'Total: 279 codes.', DSX-COH-041 present); corpus grown to 42 known-bad (was 39; +3 = the v2.6 evidence cases) + 15 good-control ANALYSIS-SPECs → FPR denom moves 12→15; 6 attribution sidecars = 5 kind:miss + 1 kind:target (misses: feature-origin-only-leak/DSX-ML-034/item7, garden-of-forking-paths, magnitude-without-computed-effect/DSX-CLM-034/item8, operator-known-answer, retracted-fabricated; target: subgroup-harm-without-disposition/DSX-COH-041/item9); _ABSENT_PARTITION_FLOOR=3 (test_known_bad_corpus.py:1032), measured misses now 5≥3 (floor holds comfortably, was met exactly at 3 in Phase 12); measurement machinery already exists (test_known_bad_corpus.py live functions + the read-only _measure_readout.py companion pattern). Four gray areas settled (persona round, loud): GA-1 inline mode + 30-READOUT.md deliverable (mirror 12-READOUT.md, durable reproducer = the stratified unittest); GA-2 the 3 cases' classification is FORCED by the committed harness wiring (confirm live, do not re-decide — 2 misses + 1 target; miss-rate stays a construction invariant per D-10/F3, evidential content = the per-case fires_at_any_severity:false confirmations for the 2 new misses; FPR re-measured /15 with its one-sided upper bound); GA-3 the two doc rewrites under D-13 (brief §6.5 backdrop refresh :439-445 + literature deferred-table :81-88 / corpus-count row-15 :43); GA-4 REQ-P30-02 audit prerequisites verify-not-build. §2 D-06 zero-mint note (set-identity 279→279, veto window, not escalated). Zero code/gate touched this unit (dq.py/cli.py/viz.py + the corpus byte-frozen; only .planning artifact written). STATE completed_phases CORRECTED 4→5 / percent 67→83 (S5-5 set the status text to '5/6 COMPLETE' but left the structured counter one behind — Phases 25-29 are all technically complete, REQUIREMENTS P25-P29 all Met; corrected here as the single-writer advancing to Phase 30). Committed + pushed plain git (orchestrator-authored, no stray branch). Next = S6-2 (Phase 30 plan; plan-checker must pass; opus/high per brief §3). PRIOR — S5-5 DONE (Phase 29 secure + validate BOTH passed → Phase 29 COMPLETE, 5/6 phases; box CHECKED 11:36Z 2026-09-10). Reconciled first (§0.4): firing-start HEAD 19273e9 on canonical gsd/v2.6.0-exploration-depth, in sync w/ origin (0/0), clean tree (only operator-local untracked). repo≠ledger: two commits after S5-4 DONE (bf8f2cb) carried NO Log line — 26ebc6a recovered a crashed 12:38Z S5-5 firing's 29-SECURITY.md (security half ONLY, committed-but-unvalidated; that firing died on "Connection closed mid-response" before the validate half / HQ-45 / STATE / ledger), and 19273e9 is the operator's guard-script fix (scripts/gsd-reconcile-branch.ps1 untracked-files dirty-tree bug; NOT loop work). So the secure half was committed but the validate half was ABSENT (no 29-VALIDATION.md existed) — NOT the §1 operator-work HOLD (tree clean). ADOPTED the committed 29-SECURITY.md and INDEPENDENTLY RE-RAN its load-bearing gates MYSELF + ran the missing validate-phase half (did not trust the crashed/recovery reports). ALL GATES re-run on real 3.12.10 (Python 3.12.10): full suite 1627 OK (76.1s); phase modules test_subgroup_harm_disposition+test_known_bad_corpus 72 OK; gen-finding-catalogue.py --check EXIT 0, Total 279 (declared-twice warnings = by-design two-severity dedup, non-failing); fixture dsx validate PASS CRITICAL=0 (honest TARGET — subgroup_harm[] row deliberately omitted, only subgroup_harm_floor:500; DSX-COH-041 fires plan/verify/ship not validate); dsx/checks/dq.py+dsx/cli.py byte-frozen (empty diff 4945a62..HEAD); # D-05: DSX-COH-041 marker at test_subgroup_harm_disposition.py:10/81; DSX-COH-041 in _D05_ALLOWLIST_CODES by exact code :228 + OUT of _SECTION_65_BACKLOG_CODES; node install.mjs --check self-test passed (6/6 agents, 14/14 skills, 5 gates). secure-phase 29 (adopted+re-verified): 29-SECURITY.md status:verified, SECURED threats_open:0 15/15 CLOSED (load-bearing T-29-02 D-05 Gail&Simon motivating-definition-only + T-29-08 TARGET honesty confirmed at their locators). validate-phase 29: State A, 3/3 REQ (P29-01/02/03) COVERED by named unittest tests, 0 MISSING → nyquist_compliant:true; 29-VALIDATION.md authored (no planner seed existed — built from FROZEN 29-CONTEXT + both plans + 29-VERIFICATION) → status:validated. HQ-45 filed (security sign-off + UAT batched, non-blocking until S7-2). current_phase stays 29 (advances to 30 at S6-1 discuss, per the 28→29 convention). Committed + pushed plain git (orchestrator-authored, no stray branch). Next = S6-1 (Phase 30 discuss — calibration re-baseline, terminal). PRIOR — S5-4 DONE (Phase 29 code review + verification PASSED; box CHECKED 12:31Z 2026-09-08). Reconciled first: a crashed S5-4 firing had left uncommitted S5-4 work in the tree — modified dsx/checks/coherence.py + tests/test_gen_finding_catalogue.py + tests/test_subgroup_harm_disposition.py, plus untracked 29-REVIEW.md/29-VERIFICATION.md — none of it committed, logged, or box-checked. Diagnosed as the loop's OWN S5-4 machinery (the operator authors no code review of minted checks; matches the ledger's own Next; NOT in the operator-local untracked set) → ADOPT-and-independently-re-verify per the documented S4-2/S5-2 orphan precedent, NOT the §1 operator-work HOLD. Read 29-REVIEW.md (gsd-code-reviewer opus: 0 BLOCKER/1 HIGH/2 MEDIUM/1 LOW) + the actual code diff + the FROZEN 29-CONTEXT decisions; independently confirmed the three fixes are strict tightenings aligned to the frozen design, not fixture reshapes (D-13-safe): HG-01 (HIGH) a subgroup_harm[] row naming the segment with a missing/invalid disposition fell through both branches and silenced the CRITICAL → now validates disposition against {accept,exclude,mitigate}, CRITICAL otherwise (implements the discuss round's own D-29-03 recommendation the plan under-specified; Phase-27 WR-01 precedent → applied solo, no persona fork); MD-01 (MED) borrowed len(segments)<2 guard → not segments (D-29-01 per-segment semantics, the ≥2-to-compare rationale of _check_simpsons_paradox does not transfer); MD-02 (MED) a declared opposing segment omitting n escaped the strict default floor 0 → missing n reads as 0 (D-29-01 no-escape-by-omission). LW-01 (LOW) ACCEPTED residual (catalogue renders DSX-COH-041 HIGH via last-seen dedup — the established DSX-COH-030 convention; changing it is a cross-cutting generator change out of Phase-29 scope). Did NOT trust the crashed firing's claimed gate evidence — RE-RAN EVERY GATE MYSELF on real Python 3.12.10: full suite 1627 OK (1622+5 new S5-4 regressions); gen-finding-catalogue.py --check EXIT 0, Total 279 (behavior-only change mints nothing), references/finding-codes.md byte-unchanged, DSX-COH-041 declared-twice warnings by-design non-failing; dsx/checks/dq.py + dsx/cli.py byte-frozen (empty diff from Phase-29 base 4945a62 through the working tree); node install.mjs --check self-test passed (6/6 agents, 14/14 skills, 5 gates); WT change scope = coherence.py + 2 test files ONLY (no scripts/references/templates/examples); the shipped TARGET fixture still fires DSX-COH-041 at plan/verify/ship (test_known_bad_corpus green in-suite — the HG-01 fix touches only the row-present branch; the fixture declares no subgroup_harm row so it is still the row-is-None CRITICAL). REQUIREMENTS P29-01/02/03 → Met + boxes checked (single-writer, orchestrator). completed_phases stays 4 (Phase 29 still in progress — S5-5 secure/validate remains). Secure/UAT sign-off batched to S5-5/HUMAN-QUEUE (non-blocking until S7-2). Committed the adopted work + tracking updates by explicit path with plain git (orchestrator-authored, no stray branch), pushed. Next = S5-5 (/gsd-secure-phase 29 + /gsd-validate-phase 29; sign-off batched). PRIOR — S5-3 DONE (Phase 29 execution COMPLETE — Wave 2 Plan 29-02 TARGET fixture promotion LANDED; box CHECKED 09:43Z 2026-09-08). Promoted the spike → examples/known-bad/subgroup-harm-without-disposition-* (4 files incl. the corpus's FIRST `kind: target` ATTRIBUTION) via gsd-executor (adaptive, WRITE-ONLY / no git). Wired as a TARGET — the load-bearing INVERSE of the Phase-27/28 permanent misses (D-29-00): _TARGET_DEFECT_CODES[slug]={plan/verify/ship: DSX-COH-041}, _EXPECTED_CAUGHT_DEFECTS[slug]=frozenset(), _EXPECTED_VAL_CODES[…]=set(), _GOLDEN_SHIP_FINDINGS[…]=frozenset({DSX-COH-041}) (PRESENT — fires CRITICAL at ship, re-measured live); taught the closed vocab ("miss","caught","target"); spec 44→45; brief §6.5 item 9 rewritten; installer re-synced. ALL GATES re-run by the orchestrator on real 3.12.10 (subagent NOT trusted): full suite 1622 OK; catalogue --check EXIT 0, Total 279 (29-02 mints nothing); fixture validate PASS CRITICAL=0; DSX-COH-041 OUT of _SECTION_65_BACKLOG_CODES (disjointness green); _ABSENT_PARTITION_FLOOR stays 3; dq.py+cli.py byte-frozen (empty diff); no REQUIREMENTS/STATE/ROADMAP or wave-1 catalogue edits by the executor; node install.mjs + --check EXIT 0 (self-test passed). D-05 honesty spot-checked in POSTMORTEM + ATTRIBUTION (Gail & Simon 1985 motivating-definition-only + explicit no-mechanism/D-02 disclaimer; Obermeyer 2019 abstract+metadata grade, paper body NOT read; bounded-catch stated). One disclosed brief.md deviation verified sound: item-9 disposition bullet (carried→promoted) + calibration list (1/3/7/9→1/3/7) — same-section consistency fixes, NO headline number changed (miss-rate 1.0 / FPR 0.0 untouched, Phase-30 re-measure). Committed + pushed plain git (orchestrator-authored, no stray branch). Phase 29 execution complete → S5-3 CHECKED. Next = S5-4 (Phase 29 code review + fixes; verification passed, REQ-P29-01..03) — the S5-2 non-blocking nit (stale `else: # kind == "caught"` comment in the coherence harness) is an explicit S5-4 review item. PRIOR — S5-3 Wave 1 DONE (Phase 29 DSX-COH-041 mint LANDED; catalogue 278→279). Executed Plan 29-01 Tasks 2-4 (Branch B, LIVE MISS — Task 1 measurement landed prior, VERDICT: LIVE MISS in 29-MEASUREMENT.md) via gsd-executor (adaptive, WRITE-ONLY / no git — orchestrator commits, sidesteps the stray-branch hazard). Task 2 RED (7 unit tests, 4 firing-behaviour tests failed pre-impl) → Task 3 GREEN (brand-new `_check_subgroup_harm_disposition` in dsx/checks/coherence.py:223 dispatched from check():54; DSX-COH-041 minted — CRITICAL on a missing decision.subgroup_harm[] row for an opposing-sign segment at n≥floor, HIGH on accept-without-rationale; catalogue 278→279; template ANALYSIS-SPEC.yaml subgroup_harm[]+subgroup_harm_floor additive-commented) → Task 4 Branch B verification-only (brief untouched, that is 29-02's job). ALL GATES re-run by the orchestrator on real Python 3.12.10 (subagent NOT trusted): full suite **1622 OK** (1615+7); new module 7 OK; gen-finding-catalogue.py --check EXIT 0, Total 279, exactly one DSX-COH-041 row (rendered HIGH — two-severity dedup to last-seen report.add, identical to DSX-COH-030; both severities fire live, proven by the unit tests), byte-deterministic on re-write (sha256 4114921c… stable); FOUR count pins at 279 (finding-codes.md:16 + invariant:43 + phase20:101 + p19:40); DSX-COH-041 in _D05_ALLOWLIST_CODES by EXACT CODE (gen-finding-catalogue.py:228) + OUT of _SECTION_65_BACKLOG_CODES (grep 0); Gail & Simon (1985) D-05 docstring = MOTIVATING DEFINITION only, explicit no-mechanism/D-02 disclaimer + Structural criterion + bounded-catch honesty (coherence.py:229) + `# D-05: DSX-COH-041` marker in the test; dq.py + cli.py byte-frozen (empty diff); no REQUIREMENTS/STATE/ROADMAP edits by the executor; HEAD on canonical, in sync w/ origin, no stray branch. One executor deviation disclosed + orchestrator-verified as the exact Phase-27/28 lockstep class: `test_gen_finding_catalogue.py` _CANONICAL_DECLARATIONS gains DSX-COH-041's two (severity,title) pins (a two-severity code MUST be pinned there or the suite reds — same as DSX-COH-030/DSX-ML-034/DSX-CLM-034); + _MINTED_CODES set-identity add in test_finding_catalogue_invariant.py — neither weakens an invariant, suite green, no design change. Installer re-sync deferred to Plan 29-02's close gate (Phase-27/28 mint-wave precedent — templates/references drift is reconciled at Wave 2). DSX-COH-041 now ACTIVE (no longer RESERVED-INACTIVE). Committed + pushed plain git (orchestrator-authored, no stray branch). Next = S5-3 Wave 2 (Plan 29-02 TARGET fixture promotion: `examples/known-bad/subgroup-harm-without-disposition-*`, DSX-COH-041 PRESENT in _TARGET_DEFECT_CODES + _GOLDEN_SHIP_FINDINGS, `kind: target` sidecar, spec 44→45, brief §6.5 item 9 rewrite, install.mjs re-sync → full re-gate → then check S5-3 box). PRIOR — S5-2 DONE (Phase 29 plan-checker gate PASSED + orchestrator re-verify; box CHECKED 08:31Z 2026-09-08). gsd-plan-checker (adaptive routing, GSD subagent NOT overridden §3) on the AUTHORED-BUT-UNGATED 29-01+29-02 set → VERIFICATION PASSED (A–G all PASS, 0 blockers / 0 warnings, every cited locator matched fact); the binding gate — orchestrator re-verified the harness code ITSELF on the live repo (repo=fact, subagent NOT trusted): B point-scoped _TARGET_DEFECT_CODES + empty _EXPECTED_CAUGHT_DEFECTS is FORCED-correct (_CRITICAL_THRESHOLD_POINTS=(plan,execute) test_known_bad_corpus.py:57 + coherence absent from execute cli.py:120, present plan/verify/ship :117/:123/:128) — divergence 1; C the ONLY three kind-switches (closed-vocab:1771, falsifiability if-miss/else:1870/:1878, ABSENT-partition guard !=miss:1958) all route kind:target correctly once "target" joins the tuple — COMPLETE, no other switch exists — divergence 2; D live 278/44 confirmed → 279/45, four catalogue pins move together, _ABSENT_PARTITION_FLOOR=3 (:993) untouched; E DSX-COH-041 free (grep 0), item-id 6.5-item-9-subgroup-harm-declaration frozen _SECTION_65_ITEM_IDS:951, disjointness assert:1724, exact-code allowlist; F no dq.py/cli.py/REQ/STATE/ROADMAP edits, disjoint files_modified, installer close-gate present; G REQ-P29-01/02/03 covered; A measure-first Task 1 + correct CAUGHT→Branch-A-terminal / LIVE-MISS→29-02 routing, no WR-01 misroute. Both divergences recorded LOUDLY in 29-02 <recorded_authoring_choices>:50-58. One non-blocking nit (self-addressed by 29-02 choice #2b): else:# kind=="caught" comment:1878 goes stale on the vocab extension → 29-02 makes the caught/target intent explicit at execute (an S5-4 code-review item, not a gate blocker). dsx/ byte-untouched this unit, 0 codes minted (DSX-COH-041 stays RESERVED-INACTIVE until S5-3 measures a live miss). Committed + pushed plain git (orchestrator-authored, no stray branch). Next = S5-3 (Phase 29 execute: measure-first at the four gate points from a fresh tempdir; then IF live miss AND case found → author vocab + DSX-COH-041 + Gail & Simon docstring + TARGET harness; ELSE the no-mint record). PRIOR — S5-1 DONE (Phase 29 discuss frozen → subgroup-harm evidence case; Phase 28 remains COMPLETE, 4/6 phases). Ran the §4 persona round (Architect dsx-analysis-architect + Statistician dsx-statistician, opus override §3, parallel — each grounded INDEPENDENTLY in the check code, not trusting the orchestrator's measured facts). Wrote 29-CONTEXT.md with 6 decisions (D-29-00..05), design FROZEN before measurement (D-13). Four settlements: floor `decision.subgroup_harm_floor` (int, default 0 = un-gameable, may only tighten); slug `subgroup-harm-without-disposition` (prescriptive+experiment, 4 segments — 3 positive + 1 minority opposing above floor, overall +3.2pp, no `decision.subgroup_harm[]`); disposition `{segment,effect,ci,n,disposition:accept|exclude|mitigate,rationale}` as ONE two-severity code (missing-row CRITICAL / accept-no-rationale HIGH); reserved DSX-COH-041 (D-06 — catalogue re-measured 278 live, family {001,010,020,030,031,040}, 041 free, 04x decision-obligation tier sibling of 040, Phase-28 fill-the-tier precedent; veto window OPEN). LOAD-BEARING D-29-00 (Architect): REQ-P29-03's missing-row-CRITICAL fires on the honestly-declared opposing segment (no opt-in pointer to omit, unlike Phase 27/28) → fixture is a pre-mint LIVE MISS that becomes a post-mint TARGET, not a permanent kind:miss — S5-2 wires _TARGET_DEFECT_CODES + re-verifies _ABSENT_PARTITION_FLOOR=3 (harness:993). Grounded live: MET-030/031 structurally silent at 1-of-4 (metrics.py:316 needs ALL oppose / :334 needs >=half); grep — results.segments read by ONLY _check_simpsons_paradox, subgroup_harm absent from dsx/; segments schema {name,effect,n} has no ci ⇒ trigger sign+n>=floor not CI (F3 circularity); item id 6.5-item-9-subgroup-harm-declaration frozen _SECTION_65_ITEM_IDS member (harness:951). D-05 Gail & Simon (1985) = MOTIVATING DEFINITION only (D-02, never the likelihood-ratio mechanic; mirrors Phase 28 D-28-05). REQ-P29-02 HALF-MET: the documented public failure case is UNFOUND — an S5-2 research deliverable (NOT a D-05 read; named source confirmed); "not found" is a valid recorded outcome. Zero codes minted (RESERVED-INACTIVE until S5-3 measures a live miss AND the case source is found); dsx/ byte-untouched; current_phase advanced 28→29 (single-writer). No HUMAN-QUEUE escalation (D-06 number recorded loudly with veto window). Committed + pushed plain git (no stray branch). Next = S5-2 (Phase 29 plan: find/record-not-found the failure case + plan the conditional mint; plan-checker must pass). PRIOR — S4-5 DONE (Phase 28 secure/validate passed → Phase 28 COMPLETE, 4/6 phases). Both verify:post gates re-run by the orchestrator on real 3.12.10 (NOT trusted from a subagent). secure-phase 28: State B, register from 2/2 PLAN threat_model blocks = 14 threats (10 HIGH/3 MED/1 LOW-accept; T-28-06+T-28-SC shared across both plans, deduped), ASVS L1, block_on=high → SECURED, threats_open:0, 14/14 CLOSED (each re-gated at its locator: T-28-01 D-05 Wilkinson motivating-principle-only honesty claims.py:482 + marker:11; T-28-08 fixture no supported_by grep 0 + dsx validate PASS CRITICAL=0 + falsifiability :2620-2633; T-28-05 VERDICT LIVE MISS; T-28-02/04 catalogue --check current + one DSX-CLM-034|HIGH row finding-codes.md:236 + exact-code allowlist :218; T-28-03 mint in brand-new _check_supported_by_traceability claims.py:477 separate from _check_numeric_overlap:344; T-28-09/10 sidecar frozen _SECTION_65_ITEM_IDS id :950 + DSX-CLM-034 out of backlog disjoint :1724; T-28-11 spec 44 test_dsx.py:588; T-28-12 golden re-measured live; T-28-07 tolerance-direction :135/:198; T-28-06 dq.py byte-frozen; T-28-13 install --check passed). 28-SECURITY.md status:verified (Approval unsigned §4.4). validate-phase 28: State A, 3/3 REQ COVERED by named tests, 0 MISSING → nyquist_compliant:true; phase modules 67 OK (test_claims_supported_by 7 + test_known_bad_corpus 60); full suite 1615 OK; 28-VALIDATION.md filled from seed → status:validated. Honesty fix: corrected 28-VERIFICATION.md test count 13→7 (measured). HQ-44 filed (non-blocking until S7-2). Next = S5-1 (Phase 29 discuss), unblocked by HQ-40 row 40e (Gail & Simon 1985 CONFIRMED). PRIOR — S4-4 DONE (Phase 28 code review + verification passed, 3/3 REQ MET). gsd-code-reviewer (opus, direct spawn §3) wrote 28-REVIEW.md — 0 BLOCKER/0 HIGH/1 MEDIUM/2 LOW, all 7 load-bearing invariants PASS. WR-01 (MEDIUM) + IN-02 (LOW) FIXED — catalogue-invariant test name/docstring said 277 while enforcing 278, renamed + named DSX-ML-034/DSX-CLM-034 (test-doc only). IN-01 (LOW) ACCEPTED+tracked — DSX-CLM-034 folds claim.ci into claim_numbers not reference unlike -033, latent (no shipped spec reaches it), opposed design judgment deferred. ALL GATES re-run by orchestrator on real 3.12.10 — full suite 1615 OK (doc-only fix), catalogue --check EXIT 0 Total 278 one DSX-CLM-034|HIGH row, dq.py byte-frozen, install.mjs --check passed, change scope tests/test_finding_catalogue_invariant.py only. 28-VERIFICATION.md passed, REQUIREMENTS P28-01/02/03 Met + boxes checked. Secure/UAT batched to S4-5/HQ. Next = S4-5 (Phase 28 secure/validate). PRIOR — S4-3 DONE (Phase 28 execution complete). Wave 1 = DSX-CLM-034 HIGH mint (catalogue 277→278); Wave 2 = Plan 28-02 fixture promotion (`examples/known-bad/magnitude-without-computed-effect-*`, D-28-06 `_PER_FIXTURE_INCIDENTAL_CODES` incidental, spec 43→44, brief §6.5 item 8 rewrite, installer re-sync) landed 2026-09-08T03:15Z via gsd-executor (adaptive, WRITE-ONLY). ALL GATES re-run by the orchestrator on real 3.12.10 (subagent NOT trusted): full suite 1615 OK (1606+9); catalogue --check EXIT 0, Total 278 (mints nothing); fixture validate PASS CRITICAL=0 = honest MISS (no supported_by); golden set {DSX-COH-001}, DSX-CLM-034 ABSENT; DSX-CLM-034 OUT of _SECTION_65_BACKLOG_CODES (disjointness green); spec pin 44; dq.py byte-frozen; no tracking-file edits by executor; install.mjs + --check EXIT 0 (self-test passed). One executor deviation disclosed + verified sound (Phase-27 lockstep class): _NON_CAUSAL_KNOWN_BAD add in test_frame_interference.py — fixture is descriptive/observational per frozen D-28-01, suite-green, weakens no invariant. Committed by explicit path + plain git (no stray branch), pushed. Next = S4-4 (Phase 28 code review + verification passed, REQ-P28-01..03). — HISTORICAL DETAIL (Wave 1) BELOW: Executed the LIVE-MISS branch of Plan 28-01 Tasks 2-4 via gsd-executor (adaptive, WRITE-ONLY / no git); ALL GATES re-run by the orchestrator on real 3.12.10 (subagent NOT trusted): full suite 1606 OK (1599+7); test_claims_supported_by 7 OK; gen-finding-catalogue.py --check EXIT 0, Total 278, exactly one DSX-CLM-034|HIGH row (finding-codes.md:236); THREE count pins at 278 (invariant:42 + p19:39 + phase20:100); spec count correctly STILL 43 (28-02's job); report.add("DSX-CLM-034","HIGH") string-literal at claims.py:557 in the brand-new _check_supported_by_traceability (def:477, separate from _check_numeric_overlap:344), dispatched from per-claim loop:84, early-returns on absent supported_by (:512-514); D-05 docstring = Wilkinson & TFSI (1999) as MOTIVATING PRINCIPLE + Structural criterion + bounded-catch honesty + `# D-05:` marker + 7 methods; DSX-CLM-034 in _D05_ALLOWLIST_CODES by EXACT CODE (:218) + OUT of _SECTION_65_BACKLOG_CODES; schema keys additive/commented (template:309/312); dq.py byte-frozen; no tracking-file edits by executor. TWO executor deviations disclosed + verified as the exact Phase-27 lockstep class (S3-3 Wave 1): third total pin test_p19_categorical_rows.py 277→278, and _MINTED_CODES set add DSX-CLM-034 (set-identity leg) — neither weakens an invariant. Committed mint artifacts by explicit path + ledger/STATE with plain git (no stray branch), pushed. Stopped at Wave-1 boundary (~12-min pacing cap; executor ~12.2m). Next = S4-3 Wave 2 (Plan 28-02 fixture promotion, carrying the DSX-COH-001 incidental via D-28-06 _PER_FIXTURE_INCIDENTAL_CODES + spec 43→44 + brief §6.5 item 8 rewrite + install.mjs re-sync → full re-gate → then check S4-3 box). Spawned gsd-executor (adaptive, WRITE-ONLY, Task 1 only) → frozen D-28-01 collision spike + 28-MEASUREMENT.md (VERDICT: LIVE MISS first line). Orchestrator INDEPENDENTLY re-ran the four-point protocol on real 3.12.10 (repo=fact): reproduced the table byte-for-byte — DSX-CLM-033 SILENT at verify/ship (27/18 clear via ×100 bridge: test A effect 0.27, test B effect 0.18), HONEST swap no-toggle (nothing catches), BREAK swap DSX-CLM-033 fires (metric-blind, silence IS the miss); no stray DECISIONS.jsonl. §6.5 item 8 entry condition MET; DSX-CLM-034 mint authorized. LOUD FLAG for 28-02: DSX-COH-001 (CRITICAL, coherence.py) fires at plan/verify/ship — inherent to the frozen association-claim-under-descriptive-question shape, swap-INVARIANT → incidental per D-28-02 (NOT a catch), BUT the fixture is blocked at ship by it, so 28-02 _EXPECTED_CAUGHT_DEFECTS cannot be frozenset() (S4-1 note assumed empty): next firing reconciles the caught-incidental-plus-missed-target encoding before promotion (frozen design stands, guardrail 1). Spike+measurement committed (plain git, no stray branch; dsx/ byte-untouched, mints nothing). Stopped at Task-1 sub-boundary (executor ~14.75m, past the ~12-min cap). PRIOR: S4-2 DONE (Phase 28 plan-checker gate PASSED; both plans corrected + gated). gsd-plan-checker (haiku, adaptive) FAILED on ONE real BLOCKER — 28-01 Task 1 <done> (:195) routed a VERDICT:CAUGHT to "Task 4 branch B" (the LIVE-MISS handoff-to-28-02 path) instead of Branch A (the CAUGHT no-mint terminal), the WR-01-class misroute 28-RESEARCH.md:430 warned of (a literal executor on CAUGHT would skip the no-mint closure record AND wrongly proceed to fixture promotion). Orchestrator did NOT trust the verdict — confirmed the defect against the code (Task 4 Branch A=CAUGHT :321, Branch B=LIVE MISS :329; grep proved :195 the ONLY misrouted pointer), applied the single §5 repair (:195 "branch B"→"Branch A — the CAUGHT branch"; a mechanically-forced cross-ref fix, no design change), re-checked → VERIFICATION PASSED (12 invariants). Orchestrator re-verified the gate itself (repo=fact): REQ IDs in frontmatter (28-01 P28-01/02, 28-02 P28-01/03); NO dq.py/REQUIREMENTS/STATE/ROADMAP in either files_modified; deps 28-01 wave1[] / 28-02 wave2[28-01]; threat_model + Artifacts x2; live baselines re-measured catalogue Total 277 + spec len(paths),43@test_dsx.py:583 (plans move 277→278/43→44 conditional on the live miss); D-05 Wilkinson-alone + exact-code _D05_ALLOWLIST_CODES; DSX-CLM-034 OUT of _SECTION_65_BACKLOG_CODES; frozen id 6.5-item-8-magnitude-without-computed-effect@test_known_bad_corpus.py:853; fixture declares no supported_by (MISS holds). Corrected 28-01 + ledger/STATE committed (plain git, no stray branch). Next = S4-3 (execute: D-13 measure live at all 4 gate points FIRST → 28-MEASUREMENT.md VERDICT; then only on LIVE MISS the DSX-CLM-034 mint (28-01 Tasks 2-4) + fixture promotion (28-02); a CAUGHT verdict is a valid no-mint terminal).
stopped_at: "S3-5 DONE (box checked) → Phase 27 complete (3/6 phases). Both verify:post gates re-run by the orchestrator on real Python 3.12.10 (NOT trusted from a subagent). secure-phase 27: State B (no prior SECURITY.md); register from 2/2 PLAN <threat_model> blocks = 12 threats (8 HIGH / 3 MEDIUM / 1 LOW-accept), ASVS L1, block_on=high → auditor short-circuit; each re-gated at its locator → SECURED, threats_open:0, 12/12 CLOSED — T-27-03 (HIGH) docstring+# D-05: marker state 'secondary-corroborated, primary PDF paywalled', no first-hand claim (grep 0); T-27-01 (HIGH) fixture declares no feature_provenance block (grep 0) + dsx validate PASS CRITICAL=0 (honest miss); T-27-04 catalogue --check current + exactly one DSX-ML-034 CRITICAL row (finding-codes.md:163); T-27-02a/02b count pins 277 / spec 43; T-27-06 DSX-ML-034 out of _SECTION_65_BACKLOG_CODES; T-27-07 sidecar id a frozen _SECTION_65_ITEM_IDS member (:852); T-27-08 exact allowlist code (:209); T-27-09 dq.py byte-frozen (empty diff dd930af..HEAD); T-27-10 golden test_causal_verb_golden 6 OK; T-27-11 install.mjs --check self-test passed; T-27-SC accept (entrypoint read as text, never executed). 27-SECURITY.md written status:verified (technical; Approval unsigned per §4.4). validate-phase 27: State A; 3/3 REQ (P27-01/02/03) COVERED by named unittest tests, 0 MISSING → nyquist_compliant:true; phase module (test_ml_feature_provenance + test_known_bad_corpus) re-run 60 tests OK; full suite 1599 OK; 27-VALIDATION.md status:validated, per-task map green, sign-off filled. Human sign-off + UAT batched as HQ-43 (non-blocking until S7-2). Zero code/gate touched this unit (only .planning artifacts). [SUPERSEDED — see `status` + `last_activity_desc`: S4-1 discuss done; S4-2 full 2-plan set authored + committed but UNGATED as of 22:39Z 2026-09-07; next = S4-2 plan-checker gate + orchestrator re-verify.]"
last_updated: "2026-09-10T17:20:00.000Z"
last_activity: 2026-09-10
last_activity_desc: "S5-4 firing (12:31Z 2026-09-08): Phase 29 code review + verification PASSED (box CHECKED). Adopted a crashed S5-4 firing's uncommitted orphan work (coherence.py fix + 2 test files + untracked 29-REVIEW.md/29-VERIFICATION.md — the loop's own S5-4 machinery, matching the ledger's Next, NOT operator work) per the S4-2/S5-2 orphan precedent; independently confirmed the 3 fixes are FROZEN-design-aligned strict tightenings (HG-01 invalid/missing-disposition→CRITICAL per D-29-03; MD-01 len<2→not segments per D-29-01; MD-02 missing-n→0 per D-29-01), LW-01 accepted residual. Did NOT trust the crashed firing's claimed gates — re-ran EVERY gate on real 3.12.10: full suite 1627 OK (1622+5); catalogue --check EXIT 0 Total 279 (mints nothing) finding-codes.md byte-unchanged; dq.py+cli.py byte-frozen (empty diff from base 4945a62); install.mjs --check self-test passed; WT scope coherence.py+2 test files only; TARGET fixture still fires DSX-COH-041 (test_known_bad_corpus green in-suite). REQUIREMENTS P29-01/02/03 Met + boxes checked. Secure/UAT batched to S5-5/HQ. Next = S5-5 (Phase 29 secure/validate). PRIOR — S5-3 Wave 2 firing (09:43Z 2026-09-08): Phase 29 execution COMPLETE — Plan 29-02 TARGET fixture promotion. gsd-executor (adaptive, WRITE-ONLY) promoted the spike into examples/known-bad/subgroup-harm-without-disposition-* (4 files incl. the corpus's FIRST kind: target ATTRIBUTION), wired the maps as a TARGET (DSX-COH-041 PRESENT/firing — the INVERSE of the 27/28 misses), taught the closed vocab (miss,caught,target), moved spec 44→45, rewrote brief §6.5 item 9, re-synced the installer. Orchestrator re-ran every gate on real 3.12.10 (subagent NOT trusted): full suite 1622 OK; catalogue --check EXIT 0 Total 279 (mints nothing); fixture validate PASS CRITICAL=0; golden set frozenset({DSX-COH-041}) PRESENT (live re-measure); DSX-COH-041 OUT of _SECTION_65_BACKLOG_CODES; _ABSENT_PARTITION_FLOOR stays 3; dq.py+cli.py byte-frozen; no tracking/wave-1 edits; install.mjs + --check EXIT 0. D-05 honesty spot-checked (Gail & Simon motivating-definition-only; Obermeyer abstract+metadata grade; bounded-catch). One disclosed brief.md deviation verified sound (item-9 bullet + calibration-list consistency fixes, no numbers changed). Committed + pushed (plain git, no stray branch). S5-3 CHECKED. Next = S5-4. PRIOR — S5-3 Wave 1 firing (09:13Z 2026-09-08): the DSX-COH-041 mint (Plan 29-01 Tasks 2-4, Branch B / LIVE MISS — Task 1 measurement landed prior). gsd-executor (adaptive, WRITE-ONLY) authored the RED tests → implemented brand-new _check_subgroup_harm_disposition in coherence.py (CRITICAL missing-row / HIGH accept-no-rationale) → minted DSX-COH-041, catalogue 278→279 + 4 count pins in lockstep; template subgroup_harm vocab additive-commented; Task 4 Branch B verification-only. Orchestrator re-ran every gate on real 3.12.10 (subagent NOT trusted): full suite 1622 OK; new module 7 OK; --check EXIT 0, Total 279, one DSX-COH-041 row (HIGH-rendered two-severity dedup, both fire live), byte-deterministic; 4 pins @279; exact-code allowlist + OUT of _SECTION_65_BACKLOG_CODES; honest Gail & Simon D-05 docstring (motivating-definition only, no-mechanism/D-02 disclaimer) + `# D-05:` marker; dq.py+cli.py byte-frozen; no tracking-file edits. One disclosed deviation verified as the Phase-27/28 lockstep class (test_gen_finding_catalogue.py _CANONICAL_DECLARATIONS two-severity pin + _MINTED_CODES set add). Installer re-sync deferred to 29-02 close (mint-wave precedent). Committed + pushed plain git (no stray branch). S5-3 box UNCHECKED — Wave 2 (Plan 29-02 TARGET fixture promotion) remains. Next = S5-3 Wave 2. PRIOR — S5-2 plan-authoring firing (08:16Z 2026-09-08): authored 29-02-PLAN.md (Phase 29 wave-2 TARGET fixture-promotion) via gsd-planner (opus, WRITE-ONLY, no git) mirroring 28-02 with the load-bearing miss→TARGET inversion; committed AUTHORED-BUT-UNGATED (plain git, explicit path, no stray branch). Independently verified the frontmatter + 3 load-bearing harness claims against the code (not trusted from the subagent); both mechanism divergences from the literal CONTEXT D-29-00 (map wiring → _TARGET_DEFECT_CODES with empty _EXPECTED_CAUGHT_DEFECTS per prescriptive-churn-recommendation; new kind:target vocab) recorded in 29-02 <recorded_authoring_choices> for the gate. Box UNCHECKED — plan-checker gate deferred to next firing (S4-2 author/gate split; the intricate TARGET wiring must not be rushed near the pacing cap). Log at 25 entries — trim-to-archive re-flagged as a light unit. Next = gsd-plan-checker on 29-01+29-02 + orchestrator re-verify → check S5-2. PRIOR — S5-2 reconcile firing (07:25Z 2026-09-08): adopted the crashed 04:53Z firing's orphan 29-01-PLAN.md (its S5-2 planner wrote the plan at 05:10Z, then the firing hit the session usage limit — resets 07:20Z São Paulo — and exited code 1 before gating/committing/logging/authoring 29-02; firings 05:38→07:08Z re-hit the same limit and quick-exited, wrapper reconcile ABORTED each on an unclean tree — nothing lost; my 07:23Z firing is past the reset). Diagnosed from firing-20260908-045344.log. Governance (loud, §4 reconcile not a HUMAN-QUEUE escalation): the orphan is a prior LOOP firing's plan-authoring output for the current in-progress S5-2 unit (machine-generated GSD plan; operator remote, authors no plans locally), matching the ledger's own Next → ADOPT per the S4-2 orphan precedent. Verified sound before adopting (faithful to frozen D-29-00..05; correct CAUGHT→A/LIVE-MISS→B routing, no WR-01 misroute; honest Gail & Simon D-05 motivating-def framing; DSX-COH-041 brand-new fn + exact-code allowlist) and pins re-measured live on real 3.12.10: catalogue 278, DSX-COH-041 free, spec 44, three count pins @278. Committed 29-01 AUTHORED-BUT-UNGATED (plain git, no stray branch). Box UNCHECKED: 29-02 TARGET-promotion plan unauthored (Phase-29 fixture is a post-mint TARGET per D-29-00 — DSX-COH-041 PRESENT in _TARGET_DEFECT_CODES + _GOLDEN_SHIP_FINDINGS, kind:target sidecar, spec 44→45, wiring differs from the 27/28 misses) AND plan-checker gate unrun. Next = author 29-02 (mirror 28-02 adapted for present-target) → gsd-plan-checker + orchestrator re-verify → check S5-2. PRIOR — S5-1 firing (04:31Z 2026-09-08): Phase 29 discuss → design frozen (subgroup-harm evidence case). §4 persona round (Architect + Statistician, opus, parallel, each grounded independently in the check code). 29-CONTEXT.md written with 6 decisions (D-29-00..05). Four settlements: floor `decision.subgroup_harm_floor` (int, default 0); slug `subgroup-harm-without-disposition` (prescriptive+experiment, 4 seg / 1 minority opposing above floor, overall +3.2pp, no disposition); disposition vocab `{segment,effect,ci,n,disposition:accept|exclude|mitigate,rationale}` as ONE two-severity code (missing-row CRITICAL / accept-no-rationale HIGH); reserved DSX-COH-041 (D-06, catalogue 278 live, 04x decision-obligation tier, veto window OPEN). Load-bearing D-29-00: the fixture becomes a post-mint TARGET (not a Phase-27/28 permanent miss) because REQ-P29-03's missing-row-CRITICAL fires on the honestly-declared segment — S5-2 wires _TARGET_DEFECT_CODES + verifies _ABSENT_PARTITION_FLOOR=3. Live groundings: MET-030/031 structurally silent at 1-of-4 (metrics.py:316/:334); results.segments read only by _check_simpsons_paradox; segments schema has no ci ⇒ trigger sign+n>=floor. D-05 Gail & Simon (1985) = motivating definition only (D-02). REQ-P29-02 half-met — the documented public failure case is unfound (S5-2 research deliverable). Zero codes minted (RESERVED-INACTIVE); dsx/ byte-untouched; current_phase 28→29. Committed + pushed (plain git, no stray branch). Next = S5-2 (Phase 29 plan). PRIOR — S4-5 firing (04:03Z 2026-09-08): Phase 28 secure/validate → Phase 28 COMPLETE (4/6 phases). Both verify:post gates re-run by the orchestrator on real 3.12.10 (not trusted from a subagent). secure-phase 28: register from 2/2 PLAN threat_model blocks = 14 threats (10 HIGH/3 MED/1 LOW-accept) → SECURED, threats_open:0, 14/14 CLOSED; load-bearing HIGH = T-28-01 (D-05 over-claim: Wilkinson motivating-principle-only, no numeric-overlap mandate) + T-28-08 (MISS-integrity: fixture no supported_by + validate PASS CRITICAL=0). 28-SECURITY.md status:verified (Approval unsigned). validate-phase 28: State A, 3/3 REQ COVERED by named tests, nyquist_compliant:true; phase modules 67 OK; full suite 1615 OK; 28-VALIDATION.md filled from seed → status:validated. Corrected 28-VERIFICATION.md test count 13→7 (measured honesty fix). HQ-44 filed (non-blocking until S7-2). Zero code/gate touched this unit — only .planning artifacts. Next = S5-1 (Phase 29 discuss). PRIOR — S4-4 firing (03:44Z 2026-09-08): Phase 28 code review + verification. gsd-code-reviewer (opus, direct spawn §3) → 28-REVIEW.md 0 BLOCKER/0 HIGH/1 MEDIUM/2 LOW, all 7 load-bearing invariants PASS. WR-01 (MEDIUM) + IN-02 (LOW) FIXED (catalogue-invariant test asserted 277 while enforcing 278 — renamed + named the two new mints; test-doc only, no assertion change). IN-01 (LOW) ACCEPTED+tracked (DSX-CLM-034 folds a claim's own ci into claim_numbers not reference unlike DSX-CLM-033; latent — no shipped spec reaches it since the fixture early-returns on absent supported_by; fix-vs-keep is an opposed design judgment deferred to a recorded decision). ALL GATES re-run by the orchestrator on real 3.12.10 (subagent NOT trusted): full suite 1615 OK (doc-only fix); catalogue --check EXIT 0, Total 278, one DSX-CLM-034|HIGH row; dq.py byte-frozen; install.mjs --check passed; change scope = tests/test_finding_catalogue_invariant.py only. 28-VERIFICATION.md passed, 3/3 REQ MET; REQUIREMENTS P28-01/02/03 → Met + boxes checked. Committed + pushed (plain git, no stray branch). Secure/UAT batched to S4-5/HQ. Next = S4-5 (Phase 28 secure/validate). PRIOR — S4-3 Wave 1 firing (02:43Z 2026-09-08): the DSX-CLM-034 mint (28-01 Tasks 2-4, LIVE-MISS branch). gsd-executor (adaptive, WRITE-ONLY) authored the RED test → implemented _check_supported_by_traceability → minted DSX-CLM-034 (HIGH) → catalogue 277→278; Task 4 Branch B verification-only (brief untouched). Orchestrator re-ran every gate on real 3.12.10 (subagent NOT trusted): full suite 1606 OK; new module 7 OK; --check EXIT 0, Total 278, one DSX-CLM-034|HIGH row; three count pins at 278; spec count still 43; string-literal severity in the separate new function; honest Wilkinson D-05 docstring + marker; exact-code allowlist + out of backlog set; dq.py byte-frozen; no tracking edits. Two disclosed deviations verified as the Phase-27 lockstep class (third total pin p19; _MINTED_CODES set add). Committed by explicit path + plain git (no stray branch), pushed. S4-3 box UNCHECKED — Wave 2 (28-02 fixture promotion) remains. Next = S4-3 Wave 2. PRIOR — S4-3 Task 1 firing (23:31Z): the D-13 measure-first act. gsd-executor (adaptive, WRITE-ONLY, 28-01 Task 1 only) built the frozen D-28-01 collision spike + 28-MEASUREMENT.md (VERDICT: LIVE MISS first line). Orchestrator did NOT trust it — read the spike (matches frozen shape, ×100 collision 0.27→27/0.18→18), read MEASURE.py (drives the REAL dsx gate on fresh tempdirs, seeds entrypoint + plan header like _gate_findings), and RE-RAN it on real 3.12.10 → reproduced the four-point table byte-for-byte: validate=0, plan/verify/ship=1 CRITICAL DSX-COH-001 only, execute=0; DSX-CLM-033 SILENT at verify/ship; HONEST swap (27→15,18→9) no-toggle (nothing catches); BREAK swap (27→33,18→44) DSX-CLM-033 fires (metric-blind ⇒ collision-silence is the miss); no stray DECISIONS.jsonl. LIVE MISS confirmed ⇒ §6.5 item 8 entry condition MET, DSX-CLM-034 mint authorized. FLAG: DSX-COH-001 fires (association claim under descriptive question, inherent to the frozen shape, swap-invariant → incidental per D-28-02) but blocks the fixture at ship, so 28-02 _EXPECTED_CAUGHT_DEFECTS cannot be frozenset() — reconcile before promotion. Committed spike+measurement (plain git, no stray branch), pushed. S4-3 box UNCHECKED (Tasks 2/3/4B + Plan 28-02 remain). Next = S4-3 continue (LIVE MISS branch mint + promotion). PRIOR — S4-2 GATE firing (23:04Z): ran gsd-plan-checker (haiku, adaptive) on the committed 28-01/28-02 → VERIFICATION FAILED on ONE real BLOCKER (28-01 Task 1 :195 mislabeled the CAUGHT route as 'Task 4 branch B' = the LIVE-MISS handoff, instead of Branch A the CAUGHT no-mint terminal; WR-01-class misroute per 28-RESEARCH.md:430). Did NOT trust the verdict — confirmed the defect real against the code (Task 4 Branch A=CAUGHT :321, Branch B=LIVE MISS :329; grep proved :195 the ONLY misrouted pointer), applied the single §5 repair (:195 'branch B'→'Branch A — the CAUGHT branch'), re-checked (same agent context) → VERIFICATION PASSED (12 invariants). Orchestrator re-verified the gate itself: REQ IDs in frontmatter, NO dq.py/tracking in either files_modified, deps wave1[]/wave2[28-01], threat_model+Artifacts x2, live baselines catalogue 277 + spec 43, D-05 Wilkinson-alone + exact-code allowlist, DSX-CLM-034 out of _SECTION_65_BACKLOG_CODES, frozen id verified @test_known_bad_corpus.py:853, MISS holds (no supported_by). Committed corrected 28-01 + ledger/STATE (plain git, no stray branch), pushed. Ledger ~35 entries — trim-to-archive light unit still pending (flagged 5x). Next = S4-3 execute (D-13 measure-first)."
progress:
  total_phases: 6
  completed_phases: 5
  total_plans: 14
  completed_plans: 14
  percent: 83
---

# Project state

**Status:** v2.6 Exploration Depth and Backlog Evidence — ACTIVE (opened 2026-09-06 by operator direction, HQ-39); branch `gsd/v2.6.0-exploration-depth`; ships as tag `v2.6.0`
**Progress:** [█████████████████░░░] v2.6 — 5/6 phases (✅25 hermetic profile depth → ✅26 per-skill read contracts → ✅27 feature-origin-only leak case (DSX-ML-034 minted, corpus MISS fixture, suite 1599 OK) → ✅28 magnitude-no-test case (DSX-CLM-034 HIGH minted 277→278, `magnitude-without-computed-effect` corpus MISS, spec 43→44, suite 1615 OK) → ✅29 subgroup-harm case (COMPLETE — DSX-COH-041 minted 278→279, `subgroup-harm-without-disposition` corpus TARGET (first `kind: target` sidecar, DSX-COH-041 PRESENT/firing), spec 44→45, secure 15/15 CLOSED, validate nyquist_compliant:true, suite 1627 OK; HQ-45 filed) → 🔄30 calibration re-baseline (S6-4 code review + verification PASSED — miss 5/5, FPR 0/15, 279→279 zero-mint, suite 1629 OK; stats review RECORD-WITH-AMENDMENTS F1/F2 applied; next = S6-5 secure/validate))
**Predecessors:** v2.5.0 and v2.4.1 SHIPPED 2026-09-06 interactively (`ad43ec6`, `07d3db0`); v2.4 Visual Excellence SHIPPED 2026-09-03 (tag `v2.4.0`); v2.3 Test Catalog SHIPPED 2026-09-02 (`v2.3.0`); v2.2 Analytic Surface SHIPPED 2026-08-29 (`v2.2.0`); v2.0.0 DSX Validity Frame SHIPPED 2026-08-28 (`v2.1.0`). Archives under `.planning/milestones/`.

**Loop control:** the autonomous ceremony drives this milestone from S0-1 onward per
`.planning/LOOP-BRIEF.md` and `.planning/LOOP-LEDGER.md` (18 requirements, stages
S0–S7). The pause flag was removed at open; `scripts/run-ceremony-firing.ps1`
`$Branch` points at `gsd/v2.6.0-exploration-depth`. S7-5 (`/gsd-complete-milestone`)
and S7-6 (ship) run interactively per the ledger's own design.

**Usage-limit posture (proven 2026-08-30 through 2026-09-03):** the firing
wrapper detects limit hits, backs off gracefully, and re-probes every 30 minutes
during any hold to catch an early release rather than blindly waiting the full
computed window. Firings must not retry in a loop — log one line and stop; the
wrapper owns the pacing.

## Project Reference

See: .planning/PROJECT.md (updated 2026-09-03; Key Decisions table there is the full decision log)

**Core value:** Gate analytical work on validity before the data is touched.
**Current focus:** v2.6 Exploration Depth and Backlog Evidence — Phase 25 (hermetic
profile depth) is first. Scope in `.planning/research/V2.6-SCOPE.md`; requirements
REQ-P25-01 … REQ-P30-03 in `.planning/REQUIREMENTS.md`. The one rule that shapes the
milestone: Phases 27–29 measure their corpus case live before any check is designed,
and a case the gate already catches closes its phase with no mint (D-13).

## Current Position

Phase: 30 — Calibration re-baseline (terminal). S6-1 discuss / S6-2 plan / S6-3 execute / S6-4 code review + verification all DONE. Phases 25-29 all COMPLETE (5/6); Phase 30 in progress (S6-5 secure/validate remains). Next = S6-5 (/gsd-secure-phase 30 + /gsd-validate-phase 30; sign-off batched to S7-2).
Plan: none yet (S6-2 authors them). Design in 30-CONTEXT.md: Phase 30 MEASURES the grown corpus (42 known-bad + 15 good-control), it designs nothing. Deliverables — a 30-READOUT.md (headline pair + stratified PRESENT/ABSENT + FPR/15 + per-family friction + adversarial Statistician review) mirroring 12-READOUT.md, durable reproducer = test_stratified_catch_rate_and_fpr_report, numbers extracted via a read-only _measure_readout.py companion; refresh brief §6.5 calibration backdrop (:439-445) + the literature deferred table (docs/literature/the-ai-data-scientist.md :81-88) + corpus-count row-15 (:43, 39→42); verify all milestone-audit prerequisites; ZERO mint (set-identity 279→279). The 3 v2.6 cases' classification is FORCED by the committed harness wiring (2 misses: DSX-ML-034/DSX-CLM-034; 1 target: DSX-COH-041) — confirmed live at S6-3, not re-decided. Miss floor 3 holds at 5 misses. dq.py/cli.py/viz.py + the corpus fixtures byte-frozen.
Status: In-progress — NOT all-blocked. S6-4 (Phase 30 code review + verification) DONE, box CHECKED. Code review verdict SHIP (0 BLOCKER/HIGH/MEDIUM, 2 LOW accepted); adversarial dsx-statistician RECORD-WITH-AMENDMENTS (F1/F2 applied as prose framing, F3-F7 held, no measured number changed); 30-READOUT.md §6 composed; 30-VERIFICATION.md passed, REQUIREMENTS P30-01/02/03 → Met. All gates re-run on real 3.12.10 (suite 1629 OK, 279→279 zero-mint, reproducer OK, install/check.sh green, frozen surfaces byte-clean). HUMAN-QUEUE HQ-40 fully answered; HQ-41..45 non-blocking end-of-phase sign-offs batched to S7-2 (the only permitted blocking wait); HQ-46 (Phase 30 sign-off) filed at S6-5. Operator concurrent work observed and left untouched (SEED-003, brief §1). Only .planning artifacts + REQUIREMENTS/STATE edited this unit; dsx/cli.py/viz.py + the corpus byte-frozen. Next = S6-5 (Phase 30 secure/validate; opus/high per brief §3).
Last activity: 2026-09-10 (17:20Z) — Phase 30 S6-4 code review + verification PASSED (box CHECKED). Ran the code review (gsd-code-reviewer opus, direct spawn §3) + the adversarial dsx-statistician readout review (opus, the GA-1-deferred S6-4 sub-task) CONCURRENTLY. Code review 0 BLOCKER/0 HIGH/0 MEDIUM/2 LOW (accepted), verdict SHIP, all four load-bearing invariants hold. Statistician RECORD-WITH-AMENDMENTS: F1 (§4 'four'→'three' reliability fixtures — CONFIRMED by a live corpus glob) + F2 (§2c catchable-in-declared-form distinction) applied as prose framing; F3-F7 held; no measured number changed. Composed 30-READOUT.md §6 (review + orchestrator adjudication per brief §5). Re-ran ALL gates on real 3.12.10: full suite 1629 OK; catalogue --check current + 279→279; reproducer OK; agreement+snapshot 30 OK; install.mjs --check + check.sh green; frozen-surface diff EMPTY. Wrote 30-VERIFICATION.md (passed); REQUIREMENTS P30-01/02/03 → Met + boxes checked. Left the operator's mid-firing SEED-003 untouched (brief §1). Committed + pushed plain git (explicit path, no stray branch). Next = S6-5 (Phase 30 secure/validate).

## Performance Metrics

v2.4: 4 phases, 11 plans, 54 commits, 113 files changed (+15324/-161), 2026-09-02 → 2026-09-03.
v2.3, v2.2, and v2.0.0 velocity are archived with their milestone artifacts.

## Accumulated Context

### Decisions

Full decision log: PROJECT.md Key Decisions. Standing v2.4 decisions (carried from the original v2.3/v2.4 scoping, operator-directed 2026-08-29, re-confirmed at open):

- **The chart catalog is citable, not exhaustive-for-its-own-sake** — union of five named taxonomies (FT Visual Vocabulary, Wilke, Graphic Continuum, DVC, Datawrapper) after synonym merge and principled exclusions, target band 75–90 entries.
- **License audit is a plan-review gate, not an afterthought** — dsx-538/dsx-urban forked or built from permissively-licensed sources; dsx-econ/dsx-bbc reimplemented from published doctrine only, never porting GPL code (bbplot) or embedding an unlicensed PDF (the 2017 Economist styleguide).
- **Faceting is a declaration, not a chart type** — `facet_by` orthogonal to the mark, per the scope research's completeness-critic finding.
- **Contingency:** split Phase 23–24 off as v2.5 if the D-05 queue materially outruns cadence (expected lighter than v2.3's — ~8–12 reads vs 27).

### Pending Todos

None beyond the ledger.

### Blockers/Concerns

**The milestone is NOT all-blocked; Phase 27's mint is now UNBLOCKED.** HQ-40 is
**ANSWERED** (all five D-05 rows read at their locators, 2026-09-07). Row 40b (Kaufman
2012 TKDD) CONFIRMED — secondary-corroborated, ACM primary PDF paywalled — with operator
direction "proceed with `DSX-ML-034`". Combined with the already-measured LIVE MISS
(`27-MEASUREMENT.md`), this closed S3-1 and clears the whole Phase 27 mint pipeline. Next
= **S3-2** (plan the mint: promote the `spike/` fixture into a committed corpus fixture,
add the ATTRIBUTION sidecar with `absent_code: DSX-ML-034` + `promotes_backlog_item:
6.5-item-7-…`, and the harness entries). **S3-3 must keep the `# D-05:` marker honest**
("secondary-corroborated, primary PDF paywalled" — no first-hand PDF claim). HQ-40 also
unblocked **S4-1** (Phase 28 — cite Wilkinson & TFSI 1999 alone, drop JARS 40d) and
**S5-1** (Phase 29 — Gail & Simon confirmed); those phases still need their own D-13
measurement spikes (each a §4 persona round first, as Phase 27 had).

## Deferred Items

Carried forward from v2.0.0 close — captured future ideas, not gaps:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| seed | SEED-001-deepen-dsx-explore-data-eda-protocol | E-26 promoted into v2.6 Phase 26 (HQ-39); E-27 … E-31 still deferred with their entry conditions | 2026-08-28 |
| seed | SEED-002-grow-data-profile-hermetic-eda-artifacts | PROMOTED into v2.6 Phase 25 by operator direction (HQ-39, reversing HQ-38 the same day) | 2026-08-28 |

Acknowledged at v2.4 milestone close (`gsd_run query audit-open`, 2026-09-03) — the two
seeds above, genuinely deferred; plus 3 items the CLI flags as open that are **not**
actually open:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| context-question | 21-CONTEXT.md open questions (HQ-28) | **false positive** — HQ-28 answered 2026-09-03 (accepted, no veto); the CLI text-matches "open question" bullet markers in CONTEXT.md, which are never rewritten once the linked HUMAN-QUEUE item resolves. Standing CLI blind spot, same class as `/gsd-audit-uat`'s documented under-reporting. | 2026-09-03 |
| context-question | 22-CONTEXT.md open questions (HQ-30) | same false positive — HQ-30 answered 2026-09-03 (accepted, no veto) | 2026-09-03 |
| context-question | 23-CONTEXT.md open questions (HQ-32) | same false positive — HQ-32 answered 2026-09-03 (accepted, no veto) | 2026-09-03 |

## Session Continuity

Last session: 2026-09-06 (interactive). Shipped v2.4.1 (`07d3db0`) and v2.5.0
(`ad43ec6`); recorded HQ-38 (hold SEED-002); then, on operator direction, opened
v2.6 with both halves (HQ-39, reversing HQ-38): scope document, 18 requirements,
roadmap Phases 25–30, brief/ledger/queue rewritten, v2.4 loop artifacts archived,
wrapper repointed, pause flag removed.
Stopped at: milestone open committed and pushed on `gsd/v2.6.0-exploration-depth`;
the ceremony's next firing starts at S0-1.
Resume file: None — the loop reads `LOOP-LEDGER.md`'s Log.

## Operator Next Steps

- **Nothing blocks the loop today.** S0-3 will file the v2.6 D-05 citation evidence
  pack as HQ-40 (Hyndman & Fan 1996; Kaufman et al. 2012; Wilkinson & TFSI 1999 /
  APA JARS 2018; Gail & Simon 1985). Phases 25–26 do not wait on it; Phases 27–29
  do, row by row. Answer it in an interactive session when convenient.

- **S7-5 / S7-6 are interactive** (`/gsd-complete-milestone`, merge by explicit
  branch name, tag `v2.6.0`), as at every prior close.

- **Two kinds of local file stay untracked:** `references/The AI Data Scientist.md`
  (a full-text clipping of an arXiv paper — do not commit) and the `.claude/`,
  `.vscode/`, `graphify-out/` operator files.

- The stale agent worktree at `.claude/worktrees/agent-a9a54fddf75afc02f` is fully
  merged into `main`; `git worktree remove` it at leisure.
