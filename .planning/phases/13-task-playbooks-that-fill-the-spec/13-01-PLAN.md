---
phase: 13-task-playbooks-that-fill-the-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/dsx-cohort/SKILL.md
  - skills/dsx-funnel/SKILL.md
  - capabilities/dsx/capability.json
autonomous: true
requirements: [REQ-P13-01]
tags: [dsx, skill, playbook, cohort, funnel, routing]

must_haves:
  truths:
    - "A skill `dsx-cohort` exists at skills/dsx-cohort/SKILL.md with house frontmatter (name/description/argument-hint/allowed-tools) and routes retention-ratio metrics, a matrix cohort visual, and decision.revisit_when at the existing gates DSX-SPEC-020..026 + DSX-MET-*, DSX-VIZ-013, and DSX-COH-040 (Success Criterion 1)."
    - "A skill `dsx-funnel` exists at skills/dsx-funnel/SKILL.md with house frontmatter and routes step-conversion metrics and an event-time funnel-mark visual at DSX-MET-* and DSX-VIZ-013, deferring ordering integrity to the existing conversion-funnel routine (Success Criterion 1)."
    - "All four new playbooks (dsx-cohort, dsx-funnel, dsx-root-cause, dsx-segment) are registered in the flat \"skills\" array of capabilities/dsx/capability.json by this plan's manifest append — root-cause/segment's SKILL.md files are authored by 13-02, but 13-01 is the sole manifest writer and registers all four names (Success Criterion 1)."
    - "D-02 anti-parallel-advice: no line in either new SKILL.md states a numeric statistical threshold or correction method that is not on the same line as a DSX-* code citation."
  artifacts:
    - skills/dsx-cohort/SKILL.md
    - skills/dsx-funnel/SKILL.md
    - capabilities/dsx/capability.json
  key_links:
    - "capabilities/dsx/capability.json \"skills\" array must list all four new playbooks (dsx-cohort, dsx-funnel, dsx-root-cause, dsx-segment) by exact name, or the unregistered ones are unreachable by name (this file is the phase's single shared-file write; 13-02 authors root-cause/segment but does NOT touch the manifest)."
    - "Each routed field name must match a field the shipped gate actually reads (metrics[], data_input_type, decision.revisit_when), or the route points at nothing."
  prohibitions:
    - statement: "Zero new DSX-* finding codes are minted; the catalogue stays exactly 256."
      status: enforced
      verification: "These files are markdown + a JSON array append; the catalogue generator only walks dsx/**/*.py (gen-finding-catalogue.py:226-229), so it is structurally incapable of minting a code. Certified phase-wide by plan 13-05 (set-identity diff)."
    - statement: "No edits to the deterministic dsx gate path — dsx/cli.py CHECKS/GATE_PROFILES (:64-131), dsx/checks/*, dsx/frame/* stay read-only."
      status: enforced
      verification: "files_modified touches no path under dsx/."
    - statement: "No Python added under skills/ or capabilities/; no steps/contributions/gates/agents entry for the new playbooks."
      status: enforced
      verification: "the two new SKILL.md are markdown; the capability.json change is a name append to the flat \"skills\" array only (D-01)."
---

<objective>
Create the first two of the four task-router playbooks — `dsx-cohort` and `dsx-funnel` — and
register ALL FOUR new playbook names (`dsx-cohort`, `dsx-funnel`, `dsx-root-cause`, `dsx-segment`) in
the capability manifest. 13-01 is the phase's sole `capability.json` writer, so its append registers
the two playbooks 13-02 authors (root-cause, segment) as well as its own two. Each playbook is a ROUTER, not an
author: it tells the analyst which existing `ANALYSIS-SPEC.yaml` fields to fill and which
already-shipped deterministic gate reads each field. It states no threshold, correction method, or
admissible-chart rule of its own (D-02).

Purpose: give marketing cohort and funnel work a skill that fills the spec at the existing gates,
so the loop's rigour comes from the deterministic checks that are already in `dsx/`, not from a
second, drifting copy of the advice inside a markdown file.

Output: `skills/dsx-cohort/SKILL.md`, `skills/dsx-funnel/SKILL.md`, and a four-name append to the
`"skills"` array in `capabilities/dsx/capability.json` (registering all four new playbooks: cohort,
funnel, root-cause, segment).
</objective>

<artifacts_this_phase_produces>
This plan creates:
- `skills/dsx-cohort/SKILL.md` (new markdown playbook)
- `skills/dsx-funnel/SKILL.md` (new markdown playbook)
- an edit to `capabilities/dsx/capability.json` (all four new playbook names — dsx-cohort, dsx-funnel, dsx-root-cause, dsx-segment — appended to the flat `"skills"` array; root-cause/segment's SKILL.md files are authored by 13-02, but 13-01 is the sole manifest writer, so it registers all four — the phase's single shared-file write)

It produces NO new symbols under `dsx/` and NO new `DSX-*` finding codes. The four new playbook
files across plans 13-01 and 13-02, the capability.json append, the three existing-skill edits
(13-03/13-04), the executor-fragment bullet (13-04), the frozen snapshot fixture and the set-diff
test (13-05) are the phase's entire product.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-task-playbooks-that-fill-the-spec/13-CONTEXT.md

# Frontmatter house shape every new SKILL.md must mirror
@skills/dsx-define-metrics/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skills/dsx-cohort/SKILL.md as a router to the existing gates</name>
  <files>skills/dsx-cohort/SKILL.md</files>
  <read_first>
    - skills/dsx-define-metrics/SKILL.md:1-13 (the house frontmatter shape: name / description / argument-hint / allowed-tools)
    - .planning/phases/13-task-playbooks-that-fill-the-spec/13-CONTEXT.md (D-01 packaging, D-02 field->gate contract table row for dsx-cohort, D-08 naming caveat)
    - dsx/spec.py:892-971 (metric structural checks DSX-SPEC-020..026 that read metrics[]) and :755-777 (revisit_when_is_discriminating, DSX-COH-040)
    - dsx/spec.py:290-319 and dsx/cli.py:1060-1063 (the `dsx charts` matrix + DSX-VIZ-013)
    - references/finding-codes.md:323-334 (the DSX-COH-* family is "Coherence", not "Cohort")
  </read_first>
  <action>
    Create `skills/dsx-cohort/SKILL.md`. Frontmatter mirrors the house shape at
    `skills/dsx-define-metrics/SKILL.md:1-13`: `name: dsx-cohort`; a one-sentence `description`
    stating when to reach for it (a retention / cohort-grid question); an `argument-hint`; and
    `allowed-tools` (Read, Write, Edit, Bash, Grep, Glob, Agent). Body is a ROUTER (D-01/D-02): it
    names which existing `ANALYSIS-SPEC.yaml` fields the analyst writes and which shipped gate reads
    each — (1) a retention-ratio entry in `metrics[]`, whose semantics are adjudicated by the metric
    structural checks `DSX-SPEC-020..026` (`dsx/spec.py:892-971`) and the `DSX-MET-*` family; the
    skill names the field and defers the numeric definition to the gate. (2) A cohort visual declared
    as `data_input_type: matrix`, consulted through `dsx charts` and adjudicated by `DSX-VIZ-013`
    (`dsx/spec.py:290-319`, `dsx/cli.py:1060-1063`); the skill must NOT state which marks are
    admissible — that ruling is the chart matrix's. (3) A `decision.revisit_when` trigger adjudicated
    by the coherence check `DSX-COH-040` (`dsx/spec.py:755-777`). Instruct the analyst to run
    `dsx gate plan`, `dsx charts`, and `dsx check` to adjudicate. Naming caveat (D-08): state that
    cohort points at the EXISTING coherence `revisit_when` check `DSX-COH-040`; the SKILL.md must NOT
    imply a `DSX-COH-*` "cohort" code family exists (that family is Coherence). State no numeric
    threshold, correction method, or admissible-chart rule of its own — every number, if present, is
    on the same line as the `DSX-*` code that owns it (route-and-cite, mirroring
    `capabilities/dsx/fragments/executor.md:34-35`). Add no Python; register nothing here.
  </action>
  <acceptance_criteria>
    - `skills/dsx-cohort/SKILL.md` exists and its frontmatter contains `name: dsx-cohort`, a `description`, an `argument-hint`, and an `allowed-tools` list (grep `^name: dsx-cohort` and `^argument-hint:` and `^allowed-tools:` each return a hit).
    - The body cites, verbatim, the codes `DSX-SPEC-020`, `DSX-MET-`, `DSX-VIZ-013`, and `DSX-COH-040` (grep each returns a hit).
    - The body contains no phrase asserting a `DSX-COH-*` cohort code family; it states cohort routes to the existing coherence `revisit_when` check `DSX-COH-040` (manual read confirms; grep `cohort code` returns no hit implying a new family).
    - Anti-parallel-advice: `grep -hEi '(p ?[<>=]|\balpha\b|α|[0-9]+ ?%|[<>]=? ?0?\.[0-9]|\bbonferroni\b|\bholm\b|\bbenjamini\b|\bfdr\b|\bsidak\b)' skills/dsx-cohort/SKILL.md | grep -v 'DSX-'` returns no lines.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "^name: dsx-cohort" skills/dsx-cohort/SKILL.md && grep -q "DSX-COH-040" skills/dsx-cohort/SKILL.md && grep -q "DSX-VIZ-013" skills/dsx-cohort/SKILL.md && ! (grep -hEi '(p ?[<>=]|\balpha\b|α|[0-9]+ ?%|[<>]=? ?0?\.[0-9]|\bbonferroni\b|\bholm\b|\bbenjamini\b|\bfdr\b|\bsidak\b)' skills/dsx-cohort/SKILL.md | grep -q 'DSX-\|.')</automated>
  </verify>
  <done>skills/dsx-cohort/SKILL.md exists, mirrors the house frontmatter, routes the three fields to DSX-SPEC-020..026/DSX-MET-*, DSX-VIZ-013 and DSX-COH-040, holds the D-08 naming caveat, and carries no unattributed threshold.</done>
</task>

<task type="auto">
  <name>Task 2: Author skills/dsx-funnel/SKILL.md as a router to the existing gates</name>
  <files>skills/dsx-funnel/SKILL.md</files>
  <read_first>
    - skills/dsx-define-metrics/SKILL.md:1-13 (house frontmatter shape)
    - .planning/phases/13-task-playbooks-that-fill-the-spec/13-CONTEXT.md (D-02 contract row for dsx-funnel)
    - dsx/spec.py:290-319 (CHART_CAPABILITIES; funnel mark admitted only under event-time, :311-312) and dsx/cli.py:1060-1063 (`dsx charts` + DSX-VIZ-013)
    - skills/dsx-explore-data/SKILL.md:550-557 (the existing conversion-funnel integrity routine — n_ordering_violations / violation_share and its block threshold; dsx-funnel routes to this, it does NOT restate the number)
  </read_first>
  <action>
    Create `skills/dsx-funnel/SKILL.md`. Frontmatter mirrors the house shape: `name: dsx-funnel`, a
    one-sentence `description` (an ordered step-conversion / drop-off question), an `argument-hint`,
    and `allowed-tools` (Read, Write, Edit, Bash, Grep, Glob, Agent). Router body: (1) step-conversion
    entries in `metrics[]`, adjudicated by `DSX-MET-*` and the metric structural checks
    `DSX-SPEC-020..026`. (2) A visual declared with `data_input_type: event-time` carrying the
    `funnel` mark — the chart matrix admits `funnel` only under `event-time` (`dsx/spec.py:311-312`),
    else `DSX-VIZ-013` fires; the skill names the field pairing and defers admissibility to the gate.
    (3) Route the ordering / changing-denominator integrity to the EXISTING conversion-funnel routine
    at `skills/dsx-explore-data/SKILL.md:550-557` (which computes `n_ordering_violations` and
    `violation_share` and owns the block threshold) rather than restating any number. Tell the analyst
    to run `dsx charts` and `dsx gate plan` to adjudicate. State no threshold or admissible-chart rule
    of its own; every number, if present, sits on the same line as its owning `DSX-*` code.
  </action>
  <acceptance_criteria>
    - `skills/dsx-funnel/SKILL.md` exists; frontmatter has `name: dsx-funnel`, `description`, `argument-hint`, `allowed-tools` (grep confirms each).
    - The body cites `DSX-MET-`, `DSX-VIZ-013`, and the `event-time` + `funnel` pairing, and points at `dsx-explore-data/SKILL.md` for the ordering-integrity routine rather than restating its threshold (grep `dsx-explore-data` returns a hit; grep for a bare `1%`/`1 %` ordering-violation threshold on a line without `DSX-` returns none).
    - Anti-parallel-advice: `grep -hEi '(p ?[<>=]|\balpha\b|α|[0-9]+ ?%|[<>]=? ?0?\.[0-9]|\bbonferroni\b|\bholm\b|\bbenjamini\b|\bfdr\b|\bsidak\b)' skills/dsx-funnel/SKILL.md | grep -v 'DSX-'` returns no lines.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "^name: dsx-funnel" skills/dsx-funnel/SKILL.md && grep -q "DSX-VIZ-013" skills/dsx-funnel/SKILL.md && grep -q "event-time" skills/dsx-funnel/SKILL.md && grep -q "dsx-explore-data" skills/dsx-funnel/SKILL.md && ! (grep -hEi '(p ?[<>=]|\balpha\b|α|[0-9]+ ?%|[<>]=? ?0?\.[0-9]|\bbonferroni\b|\bholm\b|\bbenjamini\b|\bfdr\b|\bsidak\b)' skills/dsx-funnel/SKILL.md | grep -q 'DSX-\|.')</automated>
  </verify>
  <done>skills/dsx-funnel/SKILL.md exists, mirrors the house frontmatter, routes step-conversion metrics and the event-time funnel visual to DSX-MET-*/DSX-VIZ-013, defers ordering integrity to the existing conversion-funnel routine, and carries no unattributed threshold.</done>
</task>

<task type="auto">
  <name>Task 3: Register all four new playbooks in the capability manifest</name>
  <files>capabilities/dsx/capability.json</files>
  <read_first>
    - capabilities/dsx/capability.json:35-45 (the flat "skills" array — the append target) and :125-192 (steps/contributions — proof the eight non-scope skills are name-invoked, not loop-wired)
    - .planning/phases/13-task-playbooks-that-fill-the-spec/13-CONTEXT.md (D-01: name-append only; no steps/contributions/gates/agents entry)
  </read_first>
  <action>
    Edit `capabilities/dsx/capability.json`. Append ALL FOUR new playbook names — `dsx-cohort`,
    `dsx-funnel`, `dsx-root-cause`, and `dsx-segment` — to the flat `"skills"` array
    (`capability.json:35-45`), after the existing `dsx-review-analysis` entry (order within the array is
    not significant, JSON validity is). 13-01 is the phase's SOLE `capability.json` writer: plan 13-02
    authors the `dsx-root-cause` and `dsx-segment` SKILL.md files but deliberately does NOT touch the
    manifest and runs concurrently in this same wave, so their names are never already present — this
    task must register all four, or root-cause/segment ship unregistered and unreachable by name
    (Success Criterion 1 / REQ-P13-01). Do NOT add any `steps`, `contributions`, `gates`, or `agents`
    entry for these playbooks (D-01: they are on-demand, invoked by name like the other registered
    skills; only `dsx-scope-analysis` is loop-wired at `:125-134`, and loop-wiring an advisory playbook
    would fire it unconditionally at a loop point). Preserve JSON validity — comma placement, no
    trailing comma. This is the phase's single shared-file write; no other plan edits this file.
  </action>
  <acceptance_criteria>
    - `python -c "import json; json.load(open('capabilities/dsx/capability.json'))"` exits 0 (the file is still valid JSON).
    - The parsed `"skills"` array contains all four new names (`python -c "import json; s=json.load(open('capabilities/dsx/capability.json'))['skills']; assert all(n in s for n in ('dsx-cohort','dsx-funnel','dsx-root-cause','dsx-segment'))"` exits 0).
    - No new key was added under `steps`, `contributions`, `gates`, or `agents` referencing any of the four new playbook names (grep for those four names outside the `"skills"` array returns no hits).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import json; s=json.load(open('capabilities/dsx/capability.json'))['skills']; assert all(n in s for n in ('dsx-cohort','dsx-funnel','dsx-root-cause','dsx-segment')); print('registered')"</automated>
  </verify>
  <done>capability.json is valid JSON, its "skills" array lists all four new playbooks (dsx-cohort, dsx-funnel, dsx-root-cause, dsx-segment), and no steps/contributions/gates/agents entry was added for any of them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds two markdown SKILL.md files and appends four playbook names to one JSON array. It introduces
no executable surface, no network or file input on any runtime path, no auth, and no user-supplied
input on any deterministic gate path. ASVS Level 1 injection / auth / session vectors are N/A here —
there is no exploitable surface to assess. No npm / pip / cargo install occurs, so the supply-chain
vector (T-13-SC) is N/A this plan.

| Boundary | Description |
|----------|-------------|
| analyst → ANALYSIS-SPEC.yaml | The playbook is prose read by an analyst; the spec it produces is adjudicated only by the existing deterministic `dsx` gates. No new trust boundary is created. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | The two SKILL.md files stating a threshold/correction/chart rule of their own (parallel-advice drift) | medium | mitigate | D-02 route-and-cite rule: a playbook may name a gate + code and tell the analyst to run `dsx gate/check`, but may not state a number of its own; enforced by the per-task anti-parallel-advice grep (S1-4). |
| T-13-02 | Tampering | Accidental edit to the deterministic gate path, or a JSON edit that mints/loop-wires a check | high | mitigate | D-01/D-08 scope fence: name-append only, no steps/contributions/gates/agents entry; files_modified touches no path under `dsx/`; certified phase-wide by the plan 13-05 catalogue set-identity diff (REQ-P13-06). |
| T-13-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs occur in this phase. |
</threat_model>

<verification>
Phase-level goal-backward coverage (union across all five plans): Success Criterion 1 (the four
skills exist + are registered + route to existing gates) is delivered by this plan (cohort, funnel)
and plan 13-02 (root-cause, segment); Criteria 2–3 by 13-03; Criteria 4–5 by 13-04; Criterion 6
(catalogue diff) by 13-05; the D-02 anti-parallel-advice check is verified per-file in 13-01 and
13-02, whose union covers all four playbooks.

For this plan specifically:
- Both new SKILL.md files exist with house frontmatter and route their fields to the named existing codes.
- `python -c "import json; json.load(open('capabilities/dsx/capability.json'))"` exits 0 and the "skills" array lists all four new playbook names (cohort, funnel, root-cause, segment).
- The anti-parallel-advice grep returns no unattributed threshold lines for either file.
</verification>

<success_criteria>
- skills/dsx-cohort/SKILL.md and skills/dsx-funnel/SKILL.md exist, mirror the house frontmatter, and are pure routers citing existing DSX-* codes.
- capabilities/dsx/capability.json remains valid JSON and registers all four new skill names (cohort, funnel, root-cause, segment) in the flat "skills" array, with no steps/contributions/gates/agents entry added.
- No path under dsx/ is modified; no new finding code is minted.
</success_criteria>

<output>
Create `.planning/phases/13-task-playbooks-that-fill-the-spec/13-01-SUMMARY.md` when done.
</output>
