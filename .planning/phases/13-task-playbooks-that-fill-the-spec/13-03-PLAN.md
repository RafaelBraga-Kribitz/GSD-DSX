---
phase: 13-task-playbooks-that-fill-the-spec
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/dsx-explore-data/SKILL.md
  - skills/dsx-narrate/SKILL.md
autonomous: true
requirements: [REQ-P13-02, REQ-P13-03]
tags: [dsx, skill, hypothesis-register, narrative]

must_haves:
  truths:
    - "dsx-explore-data writes a hypothesis register: a deterministic mapping rule routing an untested belief the analysis rests on into assumptions[] (adjudicated by DSX-COH-030/031) and a promoted confirmatory hypothesis into design.multiplicity.family[] -> results.tests[] (adjudicated by DSX-EXP-050..053), riding the existing EDA.md ledgers (Success Criterion 2 / REQ-P13-02)."
    - "dsx-narrate requires an explicit What / So What / Now What shape mapped onto its existing five-part structure, template-only, riding DSX-COH-040 (revisit_when) and DSX-CLM-080 (limitations) — no new DSX-NAR code (Success Criterion 3 / REQ-P13-03)."
  artifacts:
    - skills/dsx-explore-data/SKILL.md
    - skills/dsx-narrate/SKILL.md
  key_links:
    - "The hypothesis-register mapping must key on shape (untested belief vs promoted test) so each hypothesis lands in a carrier a shipped check already reads (DSX-COH-031 or DSX-EXP-050..053)."
    - "The What/So What/Now What shape must map onto the existing narrate sections and cite only existing codes; adding a DSX-NAR shape code would move the catalogue off 256."
  prohibitions:
    - statement: "Zero new DSX-* finding codes are minted; the catalogue stays exactly 256 (no DSX-NAR-0xx shape code)."
      status: enforced
      verification: "Both edits are markdown; the catalogue generator only walks dsx/**/*.py. Certified phase-wide by plan 13-05 (set-identity diff)."
    - statement: "No edits to the deterministic dsx gate path — dsx/checks/narrative.py and all of dsx/ stay read-only; the narrate shape adds no heading-scanner gate."
      status: enforced
      verification: "files_modified touches only the two SKILL.md files; no path under dsx/."
    - statement: "No new schema field; REQ-P13-02 is a mapping rule over the existing assumptions[]/results.tests carriers and the existing EDA.md ledgers, not a new register format."
      status: enforced
      verification: "the edit references existing fields (assumptions[], design.multiplicity.family[], results.tests[]) and the existing EDA.md ledgers (dsx-explore-data/SKILL.md:664-751); it declares no new spec field."
---

<objective>
Two focused edits to existing skills: (1) add the hypothesis-register mapping rule to
`dsx-explore-data` (REQ-P13-02) and (2) make the What / So What / Now What shape explicit in
`dsx-narrate` (REQ-P13-03). Both are prompt-guidance edits that ride existing carriers and existing
finding codes; neither mints a code or adds a schema field.

Purpose: EDA should route each hypothesis to a carrier a shipped check already reads, and the
narrative should carry a decision-ready three-part shape without a new gate. Both requirements were
shaped to be deliverable without a new code (REQ-P13-06).

Output: edits to `skills/dsx-explore-data/SKILL.md` and `skills/dsx-narrate/SKILL.md`.
</objective>

<artifacts_this_phase_produces>
This plan edits two existing files:
- `skills/dsx-explore-data/SKILL.md` (adds the hypothesis-register mapping rule)
- `skills/dsx-narrate/SKILL.md` (makes the What / So What / Now What shape explicit)

It produces NO new files, NO new symbols under `dsx/`, NO new `DSX-*` finding codes, and NO new
spec/schema field. Zero file overlap with plans 13-01, 13-02, 13-04 — same wave.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-task-playbooks-that-fill-the-spec/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the hypothesis-register mapping rule to dsx-explore-data</name>
  <files>skills/dsx-explore-data/SKILL.md</files>
  <read_first>
    - skills/dsx-explore-data/SKILL.md:586-599 (the candidate→confirmatory promotion handshake to reuse verbatim) and :664-751 (the existing EDA.md findings/comparisons ledgers = the register)
    - .planning/phases/13-task-playbooks-that-fill-the-spec/13-CONTEXT.md (D-03: carrier = both existing carriers, keyed by shape; discretion to ride the existing EDA.md ledgers rather than a new templates/ heading)
    - references/finding-codes.md:332-333 (DSX-COH-030 present-when-causal/prescriptive; DSX-COH-031 each assumptions row checked:true XOR waiver)
    - dsx/checks/design.py:362-412 (DSX-EXP-050..053 adjudicating results.tests against design.multiplicity.family)
  </read_first>
  <action>
    Edit `skills/dsx-explore-data/SKILL.md` to add a hypothesis-register mapping rule (D-03), stated as
    a deterministic router over the EXISTING carriers — not a new schema. A hypothesis that is an
    untested belief the analysis rests on maps to an `assumptions[]` row
    `{assumption, rationale, impact_if_wrong, checked, waiver}`, adjudicated by `DSX-COH-030`
    (present-when-causal/prescriptive) and `DSX-COH-031` (each row `checked:true` XOR `waiver`). A
    hypothesis promoted to a confirmatory test is declared in `design.multiplicity.family[]` at scope
    time and filled in `results.tests[]` at execute, adjudicated by `DSX-EXP-050..053`. State that the
    "register" itself is the existing `EDA.md` findings / comparisons ledgers
    (`skills/dsx-explore-data/SKILL.md:664-751`), and reuse the candidate→confirmatory promotion path
    the skill already ships (`:586-599`: promotion is a spec amendment adding the test to
    `design.multiplicity.family`; a candidate is never promoted into `decision.replay`). Recommended
    placement (Claude's discretion per D-03): add the mapping rule adjacent to the existing ledger
    section so it rides the EDA.md ledgers rather than introducing a new `templates/` heading. Declare
    no new spec field and no new code.
  </action>
  <acceptance_criteria>
    - `skills/dsx-explore-data/SKILL.md` now contains a hypothesis-register mapping rule that names both carriers: `assumptions[]` and `results.tests` / `design.multiplicity.family` (grep each returns a hit).
    - The mapping cites `DSX-COH-030`, `DSX-COH-031`, and `DSX-EXP-050` (or the `DSX-EXP-050..053` range) as the adjudicating checks (grep confirms).
    - The edit references the existing EDA.md ledgers as the register and declares no new spec field (grep for a new field declaration or a new `templates/` schema returns none; manual read confirms it routes to existing carriers).
    - The candidate→confirmatory promotion path at :586-599 remains intact (the "never promotes a candidate into decision.replay" clause is still present — grep `decision.replay` returns its existing hit).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "assumptions" skills/dsx-explore-data/SKILL.md && grep -q "DSX-COH-031" skills/dsx-explore-data/SKILL.md && grep -q "DSX-EXP-05" skills/dsx-explore-data/SKILL.md && grep -q "results.tests" skills/dsx-explore-data/SKILL.md && grep -q "decision.replay" skills/dsx-explore-data/SKILL.md</automated>
  </verify>
  <done>skills/dsx-explore-data/SKILL.md carries a hypothesis-register mapping rule routing an untested belief into assumptions[] (adjudicated by DSX-COH-030/031) and a promoted confirmatory hypothesis into design.multiplicity.family[] -> results.tests[] (adjudicated by DSX-EXP-050..053), riding the existing EDA.md ledgers and reusing the candidate->confirmatory handshake (:586-599, "never promotes a candidate into decision.replay" intact), with no new spec field and no new code.</done>
</task>

<task type="auto">
  <name>Task 2: Make the What / So What / Now What shape explicit in dsx-narrate</name>
  <files>skills/dsx-narrate/SKILL.md</files>
  <read_first>
    - skills/dsx-narrate/SKILL.md:26-64 (the existing five-part `<structure>` the three-part shape maps onto: §1 the answer, §2 what it means / the action the pre-declared rule implies, §4 what would change it; and the existing citations of DSX-CLM-080 (:47) and DSX-COH-040 (:50))
    - .planning/phases/13-task-playbooks-that-fill-the-spec/13-CONTEXT.md (D-04: template-only, NO DSX-NAR mint; What -> §1, So What -> §2, Now What -> §4 citing gate-read decision.revisit_when (DSX-COH-040) and non-empty limitations[] (DSX-CLM-080); do NOT add a heading-scanner DSX-NAR-0xx code — it would move the catalogue off 256 and fail S1-5)
    - references/finding-codes.md:223 (DSX-CLM-080 non-empty limitations) and :332-334 (DSX-COH-040 revisit_when) — the existing codes the Now What part rides
  </read_first>
  <action>
    Edit `skills/dsx-narrate/SKILL.md` to make the What / So What / Now What shape explicit (D-04),
    as a TEMPLATE requirement layered onto the existing five-part `<structure>` (`:26-35`) — not a new
    gate. Map the three parts onto the sections the skill already prescribes: **What** = the answer
    (§1, "one sentence, with the number and its interval"); **So What** = what it means / the action the
    pre-declared decision rule implies (§2); **Now What** = what would change it (§4), which for
    prescriptive / experiment readouts cites the gate-read `decision.revisit_when` trigger (already
    enforced by `DSX-COH-040`) and the non-empty `limitations[]` (already enforced by `DSX-CLM-080`).
    Route-and-cite: name those existing codes rather than restating their rules (the skill already cites
    both at `:47` and `:50`). HARD BOUND (D-04): do NOT add a heading-scanner narrative gate and do NOT
    introduce any new `DSX-NAR` shape/heading code — the shape gets exactly as much teeth as the
    existing codes allow, and minting a new narrative code would move the catalogue off 256 and fail the
    plan 13-05 scope-bound diff. Add no new spec field. The only `DSX-NAR` code the file references stays
    the existing forbidden-wording `DSX-NAR-030` (`:55`); introduce no second one.
  </action>
  <acceptance_criteria>
    - `skills/dsx-narrate/SKILL.md` now contains the explicit "So What" and "Now What" wording (both absent today), mapped onto the existing structure (grep each returns a hit).
    - The Now What part ties to the existing gate-read fields/codes: `decision.revisit_when` / `DSX-COH-040` and `limitations` / `DSX-CLM-080` (grep confirms each is present).
    - No new `DSX-NAR` code string is introduced: the file references exactly one distinct `DSX-NAR-*` code (the pre-existing `DSX-NAR-030`); the shape is template-only and adds no heading-scanner gate (manual read confirms).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "So What" skills/dsx-narrate/SKILL.md && grep -q "Now What" skills/dsx-narrate/SKILL.md && grep -q "DSX-COH-040" skills/dsx-narrate/SKILL.md && grep -q "DSX-CLM-080" skills/dsx-narrate/SKILL.md && grep -q "revisit_when" skills/dsx-narrate/SKILL.md && test $(grep -oE "DSX-NAR-[0-9]+" skills/dsx-narrate/SKILL.md | sort -u | wc -l) -eq 1</automated>
  </verify>
  <done>skills/dsx-narrate/SKILL.md makes the What / So What / Now What shape explicit over its existing five-part structure, with Now What citing the gate-read decision.revisit_when (DSX-COH-040) and non-empty limitations[] (DSX-CLM-080); it mints no DSX-NAR shape code (exactly one distinct DSX-NAR code, the pre-existing DSX-NAR-030) and adds no heading-scanner gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan edits two markdown SKILL.md files. It introduces no executable surface, no network or file
input on any runtime path, no auth, and no user-supplied input on any deterministic gate path. ASVS
Level 1 injection / auth / session vectors are N/A here — there is no exploitable surface to assess.
No npm / pip / cargo install occurs, so the supply-chain vector (T-13-SC) is N/A this plan.

| Boundary | Description |
|----------|-------------|
| analyst -> EDA.md ledgers / ANALYSIS-SPEC.yaml | The hypothesis-register rule routes each hypothesis into an existing carrier (assumptions[] or design.multiplicity.family[] -> results.tests[]) that a shipped check already reads; no new carrier or trust boundary is created. |
| narrative -> NARRATIVE.md | The What / So What / Now What shape is prose read by the analyst; the deliverable is adjudicated only by the existing narrative/coherence/claims gates. No new trust boundary is created. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | The dsx-narrate edit adding a heading-scanner narrative gate or a new `DSX-NAR-0xx` shape code (which would move the catalogue off 256 and fail S1-5) | high | mitigate | D-04 template-only: the three-part shape maps onto the existing five-part structure and cites only existing codes (DSX-COH-040, DSX-CLM-080); no DSX-NAR code is minted (exactly one distinct DSX-NAR code, the pre-existing DSX-NAR-030); certified phase-wide by the plan 13-05 catalogue set-identity diff. |
| T-13-02 | Tampering | The hypothesis-register edit declaring a NEW schema field / register format instead of routing to the existing carriers (parallel-schema drift) | medium | mitigate | D-03 mapping rule: route an untested belief to assumptions[] (DSX-COH-030/031) and a promoted test to design.multiplicity.family[] -> results.tests[] (DSX-EXP-050..053), riding the existing EDA.md ledgers; declare no new spec field. |
| T-13-03 | Tampering | Accidental edit to the deterministic gate path (dsx/checks/narrative.py or any dsx/) | high | mitigate | Scope fence: markdown only; files_modified touches only the two SKILL.md files, no path under `dsx/`; certified phase-wide by the plan 13-05 set-identity diff (REQ-P13-06). |
| T-13-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs occur in this phase. |
</threat_model>

<verification>
Phase-level goal-backward coverage (union across all five plans) is documented in plan 13-01. This
plan delivers Success Criterion 2 (dsx-explore-data writes a hypothesis register mapping into
assumptions[] and/or results.tests, REQ-P13-02) and Success Criterion 3 (dsx-narrate uses an explicit
What / So What / Now What shape, REQ-P13-03), both template-only and minting no code.

For this plan specifically:
- `skills/dsx-explore-data/SKILL.md` carries the hypothesis-register mapping rule routing to the existing carriers and codes, with the candidate->confirmatory handshake intact and no new field.
- `skills/dsx-narrate/SKILL.md` makes the What / So What / Now What shape explicit over its existing five-part structure, citing DSX-COH-040 and DSX-CLM-080, minting no DSX-NAR shape code.
</verification>

<success_criteria>
- skills/dsx-explore-data/SKILL.md maps each hypothesis to an existing carrier (assumptions[] via DSX-COH-030/031, or design.multiplicity.family[] -> results.tests[] via DSX-EXP-050..053) riding the existing EDA.md ledgers, with no new spec field.
- skills/dsx-narrate/SKILL.md makes the What / So What / Now What shape explicit over its five existing sections, with Now What citing DSX-COH-040 and DSX-CLM-080, and no new DSX-NAR code (exactly one distinct DSX-NAR code remains).
- No path under dsx/ is modified; no new finding code is minted; no new schema field is added.
</success_criteria>

<output>
Create `.planning/phases/13-task-playbooks-that-fill-the-spec/13-03-SUMMARY.md` when done.
</output>