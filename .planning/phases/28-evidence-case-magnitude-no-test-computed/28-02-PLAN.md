---
phase: 28-evidence-case-magnitude-no-test-computed
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - examples/known-bad/magnitude-without-computed-effect-ANALYSIS-SPEC.yaml
  - examples/known-bad/magnitude-without-computed-effect-entrypoint.py
  - examples/known-bad/magnitude-without-computed-effect-POSTMORTEM.md
  - examples/known-bad/magnitude-without-computed-effect-ATTRIBUTION.yaml
  - tests/test_known_bad_corpus.py
  - tests/test_frame_val.py
  - tests/test_causal_verb_golden.py
  - tests/test_dsx.py
  - brief.md
autonomous: true
requirements: [REQ-P28-01, REQ-P28-03]
must_haves:
  truths:
    - "The promotion is a single-fixture add whose _EXPECTED_CAUGHT_DEFECTS entry is the empty frozenset() and whose _EXPECTED_VAL_CODES entry is the empty set() — a MISS catches nothing (the empty/single-element result)."
    - "The measured golden ship set for the promoted fixture is stored as an order-independent frozenset, and DSX-CLM-034 appears at no gate point on the fixture (it declares no supported_by), so the ordering of equal-severity residual findings is immaterial to the equality check (ordering)."
    - "The corpus slug `magnitude-without-computed-effect` is textually and semantically distinct from the existing `chart-takeaway-without-magnitude` fixture — no collision when the new fixture touches the existing corpus glob (adjacency)."
    - "examples/known-bad/magnitude-without-computed-effect-* validates (dsx validate exit 0) and is discovered by every corpus glob; its ATTRIBUTION sidecar sets kind: miss with absent_code: DSX-CLM-034, and the falsifiability test confirms DSX-CLM-034 fires nowhere CRITICAL — the empty/single case whose only attribution is the declared absent code (empty)."
    - "All three glob-discovered harness maps (_EXPECTED_CAUGHT_DEFECTS, _EXPECTED_VAL_CODES, _GOLDEN_SHIP_FINDINGS) gain exactly one keyed entry under the fixture basename; the key-parity tests fail loudly if any is missing, and the golden set is re-measured live rather than guessed (ordering)."
    - "The spec count moves 43 → 44 in tests/test_dsx.py exactly when the fixture lands — one added spec, no drift (boundary/precision)."
    - "The ATTRIBUTION sidecar's promotes_backlog_item is the frozen id `6.5-item-8-magnitude-without-computed-effect` (a verbatim member of _SECTION_65_ITEM_IDS at tests/test_known_bad_corpus.py:853), and DSX-CLM-034 stays OUT of _SECTION_65_BACKLOG_CODES so the disjointness assertion passes (encoding/equality: the id must match the frozen string exactly)."
    - "brief.md §6.5 item 8 states the measured evidence: a LIVE MISS at all four gate points with DSX-CLM-034 minted under D-05 to attribute it — attribution, not a catch (REQ-P28-03)."
    - "`node install.mjs` then `node install.mjs --check` pass at phase end — the dsx/, references/, templates/, and examples/ overlays are re-synced (boundary: the installed copy equals the source tree)."
  artifacts:
    - examples/known-bad/magnitude-without-computed-effect-ANALYSIS-SPEC.yaml
    - examples/known-bad/magnitude-without-computed-effect-entrypoint.py
    - examples/known-bad/magnitude-without-computed-effect-POSTMORTEM.md
    - examples/known-bad/magnitude-without-computed-effect-ATTRIBUTION.yaml
  key_links:
    - "The three glob-discovered harness maps (_EXPECTED_CAUGHT_DEFECTS, _EXPECTED_VAL_CODES, _GOLDEN_SHIP_FINDINGS) each gain one keyed entry under the corpus basename magnitude-without-computed-effect; key-parity tests fail if any is missing."
    - "The sidecar's absent_code DSX-CLM-034 resolves against the catalogue shipped in wave 1 (plan 28-01); its promotes_backlog_item resolves against the frozen _SECTION_65_ITEM_IDS member at tests/test_known_bad_corpus.py:853."
  prohibitions:
    - "This plan executes ONLY if Plan 28-01 recorded VERDICT: LIVE MISS; if 28-MEASUREMENT.md records VERDICT: CAUGHT, this plan does not run (the phase already closed in 28-01)."
    - "The promoted ANALYSIS-SPEC must not declare a claims[].supported_by field — that would make DSX-CLM-034 bite and convert the MISS into a CATCH (kind: miss would flip). Its absence is asserted behaviourally by the falsifiability and golden-set tests, not by editing the field in."
    - "DSX-CLM-034 is not added to _SECTION_65_BACKLOG_CODES — it ships to the catalogue; the disjointness assertion forbids a shipped code there."
    - "No entry is added to _TARGET_DEFECT_CODES and no edit is made to _INCIDENTAL_GAP_CODES unless the live measurement documents a new residual — a MISS catches nothing."
    - "The golden ship set is re-measured live on the promoted fixture, never guessed; if the measured set differs from the expected, stop and record the discrepancy rather than silently overwrite."
    - "dsx/checks/dq.py is not modified (byte-frozen)."
---

<objective>
Promote the measured spike into a committed known-bad corpus fixture and complete every harness
entry, then rewrite the brief's §6.5 item 8 row with the measured evidence. This is the REQ-P28-01
(corpus case) and REQ-P28-03 (harness + brief) deliverable, and it executes ONLY on the LIVE MISS
branch — Plan 28-01's Task 1 must have recorded `VERDICT: LIVE MISS`. (If 28-01 recorded CAUGHT, the
phase already closed in 28-01 and this plan does not run.)

The spike (spike/magnitude-no-test-computed-*) was measured a LIVE MISS at all four gate points
(28-MEASUREMENT.md; D-13 satisfied); promote it under the corpus basename
`magnitude-without-computed-effect` with a new POSTMORTEM (reproducing the four-point table verbatim)
and an ATTRIBUTION sidecar naming `absent_code: DSX-CLM-034` (the code minted in plan 28-01) and
`promotes_backlog_item: 6.5-item-8-magnitude-without-computed-effect` (the frozen id at
tests/test_known_bad_corpus.py:853 — NOT the phase slug). Register the fixture as a MISS in the four
harness maps, move the spec count 43→44, and keep DSX-CLM-034 OUT of the backlog disjoint set. The
fixture must stay a MISS: it declares NO claims[].supported_by, so the wave-1 check stays silent on
it (attribution, not detection — the Phase-27 precedent).

Purpose: turn the measured miss into a permanent, self-testing corpus case with a falsifiable
attribution sidecar.
Output: four `examples/known-bad/magnitude-without-computed-effect-*` files; four harness map/count
edits; the rewritten brief row; a re-synced installer overlay.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Frozen design, measured verdict, wiring research (do NOT re-open D-28-00..05; guardrail 1)
@.planning/phases/28-evidence-case-magnitude-no-test-computed/28-CONTEXT.md
@.planning/phases/28-evidence-case-magnitude-no-test-computed/28-MEASUREMENT.md
@.planning/phases/28-evidence-case-magnitude-no-test-computed/28-RESEARCH.md

# Spike sources to promote + corpus siblings to mirror
@.planning/phases/28-evidence-case-magnitude-no-test-computed/spike/magnitude-no-test-computed-ANALYSIS-SPEC.yaml
@.planning/phases/28-evidence-case-magnitude-no-test-computed/spike/magnitude-no-test-computed-entrypoint.py
@examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml
@examples/known-bad/full-frame-cleaning-POSTMORTEM.md
@examples/known-bad/retracted-fabricated-field-experiment-ATTRIBUTION.yaml
</context>

<artifacts>
## Artifacts this phase produces (this plan's share)

- `examples/known-bad/magnitude-without-computed-effect-ANALYSIS-SPEC.yaml` — promoted from the
  spike spec; `spec_id` and `reproducibility.entrypoint` renamed to the corpus basename; spike
  header stripped, corpus header added; NO claims[].supported_by field (keeps it a MISS).
- `examples/known-bad/magnitude-without-computed-effect-entrypoint.py` — promoted from the spike
  entrypoint (basename in the header only; body is static text, never executed).
- `examples/known-bad/magnitude-without-computed-effect-POSTMORTEM.md` — authored new; reproduces the
  four-point measured table (28-MEASUREMENT.md) and names DSX-CLM-034 as the absent code.
- `examples/known-bad/magnitude-without-computed-effect-ATTRIBUTION.yaml` — authored new;
  `absent_code: DSX-CLM-034`, `promotes_backlog_item: "6.5-item-8-magnitude-without-computed-effect"`,
  `kind: miss`, `protocol_adherence: skipped`, rationale.

(Plan 28-01 produces _check_supported_by_traceability / DSX-CLM-034 / the catalogue row at 278.)
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: promote the spike spec + entrypoint into the known-bad corpus</name>
  <files>examples/known-bad/magnitude-without-computed-effect-ANALYSIS-SPEC.yaml, examples/known-bad/magnitude-without-computed-effect-entrypoint.py</files>
  <read_first>
    - .planning/phases/28-evidence-case-magnitude-no-test-computed/28-MEASUREMENT.md (confirm the recorded VERDICT is LIVE MISS before doing anything; this plan does not run on CAUGHT)
    - .planning/phases/28-evidence-case-magnitude-no-test-computed/spike/magnitude-no-test-computed-ANALYSIS-SPEC.yaml (source; spike header, spec_id, reproducibility.entrypoint)
    - .planning/phases/28-evidence-case-magnitude-no-test-computed/spike/magnitude-no-test-computed-entrypoint.py (source header)
    - examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml (corpus header precedent :1-38; spec_id == basename)
    - .planning/phases/28-evidence-case-magnitude-no-test-computed/28-CONTEXT.md (D-28-01 exact fixture shape; the "no supported_by → stays a MISS" invariant)
  </read_first>
  <action>
    First confirm 28-MEASUREMENT.md records VERDICT: LIVE MISS; if it records CAUGHT, stop — this plan
    must not run. Copy spike/magnitude-no-test-computed-ANALYSIS-SPEC.yaml to
    examples/known-bad/magnitude-without-computed-effect-ANALYSIS-SPEC.yaml and
    spike/magnitude-no-test-computed-entrypoint.py to
    examples/known-bad/magnitude-without-computed-effect-entrypoint.py. In the promoted spec: change
    spec_id to "magnitude-without-computed-effect" (basename convention, full-frame precedent) and
    change reproducibility.entrypoint to "magnitude-without-computed-effect-entrypoint.py". Strip the
    spike "MEASUREMENT-SPIKE / NOT a committed corpus fixture" header and replace it with a corpus
    header modelled on full-frame-cleaning-ANALYSIS-SPEC.yaml:1-38 — naming REQ-P28-01/02/03, stating
    the FROZEN D-28-01 defect (a churn magnitude claim whose 27/18 literals are the real reported
    numbers of the revenue_per_user and activation_rate tests, so DSX-CLM-033 clears on its full
    union-membership logic while no test computed churn), and that the case is a measured LIVE MISS
    (28-MEASUREMENT.md) whose only attribution is DSX-CLM-034. Leave every other block byte-equal to
    the spike. Do NOT add a claims[].supported_by field and do NOT add an explanatory comment that
    names that field token — its absence is what keeps the fixture a MISS (the wave-1 check stays
    silent). In the promoted entrypoint, update only the basename reference in its head comment; the
    body is static text read by the code scan and is never executed. Use python312 for any validate
    run.
  </action>
  <acceptance_criteria>
    - Both target files exist under examples/known-bad/ with the `magnitude-without-computed-effect-` basename.
    - `python312 -m dsx.cli validate --spec examples/known-bad/magnitude-without-computed-effect-ANALYSIS-SPEC.yaml` exits 0.
    - The promoted spec's spec_id is `magnitude-without-computed-effect` and reproducibility.entrypoint is `magnitude-without-computed-effect-entrypoint.py`.
    - The promoted spec declares no claims[].supported_by field (the fixture stays a MISS) — asserted behaviourally by Task 3's golden-set and falsifiability tests, not by editing the field in.
    - The spike header text ("MEASUREMENT-SPIKE", "NOT a committed corpus fixture") is absent from the promoted spec; a corpus header naming REQ-P28-01/02/03 is present.
  </acceptance_criteria>
  <verify>
    <automated>python312 -m dsx.cli validate --spec examples/known-bad/magnitude-without-computed-effect-ANALYSIS-SPEC.yaml</automated>
  </verify>
  <done>The spike spec and entrypoint are promoted under the corpus basename, validate exit 0, and the fixture declares no supported_by field.</done>
</task>

<task type="auto">
  <name>Task 2: author the POSTMORTEM and the falsifiable ATTRIBUTION sidecar</name>
  <files>examples/known-bad/magnitude-without-computed-effect-POSTMORTEM.md, examples/known-bad/magnitude-without-computed-effect-ATTRIBUTION.yaml</files>
  <read_first>
    - examples/known-bad/full-frame-cleaning-POSTMORTEM.md (postmortem structure; four-point gate table format)
    - examples/known-bad/retracted-fabricated-field-experiment-ATTRIBUTION.yaml (sidecar schema fields)
    - .planning/phases/28-evidence-case-magnitude-no-test-computed/28-MEASUREMENT.md (the verbatim four-point table + swap-invariance record)
    - tests/test_known_bad_corpus.py (`_SECTION_65_ITEM_IDS` member :853 — the exact promotes_backlog_item string; the corpus pairing test's required DSX-<LETTERS>-<digits> POSTMORTEM code; the falsifiability test for kind: miss :1725-1781)
  </read_first>
  <action>
    Author examples/known-bad/magnitude-without-computed-effect-POSTMORTEM.md modelled on
    full-frame-cleaning-POSTMORTEM.md: name the paired spec and entrypoint; describe the encoded
    defect (the mislabelled churn magnitude — 27/18 are the reported effects of other metrics, no
    test computed churn, attributable only through the missing claim→test traceability); reproduce
    the four-point measured gate table verbatim from 28-MEASUREMENT.md (validate/plan/execute/verify/
    ship, exit codes and CRITICAL/HIGH codes) and state it is the D-28-02 entry-condition test;
    record the swap-still-fires counterfactual (name the residual codes and state they are
    swap-invariant incidental corpus-gap codes, none about claim→test traceability); and name
    DSX-CLM-034 as the code that now attributes the miss. The POSTMORTEM MUST contain at least one
    finding code matching the DSX-<LETTERS>-<digits> shape (the corpus pairing test requires it).
    Author examples/known-bad/magnitude-without-computed-effect-ATTRIBUTION.yaml with exactly:
    `absent_code: DSX-CLM-034`; `promotes_backlog_item: "6.5-item-8-magnitude-without-computed-effect"`
    (the frozen id at tests/test_known_bad_corpus.py:853 — NOT the phase slug); `kind: miss`;
    `protocol_adherence: skipped`; and a rationale explaining that DSX-CLM-034 reads a declared
    claims[].supported_by pointer and this honest spec declares none, so the code correctly stays
    silent — attribution, not detection. Use python312 for any check run.
  </action>
  <acceptance_criteria>
    - Both files exist under examples/known-bad/ with the `magnitude-without-computed-effect-` basename.
    - The POSTMORTEM contains at least one token matching the pattern `DSX-[A-Z]+-[0-9]+` and contains the string `DSX-CLM-034`.
    - The POSTMORTEM contains a gate table with rows for validate, plan, execute, verify, ship matching 28-MEASUREMENT.md (verify/ship exit codes and residual codes as measured).
    - The ATTRIBUTION file's `absent_code` is `DSX-CLM-034`, `promotes_backlog_item` is `6.5-item-8-magnitude-without-computed-effect`, `kind` is `miss`, and `protocol_adherence` is `skipped`.
    - `python312 -m unittest tests.test_known_bad_corpus -q` reports OK (pairing, code-presence, and — after Task 3's map entries — falsifiability all pass).
  </acceptance_criteria>
  <verify>
    <automated>python312 -m unittest tests.test_known_bad_corpus -q</automated>
  </verify>
  <done>A POSTMORTEM reproducing the measured table and an ATTRIBUTION sidecar naming DSX-CLM-034 against the frozen backlog id exist and satisfy the corpus tests.</done>
</task>

<task type="auto">
  <name>Task 3: complete the three harness maps + spec count, rewrite brief §6.5 item 8, re-sync installer</name>
  <files>tests/test_known_bad_corpus.py, tests/test_frame_val.py, tests/test_causal_verb_golden.py, tests/test_dsx.py, brief.md</files>
  <read_first>
    - tests/test_known_bad_corpus.py (`_EXPECTED_CAUGHT_DEFECTS` :432; `_TARGET_DEFECT_CODES` :185; `_INCIDENTAL_GAP_CODES` :65; `_SECTION_65_BACKLOG_CODES` :832-837 + disjointness assertion :1619-1624; `_SECTION_65_ITEM_IDS` :845-855, item 8 at :853; falsifiability test :1725-1781)
    - tests/test_frame_val.py (`_EXPECTED_VAL_CODES` :1363; a `set()` entry precedent; key-parity test)
    - tests/test_causal_verb_golden.py (`_GOLDEN_SHIP_FINDINGS` :82; a golden entry precedent; per-fixture re-measure; key-parity)
    - tests/test_dsx.py (spec count `len(paths), 43` :583)
    - brief.md (§6.5 item 8 row at ~:378 — the "magnitude residual" row named by the comment at tests/test_known_bad_corpus.py:853)
    - .planning/phases/28-evidence-case-magnitude-no-test-computed/28-MEASUREMENT.md (the measured ship residual set to pin in the golden map)
  </read_first>
  <action>
    Add exactly these keyed entries, all under the corpus basename `magnitude-without-computed-effect`:
    in tests/test_known_bad_corpus.py add the empty frozenset() entry to _EXPECTED_CAUGHT_DEFECTS
    (a MISS catches nothing); in tests/test_frame_val.py add the empty set() entry keyed by
    `magnitude-without-computed-effect-ANALYSIS-SPEC.yaml` to _EXPECTED_VAL_CODES; in
    tests/test_causal_verb_golden.py add the measured ship-residual frozenset keyed by
    `examples/known-bad/magnitude-without-computed-effect-ANALYSIS-SPEC.yaml` to _GOLDEN_SHIP_FINDINGS.
    These map values are MEASURED, not assumed — the golden and val tests re-run the live gate on the
    promoted fixture; run them and, if the measured sets differ from what 28-MEASUREMENT.md recorded,
    stop and record the discrepancy (do NOT silently overwrite — a golden set containing DSX-CLM-034
    would mean the fixture stopped being a MISS, contradicting the frozen design). Do NOT add an entry
    to _TARGET_DEFECT_CODES, do NOT edit _INCIDENTAL_GAP_CODES unless a new residual is measured, and
    do NOT add DSX-CLM-034 to _SECTION_65_BACKLOG_CODES (it ships to the catalogue; the disjointness
    assertion at :1619-1624 forbids it). Move the spec count in tests/test_dsx.py:583 from 43 to 44
    and update the running-tally comment above it. Rewrite the brief.md §6.5 item 8 row (the magnitude
    residual row) so its entry-condition column states the measured evidence: the corpus now contains
    the magnitude-without-computed-effect case, measured a LIVE MISS at all four gate points (D-13),
    and DSX-CLM-034 was minted under D-05 to attribute it — attribution, not a catch. Finally re-sync
    the installer overlay: run `node install.mjs` (syncs dsx/, references/, templates/, examples/) then
    `node install.mjs --check`. Do NOT edit REQUIREMENTS.md, STATE.md, or ROADMAP.md (single-writer).
  </action>
  <acceptance_criteria>
    - `python312 -m unittest tests.test_known_bad_corpus tests.test_frame_val tests.test_causal_verb_golden -q` reports OK (all three key-parity + per-fixture-equality tests pass with the new entries).
    - The golden entry for the fixture equals the measured ship residual set from 28-MEASUREMENT.md and DSX-CLM-034 is NOT among the fixture's measured ship findings (falsifiability test green).
    - `_SECTION_65_BACKLOG_CODES` does not contain DSX-CLM-034 (the disjointness assertion passes); `_TARGET_DEFECT_CODES` has no magnitude-without-computed-effect key.
    - `python312 -m unittest tests.test_dsx -q` reports OK with the spec count at 44.
    - brief.md's §6.5 item 8 row contains the strings `LIVE MISS` (or "live miss") and `DSX-CLM-034`.
    - Full suite green: `python312 -m unittest discover -s tests -q` reports OK; `python312 scripts/gen-finding-catalogue.py --check` exits 0.
    - `node install.mjs` completes and `node install.mjs --check` exits 0 (overlays re-synced).
    - `git status --porcelain .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md` prints nothing (single-writer files untouched).
  </acceptance_criteria>
  <verify>
    <automated>python312 -m unittest discover -s tests -q && python312 scripts/gen-finding-catalogue.py --check && node install.mjs --check</automated>
  </verify>
  <done>All three harness maps and the spec count are consistent, the golden ship set is re-measured with DSX-CLM-034 absent, the brief row states the measured evidence, and the installer overlay is re-synced and verified; no single-writer tracking file was edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| promoted fixture → corpus harness | A fixture that silently gains a supported_by field converts the MISS into a CATCH and defeats the requirement's whole point (kind: miss). |
| ATTRIBUTION sidecar → §6.5 backlog id set | An id that does not match the frozen `_SECTION_65_ITEM_IDS` string fails the sidecar gate; a shipped absent_code placed in the backlog set fails the disjointness gate. |
| source tree → installed overlay | An un-synced overlay means the shipped package disagrees with the repo; `install --check` is the boundary check. |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-08 | Tampering | promoted ANALYSIS-SPEC | high | mitigate | The fixture MUST declare no claims[].supported_by field; the falsifiability test (:1725-1781) runs the live gate and requires DSX-CLM-034 silent nowhere-CRITICAL, and the golden-set test re-measures the ship set — either flips if a supported_by is added (Task 1/Task 3). |
| T-28-09 | Spoofing | ATTRIBUTION promotes_backlog_item | high | mitigate | Use the harness-frozen id `6.5-item-8-magnitude-without-computed-effect` (:853); the sidecar membership gate fails on any other string. |
| T-28-10 | Tampering | _SECTION_65_BACKLOG_CODES | high | mitigate | Keep DSX-CLM-034 out of the backlog set; the disjointness assertion (:1619-1624) fails if a shipped code is added there (Task 3). |
| T-28-11 | Tampering | spec-count pin | high | mitigate | Move tests/test_dsx.py:583 43→44 exactly when the fixture lands; the count assert fails on drift (Task 3). |
| T-28-12 | Tampering | golden ship set (vacuous pin) | high | mitigate | Re-measure via the live per-fixture golden test rather than pinning a guessed set; a wrong set fails the equality re-measure (Task 3). |
| T-28-13 | Information disclosure | installed overlay drift | medium | mitigate | Re-run `node install.mjs` then `node install.mjs --check` after all synced-dir edits (Task 3). |
| T-28-06 | Tampering | dsx/checks/dq.py byte-freeze | medium | mitigate | This plan edits no dsx/ source; `git diff --stat -- dsx/checks/dq.py` must be empty. |
| T-28-SC | Supply chain | package installs | low | accept | No npm/pip/cargo installs. The promoted entrypoint is read as text by the code scan and never executed; no Package Legitimacy Gate applies. |
</threat_model>

<verification>
- `python312 -m unittest tests.test_known_bad_corpus tests.test_frame_val tests.test_causal_verb_golden tests.test_dsx -q` — OK with all new entries.
- `python312 -m unittest discover -s tests -q` — full suite OK (spec count 44; catalogue 278 from wave 1).
- `python312 scripts/gen-finding-catalogue.py --check` — exit 0.
- `node install.mjs --check` — exit 0 (overlays re-synced).
- `git diff --stat -- dsx/checks/dq.py` empty — byte-freeze held.
</verification>

<success_criteria>
- The four `examples/known-bad/magnitude-without-computed-effect-*` files exist, validate, and are a MISS (no supported_by field; DSX-CLM-034 silent) (REQ-P28-01).
- The POSTMORTEM reproduces the four-point measured table and names DSX-CLM-034; the ATTRIBUTION sidecar is falsifiable against the live gate with the frozen backlog id (REQ-P28-01/03).
- All three harness maps + the spec count (44) are consistent and green; DSX-CLM-034 stays out of the backlog set (REQ-P28-03).
- brief.md §6.5 item 8 states the measured evidence (LIVE MISS; DSX-CLM-034 minted) (REQ-P28-03).
- The installer overlay is re-synced and `node install.mjs --check` passes.
</success_criteria>

<output>
Create `.planning/phases/28-evidence-case-magnitude-no-test-computed/28-02-SUMMARY.md` when done.
</output>
