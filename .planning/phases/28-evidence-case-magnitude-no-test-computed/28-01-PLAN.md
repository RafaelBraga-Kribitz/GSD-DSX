---
phase: 28-evidence-case-magnitude-no-test-computed
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - dsx/checks/claims.py
  - tests/test_claims_supported_by.py
  - templates/ANALYSIS-SPEC.yaml
  - scripts/gen-finding-catalogue.py
  - references/finding-codes.md
  - tests/test_finding_catalogue_invariant.py
  - tests/test_phase20_zero_mint_close.py
  - brief.md
autonomous: true
requirements: [REQ-P28-01, REQ-P28-02]
must_haves:
  truths:
    - "The four-point measurement runs at the DEFAULT gate threshold and records that DSX-CLM-070 clears (base_n declared) and DSX-STA-012 clears (effect_size_kind: d on both reported tests) — the REQ-P28-01 threshold-and-one-step-either-side behaviour, recorded verbatim before any interpretation."
    - "The claim's literals 27 and 18 each coincide, within DSX-CLM-033's rel-5%/abs-5e-4 window (via the ×100 proportion bridge), with a reported number of test A (revenue_per_user) or test B (activation_rate), so DSX-CLM-033 clears on its full union-membership logic rather than by a skip — the collision that makes the case a live-miss seam (adjacency: two numbers that just touch merge into the union) (D-28-00)."
    - "DSX-CLM-033 extracts the claim's percent literals (bare integers are not extracted) and matches them against the union {effect, effect×100, CI bound, bound×100}; the measurement exercises this extraction-and-scale path when DSX-CLM-033 clears on 27/18 — the definition-of-a-number that applies is the existing _extract_claim_magnitudes percent/decimal rule (encoding/equality)."
    - "The collision holds under DSX-CLM-033's exact tolerance contract (relative ≤ 5% OR absolute ≤ 5e-4, whichever is looser), recorded in 28-MEASUREMENT.md, and the swap-still-fires counterfactual is applied by literal code (never narrative) to prove which residual codes are incidental (precision: the exact rounding/tolerance contract is named, not assumed)."
    - "28-MEASUREMENT.md opens with an explicit `VERDICT: LIVE MISS` or `VERDICT: CAUGHT` line, produced from a fresh temp directory mirroring tests/test_known_bad_corpus.py::_gate_findings, before any mint code is authored (D-13, measure-first)."
    - "On the LIVE MISS branch, DSX-CLM-034 stays silent when a claim literal agrees with a cited-test number to `rounding` significant figures (default 2) and fires at the finalised severity when it disagrees at that precision — the REQ-P28-02 threshold and one step either side, asserted by the RED unit test (boundary)."
    - "On the LIVE MISS branch, a claim number that exactly equals — or sits just inside the significant-figures boundary of — a cited-test number reconciles silently, and a number just outside fires; the just-touching case is asserted by paired unit tests (adjacency)."
    - "On the LIVE MISS branch, DSX-CLM-034 returns early and emits nothing when claims[].supported_by is absent (declaration-gated silence), which the corpus fixture proves by declaring none — the empty/absent boundary, attribution not detection (empty)."
    - "On the LIVE MISS branch, the comparator reuses DSX-CLM-033's ×100 percent/proportion scale bridge, so a claim quoted in percentage points reconciles with a cited-test effect declared as a proportion — one definition of equality across scales (encoding)."
    - "On the LIVE MISS branch, when claims[].supported_by names a single test the findings are compared as sets (order immaterial); the single-string case is the primary implementation per REQ-P28-02's singular text, and any tolerated list is an order-independent union (ordering)."
    - { statement: "On the LIVE MISS branch, the fresh significant-figures comparator declares its rounding/tie-break contract explicitly in the DSX-CLM-034 docstring (never looser than DSX-CLM-033's rel-5%/abs-5e-4 window) and a unit test exercises a value exactly at the N-significant-figure tie boundary", verification: backstop }
    - "On the LIVE MISS branch, the catalogue moves 277 → 278 in lockstep across references/finding-codes.md Total, _EXPECTED_TOTAL, and the Phase-20 pin, with exactly one DSX-CLM-034 row at the finalised severity, regenerated deterministically (byte-identical on a second --write) — the three count pins move together with no off-by-one (precision)."
    - "On the LIVE MISS branch, DSX-CLM-034 is the FIRST DSX-CLM code to carry a D-05 citation: its docstring has a `Citation:` line naming Wilkinson & the Task Force on Statistical Inference (1999) as the motivating principle and a `Structural criterion:` line describing the traceability corollary, and DSX-CLM-034 is named by exact code in _D05_ALLOWLIST_CODES so the generator's D-05 gate passes."
  artifacts:
    - .planning/phases/28-evidence-case-magnitude-no-test-computed/28-MEASUREMENT.md
    - dsx/checks/claims.py (new _check_supported_by_traceability function; DSX-CLM-034)
    - tests/test_claims_supported_by.py
    - references/finding-codes.md (DSX-CLM-034 row; Total 278)
  key_links:
    - "The new check is dispatched from the existing per-claim loop in dsx/checks/claims.py (:76-88), reading claim.get('supported_by') and claim.get('rounding', 2) for each claim, and resolving numbers from ONLY the named test(s) — not the all-tests union DSX-CLM-033 uses."
    - "DSX-CLM-034 is named by exact code in _D05_ALLOWLIST_CODES (scripts/gen-finding-catalogue.py) so the D-05 docstring gate enforces its Citation + Structural criterion lines and the linked `# D-05: DSX-CLM-034` test marker."
  prohibitions:
    - "Nothing is minted before Task 1 records `VERDICT: LIVE MISS` — no supported_by/rounding keys shipped, no DSX-CLM-034 authored, no catalogue move — until the four-point measurement confirms a live miss (guardrail 2)."
    - "The frozen D-28-01 case is not widened, weakened, or re-shaped to manufacture a miss; a `VERDICT: CAUGHT` is a valid no-mint terminal that satisfies the requirement (D-13; guardrail 1)."
    - "DSX-CLM-034's report.add is NOT placed inside the existing _check_numeric_overlap function; it is a brand-new function, so the D-05 citation obligation does not retrofit DSX-CLM-033's uncited docstring."
    - "DSX-CLM-034 is added to _D05_ALLOWLIST_CODES as an exact code, never as a `DSX-CLM-` prefix (a prefix add retroactively obligates the uncited legacy CLM codes and fails the build red)."
    - "The DSX-CLM-034 docstring and `# D-05:` marker do NOT claim Wilkinson mandates the overlap mechanism, and do NOT claim the check catches via metric-identity — it catches only via a stray claim number outside the cited test (bounded-catch honesty, D-28-05)."
    - "The significant-figures tolerance may only tighten, never loosen, relative to DSX-CLM-033's rel-5%/abs-5e-4 window (D-28-03)."
    - "The four-point measurement runs only from a fresh temp directory, never against the repo root or a fixture's own directory (no stray DECISIONS.jsonl at the repo root)."
    - "dsx/checks/dq.py is not modified (byte-frozen this milestone); the only dsx/ edit is dsx/checks/claims.py."
---

<objective>
Measure the §6.5 item 8 case live (D-13), then — and only then — mint DSX-CLM-034 if the measurement
is a live miss. This is the REQ-P28-01 measurement plus the conditional REQ-P28-02 mint. Task 1 IS
the D-13 act: it builds the FROZEN D-28-01 collision/mislabel fixture (a churn magnitude claim whose
27/18 literals are the real reported numbers of OTHER metrics, so DSX-CLM-033 clears on its full
union-membership logic while no test computed churn), runs the four-point gate protocol from a fresh
temp directory, applies the swap-still-fires counterfactual, and writes 28-MEASUREMENT.md with an
explicit VERDICT line first. Every task below Task 1 is CONDITIONAL on that recorded verdict.

Branch discipline (the single highest authoring risk for this phase, per 28-RESEARCH.md):
- `VERDICT: CAUGHT` (an existing code — predicted DSX-CLM-033 — fires on the merits of the target
  defect): STOP after the closure task. Mint nothing. Record which code caught it. Rewrite brief
  §6.5 item 8 with the CAUGHT evidence. REQ-P28-01/02/03 are all satisfied by this branch and Plan
  28-02 is NOT executed.
- `VERDICT: LIVE MISS` (all four points pass on the merits after subtracting documented incidental
  codes): proceed through the RED/GREEN mint tasks, then hand off to Plan 28-02 for fixture
  promotion.

Purpose: satisfy the measure-first rule and, on a live miss, close the miss with a cited
declaration-level traceability check so the mislabelled magnitude is at least attributable.
Output: 28-MEASUREMENT.md with a VERDICT; on a live miss, _check_supported_by_traceability +
DSX-CLM-034 in claims.py, the two additive claims-schema keys, a new unit test carrying the
`# D-05: DSX-CLM-034` marker, the regenerated catalogue at 278, and the two catalogue count pins moved.
</objective>

<assumption_delta_decision>
The additive claims[].supported_by field admits a singular→plural transition (a single cited-test
metric name, or a list of them). Advisory, non-blocking.

- Primary noun: the single cited test a claim rests on.
- Decision: no-change (single-string is the primary implementation, matching REQ-P28-02's singular
  wording "the results.tests[] entry a claim rests on"). List-tolerance is add-alongside accepted
  debt: it can be added additively later without a new finding code, and only a real multi-test
  claim in the corpus would force promoting the list form to primary.
- Rationale: shipping the singular case first satisfies the binding requirement text; the list form
  is an order-independent union with no identity consequence, so deferring it costs nothing now.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Frozen design + plan-input research (do NOT re-open D-28-00..05; guardrail 1)
@.planning/phases/28-evidence-case-magnitude-no-test-computed/28-CONTEXT.md
@.planning/phases/28-evidence-case-magnitude-no-test-computed/28-RESEARCH.md

# The measurement protocol + the file being modified + the mould/citation idioms to mirror
@tests/test_known_bad_corpus.py
@dsx/checks/claims.py
@dsx/checks/repro.py
@scripts/gen-finding-catalogue.py
@references/finding-codes.md
</context>

<artifacts>
## Artifacts this phase produces (this plan's share)

- `.planning/phases/28-evidence-case-magnitude-no-test-computed/28-MEASUREMENT.md` — the four-point
  measured gate table + swap-still-fires counterfactual + an explicit `VERDICT:` line. Produced
  ALWAYS (both branches).
- `.planning/phases/28-evidence-case-magnitude-no-test-computed/spike/` — the throwaway measurement
  fixture (spec + minimal static entrypoint), NOT under examples/known-bad/ until Plan 28-02
  promotes it. Produced ALWAYS.
- `_check_supported_by_traceability` — new function in `dsx/checks/claims.py`, called from the
  per-claim dispatch loop. LIVE MISS branch only.
- `DSX-CLM-034` — the minted finding code (a per-cited-test, declaration-gated numeric-traceability
  check; the first DSX-CLM code to carry a D-05 citation). LIVE MISS branch only.
- `claims[].supported_by` (string; list tolerated) + `claims[].rounding` (int significant figures,
  default 2) — two additive commented-optional keys in `templates/ANALYSIS-SPEC.yaml:299-308`. NO
  `dsx/spec.py` change: `_validate_claims_shape` (:1285-1319) does not reject unknown claim keys.
  LIVE MISS branch only.
- `tests/test_claims_supported_by.py` — new unit test carrying `# D-05: DSX-CLM-034`. LIVE MISS
  branch only.
- The `DSX-CLM-034` row in `references/finding-codes.md` (catalogue total 277 → 278). LIVE MISS
  branch only.

(Plan 28-02 promotes the fixture into examples/known-bad/, wires the four corpus harness maps, moves
the spec count 43→44, and rewrites brief §6.5 item 8 — all on the LIVE MISS branch only.)
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1 (MEASURE — the D-13 act): build the frozen collision fixture, run the four-point protocol, write 28-MEASUREMENT.md with a VERDICT</name>
  <files>.planning/phases/28-evidence-case-magnitude-no-test-computed/spike/magnitude-no-test-computed-ANALYSIS-SPEC.yaml, .planning/phases/28-evidence-case-magnitude-no-test-computed/spike/magnitude-no-test-computed-entrypoint.py, .planning/phases/28-evidence-case-magnitude-no-test-computed/28-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/28-evidence-case-magnitude-no-test-computed/28-CONTEXT.md (D-28-00 collision construction; D-28-01 exact fixture shape — claim text, two tests A/B, no supported_by; D-28-02 pass/fail rule + existing-checks-clear table)
    - tests/test_known_bad_corpus.py (`_gate_findings` :996-1052 — fresh-tempdir discipline; `_seed_entrypoint` runs before every gate point :1043; `seed_plan_header` for verify/ship :1044-1045)
    - dsx/cli.py (`GATE_PROFILES`/`GATE_THRESHOLDS` :100-149 — `claims` registers at verify/ship only, at lines 122 and 127; thresholds HIGH at :138-139; corrected from 28-CONTEXT's :123,128 per 28-RESEARCH Discrepancy 2)
    - dsx/checks/claims.py (`_check_numeric_overlap`/DSX-CLM-033 :343-401; `_extract_claim_magnitudes` :404-436; `_close_enough` :439-443; ×100 bridge :360-368)
    - .planning/phases/27-evidence-case-feature-origin-only-leak/27-MEASUREMENT.md (the precedent four-point measured table + swap-invariance record shape)
    - dsx/pct_base.py (`claim_supplies_base` at :50, not :56 per 28-RESEARCH Discrepancy 1 — DSX-CLM-070 clears when base_n declared)
  </read_first>
  <action>
    Author the FROZEN D-28-01 fixture under the phase spike/ path (NOT under examples/known-bad/ —
    it stays a throwaway until the verdict is known, mirroring Phase 27's spike/ convention). The
    ANALYSIS-SPEC.yaml is a descriptive/observational churn fixture with exactly two results.tests[]
    entries on OTHER metrics — test A metric revenue_per_user and test B metric activation_rate —
    each fully reporting effect, a two-bound ci, p_value, effect_size_kind d, a non-negligible
    standardized_effect, and a non-null interpretation. Add one association-typed claim on churn
    (the metric NO test computes) reading exactly "Customers holding fewer than two products churn
    at 27% against 18% for the rest of the book, over the 2026-Q2 window." Construct the two tests'
    reported numbers so the claim's 27 and 18 each coincide, within DSX-CLM-033's rel-5%/abs-5e-4
    window via the ×100 proportion bridge, with a reported number of test A or test B (e.g. an
    effect declared as a proportion whose ×100 is 27, and another whose ×100 is 18) — this is the
    collision that clears DSX-CLM-033 on its full union-membership logic. Declare base_n on the
    claim; write the narrative body so it carries base language (a phrase like "of 5,000 customers"
    or "n=5,000") for DSX-NAR-040; put the claim text verbatim in the narrative path for DSX-NAR-020;
    state a population and avoid GENERALISATION_TERMS for DSX-CLM-050; declare a one-line limitations
    entry for DSX-CLM-080; point evidence at the committed narrative for DSX-CLM-030/031. Declare NO
    claims[].supported_by anywhere in the fixture (its absence keeps the case a live-miss seam and,
    later, a MISS). Author the minimal static entrypoint referenced by reproducibility.entrypoint —
    a plausible descriptive-stats script that is read as text and never executed (per 28-RESEARCH A1,
    the entrypoint must exist and be resolvable so DSX-REP-030/031 clear; no CSV is needed, the path
    string only needs to exist). Then run the four-point protocol (validate/plan/execute/verify/ship
    as the harness registers them) from a FRESH tempfile.TemporaryDirectory mirroring
    tests/test_known_bad_corpus.py::_gate_findings — copy its fresh-tempdir discipline exactly, never
    running dsx gate against the repo root. Record every measured code verbatim at each point BEFORE
    interpreting. Apply the swap-still-fires counterfactual by literal code: re-run a variant in which
    the claim's collision numbers are replaced by numbers that genuinely reconcile against a cited
    test, and diff the CRITICAL/HIGH residual sets; a code that stops firing under the swap IS a catch.
    Write 28-MEASUREMENT.md with the verbatim four-point table, the swap diff, and — as its FIRST
    content line — either `VERDICT: LIVE MISS` or `VERDICT: CAUGHT`, naming the catching code(s) if
    CAUGHT. Use the real interpreter at C:/Users/Benutzer1/AppData/Local/Programs/Python/Python312/python.exe
    (invoked as `python312`); never the python3 3.14 stub. This task mints nothing.
  </action>
  <acceptance_criteria>
    - The spike spec and entrypoint exist under .planning/phases/28-evidence-case-magnitude-no-test-computed/spike/ and are NOT under examples/known-bad/.
    - The spike spec declares exactly two results.tests[] entries (metrics revenue_per_user and activation_rate), each with effect, a two-bound ci, effect_size_kind, standardized_effect, and interpretation; and one association-typed claim whose text contains both `27` and `18` and declares base_n.
    - 28-MEASUREMENT.md exists and its FIRST content line matches `VERDICT: LIVE MISS` or `VERDICT: CAUGHT`.
    - 28-MEASUREMENT.md records a four-point gate table (validate/plan/execute/verify/ship) with exit codes and the verbatim CRITICAL/HIGH codes at each point, and a swap-still-fires diff.
    - The measurement was run from a fresh temp directory (the record states this and the repo root has no new DECISIONS.jsonl: `git status --porcelain DECISIONS.jsonl` prints nothing).
    - If VERDICT is CAUGHT: the catching code is named (predicted DSX-CLM-033) and shown to stop firing under the swap (proving it is a catch, not incidental).
  </acceptance_criteria>
  <verify>
    <automated>grep -Eq "VERDICT: *(LIVE MISS|CAUGHT)" .planning/phases/28-evidence-case-magnitude-no-test-computed/28-MEASUREMENT.md && echo VERDICT-RECORDED</automated>
  </verify>
  <done>28-MEASUREMENT.md records the four-point measured verdict from a fresh tempdir with an explicit VERDICT line first, and the swap counterfactual is applied by literal code. If VERDICT is CAUGHT, stop here and perform only Task 4 branch B (do NOT run Task 2 or Task 3). If VERDICT is LIVE MISS, proceed to Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (RED — CONDITIONAL, LIVE MISS only): failing unit test for the traceability check + D-05 marker</name>
  <files>tests/test_claims_supported_by.py</files>
  <read_first>
    - 28-MEASUREMENT.md (this task runs ONLY if its first line is `VERDICT: LIVE MISS`)
    - dsx/checks/claims.py (per-claim loop :76-88; `tests` resolution :73-74; `_check_numeric_overlap` emit idiom :343-401; `_extract_claim_magnitudes` :404-436; ×100 bridge :360-368)
    - .planning/phases/27-evidence-case-feature-origin-only-leak/27-01-PLAN.md (Task 1 RED shape — behaviour bullets, marker placement, honesty phrase)
    - .planning/phases/28-evidence-case-magnitude-no-test-computed/28-CONTEXT.md (D-28-03 rounding rule; D-28-05 Wilkinson-as-principle + bounded-catch honesty)
  </read_first>
  <behavior>
    - fires-on-uncovered: a claim declaring supported_by naming a test whose reported numbers do NOT cover a claim literal (beyond `rounding` significant figures) makes check(spec) emit DSX-CLM-034 at the finalised severity.
    - silent-when-covered: a claim whose every literal agrees with a cited-test number to `rounding` significant figures emits no DSX-CLM-034 finding.
    - silent-when-absent: a claim with no supported_by key emits no DSX-CLM-034 finding (the declaration-gated empty/absent boundary).
    - scale-bridge: a claim literal quoted in percentage points reconciles with a cited-test effect declared as a proportion (×100 bridge) and emits nothing.
    - tie-boundary: a claim literal that agrees to exactly `rounding` significant figures is silent, and one that disagrees at the last significant figure fires — the precision tie-break contract exercised at the boundary.
    - rounding-default: when claims[].rounding is absent, the comparator uses 2 significant figures.
    - rounding-may-tighten: a smaller claims[].rounding (e.g. 3) makes a borderline literal that passed at 2 significant figures now fire (tighten, never loosen).
  </behavior>
  <action>
    Author tests/test_claims_supported_by.py as a stdlib unittest.TestCase importing `check` from
    dsx.checks.claims and building minimal in-memory spec dicts (a results.tests list with one or two
    fully-reported tests plus a claims list) to drive each behaviour bullet. Assert on the presence,
    absence, and severity of DSX-CLM-034 in the returned Report's findings, mirroring how existing
    claims tests read findings by code. Include one test method per behaviour bullet. Add the line
    `# D-05: DSX-CLM-034` as a standalone comment, and beside it a comment stating the citation is
    Wilkinson & the Task Force on Statistical Inference (1999) as the MOTIVATING PRINCIPLE and that
    the check enforces a declaration-level traceability corollary, not the literal report-an-effect
    rule (per D-28-05 — do NOT phrase it as Wilkinson mandating the overlap mechanic). The tests
    reference DSX-CLM-034 behaviour that does not exist yet, so the module fails its assertions — the
    intended RED state before Task 3. Do NOT stub or weaken any assertion to make it pass early.
  </action>
  <acceptance_criteria>
    - tests/test_claims_supported_by.py exists and contains the standalone comment line `# D-05: DSX-CLM-034`.
    - The file names `Wilkinson` and the phrase `motivating principle` (or `principle that motivates`) in a comment, and does not assert Wilkinson mandates a numeric-overlap mechanism.
    - Running `python312 -m unittest tests.test_claims_supported_by` exits NON-ZERO (RED) because DSX-CLM-034 is not yet emitted.
    - The file contains at least seven test methods, one per behaviour bullet (fires-on-uncovered, silent-when-covered, silent-when-absent, scale-bridge, tie-boundary, rounding-default, rounding-may-tighten).
  </acceptance_criteria>
  <verify>
    <automated>python312 -m unittest tests.test_claims_supported_by 2>&1 | grep -Eq "FAILED|Error|Traceback" && echo RED-CONFIRMED</automated>
  </verify>
  <done>The unit test exists, carries the D-05 marker with the Wilkinson-as-principle honesty phrase, and fails (RED) because the check is not yet implemented. Skip this task entirely if Task 1 recorded VERDICT: CAUGHT.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN — CONDITIONAL, LIVE MISS only): implement the traceability check, add the schema keys + D-05 docstring, allowlist and regenerate the catalogue, move the two count pins</name>
  <files>dsx/checks/claims.py, templates/ANALYSIS-SPEC.yaml, scripts/gen-finding-catalogue.py, references/finding-codes.md, tests/test_finding_catalogue_invariant.py, tests/test_phase20_zero_mint_close.py</files>
  <read_first>
    - dsx/checks/claims.py (per-claim loop :76-88 — add the dispatch call; `_check_numeric_overlap` :343-401 — mirror the read/emit idiom and the ×100 bridge :360-368, but write a SEPARATE function; `_extract_claim_magnitudes` :404-436 — the claim-literal extractor to reuse)
    - dsx/checks/repro.py (`_check_reproduce_report` :285-388, fire site :372-386 — the declaration-gated, never-recompute mould; note its tolerance is math.isclose(rel_tol=1e-2), which the sig-figs comparator must NOT copy — write sig-figs fresh)
    - scripts/gen-finding-catalogue.py (`_D05_ALLOWLIST_CODES` :183-211, DSX-ML-034 exact-code precedent at :209; `_TEST_MARKER_RE` :217; `_CITATION_RE`/`_REFVALUE_RE` :213-216; `_resolve_docstrings` :336-375; `check_d05` :393-423; literal-severity requirement)
    - references/finding-codes.md (Total line :16 `**Total: 277 codes.**`; the DSX-CLM 03x family rows — 030/031/032/033, 034 slot free)
    - tests/test_finding_catalogue_invariant.py (`_EXPECTED_TOTAL = 277` :40)
    - tests/test_phase20_zero_mint_close.py (`_declared_total(_CATALOGUE), 277` :100 and the cosmetic 277 literals :95,101)
    - .planning/phases/28-evidence-case-magnitude-no-test-computed/28-CONTEXT.md (D-28-05 exact docstring obligations); 28-RESEARCH.md §5 (D-05 placement risk)
  </read_first>
  <behavior>
    - Implements every behaviour bullet from Task 2; running Task 2's unit test now passes (GREEN).
    - Existing specs (no supported_by) remain unaffected — the check returns early.
  </behavior>
  <action>
    Add a brand-new function (name it `_check_supported_by_traceability(claim, text, tests, where,
    report)` or similar) to dsx/checks/claims.py and dispatch it from the existing per-claim loop
    alongside the other per-claim checks. The function reads claim.get("supported_by"); when that is
    absent or empty it returns immediately (declaration-gated, so every existing spec still passes).
    It reads claim.get("rounding", 2), resolves the union of reported numbers from ONLY the named
    test(s) (NOT the all-tests union DSX-CLM-033 uses — this per-cited-test scoping is the whole
    distinction), extracts the claim's literals with the existing _extract_claim_magnitudes idiom,
    and compares each literal against the cited test's numbers using a FRESH significant-figures
    comparator plus DSX-CLM-033's ×100 scale bridge. Any literal not covered to `rounding`
    significant figures emits `report.add("DSX-CLM-034", <severity>, ...)` with the severity as a
    STRING LITERAL (the catalogue extractor skips non-literal severities) and the offending number
    named in the detail. Finalise the severity per REQ-P28-02's text and the persona round's HIGH
    lean (28-CONTEXT.md severity note); the falsifiability guarantee holds at HIGH (28-RESEARCH §4).
    Do NOT place this report.add inside _check_numeric_overlap. Write the function docstring to satisfy
    the D-05 gate: a `Citation:` line naming Wilkinson, L. & the Task Force on Statistical Inference
    (1999), American Psychologist 54(8):594-604, stated as the MOTIVATING PRINCIPLE (it mandates
    reporting effect sizes and intervals for primary outcomes); a `Structural criterion:` line
    describing the declaration-level traceability corollary (a claim's literals must trace to the
    specific cited test, a text-to-declared-number overlap, never a recomputation — the DSX-REP-061
    mould); and a plain statement of the bounded-catch honesty (the check catches only via a stray
    claim number outside the cited test, not via metric-identity). Name the rounding/tie-break
    contract of the comparator explicitly in the docstring, and hold it never looser than DSX-CLM-033's
    window. Add claims[].supported_by and claims[].rounding as two commented-optional keys after the
    `# to_value: null` line in templates/ANALYSIS-SPEC.yaml:299-308 (no dsx/spec.py change needed).
    Add the exact code string DSX-CLM-034 to _D05_ALLOWLIST_CODES in scripts/gen-finding-catalogue.py
    (exact code, following the DSX-ML-034 precedent comment at :209 — never a DSX-CLM- prefix).
    Regenerate the catalogue via the generator's --write mode so references/finding-codes.md gains the
    DSX-CLM-034 row between DSX-CLM-033 and the next code and the Total line moves to 278 — never
    hand-edit the catalogue. Then move the two catalogue count pins in lockstep: set _EXPECTED_TOTAL
    to 278 (tests/test_finding_catalogue_invariant.py:40) with its cosmetic method-name/message
    literals updated for honesty, and set _declared_total(_CATALOGUE) to 278
    (tests/test_phase20_zero_mint_close.py:100) with its cosmetic 277 literals updated. Do NOT add
    DSX-CLM-034 to any backlog set; do NOT alter the Phase-12 256 snapshot pin. Do NOT modify
    dsx/checks/dq.py.
  </action>
  <acceptance_criteria>
    - `python312 -m unittest tests.test_claims_supported_by` exits 0 (GREEN — every behaviour bullet passes).
    - The per-claim loop in dsx/checks/claims.py dispatches the new function, and the new function returns early when claim.get("supported_by") is falsy (grep confirms an early return guarded on that key inside a function distinct from _check_numeric_overlap).
    - The DSX-CLM-034 report.add is NOT inside _check_numeric_overlap (the function emitting DSX-CLM-033); it is in a separate function with its own docstring.
    - The new function's docstring contains a `Citation:` line naming `Wilkinson`, a `Structural criterion:` line, and states the check catches via a stray number, not metric-identity.
    - `python312 scripts/gen-finding-catalogue.py --check` exits 0 (catalogue current; the D-05 gate resolves Citation + Structural criterion + the `# D-05: DSX-CLM-034` marker).
    - references/finding-codes.md contains exactly one line matching the DSX-CLM-034 code cell and its Total line reads `**Total: 278 codes.**`; regenerating a second time leaves `git diff --stat -- references/finding-codes.md` empty (deterministic).
    - `python312 -m unittest tests.test_finding_catalogue_invariant tests.test_phase20_zero_mint_close -q` reports OK with both pins at 278.
    - `git diff --stat -- dsx/checks/dq.py` prints nothing (byte-frozen).
    - Full suite green: `python312 -m unittest discover -s tests -q` reports OK (the check is silent for all existing specs).
  </acceptance_criteria>
  <verify>
    <automated>python312 -m unittest tests.test_claims_supported_by -q && python312 scripts/gen-finding-catalogue.py --check && python312 -m unittest tests.test_finding_catalogue_invariant tests.test_phase20_zero_mint_close -q && python312 -m unittest discover -s tests -q</automated>
  </verify>
  <done>DSX-CLM-034 fires at the finalised severity on an uncovered claim literal, is silent when supported_by is absent, carries an honest Wilkinson D-05 docstring, the catalogue is deterministically at 278 with both count pins moved, and the full suite is green; dq.py untouched. Skip this task entirely if Task 1 recorded VERDICT: CAUGHT.</done>
</task>

<task type="auto">
  <name>Task 4 (CLOSURE — branches on Task 1's verdict)</name>
  <files>brief.md</files>
  <read_first>
    - 28-MEASUREMENT.md (the recorded VERDICT decides which branch runs)
    - brief.md (§6.5 item 8 row at ~:378 — the "magnitude residual" row named by the comment at tests/test_known_bad_corpus.py:853)
    - .planning/phases/27-evidence-case-feature-origin-only-leak/27-02-PLAN.md (Task 3 brief-rewrite shape for the LIVE MISS handoff wording)
  </read_first>
  <action>
    Read the VERDICT line from 28-MEASUREMENT.md and branch:

    Branch A (VERDICT: CAUGHT): the case is caught by an existing code (predicted DSX-CLM-033). Write
    the no-mint record into 28-MEASUREMENT.md (which code caught it, confirmed against the swap
    counterfactual) and rewrite the brief.md §6.5 item 8 row so its entry-condition column states the
    measured CAUGHT verdict and names the catching code — the case is caught, nothing minted, which
    satisfies REQ-P28-01/02/03 (D-13). Tasks 2 and 3 were correctly skipped and Plan 28-02 is NOT
    executed. Do not edit REQUIREMENTS.md, STATE.md, or ROADMAP.md (single-writer — the orchestrator
    owns them).

    Branch B (VERDICT: LIVE MISS): this is a verification-only step. Do NOT rewrite the brief here —
    that belongs to Plan 28-02 (which promotes the fixture and states the LIVE MISS evidence with
    DSX-CLM-034). Confirm Tasks 2 and 3 landed cleanly: the full suite is green, the catalogue is at
    278, and dq.py is byte-frozen. Record in 28-MEASUREMENT.md that the phase proceeds to Plan 28-02
    for fixture promotion. Leave brief.md unchanged in this branch.
  </action>
  <acceptance_criteria>
    - Branch A only: brief.md §6.5 item 8 row contains the measured verdict wording (`CAUGHT` or `caught`) and names the catching code; 28-MEASUREMENT.md carries the no-mint record.
    - Branch B only: brief.md is unchanged by this plan; `python312 -m unittest discover -s tests -q` reports OK and `python312 scripts/gen-finding-catalogue.py --check` exits 0; 28-MEASUREMENT.md states the phase proceeds to Plan 28-02.
    - Neither branch edits REQUIREMENTS.md, STATE.md, or ROADMAP.md (`git status --porcelain .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md` prints nothing).
  </acceptance_criteria>
  <verify>
    <automated>python312 -m unittest discover -s tests -q && python312 scripts/gen-finding-catalogue.py --check</automated>
  </verify>
  <done>The correct branch ran off Task 1's recorded verdict: on CAUGHT, the brief records the catch and the phase closes with no mint; on LIVE MISS, the suite and catalogue are green and the phase hands off to Plan 28-02. No single-writer tracking file was edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured verdict → mint decision | Task 1's recorded VERDICT is the sole gate on whether any code is minted; an ambiguous conditional read as "mint by default" would mint on a CAUGHT verdict and violate D-13. |
| minted code → finding catalogue | A new report.add call-site becomes a catalogue row via AST extraction; a non-deterministic or over-claiming emission corrupts the committed catalogue. |
| docstring / test marker → D-05 honesty record | The Citation text is the durable provenance claim; over-claiming that Wilkinson mandates the overlap mechanic launders a motivating principle into an asserted mechanism. |
| untrusted spec content → static check | claim.get("supported_by")/claim.get("rounding") are read off a parsed YAML dict; the entrypoint is read as text and never executed (V5 input validation, standing gate-path rule). |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-05 | Tampering (of design intent) / Repudiation (of D-13) | plan conditional executed as "mint by default" on a CAUGHT verdict | high | mitigate | Task 1's `<done>` states the branch explicitly; every mint task (2/3) is skip-on-CAUGHT and Task 4 branches on the recorded VERDICT. The measurement is the literal first act. |
| T-28-01 | Repudiation | DSX-CLM-034 docstring + `# D-05:` marker | high | mitigate | Both MUST cite Wilkinson & TFSI (1999) as the motivating principle only, state the traceability corollary as the mechanism, and record the stray-number bounded-catch honesty; presence-grepped in Task 2/Task 3 acceptance (D-28-05). |
| T-28-02 | Tampering | catalogue count pins (277→278) | high | mitigate | Move references/finding-codes.md Total, _EXPECTED_TOTAL, and the Phase-20 pin together; the invariant + Phase-20 tests fail on drift (Task 3). |
| T-28-03 | Tampering | D-05 docstring placement | high | mitigate | Write DSX-CLM-034 as a brand-new function so its Citation obligation does not retrofit the uncited _check_numeric_overlap docstring shared with DSX-CLM-033 (Task 3 acceptance greps for the separate function). |
| T-28-04 | Tampering | _D05_ALLOWLIST_CODES entry | high | mitigate | Add DSX-CLM-034 as an exact code, never a `DSX-CLM-` prefix (a prefix add obligates the uncited legacy CLM family and fails the build red). |
| T-28-07 | Tampering | significant-figures tolerance loosening | medium | mitigate | The comparator is never looser than DSX-CLM-033's rel-5%/abs-5e-4 window; the tie-boundary and rounding-may-tighten unit tests assert the direction (Task 2/Task 3). |
| T-28-06 | Tampering | dsx/checks/dq.py byte-freeze | medium | mitigate | Only dsx/checks/claims.py is edited; `git diff --stat -- dsx/checks/dq.py` must be empty (Task 3 acceptance). |
| T-28-SC | Supply chain | package installs | low | accept | This plan installs no npm/pip/cargo packages. The spike entrypoint is read as text by the code scan and never executed; no Package Legitimacy Gate applies. |
</threat_model>

<verification>
- `python312 -c "... VERDICT ..."` — 28-MEASUREMENT.md carries an explicit VERDICT line (Task 1).
- LIVE MISS branch: `python312 -m unittest tests.test_claims_supported_by -q` GREEN after Task 3.
- LIVE MISS branch: `python312 scripts/gen-finding-catalogue.py --check` exit 0 — catalogue current + D-05 gate green at 278.
- LIVE MISS branch: `python312 -m unittest tests.test_finding_catalogue_invariant tests.test_phase20_zero_mint_close -q` — both pins at 278.
- Both branches: `python312 -m unittest discover -s tests -q` — full suite OK.
- Both branches: `git diff --stat -- dsx/checks/dq.py` empty — byte-freeze held.
</verification>

<success_criteria>
- The case is measured live FIRST, from a fresh tempdir, with an explicit VERDICT recorded before any mint code (REQ-P28-01; D-13).
- On CAUGHT: nothing is minted, the catching code is recorded, brief §6.5 item 8 states the measured catch, and Plan 28-02 does not run — a valid no-mint close (REQ-P28-01/02/03).
- On LIVE MISS: DSX-CLM-034 fires at the finalised severity on an uncovered claim literal and is silent when supported_by is absent; the catalogue is deterministically at 278 with both count pins moved; the D-05 docstring cites Wilkinson honestly (motivating principle, not mechanism; stray-number bounded catch) and passes the generator's D-05 gate (REQ-P28-02).
- dsx/checks/dq.py unchanged.
</success_criteria>

<output>
Create `.planning/phases/28-evidence-case-magnitude-no-test-computed/28-01-SUMMARY.md` when done.
</output>
