---
phase: 22-catalog-spine-uncertainty-heuristic
plan: 04
type: execute
wave: 4
depends_on:
  - 22-01
  - 22-02
  - 22-03
files_modified:
  - references/chart-selection.md
  - references/question-taxonomy.md
  - skills/dsx-visualize/SKILL.md
  - tests/test_selection_heuristic_docs.py
autonomous: true
requirements:
  - REQ-P22-04

must_haves:
  truths:
    - "references/chart-selection.md's 'Encoding accuracy' section states the D-1 six-rank-with-ties ordering (rank3 {length, direction/slope, angle} tied, rank5 {volume, curvature}, rank6 {shading, colour_saturation}), and the superseded pre-HQ-27 strict arrow chain (with length above angle, colour saturation before volume, and 'density') is gone (Pitfall 3, a correction not a leave-alone)."
    - "The 5-layer question→chart heuristic ships as route-and-cite edits threaded into the two existing reference files (question-taxonomy.md for the L1 question→task layer; chart-selection.md for L2-L5), pointing to finding codes and references/chart-catalog.md — no new parallel decision-tree file is created (REQ-P22-04)."
    - "chart-selection.md gains an uncertainty relationship row routing to the eleven-key vocabulary and referencing the ten §5.6 marks; skills/dsx-visualize/SKILL.md's <method> step-1 relationship enumeration lists eleven names (adds 'uncertainty') and its <references> block points at references/chart-catalog.md."
    - "The heuristic cites only HQ-27-signed sources — Munzner ch.3 (task taxonomy) for the L1 question→task layer and the FT nine-category axis for L2 — and drops Abela 2008 and Few's Graph Selection Matrix entirely (Research Open Question 1, option (a))."
  artifacts:
    - "references/chart-selection.md (corrected perceptual line + uncertainty row + L2-L5 route-and-cite + chart-catalog.md pointer)"
    - "references/question-taxonomy.md (L1 question→task layer pointer, route-and-cite)"
    - "skills/dsx-visualize/SKILL.md (relationship enumeration 10→11; chart-catalog.md reference added)"
    - "tests/test_selection_heuristic_docs.py (new repo-integrity: eleven relationship names present in SKILL.md, the superseded perceptual chain absent, the D-1 rank tokens present, the chart-catalog.md pointer resolves, no new decision-tree file)"
  key_links:
    - "SKILL.md <method> step 1 enumerates all relationship names by hand (a ripple point CONTEXT.md does not list); adding the 'uncertainty' key in Plan 22-01 obliges an eleventh name here or the skill and the vocabulary disagree."
    - "chart-selection.md's 'Encoding accuracy' line is the pre-HQ-27 strict ordering (Pitfall 3); REQ-P22-04's edit MUST rewrite it to D-1's six-rank-with-ties form — leaving it produces a repo that contradicts its own new tie-break test."
    - "The heuristic points at references/chart-catalog.md (Plan 22-03) — so this plan runs after the catalog exists, and the doc-conformance test asserts the pointer resolves."
  prohibitions:
    - "[unverified — never auto-dismiss] No citation drawn from outside the signed HQ-27 pack: drop Abela 2008 and Few's Graph Selection Matrix (never submitted to HQ-27); cite only Munzner ch.3 and the FT nine-category axis for the heuristic layers."
    - "[unverified — never auto-dismiss] No claim of independent triangulation across FT / Graphic Continuum / Data Visualisation Catalogue in the heuristic prose — one Ribecca lineage."
    - "[unverified — never auto-dismiss] The superseded perceptual ordering is corrected, not left alone: no strict total order, no 'length above angle', no 'density', no arrow chain placing colour saturation before volume (D-1)."
    - "[unverified — never auto-dismiss] No parallel decision tree: the 5-layer heuristic is route-and-cite edits into the two existing files plus skill pointers, never a new standalone decision-tree document (REQ-P22-04)."
---

<objective>
Ship the 5-layer question→chart selection heuristic as route-and-cite edits into the two
existing reference files and the visualize skill — never a parallel decision tree — and, in
the same pass, correct the one pre-HQ-27 error still living in chart-selection.md: its
"Encoding accuracy" line ships the superseded 7-item strict perceptual ordering that D-1
corrected, and must be rewritten to the six-rank-with-ties form.

This edits three reference/skill files and adds one small repo-integrity test. It implements
REQ-P22-04 (the 5-layer heuristic as edits to question-taxonomy.md / chart-selection.md plus
skill pointers, route-and-cite, no parallel tree) and folds in the REQ-P22-05-adjacent
Pitfall-3 correction (the perceptual line). The skill's relationship enumeration is a ripple
point CONTEXT.md does not name (Research): step 1 lists every relationship by hand, so the
eleventh key from Plan 22-01 must appear there or the skill and the live vocabulary disagree.

Purpose: the heuristic is the agent- and human-facing front door to the catalog and the
gate. If it still teaches the wrong perceptual order while the new tie-break test asserts the
right one, the repo contradicts itself. Threading the five layers into the existing files
(rather than a new tree) keeps one selection surface, not two that can drift.

Output: a corrected perceptual line, an uncertainty row and the L2-L5 heuristic route-and-cite
in chart-selection.md, the L1 layer pointer in question-taxonomy.md, an eleven-relationship
skill with a catalog pointer, and a doc-conformance test.
</objective>

<artifacts_this_phase_produces>
This plan's share of the phase artifact index (full index in 22-01-PLAN.md):
- `references/chart-selection.md` — corrected perceptual line + uncertainty row + L2-L5 route-and-cite + chart-catalog.md pointer
- `references/question-taxonomy.md` — L1 question→task layer pointer
- `skills/dsx-visualize/SKILL.md` — relationship enumeration 10→11; chart-catalog.md reference
- `tests/test_selection_heuristic_docs.py` — new doc-conformance repo-integrity test
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-catalog-spine-uncertainty-heuristic/22-CONTEXT.md
@.planning/phases/22-catalog-spine-uncertainty-heuristic/22-RESEARCH.md
@.planning/v2.4-D05-EVIDENCE-PACK.md

# The three files this plan edits (read them in full — they are short):
@references/chart-selection.md
@references/question-taxonomy.md
@skills/dsx-visualize/SKILL.md

# The catalog this heuristic points at (created in Plan 22-03):
@references/chart-catalog.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the perceptual line and thread L2-L5 route-and-cite into chart-selection.md</name>
  <files>references/chart-selection.md</files>
  <read_first>
    - references/chart-selection.md (full, 63 lines) — the "Encoding accuracy" section (the pre-HQ-27 arrow chain Pitfall 3 names), the relationship table, the "Non-negotiables" and "The title" sections.
    - .planning/v2.4-D05-EVIDENCE-PACK.md D-1 (T1-1: the six-rank-with-ties shipping form and the p.536/p.537 locators; density absent) and D-3 (FT nine categories) and T2-6 (Munzner ch.3 task taxonomy).
    - 22-CONTEXT.md item 6 of the bound list (5-layer heuristic route-and-cite, no parallel tree) and 22-RESEARCH.md Pitfall 3 + Open Question 1 (drop Abela 2008 / Few's Graph Selection Matrix; cite Munzner ch.3 + FT).
  </read_first>
  <action>
    Rewrite the "Encoding accuracy" section of references/chart-selection.md so it states the Cleveland–McGill ordering as six ranks WITH TIES, not a strict single-arrow chain: rank 1 position on a common scale; rank 2 position on nonaligned scales; rank 3 the tied group of length, direction/slope, and angle; rank 4 area; rank 5 the tied group of volume and curvature; rank 6 the tied group of shading and colour saturation. State explicitly that ranks 3, 5 and 6 each hold more than one channel and the 1984 paper does not separate the ties (cite Cleveland–McGill 1984, p.536 for the list and p.537 for the tie caveat), and that the density channel does not appear in that paper. Remove the prior strict ordering entirely — do not leave the old arrow chain in place beneath the correction (Pitfall 3: a repo that ships both is self-contradictory). Then add a "Selection heuristic" section threading layers L2-L5 as route-and-cite pointers, not a new decision tree: L2 = pick the relationship (the nine FT function categories, attributed to the FT, plus the eleventh uncertainty function) → point to references/chart-catalog.md and the relationship table already in this file; L3 = pick the mark admissible for both the relationship and the data signature → point to references/chart-catalog.md's three axes and the DSX-VIZ-012/013 codes; L4 = check the encoding is in the most accurate available channel → point to the corrected rank list above; L5 = show uncertainty and route uncertainty-mark choice to the ten §5.6 members and DSX-VIZ-070/071. Add a new relationship row to the selection table for "uncertainty" (default = error bars; also works = the other §5.6 marks; avoid = a point estimate with no interval), attributing Wilke's Uncertainty category. Cite only Munzner ch.3 (task taxonomy) and the FT nine-category axis for the heuristic; do NOT cite Abela 2008 or Few's Graph Selection Matrix (never HQ-27-submitted). Add a one-line pointer to references/chart-catalog.md near the top. Do not restate any numeric gate threshold — route to the finding code instead.
  </action>
  <verify>
    <automated>python -c "import pathlib; t=pathlib.Path('references/chart-selection.md').read_text(encoding='utf-8'); low=t.lower(); assert 'saturation → volume' not in t and 'saturation -> volume' not in low, 'superseded ordering still present'; assert 'chart-catalog.md' in t; assert 'uncertainty' in low; assert 'tie' in low or 'tied' in low; print('chart-selection corrected')"</automated>
    Expected: the superseded arrow chain that places colour saturation before volume is absent; a chart-catalog.md pointer, an uncertainty row, and tie language are present.
  </verify>
  <acceptance_criteria>
    references/chart-selection.md's "Encoding accuracy" states the six-rank-with-ties D-1 ordering with the p.536/p.537 citation and the density-absent note, and the old strict arrow chain is removed. A "Selection heuristic" section threads L2-L5 as pointers to chart-catalog.md and finding codes (no new tree). An uncertainty relationship row is added attributing Wilke. No Abela 2008 / Few's Graph Selection Matrix citation appears. The verify one-liner passes.
  </acceptance_criteria>
  <done>The perceptual line is corrected to D-1's six-rank-with-ties form (old strict chain removed), L2-L5 are threaded as route-and-cite pointers, an uncertainty row is added, and only Munzner ch.3 + FT are cited.</done>
</task>

<task type="auto">
  <name>Task 2: Thread the L1 question→task layer into question-taxonomy.md and ripple the visualize skill to eleven relationships</name>
  <files>references/question-taxonomy.md, skills/dsx-visualize/SKILL.md</files>
  <read_first>
    - references/question-taxonomy.md (full, 38 lines) — the descriptive/diagnostic/predictive/causal/prescriptive table and the escalation/downgrading sections the L1 pointer attaches to.
    - skills/dsx-visualize/SKILL.md (full) — <method> step 1 enumerates the ten relationship names by hand (comparison … composition_over_time); the <references> block lists chart-selection.md / data-input-types.md / viz-smells.md.
    - .planning/v2.4-D05-EVIDENCE-PACK.md T2-6 (Munzner ch.3 task taxonomy — the L1 citation) and 22-CONTEXT.md item 6 (route-and-cite, no parallel tree).
  </read_first>
  <action>
    In references/question-taxonomy.md, add a short "Selection heuristic — Layer 1 (question → task)" pointer that maps the five question types already in the table to the analytical task, citing Munzner ch.3's task taxonomy (Actions × Targets), and routes onward to references/chart-selection.md for L2-L5 and references/chart-catalog.md for the marks. Keep it a pointer that reuses the existing table — do NOT add a parallel decision tree or restate the table. In skills/dsx-visualize/SKILL.md <method> step 1, add "uncertainty" to the hand-enumerated relationship list so it names eleven relationships (comparison, trend, part_to_whole, distribution, correlation, deviation, ranking, flow, geographic, composition_over_time, uncertainty). Add a line to step 6 ("Show uncertainty") pointing the uncertainty-mark choice at the ten Wilke §5.6 members and DSX-VIZ-071, and to the retained DSX-VIZ-070 property check. Add @references/chart-catalog.md to the <references> block alongside @references/chart-selection.md. Do not otherwise restructure the skill.
  </action>
  <verify>
    <automated>python -c "import pathlib; s=pathlib.Path('skills/dsx-visualize/SKILL.md').read_text(encoding='utf-8'); q=pathlib.Path('references/question-taxonomy.md').read_text(encoding='utf-8'); rels=['comparison','trend','part_to_whole','distribution','correlation','deviation','ranking','flow','geographic','composition_over_time','uncertainty']; missing=[r for r in rels if r not in s]; assert not missing, missing; assert 'chart-catalog.md' in s; assert 'munzner' in q.lower() and 'chart-selection.md' in q; print('skill lists 11 relationships; L1 pointer present')"</automated>
    Expected: all eleven relationship names appear in SKILL.md; the chart-catalog.md reference is present; question-taxonomy.md carries the Munzner-cited L1 pointer that routes to chart-selection.md.
  </verify>
  <acceptance_criteria>
    references/question-taxonomy.md has a Layer-1 question→task pointer citing Munzner ch.3 and routing to chart-selection.md and chart-catalog.md, reusing (not duplicating) the existing table. skills/dsx-visualize/SKILL.md step 1 lists eleven relationships including "uncertainty", step 6 routes uncertainty-mark choice to the §5.6 members / DSX-VIZ-071 / DSX-VIZ-070, and the <references> block includes @references/chart-catalog.md. The verify one-liner passes.
  </acceptance_criteria>
  <done>question-taxonomy.md carries the Munzner-cited L1 pointer (no parallel tree), and the visualize skill enumerates eleven relationships, routes the uncertainty-mark choice to the §5.6 members and the codes, and points at the catalog.</done>
</task>

<task type="auto">
  <name>Task 3: Add the doc-conformance repo-integrity test for the heuristic edits</name>
  <files>tests/test_selection_heuristic_docs.py</files>
  <read_first>
    - tests/test_finding_catalogue_invariant.py:52-58 (CRLF-safe, non-line-anchored regex discipline — this file reads Markdown, so it must be \r?\n-tolerant / whitespace-collapse).
    - references/chart-selection.md, references/question-taxonomy.md, skills/dsx-visualize/SKILL.md (post-edit) — the strings this test asserts present/absent.
    - 22-CONTEXT.md D-1 (the rank tokens) and Research Open Question 1 (the forbidden heuristic citations).
  </read_first>
  <action>
    Create tests/test_selection_heuristic_docs.py as a stdlib unittest module reading the three edited files with read_text(encoding="utf-8") and CRLF-safe, non-line-anchored matching (collapse whitespace before matching multi-word phrases; never a bare \n-anchor). Assert: (a) skills/dsx-visualize/SKILL.md contains all eleven relationship names including "uncertainty"; (b) chart-selection.md contains the D-1 tie language and the p.536/p.537 (or "Cleveland" + "1984") citation tokens, AND does NOT contain the superseded arrow chain that places colour saturation before volume (match the whitespace-collapsed substring form so a wrapped line still trips it) NOR a standalone "density" channel listed among the perceptual ranks; (c) chart-selection.md and skills/dsx-visualize/SKILL.md both reference chart-catalog.md; (d) question-taxonomy.md cites Munzner and routes to chart-selection.md; (e) no new standalone decision-tree file was added under references/ (assert a fixed allowlist of reference filenames, or assert no file matching a decision-tree name pattern exists) — the "no parallel tree" structural guard; (f) none of the three files contains the forbidden heuristic-citation tokens (Abela / "Graph Selection Matrix"). Add the "Run: python -m unittest tests.test_selection_heuristic_docs -v" footer. This module is off the gate path by construction.
  </action>
  <verify>
    <automated>python -m unittest tests.test_selection_heuristic_docs</automated>
    Expected: green — the skill lists eleven relationships; chart-selection.md carries the corrected D-1 ordering with its citation and no superseded chain; both surfaces point at the catalog; question-taxonomy.md carries the Munzner-cited L1 pointer; no new decision-tree file exists; no forbidden citation token appears.
  </verify>
  <acceptance_criteria>
    tests/test_selection_heuristic_docs.py passes all six clauses CRLF-safely: eleven relationships in the skill; the corrected D-1 perceptual ordering present and the superseded chain plus stray "density" channel absent in chart-selection.md; both surfaces reference chart-catalog.md; question-taxonomy.md cites Munzner and routes onward; no new decision-tree file; no Abela / Graph Selection Matrix citation. python -m unittest tests.test_selection_heuristic_docs is green.
  </acceptance_criteria>
  <done>The doc-conformance test is green, proving the eleven-relationship skill, the corrected perceptual line, the catalog pointers, the Munzner-cited L1 layer, the no-parallel-tree guard, and the absence of the forbidden heuristic citations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan edits three human/agent-facing reference and skill documents and adds one
off-gate-path test. No network, no untrusted input, no auth/session, no persistence beyond
the repo's Markdown/test files. The realistic threats are integrity/repudiation threats on
the guidance itself — a document that teaches the superseded perceptual order the new gate
test contradicts, or that cites a source never verified.

| Boundary | Description |
|----------|-------------|
| guidance → reader trust | The heuristic files make encoding-accuracy and citation claims agents act on. The doc-conformance test is the control that keeps them consistent with D-1 and the signed pack. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-10 | Tampering | chart-selection.md continuing to ship the superseded 7-item strict perceptual ordering while the new tie-break test asserts the corrected one (a self-contradicting repo) | medium | mitigate | Task 1 rewrites the line to D-1's six-rank-with-ties form; the doc-conformance test (Task 3 b) fails if the superseded chain or a stray 'density' channel survives (Pitfall 3). |
| T-22-11 | Repudiation | The heuristic citing Abela 2008 or Few's Graph Selection Matrix as if HQ-27-signed (never submitted), or claiming triangulation across the Ribecca lineage | medium | mitigate | Tasks 1-2 cite only Munzner ch.3 + FT; the doc-conformance test (Task 3 f) fails on the forbidden tokens; lineage is named, not triangulated (Open Question 1 option (a)). |
| T-22-12 | Tampering | A parallel decision-tree document introduced alongside the two existing selection surfaces (two surfaces that drift) | low | mitigate | The edits are pointers into the existing files; the doc-conformance test (Task 3 e) asserts no new decision-tree file exists under references/ (REQ-P22-04 no-parallel-tree). |

No threat rises to `high`, so nothing blocks under the ASVS level-1 block-on-high policy.

**Package legitimacy gate: N/A.** No npm/pip/cargo install — no T-22-SC threat, no legitimacy checkpoint.
</threat_model>

<verification>
Phase-level checks for this plan, in order:

1. Perceptual correction: `python -m unittest tests.test_selection_heuristic_docs -v` — chart-selection.md carries the D-1 six-rank-with-ties ordering with its p.536/p.537 citation and no superseded chain / stray density channel.
2. Relationship ripple: the same module's skill clause — SKILL.md enumerates eleven relationships including "uncertainty".
3. Route-and-cite, no parallel tree: the catalog-pointer clause and the no-new-decision-tree-file clause pass; question-taxonomy.md cites Munzner and routes onward.
4. Citation hygiene: the forbidden-token clause passes (no Abela 2008 / Few's Graph Selection Matrix).
5. Full suite green (per-wave gate): `python -m unittest discover -s tests` — the baseline plus the new doc-conformance module.
</verification>

<success_criteria>
- The 5-layer heuristic ships as route-and-cite edits into references/question-taxonomy.md (L1) and references/chart-selection.md (L2-L5) plus skill pointers — no parallel decision tree (REQ-P22-04).
- chart-selection.md's perceptual line is corrected to D-1's six-rank-with-ties form (superseded strict chain and 'density' removed); an uncertainty relationship row is added (Pitfall 3).
- skills/dsx-visualize/SKILL.md enumerates eleven relationships including "uncertainty", routes the uncertainty-mark choice to the §5.6 members and DSX-VIZ-070/071, and references references/chart-catalog.md.
- Only HQ-27-signed sources are cited (Munzner ch.3 + FT); Abela 2008 and Few's Graph Selection Matrix are dropped; no triangulation claim across the Ribecca lineage.
- tests/test_selection_heuristic_docs.py is green; full tests/ suite green.
</success_criteria>

<output>
Create `.planning/phases/22-catalog-spine-uncertainty-heuristic/22-04-SUMMARY.md` when done.
</output>
