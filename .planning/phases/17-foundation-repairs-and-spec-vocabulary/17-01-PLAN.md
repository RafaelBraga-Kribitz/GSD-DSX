---
phase: 17-foundation-repairs-and-spec-vocabulary
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - dsx/checks/stats.py
  - tests/test_boschloo_reconciliation.py
autonomous: true
requirements: [REQ-P17-01]
tags: [statistics, routing-table, boschloo, doc-code-reconciliation]

must_haves:
  truths:
    - "recommend_test for a two-group proportion names boschloo_exact as the small-expected-cell alternative (not fisher_exact)"
    - "boschloo_exact is a member of NONPARAMETRIC_TESTS"
    - "references/test-selection.md still names Boschloo, pinned against the code by a regression test so this divergence class cannot recur silently"
  artifacts:
    - dsx/checks/stats.py
    - tests/test_boschloo_reconciliation.py
  key_links:
    - "recommend_test's two-proportion alternatives string <-> references/test-selection.md Boschloo row, bound by test_boschloo_reconciliation.py (doc<->code down payment for REQ-P20-04)"
    - "a declared boschloo_exact is accepted by the DSX-STA-041 acceptable-alternatives set because alt.split(' ')[0] on 'boschloo_exact (any expected cell < 5)' yields 'boschloo_exact' — no further edit needed"
---

<objective>
Reconcile the live Boschloo doc/code divergence (D-04) by fixing the CODE to match the already-correct DOC, and pin the two together with a regression test.

Purpose: This is the first inherited defect every later catalogue row would compound. The documentation (references/test-selection.md) already correctly names Boschloo's exact test for the small-expected-cell proportion case and cites Lydersen, Fagerland & Laake (2009) §9 (Boschloo dominates Fisher on power while holding size); the code still emits fisher_exact and omits boschloo_exact from NONPARAMETRIC_TESTS. Reconcile to the doc per D-04, and lock them so the divergence cannot recur silently.

Output: Two edits in dsx/checks/stats.py and a new pinned regression test. Adds NO new finding code — boschloo_exact is a test-name routing string, not a DSX-STA-* code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-foundation-repairs-and-spec-vocabulary/17-CONTEXT.md
@.planning/phases/17-foundation-repairs-and-spec-vocabulary/17-RESEARCH.md
@.planning/phases/17-foundation-repairs-and-spec-vocabulary/17-VALIDATION.md
@dsx/checks/stats.py
@references/test-selection.md
@tests/test_cuped_vocab.py
</context>

<artifacts_produced>
Symbols this plan creates or widens (zero new finding codes — REQ-P17-05 intact):

- `dsx/checks/stats.py` — `NONPARAMETRIC_TESTS` set literal (currently at line 23-26) WIDENED by one member: `boschloo_exact` is added; `fisher_exact` is KEPT (it is still the correct 3-plus-group sparse-cell alternative at line 69). No membership removed.
- `dsx/checks/stats.py` — the two-proportion branch alternatives string (currently line 65) changes its first alternative from `fisher_exact (any expected cell < 5)` to `boschloo_exact (any expected cell < 5)`. The PRIMARY returned test stays `two_proportion_z`.
- `tests/test_boschloo_reconciliation.py` — NEW test module (class `BoschlooReconciliationTest`) pinning REQ-P17-01: behavioural assertions on recommend_test + a CRLF-safe doc<->code pin reading references/test-selection.md.

No `report.add(...)` call site is touched by this plan, so the finding-code catalogue is unaffected.
</artifacts_produced>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Write the failing Boschloo reconciliation regression test</name>
  <read_first>
    - dsx/checks/stats.py lines 15-70 (OUTCOME_TYPES, PARAMETRIC_TESTS, NONPARAMETRIC_TESTS at 23-26, the proportion branch and its two-proportion alternatives string at 62-67)
    - references/test-selection.md (the "Boschloo's exact test if any expected cell < 5" table cell around line 10 and footnote [^1] citing Lydersen, Fagerland & Laake 2009 — confirm the doc side is intact and needs NO edit)
    - tests/test_cuped_vocab.py (house test style: stdlib unittest.TestCase, no pytest, CRLF-agnostic — no line-anchored parsing)
    - .planning/phases/17-foundation-repairs-and-spec-vocabulary/17-CONTEXT.md D-04 (reconcile-to-doc direction) and 17-VALIDATION.md Per-Task Verification Map row REQ-P17-01
  </read_first>
  <files>tests/test_boschloo_reconciliation.py</files>
  <behavior>
    - Behavioural: recommend_test("proportion", 2) returns a recommendation whose alternatives list contains an entry whose first whitespace-delimited token is boschloo_exact, and whose primary test stays two_proportion_z.
    - Membership: boschloo_exact is an element of NONPARAMETRIC_TESTS; fisher_exact remains an element of NONPARAMETRIC_TESTS (additive, not a replace).
    - Doc<->code pin: references/test-selection.md, read with encoding utf-8 and matched after whitespace-collapse (never a line-anchored regex, per repo CRLF rule), still contains the token Boschloo — proving the doc names what the code now emits.
  </behavior>
  <action>Create tests/test_boschloo_reconciliation.py as a stdlib unittest.TestCase module named BoschlooReconciliationTest, modelled on the shape of tests/test_cuped_vocab.py. Import recommend_test and NONPARAMETRIC_TESTS from dsx.checks.stats. Write three assertions matching the behavior block: (1) the two-proportion recommendation's alternatives contain a boschloo_exact entry and its primary test is two_proportion_z; (2) boschloo_exact is in NONPARAMETRIC_TESTS and fisher_exact is still in NONPARAMETRIC_TESTS; (3) references/test-selection.md read via pathlib from the repo root, whitespace-collapsed, still contains the Boschloo name. Extract the alternative's leading token with a split on whitespace mirroring recommend_test's own acceptable-alternatives logic (the token before the parenthetical). Do NOT add any pytest syntax. Run the module now and confirm it fails RED on assertions (1) and (2) because the code still emits fisher_exact and omits boschloo_exact.</action>
  <verify>
    <automated>python3 -m unittest tests.test_boschloo_reconciliation -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_boschloo_reconciliation.py exists and is discovered by unittest.
    - Running the module RED-fails on the alternatives assertion and the NONPARAMETRIC_TESTS membership assertion (proving the test genuinely exercises the divergence, not a tautology).
    - The doc-pin assertion PASSES already (the doc side is correct pre-edit) — confirming it pins the doc, not asserts a doc edit.
    - No fenced pytest constructs; module uses unittest.TestCase only.
  </acceptance_criteria>
  <done>tests/test_boschloo_reconciliation.py exists and RED-fails on the two divergence assertions while the doc-pin assertion already passes.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Reconcile the code to the doc — boschloo_exact into NONPARAMETRIC_TESTS and the two-proportion alternative</name>
  <read_first>
    - dsx/checks/stats.py lines 23-26 (NONPARAMETRIC_TESTS) and 62-67 (two-proportion branch alternatives string)
    - dsx/checks/stats.py lines 462-465 (the DSX-STA-041 acceptable-alternatives set builder — read-only; confirm alt.split(' ')[0] on the new string yields boschloo_exact so no edit is needed there)
    - dsx/checks/stats.py line 69 (the 3-plus-group sparse-cell branch still using fisher_exact — must NOT be touched)
    - 17-RESEARCH.md § "Anti-Patterns to Avoid" (do NOT remove fisher_exact; do NOT edit references/test-selection.md)
  </read_first>
  <files>dsx/checks/stats.py</files>
  <action>Make exactly two edits in dsx/checks/stats.py. First, add the string member boschloo_exact to the NONPARAMETRIC_TESTS set literal (the set currently at line 23-26); keep every existing member including fisher_exact. Second, in the two-proportion branch (n_groups <= 2, currently line 62-67), change the first alternatives entry from the fisher_exact small-expected-cell string to the boschloo_exact small-expected-cell string — same parenthetical qualifier text ("any expected cell < 5"), only the leading test name changes. Leave the PRIMARY returned test as two_proportion_z. Do NOT touch the 3-plus-group branch at line 69 (fisher_exact stays there — it is the correct sparse-cell alternative for that case). Do NOT edit references/test-selection.md (D-04 direction is reconcile-to-doc; the doc is already correct). Do NOT add or change any report.add(...) call — this fix mints no finding code. Run the Task 1 module and confirm it now passes GREEN.</action>
  <verify>
    <automated>python3 -m unittest tests.test_boschloo_reconciliation -v && python3 -m unittest tests.test_finding_catalogue_invariant -v</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m unittest tests.test_boschloo_reconciliation -v` is GREEN (all three assertions pass).
    - Source assertion: dsx/checks/stats.py contains boschloo_exact in the NONPARAMETRIC_TESTS set literal AND in the two-proportion alternatives string; fisher_exact still present in both NONPARAMETRIC_TESTS and the 3-plus-group branch.
    - `python3 -m unittest tests.test_finding_catalogue_invariant -v` stays GREEN at 260 codes (this edit touches no report.add site — cheap early proof no code was minted, REQ-P17-05).
    - references/test-selection.md is byte-unchanged (git diff shows no edit to the doc).
  </acceptance_criteria>
  <done>boschloo_exact is in NONPARAMETRIC_TESTS and the two-proportion alternative; test_boschloo_reconciliation and test_finding_catalogue_invariant are green; the doc is unedited; zero new codes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyst-authored ANALYSIS-SPEC.yaml -> dsx loader/parser | The only external input surface; unchanged by this plan. No network, no auth, no subprocess, no new file-write path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01-01 | Tampering | references/test-selection.md drifting from dsx/checks/stats.py routing table | low | mitigate | The new test_boschloo_reconciliation.py binds the doc's Boschloo name to the code's emitted alternative — a future silent divergence turns it red. This is the Boschloo-specific down payment on the general doc/code agreement test (REQ-P20-04). |
| T-17-01-02 | Tampering | NONPARAMETRIC_TESTS set — an accidental replace-not-add dropping fisher_exact | low | mitigate | Test asserts fisher_exact remains a member alongside boschloo_exact; the 3-plus-group branch is explicitly out of edit scope. |
| T-17-01-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero packages (stdlib unittest only; python3 3.14.6 confirmed). No package legitimacy audit owed. |
</threat_model>

<verification>
- After Task 1 commit: `python3 -m unittest tests.test_boschloo_reconciliation -v` (expect RED on the two divergence assertions).
- After Task 2 commit: same module GREEN, plus `python3 -m unittest tests.test_finding_catalogue_invariant -v` GREEN (260, no mint).
- Wave-1 merge gate (orchestrator): `python3 -m unittest discover -s tests -q` fully green from a clean tree (see 17-VALIDATION.md clean-tree caveat: if only the two DECISIONS.jsonl-sensitive tests fail, remove stray root ledgers and re-run).
- Feedback latency target < 40s per 17-VALIDATION.md.
</verification>

<success_criteria>
- boschloo_exact is a member of NONPARAMETRIC_TESTS and fisher_exact is retained.
- recommend_test's two-proportion small-expected-cell alternative names boschloo_exact; the primary test stays two_proportion_z.
- references/test-selection.md is unedited and still names Boschloo; a regression test binds the two.
- Zero new finding codes (catalogue invariant green at 260).
</success_criteria>

<output>
Create `.planning/phases/17-foundation-repairs-and-spec-vocabulary/17-01-SUMMARY.md` when done.
</output>
