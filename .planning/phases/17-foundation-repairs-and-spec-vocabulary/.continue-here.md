# Continue here — Phase 17, S1-3 execute (Wave 2 / plan 17-03 pending)

**Written:** 2026-09-01T21:35Z, at the ~12-min pacing-cap stop on the clean Wave-1 boundary.
**Firing that wrote this:** executed S1-3 Wave 1 inline on the ceremony branch.

## State on disk (the fact — reconcile against `git log` first)

- **Branch:** `gsd/v2.3.0-test-catalog` (the ceremony branch — see the blocking anti-pattern below).
- **Wave 1 DONE, with SUMMARYs (GSD `has_summary` will auto-skip these):**
  - `17-01` Boschloo reconciliation — RED `c2c91cd` → GREEN `99622fe` → SUMMARY `b10f328`. REQ-P17-01.
  - `17-02` time_to_event fallthrough pin — `048d203` → SUMMARY `c09b0b9`. REQ-P17-03/04.
- **Wave 2 PENDING — the one remaining plan:** `17-03-PLAN.md` (estimand_kind 6-member vocab in `dsx/spec.py`; widen DSX-STA-040 in `dsx/checks/stats.py` into a single-call-site `(field, vocab)` membership loop over outcome_type + estimand_kind that runs independently of the declared-test early return; extend good/bad ANALYSIS-SPEC + template with a valid `estimand_kind:` line; regen `references/finding-codes.md` via `scripts/gen-finding-catalogue.py --write`; prove 260-code set identity). REQ-P17-02, REQ-P17-05. **Wave 2, depends_on 17-01 (both write stats.py) — now unblocked.**

## Baseline facts (measured this firing)

- Full suite GREEN: **1312 tests** (`python3 -m unittest discover -s tests -q`).
- Catalogue invariant: **260 codes** by set identity; `scripts/gen-finding-catalogue.py --check` is the doc-staleness gate 17-03 Task 3 must leave green after `--write`.
- Root `DECISIONS.jsonl` absent (the two DECISIONS-sensitive tests won't false-fail). Gitignored `examples/`,`templates/` ledgers present but harmless.

## Next steps

1. Execute `17-03` only (Wave 1 is summarised — will be skipped). Follow `17-03-PLAN.md` task-by-task: Task 1 RED test, Task 2 spec.py + stats.py GREEN, Task 3 fixtures + catalogue regen + full-suite + golden/good-fixture gates.
2. Re-run every gate as orchestrator; paste real evidence.
3. Then S1-3 becomes checkable (all 3 plans done) → proceed to S1-4 (code review + verification).

## Critical Anti-Patterns

| # | Anti-pattern | severity |
|---|---|---|
| 1 | Running the framework's branch switch onto the milestone-template name | blocking |

**Anti-pattern 1 — full context (answerable without external lookup):**
- *What it is:* `/gsd-execute-phase`'s `init.execute-phase` computes `branch_name: gsd/v2.3-test-catalog` (milestone template `gsd/{milestone}-{slug}`, `milestone_version: v2.3`), but the ceremony branch is `gsd/v2.3.0-test-catalog` (with `.0`). The `handle_branching` step would `git checkout -b gsd/v2.3-test-catalog origin/main`.
- *How it manifests:* a fresh branch forked off `main`, **orphaning every committed S0/S1/S1-3 unit** — catastrophic, silent data-loss of the milestone's work.
- *Structural mechanism that prevents it:* do NOT run `handle_branching`/the subagent-wave path. Execute `17-03` **inline on the current branch** as orchestrator (this firing did exactly that for Wave 1). Assert `git rev-parse --abbrev-ref HEAD` == `gsd/v2.3.0-test-catalog` before AND after execute. Standing note also filed in HUMAN-QUEUE.
