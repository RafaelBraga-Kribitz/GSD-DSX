---
phase: 23-style-snippet-layer
plan: 03
type: tdd
wave: 3
depends_on:
  - 23-02
files_modified:
  - references/chart-snippets.md
  - skills/dsx-visualize/SKILL.md
  - tests/test_snippet_catalog_routing.py
autonomous: true
requirements:
  - REQ-P23-04

must_haves:
  truths:
    - "references/chart-snippets.md holds one worked snippet per function category on references/chart-catalog.md's Function axis (Change over Time, Magnitude, Distribution, Correlation, Ranking, Part-to-whole, Deviation, Flow, Spatial, Uncertainty) plus a sealing-workflow section; each snippet imports templates/dsx_plotstyle.py (finalise_figure/save_deterministic) and routes to existing finding codes by name (REQ-P23-04)."
    - "Every DSX-code token cited in references/chart-snippets.md exists as a row in references/finding-codes.md, and no snippet restates a numeric threshold that lives in dsx/checks/viz.py (MAX_PIE_SLICES, MAX_CATEGORICAL_COLORS) — proven by tests/test_snippet_catalog_routing.py whose forbidden values are read LIVE from the constants (non-vacuity anchor) (REQ-P23-04, D-P23-04)."
    - "skills/dsx-visualize/SKILL.md <references> block lists references/chart-snippets.md so the skill wires the catalog in (REQ-P23-04)."
    - "The finding-code catalogue set-identity stays 276 → 276: gen-finding-catalogue.py --check exits 0 and tests/test_finding_catalogue_invariant.py passes unchanged — Phase 23 mints ZERO codes (D-P23-04, D-06)."
    - "The whole tests/ suite is green from a clean tree — every REQ-P23-01..05 behaviour has an automated test (header, WCAG, api, determinism, hermeticity, routing) and the zero-mint gate passes."
  artifacts:
    - "references/chart-snippets.md (new: per-function-category snippet catalog, routes to finding codes)"
    - "skills/dsx-visualize/SKILL.md (edit: <references> += @references/chart-snippets.md)"
    - "tests/test_snippet_catalog_routing.py (new: code-citation cross-check + no-threshold-restatement, values read live from viz.py)"
  key_links:
    - "test_snippet_catalog_routing.py parses cited codes with the CRLF-safe _ROW_RE shape ( | `DSX-...` | ) used by test_finding_catalogue_invariant.py, and reads MAX_PIE_SLICES/MAX_CATEGORICAL_COLORS live from dsx.checks.viz — so a stale citation or a restated threshold turns it red, and the check cannot drift from the real constants (Pitfall 3)."
    - "The snippets reference finalise_figure/save_deterministic from Plan 02 and route (never restate) to DSX-VIZ-062 (source note ↔ mandatory source kwarg), DSX-VIZ-020, DSX-VIZ-040, DSX-VIZ-050, DSX-FIG-011 — depends_on 23-02."
    - "The zero-mint 276→276 proof reuses the existing test_finding_catalogue_invariant.py + gen-finding-catalogue.py --check (no edit expected) — the phase-close additive-only property, exactly as Phase 21 proved 275→275."
  prohibitions:
    - "[unverified — never auto-dismiss] No snippet may restate a gate threshold value (the pie/donut slice maximum or the categorical-colour maximum) — cite DSX-VIZ-040 / DSX-VIZ-050 by name instead (D-P23-04). A restated value is a second source of truth that can drift from viz.py."
    - "[unverified — never auto-dismiss] No new finding code may be minted to 'clean up' routing — every cited code must already exist in references/finding-codes.md; the 276→276 set-identity is the proof (D-P23-04, Pitfall 3). Do NOT edit tests/test_finding_catalogue_invariant.py, scripts/gen-finding-catalogue.py, or references/finding-codes.md."
    - "[unverified — never auto-dismiss] chart-snippets.md must not embed the negative-grep threshold literal the routing test forbids (comment-text discipline); route by code name, not by number."
---

<objective>
Author the per-chart-type snippet catalog `references/chart-snippets.md` — one worked code
snippet per function category on Phase 22's `references/chart-catalog.md` Function axis — each
importing the Plan 02 helper (`finalise_figure` / `save_deterministic`) and **routing to
existing finding codes by name**, never restating a gate threshold. Wire the catalog into the
`dsx-visualize` skill's `<references>` block, prove the routing with a repo-integrity test
(every cited code exists in the catalogue; no snippet restates a live `viz.py` threshold), and
close the phase by proving the finding-code set-identity stays **276 → 276** (zero mint).

This plan implements REQ-P23-04 (the snippet catalog that routes to finding codes without
restating thresholds) and discharges D-P23-04's zero-mint proof for the whole phase. It is
Wave 3: the snippets demonstrate the Plan 02 helper.

Purpose: the catalog is the analyst-facing bridge between the style layer and the gate — it
shows *how* to finalise and seal a figure of each function type and *which* codes govern its
correctness, so authors never re-derive (and drift from) the rules in `dsx/checks/viz.py`.
Routing-not-restating is exactly the D-P23-04 constraint that keeps this a zero-mint phase.

Output: `references/chart-snippets.md`, a `<references>` edit to
`skills/dsx-visualize/SKILL.md`, one new routing test, and the phase-close 276→276 proof.
Zero finding codes minted.

## Analytical contract: Not Applicable
Non-analytical style/tooling/documentation phase — no metric/model/experiment/decision-rule/
statistical claim. No ANALYSIS-SPEC.yaml; `dsx gate plan --allow-missing` passes clean
(dsx.require_spec disabled). Do not scaffold an analysis.

## Artifacts this plan produces
- `references/chart-snippets.md` (new)
- `skills/dsx-visualize/SKILL.md` — `<references>` += `@references/chart-snippets.md` (edit)
- `tests/test_snippet_catalog_routing.py` (new)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-style-snippet-layer/23-CONTEXT.md
@.planning/phases/23-style-snippet-layer/23-RESEARCH.md

# The catalog the snippets point at (function axis + admissible marks), the live codes to
# route to, the skill wiring shape, and the zero-mint mechanism:
@references/chart-catalog.md
@references/finding-codes.md
@skills/dsx-visualize/SKILL.md
@dsx/checks/viz.py
@tests/test_finding_catalogue_invariant.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write the routing test — cited codes exist, and no snippet restates a live viz.py threshold</name>
  <files>tests/test_snippet_catalog_routing.py</files>
  <read_first>
    - tests/test_finding_catalogue_invariant.py:56-61 (_ROW_RE = r"\|\s*`(DSX-[A-Z]+-\d+)`\s*\|" — the CRLF-safe row shape to reuse for parsing which codes references/finding-codes.md defines) and its CRLF discipline note (whitespace-collapse, non-line-anchored).
    - tests/test_selection_heuristic_docs.py (Phase 22's doc/code agreement pattern — the model for a documentation↔code cross-check test; mirror its structure).
    - dsx/checks/viz.py:104-105 (MAX_PIE_SLICES = 5, MAX_CATEGORICAL_COLORS = 7 — imported LIVE so the forbidden-restatement values track the real constants, never transcribed).
    - 23-RESEARCH.md §Pattern 2 (snippet-catalog routing, never restating a threshold) and §Anti-Patterns (restating a threshold creates a second source of truth) and Pitfall 3 (do not mint a code to make routing cleaner).
    - 23-RESEARCH.md "Live DSX-VIZ-*/DSX-FIG-* codes available for snippet routing" table (the codes the snippets may cite).
  </read_first>
  <behavior>
    - Cited-codes-exist check (RED, catalog absent): extract every DSX-code token matching r"DSX-[A-Z]+-\d+" from references/chart-snippets.md; parse the set of defined codes from references/finding-codes.md with _ROW_RE; assert every cited token is a defined code (cited ⊆ defined). Non-vacuity: assert at least one code is cited (a catalog that routes to nothing must not pass vacuously).
    - No-threshold-restatement check (RED): import MAX_PIE_SLICES and MAX_CATEGORICAL_COLORS from dsx.checks.viz; build forbidden restatement regexes parameterised by those LIVE integer values (a value adjacent to a threshold noun/comparator — e.g. the slice-count value next to a slice word, or the colour-count value next to a "colours/hues" word with a max/no-more-than/beyond comparator); assert none match anywhere in chart-snippets.md. The values come from the imported constants, not from string literals in the test — that live derivation is the non-vacuity anchor (if viz.py changes the number, the test tracks it).
    - Both fail at RED because references/chart-snippets.md does not exist.
  </behavior>
  <action>
    Create tests/test_snippet_catalog_routing.py as a stdlib unittest module. Insert ROOT on sys.path; import MAX_PIE_SLICES, MAX_CATEGORICAL_COLORS from dsx.checks.viz. Reuse the _ROW_RE shape from test_finding_catalogue_invariant.py to read the DEFINED code set from references/finding-codes.md, and a token regex r"DSX-[A-Z]+-\d+" to read the CITED tokens from references/chart-snippets.md (both read with encoding="utf-8", CRLF-safe). Test method 1: assert cited set is non-empty and cited ⊆ defined; on failure, name the offending tokens. Test method 2: construct the forbidden restatement patterns from the imported constant VALUES (build the regexes at runtime using the integer values, so nothing is transcribed) and assert re.search finds none in chart-snippets.md; on failure, name the matched span. Add the "Run: python -m unittest tests.test_snippet_catalog_routing -v" footer. Do NOT hardcode 5 or 7 as literals in the test — derive them from the imports. Do NOT soften either assertion — RED is the goal.
  </action>
  <verify>
    <automated>python -m unittest tests.test_snippet_catalog_routing</automated>
    RED expected: exits non-zero — references/chart-snippets.md does not exist, so both the cited-codes-exist and the no-restatement reads fail (file absence). Confirm the failure is file-absence, not a test bug.
  </verify>
  <acceptance_criteria>
    tests/test_snippet_catalog_routing.py imports MAX_PIE_SLICES/MAX_CATEGORICAL_COLORS live from dsx.checks.viz, reuses the CRLF-safe _ROW_RE to read finding-codes.md's defined set, asserts cited ⊆ defined (non-empty), and asserts no live-threshold restatement appears in the catalog — with the forbidden values derived from the imported constants (no transcribed 5/7). Both are RED for file-absence; no assertion softened.
  </acceptance_criteria>
  <done>The routing test exists, derives its forbidden threshold values live from viz.py, asserts every cited code is defined and no threshold is restated, and is RED because the snippet catalog is not written yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Author references/chart-snippets.md and wire it into the dsx-visualize skill</name>
  <files>references/chart-snippets.md, skills/dsx-visualize/SKILL.md</files>
  <read_first>
    - references/chart-catalog.md:15-42 (the Function axis: the ten categories Change over Time, Magnitude, Distribution, Correlation, Ranking, Part-to-whole, Deviation, Flow, Spatial, Uncertainty — one snippet per category) and its admissible marks per function.
    - 23-RESEARCH.md §Pattern 2 (the exact snippet shape: a minimal dsx_plotstyle-based code block, then the governing codes named by code — NEVER by re-deriving the rule) and the "Live codes available for snippet routing" table.
    - skills/dsx-visualize/SKILL.md:66-71 (the <references> block currently listing chart-catalog.md, chart-selection.md, data-input-types.md, viz-smells.md — add chart-snippets.md here) and its <method> step 5/6 (seal workflow via `dsx seal`, uncertainty routing to DSX-VIZ-071/070).
    - templates/dsx_plotstyle.py (Plan 02) — the finalise_figure(title=, source=)/save_deterministic(path, metadata={'Date': None}) calls the snippets demonstrate.
    - 23-RESEARCH.md §Anti-Patterns + Pitfall 3 — route by code, never restate a numeric threshold; never mint a new code to make routing look cleaner.
  </read_first>
  <behavior>
    - references/chart-snippets.md contains one section per function category (ten), each with: a representative mark from chart-catalog.md, a minimal code snippet importing finalise_figure + save_deterministic from templates/dsx_plotstyle.py and calling finalise_figure(title=<takeaway sentence>, source=<dataset, period>) then save_deterministic(fig, path, metadata={'Date': None}), and a "Gate-enforced" line naming the governing finding codes BY CODE (routing) — e.g. DSX-VIZ-062 (source note ↔ mandatory source kwarg), DSX-VIZ-020 (zero baseline for length marks), DSX-FIG-011 (seal present), and for Uncertainty DSX-VIZ-070/071. A header "Sealing workflow" section shows save_deterministic → `dsx seal <path>` → paste into visuals[].svg_sha256 (the single-hasher flow, GA-2). Every code named must already exist in references/finding-codes.md.
    - No section restates a numeric threshold: the pie/donut section cites DSX-VIZ-040 by name and the categorical-colour guidance cites DSX-VIZ-050 by name, without writing either maximum's value.
    - skills/dsx-visualize/SKILL.md <references> block gains a line `@references/chart-snippets.md`.
  </behavior>
  <action>
    Author references/chart-snippets.md. Open with a one-line provenance note that it describes/demonstrates and does not adjudicate (the gate reads dsx/checks/viz.py, never this file — mirror chart-catalog.md's stance). Add a "Sealing workflow" section wiring save_deterministic → `dsx seal <path>` → visuals[].svg_sha256 (GA-2 single-hasher). Then one section per function category from chart-catalog.md's Function axis (Change over Time, Magnitude, Distribution, Correlation, Ranking, Part-to-whole, Deviation, Flow, Spatial, Uncertainty), each: name a representative admissible mark; show a minimal snippet importing finalise_figure + save_deterministic and calling finalise_figure(title=..., source=...) then save_deterministic(fig, "figures/<name>.svg", metadata={'Date': None}); end with a "Gate-enforced:" line that ROUTES to the governing codes by code name only (draw from 23-RESEARCH's live-codes table — e.g. DSX-VIZ-062, DSX-VIZ-020, DSX-VIZ-061, DSX-VIZ-060/063/064, DSX-FIG-011; Uncertainty routes to DSX-VIZ-070 and DSX-VIZ-071; part-to-whole routes to DSX-VIZ-040; multi-series colour routes to DSX-VIZ-050). Point to references/chart-catalog.md for the full per-function mark set. For the part-to-whole and categorical-colour sections, cite the governing code by name and describe the constraint qualitatively (e.g. "keep the slice count within what angle can encode — DSX-VIZ-040 enforces the limit") WITHOUT writing the numeric maximum — the number lives only in viz.py (D-P23-04). Every cited code MUST be one already present in references/finding-codes.md (verify against it; do not invent a code).

    Edit skills/dsx-visualize/SKILL.md: add `@references/chart-snippets.md` to the <references> block (alongside chart-catalog.md, chart-selection.md, data-input-types.md, viz-smells.md). Change nothing else in the skill.
  </action>
  <verify>
    <automated>python -m unittest tests.test_snippet_catalog_routing -v && python -c "import pathlib; s=pathlib.Path('references/chart-snippets.md').read_text(encoding='utf-8'); cats=['Change over Time','Magnitude','Distribution','Correlation','Ranking','Part-to-whole','Deviation','Flow','Spatial','Uncertainty']; missing=[c for c in cats if c.lower() not in s.lower()]; assert not missing, ('missing function sections: '+str(missing)); k=pathlib.Path('skills/dsx-visualize/SKILL.md').read_text(encoding='utf-8'); assert 'references/chart-snippets.md' in k, 'skill references not wired'; print('10 function sections present; skill wired')"</automated>
    Expected: the routing test goes GREEN (every cited code is defined in finding-codes.md; no live threshold restated); all ten function-category sections are present; the skill's references block lists chart-snippets.md.
  </verify>
  <acceptance_criteria>
    references/chart-snippets.md holds a sealing-workflow section plus one section per function category (ten), each importing finalise_figure + save_deterministic and routing to governing codes BY CODE; the pie/donut and categorical-colour sections cite DSX-VIZ-040 / DSX-VIZ-050 by name without writing their numeric maxima. Every cited code is defined in references/finding-codes.md. skills/dsx-visualize/SKILL.md <references> lists @references/chart-snippets.md. tests.test_snippet_catalog_routing is GREEN. No new code invented.
  </acceptance_criteria>
  <done>The per-function-category snippet catalog exists, routes to existing finding codes without restating any threshold, is wired into the dsx-visualize skill, and the routing test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (prove): Phase-close — full suite green and the zero-mint 276 → 276 set-identity</name>
  <files>tests/test_snippet_catalog_routing.py</files>
  <read_first>
    - tests/test_finding_catalogue_invariant.py:39 (_EXPECTED_TOTAL = 276) and its two methods (the count invariant + the set-identity diff current_set == snapshot_set | _MINTED_CODES) — this is the zero-mint proof; it must pass UNCHANGED (no edit).
    - 23-CONTEXT.md D-P23-04 (Phase 23 mints zero codes; the set-identity diff proves 276 → 276, empty symmetric difference — the Phase-21 275→275 precedent).
    - 23-RESEARCH.md §Validation Architecture "Sampling Rate" / "Wave 0 Gaps" (full-suite command `python -m unittest discover -s tests`; per-wave gate adds `python scripts/gen-finding-catalogue.py --check`).
    - ./.claude/CLAUDE.md — sweep any stray root DECISIONS.jsonl before the clean-tree full-suite run (standing note).
  </read_first>
  <behavior>
    - `python scripts/gen-finding-catalogue.py --check` exits 0: the catalogue is current at 276 and every D-05-covered code still carries its citation — Phase 23 added no report.add, so the catalogue is unchanged.
    - tests/test_finding_catalogue_invariant.py passes unchanged: the count invariant reads 276 two ways and the set-identity diff shows added={} removed={} beyond the sanctioned mints — the empty symmetric difference IS the 276→276 zero-mint proof.
    - The whole tests/ suite is green from a clean tree: header, WCAG, api, determinism, hermeticity (with matplotlib in FORBIDDEN), and routing tests all pass alongside the pre-existing suite.
  </behavior>
  <action>
    Do NOT edit tests/test_finding_catalogue_invariant.py, scripts/gen-finding-catalogue.py, or references/finding-codes.md — Phase 23 is zero-mint by construction (D-P23-04), so those must pass untouched. Run the full suite and the catalogue oracle to prove the additive-nothing property end-to-end, and record in the SUMMARY the explicit set-identity result (added={}, removed={}, total 276→276) so the phase-close proof is legible, exactly as Phase 21 recorded 275→275. If the routing test needs a trivial internal tidy (a comment or message) discovered during the full-suite run, that is the ONLY edit permitted in this task and only if it does not change behaviour; otherwise touch nothing. Sweep any stray root DECISIONS.jsonl before the clean-tree run (repo standing note).
  </action>
  <verify>
    <automated>python -m unittest discover -s tests -q && python scripts/gen-finding-catalogue.py --check</automated>
    Expected: the full suite passes; gen-finding-catalogue.py --check exits 0 ("finding catalogue is current"). The set-identity test's own pass IS the zero-mint proof (added={}, removed={}, 276→276).
  </verify>
  <acceptance_criteria>
    The full tests/ suite is green; gen-finding-catalogue.py --check exits 0 at 276. tests/test_finding_catalogue_invariant.py, scripts/gen-finding-catalogue.py, and references/finding-codes.md are unedited. The SUMMARY records the 276→276 set-identity (added={}, removed={}). Every REQ-P23-01..05 behaviour has a green automated test.
  </acceptance_criteria>
  <done>The full suite is green, gen-finding-catalogue --check exits 0, and the set-identity diff proves 276 → 276 — Phase 23 mints zero codes, additive-nothing, with every requirement covered by a passing test.</done>
</task>

</tasks>

<threat_model>
register_authored_at_plan_time: true

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan adds a Markdown catalog, one skill-file reference edit, and one repo-integrity test. No network, no untrusted input, no auth/session, no gate-path code. The realistic risk is documentation↔code drift and an accidental mint, mirroring Phase 21/22's catalogue-integrity threats. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-06 | Tampering | Snippet catalog restating a gate threshold (a second source of truth that drifts from dsx/checks/viz.py) | medium | mitigate | tests/test_snippet_catalog_routing.py builds its forbidden-restatement patterns from the LIVE MAX_PIE_SLICES/MAX_CATEGORICAL_COLORS constants and fails if any appears in the catalog; snippets route by code name only (D-P23-04, Pattern 2). |
| T-23-07 | Tampering | A finding code silently minted or dropped while authoring the catalog (a cardinality-preserving swap the count alone would pass) | medium | mitigate | tests/test_finding_catalogue_invariant.py's set-identity diff (276, current_set == snapshot ∪ _MINTED_CODES) + gen-finding-catalogue.py --check exit 0 prove added={} removed={}; this plan edits none of the mint surfaces (D-P23-04, D-06). |
| T-23-08 | Repudiation | A snippet citing a non-existent / mistyped finding code (an ungrounded route) | low | mitigate | The cited ⊆ defined assertion against references/finding-codes.md fails on any token that is not a real defined code. |

No threat rises to `high`, so nothing blocks under the ASVS level-1 block-on-high policy
(no auth/session/network/untrusted-input surface — documentation + a repo-integrity test).

**Package legitimacy gate: N/A.** No npm/pip/cargo install this plan. No T-23-SC threat.
</threat_model>

<verification>
Phase-relevant checks for this plan, in order:
1. Routing green: `python -m unittest tests.test_snippet_catalog_routing -v` — every cited
   code is defined; no live threshold restated; catalog non-empty.
2. Skill wired + sections present: the Task 2 automated check — ten function sections + the
   `@references/chart-snippets.md` reference in the skill.
3. Zero mint: `python scripts/gen-finding-catalogue.py --check` exits 0 at 276;
   `python -m unittest tests.test_finding_catalogue_invariant -v` proves 276→276 unchanged.
4. Phase gate: `python -m unittest discover -s tests` — the full suite green from a clean tree.
</verification>

<success_criteria>
- references/chart-snippets.md ships one worked snippet per function category, each importing
  the Plan 02 helper and routing to existing finding codes by name, restating no threshold
  (REQ-P23-04).
- The catalog is wired into skills/dsx-visualize/SKILL.md's <references> block (REQ-P23-04).
- The routing test derives its forbidden threshold values live from viz.py and proves cited ⊆
  defined with a non-vacuity anchor (REQ-P23-04, D-P23-04).
- The finding-code set-identity stays 276 → 276 (added={}, removed={}); gen-finding-catalogue
  --check exits 0; the mint surfaces are unedited (D-P23-04, D-06).
- The full tests/ suite is green — every REQ-P23-01..05 behaviour has a passing automated test.
</success_criteria>

<output>
Create `.planning/phases/23-style-snippet-layer/23-03-SUMMARY.md` when done (record the
276→276 set-identity result and confirm every REQ-P23-01..05 has a green test).
</output>