---
phase: 11.1-generated-pipeline-reality
verified: 2026-08-20T00:00:00Z
status: human_needed
score: 6/6 roadmap success criteria verified; 8/8 requirement IDs satisfied; 1 backstop truth abstained
behavior_unverified: 0
overrides_applied: 0
gaps: []
behavior_unverified_items:
  - truth: "Plan 11.1-07 must_have (verification: backstop): 'When more than one selection-ledger condition holds at once, DSX-ML-090/091/092 are emitted in a stable order across repeated runs.'"
    test: "Construct a specification where the selection-ledger completeness test and the basis-branch test could both be said to hold at once, run dsx.checks.ml.check() twice, and compare finding order."
    expected: "A human decision on whether this truth is even reachable: reading dsx/checks/ml.py::_check_selection_ledger directly (verified in this pass) shows the three codes are structurally mutually exclusive within one report — an incomplete ledger returns after emitting DSX-ML-090, so DSX-ML-091/092 can never additionally fire in the same run. No test in the corpus exercises 'more than one condition holding at once' because, on the current implementation, that state cannot occur. Per the honest-verifier contract, this truth cannot be confirmed with explicit evidence (no test asserts multi-condition ordering) and must not be silently marked passed on inferred code-reading alone."
    why_human: "This is an insufficient_spec / non-inferable ('backstop') truth per plan 11.1-07's own frontmatter (`verification: backstop`). A verifier must abstain rather than pass it on inference. A human should confirm whether the truth is (a) vacuously satisfied because the described scenario is structurally impossible, in which case the must_have should be reworded or retired, or (b) pointing at a real future risk (e.g. a later plan reusing _check_selection_ledger's shape without preserving mutual exclusivity) that deserves an explicit regression test now."
human_verification:
  - test: "Resolve plan 11.1-07's backstop truth on emission-order stability for the selection-ledger codes (see behavior_unverified_items above)."
    expected: "A human decision: accept as vacuously true and retire/reword the must_have, or add a regression test that pins the mutual-exclusivity invariant explicitly."
    why_human: "verification: backstop — non-inferable per the honest-verifier contract; must abstain rather than silently pass."
  - test: "Review 11.1-REVIEW.md's four CRITICAL findings (CR-01, CR-02, CR-03, CR-04) against dsx/checks/code.py and decide whether they warrant an immediate follow-up plan before Phase 11.2/11.3 proceed, or are accepted as tracked debt."
    expected: "A human decision on remediation priority. All four are independently reproduced in this verification pass (see Anti-Patterns Found) and are real, not review artifacts: FULL_FRAME_SPREAD_FILTER_RE is cubic and FULL_FRAME_IMPUTE_RE is quadratic in line length (a real, non-adversarial DoS risk against long entrypoint lines); a `model.fit (df)` with an extra space silently bypasses the pre-existing DSX-CODE-001 (not introduced by this phase, but newly spotlighted by this phase's own threat-model claims); and `model.fit(X=data, y=target)` keyword-argument calls are silently invisible to this phase's own new DSX-CODE-021 detector."
    why_human: "None of these falsify a must_have truth as literally worded (the literal truths use bare-token, non-keyword fit calls and the corpus fixture uses bracket syntax, not keyword arguments), so they do not block this phase's goal achievement on the declared corpus case. But they are genuine, reproduced adversarial-evasion and DoS weaknesses in freshly-shipped code, directly relevant to this phase's own stated threat model (LLM/agent-generated pipelines, which idiomatically use keyword arguments) — a human should decide whether to gate the next phase on a fix or track it as debt."
---

# Phase 11.1: Generated-pipeline reality Verification Report

**Phase Goal:** A multi-stage pipeline whose cleaning statistics saw the test rows cannot pass the
gate by declaring honestly — the entrypoint scan reads the pandas idioms cleaning is actually
written in, and the contract can carry a per-stage fit boundary.

**Verified:** 2026-08-20
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

All six roadmap success criteria were checked by running the actual gate and check modules
directly, not by reading SUMMARY.md claims.

| # | Truth (roadmap SC) | Status | Evidence |
|---|---|---|---|
| 1 | The 2026-08-20 reproduction (full-frame mean/mode imputation + spread-based row filter above an honest split, `preprocessing_fit_on: train_only` honestly declared) is BLOCKED at execute, where it passed with zero findings before this phase. | VERIFIED | Ran `python -m dsx.cli gate execute --spec examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml` directly: exit code 1, `gate:execute: BLOCK` with `CRITICAL=3` — `DSX-CODE-020`, `DSX-CODE-021`, `DSX-CODE-030` all fire, plus `DSX-ML-090` HIGH. Read the fixture entrypoint directly (`examples/known-bad/full-frame-cleaning-entrypoint.py`) and confirmed the idiom order matches the claim (full-frame `fillna(mean())`, a `quantile` row filter, a chi-square test on the target, a legitimate `train_test_split`, then `scaler.fit_transform(data[['Age']])` on the full frame after the split). |
| 2 | A `.fit(`/`.fit_transform(` call at or after the split whose first argument is not a recognised training-frame name fires; `SimpleImputer().fit(df)` one line after the split no longer passes clean. | VERIFIED (with a distinct adversarial-evasion caveat — see Anti-Patterns Found, CR-04) | Ran a synthetic entrypoint (`X_train, X_test, ... = train_test_split(...)` then `SimpleImputer().fit(df)` on the next line) through `dsx.checks.code.check()` directly: `['DSX-CODE-021']` fires. Also confirmed the fixture's own `scaler.fit_transform(data[['Age']])` after the split fires `DSX-CODE-021` naming that exact line. Caveat: a keyword-argument call (`model.fit(X=data, y=target)`) is invisible to `FIT_CALL_RE` — reproduced directly, `code.FIT_CALL_RE.search('model.fit(X=data, y=target)')` returns `None`. This does not falsify the truth as literally worded (bare/positional first argument) and does not affect the declared corpus fixture (which uses bracket syntax), but is a real gap against the broader "the entrypoint scan reads the pandas idioms cleaning is actually written in" phase goal, since keyword arguments are idiomatic in LLM/agent-generated code. |
| 3 | A statistical-test call whose argument list contains the declared model target fires — the marker no fit-scan can see. | VERIFIED | Ran `code.STAT_TEST_CALL_RE.search(...)` directly against `chi2_contingency(...)` and `ttest_ind(...)` call text: both match. Ran `python -m unittest tests.test_dsx.TestPhase11_1Code -v`: 41/41 pass, including `test_chi_square_before_split_referencing_target_is_critical` and `test_chi_square_after_split_referencing_target_is_high`. Confirmed live in the corpus fixture's gate-execute output above (`DSX-CODE-030` fires naming the exact `chi2_contingency` line). |
| 4 | The optional per-dataset cleaning declaration exists; a step fitted outside training rows is CRITICAL; a step contradicting `model.preprocessing_fit_on: train_only` is HIGH; absence of the block alone fires nothing. | VERIFIED | Ran `dsx.checks.ml.check()` directly against a synthetic spec with a leaky `data[].cleaning[].fit_on: all_data` and `model.preprocessing_fit_on: train_only`: both `DSX-ML-023` and `DSX-ML-024` fire. Ran `python -m unittest tests.test_dsx.TestPhase11_1MLCleaning -v`: 15/15 pass, including the no-cleaning-block-fires-nothing and blank/absent-fit_on cases. |
| 5 | A model-vs-baseline comparison whose score source is blank/`unknown`/`best_fold` fires; a declared algorithm with no selection ledger fires; selection made on the test set is CRITICAL; a margin smaller than the model's own fold-to-fold spread advises. | VERIFIED | Confirmed `ml.SCORE_SOURCES == frozenset({'cv_mean','holdout','nested_cv','best_fold','unknown'})` and `ml.SELECTION_BASES == frozenset({'train','validation','test','cv_same_fold','nested_cv'})` directly. `references/finding-codes.md` lists `DSX-ML-052` HIGH, `DSX-ML-053` MEDIUM, `DSX-ML-090` HIGH, `DSX-ML-091` CRITICAL, `DSX-ML-092` HIGH — all present. `python -m unittest tests.test_dsx.TestPhase11_1ML -v`: 48/48 pass, covering blank/unknown/best_fold/misspelled score sources, the fold-spread margin comparison, and the test-set/same-fold selection-basis branches. |
| 6 | `prediction_time_definition` is checked even when `model.features` is absent, and an undeclared positive rate under an imbalance-unsafe primary metric fires. | VERIFIED | Read `dsx/checks/ml.py` directly: `_check_prediction_time_definition` is a standalone module-level function called from `check()`'s dispatch as a sibling of `_check_features`, no longer nested inside it. `python -m unittest tests.test_dsx.TestPhase11_1ML -v` passes, including `test_no_features_no_prediction_time_produces_both_030_and_033` and the `DSX-ML-043` undeclared-positive-rate branch tests. |

**Score:** 6/6 roadmap success criteria verified directly against running code (no truth left
behavior-unverified at the roadmap level — the one abstention below is a plan-level `must_have`,
not a roadmap SC).

### Plan-Level Must-Haves (spot-verified per plan)

Every one of the 8 plans' `must_haves.truths` blocks was read in full; representative and
highest-risk truths per plan were independently re-executed (not merely trusted from SUMMARY.md).
All passed except one explicit `verification: backstop` item.

| Plan | Requirement | Spot-verification performed | Result |
|---|---|---|---|
| 11.1-01 | REQ-P11.1-01 | Ran `TestPhase11_1Code` (41 tests); reproduced `FULL_FRAME_IMPUTE_RE`/`FULL_FRAME_SPREAD_FILTER_RE`/`FIT_CALL_RE` matches directly; confirmed `TRAINING_FRAME_NAMES` has 14 members; confirmed `_D05_ALLOWLIST_CODES` contains both codes and `_D05_ALLOWLIST_PREFIXES` unchanged. | VERIFIED (ReDoS finding noted separately, see Anti-Patterns) |
| 11.1-02 | REQ-P11.1-08 | Ran the plan's own AST-style assertions directly against `references/leakage-taxonomy.md` and `agents/dsx-ml-integrity-auditor.md`: ensemble/stacking/voting/"type 7" all present; discretisation note present; 8 numbered leakage-heuristics signals present; no finding code named in either file. | VERIFIED |
| 11.1-03 | REQ-P11.1-02 | Ran `TestPhase11_1Code` (41 tests, includes this plan's 14); confirmed `STAT_TEST_CALL_RE` matches `chi2_contingency`/`ttest_ind`; confirmed catalogue rows for `DSX-CODE-030`/`031` at the locked severities. | VERIFIED |
| 11.1-04 | REQ-P11.1-04 | Ran `TestPhase11_1ML` (48 tests, includes this plan's 17); confirmed `_check_prediction_time_definition` extracted with `prediction_time_definition` absent from `_check_features`'s source. | VERIFIED |
| 11.1-05 | REQ-P11.1-03 | Ran `TestPhase11_1MLCleaning` (15 tests); ran the plan's own leaky/contradiction/no-model-section scenario directly through `ml.check()`, confirmed `{'DSX-ML-023','DSX-ML-024'}` both present. | VERIFIED |
| 11.1-06 | REQ-P11.1-05 | Confirmed `SCORE_SOURCES` vocabulary locked; confirmed catalogue rows for `DSX-ML-052`/`053`. | VERIFIED |
| 11.1-07 | REQ-P11.1-06 | Confirmed `SELECTION_BASES` vocabulary locked; read `_check_selection_ledger` source directly — confirmed the three codes are structurally mutually exclusive per run. | 8/9 truths VERIFIED; 1 truth (`verification: backstop`, emission-order-under-simultaneous-conditions) abstained — see Human Verification |
| 11.1-08 | REQ-P11.1-07, REQ-P11.1-08 | Ran `python -m unittest tests.test_known_bad_corpus -v` (30/30 pass); ran `dsx gate execute`/`plan` against the fixture directly with a real temp phase-dir and seeded entrypoint, confirming the measured exit codes in the SUMMARY match a fresh run. | VERIFIED |

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `dsx/checks/code.py` | `DSX-CODE-020/021/030/031`, `TRAINING_FRAME_NAMES`, `FIT_CALL_RE`, `STAT_TEST_CALL_RE` | VERIFIED | All symbols present, imported, and exercised by 41 passing tests; confirmed by direct `import` and `inspect.getsource`. |
| `dsx/checks/ml.py` | `DSX-ML-023/024/043/052/053/090/091/092`, `TRAIN_ONLY_FIT_VALUES`, `SCORE_SOURCES`, `SELECTION_BASES`, `_check_cleaning`, `_check_selection_ledger`, `_check_prediction_time_definition` | VERIFIED | All symbols present, wired into `check()`'s dispatch (confirmed via `inspect.getsource(ml.check)`), exercised by 48 passing tests. |
| `dsx/decisions.py` | `record_decision()` shared write path | VERIFIED | Present; used by both `dsx/checks/code.py` and `dsx/checks/ml.py` without violating `test_no_check_module_appends_to_a_decisions_list` (confirmed passing in the full suite). |
| `templates/ANALYSIS-SPEC.yaml` | Cleaning scaffold, results provenance fields, selection-ledger scaffold — all inert | VERIFIED | `dsx gate plan --spec templates/ANALYSIS-SPEC.yaml` exits 0 (re-run directly this pass, not only trusted from SUMMARY). |
| `examples/known-bad/full-frame-cleaning-{ANALYSIS-SPEC.yaml,POSTMORTEM.md,entrypoint.py}` | Corpus fixture triple | VERIFIED | All three files exist; entrypoint idiom order confirmed by direct read; post-mortem names finding codes; fixture blocks execute (exit 1) and clears plan (exit 0), reproduced directly. |
| `scripts/gen-finding-catalogue.py` / `references/finding-codes.md` | 12 new codes, all citation-enforced, catalogue current | VERIFIED | `python scripts/gen-finding-catalogue.py --check` exits 0 (re-run directly). All 12 codes' rows confirmed present in `references/finding-codes.md` at their locked severities. |
| `tests/test_known_bad_corpus.py` | `_seed_entrypoint`, multi-code `_TARGET_DEFECT_CODES`/`_own_target_codes` | VERIFIED | 30/30 tests pass in this module, re-run directly. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `dsx/checks/code.py` new patterns | regex engine on untrusted entrypoint text | direct pattern application | PARTIALLY WIRED — functionally correct, but 2 of 3 new patterns are not linear | `FIT_CALL_RE` carries and passes its own timing test. `FULL_FRAME_SPREAD_FILTER_RE` (cubic) and `FULL_FRAME_IMPUTE_RE` (quadratic) do not, despite the module's own threat-model comment claiming linearity for both — reproduced directly (400 chars -> 0.17s). See Anti-Patterns Found. |
| `TRAIN_ONLY_FIT_VALUES` | both `_check_preprocessing` (whole-pipeline) and `_check_cleaning` (per-step) | shared frozenset | WIRED | Confirmed via `inspect.getsource(ml._check_preprocessing)` containing `TRAIN_ONLY_FIT_VALUES`. |
| `_D05_ALLOWLIST_CODES` | 12 new codes' docstrings | citation + structural-criterion + test marker | WIRED | `gen-finding-catalogue.py --check` exits 0 — this check is exactly the enforcement mechanism and it passed on a fresh run. |
| `examples/known-bad/full-frame-cleaning-*` | `tests/test_known_bad_corpus.py::_TARGET_DEFECT_CODES` | catch-attribution map | WIRED | Confirmed `full-frame-cleaning` entry present with `execute -> {DSX-CODE-020, DSX-CODE-021, DSX-CODE-030}`; dedicated positive-direction test passes. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| REQ-P11.1-01 | 11.1-01 | Entrypoint scan fires on full-frame pandas cleaning idioms + non-training-frame fit after split | SATISFIED | `DSX-CODE-020`/`021` confirmed firing directly; 27 dedicated tests pass. |
| REQ-P11.1-02 | 11.1-03 | Statistical-test call referencing the declared target fires | SATISFIED | `DSX-CODE-030`/`031` confirmed firing directly; 14 dedicated tests pass. |
| REQ-P11.1-03 | 11.1-05 | Optional per-dataset cleaning declaration; fitted-outside-training CRITICAL; contradiction HIGH; absence fires nothing | SATISFIED | `DSX-ML-023`/`024` confirmed firing directly; 15 dedicated tests pass. |
| REQ-P11.1-04 | 11.1-04 | `prediction_time_definition` checked unconditionally; undeclared positive rate under imbalance-unsafe metric fires | SATISFIED | Extraction confirmed by source read; `DSX-ML-033`/`043` behavior confirmed by 17 dedicated tests. |
| REQ-P11.1-05 | 11.1-06 | Result scores carry source + fold spread; blank/unknown/best-fold HIGH; margin-below-spread advisory | SATISFIED | `DSX-ML-052`/`053` confirmed; 16 dedicated tests pass. |
| REQ-P11.1-06 | 11.1-07 | Selection ledger demanded when algorithm declared; test-set selection CRITICAL; same-fold HIGH | SATISFIED (1 must_have abstained, see Human Verification) | `DSX-ML-090`/`091`/`092` confirmed; 20 dedicated tests pass; emission-order backstop truth flagged for human review. |
| REQ-P11.1-07 | 11.1-08 | Known-bad corpus case with catch-attribution, blocked by new checks | SATISFIED | Fixture triple committed and reproduced directly blocking execute. |
| REQ-P11.1-08 | 11.1-02, 11.1-08 | Leakage-taxonomy ensemble sub-case + discretisation note; auditor prompt signals; fixtures/tests/catalogue current | SATISFIED | Both documentation additions confirmed present; catalogue current confirmed by fresh `--check` run. |

No orphaned requirements: `.planning/REQUIREMENTS.md`'s Phase 11.1 section lists exactly
REQ-P11.1-01 through -08, and every plan's `requirements:` frontmatter field, together with its
paired SUMMARY's `requirements-completed:` field, accounts for all 8 with no duplicates and no gaps.

**Tracking-file currency note (not a phase-goal gap):** `.planning/REQUIREMENTS.md`'s traceability
table still marks REQ-P11.1-02 through -08 as "Pending" (only REQ-P11.1-01 is marked "Complete"),
and `.planning/STATE.md` still reads "Executing Phase 11.1." This is expected orchestrator
bookkeeping that runs after a phase passes verification, not a defect in the shipped work — but it
should not be read as a claim that this phase is unfinished. Confirmed by reading both files
directly rather than assuming from the SUMMARYs.

### Anti-Patterns Found

Found by directly re-running the checks in the committed `11.1-REVIEW.md` (committed 2026-08-20),
not merely quoted from it. All four CRITICAL findings and both WARNING findings reproduced
independently in this pass.

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `dsx/checks/code.py` | 64-66 | `FULL_FRAME_SPREAD_FILTER_RE` is cubic in line length (`re.search("x"*400)` measured 0.17s in this pass, consistent with the review's 0.21s) despite the module's own docstring claiming linear scanning | CRITICAL (reproduced) | A single long non-matching line in an analyst entrypoint can hang `dsx check`/`dsx gate` for minutes — a real DoS risk on ordinary, not only adversarial, input. |
| `dsx/checks/code.py` | 61-63 | `FULL_FRAME_IMPUTE_RE` is quadratic in line length, same root cause | CRITICAL (reproduced) | Same DoS class, lower urgency (needs a longer line to reach multi-second hangs). |
| `dsx/checks/code.py` | 492-507 | `_first_fit_leak_line`'s redundant literal re-check (`"fit(" in line`) requires the parenthesis immediately after "fit", so `model.fit (df)` (one extra space) bypasses `DSX-CODE-001` entirely | CRITICAL (reproduced: `code_mod._first_fit_leak_line(['model.fit (df)'])` returns `None`) | This is a pre-existing check (`DSX-CODE-001`), not introduced by Phase 11.1 and explicitly out of scope for plan 11.1-01 ("Do not change ... `_first_fit_leak_line` ... in this task"). Flagged here because the review surfaced it while auditing this phase's code, not because this phase caused it. |
| `dsx/checks/code.py` | 90-92 | `FIT_CALL_RE` requires the captured token to start immediately after `(`, so a keyword-argument call (`model.fit(X=data, y=target)`) never matches — `DSX-CODE-021` (new in this phase) is invisible to it | CRITICAL (reproduced: `FIT_CALL_RE.search("model.fit(X=data, y=target)")` returns `None`) | Directly relevant to this phase's own threat model — keyword arguments are idiomatic in LLM/agent-generated pipelines. Does not affect the declared corpus fixture (uses bracket syntax) or falsify any must_have as literally worded (which describe bare-token calls). |
| `dsx/checks/code.py` | 527-548 | `_fit_call_arguments` uses `.search()` not `.finditer()`, so a semicolon-joined line with two fit calls only ever yields the first | WARNING (reproduced) | A narrower gap than CR-04; the project's own test suite already constructs semicolon-joined lines for other purposes, so the pattern is expected in this corpus. |
| `dsx/decisions.py` | 111-119 | `append()` opens the file in default text mode with no `newline=` argument, so on Windows it writes `\r\n`, contradicting the module's own documented "single `\n`" contract | WARNING (confirmed by direct source read; no `newline=` argument present) | Silently fine today only because `Path.read_text()` also does universal-newline translation; a future non-Python consumer would see the wrong bytes. |
| `dsx/checks/code.py` | 23-33 | `SPLIT_MARKERS` carries a dead, unreachable `"TimeSeriesSplit("` entry (prefix of `"TimeSeriesSplit"`, already listed) | INFO | Cosmetic; no functional impact. |

No `TBD`/`FIXME`/`XXX` debt markers found in any file this phase touched (`dsx/checks/code.py`,
`dsx/checks/ml.py`, `dsx/decisions.py`, `scripts/gen-finding-catalogue.py` — checked directly).

### Prohibitions Disposition

Every plan in this phase carries `must_haves.prohibitions` entries, all marked `flagged: true`
(judgment-tier, not machine-enforced by a dedicated test in every case). Reporting their
disposition honestly rather than silently passing them:

- **"MUST NOT add `DSX-CODE`/`DSX-ML` family prefixes to `_D05_ALLOWLIST_PREFIXES`"** (all 8 plans) — machine-verified directly in this pass: `_D05_ALLOWLIST_PREFIXES` contains no `DSX-CODE`/`DSX-ML` prefix (re-ran the plans' own assertion). Not violated.
- **"MUST NOT weaken/delete/rewrite an existing corpus expectation"** (all 8 plans) — corroborated by the full suite staying green (804/804), `tests/test_known_bad_corpus.py` passing 30/30 with no pre-existing fixture's exit code moved, and `git diff`-style claims in each SUMMARY. Not independently re-diffed against every historical commit by this verifier, but no contradicting evidence found.
- **"MUST NOT let a misspelled/out-of-vocabulary value clear a gate that a blank value would have blocked"** (plans 01, 03, 04, 05, 06, 07, 08) — spot-verified directly for `fit_on` (plan 05), `model_score_source` (plan 06), and `selected_on` (plan 07): all three vocabularies fold an unrecognised value into the same failing branch as a blank one. Not violated where checked.
- **"MUST NOT ship a docstring citation whose locator was not read from the paper itself"** (plans 01, 02, 03, 05) — every docstring inspected in this pass (`_check_selection_ledger`, `check()` in `code.py`) carries an explicit `UNVERIFIED` marker on unconfirmed locators rather than a fabricated one. This verifier did not independently re-read the five cited papers (Kaufman et al. 2012, Varma & Simon 2006, Saito & Rehmsmeier 2015, Cawley & Talbot 2010) against the docstring text — this remains a judgment-tier, human-reviewable claim, consistent with its `flagged: true` disposition, not a verified-against-source fact.
- **Plan-specific prohibitions** (e.g. "MUST NOT add the cleaning block to a required-fields list", "MUST NOT make the corpus fixture block on an unrelated second defect") — spot-verified per plan where a direct check was cheap (e.g. confirmed `templates/ANALYSIS-SPEC.yaml`'s scaffolds are commented/null and inert at `plan`). Not violated where checked.

No prohibition was found violated in this pass. The citation-authenticity prohibitions remain
judgment-tier and unconfirmed against primary sources by this verifier, consistent with an
LLM-judge, non-authoritative disposition — flagged here rather than silently passed.

### Human Verification Required

#### 1. Plan 11.1-07's backstop must_have on emission-order stability

**Test:** Determine whether "DSX-ML-090, DSX-ML-091 and DSX-ML-092 emitted in a stable order when more than one selection-ledger condition holds at once" is even a reachable state given `_check_selection_ledger`'s current structure (an incomplete ledger returns immediately after emitting DSX-ML-090; a complete ledger branches to exactly one of DSX-ML-091/092/neither).
**Expected:** A human decision: either confirm the truth is vacuously satisfied and retire/reword it, or add an explicit regression test pinning the mutual-exclusivity invariant so a future edit to this function cannot silently break it.
**Why human:** Marked `verification: backstop` in the plan's own frontmatter — per the honest-verifier contract this is a non-inferable truth a verifier must abstain on rather than pass by inference from source-reading alone.

#### 2. Remediation priority for the four reproduced CRITICAL code-review findings

**Test:** Decide whether `dsx/checks/code.py`'s two ReDoS-class regex patterns (CR-01, CR-02) and the keyword-argument blind spot in the new `DSX-CODE-021` detector (CR-04) need a fix before Phase 11.2/11.3 proceed, or are accepted as tracked follow-up debt.
**Expected:** A human decision on scope and urgency — none of the three block this phase's stated must_haves or roadmap success criteria as literally worded, but all three are reproduced, real, and directly relevant to this phase's own "generated-pipeline reality" threat model.
**Why human:** This is a scope/priority call (fix now vs. track as debt), not something a verifier can resolve unilaterally — the phase's own goal is achieved for the declared corpus case either way, but shipped-code robustness against the broader class of LLM-generated idioms is weaker than the module's own threat-model comments claim.

### Gaps Summary

No must_have truth, roadmap success criterion, or requirement failed. All 6 roadmap success
criteria and all 8 requirement IDs are satisfied by code that was independently re-executed in
this pass (804/804 tests green, `./scripts/check.sh` green, the good/bad fixture gate contract
unchanged at all four gate points, the new corpus fixture blocking execute exactly as claimed).

Status is `human_needed`, not `passed`, for two reasons that do not indicate the phase goal was
missed: (1) one plan-level `must_have` is explicitly marked `verification: backstop` and — per the
honest-verifier contract — must be routed to a human rather than silently passed on inference; and
(2) a committed, adversarial code review (`11.1-REVIEW.md`) found four CRITICAL and two WARNING
issues in the freshly-shipped detection code, all four CRITICALs independently reproduced in this
pass, that do not falsify any must_have as literally worded but do represent real robustness gaps
worth a human remediation decision before the next phase builds on this one.

---

_Verified: 2026-08-20_
_Verifier: Claude (gsd-verifier)_
