---
phase: 11.1
slug: generated-pipeline-reality
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-20
---

# Phase 11.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Python stdlib `unittest` (no pytest config in repo) |
| **Config file** | none — `tests/` discovered via `unittest discover -s tests` |
| **Quick run command** | `python3 -m unittest tests.test_dsx.TestPhase3Code tests.test_dsx.TestML -v` |
| **Full suite command** | `python3 -m unittest discover -s tests -v` (plus `./scripts/check.sh` for the complete gate) |
| **Estimated runtime** | ~30 seconds (targeted classes); full gate via `scripts/check.sh` |

---

## Sampling Rate

- **After every task commit:** Run `python3 -m unittest tests.test_dsx.TestPhase3Code tests.test_dsx.TestML -v`
- **After every plan wave:** Run `python3 -m unittest discover -s tests -v` + `python3 scripts/gen-finding-catalogue.py --check`
- **Before `/gsd-verify-work`:** Full suite must be green and `./scripts/check.sh` green (unit tests, catalogue currency, capability manifest, good/bad fixture gate-contract, determinism)
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| *(seeded at plan time — see RESEARCH.md `## Validation Architecture` requirement→test map)* | | | | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] New test methods in `tests/test_dsx.py::TestPhase3Code` for REQ-P11.1-01/02 (full-frame cleaning idioms, fit-after-split-wrong-frame, stat-test-sees-target)
- [ ] New test methods in `tests/test_dsx.py::TestML` (or a new `TestPhase11_1ML` class) for REQ-P11.1-03/04/05/06
- [ ] `examples/known-bad/full-frame-cleaning-*` fixture triple (spec + post-mortem + entrypoint) — REQ-P11.1-07
- [ ] `tests/test_known_bad_corpus.py::_TARGET_DEFECT_CODES` entry for the new fixture (catch-attribution data only; harness regex already generic)
- [ ] `_D05_ALLOWLIST_CODES` extension in `scripts/gen-finding-catalogue.py` for every new code, each with docstring `Citation:`/`Reference value:`/`Structural criterion:` lines and a `# D-05: <code>` test marker
- [ ] `references/finding-codes.md` regeneration (`python3 scripts/gen-finding-catalogue.py --write`)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| ml-integrity-auditor agent prompt additions (motivating-statistic question, large-roster heuristic) read as intended | REQ-P11.1-08 | Agent-prompt prose has no automated semantic test beyond the corpus harness | Read `agents/dsx-ml-integrity-auditor.md` diff; confirm both additions present and coherent |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
