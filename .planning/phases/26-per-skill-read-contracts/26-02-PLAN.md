---
phase: 26-per-skill-read-contracts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/dsx-build-model/SKILL.md
  - skills/dsx-narrate/SKILL.md
autonomous: true
requirements:
  - REQ-P26-01
tags:
  - skill-contracts
  - repo-integrity
  - eda
must_haves:
  truths:
    - "dsx-build-model and dsx-narrate each open with exactly one <inputs> block per D-26-01/D-26-02; narrate's block sits between </objective> and <precondition> (D-26-01's explicit narrate exception)."
    - "build-model names columns[].categorical as an intermediate-node key (D-26-02); the block lists one backtick-quoted key per bullet line."
    - "Each block's When absent line names the eda_artifact: none tier first, then the DATA-PROFILE fallback, then the computed_by declaration (D-26-03)."
    - "build-model carries an Also consult line for section 4 Wide categoricals containing zero backticks; narrate omits the Also consult line (D-26-02 prose refs = none for narrate)."
    - "Every backtick-quoted key written in these two blocks resolves verbatim against templates/EDA.md front-matter or templates/DATA-PROFILE.yaml (including uncommented example keys); zero new keys are invented."
  artifacts:
    - skills/dsx-build-model/SKILL.md
    - skills/dsx-narrate/SKILL.md
  key_links:
    - "build-model's columns[].categorical is an intermediate map header, not a leaf; it resolves ONLY because the Wave-2 guard records every intermediate dotted path (D-26-04) — this is the enforcing link for that key."
    - "narrate's dataset / base_rate.* keys are the link between the profile and the population sentence and the base for relative percentages."
---

<objective>
Add the D-26-01 `<inputs>` read-step block to the heads of the remaining two of the five
downstream skills — `dsx-build-model` and `dsx-narrate` — each naming the exact EDA
front-matter and DATA-PROFILE keys it consumes (D-26-02), what each key changes in its
output, and the `eda_artifact: none` absent-artifact fallback (D-26-03). Together with
plan 26-01, this completes REQ-P26-01 across all five skills.

Purpose: `dsx-build-model` must start from the profile's leakage suspects, dependence
structure and categorical cardinality (so leakage prevention is designed in, not audited
for); `dsx-narrate` must ground its population sentence and relative-percentage base in
measured numbers rather than restating a claim. Neither carries a read step today
(S0-2 premise 4, re-verified live).

Output: One `<inputs>` block inserted at each skill's head, byte-scoped to `skills/` only.

**Skill-only invariant (REQ-P26-03).** This plan edits only the two named `SKILL.md`
files. It MUST NOT touch anything under `dsx/`, MUST NOT edit `templates/EDA.md` or
`templates/DATA-PROFILE.yaml`, and MUST NOT mint any finding code. A genuinely needed fact
with no existing template key is a scope escalation — stop and surface it, do not invent a key.

**Single-writer prohibition.** The executor MUST NOT edit `.planning/REQUIREMENTS.md`,
`.planning/STATE.md`, or `.planning/ROADMAP.md`. Report status back to the orchestrator instead.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-per-skill-read-contracts/26-CONTEXT.md
@.planning/phases/26-per-skill-read-contracts/26-RESEARCH.md
@capabilities/dsx/fragments/executor.md
@templates/EDA.md
@templates/DATA-PROFILE.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the &lt;inputs&gt; read step to dsx-build-model</name>
  <files>skills/dsx-build-model/SKILL.md</files>
  <read_first>
    - skills/dsx-build-model/SKILL.md (insert point is between `</objective>` on line 18 and `<order_of_operations>` on line 20)
    - .planning/phases/26-per-skill-read-contracts/26-CONTEXT.md (D-26-01 skeleton; D-26-02 mapping row for build-model, including the section 4 Wide categoricals Also-consult ref; D-26-03 pattern)
    - .planning/phases/26-per-skill-read-contracts/26-RESEARCH.md (Pattern 2 table — note columns[].categorical is an INTERMEDIATE node resolving only via full dotted-path recording; time.column/time.max_gap_days are LIVE top-level, not commented; policy_recommendation is prose-only at EDA.md lines 143-145)
    - capabilities/dsx/fragments/executor.md (three-tier ladder)
    - templates/EDA.md and templates/DATA-PROFILE.yaml
  </read_first>
  <action>
    Insert exactly one `<inputs>...</inputs>` block between `</objective>` and
    `<order_of_operations>`, per the D-26-01 shape. Under "EDA front-matter keys read:" list
    one bullet each for `leakage_suspects[]`, `grain.implied_dependence.structure`,
    `grain.implied_dependence.cluster_var`, `segments_candidates[]` (D-26-02). Under
    "DATA-PROFILE keys read (the fallback source when EDA is absent):" list one bullet each
    for `columns[].n_unique`, `columns[].dtype`, `columns[].categorical`, `unit.rows_per_unit`,
    `time.column`, `time.max_gap_days`. Write a "These set:" prose line covering
    `model.features_excluded_for_leakage`, the split type (temporal / grouped /
    `grouped_temporal`), the `entity_column`, and the encoding policy. INCLUDE an "Also consult
    (EDA prose, not front-matter):" line pointing to section 4 Wide categoricals — the policy
    recommendation informing encoding — written as plain text with ZERO backticks (D-26-02).
    Author the "When absent:" line per D-26-03's three-tier ladder scoped to build-model:
    record `eda_artifact: none`, source leakage-suspect / dependence / cardinality facts from
    the DATA-PROFILE keys above, and where the profile is also absent declare each with
    `computed_by` honesty rather than asserting a clean split. One backtick token per key
    bullet; write `columns[].categorical` with its full dotted path exactly.
  </action>
  <verify>
    <automated>python -c "import io,re; t=io.open('skills/dsx-build-model/SKILL.md',encoding='utf-8').read(); m=re.search('<inputs>(.*?)</inputs>',t,re.S); assert m, 'no inputs block'; s=m.group(1); assert all(k in s for k in ['EDA front-matter keys read:','DATA-PROFILE keys read','eda_artifact: none','leakage_suspects[]','segments_candidates[]','columns[].categorical','time.max_gap_days','Also consult']); ac=[l for l in s.splitlines() if l.strip().startswith('Also consult')][0]; assert '\`' not in ac, 'Also consult line must have zero backticks'"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one `<inputs>...</inputs>` block exists, located between `</objective>` and `<order_of_operations>`.
    - The four EDA keys and six DATA-PROFILE keys above appear, one backtick key per bullet.
    - `columns[].categorical` appears with its full intermediate dotted path.
    - An "Also consult" line names section 4 Wide categoricals with zero backtick characters.
    - The `When absent:` line contains `eda_artifact: none` and names the DATA-PROFILE and `computed_by` tiers in order.
    - `git diff --stat -- dsx/ templates/` shows no change.
  </acceptance_criteria>
  <done>dsx-build-model opens with a grounded read step wiring the profile's leakage suspects and cardinality into the split and encoding decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Add the &lt;inputs&gt; read step to dsx-narrate</name>
  <files>skills/dsx-narrate/SKILL.md</files>
  <read_first>
    - skills/dsx-narrate/SKILL.md (insert point is between `</objective>` on line 19 and `<precondition>` on line 21 — the D-26-01 narrate exception)
    - .planning/phases/26-per-skill-read-contracts/26-CONTEXT.md (D-26-01 skeleton and its narrate placement exception; D-26-02 mapping row for narrate; D-26-03 pattern)
    - .planning/phases/26-per-skill-read-contracts/26-RESEARCH.md (Pattern 2 table confirming dataset/base_rate.*/segments_candidates[]/comparisons_looked_at/artifact_status resolve in EDA.md, and row_count/time.min/time.max are LIVE top-level DATA-PROFILE keys while target.overall is a commented example key)
    - capabilities/dsx/fragments/executor.md (three-tier ladder)
    - templates/EDA.md and templates/DATA-PROFILE.yaml
  </read_first>
  <action>
    Insert exactly one `<inputs>...</inputs>` block between `</objective>` and `<precondition>`,
    per the D-26-01 shape. Under "EDA front-matter keys read:" list one bullet each for
    `dataset`, `base_rate.overall`, `base_rate.metric`, `segments_candidates[]`,
    `comparisons_looked_at`, `artifact_status` (D-26-02). Under "DATA-PROFILE keys read (the
    fallback source when EDA is absent):" list one bullet each for `row_count`, `time.min`,
    `time.max`, `target.overall`. Write a "These set:" prose line covering the population
    sentence, the base for relative percentages, and the "what would change this" section.
    OMIT the "Also consult" line (D-26-02 prose refs = none for narrate). Author the "When
    absent:" line per D-26-03's three-tier ladder scoped to narrate: record `eda_artifact:
    none`, source population / base-rate / window facts from the DATA-PROFILE keys above, and
    where the profile is also absent declare each with `computed_by` honesty rather than
    narrating a base it cannot substantiate. One backtick token per key bullet.
  </action>
  <verify>
    <automated>python -c "import io,re; t=io.open('skills/dsx-narrate/SKILL.md',encoding='utf-8').read(); m=re.search('<inputs>(.*?)</inputs>',t,re.S); assert m, 'no inputs block'; s=m.group(1); assert all(k in s for k in ['EDA front-matter keys read:','DATA-PROFILE keys read','eda_artifact: none','dataset','base_rate.overall','comparisons_looked_at','row_count','time.min','time.max']); assert '<precondition>' in t and t.index('<inputs>') < t.index('<precondition>')"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one `<inputs>...</inputs>` block exists, located between `</objective>` and `<precondition>` (before `<precondition>`).
    - The six EDA keys and four DATA-PROFILE keys above appear, one backtick key per bullet.
    - The `When absent:` line contains `eda_artifact: none` and names the DATA-PROFILE and `computed_by` tiers in order.
    - No "Also consult" line is present. `git diff --stat -- dsx/ templates/` shows no change.
  </acceptance_criteria>
  <done>dsx-narrate opens with a grounded read step so the population sentence and relative-percentage base come from measured numbers, not a restated claim.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates NO new files and NO new symbols — it adds one `<inputs>` documentation
block to the head of each of two existing skill files:

- `skills/dsx-build-model/SKILL.md` — new `<inputs>` block (4 EDA keys incl. two list-of-map keys, 6 DATA-PROFILE keys incl. the intermediate-node key `columns[].categorical`, `eda_artifact: none` fallback, one Also-consult line for section 4 with zero backticks)
- `skills/dsx-narrate/SKILL.md` — new `<inputs>` block (6 EDA keys, 4 DATA-PROFILE keys, `eda_artifact: none` fallback, no Also-consult line), inserted before `<precondition>`

No `dsx/` symbols, no template keys, no finding codes are created. These `<inputs>` blocks
are the newly-created symbols the Wave-2 source-grounding drift pass should exclude.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template key set → skill read step | build-model / narrate name keys; an orphaned key silently breaks the read contract. Enforced by the Wave-2 guard. |
| intermediate node → dotted-path guard | `columns[].categorical` is a map header, not a leaf; it resolves only if the guard records intermediate paths (D-26-04). |
| skill prose → prose-only reference | The wide-categorical policy recommendation is EDA prose, not a front-matter key; naming it as a backtick key would create a false contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-02 | Tampering | `<inputs>` blocks in build-model and narrate | high | mitigate | Every backtick key is copied verbatim from the live templates (RESEARCH Pattern 2 table); the Wave-2 guard fails loudly on any orphaned key naming skill+region+key. |
| T-26-05 | Tampering | build-model `columns[].categorical` intermediate key | medium | mitigate | The key is written with its full dotted path; the Wave-2 guard records every intermediate dotted path and uses full-path matching so an unrelated leaf cannot satisfy it. |
| T-26-06 | Tampering | build-model Also-consult line | medium | mitigate | The policy-recommendation reference is zero-backtick prose; the guard asserts zero backticks on the Also-consult line. |
| T-26-04 | Tampering | `dsx/`, `templates/`, finding catalogue | high | mitigate | This plan edits only two `skills/*/SKILL.md` files; each task's acceptance criteria assert `git diff --stat -- dsx/ templates/` is empty; zero codes minted. |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs in this plan (RESEARCH Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- Per task: the python one-liner confirms the block structure, the representative keys
  (including the intermediate `columns[].categorical`), the narrate insertion order, and
  the build-model zero-backtick Also-consult line.
- After this plan lands, the authoritative membership check is the Wave-2 guard
  `python -m unittest tests.test_skill_read_contracts -v` (created in 26-03).
- Skill-only invariant: `git diff --stat -- dsx/ templates/` empty across both edits.
</verification>

<success_criteria>
- Both skills carry exactly one `<inputs>` block at the correct insertion point (narrate before `<precondition>`).
- Every backtick key resolves against the live templates (proven authoritatively by 26-03).
- Each `When absent:` line names the `eda_artifact: none` → DATA-PROFILE → `computed_by` ladder.
- `dsx/` and `templates/` byte-unchanged by this plan.
</success_criteria>

<output>
Create `.planning/phases/26-per-skill-read-contracts/26-02-SUMMARY.md` when done.
</output>
