---
phase: 26-per-skill-read-contracts
plan: 04
type: execute
wave: 3
depends_on:
  - 26-01
  - 26-02
  - 26-03
files_modified: []
autonomous: true
requirements:
  - REQ-P26-03
tags:
  - repo-integrity
  - installer
  - phase-close
must_haves:
  truths:
    - "git diff --stat -- dsx/ is empty for the phase: no dsx/ file changed byte-for-byte (skill-only invariant, REQ-P26-03)."
    - "Finding catalogue set-identity holds 276 -> 276: scripts/gen-finding-catalogue.py --check exits 0 and tests/test_finding_catalogue_invariant.py (_EXPECTED_TOTAL = 276) passes; zero new codes minted."
    - "Installed skill copies are re-synced by node install.mjs and node install.mjs --check passes, run as re-sync THEN check (not --check alone, which only verifies presence + self-test and would pass against a stale installed copy — RESEARCH Pitfall 3)."
    - "The full suite (unittest discover -s tests -q) is green on the real Python 3.12 interpreter, including the new tests/test_skill_read_contracts.py guard."
  artifacts: []
  key_links:
    - "node install.mjs is the link that re-copies skills/ into the runtime overlay; node install.mjs --check only verifies presence + self-test, so the two must run in sequence to prove the installed copies carry the new <inputs> blocks."
    - "git diff --stat -- dsx/ plus the catalogue invariant are the paired links proving the phase mutated nothing under dsx/ and minted no code."
---

<objective>
Close Phase 26: re-sync the installed skill copies into the runtime overlay and prove the
three skill-only invariants of REQ-P26-03 — `dsx/` byte-identical for the phase, finding
catalogue set-identity 276 -> 276 (zero new codes), and the full test suite green on the
real interpreter (including the new guard). This is the phase gate: it runs only after the
five skill edits (26-01/26-02) and the guard (26-03) have landed.

Purpose: The phase edits agent prose and adds one test; REQ-P26-03 requires proving it
changed nothing under `dsx/` and minted no code, and that the installed overlay actually
carries the new read steps. `--check` alone does not prove installed content is current
(it verifies presence + self-test, not file content — RESEARCH Pitfall 3), so the re-sync
step is mandatory, not optional.

Output: No new tracked files — this plan re-syncs the untracked runtime overlay and runs
read-only verification proofs. `files_modified` is intentionally empty.

**Skill-only invariant (REQ-P26-03).** This plan MUST NOT edit anything under `dsx/`, MUST
NOT edit the templates or the skills, and MUST NOT mint any finding code. If any proof shows
a `dsx/` byte change or a catalogue delta, STOP — that is an out-of-scope regression to be
reported, not worked around.

**Single-writer prohibition.** The executor MUST NOT edit `.planning/REQUIREMENTS.md`,
`.planning/STATE.md`, or `.planning/ROADMAP.md`. Report status back to the orchestrator instead.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-per-skill-read-contracts/26-CONTEXT.md
@.planning/phases/26-per-skill-read-contracts/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-sync the installed skill copies and verify the installer</name>
  <files>(runtime overlay — untracked; no repo-tracked file is modified)</files>
  <read_first>
    - .planning/phases/26-per-skill-read-contracts/26-RESEARCH.md (Pitfall 3: `--check` verifies presence + self-test only, not file content; the correct phase-end command is `node install.mjs && node install.mjs --check`, Phase 25 precedent)
    - .planning/phases/26-per-skill-read-contracts/26-CONTEXT.md (residual #6 — installer re-sync + `--check` after the five skill edits)
    - install.mjs (the `install()` copy mechanics and the `check()` presence + self-test semantics, if a detail is unclear)
  </read_first>
  <action>
    Run `node install.mjs` to re-copy `skills/` (now carrying the five new `<inputs>` blocks)
    into the runtime overlay, THEN run `node install.mjs --check` to verify presence and the
    self-test. Run them in that order as a single sequence — never `--check` alone. Confirm both
    exit 0 and that the five edited skills (dsx-scope-analysis, dsx-define-metrics,
    dsx-design-experiment, dsx-build-model, dsx-narrate) are among the synced skills. Do NOT
    modify `install.mjs` or any manifest. If the re-sync would write into a repo-tracked path
    or the self-test fails, STOP and report.
  </action>
  <verify>
    <automated>node install.mjs && node install.mjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `node install.mjs` exits 0 (re-sync completes) and `node install.mjs --check` exits 0 (presence + self-test pass).
    - The command is run as re-sync THEN check, not `--check` alone.
    - No repo-tracked file under `dsx/`, `templates/`, or `skills/` is modified by the re-sync (`git diff --stat -- dsx/ templates/ skills/` empty).
  </acceptance_criteria>
  <done>The installed overlay carries the five new read steps; the installer verifies clean.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the skill-only invariants (byte-identity, catalogue 276-&gt;276, full suite)</name>
  <files>(read-only verification — no file modified)</files>
  <read_first>
    - .planning/phases/26-per-skill-read-contracts/26-RESEARCH.md (Validation Architecture: the phase-gate command set; `_EXPECTED_TOTAL = 276`; `gen-finding-catalogue.py --check` exits 0 today with pre-existing unrelated warnings)
    - .planning/phases/26-per-skill-read-contracts/26-VALIDATION.md (Sampling Rate — the before-verify-work gate: full suite green AND catalogue --check exit 0 AND installer check AND dsx/ diff empty)
  </read_first>
  <action>
    Run the three REQ-P26-03 proofs, read-only, and confirm each: (1) `git diff --stat -- dsx/`
    produces empty output — `dsx/` is byte-identical for the phase. (2) `python
    scripts/gen-finding-catalogue.py --check` exits 0 and `python -m unittest
    tests.test_finding_catalogue_invariant -v` passes — the catalogue set-identity is 276 -> 276
    with zero new codes. (3) `python -m unittest discover -s tests -q` is green on the real
    Python 3.12 interpreter, including the new `tests/test_skill_read_contracts.py` guard. Use
    the real interpreter path from 26-RESEARCH.md's Environment Availability table if `python`
    does not resolve to 3.12. Record the pass/fail of each proof in the SUMMARY. Do NOT edit any
    file to make a proof pass — a failure here is a real regression to report.
  </action>
  <verify>
    <automated>python scripts/gen-finding-catalogue.py --check && python -m unittest tests.test_finding_catalogue_invariant -v && python -m unittest discover -s tests -q && git diff --stat -- dsx/</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- dsx/` prints no lines (empty) — `dsx/` byte-identical for the phase.
    - `python scripts/gen-finding-catalogue.py --check` exits 0 and `tests/test_finding_catalogue_invariant.py` passes (`_EXPECTED_TOTAL = 276`).
    - `python -m unittest discover -s tests -q` reports OK (full suite green), including `tests.test_skill_read_contracts`.
    - No finding code was minted (catalogue count unchanged 276 -> 276).
  </acceptance_criteria>
  <done>REQ-P26-03 proven: dsx/ byte-identical, catalogue 276 -> 276, full suite green including the new guard.</done>
</task>

</tasks>

<flagged_assumptions>
The spec-less edge probe returned one `unclassified` row for REQ-P26-03 (category
`unclassified`, "review manually") that could not be resolved to a deterministic edge
category and is therefore NOT converted to a machine check — recorded here per the
no-silent-drop rule rather than dismissed:

- **FA-26-01 (REQ-P26-03 unclassified edge).** Assumption: the phase's ONLY mutation
  outside `skills/` and `tests/` is the untracked installer overlay re-sync; no task
  legitimately touches a `dsx/` file, a template, or a finding code. If any executor step is
  found to modify a `dsx/` file or change the catalogue count, that is an out-of-scope
  regression and the phase must STOP and report — it is not a silently-absorbed edge. This
  assumption is enforced (not merely asserted) by Task 2's `git diff --stat -- dsx/` empty
  proof and the 276 -> 276 catalogue invariant.
</flagged_assumptions>

<artifacts_this_phase_produces>
This plan creates NO new files and NO new symbols. It re-syncs the untracked runtime overlay
(`node install.mjs`) and runs read-only verification proofs. No `dsx/` symbol, no template
key, no finding code, and no tracked repo file is created or modified by this plan.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo `skills/` → runtime overlay | `node install.mjs` copies skills into the overlay; a stale copy would pass `--check` while carrying old content. |
| phase edits → `dsx/` and catalogue | The skill-only invariant requires nothing under `dsx/` changed and no code minted; a silent drift here would break REQ-P26-03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-03 | Tampering | installer overlay (`install.mjs` re-sync) | medium | mitigate | Run `node install.mjs && node install.mjs --check` (re-sync THEN verify), never `--check` alone; confirm the five edited skills are among the synced skills. |
| T-26-04 | Tampering | `dsx/`, finding catalogue | high | mitigate | `git diff --stat -- dsx/` empty proof + `gen-finding-catalogue.py --check` exit 0 + `test_finding_catalogue_invariant.py` (276 -> 276); any delta stops the phase (FA-26-01). |
| T-26-07 | Tampering | full test suite on the real interpreter | medium | mitigate | `python -m unittest discover -s tests -q` must be green on real Python 3.12 (not the python3 stub), including the new guard, before `/gsd-verify-work`. |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs; `node install.mjs` runs an existing, unmodified installer against repo-local content only (RESEARCH Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- `node install.mjs && node install.mjs --check` exits 0 (re-sync then verify).
- `git diff --stat -- dsx/` empty; catalogue `--check` exit 0 and invariant test passes (276 -> 276).
- `python -m unittest discover -s tests -q` green on real Python 3.12, including the new guard.
- All proofs are read-only except the overlay re-sync; no repo-tracked file is modified.
</verification>

<success_criteria>
- Installed skill copies re-synced and `node install.mjs --check` passes.
- `dsx/` byte-identical for the phase; catalogue 276 -> 276; zero new codes.
- Full suite green on the real interpreter, including `tests/test_skill_read_contracts.py`.
</success_criteria>

<output>
Create `.planning/phases/26-per-skill-read-contracts/26-04-SUMMARY.md` when done.
</output>
