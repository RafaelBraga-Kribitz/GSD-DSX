---
phase: 14-compounding-and-data-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/dsx/learnings/README.md
  - docs/dsx/learnings/2026-08-28-join-fanout-inflates-additive-metrics.md
  - skills/dsx-scope-analysis/SKILL.md
  - capabilities/dsx/fragments/researcher.md
autonomous: true
requirements: [REQ-P14-01]
tags: [dsx, learnings, compounding-loop, plan-pre, scope-analysis, researcher]

must_haves:
  truths:
    - "docs/dsx/learnings/ exists and is not born empty: it holds a README.md that fixes the frontmatter key set (date, title, domain, question_type, tags[], metrics[], phase, source_spec, outcome, supersedes?) and the What/So What/Now What body shape, plus at least one real dated exemplar `2026-08-28-*.md` carrying every required key (REQ-P14-01 / D-02)."
    - "skills/dsx-scope-analysis/SKILL.md carries a plan:pre 'search dated learnings before framing' step that greps docs/dsx/learnings/ on the fixed frontmatter keys before scaffolding/spawning the architect, cites any prior contradicting result, records `searched, none found` when empty, and names gsd-extract-learnings as the producer of future dated files — using the already-granted Read/Grep/Glob/Bash tools, no new grant and no CLI (REQ-P14-01 / D-02)."
    - "capabilities/dsx/fragments/researcher.md points its existing 'Has this been analysed before?' framing question at docs/dsx/learnings/ in one line, so the research contract searches the dated files before concluding no prior analysis exists (REQ-P14-01 / D-02)."
  artifacts:
    - docs/dsx/learnings/README.md
    - docs/dsx/learnings/2026-08-28-join-fanout-inflates-additive-metrics.md
    - skills/dsx-scope-analysis/SKILL.md
    - capabilities/dsx/fragments/researcher.md
  key_links:
    - "The search step and the files must agree on the SAME fixed frontmatter key set, or the deterministic agent grep silently misses files — the README schema is the single source the search instruction cites (D-02)."
    - "The producer of future dated files is the existing gsd-extract-learnings skill (referenced, never edited); the read side is all REQ-P14-01 mandates, and this plan seeds one real dated exemplar so the directory is not born empty (D-02)."
  prohibitions:
    - statement: "Zero new DSX-* finding codes are minted; the catalogue stays exactly 256."
      status: enforced
      verification: "Every edit is markdown; the catalogue generator only walks dsx/**/*.py for report.add first-args (gen-finding-catalogue.py:226-243), so these artifacts are structurally incapable of minting a code. Certified phase-wide by plan 14-05 (set-identity diff)."
    - statement: "No edits to the deterministic dsx gate path — no path under dsx/ is in files_modified; no gate check is added for the learnings directory (that would mint a code, D-07)."
      status: enforced
      verification: "files_modified touches only docs/, skills/ and capabilities/fragments/; the search step is prompt guidance read by an agent, never a check."
    - statement: "No new tool grant and no dsx CLI subcommand — the search reuses dsx-scope-analysis's already-granted Read/Grep/Glob/Bash (D-02)."
      status: enforced
      verification: "the SKILL.md allowed-tools block is unchanged; the search step shells nothing under dsx/."
---

<objective>
Steal the Data Science Plugin's compounding loop (REQ-P14-01 / D-02): later sessions search dated
learnings before framing, the same loop the plugin's `/ds:plan` runs. Three markdown edits, all off
the gate path: (1) create `docs/dsx/learnings/` with a schema README and one real dated exemplar so
the directory is not born empty; (2) add a plan:pre "search dated learnings before framing" step to
`skills/dsx-scope-analysis/SKILL.md` — the loop-wired framing owner; (3) point
`capabilities/dsx/fragments/researcher.md`'s existing "Has this been analysed before?" question at
the new directory.

Purpose: a prior result that contradicts the current framing is worth more than another week of
modelling. The learnings directory is where those results accumulate, keyed on a fixed frontmatter
schema so the framing-time grep is deterministic, and the framing owner is where the search fires.

Output: `docs/dsx/learnings/README.md` (the schema), one dated exemplar
`docs/dsx/learnings/2026-08-28-join-fanout-inflates-additive-metrics.md`, the search step in
`skills/dsx-scope-analysis/SKILL.md`, and the one-line pointer in
`capabilities/dsx/fragments/researcher.md`.

Producer decision (D-02, planner's discretion): the producer of future dated files is the existing
`gsd-extract-learnings` skill, referenced not edited — it already harvests decisions, lessons,
patterns and surprises from completed-phase artifacts, and reusing it honours rigour > flexibility
(no new emit path minted in dsx-narrate, no new dsx/ surface). DSX supplies only the dated
`docs/dsx/learnings/` home, the fixed schema, and this seed exemplar; the READ side is all
REQ-P14-01 mandates and the schema stands regardless of producer.
</objective>

<artifacts_this_phase_produces>
This plan creates two files and edits two:
- `docs/dsx/learnings/README.md` (new — the fixed frontmatter schema + body shape + search contract)
- `docs/dsx/learnings/2026-08-28-join-fanout-inflates-additive-metrics.md` (new — one real dated exemplar)
- `skills/dsx-scope-analysis/SKILL.md` (edit — adds the plan:pre learnings-search step to `<process>`)
- `capabilities/dsx/fragments/researcher.md` (edit — one-line pointer at `docs/dsx/learnings/`)

It produces NO new files under `dsx/`, NO new symbols, NO new `DSX-*` finding codes, and adds NO
gate check. Zero file overlap with plans 14-02 (templates/DATA-DICTIONARY.md, dsx-explore-data),
14-03 (templates/DISCLOSURE-research.md, dsx-narrate) and 14-05 (tests/) — same wave (wave 1). The
`skills/dsx-scope-analysis/SKILL.md` `description` frontmatter is edited only by plan 14-04 in the
next wave, so no two same-wave plans touch this file.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-compounding-and-data-onboarding/14-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/dsx/learnings/ with the schema README and one real dated exemplar</name>
  <files>docs/dsx/learnings/README.md, docs/dsx/learnings/2026-08-28-join-fanout-inflates-additive-metrics.md</files>
  <read_first>
    - .planning/phases/14-compounding-and-data-onboarding/14-CONTEXT.md (D-02: the fixed frontmatter key set; date-in-filename so a plain sort is chronological; What/So What/Now What body; producer = gsd-extract-learnings referenced)
    - capabilities/dsx/fragments/researcher.md:5-22 (the reusable analytical learnings the exemplar draws from — grain/fan-out, "numbers that disagree", timezone; the exemplar restates a genuine house finding, it does not fabricate dataset numbers)
    - skills/dsx-explore-data/SKILL.md:115-128 (the fan-out matrix definition the exemplar's Now What cites: join_id | left | right | keys | type | rows_before | rows_after | fanout | left_match_rate | null_key_rate)
    - templates/EDA.md:1-15 (the house YAML-frontmatter + prose-evidence markdown shape both files mirror; the CRLF authoring note)
  </read_first>
  <action>
    Create the directory `docs/dsx/learnings/` and two files.

    First, `docs/dsx/learnings/README.md`: document the compounding loop and the FIXED frontmatter
    key set every dated file must carry, in this exact closed order — `date` (ISO, equal to the
    filename date), `title`, `domain` (a `dsx.domain` enum value: experimentation | machine_learning |
    business_intelligence | marketing_science | research), `question_type` (closed vocab:
    descriptive | diagnostic | predictive | causal | prescriptive), `tags` (list), `metrics` (list of
    metric names touched, so a prior metric definition is findable), `phase`, `source_spec` (path or
    `none`), `outcome` (the one-line compounding payload), and optional `supersedes`. State that the
    filename convention is `YYYY-MM-DD-<slug>.md` so a plain lexical sort is chronological, that the
    body follows the Phase-13 What / So What / Now What shape, and that the producer of future dated
    files is the existing `gsd-extract-learnings` skill at phase close-out (referenced, not owned by
    DSX). Add the repo CRLF authoring note (parsers reading this frontmatter must tolerate `\r\n` — use
    `\r?\n`). State plainly that these files are written and read, never gated — no `dsx` check reads
    them, so no finding code is minted.

    Second, `docs/dsx/learnings/2026-08-28-join-fanout-inflates-additive-metrics.md`: one REAL dated
    exemplar carrying every required frontmatter key. Set `date: 2026-08-28`, a `title`,
    `domain: business_intelligence`, `question_type: diagnostic`, `tags` including grain and
    join-fanout, `metrics: [revenue]` as the illustrative additive metric, `phase: 14`,
    `source_spec: none`, and a one-line `outcome`. Body in What / So What / Now What shape stating the
    genuine, reusable house finding (do NOT invent dataset-specific numbers): **What** — a fan-out
    other than 1.0 on a join declared one-to-one silently inflates every additive metric computed after
    the join; **So What** — a total that looks too high after a join is fan-out until proven otherwise,
    fixed by finding which side carries the duplicate grain, not by dividing the number down; **Now
    What** — before framing an analysis on joined sources, build the fan-out matrix and confirm
    `fanout == 1.0` on every declared one-to-one join, treating a mismatch as a blocker. Do not add any
    Python and do not reference any path under `dsx/checks/`.
  </action>
  <acceptance_criteria>
    - `docs/dsx/learnings/README.md` exists and names every fixed frontmatter key (date, title, domain, question_type, tags, metrics, phase, source_spec, outcome, supersedes) and the What/So What/Now What body shape (grep each returns a hit).
    - The README names `gsd-extract-learnings` as the producer and states the files are ungated (no dsx check reads them).
    - The dated exemplar `docs/dsx/learnings/2026-08-28-*.md` exists, opens with YAML frontmatter carrying `date:`, `domain:`, `question_type:`, `metrics:`, `phase:`, `source_spec:`, `outcome:`, and a body carrying What / So What / Now What.
    - Neither file references a path under `dsx/` and neither contains Python.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/dsx/learnings/README.md && for k in date title domain question_type tags metrics phase source_spec outcome supersedes; do grep -q "$k" docs/dsx/learnings/README.md || { echo "README missing key: $k"; exit 1; }; done && grep -Eiq "What ?/ ?So What ?/ ?Now What|So What|Now What" docs/dsx/learnings/README.md && grep -q "gsd-extract-learnings" docs/dsx/learnings/README.md && ls docs/dsx/learnings/2026-08-28-*.md >/dev/null 2>&1 && f="$(ls docs/dsx/learnings/2026-08-28-*.md | head -1)" && grep -Eq '^date:[[:space:]]*2026-08-28' "$f" && grep -Eq '^question_type:' "$f" && grep -Eq '^metrics:' "$f" && grep -Eq '^source_spec:' "$f" && grep -Eq '^outcome:' "$f" && grep -Eiq 'so what' "$f" && grep -Eiq 'now what' "$f" && ! grep -Eq 'dsx/checks/' "$f"</automated>
  </verify>
  <done>docs/dsx/learnings/ holds a schema README (fixed key set + What/So What/Now What + gsd-extract-learnings named as producer + ungated) and one real dated exemplar carrying every required frontmatter key with a What/So What/Now What body; neither file touches dsx/ or adds Python.</done>
</task>

<task type="auto">
  <name>Task 2: Add the plan:pre "search dated learnings before framing" step to dsx-scope-analysis</name>
  <files>skills/dsx-scope-analysis/SKILL.md</files>
  <read_first>
    - skills/dsx-scope-analysis/SKILL.md:1-49 (the frontmatter allowed-tools already grant Read/Grep/Glob/Bash — no new grant needed; the `<process>` list the search step joins, ahead of step 1 Scaffold and step 2 Spawn the architect)
    - docs/dsx/learnings/README.md (created in Task 1 — the schema the search greps; cite it as the authority for the key set)
    - .planning/phases/14-compounding-and-data-onboarding/14-CONTEXT.md (D-02: plan:pre prompt guidance in the framing owner; no new tool grant, no CLI; producer = gsd-extract-learnings)
  </read_first>
  <action>
    Edit `skills/dsx-scope-analysis/SKILL.md` to add a "search dated learnings before framing" step to
    the `<process>`, placed BEFORE scaffolding and before spawning `dsx-analysis-architect` (framing
    happens after the search, so a prior contradicting result reshapes the framing rather than arriving
    too late). The step instructs: grep `docs/dsx/learnings/` on the fixed frontmatter keys
    (`domain`, `question_type`, `metrics`, `tags`) for prior analyses of the same question, metric or
    domain; a plain filename sort is chronological, so the most recent prior result is last. Cite any
    prior result that contradicts or informs the current framing directly in the scope reasoning; when
    the directory yields nothing, record `searched dated learnings: none found` so the absence is a
    recorded result, not a skipped step. Name the schema authority as `docs/dsx/learnings/README.md`
    and name `gsd-extract-learnings` as the producer that writes these dated files at phase close-out.
    State that this search uses the skill's already-granted Read/Grep/Glob/Bash tools and adds no new
    tool grant, no `dsx` CLI subcommand, and no gate — it reads the files, it never gates them. Do NOT
    change the `allowed-tools` block, do NOT edit the `description` frontmatter (plan 14-04 owns that),
    and add no statistical threshold.
  </action>
  <acceptance_criteria>
    - `skills/dsx-scope-analysis/SKILL.md` references `docs/dsx/learnings` and a search/grep verb before framing (grep returns a hit).
    - The step cites the fixed frontmatter keys the grep uses and names `docs/dsx/learnings/README.md` as the schema authority.
    - The step records `searched dated learnings: none found` (or equivalent) for the empty case, and names `gsd-extract-learnings` as the producer.
    - The `allowed-tools:` block is unchanged (no new tool grant); the `description:` frontmatter line is unchanged; no path under `dsx/` is referenced by the new step.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "docs/dsx/learnings" skills/dsx-scope-analysis/SKILL.md && grep -Eiq "search(ed)? dated learnings|before framing" skills/dsx-scope-analysis/SKILL.md && grep -q "gsd-extract-learnings" skills/dsx-scope-analysis/SKILL.md && grep -Eq "question_type|metrics" skills/dsx-scope-analysis/SKILL.md && grep -Eq '^[[:space:]]*-[[:space:]]*Grep' skills/dsx-scope-analysis/SKILL.md && grep -Eq '^[[:space:]]*-[[:space:]]*Glob' skills/dsx-scope-analysis/SKILL.md</automated>
  </verify>
  <done>skills/dsx-scope-analysis/SKILL.md carries a plan:pre learnings-search step that greps docs/dsx/learnings/ on the fixed keys before framing, cites the README schema, names gsd-extract-learnings as producer, records the empty case, and adds no tool grant, no CLI and no gate.</done>
</task>

<task type="auto">
  <name>Task 3: Point the researcher fragment's prior-analysis question at docs/dsx/learnings/</name>
  <files>capabilities/dsx/fragments/researcher.md</files>
  <read_first>
    - capabilities/dsx/fragments/researcher.md:18-20 (the existing "Has this been analysed before? A prior result that contradicts yours is worth more than another week of modelling." bullet — the pointer attaches here)
    - .planning/phases/14-compounding-and-data-onboarding/14-CONTEXT.md (D-02: a one-line pointer in the fragment whose contract already asks "Has this been analysed before?")
  </read_first>
  <action>
    Edit `capabilities/dsx/fragments/researcher.md` to add ONE line to the existing "Has this been
    analysed before?" bullet pointing the check at `docs/dsx/learnings/`: before concluding no prior
    analysis exists, grep the dated files there on domain / question_type / metric. Keep it to one
    clause in the house terse style; add no new bullet section, no tool grant, no path under `dsx/`,
    and no statistical threshold. This is a pointer, not a restatement of the schema — the schema lives
    in `docs/dsx/learnings/README.md`.
  </action>
  <acceptance_criteria>
    - `capabilities/dsx/fragments/researcher.md` references `docs/dsx/learnings/` on the prior-analysis question (grep returns a hit).
    - The addition is one line/clause attached to the existing "analysed before" bullet, not a new section; no path under `dsx/` is referenced.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "docs/dsx/learnings" capabilities/dsx/fragments/researcher.md && grep -Eiq "analysed before|analyzed before" capabilities/dsx/fragments/researcher.md && ! grep -Eq 'dsx/checks/' capabilities/dsx/fragments/researcher.md</automated>
  </verify>
  <done>capabilities/dsx/fragments/researcher.md points its existing "Has this been analysed before?" question at docs/dsx/learnings/ in one line, with no new section, no tool grant and no dsx/ reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan creates two markdown files and edits two markdown files (one skill, one fragment). It
introduces no executable surface, no network or file input on any runtime gate path, no auth, and no
user-supplied input on any deterministic gate path. ASVS Level 1 injection / auth / session vectors
are N/A here — there is no exploitable surface to assess. No npm / pip / cargo install occurs, so the
supply-chain vector (T-14-SC) is N/A this plan.

| Boundary | Description |
|----------|-------------|
| scope skill -> docs/dsx/learnings/ | The search step only READS the dated files (grep) and cites them in framing prose; it mutates nothing and gates nothing. No new automated trust boundary is created (D-02). |
| learnings files -> dsx gate path | None. The files are analyst artifacts read by an agent; no `dsx` check opens them. Adding a check would mint a code — explicitly out of scope (D-07). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | A gate check added for the learnings directory (existence/schema scan), which would add a report.add in dsx/checks/* and mint a code | high | mitigate | D-07 write-then-ungated rule: the learnings files are written and read, never gated; files_modified touches no path under dsx/; certified phase-wide by plan 14-05's set-identity diff. |
| T-14-02 | Spoofing | The search grepping a key set the files do not actually carry, silently missing prior results | medium | mitigate | D-02 single-schema link: README.md fixes the key set; the search step cites it; Task 1's grep asserts the exemplar carries every key so the search and the files agree. |
| T-14-03 | Tampering | A new tool grant or a dsx CLI subcommand added to power the search, widening the gate-path/portability surface | medium | mitigate | D-02: the search reuses the already-granted Read/Grep/Glob/Bash; the verify asserts allowed-tools is unchanged; no dsx/ path is shelled. |
| T-14-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs occur in this phase. |
</threat_model>

<verification>
Phase-level goal-backward coverage (union across all five plans) is documented in plan 14-05. This
plan delivers Success Criterion 1 (docs/dsx/learnings/ holds dated YAML-frontmatter files; the
plan-pre path searches them before framing — REQ-P14-01).

For this plan specifically:
- `docs/dsx/learnings/README.md` fixes the frontmatter schema and body shape and names the producer.
- One real dated exemplar exists carrying every required key with a What/So What/Now What body.
- `skills/dsx-scope-analysis/SKILL.md` searches the directory before framing, with no new tool grant and no CLI.
- `capabilities/dsx/fragments/researcher.md` points its prior-analysis question at the directory.
- files_modified touches no path under `dsx/`; no finding code is minted (proved phase-wide by 14-05).
</verification>

<success_criteria>
- docs/dsx/learnings/ is not born empty: a schema README plus at least one real dated exemplar with the fixed frontmatter key set and a What/So What/Now What body (REQ-P14-01 / D-02).
- dsx-scope-analysis searches the dated learnings before framing, using its already-granted tools, no new grant, no CLI, no gate (REQ-P14-01 / D-02).
- researcher.md points its "analysed before?" question at the directory (REQ-P14-01 / D-02).
- No path under dsx/ is modified; no new finding code is minted; no gate check is added for the directory.
</success_criteria>

<output>
Create `.planning/phases/14-compounding-and-data-onboarding/14-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
