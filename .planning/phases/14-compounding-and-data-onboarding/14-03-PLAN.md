---
phase: 14-compounding-and-data-onboarding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/DISCLOSURE-research.md
  - skills/dsx-narrate/SKILL.md
autonomous: true
requirements: [REQ-P14-03]
tags: [dsx, disclosure, research-domain, narrate, template, opt-in, marketing-unchanged]

must_haves:
  truths:
    - "templates/DISCLOSURE-research.md exists as a GUIDE-LLM-derived template (AI-assisted steps / human-reviewed decisions / data handling / reproducibility) — NOT a third-party dependency — carrying a single stable disclosure heading, mirroring the templates/EDA.md write-then-ungated precedent (REQ-P14-03 / D-04)."
    - "skills/dsx-narrate/SKILL.md offers the disclosure block ONLY when `dsx.domain == research`, read via `node ~/.claude/gsd-core/bin/gsd-tools.cjs config-get dsx.domain` (Bash already granted, SKILL.md:5-14); the block is opt-in even under research (offer with a skip-with-reason, never impose); for the default `auto` and for `marketing_science` and every other value the narrative is byte-unchanged — no new section, no reordering (REQ-P14-03 / D-04)."
    - "The disclosure block inherits the exact rule the What/So What/Now What layer already declares (SKILL.md:36-44): it mints no new narrative code and adds no heading-scanner gate; the golden assertion is structural — the block is guarded on the literal `research` value, so for `dsx.domain != research` no disclosure heading is emitted by construction (REQ-P14-03 / D-04 / D-07)."
  artifacts:
    - templates/DISCLOSURE-research.md
    - skills/dsx-narrate/SKILL.md
  key_links:
    - "The trigger is the LITERAL `research` value only: `auto` (the default) never infers it, and marketing_science and every other value take today's path unchanged. Because dsx-narrate is a prompt skill, 'marketing default byte-unchanged' is a structural fact of the conditional guard, not a promise (D-04)."
    - "GUIDE-LLM is a TEMPLATE, not a dependency: templates/DISCLOSURE-research.md is authored in-repo from the GUIDE-LLM structure; nothing is installed and nothing is imported (D-04)."
  prohibitions:
    - statement: "Zero new DSX-* finding codes are minted; the catalogue stays exactly 256."
      status: enforced
      verification: "The template is markdown and the skill edit is prose; the catalogue generator only walks dsx/**/*.py (gen-finding-catalogue.py:226-243). Certified phase-wide by plan 14-05 (set-identity diff)."
    - statement: "No DSX-NAR mint and no disclosure heading-scanner gate — the block inherits the What/So What/Now What layer's declared no-new-code, no-heading-scanner rule (D-04/D-07)."
      status: enforced
      verification: "files_modified touches no path under dsx/; the disclosure block is prompt guidance guarded on a config read, not a check."
    - statement: "Marketing-domain (and every non-research value, incl. the default auto) narrative is byte-unchanged: the block is a conditional guarded on the literal research value, so the non-research path emits no new section and no disclosure heading (D-04)."
      status: enforced
      verification: "the disclosure step is wrapped in an explicit `dsx.domain == research` guard; the Task 2 verify asserts the guard and the literal research trigger; no reordering of the existing five sections."
---

<objective>
Add a research-domain AI-assistance disclosure (REQ-P14-03 / D-04): when `dsx.domain` is `research`,
`dsx-narrate` offers an optional disclosure block; the marketing-domain default is unchanged. Two
edits, both off the gate path and both additive: (1) ship `templates/DISCLOSURE-research.md`, a
GUIDE-LLM-derived template (not a third-party dependency), mirroring the `templates/EDA.md`
write-then-ungated precedent; (2) add an opt-in disclosure step to `skills/dsx-narrate/SKILL.md`
guarded on the literal `research` value.

Purpose: research readouts increasingly must disclose AI assistance; marketing-science readouts do
not, and must not silently gain a section. The block triggers only on the literal `dsx.domain ==
research` read — `auto` (the default) never infers it — so "marketing default byte-unchanged" is a
structural fact of the guard, not a promise. It is opt-in even under research (offer with a
skip-with-reason), so it can never become a gate.

Output: `templates/DISCLOSURE-research.md` (the template) and an opt-in, guarded disclosure step in
`skills/dsx-narrate/SKILL.md`.
</objective>

<artifacts_this_phase_produces>
This plan creates one file and edits one:
- `templates/DISCLOSURE-research.md` (new — GUIDE-LLM-derived disclosure template, single stable heading)
- `skills/dsx-narrate/SKILL.md` (edit — adds the opt-in disclosure step guarded on `dsx.domain == research`)

It produces NO new files under `dsx/`, NO new symbols, NO new `DSX-*`/`DSX-NAR` finding codes, and
adds NO gate check and NO heading-scanner. Zero file overlap with plans 14-01 (docs/dsx/learnings,
dsx-scope-analysis, researcher.md), 14-02 (templates/DATA-DICTIONARY.md, dsx-explore-data) and 14-05
(tests/) — same wave (wave 1). The `skills/dsx-narrate/SKILL.md` `description` frontmatter is edited
only by plan 14-04 in the next wave, so no two same-wave plans touch this file.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-compounding-and-data-onboarding/14-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship templates/DISCLOSURE-research.md (GUIDE-LLM-derived, write-then-ungated)</name>
  <files>templates/DISCLOSURE-research.md</files>
  <read_first>
    - templates/EDA.md:1-15 (the write-then-ungated template precedent and the CRLF authoring note; DISCLOSURE-research.md mirrors the "written, never gated" shape)
    - templates/CHART-REVIEW.md:1-30 (a second template precedent — a short frontmatter + fixed headings; the disclosure template is markdown-only, no machine-read gate contract)
    - .planning/phases/14-compounding-and-data-onboarding/14-CONTEXT.md (D-04: GUIDE-LLM-derived — AI-assisted steps / human-reviewed decisions / data handling / reproducibility — as a template, NOT a third-party dependency; opt-in; marketing byte-unchanged)
  </read_first>
  <action>
    Create `templates/DISCLOSURE-research.md`, a GUIDE-LLM-derived AI-assistance disclosure template
    authored in-repo (state explicitly that GUIDE-LLM is the STRUCTURE this template follows, not a
    package or dependency — nothing is installed or imported). Give it a single stable level-2
    disclosure heading (e.g. an AI-assistance disclosure heading) and the four GUIDE-LLM sections as
    sub-parts: AI-assisted steps (what the model did), human-reviewed decisions (what a human decided
    and checked), data handling (what data the assistant saw and how), and reproducibility (seed,
    environment, entrypoint pointer). Keep it prose-with-placeholders like `templates/EDA.md`; it
    carries no gate contract. State in the template that it is written and read for a research-domain
    readout only, is optional even then, and is NOT gated — no `dsx` check reads it and it mints no
    finding code. Add no Python and reference no path under `dsx/checks/`.
  </action>
  <acceptance_criteria>
    - `templates/DISCLOSURE-research.md` exists, carries one stable disclosure heading, and includes the four GUIDE-LLM sub-parts (AI-assisted steps, human-reviewed decisions, data handling, reproducibility).
    - The template states GUIDE-LLM is a template/structure, not a third-party dependency, and that the file is optional and ungated.
    - The template contains no Python and references no path under `dsx/`.
  </acceptance_criteria>
  <verify>
    <automated>test -f templates/DISCLOSURE-research.md && grep -Eiq 'AI[- ]assist' templates/DISCLOSURE-research.md && grep -Eiq 'human[- ]review' templates/DISCLOSURE-research.md && grep -Eiq 'data handling' templates/DISCLOSURE-research.md && grep -Eiq 'reproducib' templates/DISCLOSURE-research.md && grep -Eiq 'GUIDE-LLM' templates/DISCLOSURE-research.md && grep -Eiq 'not a (third[- ]party )?dependency|template, not' templates/DISCLOSURE-research.md && grep -Eq '^##[[:space:]]' templates/DISCLOSURE-research.md && ! grep -Eq 'dsx/checks/' templates/DISCLOSURE-research.md</automated>
  </verify>
  <done>templates/DISCLOSURE-research.md ships the GUIDE-LLM-derived four-part disclosure with one stable heading, states GUIDE-LLM is a template not a dependency and the file is optional+ungated, and adds no Python and no dsx/ reference.</done>
</task>

<task type="auto">
  <name>Task 2: Add the opt-in, research-guarded disclosure step to dsx-narrate</name>
  <files>skills/dsx-narrate/SKILL.md</files>
  <read_first>
    - skills/dsx-narrate/SKILL.md:1-14 (frontmatter: `Bash` is already in allowed-tools — the config read needs no new grant; do not touch the `description` line, plan 14-04 owns it)
    - skills/dsx-narrate/SKILL.md:26-44 (the fixed five-part `<structure>` and the What/So What/Now What declaration that "mints no new narrative code and adds no heading-scanner gate" — the disclosure block inherits this exact rule)
    - templates/DISCLOSURE-research.md (created in Task 1 — the block the step offers)
    - .planning/phases/14-compounding-and-data-onboarding/14-CONTEXT.md (D-04: literal `research` only; read via gsd-tools config-get dsx.domain; opt-in with skip-with-reason; marketing/auto byte-unchanged; golden = non-research emits no disclosure heading, by construction)
  </read_first>
  <action>
    Edit `skills/dsx-narrate/SKILL.md` to add ONE optional disclosure step, additive and guarded. The
    step: read `dsx.domain` via `node ~/.claude/gsd-core/bin/gsd-tools.cjs config-get dsx.domain`
    (Bash is already granted); ONLY when the value is the literal `research`, offer to append the
    disclosure block from `templates/DISCLOSURE-research.md` after the existing sections. State that the
    offer is opt-in even under research — the analyst may skip it with a one-line reason, and skipping
    is legitimate. State explicitly that for ANY other value — including the default `auto`,
    `marketing_science` and every other enum value — the narrative takes today's path unchanged: no new
    section, no reordering of the existing five parts, and no disclosure heading emitted. State that
    this block inherits the What/So What/Now What layer's declared rule — it mints no new narrative code
    and adds no heading-scanner gate — and cite that inheritance. Do NOT edit the `description`
    frontmatter (plan 14-04 owns it), do NOT change the `allowed-tools` block, do NOT reorder the five
    `<structure>` sections, add no Python beyond the config read, and reference no path under
    `dsx/checks/`. The golden property to preserve: because the block is guarded on the literal
    `research` value, output for `dsx.domain != research` contains no disclosure heading by
    construction.
  </action>
  <acceptance_criteria>
    - `skills/dsx-narrate/SKILL.md` references `templates/DISCLOSURE-research.md` and reads `dsx.domain` via `gsd-tools.cjs config-get dsx.domain`.
    - The step is guarded on the literal `research` value and states the offer is opt-in (skip-with-reason).
    - The step states that non-research values — including `auto` and `marketing_science` — take today's path unchanged with no new section and no disclosure heading.
    - The step states it mints no new narrative code and adds no heading-scanner gate; the five `<structure>` sections are not reordered; the `description:` and `allowed-tools:` frontmatter are unchanged; no `dsx/checks/` path is referenced.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "templates/DISCLOSURE-research.md" skills/dsx-narrate/SKILL.md && grep -q "config-get dsx.domain" skills/dsx-narrate/SKILL.md && grep -Eq 'domain[^A-Za-z]*==?[^A-Za-z]*research|literal .?research|only when .*research' skills/dsx-narrate/SKILL.md && grep -Eiq 'opt-in|skip with|skip-with|may skip' skills/dsx-narrate/SKILL.md && grep -Eiq 'marketing_science|auto' skills/dsx-narrate/SKILL.md && grep -Eiq 'byte-unchanged|unchanged|no new section' skills/dsx-narrate/SKILL.md && grep -Eiq 'no heading-scanner|mints no new' skills/dsx-narrate/SKILL.md && ! grep -Eq 'dsx/checks/' skills/dsx-narrate/SKILL.md</automated>
  </verify>
  <done>skills/dsx-narrate/SKILL.md offers the disclosure block only on the literal dsx.domain==research read (via gsd-tools config-get), opt-in with skip-with-reason, leaves auto/marketing_science and every other value byte-unchanged, inherits the no-new-code/no-heading-scanner rule, reorders nothing, and touches no dsx/ path or frontmatter it does not own.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan creates one markdown template and edits one skill. It introduces no executable surface, no
network or file input on any runtime gate path, no auth, and no user-supplied input on any
deterministic gate path. The only new read is the documented, already-granted config reader
(`gsd-tools config-get dsx.domain`), which returns a closed enum value. ASVS Level 1 injection / auth
/ session vectors are N/A here — there is no exploitable surface to assess. No npm / pip / cargo
install occurs, so the supply-chain vector (T-14-SC) is N/A this plan.

| Boundary | Description |
|----------|-------------|
| dsx-narrate -> dsx.domain config read | The skill reads a closed enum via the documented `gsd-tools config-get`; Bash is already granted; the read only selects whether to OFFER the block. No new automated trust boundary is created (D-04). |
| disclosure block -> dsx gate path | None. No DSX-NAR mint and no heading-scanner gate; the block inherits the What/So What/Now What layer's declared no-code rule (D-04/D-07). |
| non-research narrative -> new section | None by construction: the block is guarded on the literal `research` value, so marketing_science and the default auto emit no new section or heading (D-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07 | Tampering | A disclosure heading-scanner gate or a DSX-NAR mint added to enforce the block, minting a code | high | mitigate | D-04/D-07: the block inherits the existing no-new-code/no-heading-scanner rule; it is opt-in so it can never become a gate; files_modified touches no dsx/ path; certified phase-wide by plan 14-05's set-identity diff. |
| T-14-08 | Tampering | The block leaking into the marketing/auto path (a reordering or an always-on section), changing today's narrative for non-research work | high | mitigate | D-04 structural guard: the step is wrapped in an explicit `dsx.domain == research` literal check; the five `<structure>` sections are not reordered; the verify asserts the guard, the literal trigger, and the "unchanged for auto/marketing_science" statement. |
| T-14-09 | Spoofing | Reading `dsx.domain` via an ad-hoc/undocumented path instead of the granted reader, or inferring `research` from `auto` | medium | mitigate | D-04: the read uses the documented `gsd-tools config-get dsx.domain`; the trigger is the literal `research` value only, `auto` never infers it; the verify asserts both. |
| T-14-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs occur in this phase; GUIDE-LLM is an in-repo template, not a dependency. |
</threat_model>

<verification>
Phase-level goal-backward coverage (union across all five plans) is documented in plan 14-05. This
plan delivers Success Criterion 3 (when dsx.domain is research, narrate offers an optional
AI-assistance disclosure block; marketing-domain default unchanged — REQ-P14-03).

For this plan specifically:
- `templates/DISCLOSURE-research.md` ships the GUIDE-LLM-derived four-part block, ungated, GUIDE-LLM named as a template not a dependency.
- `skills/dsx-narrate/SKILL.md` offers the block only on the literal `research` read, opt-in, non-research byte-unchanged, no new code, no heading-scanner.
- Golden (structural): output for `dsx.domain != research` contains no disclosure heading by construction of the guard.
- files_modified touches no path under `dsx/`; no finding code is minted (proved phase-wide by 14-05).
</verification>

<success_criteria>
- templates/DISCLOSURE-research.md exists as a GUIDE-LLM-derived template (not a dependency), written-then-ungated (REQ-P14-03 / D-04).
- dsx-narrate offers the disclosure only on the literal dsx.domain==research read, opt-in with skip-with-reason (REQ-P14-03 / D-04).
- Marketing-domain and every other non-research value (incl. the default auto) narrative is byte-unchanged — no new section, no reordering, no disclosure heading (REQ-P14-03 / D-04).
- No DSX-NAR mint, no heading-scanner gate; no path under dsx/ is modified; no new finding code is minted.
</success_criteria>

<output>
Create `.planning/phases/14-compounding-and-data-onboarding/14-03-SUMMARY.md` when done.
</output>
</content>
