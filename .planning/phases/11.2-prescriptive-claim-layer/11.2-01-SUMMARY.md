---
phase: 11.2-prescriptive-claim-layer
plan: 01
subsystem: dsx-gate
tags: [coherence, vocabulary, claim-type, ladder, precondition]

# Dependency graph
requires: []
provides:
  - "prescriptive" as the fifth member of dsx.spec.CLAIM_TYPES
  - CLAIM_STRENGTH["prescriptive"] = 4 in dsx/checks/coherence.py, aligned with the existing QUESTION_STRENGTH row
  - Corrected five-type remedy strings for DSX-SPEC-061 and DSX-SPEC-062
  - tests/test_prescriptive_ladder.py — regression guard for the ladder and remedy wording
affects: [11.2-02, 11.2-03, 11.2-04, 11.2-05, 11.2-06, 11.2-07, 11.2-08]

# Tech tracking
tech-stack:
  added: []
  patterns: [D-02 ordering precondition — vocabulary + strength row + remedy text land in one slice]

key-files:
  created: [tests/test_prescriptive_ladder.py]
  modified: [dsx/spec.py, dsx/checks/coherence.py]

key-decisions:
  - "Reused DSX-COH-001/DSX-SPEC-061/DSX-SPEC-062; no new finding code minted (D-01, one-fact-one-code)"
  - "No logic change to _check_claim_ceiling — only the CLAIM_STRENGTH dict gained a row, so the existing c_strength > q_strength branch now reaches a prescriptive claim instead of skipping it"

patterns-established:
  - "Ordering precondition pattern (D-02): when a claim type is added to a closed vocabulary, the vocabulary member, every parallel ladder/strength dict, and every remedy string that names the vocabulary must land in the same commit slice, or the addition creates a silent pass (unrecognised-type finding fires, then the real check silently skips the now-recognised-but-unranked type)."

requirements-completed: [REQ-P11.2-01]

coverage:
  - id: D1
    description: "A claim typed prescriptive is recognised at rank 4 of the coherence ladder; under a weaker question_type it fires DSX-COH-001 CRITICAL exactly once"
    requirement: "REQ-P11.2-01"
    verification:
      - kind: unit
        ref: "tests/test_prescriptive_ladder.py#test_prescriptive_claim_exceeds_weaker_question_fires_coh001"
        status: pass
    human_judgment: false
  - id: D2
    description: "A claim typed prescriptive no longer trips DSX-SPEC-062 'unrecognised type'"
    requirement: "REQ-P11.2-01"
    verification:
      - kind: unit
        ref: "tests/test_prescriptive_ladder.py#test_prescriptive_claim_type_no_longer_trips_spec062"
        status: pass
    human_judgment: false
  - id: D3
    description: "Claim strength equal to (not exceeding) a prescriptive question does not fire DSX-COH-001"
    requirement: "REQ-P11.2-01"
    verification:
      - kind: unit
        ref: "tests/test_prescriptive_ladder.py#test_prescriptive_claim_equal_to_prescriptive_question_does_not_fire"
        status: pass
    human_judgment: false
  - id: D4
    description: "Both DSX-SPEC-061 and DSX-SPEC-062 remedy strings name all five claim types and no longer quote the stale four-type wording"
    requirement: "REQ-P11.2-01"
    verification:
      - kind: unit
        ref: "tests/test_prescriptive_ladder.py#test_remedy_strings_name_five_claim_types"
        status: pass
    human_judgment: false
  - id: D5
    description: "Verdict-neutral: no existing fixture's finding set changes"
    requirement: "REQ-P11.2-01"
    verification:
      - kind: unit
        ref: "python -m unittest discover -s tests (1056 tests, 0 failures)"
        status: pass
    human_judgment: false

duration: 5min
completed: 2026-08-24
status: complete
---

# Phase 11.2 Plan 01: Prescriptive claim-type ladder Summary

**Added `prescriptive` as the fifth claim type and its rank-4 coherence-ladder row, closing the silent-pass gap where a `type: prescriptive` claim tripped a HIGH "unrecognised type" warning and was then silently skipped by the CRITICAL strength-ceiling check.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-08-24T11:46:04Z
- **Completed:** 2026-08-24T11:50:26Z
- **Tasks:** 2 (TDD: RED then GREEN)
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments
- `prescriptive` added to `dsx.spec.CLAIM_TYPES`, joining the four existing members
- `CLAIM_STRENGTH["prescriptive"] = 4` added to `dsx/checks/coherence.py`, mirroring the `QUESTION_STRENGTH` row that already existed
- `DSX-SPEC-061` and `DSX-SPEC-062` remedy strings corrected to name all five claim types (previously both quoted a stale four-type count)
- `tests/test_prescriptive_ladder.py` created — four tests proving the ladder fires correctly, the structural check no longer misfires, the equal-strength edge case is safe, and both remedy strings are current
- Full suite (1056 tests) confirmed green — the vocabulary addition is verdict-neutral for every committed fixture

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — pin the prescriptive ladder and the five-type remedy** - `28b07ee` (test)
2. **Task 2: GREEN — add the member, the ladder row, and the five-type remedy** - `0708f66` (feat)

**Plan metadata:** committed alongside this SUMMARY (docs)

_TDD flow: RED confirmed tests 1, 2, 4 failed and test 3 passed against unmodified source; GREEN made all four pass with the minimal dict-row + remedy-string edit, no logic change._

## Files Created/Modified
- `tests/test_prescriptive_ladder.py` - Four-test regression guard for the prescriptive ladder and remedy wording (created)
- `dsx/spec.py` - `CLAIM_TYPES` gains `"prescriptive"`; `DSX-SPEC-061`/`DSX-SPEC-062` remedy strings corrected to name five types
- `dsx/checks/coherence.py` - `CLAIM_STRENGTH` gains `"prescriptive": 4`

## Decisions Made
- Followed D-01/D-02 exactly: reused `DSX-COH-001` and `DSX-SPEC-061`/`DSX-SPEC-062`, minted no new finding code, and landed the `CLAIM_TYPES` member + `CLAIM_STRENGTH` row + both remedy strings in this one plan slice — the D-02 ordering precondition that prevents the "silent pass" (unrecognised-type HIGH fires, then the CRITICAL ceiling check silently skips the claim).
- No change to `_check_claim_ceiling`'s logic: the existing `if ctype not in CLAIM_STRENGTH: continue` line now simply admits `prescriptive`, and the pre-existing `c_strength > q_strength` branch does the rest. This kept the diff to a dict-row + remedy-string edit, as the plan specified.

## Deviations from Plan

None - plan executed exactly as written. Both prohibitions verified directly against the diff:
- `grep`-equivalent check on the diff for `report.add("DSX-` shows only `DSX-SPEC-061`/`DSX-SPEC-062` (edited remedy text, same codes) touched — no new code string introduced.
- `dsx/findings.py` is absent from the diff (`git diff --name-only` confirmed).
- `git diff --diff-filter=D` between the two task commits shows no file deletions.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- The D-02 ordering precondition is satisfied: any sibling plan that reads a `type: prescriptive` claim (e.g. 11.2-02's identification-strength extension) can now rely on `prescriptive` being a fully recognised, correctly-ranked claim type.
- `CLAIM_STRENGTH` and `QUESTION_STRENGTH` are aligned at rank 4 for `prescriptive`, confirmed by the one-liner assertion in Task 2's verification.
- No blockers for downstream plans in this wave/phase.

---
*Phase: 11.2-prescriptive-claim-layer*
*Completed: 2026-08-24*
