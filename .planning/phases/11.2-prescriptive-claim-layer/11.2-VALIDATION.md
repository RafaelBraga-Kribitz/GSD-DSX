---
phase: 11.2
slug: prescriptive-claim-layer
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-24
---

# Phase 11.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> The per-requirement Nyquist mapping is authored in `11.2-RESEARCH.md` → `## Validation
> Architecture`; `/gsd-validate-phase 11.2` (ledger S1-5) formalises it into the Per-Task
> Verification Map below and flips `status: validated` / `nyquist_compliant: true`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Python 3 stdlib `unittest` (no pytest; existing `tests/` tree) |
| **Config file** | none — existing infrastructure covers all phase requirements |
| **Quick run command** | `python -m unittest tests.test_dsx.TestPhase11_1Code -q` (or the touched module's class) |
| **Full suite command** | `python -m unittest discover -s tests -q` |
| **Full gate command** | `bash scripts/check.sh` (suite + catalogue current + capability manifest + gate contract + determinism) |
| **Estimated runtime** | ~20-40 seconds full suite; `check.sh` ~1-2 min |

Baseline suite size before Phase 11.2: **1052 tests OK** (post S0-8). Every plan wave must keep
the full suite green and `bash scripts/check.sh` → `all checks passed`.

---

## Sampling Rate

- **After every task commit:** Run the touched module's quick command.
- **After every plan wave:** Run `python -m unittest discover -s tests -q`.
- **Before `/gsd-verify-work`:** `bash scripts/check.sh` must print `all checks passed`.
- **Max feedback latency:** ~40 seconds (full suite).

---

## Per-Task Verification Map

*Seeded at plan time; populated by `/gsd-validate-phase 11.2` (S1-5) against the executed plans.
The authoritative per-requirement test design is in `11.2-RESEARCH.md` → `## Validation
Architecture`. Summary of what must be proven:*

| Requirement | Behaviour to prove | Test locus (existing tree) | Test Type |
|---|---|---|---|
| REQ-P11.2-01 | `prescriptive` at ladder rank 4; weaker `question_type` → `DSX-COH-001` CRITICAL; both stale "four claim types" remedy strings updated | coherence tests + `tests/test_dsx.py` (spec remedy strings) | unit / regression |
| REQ-P11.2-02 | prescriptive claim with blank/none → `DSX-CLM-020` CRITICAL, weak → `DSX-CLM-021` HIGH; no hedge exemption | claims tests | unit |
| REQ-P11.2-03 | bare-infinitive/gerund causal verbs caught (Tier 1 full-strength / Tier 2 purpose-gated); **corpus-green** — golden-file finding-set equality on `examples/good-ANALYSIS-SPEC.yaml` + every `examples/**`; negative fixture ("sales increase in Q4") → zero new findings; two noun-collision sites stay exempt | `tests/test_known_bad_corpus.py` + claims/coherence tests | regression + negative |
| REQ-P11.2-04 | `decision.revisit_when` required for prescriptive/experiment; new sibling `revisit_when_is_discriminating` (NOT a mutation of the shared falsifier); window accept/reject cases; `DSX-COH-040` CRITICAL both-trigger | coherence tests; guard that `good:303` estimand falsifier does NOT newly fire `DSX-VAL-011` | unit + regression guard |
| REQ-P11.2-05 | `spec_id` top-level, fail-closed → `DSX-PRE-040` HIGH; >1 distinct `frame_digest` under one `spec_id` (+ trail-root floor) with no clearing amendment → `DSX-PRE-041` HIGH; amendment record cleared by `is_placeholder_or_refusal` + two-digest reference | `tests/test_frame_prereg.py`, `tests/test_decisions.py` | unit |
| REQ-P11.2-06 | `dsx explain` renders a separately-labelled "self-reported (taken on trust)" section; `escalate` untouched; returns-0 pure-reader contract preserved; `frame_digest` stays in the computed column | `tests/test_dsx.py` explain tests | unit |
| REQ-P11.2-07 | scaffold fields quarantined in a "documentation-only — NOT gate-read" template block; storyteller repointed at gate-read fields; five-type claim comment; catalogue current | template/agent presence tests + `bash scripts/check.sh` | presence + gate |

**Flagship fixture (SC-1):** `examples/known-bad/` "offer bundled incentives to reduce churn" —
fires `DSX-CLM-011`/`DSX-COH-010` on "reduce" and, if typed `prescriptive`, `DSX-CLM-020/021`
on blank/weak identification; add an `_EXPECTED_CAUGHT_DEFECTS` entry + catch-attribution tags
(the Phase 11.1/12 convention).

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements* — the `tests/` tree, the known-bad
corpus harness (`tests/test_known_bad_corpus.py`), the catalogue generator
(`scripts/gen-finding-catalogue.py --check`) and `scripts/check.sh` are all present. No new
framework install. New test files/classes are authored per-plan (TDD RED first).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| D-05 citation authenticity (Hernán 2018; Manski / Hernán & Robins Ch.1; Simmons 2011 / Gelman & Loken 2014 / Wagenmakers 2012 / Nosek 2018) | REQ-P11.2-02/03/05 | Project rule D-05 requires a **human** source read; personas prepared the evidence, they do not sign | Read the selected primary sources (queued as **HQ-3**); confirm the exact quotable sentence + page per D-16. Sign at phase verify/ship, not at plan/execute. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 40s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending (set by `/gsd-validate-phase 11.2`, ledger S1-5)
