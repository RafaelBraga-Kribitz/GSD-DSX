---
phase: 11.2
slug: prescriptive-claim-layer
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-24
validated: 2026-08-26
---

# Phase 11.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> The per-requirement Nyquist mapping is authored in `11.2-RESEARCH.md` → `## Validation
> Architecture`; `/gsd-validate-phase 11.2` (ledger S1-5) formalises it into the Per-Task
> Verification Map below and flips `status: validated` / `nyquist_compliant: true`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Python 3 stdlib `unittest` (no pytest; existing `tests/` tree) |
| **Config file** | none — existing infrastructure covers all phase requirements |
| **Quick run command** | `python -m unittest tests.test_dsx.TestPhase11_1Code -q` (or the touched module's class) |
| **Full suite command** | `python -m unittest discover -s tests -q` |
| **Full gate command** | `bash scripts/check.sh` (suite + catalogue current + capability manifest + gate contract + determinism) |
| **Estimated runtime** | ~20-40 seconds full suite; `check.sh` ~1-2 min |

Baseline suite size before Phase 11.2: **1052 tests OK** (post S0-8). Every plan wave must keep
the full suite green and `bash scripts/check.sh` → `all checks passed`.

---

## Sampling Rate

- **After every task commit:** Run the touched module's quick command.
- **After every plan wave:** Run `python -m unittest discover -s tests -q`.
- **Before `/gsd-verify-work`:** `bash scripts/check.sh` must print `all checks passed`.
- **Max feedback latency:** ~40 seconds (full suite).

---

## Per-Task Verification Map

*Seeded at plan time; formalised by `/gsd-validate-phase 11.2` (S1-5) against the executed
plans. Every requirement maps to a concrete, green automated test module (independently
re-run this firing, brief §5). **0 gaps** — no `gsd-nyquist-auditor` run, no generated
tests. `Status: COVERED` = a named test targets the behaviour and runs green.*

| Requirement | Behaviour proven | Test locus (green, count) | Test Type | Status |
|---|---|---|---|---|
| REQ-P11.2-01 | `prescriptive` at ladder rank 4 (`coherence.py:29,37`); weaker `question_type` → `DSX-COH-001` CRITICAL; both stale "four claim types" remedy strings updated (`spec.py:1080,1092`) | `tests/test_prescriptive_ladder.py` (**4 OK**) | unit / regression | COVERED |
| REQ-P11.2-02 | prescriptive blank/none → `DSX-CLM-020` CRITICAL, weak → `DSX-CLM-021` HIGH; no hedge exemption for prescriptive (`claims.py:170,206-209`) | `tests/test_prescriptive_identification.py` (**6 OK**) | unit | COVERED |
| REQ-P11.2-03 | bare-infinitive/gerund causal verbs caught (Tier 1 full / Tier 2 purpose-gated); **corpus-green** golden-file finding-set equality over every `examples/**`; negative fixture ("sales increase in Q4") → 0 new findings | `tests/test_causal_verb_tiers.py` (**14 OK**) + `tests/test_causal_verb_golden.py` (**6 OK**) + `tests/test_known_bad_corpus.py` (**30 OK**) | regression + negative | COVERED |
| REQ-P11.2-04 | `decision.revisit_when` required for prescriptive/experiment; new sibling `revisit_when_is_discriminating` (NOT a mutation of the shared falsifier — body byte-unchanged); window accept/reject; `DSX-COH-040` CRITICAL both-trigger; `good:303` estimand falsifier does NOT newly fire `DSX-VAL-011` (`test_frame_val.py:109-111`) | `tests/test_revisit_when.py` (**24 OK**) + `test_frame_val` DSX-VAL-011 guard | unit + regression guard | COVERED |
| REQ-P11.2-05 | `spec_id` top-level, fail-closed → `DSX-PRE-040` **HIGH** (`prereg.py:698`); >1 distinct `frame_digest` under one `spec_id` (+ trail-root floor) with no clearing amendment → `DSX-PRE-041` HIGH; amendment cleared by `is_placeholder_or_refusal` + two-digest reference | `tests/test_frame_prereg.py` (**104 OK**) + `tests/test_decisions.py` (**31 OK**) | unit | COVERED |
| REQ-P11.2-06 | `dsx explain` renders a separately-labelled "self-reported (taken on trust)" section; `escalate` untouched; returns-0 pure-reader contract preserved; computed `frame_digest` ABSENT from the self-reported block | `tests/test_explain_self_reported.py` (**11 OK**) | unit | COVERED |
| REQ-P11.2-07 | scaffold fields quarantined in a "documentation-only — NOT gate-read" template block (text + AST scan proves no check reads them); storyteller/narrate repointed at gate-read fields; five-type claim comment; catalogue current | `tests/test_scaffold_quarantine.py` (**9 OK**) + `bash scripts/check.sh` | presence + gate | COVERED |

**Flagship fixture (SC-1):** `examples/known-bad/` "offer bundled incentives to reduce churn" —
fires `DSX-CLM-011`/`DSX-COH-010` on "reduce" and, if typed `prescriptive`, `DSX-CLM-020/021`
on blank/weak identification; add an `_EXPECTED_CAUGHT_DEFECTS` entry + catch-attribution tags
(the Phase 11.1/12 convention).

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements* — the `tests/` tree, the known-bad
corpus harness (`tests/test_known_bad_corpus.py`), the catalogue generator
(`scripts/gen-finding-catalogue.py --check`) and `scripts/check.sh` are all present. No new
framework install. New test files/classes are authored per-plan (TDD RED first).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| D-05 citation authenticity (Hernán 2018; Manski / Hernán & Robins Ch.1; Simmons 2011 / Gelman & Loken 2014 / Wagenmakers 2012 / Nosek 2018) | REQ-P11.2-02/03/05 | Project rule D-05 requires a **human** source read; personas prepared the evidence, they do not sign | Citation *selections* approved in **HQ-3** (answered 2026-08-26); the formal verbatim-quote-at-locator read is queued as **HQ-4** for the phase ship round. Confirm the exact quotable sentence + page per D-16. Sign at phase verify/ship, not at plan/execute. |

*This manual-only item does **not** reduce Nyquist compliance: the underlying gate
behaviours for REQ-P11.2-02/03/05 are all covered by green automated tests above
(`test_prescriptive_identification`, `test_causal_verb_*`, `test_frame_prereg`). Only the
citation **authenticity** — a source read no test can perform — is manual, exactly as with
Phase 7's D-05 reads (S0-8). `nyquist_compliant: true` stands.*

---

## Validation Audit 2026-08-26

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

State A audit: all 7 requirements classified **COVERED** — each maps to a named test
module that runs green (per-module counts in the Per-Task Map: 4/6/14/6/30/24/104/31/11/9).
No `gsd-nyquist-auditor` spawned and no tests generated (0 gaps). Independent re-gate this
firing (brief §5): targeted 11-module run → **Ran 332 tests … OK**; full suite → **Ran 1147
tests … OK**; `bash scripts/check.sh` → **all checks passed**. The single Manual-Only item
(D-05 citation authenticity) is a human source read, not a Nyquist coverage gap.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (none — 0 gaps)
- [x] No watch-mode flags
- [x] Feedback latency < 40s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** validated 2026-08-26 — `nyquist_compliant: true`, 0 gaps, 7/7 requirements COVERED by green automated tests.
