---
phase: 11.1.1-detection-code-hardening-inserted
verified: 2026-08-22T00:00:00Z
status: gaps_found
score: 5/6 success criteria verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 3/6
  gaps_closed:
    - "SC2: DSX-CODE-021 now fires on keyword-argument fit calls whatever the position of the recognised keyword, on the fallback path as well as the parsed path. Reproduced directly: `FIT_CALL_RE.finditer('model.fit(y=y_train, X=full_frame)')` now returns `['full_frame']` (was `[]`). The false parity-claim comment was removed, not narrowed."
    - "SC4: identical executable code now receives an identical verdict for DSX-CODE-020/030/031 regardless of comments or docstrings. Reproduced directly: a module docstring merely describing a fillna-before-split idiom now produces zero findings (was CRITICAL DSX-CODE-020)."
  gaps_remaining:
    - "SC5: a source that cannot be parsed does NOT yet degrade to the text scan visibly in every case. Three additional, equally-valid malformed-.ipynb shapes still crash `check()` uncaught: a `.ipynb` containing non-UTF-8 bytes (UnicodeDecodeError), a `.ipynb` whose `cells` value is a non-iterable JSON scalar such as `5` (TypeError), and a `.ipynb` whose cell `source` list contains a non-string element (TypeError). Plan 04's fix only guarded `isinstance(nb, dict)` and `isinstance(cell, dict)` -- it did not guard that `cells` is a list, nor that every `source` list element is a string, nor add `UnicodeDecodeError` to the `.ipynb` read's exception guard."
  regressions: []
gaps:
  - truth: "SC5: a source that cannot be parsed degrades to the text scan VISIBLY -- never silently. An unreadable or undecodable entrypoint never becomes a silent pass, and an unscannable entrypoint emits report.ok(...) before returning."
    status: failed
    reason: >
      Three malformed-but-syntactically-valid-JSON `.ipynb` shapes still raise uncaught out of
      `check()`, reproduced directly against the current codebase (not inferred from the
      review): `{"cells": 5}` raises `TypeError: 'int' object is not iterable` (the
      `isinstance(nb, dict)` guard passes because the top-level document IS a dict; nothing
      checks that `nb.get("cells")` is itself a list before the `for cell in ... or []:` loop
      consumes it); `{"cells":[{"cell_type":"code","source":["import pandas",5]}]}` raises
      `TypeError: sequence item 1: expected str instance, int found` (the `isinstance(cell,
      dict)` guard passes because the cell IS a dict; nothing checks that every element of a
      list-typed `source` is a string before `"".join(src)` runs); and a `.ipynb` file containing
      non-UTF-8 bytes raises `UnicodeDecodeError` (the `try`/`except` around the `.ipynb` read at
      `dsx/checks/code.py:1101` only catches `(OSError, json.JSONDecodeError)` --
      `path.read_text(encoding="utf-8-sig")` raises `UnicodeDecodeError`, a `ValueError`
      subclass, which is not in that tuple; the `.py` branch three lines above already guards
      exactly this failure mode with `errors="replace"`, but the `.ipynb` branch has no
      equivalent). All three were reproduced live against `dsx/checks/code.py` at HEAD
      (`61819b9`, `10f9840`) during this verification, both at the `_read_source` level and at
      the `check()` level (`c.check({"reproducibility": {"entrypoint": <path>}})` raises
      `TypeError: 'int' object is not iterable` uncaught for the first shape). Plan 04's own
      must_have text for this criterion explicitly named "whose `cells` value is not a list of
      objects" as a shape that must route to `report.ok(...)`, and the shipped guards do not
      cover it. A raw, uncaught Python exception is a worse failure than a silent pass: it
      produces Python's default exit code 1, numerically indistinguishable from `EXIT_BLOCK`
      ("the gate legitimately blocked"), violating the project's own documented contract that
      exit 2 means "the check could not run." A related dishonesty: README.md (around lines
      258-269) states, introduced with "for example" and generalised as "a file shaped like
      that," that a structurally malformed notebook document "is now reported as NOT scanned" --
      true for the two shapes plan 04 actually closed (non-dict document, non-dict cell), false
      for these three adjacent, equally plausible ones, none of which is named as a residual
      limit anywhere in README's "What the entrypoint scan does not catch" section.
    artifacts:
      - path: dsx/checks/code.py
        issue: "_read_source (~lines 1090-1161), .ipynb branch: the try/except around json.loads (line 1101-1103) does not catch UnicodeDecodeError; the isinstance(nb, dict) guard (line 1113) does not also check isinstance(nb.get(\"cells\"), (list, type(None))) before the for-loop at line 1116 consumes it; the isinstance(cell, dict) guard (line 1128) does not also validate that every element of a list-typed cell[\"source\"] is a string before the \"\".join(src) at line 1138"
      - path: README.md
        issue: "lines ~258-269 state unconditionally that a malformed notebook document \"is now reported as NOT scanned,\" which is false for the three shapes above; none of the three is named in the 'What the entrypoint scan does not catch' section"
    missing:
      - "Add UnicodeDecodeError (or the broader ValueError it subclasses) to the .ipynb read's except clause at dsx/checks/code.py:1102, returning None exactly as the .py branch's analogous guard already does with errors=\"replace\" or as a hard None-return, whichever this module's SC5 contract for .ipynb prefers"
      - "Guard that nb.get(\"cells\") is a list (or falsy) before the for-loop iterates it, returning None otherwise, so {\"cells\": 5} and {\"cells\": true} and {\"cells\": {}} route to 'could not be read' instead of raising TypeError"
      - "Guard that every element of a list-typed cell[\"source\"] is a string before \"\".join(src) runs, returning None otherwise, so [\"import pandas\", 5] routes to 'could not be read' instead of raising TypeError"
      - "Either narrow README.md's malformed-notebook paragraph to name only the two shapes actually covered (non-dict top-level document, non-object cell entry) instead of the general \"a file shaped like that\" framing, or close the three gaps above so the existing wording becomes accurate again"
      - "Regression tests for all three shapes, following the pattern of test_non_dict_notebook_document_is_named_not_scanned_without_raising: asserting both that _read_source returns None and raises nothing, and that check() over the same file returns zero findings with a NOT scanned pass line and raises nothing"
deferred: []
human_verification:
  - test: "WR-02 (carried forward from 11.1.1-REVIEW.md and plan 03's test_unscannable_entrypoint_also_produces_no_decision_record, restated in 11.1.1-05-SUMMARY.md): confirm whether a declared-but-unscannable entrypoint producing zero DecisionRecord entries (so `dsx explain` can replay nothing for that judgment) and a pass line invisible without `--verbose` is an acceptable known gap to ship, or must close before this phase counts as done."
    expected: "A human decision on whether this is acceptable to ship as-is, or requires a new closure plan implementing the scan_path:not-scanned: decision input 11.1.1-AST-DESIGN.md's blocker table specified."
    why_human: "Self-flagged by plan 03's own test as a possible design/ship gap; explicitly a product decision, not a code defect, per the phase's own review. Confirmed still unimplemented: grep -c \"scan_path:not-scanned\" dsx/checks/code.py reports 0."
  - test: "D7/D8 README tone judgment: read README.md's heading \"### The entrypoint leak scan now parses your code\" together with the prose immediately under it and the new paragraphs plan 05 added, and judge whether the heading-plus-prose combination reads, in isolation, as an affirmative soundness claim for DSX-CODE-021 or the AST (Abstract Syntax Tree, Python's parsed-code representation) mechanism generally."
    expected: "A human reader agrees the wording does not overclaim soundness, or requests a change. The section's closing sentence (\"None of this makes DSX-CODE-001, DSX-CODE-021, or any other DSX-CODE-* check sound, complete or exhaustive\") is unchanged by plan 05, confirmed by git diff."
    why_human: "Tone/overclaim judgment on prose is explicitly named non-automatable by 11.1.1-VALIDATION.md's own Manual-Only Verifications table. Carried forward unresolved through plans 04 and 05 by design."
---

# Phase 11.1.1: Detection-code hardening (INSERTED) Re-Verification Report

**Phase Goal:** The detection code shipped in Phase 11.1 catches the argument forms that
generated pipelines actually use, rather than only the textbook phrasing its own corpus
fixtures happen to contain.

**Verified:** 2026-08-22
**Status:** gaps_found
**Re-verification:** Yes -- after gap-closure plans 04 and 05

## Goal Achievement

This is the second verification pass. The first (3/6, SC2/SC4/SC5 failed) drove gap-closure
plans 04 and 05, which executed and merged. This pass re-measured all six ROADMAP success
criteria directly against the live tree -- not against task-completion claims in the SUMMARY
files -- and independently reproduced every finding rather than restating the orchestrator's
measurements or the code review's.

SC2 and SC4 are now genuinely closed: both were reproduced fixed by direct execution
(`FIT_CALL_RE.finditer` on a reordered-keyword call now returns the token; a docstring-only
fillna mention now fires nothing). SC5 is **not** closed. Plan 04 closed exactly the two
malformed-notebook shapes its own tests exercised (a non-dict top-level document, a non-dict
cell) and left three adjacent, equally-valid JSON shapes crashing `check()` uncaught, all three
reproduced directly during this verification: `{"cells": 5}` (TypeError), a cell whose `source`
list holds a non-string element (TypeError), and non-UTF-8 bytes in the file (UnicodeDecodeError).
This is the same class of contract violation GAP-3 was chartered to close, just not closed for
every JSON shape that reaches the `.ipynb` branch.

### Observable Truths (ROADMAP Success Criteria SC1-SC6)

| # | Truth (ROADMAP SC) | Status | Evidence |
|---|------|--------|----------|
| SC1 | `DSX-CODE-001` fires on a fit-before-split call however written (whitespace, tab, backslash continuation, multi-line) | VERIFIED | Unchanged since the first verification pass. Spot-checked live: `test_fit_space_before_paren_before_split_fires_code_001` passes (`python -m unittest tests.test_dsx.TestPhase11_1Code.test_fit_space_before_paren_before_split_fires_code_001 -v` -> ok). Not touched by plans 04/05. |
| SC2 | `DSX-CODE-021` fires on keyword-argument fit calls whatever the call's shape, chained calls, `partial_fit`, and a non-training frame; nothing records DSX-CODE-021 as sound | VERIFIED | Reproduced live: `FIT_CALL_RE.finditer("model.fit(y=y_train, X=full_frame)")` -> `['full_frame']` (was `[]`); `FIT_CALL_RE.finditer("model.fit(sample_weight=w, groups=g, data=full_frame)")` -> `['full_frame']`; controls `model.fit(y=y_test)`, `model.fit(a==b, y)` -> `[]` on both. `grep -c "instead of disagreeing on it" dsx/checks/code.py` -> 0 (the overclaiming comment was removed, not narrowed). |
| SC3 | A semicolon-joined line carrying two fit calls yields both, stable line index across runs | VERIFIED | Unchanged. Spot-checked live: `test_semicolon_joined_two_fit_calls_fire_code_021` passes. Not touched by plans 04/05. |
| SC4 | Identical executable code receives an identical verdict regardless of comments, docstrings, string literals or notebook markdown cells | VERIFIED | Reproduced live: a module docstring reading `"""We deliberately avoid df.fillna(df.mean()) before splitting."""` above a clean import and `train_test_split(df)` now produces zero findings (`check()` run directly -- `codes fired: []`). Prior verification measured this same input as CRITICAL DSX-CODE-020. |
| SC5 | A source that cannot be parsed degrades to the text scan visibly, never silently; an unreadable/undecodable entrypoint never becomes a silent pass | ✗ FAILED | Reproduced live, three ways: `_read_source` on `{"cells": 5}` raises `TypeError: 'int' object is not iterable`; on `{"cells":[{"cell_type":"code","source":["import pandas",5]}]}` raises `TypeError: sequence item 1: expected str instance, int found`; on a `.ipynb` file with non-UTF-8 bytes raises `UnicodeDecodeError`. `check({"reproducibility": {"entrypoint": <path>}})` over the first shape confirms the exception propagates uncaught out of `check()` itself, not just out of the helper. See Gaps. |
| SC6 | Full suite and known-bad corpus stay green; no fixture exit code moves undeliberately | VERIFIED | Re-run live: `python -m unittest discover -s tests -q` -> 894 tests, OK. `python -m unittest tests.test_known_bad_corpus -q` -> 30 tests, OK. `bash scripts/check.sh` -> exit 0, "all checks passed" (finding catalogue current, capability manifest conformant, gate contract passed, determinism passed). `git diff --stat 8aacd07..HEAD -- tests/test_known_bad_corpus.py examples/` -> empty; no corpus expectation map edited. |

**Score:** 5/6 success criteria verified (SC1, SC2, SC3, SC4, SC6). SC5 fails on live,
independently-reproduced evidence -- a genuine, unclosed regression against the module's own
documented exit-code contract, not merely restated from the code review.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `dsx/checks/code.py::FIT_CALL_RE` (widened) | Recognises an allowlisted fit keyword at any position, bounded | ✓ VERIFIED | Reproduced live for both the fix and its `{0,8}`/`{0,80}` bound; overclaiming comment removed (`grep -c` -> 0). |
| `dsx/checks/code.py::_first_full_frame_cleaning_line` / `_stat_test_lines_referencing` | `masked` parameter wired to `prose_mask` | ✓ VERIFIED | `inspect.signature` confirms `masked` parameter present on both; live docstring-only fillna/stat-test inputs now produce no findings; true-positive controls unaffected (not independently re-run this pass, but SC4's live reproduction above exercises the same code path). |
| `dsx/checks/code.py::_read_source` `.ipynb` branch | Every structurally malformed shape routes to "could not be read" | ✗ STUB (partial) | Covers only `isinstance(nb, dict)` / `isinstance(cell, dict)`. Does NOT cover `cells` as a non-iterable scalar, a non-string element in a `source` list, or non-UTF-8 bytes -- all three crash uncaught, reproduced live. SC5 gap. |
| `README.md::"What the entrypoint scan does not catch"` | Discloses every genuinely-uncaught residue from plans 04/05 | ⚠️ PARTIAL | Discloses the fallback keyword-skip bound, the fallback's empty prose mask, the mask's opening/closing-line boundary, and the still-open DSX-CODE-002/003 limit -- all confirmed present in README.md. Does NOT disclose the three SC5 crash shapes above, because plan 04/05 did not know about them; the notebook-crash paragraph (lines ~258-269) states a broader claim ("a file shaped like that ... is now reported as NOT scanned") than the shipped code supports. |
| `tests/test_dsx.py::TestPhase11_1Code` | Regression pins for closed gaps | ✓ VERIFIED (as far as it goes) | 129 tests in this class, all pass. No test exists for the three SC5 shapes above -- they are unpinned. |
| `_END_TO_END_VARIANT_TABLE` | Measured headline table reflecting the remediation | ✓ VERIFIED | `len(test_dsx._END_TO_END_VARIANT_TABLE)` -> 22, matching SUMMARY's claim (19 + 3). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| SC2 fix: reordered keyword now resolves | `FIT_CALL_RE.finditer('model.fit(y=y_train, X=full_frame)')` | `['full_frame']` | ✓ PASS |
| SC2 control: non-allowlisted keywords stay silent | `FIT_CALL_RE.finditer('model.fit(y=y_test)')` / `('model.fit(a==b, y)')` | `[]` / `[]` | ✓ PASS |
| SC4 fix: docstring-only fillna mention fires nothing | `check()` over a docstring-only entrypoint | `codes fired: []` | ✓ PASS |
| SC5 gap 1: `cells` as non-iterable scalar | `_read_source` on `{"cells": 5}` | `TypeError: 'int' object is not iterable` | ✗ FAIL -- confirms gap |
| SC5 gap 2: non-string `source` element | `_read_source` on `{"cells":[{"cell_type":"code","source":["import pandas",5]}]}` | `TypeError: sequence item 1: expected str instance, int found` | ✗ FAIL -- confirms gap |
| SC5 gap 3: non-UTF-8 bytes | `_read_source` on a `.ipynb` with invalid UTF-8 bytes | `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xff...` | ✗ FAIL -- confirms gap |
| SC5 gap 1, at `check()` level | `check({"reproducibility": {"entrypoint": <path with {"cells": 5}>}})` | `TypeError: 'int' object is not iterable` (uncaught) | ✗ FAIL -- confirms gap propagates through `check()` |
| Full suite stays green | `python -m unittest discover -s tests -q` | 894 tests, OK | ✓ PASS |
| Known-bad corpus stays green | `python -m unittest tests.test_known_bad_corpus -q` | 30 tests, OK | ✓ PASS |
| Full phase gate | `bash scripts/check.sh` | exit 0, "all checks passed" | ✓ PASS |

### Requirements Coverage

Not applicable in the standard sense. ROADMAP states "Requirements: None new -- remediation of
detection-coverage gaps in already-shipped code," and `grep -c "11.1.1" .planning/REQUIREMENTS.md`
returns 0 -- confirmed by direct grep during this verification, zero hits. This is by design, not
an orphaned-requirement gap. Coverage is assessed against the six numbered ROADMAP success
criteria (see table above), the contract this phase committed to.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `dsx/checks/code.py:1090-1161` | `_read_source` `.ipynb` branch | Unguarded assumptions on JSON shape survive past plan 04's fix: `cells` is not verified to be a list before iteration, a `source` list's elements are not verified to be strings before `"".join`, and the `except` clause guarding `json.loads` does not also guard `path.read_text`'s `UnicodeDecodeError` | 🛑 Blocker (SC5 gap, three reproduced crashes) | A structurally malformed but plausible real-world notebook (corrupted encoding, or nbformat produced by a non-standard tool) still crashes the gate with a raw traceback and an exit code indistinguishable from a legitimate block |
| `README.md:258-269` | Malformed-notebook contract paragraph | States "a file shaped like that ... is now reported as NOT scanned" more broadly than the shipped fix supports; the three shapes above are not named as residual limits anywhere in the disclosure section | ⚠️ Warning (contributes to the SC5 gap's honesty half) | A reader who takes this paragraph at face value does not expect `dsx` to still crash on a corrupted or hand-produced notebook file, and will be wrong for three specific, plausible shapes |
| No `TBD`/`FIXME`/`XXX` debt markers found in `dsx/checks/code.py`, `tests/test_dsx.py` or `README.md` (one `"TBD"` hit in `code.py` is a detected-string literal for placeholder VALUES in a target column, not a debt marker) | -- | -- | -- | No debt-marker gate violation |

### Human Verification Required

See frontmatter `human_verification`. Both items are carried forward, unresolved by design, from
plans 04 and 05 per `workflow.human_verify_mode: end-of-phase` -- WR-02 (the missing
`DecisionRecord` / invisible-without-`--verbose` pass line for an unscannable entrypoint) and the
D7/D8 README tone judgment (whether the new section heading and prose read as an affirmative
soundness claim). Neither may be settled by this verification; both are restated here exactly as
`11.1.1-05-SUMMARY.md` records them, with the evidence needed to decide them.

### Gaps Summary

One of six ROADMAP success criteria is still falsified by live, independently-reproduced
evidence: **SC5 (visible degradation, never silent, never an uncaught crash)**. Plan 04 closed
exactly the two malformed-notebook shapes its own tests targeted -- a non-dict top-level document
and a non-dict cell entry -- and both are genuinely fixed, confirmed live. But `.ipynb` is valid
JSON that permits many more shapes than those two, and three of them still crash `check()`
uncaught, reproduced directly during this verification (not merely cited from the orchestrator's
measurements or the code review, though both independently found the same three): a `cells` value
that is a non-iterable JSON scalar (`5`, `true`), a cell `source` list containing a non-string
element, and a `.ipynb` file containing non-UTF-8 bytes. All three produce Python's default exit
code 1 -- the same number `EXIT_BLOCK` uses for "the gate legitimately blocked" -- rather than the
documented exit code 2 ("the check could not run"), which is the exact contract violation GAP-3
was chartered to close. README.md's account of the fix, read literally, claims more than the
shipped code delivers.

SC2 and SC4, the other two criteria plans 04 and 05 were chartered to close, are genuinely fixed
-- reproduced by direct execution against the live tree during this verification, not inferred
from task completion or restated from either SUMMARY. SC1, SC3 and SC6 continue to hold and were
independently spot-checked. No corpus expectation map was edited across either plan
(`git diff --stat 8aacd07..HEAD -- tests/test_known_bad_corpus.py examples/` is empty), and the
full phase gate (`scripts/check.sh`) passes end to end.

**This looks like a genuine, unclosed gap in the SAME area plan 04 was chartered to fix, not an
intentional deviation.** No override is suggested: plan 04's own must_have text explicitly named
"whose `cells` value is not a list of objects" as a shape requiring closure, and the shipped guards
do not reach it. A closure plan should extend `_read_source`'s `.ipynb` branch with the three
guards named in the `missing:` list above, add regression tests for each following the existing
`test_non_dict_notebook_document_is_named_not_scanned_without_raising` pattern, and either narrow
or make accurate the corresponding README paragraph.

---

_Verified: 2026-08-22_
_Verifier: Claude (gsd-verifier)_
