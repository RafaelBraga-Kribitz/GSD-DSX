---
phase: 11.1.1-detection-code-hardening-inserted
plan: 05
type: execute
wave: 2
depends_on: ["11.1.1-04"]
gap_closure: true
files_modified:
  - README.md
  - tests/test_dsx.py
autonomous: true
# No REQ identifiers by design -- see plan 04's frontmatter comment and
# .planning/REQUIREMENTS.md, which returns zero hits for 11.1.1. Coverage is
# keyed to the ROADMAP's six numbered success criteria. This plan owns the
# announcement obligation that SC2/SC4's fixes create, the disclosure obligation
# for what stays uncaught, and the SC6 re-proof that all three fixes leave the
# suite and the corpus green.
requirements: [SC2, SC4, SC5, SC6]

must_haves:
  truths:
    - "SC6 (literal): the full suite and the known-bad corpus stay green after all three fixes, and where a fixture's gate exit code moves it is surfaced and justified in both directions -- never absorbed by editing `_TARGET_DEFECT_CODES`, `_EXPECTED_CAUGHT_DEFECTS` or `_INCIDENTAL_GAP_CODES`. Proven by a run against the real tree at the end of plan 04, not inherited: full suite, known-bad corpus, `gen-finding-catalogue.py --check`, `validate-capability.py` and `./scripts/check.sh`, on both `python` and `python3`."
    - "Both directions of user-visible behaviour change are announced in README.md rather than slipped in. STRICTER: on the fallback path, a fit call whose recognised frame keyword arrives after another keyword now blocks where it drew nothing -- a true positive the scan previously could not see. LOOSER: on the parsed path, a docstring, a bare string statement or the interior of a multi-line string that merely describes a cleaning idiom or a statistical-test call no longer blocks DSX-CODE-020 CRITICAL, DSX-CODE-030 CRITICAL or DSX-CODE-031 HIGH. The looser direction is stated at least as prominently as the stricter one, because nobody files a bug about a gate that stopped complaining."
    - "Everything that remains genuinely uncaught after this remediation is disclosed in README.md's `### What the entrypoint scan does not catch` section: the bounded keyword-skip miss the widened fallback pattern accepts; the fallback path's retained docstring false positive for DSX-CODE-020, DSX-CODE-030 and DSX-CODE-031, because the prose mask is empty when there is no tree; and the prose mask's opening-line and closing-line boundary, which becomes user-visible for DSX-CODE-020 for the first time. The verification report noted that none of the three gaps was disclosed there; after this plan, what is left of them is."
    - "The DSX-CODE-002 / DSX-CODE-003 comment-mention limit is restated as STILL OPEN, so a reader cannot infer from the new text that it closed alongside the DSX-CODE-020/030/031 fix. It is a different mechanism -- those two loops have no leading-comment guard at all -- and it was outside this remediation's scope."
    - "A malformed notebook's changed outcome is stated as a contract change, not buried: a `.ipynb` that is valid JSON but is not a cell-bearing object used to end the run with a Python traceback and exit code 1 -- the same number the gate uses for 'blocked' -- and is now reported as NOT scanned."
    - "The phase's committed headline number stays honest about what this remediation changed. `_END_TO_END_VARIANT_TABLE` gains a row for every newly-recovered detection shape its row shape can express, so the table measures the remediation rather than silently predating it, and the SUMMARY records the new row count and its caught/missed split with the instrument named."
    - "Nothing added by this plan records DSX-CODE-021 -- or any DSX-CODE-* -- as sound. The section heading naming the parser is the single most likely place for a reader to take reassurance the text does not intend, and that judgment is carried forward to a human rather than settled here."
    - statement: "No sentence added to README.md by this plan reads, in isolation, as an affirmative soundness claim for DSX-CODE-021 or for the parsed mechanism generally, and the looser-direction announcement does not read as a footnote to the stricter one."
      verification: backstop
    - statement: "WR-02 -- that a declared-but-unscannable entrypoint emits no DecisionRecord, so `dsx explain` gets zero entries for that judgment, and its pass line is invisible without `--verbose` -- is carried forward to a human decision, unchanged and unresolved, rather than settled by any task in this plan."
      verification: backstop
  artifacts:
    - path: README.md
      provides: "the two-direction announcement of this remediation's behaviour change, inside the existing `### The entrypoint leak scan now parses your code` section, and the three new disclosures inside the existing `### What the entrypoint scan does not catch` section"
      contains: "What the entrypoint scan does not catch"
    - path: tests/test_dsx.py
      provides: "the extended `test_entrypoint_scan_scope_boundary_is_documented` required-substring set, and the new `_END_TO_END_VARIANT_TABLE` rows for the shapes this remediation recovered"
      contains: "_END_TO_END_VARIANT_TABLE"
  key_links:
    - from: "README.md::### What the entrypoint scan does not catch"
      to: "dsx/checks/code.py, the comment block above FIT_METHOD_NAMES and the _prose_line_indices docstring"
      via: "the two must name the SAME uncaught forms and the SAME set of codes for which the fallback keeps the docstring false positive. Plan 03 established this key_link and pinned it with `test_entrypoint_scan_scope_boundary_is_documented`; if the two drift, one of them is lying to a reader who has no way to tell which."
      pattern: "What the entrypoint scan does not catch"
    - from: "tests/test_dsx.py::_END_TO_END_VARIANT_TABLE"
      to: "dsx/checks/code.py::check"
      via: "every row is driven end to end through `check()`, the whole gate mechanism -- a DIFFERENT instrument from the function-level thirteen-variant table in 11.1.1-RESEARCH.md, and never printed in one series with it."
      pattern: "_END_TO_END_VARIANT_TABLE"
  prohibitions:
    - statement: "MUST NOT record DSX-CODE-021 -- or any DSX-CODE-* -- as sound, complete, reliable or exhaustive. The mechanism change makes this harder, not easier: 'it now uses a real Python parser' is exactly the sentence a reader will take as soundness, and this plan adds text directly under a heading that says so. Judge the sentences; do not just count keywords."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT weaken, delete or rewrite an existing corpus expectation -- `_TARGET_DEFECT_CODES`, `_EXPECTED_CAUGHT_DEFECTS` or `_INCIDENTAL_GAP_CODES` in `tests/test_known_bad_corpus.py` -- to absorb a verdict change. If a fixture's gate exit code moves, surface and justify it in BOTH directions."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT ship either direction of user-visible behaviour change unannounced. Files that pass today and start failing AND files that fail today and start passing both belong in README.md, and the looser direction must not read as a footnote."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT leave anything that remains genuinely uncaught after this remediation undisclosed in README.md's 'What the entrypoint scan does not catch' section, and MUST NOT let the new text imply that a limit closed when it did not -- specifically the DSX-CODE-002 / DSX-CODE-003 comment-mention limit, which stays open."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT add any parsing dependency other than `ast` on the gate path. D-01 forbids third-party imports there; this plan adds no import at all."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT settle WR-02 or the honesty-tone judgment. Both are human decisions, carried forward as `<human-check>` items and answered at `/gsd-verify-work` because `workflow.human_verify_mode` is `end-of-phase`. A task that quietly decides either -- by implementing a DecisionRecord on the unscannable path, or by declaring the heading acceptable -- has violated this plan."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT cite the uncommitted '9 of 12' figure or the twelve-variant session probe as though a committed enumeration backs them, and MUST NOT print either in one series with the committed thirteen-variant function-level table or with `_END_TO_END_VARIANT_TABLE`. They are different instruments and setting them side by side without saying so fabricates a discrepancy."
      status: unverified
      verification: flagged-unverified
---

<objective>
Announce, in README.md, both directions of the user-visible behaviour change plan 04 ships;
disclose what genuinely remains uncaught after it; extend the phase's committed end-to-end
variant table so the headline number measures the remediation rather than predating it; re-prove
that the full suite and the known-bad corpus stay green on both interpreters; and carry the two
human judgments this phase deliberately did not settle forward to `/gsd-verify-work` rather than
resolving them in a task.

Purpose: plan 04 moves a blocking gate in both directions at once. It recovers true positives on
the fallback path, which makes files that pass today start failing; and it removes false
positives on the parsed path for two CRITICAL codes and one HIGH code, which makes files that
fail today start passing. The ROADMAP's own re-scope record names both as user-visible and
requires both to be announced rather than slipped in, and says the looser direction is the more
dangerous one to leave unstated, because nobody files a bug about a gate that stopped
complaining. The verification report separately noted that none of the three gaps was disclosed
in README's limits section, which this phase's own prohibitions require for anything shipped
unfixed -- and something is left unfixed in each of the three after plan 04, so each needs a
sentence.

Output: a README.md whose two existing entrypoint-scan sections tell a reader why a file that
passed yesterday fails today, why a file that failed yesterday passes today, and what the scan
still cannot see; an extended documentation-obligation test and variant table; a recorded,
measured re-proof of SC6 on both interpreters; and three human-check items that reach the
end-of-phase verification instead of being answered by an agent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-VERIFICATION.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-REVIEW.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-AST-DESIGN.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-VALIDATION.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-04-PLAN.md
@README.md
@dsx/checks/code.py
@tests/test_dsx.py
</context>

<visible_skips_and_deferrals>
The same six skips and deferrals plan 04 records apply to this plan and are not repeated in
full: the spec-less probe fallback was **visibly skipped** and produced no probe-derived
predicates this run, because there is no `-SPEC.md` and no requirement identifier to derive one
from; no `ANALYSIS-SPEC.yaml` is required, because this phase produces no number, model or
chart; the application-programming-interface coverage checkpoint was skipped on a
`{"detected": false}` result from the deterministic detector, and no `COVERAGE.md` was created;
no object-relational-mapper schema files exist, so no push task was injected;
`.planning/intel/API-SURFACE.md` returned `symbolCount: 0` and carries no signal; and WR-01's
redundancy half is deferred with a measured reason. See plan 04's
`<visible_skips_and_deferrals>` for the full text of each.

One deferral is this plan's own to state, because this plan writes the text a reader will judge
it by:

- **The DSX-CODE-002 / DSX-CODE-003 comment-mention limit stays open.** Those two codes' text
  loops still have no leading-comment guard, so `# never use SMOTE before the split` still draws
  DSX-CODE-003 HIGH and `# StandardScaler().fit_transform(X)` still draws DSX-CODE-002 HIGH.
  This is not one of the verification report's gaps; the orchestrator scoped SC4's closure
  explicitly to DSX-CODE-020, DSX-CODE-030 and DSX-CODE-031. README already discloses it, and
  two committed `measured_not_assumed` tests pin it. Task 1's job is to make sure the NEW text
  cannot be read as having closed it too.
</visible_skips_and_deferrals>

## Artifacts this phase produces

Every symbol this plan creates or renames. The source-grounding pass reads this section to
exclude newly-created symbols from drift verification.

**No new README headings.** Both target sections already exist and were created by plan 03:
`### The entrypoint leak scan now parses your code` (README.md line 154) and
`### What the entrypoint scan does not catch` (README.md line 497). This plan adds paragraphs
inside them and rewrites sentences that plan 04 made false. Creating a third section would
split one announcement across two places a reader has no reason to connect.

**New `_END_TO_END_VARIANT_TABLE` row names** (module-level constant in `tests/test_dsx.py`,
row shape `(name, source, expected_codes, filename)`, unchanged):

- `nowcaught_reordered_keyword_on_the_fallback`
- `fp_closed_docstring_describing_a_cleaning_idiom`
- `contract_malformed_notebook_is_named_not_scanned`

**New required substrings** added to the existing
`tests/test_dsx.py::TestPhase11_1Code::test_entrypoint_scan_scope_boundary_is_documented`
tuple. The method itself is NOT renamed and no new test method is created for it.

**No new functions, classes, constants or parameters in `dsx/checks/code.py`.** This plan makes
no source change to the module at all.

<tasks>

<task type="auto">
  <name>Task 1: Announce both directions and disclose what is left, in the two README sections that already exist</name>

  <files>README.md, tests/test_dsx.py</files>

  <read_first>
    - `README.md` lines 154-230 -- `### The entrypoint leak scan now parses your code` in full.
      Note its existing structure: the looser direction is stated FIRST, under a bolded lead-in
      telling the reader to read that part first. Match that ordering; do not invert it.
    - `README.md` lines 497-610 -- `### What the entrypoint scan does not catch` in full,
      including the three-fact framing ("a function call whose target it cannot resolve to a
      name, an argument shape it does not read, or a file it never opens"), the
      `**What the fallback text scan additionally misses, and when it runs.**` paragraph at
      lines 555-576, and the closing sentence at lines 607-609 that states plainly that none of
      this makes any check sound.
    - `README.md` lines 385-400 -- the `## Known limits` group these subsections sit inside, so
      new text lands in the existing group rather than starting a second one.
    - `tests/test_dsx.py` lines 5294-5311 --
      `test_entrypoint_scan_scope_boundary_is_documented` and its `required_substrings` tuple.
      This is the mechanical half of the README-to-source key_link.
    - `dsx/checks/code.py` lines 206-241 -- the comment block above `FIT_METHOD_NAMES`, the
      other end of that key_link. It must name the same forms README names.
    - `dsx/checks/code.py` lines 336-382 -- `_prose_line_indices`' docstring AS PLAN 04 LEFT
      IT, including the sentence stating that the mask is empty on the fallback path. README's
      disclosure and that sentence must name the same set of codes.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-04-SUMMARY.md` -- the
      two repetition bounds plan 04 actually chose for the widened pattern. The README
      disclosure must state the real number, not a placeholder.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-AST-DESIGN.md`
      sections 8 and 9 -- the uncaught list and the announcement list this project's README
      register is derived from.
    - `.claude/skills/plain-language/SKILL.md` -- README text is written for a reader who has
      not read the planning directory. Expand every abbreviation on first use and keep the
      sentences plain.
  </read_first>

  <action>
    One commit: `docs(11.1.1-05): announce both directions of the detection-hardening change`.

    **Inside `### The entrypoint leak scan now parses your code`, add the remediation's two
    directions to the two paragraphs that already carry them, in the ordering that section
    already uses -- looser first.**

    Looser (plan 04 task 2). State that a module, class or function docstring, a bare string
    statement, or the interior lines of a multi-line string that merely DESCRIBES a full-frame
    cleaning idiom -- for example a sentence saying the file deliberately avoids filling missing
    values with a column mean before splitting -- used to block at CRITICAL under
    `DSX-CODE-020`, and no longer does. State the same for `DSX-CODE-030` (CRITICAL) and
    `DSX-CODE-031` (HIGH) when the prose mentions a statistical-test call on the declared target
    column. Add the second shape explicitly: a target-column reference that appears only in
    prose within the three-line window above a real statistical-test call no longer counts as
    that call's target reference, so the call stops being blocked on the strength of a sentence
    near it. Say in one clause why each is a false alarm and not a weakening: the executable
    code never performed the operation the sentence described. Say plainly that a file blocked
    yesterday can pass today.

    Stricter (plan 04 task 1). State that on the FALLBACK path only -- the weaker text scan that
    runs when a file cannot be parsed as Python -- a fit call whose recognised training-frame
    keyword arrives after another keyword now blocks where it previously drew nothing. Give the
    two concrete shapes a reader can match against their own code: a call written
    `model.fit(y=y_train, X=full_frame)`, and one written with several unrelated keywords before
    the frame keyword. Say that this is a true positive: the leak was always in the file, the
    parsed path already caught it, and only the fallback changed. Say which files reach the
    fallback -- the ones that cannot be parsed as Python -- so a reader can tell whether this
    applies to them.

    Contract change (plan 04 task 3). Add a short paragraph, not a footnote. A Jupyter notebook
    file that is valid JSON but is not a cell-bearing object -- or whose cell list holds
    anything that is not an object -- used to end the run with a raw Python traceback and exit
    code 1, which is the same number the gate uses to mean "blocked", so a caller reading only
    the exit code could not tell the two apart. It is now reported as NOT scanned, with the
    check reporting a state a caller can act on. Name the outcome, not the internal function.

    **Inside `### What the entrypoint scan does not catch`, add the three residues plan 04
    leaves.** Each goes in the paragraph that already covers its class, so the section keeps
    its three-fact framing:

    1. In the `**What the fallback text scan additionally misses, and when it runs.**`
       paragraph: the fallback recognises a training-frame keyword arriving after at most a
       stated number of other keyword arguments, and a call with more than that draws nothing
       there even though the parsed path still catches it. State the real bound from plan 04's
       SUMMARY as a number.
    2. In the same paragraph, or a new bolded one if it reads better: on the fallback there is
       no parsed structure to derive the prose mask from, so the mask is empty and a docstring
       describing a cleaning idiom or a statistical-test call STILL blocks `DSX-CODE-020`,
       `DSX-CODE-030` and `DSX-CODE-031` there, even though it no longer does on the parsed
       path. This is the honest residue of the fix announced above, and it must name the same
       three codes `_prose_line_indices`' own docstring names.
    3. A sentence on the mask's boundary, which becomes visible for `DSX-CODE-020` for the
       first time: the mask covers a whole bare string statement and the strictly interior lines
       of a multi-line string, but deliberately spares the opening and the closing line, because
       either can carry real code alongside the quote. A cleaning idiom written on the same
       physical line as a multi-line string's opening quote therefore still blocks. Say the
       sparing is deliberate and say what it buys -- not masking a line that carries executable
       code.

    **Restate the DSX-CODE-002 / DSX-CODE-003 limit as still open.** The existing sentence at
    lines 567-573 says two related limits stay open on the primary text checks that never moved
    to the parser. After plan 04 that sentence is ambiguous, because the parsed path's mask now
    reaches three MORE codes. Rewrite it so a reader can tell exactly which codes gained the
    mask and which did not, and why: `DSX-CODE-002` and `DSX-CODE-003` have no leading-comment
    guard in their own text loops at all, which is a different defect from the missing mask, and
    it was outside this remediation's scope.

    **Do not touch the closing sentence** at README.md lines 607-609. It already states that
    none of this makes any check sound, and it is the last thing a reader sees.

    **Do not repeat the uncommitted variant figure.** The "Where these numbers come from"
    paragraph at lines 593-605 already handles instrument provenance; if the end-to-end count
    changes because task 2 adds rows, update that paragraph's description rather than printing a
    second number beside the first.

    **Extend `test_entrypoint_scan_scope_boundary_is_documented`'s `required_substrings`
    tuple** with one short, stable substring per new disclosure -- something that will survive
    a copy-edit, in the style of the existing entries (`getattr`, `bound method`, `laundered`).
    Candidates: a token naming the keyword-skip bound, one naming the fallback's empty mask, and
    one naming the multi-line string's opening line. Do not rename the method and do not add a
    second documentation test; this one already carries the key_link.
  </action>

  <verify>
    <automated>python -m unittest tests.test_dsx.TestPhase11_1Code.test_entrypoint_scan_scope_boundary_is_documented -v</automated>
    <human-check>
      Read the two README sections end to end, as someone who has not read the planning
      directory.
      1. Does the looser direction land at least as hard as the stricter one? A reader whose
         file stopped being blocked must be able to tell that it stopped being blocked, and why
         that is a corrected false alarm rather than a weakened check. If the looser half reads
         as a qualification of the stricter half, it is not adequate.
      2. Does the section heading `### The entrypoint leak scan now parses your code`, together
         with the prose immediately under it, read in isolation as an affirmative claim that
         `DSX-CODE-021` -- or the parsed mechanism generally -- is sound? This is coverage item
         D7/D8 from plans 01, 02 and 03, named as non-automatable by
         `11.1.1-VALIDATION.md`'s Manual-Only Verifications table, and it is carried forward
         unresolved. Agree the wording, or request a change.
      3. Could any sentence added by this task be quoted, on its own, as reassurance -- in
         particular any sentence containing the words "parser", "parses", "structure" or "now
         catches"? If so, rewrite it.
      4. Does the limits section leave a reader in any doubt that the DSX-CODE-002 and
         DSX-CODE-003 comment-mention limit is STILL open, and that it is a different defect
         from the one just fixed?
      5. Does any new sentence contain a number without a source, an abbreviation expanded
         nowhere, or a promise about a future phase?
    </human-check>
  </verify>

  <acceptance_criteria>
    - Source: `grep -c "### The entrypoint leak scan now parses your code" README.md` reports 1
      and `grep -c "### What the entrypoint scan does not catch" README.md` reports 1 -- content
      was added inside the existing sections, not as new ones.
    - Source: `python -c "print(open('README.md',encoding='utf-8').read().count('## Known limits'))"`
      prints 1 -- the limits additions landed inside the existing group.
    - Source: `grep -c "DSX-CODE-020" README.md` is at least 1 and `grep -c "DSX-CODE-030" README.md`
      is at least 1. Before this task both report 0, which is what the verification report cited
      as the missing disclosure.
    - Source: `grep -c "DSX-CODE-031" README.md` is at least 1.
    - Source: `grep -c "DSX-CODE-002" README.md` is at least 1 and the sentence containing it
      states the limit is still open.
    - Source: the number stated for the fallback's keyword-skip bound equals the outer
      repetition bound plan 04 installed in `FIT_CALL_RE`, verified against
      `11.1.1-04-SUMMARY.md` and against the pattern itself with
      `python -c "import sys;sys.path.insert(0,'.');from dsx.checks import code as c;print(c.FIT_CALL_RE.pattern)"`.
    - Source: `grep -c "9 of 12" README.md` reports 0 and `grep -c "one line at a time" README.md`
      reports 0 -- the uncommitted figure and the superseded false premise are still absent.
      Both greps are deliberately unfiltered; README has no comment syntax to filter.
      <!-- planner-discipline-allow: 9 of 12 -->
      <!-- planner-discipline-allow: one line at a time -->
    - Source: `git diff HEAD~1 -- README.md` shows no deleted line outside the two target
      sections, and the closing sentence stating that no check is sound is unchanged.
    - Command: `python -m unittest tests.test_dsx.TestPhase11_1Code.test_entrypoint_scan_scope_boundary_is_documented -v`
      exits 0 with the extended substring set.
    - Command: `python -m unittest tests.test_dsx -q` exits 0.
    - Source: `git diff --stat -- dsx/` prints nothing -- this task changes no source.
  </acceptance_criteria>

  <done>
    A reader who has not opened the planning directory can learn from README.md why a file that
    passed the gate yesterday fails it today, why a file that was blocked yesterday passes it
    today, what changed for a malformed notebook, and what the scan still cannot see -- including
    the three things this remediation left open, each named rather than implied. The
    documentation-obligation test holds the disclosure to the source comment it must agree with.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-prove SC6 on both interpreters, extend the committed headline table, and carry the two human judgments forward</name>

  <files>tests/test_dsx.py</files>

  <read_first>
    - `tests/test_dsx.py` lines 4468-4545 -- the `_END_TO_END_VARIANT_TABLE` comment block,
      `_SPLIT_THEN`, and every existing row. The row shape is
      `(name, source, expected_codes, filename)` and the driving spec has no `model.target`, so
      a row can only express codes that fire without one.
    - `tests/test_dsx.py` line 6851 onward --
      `test_end_to_end_variant_table_matches_committed_expectations`, the driver. Read how it
      builds each fixture and which spec it passes, so a new row's expected code set is measured
      rather than assumed.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-04-SUMMARY.md` -- the
      measured figures and the list of limits plan 04 left open.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-03-SUMMARY.md`, the
      "Corpus Reconciliation" and "End-to-End Variant Table" sections -- the format this task's
      SUMMARY entry must match, including the phase base commit `e0fe56a` and the reason the
      literal `06ff2d7` reference does not scope the phase footprint correctly.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-REVIEW.md`, finding
      WR-02 in full -- the item being carried forward, so it is carried forward accurately
      rather than paraphrased into something narrower.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-VERIFICATION.md`
      frontmatter, the `human_verification` block -- the two items already recorded there.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-VALIDATION.md`, the
      "Manual-Only Verifications" table and the "Concrete SC4 proof procedure" section.
    - `scripts/check.sh` -- note it invokes `python3` while the quick command invokes `python`.
      Both must be exercised.
  </read_first>

  <action>
    One commit: `test(11.1.1-05): extend the end-to-end variant table and re-prove the phase gate`.

    **Add rows to `_END_TO_END_VARIANT_TABLE` for the shapes this remediation recovered**, so
    the phase's committed headline number measures the remediation rather than predating it.
    Three rows, using the names listed in "Artifacts this phase produces":

    - `nowcaught_reordered_keyword_on_the_fallback`: `_SPLIT_THEN`, then a fit call written with
      the target keyword `y` first and the frame keyword `X` second on a full frame, then an
      independent unclosed-parenthesis line to force the fallback. Measured while planning
      against a patched clean copy: this yields exactly `DSX-CODE-021`, and nothing else.
    - `fp_closed_docstring_describing_a_cleaning_idiom`: a single-line module docstring whose
      text describes avoiding a mean-fill before splitting, then `_SPLIT_THEN`. Measured: an
      empty code set. Before plan 04 the same fixture yields `DSX-CODE-020`.
    - `contract_malformed_notebook_is_named_not_scanned`: a `.ipynb` fixture whose content is
      the two characters for an empty JSON array, expected code set empty, filename
      `entry.ipynb`. Before plan 04 this row would raise rather than return a code set at all,
      which is precisely why it earns a row.

    Measure every new row's expected code set against the shipped code BEFORE writing it into
    the table, exactly as the table's own comment block instructs. Do not add a row for the
    `DSX-CODE-030` or `DSX-CODE-031` docstring shapes: the driver's spec declares no
    `model.target`, so neither can fire in that harness. Say so in a comment beside the new
    rows and name the tests in `TestPhase11_1Code` that pin those two shapes instead, so a
    reader does not read the table's silence as coverage.

    **Run the phase gate and record every result as a literal number, not as a claim that it
    passed.** All of the following, on `python` and on `python3` where the command is
    interpreter-sensitive:

    - `python -m unittest tests.test_dsx.TestPhase11_1Code tests.test_dsx.TestPhase3Code -v`
    - `python -m unittest discover -s tests -v`
    - `python -m unittest tests.test_known_bad_corpus -v`
    - `python scripts/gen-finding-catalogue.py --check`
    - `python scripts/validate-capability.py`
    - `./scripts/check.sh`

    Baselines to compare against, from the verification report and plan 03's SUMMARY: full suite
    878 tests OK; known-bad corpus 30 tests OK; catalogue check and capability validation both
    exit 0. Measured while planning against a clean `git archive HEAD` copy with all three of
    plan 04's fixes applied: 878 tests OK, 30 corpus tests OK, catalogue exit 0, capability exit
    0 -- so the expected end state is the same counts plus the methods plans 04 and 05 add.
    Record the actual numbers.

    **Prove no corpus fixture's exit code moved, in both directions.** Follow plan 03's own
    procedure: `python -m unittest tests.test_known_bad_corpus -v` at the phase base and at
    HEAD, and `git diff --stat -- tests/test_known_bad_corpus.py examples/` printing nothing
    across the whole phase. If any fixture's `plan` or `execute` exit code has moved, STOP and
    surface it with a justification in both directions. Do not edit `_TARGET_DEFECT_CODES`,
    `_EXPECTED_CAUGHT_DEFECTS` or `_INCIDENTAL_GAP_CODES` under any circumstances -- if a
    fixture moved, either the change or the fixture is wrong, and the map is the only evidence
    of which.

    **Assert the six full-frame-cleaning symbols are untouched.** `_is_full_frame_impute`,
    `_is_full_frame_spread_filter`, `_IMPUTE_FILLNA_RE`, `_IMPUTE_STAT_RE`,
    `_SPREAD_SUBSCRIPT_RE` and `_SPREAD_STAT_RE` must be byte-identical to commit `06ff2d7`
    (threat T-11.1.1-04). Note plan 03's finding that `06ff2d7` is the correct reference for
    those six symbols but NOT for the phase footprint, because an unrelated quick-task landed on
    this branch between `06ff2d7` and the phase's first commit; use `e0fe56a` as the
    phase-scoped base for any footprint claim, and record which base each claim uses.

    **Carry the two human judgments forward. Do not settle either.** They are recorded as
    `<human-check>` items on this task and on task 1, and they are answered at
    `/gsd-verify-work` because `workflow.human_verify_mode` is `end-of-phase`. Restate both in
    the SUMMARY under a heading a human will find, with the evidence needed to decide them and
    an explicit statement that this plan did not decide them. Specifically: do NOT implement a
    `DecisionRecord` or a `scan_path:not-scanned:` decision input on the declared-but-unscannable
    path while closing WR-02, and do NOT declare the README heading acceptable on the phase's
    behalf.

    **Windows note.** This is a PowerShell machine. Write the commit message to a file and apply
    it with `git commit -F <file>`; do not use a here-string. `./scripts/check.sh` needs a POSIX
    shell -- run it through the available bash and record the interpreter you used alongside the
    exit code, so the result is reproducible.
  </action>

  <verify>
    <automated>python -m unittest discover -s tests -q</automated>
    <human-check>
      WR-02, carried forward from `11.1.1-REVIEW.md` and from plan 03's own test
      `test_unscannable_entrypoint_also_produces_no_decision_record`, which pinned the current
      behaviour rather than closing it.

      When a declared entrypoint resolves to a path that does not exist, or resolves but cannot
      be read -- an unsupported suffix, an unreadable file, or, after plan 04, a structurally
      malformed notebook -- `check()` returns before either `record_decision` call. So:
      1. `DECISIONS.jsonl` gets zero entries for that run of the code check, and `dsx explain`
         can replay nothing, even though a real judgment was made -- that this entrypoint could
         not be scanned, so nothing in it can be treated as clean.
      2. `Report.render()` only prints passed checks under `--verbose`, which defaults to off,
         so by default `dsx gate` prints a bare passing line for the code check that is
         indistinguishable from a real clean scan.

      `11.1.1-AST-DESIGN.md`'s blocker B4/B6 table specified a `scan_path:not-scanned:<reason>`
      decision input for exactly this case and it was not shipped. Plan 04 has now made this
      path REACHABLE for one more input class (the malformed notebook), which raises how often
      it matters without changing the gap itself.

      The decision required: is this an acceptable known gap to ship, or must it close before
      the phase counts as done? If it must close, it is a new plan, not an edit to this one.
    </human-check>
  </verify>

  <acceptance_criteria>
    - Command: `python -m unittest discover -s tests -q` exits 0, and the reported test count is
      recorded as a literal alongside the 878 baseline.
    - Command: `python3 -m unittest discover -s tests -q` exits 0 with the same count.
    - Command: `python -m unittest tests.test_known_bad_corpus -v` exits 0 and reports 30 tests.
    - Command: `python scripts/gen-finding-catalogue.py --check` exits 0.
    - Command: `python scripts/validate-capability.py` exits 0.
    - Command: `./scripts/check.sh` exits 0, with the interpreter used recorded.
    - Command: `python -m unittest tests.test_dsx.TestPhase11_1Code.test_end_to_end_variant_table_matches_committed_expectations -v`
      exits 0 with the three added rows.
    - Source: `python -c "import sys;sys.path.insert(0,'tests');import test_dsx;print(len(test_dsx._END_TO_END_VARIANT_TABLE))"`
      prints 22 -- the 19 committed rows plus the three added here. Measured while planning:
      the table currently holds 19 rows. `11.1.1-03-SUMMARY.md` describes it as an 18-row table
      in three places; that figure is off by one against the shipped constant. Correct the count
      in this plan's SUMMARY rather than repeating it, and say which number was measured and
      when -- an unsourced count is threat T-11.1.1-09 whichever direction it errs in.
    - Source: `git diff --stat -- tests/test_known_bad_corpus.py examples/` prints nothing.
    - Source: `git diff e0fe56a..HEAD -- dsx/checks/code.py` shows no change to any of
      `_is_full_frame_impute`, `_is_full_frame_spread_filter`, `_IMPUTE_FILLNA_RE`,
      `_IMPUTE_STAT_RE`, `_SPREAD_SUBSCRIPT_RE` or `_SPREAD_STAT_RE`.
    - Source: `git diff --stat -- dsx/` prints nothing for this task -- it changes no source.
    - Source: `grep -c "scan_path:not-scanned" dsx/checks/code.py` reports 0. WR-02 was carried
      forward, not quietly implemented.
    - Source: the SUMMARY contains a section naming both human items, the evidence for each, and
      an explicit statement that this plan did not decide them.
    - Behavior: `python -W error::SyntaxWarning -c "import ast;ast.parse(open('tests/test_dsx.py',encoding='utf-8').read())"`
      raises nothing.
  </acceptance_criteria>

  <done>
    The full suite, the known-bad corpus, the catalogue check, the capability validation and the
    phase gate are green on both interpreters, with every result recorded as a literal number
    against its baseline rather than asserted as passing. No corpus expectation map was edited
    and no fixture exit code moved. The phase's committed headline table now contains a row for
    each shape this remediation recovered, measured before being written down, with the two
    shapes the harness cannot express named alongside the tests that pin them instead. Both
    human judgments reach `/gsd-verify-work` unresolved and with the evidence needed to answer
    them.
  </done>
</task>

</tasks>

<threat_model>
Application Security Verification Standard enforcement level 1; blocking severity threshold
`high`. This plan changes no source under `dsx/`. Its threat surface is documentation accuracy
and the integrity of the evidence the phase publishes -- which is a real surface for a blocking
gate, because the repudiation threats in this phase's register are about documentation
overstating coverage.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md -> an operator deciding whether to trust a clean scan | prose crosses here; a sentence that reads as reassurance becomes an operational decision to ship a model |
| `_END_TO_END_VARIANT_TABLE` -> the phase's published headline number | a committed table is the phase's evidence; a row that was assumed rather than measured is a fabricated measurement |
| the gate commands' exit codes -> the SC6 claim | a claim recorded as "passed" rather than as a literal number cannot be audited later |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.1.1-08 | Repudiation | README.md's two entrypoint-scan sections | high | mitigate | The section that announces a parser is the single most likely place for a reader to take soundness the text does not offer. Mitigated by task 1's `<human-check>` items 2 and 3, by the untouched closing sentence stating no check is sound, and by prohibition 1. The judgment is carried to a human rather than settled by an agent, because `11.1.1-VALIDATION.md` names tone judgment on prose as non-automatable. |
| T-11.1.1-16 | Repudiation | the looser-direction announcement | high | mitigate | A blocking gate that silently stops complaining costs its users a leak they believed was caught. Mitigated by requiring the looser direction FIRST, in the ordering README's existing section already uses, and by `<human-check>` item 1, which fails the task if the looser half reads as a qualification of the stricter half. |
| T-11.1.1-17 | Repudiation | a disclosure that implies a limit closed when it did not | medium | mitigate | The DSX-CODE-002 / DSX-CODE-003 comment-mention limit stays open while three sibling codes gain the mask. Task 1 must rewrite the existing sentence so a reader can tell which codes gained it and which did not, and an acceptance criterion checks that `DSX-CODE-002` still appears in README with the limit stated as open. |
| T-11.1.1-09 | Repudiation | the phase's headline number | medium | mitigate | Every added row's expected code set is measured against the shipped code before being written down, per the table's own comment block. The uncommitted twelve-variant figure is not repeated and the function-level thirteen-variant table is never printed in one series with the end-to-end one. Prohibition 7. |
| T-11.1.1-10 | Tampering | `tests/test_known_bad_corpus.py` expectation maps | high | mitigate | Prohibition 2, plus an acceptance criterion that `git diff --stat -- tests/test_known_bad_corpus.py examples/` prints nothing, plus an explicit STOP instruction if any fixture's exit code has moved. |
| T-11.1.1-04 | Tampering | the six full-frame-cleaning symbols | high | mitigate | Asserted by diff against commit `06ff2d7` in task 2, with plan 03's correction recorded: `06ff2d7` scopes those six symbols correctly but not the phase footprint, for which `e0fe56a` is the phase-scoped base. |
| T-11.1.1-18 | Repudiation | WR-02 and the honesty-tone judgment | medium | mitigate | Prohibition 6 makes settling either a violation, and an acceptance criterion checks that `grep -c "scan_path:not-scanned" dsx/checks/code.py` still reports 0. Both reach `/gsd-verify-work` with the evidence needed to decide them. |
| T-11.1.1-SC | Tampering | package installs | low | accept | No `npm`, `pip` or `cargo` step exists on this path and this plan adds no import. The Package Legitimacy Gate stays untriggered and no legitimacy checkpoint is required. |
</threat_model>

<verification>
1. Every gate command in task 2 is run and its exit code recorded as a literal, on both
   `python` (3.12.10) and `python3` (3.14.6) where the command is interpreter-sensitive.
2. `python -m unittest discover -s tests -q` exits 0 and its count is compared against the 878
   baseline plus the methods plans 04 and 05 added.
3. `python -m unittest tests.test_known_bad_corpus -v` exits 0 and reports 30 tests, and
   `git diff --stat -- tests/test_known_bad_corpus.py examples/` prints nothing.
4. `grep -c "### The entrypoint leak scan now parses your code" README.md` and
   `grep -c "### What the entrypoint scan does not catch" README.md` each report 1.
5. `grep -c "DSX-CODE-020" README.md`, `grep -c "DSX-CODE-030" README.md` and
   `grep -c "DSX-CODE-031" README.md` are each at least 1 -- all three were 0 before this plan,
   which is exactly what the verification report cited as the missing disclosure.
6. `grep -c "scan_path:not-scanned" dsx/checks/code.py` reports 0 -- WR-02 carried forward, not
   implemented.
7. `git diff --stat -- dsx/` prints nothing across this whole plan.
8. The three `<human-check>` items -- task 1's five-part README judgment, and task 2's WR-02
   decision -- are answered by a human before the phase closes.
   `workflow.human_verify_mode` is `end-of-phase`, so they are recorded at `/gsd-verify-work`
   rather than as blocking checkpoints mid-execution.
9. Every command runs on Windows PowerShell as written, except `./scripts/check.sh`, which needs
   a POSIX shell and whose interpreter is recorded alongside its exit code. No heredoc, no
   `printf`, no bash-only quoting anywhere. Commit messages are written to a file and applied
   with `git commit -F <file>`.
</verification>

<success_criteria>
- README.md announces both directions of the behaviour change, with the looser direction stated
  at least as prominently as the stricter one, in the two sections that already exist rather
  than in a third a reader has no reason to find.
- Everything plan 04 leaves genuinely uncaught is disclosed: the fallback's bounded keyword-skip
  miss with its real number, the fallback's retained docstring false positive for the same three
  codes the source names, and the prose mask's opening-line and closing-line boundary.
- The DSX-CODE-002 / DSX-CODE-003 comment-mention limit is unambiguously still open in the text,
  distinguished from the defect that just closed.
- The full suite, the known-bad corpus, the catalogue check, the capability validation and the
  phase gate are green on both interpreters, recorded as literal numbers, with no corpus
  expectation map edited and no fixture exit code moved.
- The committed end-to-end variant table has a measured row for each shape this remediation
  recovered, and names the two shapes its harness cannot express alongside the tests that pin
  them.
- WR-02 and the honesty-tone judgment reach the end-of-phase human check unresolved, with the
  evidence needed to decide them, and nothing in this plan decides either.
</success_criteria>

<output>
Create `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-05-SUMMARY.md` when
done. Record in it, as literal values rather than as a claim that they passed: every gate
command's exit code, on both interpreters where the command is interpreter-sensitive; the
full-suite test count before and after, against the 878 baseline; the known-bad corpus count and
one line per fixture with its `plan` and `execute` exit codes; the new
`_END_TO_END_VARIANT_TABLE` row count and its caught/missed split with the instrument named and
a sentence saying why it is never printed beside the function-level thirteen-variant table; the
exact base commit used for each diff claim and why (`06ff2d7` for the six full-frame symbols,
`e0fe56a` for the phase footprint); and a clearly headed section restating both human-check
items with their evidence and an explicit statement that this plan did not decide them.
</output>
