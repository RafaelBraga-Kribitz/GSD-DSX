---
phase: 11.1.1-detection-code-hardening-inserted
plan: 06
type: tdd
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - dsx/checks/code.py
  - tests/test_dsx.py
autonomous: true
# This phase carries no requirement identifiers by design. The roadmap says
# "Requirements: None new -- remediation of detection-coverage gaps in
# already-shipped code", and `grep -c "11.1.1" .planning/REQUIREMENTS.md`
# returns 0, re-confirmed by direct grep while planning this run. Plans 04 and
# 05 used the labels SC2/SC4/SC5/SC6 in this field; this run follows the
# orchestrator's explicit instruction to leave the field empty instead, because
# those labels are roadmap success criteria, not requirement identifiers. The
# coverage they expressed is carried in `must_haves.truths` below, where each
# truth names the success criterion it belongs to.
requirements: []

must_haves:
  truths:
    - "SC5 (literal, the open criterion): a `.ipynb` entrypoint whose bytes are not valid UTF-8 (Unicode Transformation Format, 8-bit) returns None from `_read_source` and raises nothing, and `check()` over the same file returns zero findings, emits a passed-check line containing `NOT scanned`, and raises nothing. Reproduced raising `UnicodeDecodeError` against HEAD (8c358dc) while planning this run."
    - "SC5 (literal): a `.ipynb` document whose `cells` value is not a list returns None from `_read_source` and raises nothing, and `check()` over the same file returns zero findings with a `NOT scanned` passed-check line and raises nothing. The shapes covered are `{\"cells\": 5}` and `{\"cells\": true}` (both reproduced raising `TypeError: 'int'/'bool' object is not iterable` against HEAD), and `{\"cells\": {}}`, `{\"cells\": null}`, `{\"cells\": \"cells\"}` and `{}` (all four reproduced returning the empty string against HEAD, which makes `check()` print the affirmative parsed pass line over a document nothing was read from -- the silent pass SC5 forbids, and the shape plan 04's own must_have text named as needing to route to `report.ok(...)`)."
    - "SC5 (literal): a `.ipynb` cell whose `source` is a list containing any element that is not a string returns None from `_read_source` and raises nothing, and `check()` over the same file returns zero findings with a `NOT scanned` passed-check line and raises nothing. Reproduced raising `TypeError: sequence item N: expected str instance, ... found` against HEAD for an integer element, a null element and a nested-list element."
    - "SC5 (literal): a `.ipynb` cell whose `source` is neither absent, nor a string, nor a list -- a JSON (JavaScript Object Notation) object or a number -- returns None from `_read_source` and raises nothing, and `check()` returns zero findings with a `NOT scanned` passed-check line. Against HEAD these return the Python `str()` rendering of the value (`{'a': 'model.fit(df)'}`, `5`), which is scanned as if it were code the file contains."
    - "SC5 (literal, found while planning and not named by either the verification report or the code review): a `.ipynb` document nested more deeply than the running interpreter's recursion limit returns None from `_read_source` and raises nothing, and `check()` returns zero findings with a `NOT scanned` passed-check line. Reproduced against HEAD: 20,000 nested JSON arrays raise `RecursionError` out of `json.loads`, which the `.ipynb` read's exception guard does not catch. This is a fourth uncaught crash shape in the same branch, closed here rather than left undisclosed."
    - "Boundary that must NOT move: `{\"cells\": []}` -- a notebook that legitimately contains no cells -- still reads as the empty string, not as None, and still takes the affirmative `entrypoint parsed with ast` passed-check line. A guard that cannot tell an empty notebook from a document that is not a notebook would trade one dishonesty for another."
    - "The two shapes plan 04 closed stay closed: a top-level document of `[]` or `null`, a `cells` list holding a bare string, and a `cells` value that is a JSON object all still return None and still route to the `NOT scanned` passed-check line. `test_non_dict_notebook_document_is_named_not_scanned_without_raising` and `test_non_dict_notebook_cell_is_named_not_scanned_without_raising` keep passing unedited."
    - "The exit-code contract holds for every shape above: none of them produces Python's default exit code 1, which is the number `EXIT_BLOCK` uses for 'this file was scanned and blocked'. Proven end to end by `bash scripts/check.sh`, whose gate-contract loop asserts that a missing spec exits 2 and that the good and bad fixtures keep their exit codes."
    - "SC1, SC2, SC3, SC4 and SC6 do not regress: `python -m unittest discover -s tests -q` and `python3 -m unittest discover -s tests -q` both pass, `python -m unittest tests.test_known_bad_corpus -q` passes at 30 tests, and `bash scripts/check.sh` exits 0. A well-formed notebook still takes the parsed path -- `test_bom_notebook_entrypoint_takes_the_ast_path` still reports `scan` as `ast` for its one DSX-CODE-001 finding."
    - statement: "No new module-level symbol is added to `dsx/checks/code.py`. Every guard is inline in `_read_source`, which stays a pure function of its one argument, with no new shared state, no new cache and no third-party import (D-01)."
      verification: backstop
    - statement: "No guard added by this plan can itself raise. Each one is an `isinstance` test or a generator of `isinstance` tests over already-decoded values; none calls a user-controlled callable, none indexes without a preceding type test, and none formats an untrusted value into a message."
      verification: backstop
  artifacts:
    - path: dsx/checks/code.py
      provides: "the hardened `.ipynb` branch of `_read_source`: a widened exception tuple on the read-and-decode step, a guard requiring `cells` to be a list, and a guard requiring a cell's `source` to be absent, a string, or a list whose every element is a string"
      contains: "isinstance(cells, list)"
    - path: dsx/checks/code.py
      provides: "the widened `entrypoint NOT scanned -- could not be read (...)` reason text in `check()`, naming the whole class of failures the branch now routes here rather than only the class it named before"
      contains: "NOT scanned"
    - path: tests/test_dsx.py
      provides: "six regression tests in `TestPhase11_1Code`, each asserting both that `_read_source` returns the expected value and raises nothing, and that `check()` over the same file returns zero findings with the expected passed-check line and raises nothing"
      contains: "test_deeply_nested_notebook_json_is_named_not_scanned_without_raising"
  key_links:
    - from: "dsx/checks/code.py::_read_source, the .ipynb branch"
      to: "dsx/checks/code.py::check, the `source is None` branch"
      via: "returning None is the ONLY channel by which a malformed notebook reaches the controlled `report.ok('entrypoint NOT scanned -- could not be read ...')` line. Any shape that raises instead of returning None bypasses that line entirely and ends the process with Python's default exit code 1."
      pattern: "could not be read"
    - from: "dsx/checks/code.py::check, the `could not be read` reason text"
      to: "the set of shapes `_read_source` actually routes to None"
      via: "the reason text names the class of failures a reader is told happened. If the guards widen and the text does not, the tool tells the user a narrower story than the truth -- the same failure the README half of this gap is about, one layer down."
      pattern: "could not be read"
  prohibitions:
    - statement: "MUST NOT record `DSX-CODE-001`, `DSX-CODE-021` or any other `DSX-CODE-*` check as sound, complete, reliable or exhaustive -- not in a comment, not in a docstring, not in a test docstring. Plan 04 had an overclaiming comment removed for exactly this reason. A comment explaining a guard should say which shape it closes, not what the check as a whole now guarantees."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT edit `_TARGET_DEFECT_CODES`, `_EXPECTED_CAUGHT_DEFECTS` or `_INCIDENTAL_GAP_CODES` in `tests/test_known_bad_corpus.py`, and MUST NOT edit any fixture under `examples/`, to absorb a verdict change. `git diff --stat 8aacd07..HEAD -- tests/test_known_bad_corpus.py examples/` is empty today and must still be empty after this plan. If a fixture's exit code moves, surface and justify it in the summary; never absorb it."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT import any third-party module on the gate path (D-01). Every guard uses the standard library only -- `isinstance`, `json` and `re`, all already imported by this module."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT implement the `scan_path:not-scanned:<reason>` decision input, or any DecisionRecord for the unscannable-entrypoint path, in this plan. WR-02 is carried forward to the end-of-phase human check as a product decision, unresolved by design, exactly as plans 04 and 05 carried it. Closing it here would settle a decision that is not this plan's to settle."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT leave any residual limit of the hardened branch undisclosed. Plan 07 owns the README text; this plan owns handing plan 07 the exact list, in its summary, of what still degrades and how -- including the deliberate asymmetry between the `.py` read (which degrades with replacement characters and is still scanned) and the `.ipynb` read (which reports NOT scanned)."
      status: unverified
      verification: flagged-unverified
---

<objective>
Close the SC5 gap in the notebook branch of `_read_source` so that every JSON
(JavaScript Object Notation) document shape which reaches it either scans, or
returns None and is named as NOT scanned -- and none of them ends the process
with an uncaught Python exception.

Purpose: a raw uncaught exception produces Python's default exit code 1, the
same number the gate uses to mean "this file was scanned and blocked". A
continuous-integration system reading only the exit code gets the right answer
for the wrong reason, and one configured to continue on error gets no answer at
all. That is the exact contract violation this phase was chartered to close,
and it is still open for five shapes.

Output: three inline guards in the `.ipynb` branch of `_read_source`, a widened
"could not be read" reason text in `check()`, and six regression tests that pin
every shape -- including one crash shape (deeply nested JSON) that neither the
verification report nor the code review found.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-VERIFICATION.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-REVIEW.md
@dsx/checks/code.py
@tests/test_dsx.py
</context>

<recorded_skips>
Two planning checkpoints were skipped this run. Both are recorded here so the
skip is visible rather than silent.

**Spec-less probe fallback.** This phase has no `-SPEC.md`, so no
`## Edge Coverage` and no `## Prohibitions` section exists to derive predicates
from. The fallback that would normally derive predicates from requirement text
cannot run either, because this phase has zero requirement identifiers
(`grep -c "11.1.1" .planning/REQUIREMENTS.md` returns 0). The gate's own
nothing-to-probe branch therefore fires: skip visibly, do not hard-fail. No
probe-derived predicates were generated. `must_haves.truths` is derived instead
from the roadmap success criteria SC1 to SC6 and from the `gaps:` and `missing:`
blocks of `11.1.1-VERIFICATION.md`, and the hard prohibitions are authored into
`must_haves.prohibitions` without check descriptors, so each disposes as
flagged-unverified rather than auto-dismissing.

**Analytical-spec gate.** The `dsx` capability is enabled globally, but its
own first instruction is to decide whether the phase is analytical at all.
This phase hardens the dsx tool's own detection code and produces no number,
model or chart, so no `ANALYSIS-SPEC.yaml` is required and the
decompose-backwards-from-the-decision rules do not apply. Plans 04 and 05 ran
the same way and no `ANALYSIS-SPEC.yaml` exists in this phase directory.
</recorded_skips>

<assumption_delta_decision>
**Primary noun:** the scan path (the parsed path, the text fallback, and the
not-scanned outcome).

**Decision:** no-change.

**Rationale:** the singular-to-plural transition the detector caught -- one text
scan becoming a parsed primary path plus a text fallback -- happened in plans 01
to 03 and was decided there. This plan introduces no new variant. It makes the
existing not-scanned outcome reachable for five more input shapes that currently
crash or silently affirm a parsed verdict. The orchestrator's reading was
checked against the code rather than accepted: `check()` already has exactly
three outcomes for a declared entrypoint (`entrypoint parsed with ast`,
`entrypoint NOT parsed -- text-only fallback scan`, and `entrypoint NOT scanned
-- could not be read`), and all three predate this plan. Nothing is promoted and
nothing is added alongside.
</assumption_delta_decision>

## Artifacts this phase produces

<artifacts_this_phase_produces>
New symbols created by this plan, listed so the source-grounding pass excludes
them from drift verification:

- `tests/test_dsx.py::TestPhase11_1Code.test_non_utf8_notebook_is_named_not_scanned_without_raising`
- `tests/test_dsx.py::TestPhase11_1Code.test_non_list_cells_value_is_named_not_scanned_without_raising`
- `tests/test_dsx.py::TestPhase11_1Code.test_empty_cells_list_still_scans_as_an_empty_notebook`
- `tests/test_dsx.py::TestPhase11_1Code.test_non_string_source_element_is_named_not_scanned_without_raising`
- `tests/test_dsx.py::TestPhase11_1Code.test_non_string_non_list_source_is_named_not_scanned_without_raising`
- `tests/test_dsx.py::TestPhase11_1Code.test_deeply_nested_notebook_json_is_named_not_scanned_without_raising`

No new module-level symbol is created in `dsx/checks/code.py`. Every guard is
inline inside the existing `_read_source` function. No new README heading is
created by this plan; plan 07 owns the documentation half.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): pin all five crash-or-silent-pass notebook shapes, plus the empty-notebook boundary</name>
  <files>tests/test_dsx.py</files>

  <read_first>
    - `tests/test_dsx.py` lines 4575 to 4592 -- the `TestPhase11_1Code` class header and its two helpers, `_entrypoint(tmp, text, name)` and `_check(tmp, entry)`. Every new test uses these.
    - `tests/test_dsx.py` lines 5908 to 5975 -- `test_non_dict_notebook_document_is_named_not_scanned_without_raising`, `test_non_dict_notebook_cell_is_named_not_scanned_without_raising` and `test_bom_notebook_entrypoint_takes_the_ast_path`. The first two are the exact pattern the new tests must follow (import `code as code_mod`, assert `_read_source` first, then assert `check()`); the third shows how to write raw bytes rather than text.
    - `dsx/checks/code.py` lines 1066 to 1161 -- the current `_read_source`, so the test author sees which guards exist rather than assuming.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-VERIFICATION.md`, the `missing:` list in the frontmatter -- the five-item closure checklist these tests must cover.
  </read_first>

  <behavior>
    Six test methods, added to `TestPhase11_1Code` immediately after
    `test_non_dict_notebook_cell_is_named_not_scanned_without_raising` so the
    malformed-notebook group stays contiguous. Five assert the not-scanned
    outcome; the sixth is the control that pins the boundary.

    For every not-scanned test, both halves are asserted, in this order:
    `code_mod._read_source(path)` returns None and raises nothing; then
    `check()` over the same file returns an empty `findings` list, has at least
    one entry in `passed_checks` containing the text `NOT scanned`, and raises
    nothing.

    - `test_non_utf8_notebook_is_named_not_scanned_without_raising` -- one file
      written with `write_bytes`, a valid notebook document whose code cell
      source contains the bytes `0xff 0xfe`. Today: raises `UnicodeDecodeError`.
    - `test_non_list_cells_value_is_named_not_scanned_without_raising` -- six
      subtests over the document texts `{"cells": 5}`, `{"cells": true}`,
      `{"cells": {}}`, `{"cells": null}`, `{"cells": "cells"}` and `{}`. Today:
      the first two raise `TypeError`, the other four return the empty string.
    - `test_empty_cells_list_still_scans_as_an_empty_notebook` -- the control.
      Document text `{"cells": []}`. Asserts `_read_source` returns the empty
      string (NOT None), that `check()` returns zero findings, and that some
      entry in `passed_checks` contains `entrypoint parsed with ast`. This test
      passes today and must still pass after Task 2.
    - `test_non_string_source_element_is_named_not_scanned_without_raising` --
      four subtests: a code cell whose `source` list holds an integer, one whose
      list holds null, one whose list holds a nested list, and a markdown cell
      whose list holds an integer. Today: all four raise `TypeError`.
    - `test_non_string_non_list_source_is_named_not_scanned_without_raising` --
      two subtests: a code cell whose `source` is a JSON object, and one whose
      `source` is the number 5. Today: both return the Python `str()` rendering
      of the value.
    - `test_deeply_nested_notebook_json_is_named_not_scanned_without_raising` --
      a document of 20,000 nested JSON arrays, built by repeating the two
      bracket characters. Today: raises `RecursionError` out of `json.loads`.
      Measured while planning on CPython 3.12.10: 20,000 raises in under one
      millisecond, 5,000 also raises, 2,000 parses cleanly. 20,000 is chosen
      for margin across the 3.10, 3.12 and 3.14 interpreters this repository
      is tested on, and costs about forty kilobytes of input.
  </behavior>

  <action>
    Add the six methods described in the behavior block to `TestPhase11_1Code`
    in `tests/test_dsx.py`, placed directly after
    `test_non_dict_notebook_cell_is_named_not_scanned_without_raising` (around
    line 5952) and before `test_bom_notebook_entrypoint_takes_the_ast_path`.

    Follow the existing pattern exactly: `from dsx.checks import code as
    code_mod` inside the method, a `tempfile.TemporaryDirectory()` per subtest,
    `Path(tmp, "entry.ipynb")` written with `write_text(..., encoding="utf-8")`
    for the text shapes and `write_bytes(...)` for the non-UTF-8 shape, then
    `self.assertIsNone(code_mod._read_source(path))`, then
    `report = self._check(tmp, "entry.ipynb")`,
    `self.assertEqual(report.findings, [])`, and
    `self.assertTrue(any("NOT scanned" in line for line in
    report.passed_checks))`.

    Do NOT use `self._entrypoint(...)` for the non-UTF-8 test -- that helper
    writes text and would re-encode the bytes. Write the file directly and pass
    the literal name `entry.ipynb` to `self._check`.

    Give each method a short docstring naming the shape it pins, the exception
    or value it produces today, and the success criterion it belongs to (SC5).
    Do not write any sentence claiming a check is sound, complete or exhaustive.

    Write no production code in this task. The guards are Task 2's work.
  </action>

  <verify>
    <automated>python -m unittest tests.test_dsx.TestPhase11_1Code -v 2>&1 | grep -E "not_scanned_without_raising|empty_cells_list"</automated>
  </verify>

  <acceptance_criteria>
    - `python -m unittest tests.test_dsx.TestPhase11_1Code -q` reports a non-zero exit code, with at least one FAIL or ERROR for each of the five new not-scanned test methods.
    - The failure reason for `test_non_list_cells_value_is_named_not_scanned_without_raising` includes both `TypeError` (for the `5` and `true` subtests) and an `assertIsNone` failure (for the `{}`, `null`, `{"cells": {}}` and `{"cells": "cells"}` subtests). Both halves of the shape class are pinned, not just the crashing half.
    - The failure reason for `test_deeply_nested_notebook_json_is_named_not_scanned_without_raising` names `RecursionError`.
    - `python -m unittest tests.test_dsx.TestPhase11_1Code.test_empty_cells_list_still_scans_as_an_empty_notebook -q` exits 0 -- the control passes before any production change.
    - `python -m unittest tests.test_dsx.TestPhase11_1Code.test_non_dict_notebook_document_is_named_not_scanned_without_raising tests.test_dsx.TestPhase11_1Code.test_non_dict_notebook_cell_is_named_not_scanned_without_raising -q` exits 0 -- plan 04's two tests are untouched and still pass.
    - `git diff --stat -- dsx/` produces no output. No production file changed in this task.
    - Committed as `test(11.1.1-06): add failing regression tests for five malformed-notebook shapes`.
  </acceptance_criteria>

  <done>
    Six new test methods exist in `TestPhase11_1Code`. Five fail or error for
    the exact reasons named above; the sixth passes. No file under `dsx/` was
    modified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): guard the notebook branch of _read_source, and widen the reason text check() prints</name>
  <files>dsx/checks/code.py</files>

  <read_first>
    - `dsx/checks/code.py` lines 1066 to 1161 -- the whole of `_read_source`. In particular: the `.py` branch's `errors="replace"` guard and the comment above it (lines 1078 to 1089), which states this module's contract for an undecodable `.py` file; and the `.ipynb` branch's `try`/`except` (lines 1090 to 1103), `isinstance(nb, dict)` guard (line 1113), cell loop (line 1116), `isinstance(cell, dict)` guard (line 1128) and source join (lines 1137 to 1138).
    - `dsx/checks/code.py` lines 547 to 559 -- `check()`'s `source is None` branch and the exact reason text it prints today.
    - `dsx/checks/code.py` around the `_parse_source` fallback, lines 1058 to 1064 -- the exception tuple `(SyntaxError, ValueError, RecursionError, MemoryError)` already used for the parse step. The read step's tuple is being brought into line with it, so read it rather than inventing a new set.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-REVIEW.md` lines 52 to 132 -- findings CR-01, CR-02 and CR-03 with their suggested fixes. Treat those snippets as prior art to check against the current file, not as text to paste: CR-02's suggested guard permits a missing `cells` key through to the empty-string path, and this plan deliberately does not (see the action below).
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-AST-DESIGN.md` line 13 -- the blocker-table row on the undecodable entrypoint, which is about the `.py` branch and is the reason the two branches are deliberately asymmetric here.
  </read_first>

  <behavior>
    After this task, for a path whose suffix is `.ipynb`:

    - bytes that are not valid UTF-8, a JSON document that will not decode, a
      document nested past the interpreter's recursion limit, and a read that
      exhausts memory all return None;
    - a `cells` value that is not a list returns None, including when the key is
      absent or null;
    - a `cells` value that is an empty list still returns the empty string;
    - a cell whose `source` is absent or null contributes the empty string;
    - a cell whose `source` is a string contributes that string;
    - a cell whose `source` is a list of strings contributes their
      concatenation;
    - a cell whose `source` is anything else, including a list with one
      non-string element, returns None;
    - every other behaviour of the branch -- the markdown character-wise
      blanking, the dropping of raw and unknown cell types, the newline join --
      is unchanged.
  </behavior>

  <action>
    Make four edits, all inside `dsx/checks/code.py`.

    First, the read-and-decode exception tuple. Replace the `.ipynb` branch's
    `except (OSError, json.JSONDecodeError):` with an explicit five-member tuple
    naming `OSError`, `UnicodeDecodeError`, `json.JSONDecodeError`,
    `RecursionError` and `MemoryError`, still returning None. Two of those are
    subclasses of `ValueError`; name them individually anyway, so the tuple
    documents which failures are expected rather than swallowing every
    `ValueError` the block might ever raise. `RecursionError` and `MemoryError`
    are the same two the parse step already guards -- the read step was the
    only step in this function without them.

    Second, decide the decode contract for `.ipynb` and record it. This plan
    locks a hard None-return rather than the `errors="replace"` degradation the
    `.py` branch three lines above uses. Write a comment saying so and why: a
    replacement-character decode of a JSON document either fails to parse
    anyway, or succeeds and produces cell text that differs from the file's
    bytes, which `check()` would then report under the affirmative `entrypoint
    parsed with ast` line -- a parsed verdict claimed over text the tool could
    not actually decode. Returning None routes to the controlled `NOT scanned`
    line instead. The comment must state the asymmetry with the `.py` branch
    plainly; it must not claim any check is sound.

    Third, the `cells` guard. Immediately after the existing `isinstance(nb,
    dict)` guard, bind the value once to a local named `cells` and return None
    unless `isinstance(cells, list)` holds, then iterate `cells` directly rather
    than the `or []` expression. This is deliberately stricter than the code
    review's suggested `cells is not None and not isinstance(cells, list)`: a
    document with no `cells` key at all, or with `cells` set to null, is not a
    notebook, and letting it through produces the affirmative parsed pass line
    over a document nothing was read from -- the silent pass plan 04's own
    comment argues against for the non-object-cell case. An empty list is a
    list, so a legitimately empty notebook still scans as the empty string, and
    that boundary is pinned by Task 1's control test.

    Fourth, the `source` shape guard. Replace the `cell.get("source") or ""`
    binding and the `"".join(src) if isinstance(src, list) else str(src)`
    expression with an explicit four-way shape test: absent or null gives the
    empty string; a string is used as-is; a list every element of which is a
    string is joined; anything else returns None. This closes the code review's
    CR-03 (a non-string element in a list) and also removes the `str()`
    coercion, under which a `source` that is a JSON object or a number is
    scanned as the Python rendering of that value -- text the notebook does not
    contain. Keep the markdown branch's character-wise blanking exactly as it
    is; it now only ever receives a string.

    Then update the reason text in `check()`'s `source is None` branch so it
    names the whole class of failures that now routes there. Keep the leading
    `entrypoint NOT scanned` and the trailing `(no leak scan ran)` untouched --
    the passed-check assertions in the suite key on `NOT scanned`. Widen the
    parenthetical to cover: unreadable, undecodable, unsupported suffix, invalid
    or too deeply nested notebook JSON, and a notebook document whose cells or
    cell sources are not the shapes the notebook format defines. Update the
    comment above it in the same edit, which currently enumerates the older,
    narrower class.

    Do not add a `scan_path:not-scanned:` decision input and do not construct a
    DecisionRecord on this path. That is WR-02, carried forward to the
    end-of-phase human check unresolved by design.

    Do not touch `tests/test_known_bad_corpus.py` or anything under `examples/`.
  </action>

  <verify>
    <automated>python -m unittest tests.test_dsx.TestPhase11_1Code -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -m unittest tests.test_dsx.TestPhase11_1Code -q` exits 0 and reports at least 135 tests (129 before this plan, plus the six from Task 1).
    - All six Task 1 methods pass, including the control `test_empty_cells_list_still_scans_as_an_empty_notebook`.
    - `grep -n "isinstance(cells, list)" dsx/checks/code.py` returns exactly one line.
    - `grep -c "UnicodeDecodeError" dsx/checks/code.py` returns at least 2 -- one in the `.py` branch that was already there, one in the `.ipynb` branch's new tuple.
    - `grep -n "RecursionError" dsx/checks/code.py` returns at least two lines: the existing parse-step guard and the new read-step guard.
    - `grep -v '^#' dsx/checks/code.py | grep -c "scan_path:not-scanned"` returns 0 -- WR-02 was not closed in code by this task.
    - `git diff --stat -- tests/test_known_bad_corpus.py examples/` produces no output.
    - `python -m unittest discover -s tests -q` exits 0 and reports at least 900 tests.
    - Committed as `fix(11.1.1-06): guard every JSON shape the notebook read can meet`.
  </acceptance_criteria>

  <done>
    Every shape in the behavior block behaves as stated, proven by the six tests
    from Task 1 passing and by the full suite staying green. `check()`'s
    not-scanned reason names the whole class of failures that reaches it.
  </done>
</task>

<task type="auto">
  <name>Task 3: re-prove the regression surface on both interpreters and hand plan 07 the residual list</name>
  <files>.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-06-SUMMARY.md (verification only — no source file is modified by this task)</files>

  <read_first>
    - `scripts/check.sh` -- the phase gate. Note that it runs `python3`, that its gate-contract loop asserts a missing spec exits 2, and that it ends by printing `all checks passed`.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-VERIFICATION.md`, the SC6 row of the success-criteria table -- the exact commands and counts the verifier used, so this run is comparable rather than merely green.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-05-SUMMARY.md` lines 400 to 460 -- the two carried-forward items, restated verbatim by plan 07, so this task's summary hands them over unchanged.
  </read_first>

  <action>
    Run the regression surface and record the measured numbers in the summary,
    not the expected ones. Run, in order: the target class, the full suite on
    `python`, the full suite on `python3`, the known-bad corpus, the
    corpus-and-fixture diff check against `8aacd07`, and the full phase gate.

    Then write, in the summary, the residual list plan 07 needs. It has four
    entries, and plan 07's README text is written from it, so state each one as
    a sentence a reader could act on rather than as a label:

    1. the deliberate asymmetry between the two reads -- a `.py` file whose
       bytes are not valid UTF-8 is still scanned, with the undecodable bytes
       replaced, while a `.ipynb` file whose bytes are not valid UTF-8 is
       reported as NOT scanned;
    2. a notebook nested more deeply than the running interpreter's recursion
       limit is reported as NOT scanned rather than scanned, so a legitimately
       very deeply nested document would read as unscannable;
    3. a notebook large enough to exhaust memory during the read is reported as
       NOT scanned if the failure surfaces as `MemoryError`, and no size bound
       is enforced before the read -- the same exposure the `.py` read already
       has;
    4. NOT scanned is a passing outcome with exit code 0, and its pass line is
       only printed under `--verbose`, so a notebook corrupted deliberately or
       accidentally makes the leak scan quiet rather than loud. This is the
       disclosure half of WR-02 and must be handed over as a limit to disclose,
       not as a defect to fix.

    Record in the summary that WR-02 and the D7/D8 README tone judgment are
    carried forward unresolved, and that no task in this plan touched either.
  </action>

  <verify>
    <automated>bash scripts/check.sh</automated>
  </verify>

  <acceptance_criteria>
    - `python -m unittest tests.test_dsx.TestPhase11_1Code -q` exits 0; the reported test count is written into the summary.
    - `python -m unittest discover -s tests -q` exits 0 and reports at least 900 tests; the count is written into the summary.
    - `python3 -m unittest discover -s tests -q` exits 0 -- the suite passes on the 3.14 interpreter as well as the 3.12 one.
    - `python -m unittest tests.test_known_bad_corpus -q` exits 0 and reports 30 tests.
    - `git diff --stat 8aacd07..HEAD -- tests/test_known_bad_corpus.py examples/` produces no output.
    - `bash scripts/check.sh` exits 0 and its final line is `all checks passed`.
    - The summary contains all four residual entries from the action, each written as a sentence, and states that WR-02 and the D7/D8 tone judgment were not touched.
    - Committed as `docs(11.1.1-06): record the regression proof and the residual list for plan 07`.
  </acceptance_criteria>

  <done>
    Every command above ran and its real result is recorded in the summary. Plan
    07 has the four-entry residual list it needs to write accurate README text.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec to gate | `reproducibility.entrypoint` in a user-supplied ANALYSIS-SPEC names a file path the gate opens. The path and the file's whole content are untrusted input. |
| file to parser | The bytes of that file cross into `path.read_text`, `json.loads` and `ast.parse`, none of which the gate controls the input shape of. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.1.1-19 | Denial of Service | `dsx/checks/code.py::_read_source`, the `.ipynb` read step | high | mitigate | A crafted or corrupted notebook that raises `UnicodeDecodeError`, `TypeError` or `RecursionError` ends the process with Python's default exit code 1, which a continuous-integration system reads as a legitimate gate block, and which a job configured to continue on error turns into no verdict at all. Task 2 adds the five-member exception tuple and the two shape guards; Task 1 pins every shape with a test that asserts both `_read_source` and `check()` raise nothing. |
| T-11.1.1-20 | Spoofing | the gate's exit code, as read by an automated caller | high | mitigate | Exit code 1 currently carries two different meanings on this path -- "scanned and blocked" and "crashed". Task 2 removes the second meaning for every shape reachable through the notebook branch. `bash scripts/check.sh` asserts the surviving contract directly, including that a missing spec exits 2. |
| T-11.1.1-21 | Denial of Service | `path.read_text` and `json.loads` on an arbitrarily large notebook | medium | accept | No size bound is enforced before the read, so a sufficiently large declared entrypoint can exhaust memory. `MemoryError` is now caught and routed to NOT scanned where it surfaces inside the guarded block, but a failure outside it is not caught. Accepted at ASVS (Application Security Verification Standard) level 1: the file is one the user named in their own spec, the same exposure the `.py` read already carries, and a bound would be a new policy this plan has no mandate to set. Disclosed by plan 07 rather than silently accepted. |
| T-11.1.1-22 | Elevation of privilege | the NOT scanned outcome as a way to quiet the gate | medium | accept | NOT scanned is a passing outcome with exit code 0 and a pass line printed only under `--verbose`, so saving a notebook in a non-UTF-8 encoding makes the leak scan stop complaining. Accepted: this gate exists for a user checking their own work, not as an adversarial control, and every other check still runs. The visibility half is WR-02, carried forward to the end-of-phase human check; the disclosure half is plan 07's fourth residual entry. |
| T-11.1.1-23 | Tampering | the new guards themselves | low | mitigate | A guard that can raise moves the crash rather than removing it. Every guard added is an `isinstance` test or a generator of `isinstance` tests over an already-decoded value; none calls a user-controlled callable, indexes without a preceding type test, or formats an untrusted value into a message. Pinned by the six tests, which assert that nothing is raised rather than only that the return value is right. |

No package-manager install occurs in this plan, so the package-legitimacy gate
is not engaged and no `T-11.1.1-SC` supply-chain threat is opened. D-01 forbids
third-party imports on the gate path and every guard here uses the standard
library only.
</threat_model>

<verification>
1. `python -m unittest tests.test_dsx.TestPhase11_1Code -q` exits 0 with at
   least 135 tests.
2. `python -m unittest discover -s tests -q` exits 0 with at least 900 tests.
3. `python3 -m unittest discover -s tests -q` exits 0.
4. `python -m unittest tests.test_known_bad_corpus -q` exits 0 with 30 tests.
5. `git diff --stat 8aacd07..HEAD -- tests/test_known_bad_corpus.py examples/`
   produces no output.
6. `bash scripts/check.sh` exits 0 and prints `all checks passed`.
7. `grep -v '^#' dsx/checks/code.py | grep -c "scan_path:not-scanned"` returns
   0, confirming WR-02 was not closed here.
</verification>

<success_criteria>
Every one of the eleven malformed-notebook document shapes named in
`must_haves.truths` returns None from `_read_source` and produces zero findings
plus a `NOT scanned` passed-check line from `check()`, with nothing raised at
either level; `{"cells": []}` still scans as an empty notebook; the full suite,
the known-bad corpus and the phase gate are green on both interpreters; and no
corpus expectation map or fixture was edited.
</success_criteria>

<output>
Create `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-06-SUMMARY.md`
when done, including the four-entry residual list plan 07 needs and the
restatement that WR-02 and the D7/D8 tone judgment stay unresolved.
</output>
