---
phase: 15-cuped-and-bi-declaration-checks-new-codes-d-05
plan: 06
type: execute
wave: 3
depends_on: [15-02, 15-04]
files_modified:
  - scripts/gen-finding-catalogue.py
  - references/finding-codes.md
  - tests/test_finding_catalogue_invariant.py
autonomous: true
requirements: [REQ-P15-07]
tags: [dsx, finding-catalogue, regen, D-05-allowlist, D-08-additive-rebaseline, invariant, 258-to-260, snapshot-frozen, D-09]

must_haves:
  truths:
    - "`scripts/gen-finding-catalogue.py::_D05_ALLOWLIST_CODES` gains the two EXACT strings `DSX-EXP-070` and `DSX-MET-021` (never the `DSX-EXP-`/`DSX-MET-` family prefixes — those carry legacy uncited members and would turn the build red), so the D-05 build gate now enforces the two new codes' Citation + Reference-value/Structural-criterion docstring lines and `# D-05:` test markers (REQ-P15-07 / D-09)."
    - "`references/finding-codes.md` is REGENERATED (not hand-edited) via `python scripts/gen-finding-catalogue.py --write` to `**Total: 260 codes.**`, adding a DSX-EXP-070 (CRITICAL) row in the EXP group and a DSX-MET-021 (HIGH) row in the MET group with the two fixed literal titles; `python scripts/gen-finding-catalogue.py --check` then exits 0 (REQ-P15-07 / trap #10)."
    - "`tests/test_finding_catalogue_invariant.py` is rebaselined ADDITIVELY (D-08): `_EXPECTED_TOTAL` 258→260; `_MINTED_CODES` becomes `{DSX-REP-060, DSX-REP-061, DSX-EXP-070, DSX-MET-021}` so the expected set is `snapshot ∪ {REP-060, REP-061, EXP-070, MET-021}`; `_SNAPSHOT_TOTAL` stays 256; the two test-method names, docstrings and 258/phase-16 prose are updated for internal consistency; both legs pass."
    - "The byte-frozen `tests/fixtures/finding-codes-phase12.md` (256) is NEVER mutated — the same separate-snapshot-leg pattern Phase 16 used for 256→258 (D-08 / trap #3); the invariant's snapshot-length leg stays anchored to the separate literal 256."
  artifacts:
    - scripts/gen-finding-catalogue.py
    - references/finding-codes.md
    - tests/test_finding_catalogue_invariant.py
  key_links:
    - "The regen walks the real `report.add(...)` call sites: DSX-EXP-070 in dsx/checks/design.py (15-04) and DSX-MET-021 in dsx/checks/metrics.py (15-02) — this plan `depends_on: [15-02, 15-04]` so BOTH sites exist before `--write` yields 260 and `--check` expects 260. Between those mints and this regen the catalogue is intentionally stale; this plan is the single, final regen point."
    - "The D-05 gate (check_d05) reads each allowlisted code's ENCLOSING-function docstring for `Citation:` + `Reference value:`/`Structural criterion:` and a `# D-05: <CODE>` test marker under tests/: `_check_cuped` (15-04) and `_check_cohort_denominator_shift` (15-02) carry those docstring lines, and tests/test_cuped.py + tests/test_cohort_denominator.py carry the two markers — so adding the exact codes to the allowlist passes green, not red."
  prohibitions:
    - statement: "Add the two codes to `_D05_ALLOWLIST_CODES` as EXACT strings only — never add `DSX-EXP-`/`DSX-MET-` to `_D05_ALLOWLIST_PREFIXES`. A prefix add would drag those pre-existing families' legacy uncited members into D-05 enforcement and fail the build red (D-09)."
      status: enforced
      verification: "`_D05_ALLOWLIST_PREFIXES` is byte-unchanged; `_D05_ALLOWLIST_CODES` contains both new exact strings; `gen-finding-catalogue.py --check` exits 0."
    - statement: "`references/finding-codes.md` is REGENERATED by the generator, never hand-edited; `tests/fixtures/finding-codes-phase12.md` is NEVER mutated (byte-frozen at 256, D-08 / trap #3)."
      status: enforced
      verification: "`git status --porcelain -- tests/fixtures/finding-codes-phase12.md` is empty; references/finding-codes.md matches `gen-finding-catalogue.py`'s output (--check exit 0)."
    - statement: "No dsx/ gate module, spec vocabulary, fixture, or tracking file is edited by this plan — it only regenerates the catalogue, adds the D-09 allowlist entries, and rebaselines the invariant test."
      status: enforced
      verification: "`git status --porcelain -- dsx/ examples/ .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md` is empty for this plan."
---

<objective>
Deliver REQ-P15-07: close the catalogue over the two new codes. Add `DSX-EXP-070` and `DSX-MET-021` to
`_D05_ALLOWLIST_CODES` in scripts/gen-finding-catalogue.py by exact code (D-09), regenerate
references/finding-codes.md to 260 with `--write`, confirm `--check` exits 0, and rebaseline
tests/test_finding_catalogue_invariant.py additively (258→260) while leaving the byte-frozen Phase-12
snapshot at 256.

Purpose: this is the phase's single, final regen point. Splitting the two mints across waves 1 and 2
(DSX-MET-021 in 15-02, DSX-EXP-070 in 15-04) leaves the catalogue intentionally stale until now; this
plan depends on both so every `report.add` site exists before the generator is run. The D-09 exact-code
allowlist is what turns the two new codes' D-05 obligations (Citation + Reference value/Structural
criterion docstring lines and `# D-05:` test markers, all shipped in 15-02/15-04) from convention into
an enforced build gate — added by exact string, never by family prefix, because DSX-EXP-* and DSX-MET-*
are pre-existing families with legacy uncited members. The invariant moves 258→260 additively over the
byte-frozen 256 snapshot, catching any silent mint or drop beyond the sanctioned four-code delta.

Output: the D-09 allowlist entries, the regenerated 260-code catalogue, and the additively-rebaselined
invariant — the frozen snapshot untouched.
</objective>

<artifacts_this_phase_produces>
This plan (15-06) runs in wave 3, `depends_on: [15-02, 15-04]`, and is the sole writer of
scripts/gen-finding-catalogue.py, references/finding-codes.md and tests/test_finding_catalogue_invariant.py
in Phase 15. It edits three files:
- `scripts/gen-finding-catalogue.py` (edit — add the two exact codes to `_D05_ALLOWLIST_CODES`)
- `references/finding-codes.md` (REGENERATE via the generator — 258 → 260)
- `tests/test_finding_catalogue_invariant.py` (D-08 additive rebaseline: `_EXPECTED_TOTAL` 258→260; `_MINTED_CODES` +EXP-070+MET-021; prose + method names updated; `_SNAPSHOT_TOTAL` stays 256)

Its `files_modified` are disjoint from the other wave-3 plan (15-05 owns examples/good-ANALYSIS-SPEC.yaml
+ its test). It depends on 15-02 (DSX-MET-021 report.add site + docstring + marker) and 15-04
(DSX-EXP-070 report.add site + docstring + marker) — both must exist before the regen yields 260 and
the D-05 gate can pass. See 15-01 for the full phase wave structure and single-writer justification.

## Phase-level requirement coverage (union across all six Phase-15 plans)
- REQ-P15-01 — plan 15-01; REQ-P15-02 — plan 15-04; REQ-P15-03 — plan 15-05; REQ-P15-04 — plan 15-02;
  REQ-P15-05 — plan 15-03; REQ-P15-06 — plan 15-03; REQ-P15-07 — **this plan (15-06)**.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cuped-and-bi-declaration-checks-new-codes-d-05/15-CONTEXT.md
@scripts/gen-finding-catalogue.py
@tests/test_finding_catalogue_invariant.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two exact codes to _D05_ALLOWLIST_CODES and regenerate the catalogue to 260</name>
  <files>scripts/gen-finding-catalogue.py, references/finding-codes.md</files>
  <read_first>
    - scripts/gen-finding-catalogue.py:87-89 (`_D05_ALLOWLIST_PREFIXES` — must stay byte-unchanged; do NOT add DSX-EXP-/DSX-MET- here)
    - scripts/gen-finding-catalogue.py:139-154 (`_D05_ALLOWLIST_CODES` frozenset + the DSX-COH-040 precedent comment — add `DSX-EXP-070` and `DSX-MET-021` as exact strings, with a Phase-15 comment mirroring the COH-040 rationale: a single code inside a pre-existing family whose legacy siblings carry no citation)
    - scripts/gen-finding-catalogue.py:336-366 (`check_d05` — reads each allowlisted code's enclosing-function docstring for `Citation:` + `Reference value:`/`Structural criterion:` and a `# D-05: <CODE>` marker under tests/; these exist from 15-02/15-04, so the allowlist add passes green)
    - scripts/gen-finding-catalogue.py:246-276 (`render` — writes `**Total: N codes.**` and the per-family tables; --write rewrites references/finding-codes.md from the real report.add sites)
    - scripts/gen-finding-catalogue.py:422-455 (main: --write rewrites the file; --check compares + runs the D-05 and families gates; the pre-existing "declared twice" stderr warnings are known shipped-tree noise and do not change the exit code)
  </read_first>
  <action>
    First add the two exact codes to `_D05_ALLOWLIST_CODES` in scripts/gen-finding-catalogue.py: insert
    `"DSX-EXP-070"` and `"DSX-MET-021"` into the frozenset (exact strings). Add a short Phase-15 comment
    above the set, mirroring the DSX-COH-040 rationale: each lives inside a pre-existing family
    (DSX-EXP-*, DSX-MET-*) whose legacy siblings carry no Citation/Structural-criterion docstring line,
    so a `_D05_ALLOWLIST_PREFIXES` add would fail the build red; the exact-code path (Phase 6's
    DSX-SPEC-080..086 precedent) obligates only the two new codes. Do NOT touch `_D05_ALLOWLIST_PREFIXES`.

    Then REGENERATE the catalogue: run `python scripts/gen-finding-catalogue.py --write`. After 15-02 and
    15-04, this walks the real report.add sites and adds the DSX-MET-021 (HIGH) row in the MET group and
    the DSX-EXP-070 (CRITICAL) row in the EXP group, updating the total line to `**Total: 260 codes.**`.
    Confirm the two new rows read exactly, with the two fixed literal titles from 15-02/15-04. Then run
    `python scripts/gen-finding-catalogue.py --check` and confirm it exits 0 (the D-05 gate now enforces
    the two new codes and passes because their docstrings and test markers exist). The pre-existing
    "declared twice" warnings (DSX-CLM/COH/PAR/SPEC/VAL) on stderr are known shipped-tree noise; do not
    attempt to fix them. Do not hand-edit references/finding-codes.md — the generator writes it.
  </action>
  <acceptance_criteria>
    - `_D05_ALLOWLIST_CODES` contains `DSX-EXP-070` and `DSX-MET-021` (exact strings); `_D05_ALLOWLIST_PREFIXES` is byte-unchanged.
    - references/finding-codes.md declares `**Total: 260 codes.**` and contains a DSX-EXP-070 (CRITICAL) row and a DSX-MET-021 (HIGH) row with the two fixed literal titles.
    - `python scripts/gen-finding-catalogue.py --check` exits 0 (catalogue current at 260; D-05 and families gates pass).
    - references/finding-codes.md was regenerated, not hand-edited (it matches the generator output).
  </acceptance_criteria>
  <verify>
    <automated>python scripts/gen-finding-catalogue.py --write && python scripts/gen-finding-catalogue.py --check && python -c "import importlib.util,pathlib; spec=importlib.util.spec_from_file_location('gfc','scripts/gen-finding-catalogue.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert {'DSX-EXP-070','DSX-MET-021'} <= set(m._D05_ALLOWLIST_CODES), m._D05_ALLOWLIST_CODES; assert 'DSX-EXP-' not in m._D05_ALLOWLIST_PREFIXES and 'DSX-MET-' not in m._D05_ALLOWLIST_PREFIXES" && [ "$(grep -c 'Total: 260 codes' references/finding-codes.md)" -ge 1 ] && [ "$(grep -c 'DSX-EXP-070' references/finding-codes.md)" -ge 1 ] && [ "$(grep -c 'DSX-MET-021' references/finding-codes.md)" -ge 1 ]</automated>
  </verify>
  <done>scripts/gen-finding-catalogue.py allowlists DSX-EXP-070 and DSX-MET-021 by exact code (prefixes untouched), references/finding-codes.md is regenerated to 260 with the two new family rows, and `--check` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Rebaseline tests/test_finding_catalogue_invariant.py additively (258 → 260)</name>
  <files>tests/test_finding_catalogue_invariant.py</files>
  <read_first>
    - tests/test_finding_catalogue_invariant.py:30-39 (`_EXPECTED_TOTAL = 258`, `_SNAPSHOT_TOTAL = 256`, `_MINTED_CODES = {"DSX-REP-060", "DSX-REP-061"}` — bump _EXPECTED_TOTAL to 260, add the two Phase-15 codes to _MINTED_CODES, keep _SNAPSHOT_TOTAL at 256)
    - tests/test_finding_catalogue_invariant.py:1-15 (module docstring naming Phase 16 and 256→258 — update the prose to describe the additive Phase-15 mint at 260 over the frozen 256, keeping the CRLF discipline note)
    - tests/test_finding_catalogue_invariant.py:52-95 (`test_finding_catalogue_stays_at_258_codes` — rename to `..._260_codes`; update its docstring and the "258"/"Phase 16" assertion messages to read honestly at 260 for the additive Phase-15 mint; the count-leg logic already reads `_EXPECTED_TOTAL`)
    - tests/test_finding_catalogue_invariant.py:98-131 (`test_code_set_is_phase12_snapshot_plus_the_phase16_mint` — rename to include the Phase-15 mint, e.g. `test_code_set_is_phase12_snapshot_plus_the_phase15_and_phase16_mints`; update docstring/messages; the set-identity leg already compares against `snapshot_set | _MINTED_CODES` and the snapshot-length leg against `_SNAPSHOT_TOTAL`, so only the constants + prose change)
  </read_first>
  <action>
    Rebaseline tests/test_finding_catalogue_invariant.py additively (D-08):
    (a) change `_EXPECTED_TOTAL = 258` to `_EXPECTED_TOTAL = 260`;
    (b) change `_MINTED_CODES` to `{"DSX-REP-060", "DSX-REP-061", "DSX-EXP-070", "DSX-MET-021"}` — the
        expected live set is now `snapshot ∪ {REP-060, REP-061, EXP-070, MET-021}`;
    (c) keep `_SNAPSHOT_TOTAL = 256` UNCHANGED — the byte-frozen Phase-12 snapshot stays at 256 while the
        live catalogue is 260; conflating them would break the snapshot-length leg (trap #3);
    (d) rename `test_finding_catalogue_stays_at_258_codes` to `test_finding_catalogue_stays_at_260_codes`
        and update its docstring and assertion messages so the "258"/"Phase 16" prose reads honestly for
        the additive Phase-15 mint at 260;
    (e) rename `test_code_set_is_phase12_snapshot_plus_the_phase16_mint` to name the Phase-15 mint too
        (e.g. `..._plus_the_phase15_and_phase16_mints`), updating its docstring and the added/removed diff
        message to describe the `∪ {DSX-REP-060, DSX-REP-061, DSX-EXP-070, DSX-MET-021}` delta;
    (f) update the module docstring's 256→258 / Phase-16 prose to describe the additive Phase-15 mint at
        260 over the frozen 256, keeping the CRLF-discipline note.

    Do NOT edit tests/fixtures/finding-codes-phase12.md — it is the byte-frozen 256-code anchor and must
    stay untouched. Do not touch any tracking file.
  </action>
  <acceptance_criteria>
    - `_EXPECTED_TOTAL == 260`; `_MINTED_CODES == {"DSX-REP-060","DSX-REP-061","DSX-EXP-070","DSX-MET-021"}`; `_SNAPSHOT_TOTAL == 256` (unchanged).
    - Both test methods are renamed to name the 260 count and the Phase-15+16 mint; their docstrings and messages read honestly at 260.
    - `tests/fixtures/finding-codes-phase12.md` is byte-unchanged (still 256).
    - `python -m unittest tests.test_finding_catalogue_invariant` passes both legs.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_finding_catalogue_invariant >/dev/null 2>&1 && python -c "import importlib.util, ast; spec=importlib.util.spec_from_file_location('inv','tests/test_finding_catalogue_invariant.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert m._EXPECTED_TOTAL==260, m._EXPECTED_TOTAL; assert m._SNAPSHOT_TOTAL==256, m._SNAPSHOT_TOTAL; assert set(m._MINTED_CODES)=={'DSX-REP-060','DSX-REP-061','DSX-EXP-070','DSX-MET-021'}, m._MINTED_CODES; t=ast.parse(open('tests/test_finding_catalogue_invariant.py',encoding='utf-8').read()); ms={f.name for f in ast.walk(t) if isinstance(f,ast.FunctionDef)}; assert any('260' in n for n in ms), ms" && [ -z "$(git status --porcelain -- tests/fixtures/finding-codes-phase12.md)" ]</automated>
  </verify>
  <done>tests/test_finding_catalogue_invariant.py is rebaselined to _EXPECTED_TOTAL=260 with _MINTED_CODES={REP-060,REP-061,EXP-070,MET-021}, _SNAPSHOT_TOTAL held at 256, both methods renamed and their prose updated, and the byte-frozen Phase-12 snapshot untouched — both legs pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan regenerates a generated documentation artifact, extends a build-gate allowlist, and
rebaselines a test. It introduces no runtime gate surface, no network/auth/input, and executes only the
generator and the test over local source. No npm/pip/cargo install occurs, so T-15-SC is N/A this plan.

| Boundary | Description |
|----------|-------------|
| report.add sites → generator → catalogue | The generator walks dsx/**.py report.add sites (including 15-02/15-04's new codes) and rewrites references/finding-codes.md; --check re-derives currency and the D-05/families gates. |
| _D05_ALLOWLIST_CODES → check_d05 | Adding the two exact codes turns their D-05 obligations into an enforced gate; the docstrings + markers from 15-02/15-04 satisfy it. |
| _MINTED_CODES + _SNAPSHOT_TOTAL → invariant | The two-leg invariant pins count 260 AND set-identity vs snapshot ∪ {four codes}; the frozen 256 snapshot anchor makes any drift name itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-60 | Tampering | Adding DSX-EXP-/DSX-MET- to `_D05_ALLOWLIST_PREFIXES`, dragging legacy uncited family members into D-05 enforcement and failing the build red | high | mitigate | D-09: only the two exact codes are allowlisted; the verify asserts `_D05_ALLOWLIST_PREFIXES` is byte-unchanged and `--check` exits 0. |
| T-15-61 | Tampering | Hand-editing references/finding-codes.md or a cardinality-preserving mint/drop slipping the count | high | mitigate | trap #10 / D-08: the file is regenerated by the generator (--check exit 0), and the invariant's set-identity leg compares against snapshot ∪ {REP-060,REP-061,EXP-070,MET-021}, naming any code beyond the sanctioned delta. |
| T-15-62 | Tampering | Mutating the byte-frozen Phase-12 snapshot to 260 to "make the leg pass", erasing the additive-mint audit | high | mitigate | D-08 / trap #3: `_SNAPSHOT_TOTAL` stays 256, the snapshot-length leg anchors to it, and the verify asserts `git status` on tests/fixtures/finding-codes-phase12.md is empty. |
| T-15-63 | Tampering | A stale invariant (docstrings/method names still say 258/256) masking the real 260 baseline | medium | mitigate | D-08 internal-consistency: the method names and prose are updated to 260 and the Phase-15+16 mint; the verify asserts a method name carries '260' and the three constants hold. |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs occur; the change is a generator regen, an allowlist add, and a test rebaseline. |
</threat_model>

<verification>
This plan delivers REQ-P15-07: `scripts/gen-finding-catalogue.py --check` exits 0 on the new codes at
260, and both canonical fixtures still satisfy D-08 (the good fixture stays clean per 15-05; the frozen
Phase-12 snapshot stays byte-identical at 256).

For this plan specifically:
- `python scripts/gen-finding-catalogue.py --write` then `--check` exits 0 at 260; the DSX-EXP-070 (CRITICAL) and DSX-MET-021 (HIGH) rows are present with their fixed titles.
- `python -m unittest tests.test_finding_catalogue_invariant` passes both legs at _EXPECTED_TOTAL=260 with _MINTED_CODES={REP-060,REP-061,EXP-070,MET-021} and _SNAPSHOT_TOTAL=256.
- `git status --porcelain -- tests/fixtures/finding-codes-phase12.md dsx/ examples/ .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md` is empty (snapshot frozen; no gate/fixture/tracking edit).
</verification>

<success_criteria>
- DSX-EXP-070 and DSX-MET-021 are allowlisted by exact code (never prefix); `_D05_ALLOWLIST_PREFIXES` is byte-unchanged (D-09).
- references/finding-codes.md is regenerated to 260 with the two new family rows; `gen-finding-catalogue.py --check` exits 0 (REQ-P15-07 / trap #10).
- The invariant is rebaselined additively to 260 with the four-code minted set and a separate 256 snapshot anchor; both legs pass (D-08).
- The byte-frozen Phase-12 snapshot is untouched; no gate module, fixture or tracking file is edited (trap #3).
</success_criteria>

<output>
Create `.planning/phases/15-cuped-and-bi-declaration-checks-new-codes-d-05/15-06-SUMMARY.md` when done.
</output>
