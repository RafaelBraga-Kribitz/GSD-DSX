---
phase: 15-cuped-and-bi-declaration-checks-new-codes-d-05
plan: 05
type: execute
wave: 3
depends_on: [15-01, 15-02, 15-04]
files_modified:
  - examples/good-ANALYSIS-SPEC.yaml
  - tests/test_good_fixture_phase15.py
autonomous: true
requirements: [REQ-P15-03]
tags: [dsx, good-fixture, thin-fields, cohort-grain, cohort-comparisons, funnel-steps, cuped-pre-experiment, D-04, D-08, silent-at-every-threshold, trap-2]

must_haves:
  truths:
    - "`examples/good-ANALYSIS-SPEC.yaml` accepts thin cohort/funnel BI fields — a `metrics[].cohort_grain` label, a well-behaved `results.cohort_comparisons[]` (equal sampling_rate or `reweighted: true`), a monotone `results.funnel_steps[]`, and a `design.cuped` block with `covariate_timing: pre_experiment` — and STILL passes every gate at every severity threshold (plan/execute/verify/ship), drawing ZERO findings including from the new DSX-MET-021 and DSX-EXP-070 checks (REQ-P15-03 / D-04 / D-08 / trap #2)."
    - "The new checks `report.ok`/return on the good-fixture values — never `.add`: DSX-MET-021 is silent because the declared buckets share a sampling_rate (or declare `reweighted: true`); DSX-EXP-070 is silent because the CUPED covariate_timing is `pre_experiment` — proven by asserting neither code is in the good fixture's finding set at any gate point (trap #2)."
    - "Every new key is TOP-LEVEL under `results`/`metrics`/`design`, never inside `validity_frame.exclusions` (the one strict-key block, spec.py:1184), so `frame_digest` is unchanged and the D-08 canonical good fixture is extended, not replaced (D-04)."
    - "This plan mints NO finding code and edits no gate code — it extends a fixture and adds one silence-proof test; the catalogue count is untouched by this plan (D-08)."
  artifacts:
    - examples/good-ANALYSIS-SPEC.yaml
    - tests/test_good_fixture_phase15.py
  key_links:
    - "The good fixture already has randomization_unit == analysis_unit == user (design.py:132-133), so adding `variance_adjustment: cuped` does not perturb `_check_units` (which returns ok on matched units before reading the adjustment); `_check_cuped` then runs on the cuped adjustment and report.ok's on `pre_experiment` — the pre-experiment PASS path REQ-P15-02 requires, proven here at every threshold."
    - "`results.cohort_comparisons[]` here declares buckets with equal `sampling_rate` (or `reweighted: true`) so DSX-MET-021's spread-vs-tolerance test resolves to report.ok; `results.funnel_steps[]` is a natural monotone drop-off that no check fires on (D-04) — both are documentation-grade thin fields the tolerant results parser accepts without a schema key."
  prohibitions:
    - statement: "No new key is placed inside `validity_frame` or its `exclusions` sub-block — all additions are top-level under results/metrics/design, so frame_digest is unchanged (D-04). The good fixture is extended, never replaced (D-08)."
      status: enforced
      verification: "tests/test_good_fixture_phase15.py asserts the new keys are reachable under results/metrics/design and absent from validity_frame; the existing good-fixture-consuming golden suites stay green."
    - statement: "No finding code is minted and no dsx/ gate module is edited; only examples/good-ANALYSIS-SPEC.yaml and the new test are touched."
      status: enforced
      verification: "`git status --porcelain -- dsx/ references/finding-codes.md` is empty for this plan."
    - statement: "Do NOT run `python scripts/gen-finding-catalogue.py --check` or `tests.test_finding_catalogue_invariant` in this plan — the catalogue is regenerated once in 15-06 and is intentionally stale until then; the good-fixture gate passes do not read the catalogue."
      status: enforced
      verification: "this plan's verify runs the good-fixture silence test plus the good-fixture-consuming golden suites — never the catalogue check or the invariant test."
---

<objective>
Deliver REQ-P15-03: extend the canonical good fixture `examples/good-ANALYSIS-SPEC.yaml` with thin
cohort-grain and funnel-step BI fields plus a well-behaved cohort-comparison surface and a
pre-experiment CUPED block, and prove it still passes every gate at every severity threshold (D-08).
The extension must draw ZERO findings — including from the new DSX-MET-021 (cohort mix-shift) and
DSX-EXP-070 (CUPED) checks — so the new checks `report.ok`/return on the good-fixture values rather
than `.add`.

Purpose: this is the D-08 half of the phase — a check-shipping milestone must extend the canonical
good fixture rather than replace it, and the extended fixture must stay clean at plan/execute/verify/
ship. The new thin fields live top-level under results/metrics/design (never inside the strict-key
validity_frame.exclusions block), so frame_digest is unchanged. The cohort_comparisons declare equal
sampling rates (or reweighted true) so MET-021 stays silent; the CUPED covariate is pre_experiment so
EXP-070 report.ok's — this is also where the REQ-P15-02 pre-experiment PASS path is proven at every
threshold.

Output: the extended `examples/good-ANALYSIS-SPEC.yaml` + `tests/test_good_fixture_phase15.py` proving
silence at every threshold and the D-04 placement invariant.
</objective>

<artifacts_this_phase_produces>
This plan (15-05) runs in wave 3, `depends_on: [15-01, 15-02, 15-04]`, and is the sole writer of
examples/good-ANALYSIS-SPEC.yaml in Phase 15. It edits one file and creates one:
- `examples/good-ANALYSIS-SPEC.yaml` (edit — add cohort_grain, cohort_comparisons, funnel_steps, and a pre_experiment CUPED block)
- `tests/test_good_fixture_phase15.py` (new — silence at every threshold + the D-04 placement invariant)

It depends on 15-01 (else `variance_adjustment: cuped` draws a stray DSX-SPEC-044 and the fixture is
not clean), 15-02 (the MET-021 check must exist to prove it stays silent), and 15-04 (the EXP-070 check
must exist to prove it stays silent). Its `files_modified` are disjoint from the other wave-3 plan
(15-06 owns references/finding-codes.md, tests/test_finding_catalogue_invariant.py and
scripts/gen-finding-catalogue.py). It mints zero finding codes. See 15-01 for the full phase wave
structure and single-writer justification.

## Phase-level requirement coverage (union across all six Phase-15 plans)
- REQ-P15-01 — plan 15-01; REQ-P15-02 — plan 15-04; REQ-P15-03 — **this plan (15-05)**;
  REQ-P15-04 — plan 15-02; REQ-P15-05 — plan 15-03; REQ-P15-06 — plan 15-03; REQ-P15-07 — plan 15-06.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cuped-and-bi-declaration-checks-new-codes-d-05/15-CONTEXT.md
@examples/good-ANALYSIS-SPEC.yaml
@dsx/checks/metrics.py
@dsx/checks/design.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend examples/good-ANALYSIS-SPEC.yaml with thin cohort/funnel fields and a pre_experiment CUPED block</name>
  <files>examples/good-ANALYSIS-SPEC.yaml</files>
  <read_first>
    - examples/good-ANALYSIS-SPEC.yaml:31-56 (the metrics[] block — add a `cohort_grain:` label line to one metric, e.g. activation_rate; it is documentation only, read by no check)
    - examples/good-ANALYSIS-SPEC.yaml:129-146 (design: kind experiment, randomization_unit == analysis_unit == user, no existing variance_adjustment — add `variance_adjustment: cuped` and a `cuped:` sub-block with covariate/covariate_timing: pre_experiment/covariate_source)
    - examples/good-ANALYSIS-SPEC.yaml:221-258 (results: existing period_comparisons + tests — add sibling `cohort_comparisons:` and `funnel_steps:` here, top-level under results, NOT inside validity_frame)
    - dsx/checks/metrics.py::_check_cohort_denominator_shift (15-02 — the exact silence condition: equal sampling_rate across buckets, or reweighted: true, keeps DSX-MET-021 at report.ok)
    - dsx/checks/design.py::_check_cuped (15-04 — the silence condition: covariate_timing pre_experiment → report.ok)
    - examples/good-ANALYSIS-SPEC.yaml:296-357 (the validity_frame block and its exclusions strictness — do NOT put any new key here; keep additions under results/metrics/design so frame_digest is unchanged, D-04)
  </read_first>
  <action>
    Extend examples/good-ANALYSIS-SPEC.yaml additively (never remove or renumber an existing field):

    1. On the `activation_rate` metric, add a `cohort_grain: user_cohort` line (a documentation label
       read by no check).
    2. Under `design`, add `variance_adjustment: cuped` and a `cuped:` sub-block with `covariate`
       (a pre-experiment covariate name, e.g. `pre_period_activation_rate`), `covariate_timing:
       pre_experiment`, and `covariate_source` (e.g. `warehouse.fct_signups_prior_28d`). Because the
       randomization and analysis units already match (user == user), this does not perturb _check_units,
       and _check_cuped report.ok's on the pre_experiment timing.
    3. Under `results`, add a `cohort_comparisons:` list with at least one entry whose declared buckets
       share an equal `sampling_rate` (spread 0) — or set `reweighted: true` on the entry — so
       DSX-MET-021 resolves to report.ok. Give the entry a `metric` name matching a declared metric.
    4. Under `results`, add a `funnel_steps:` list — an ordered, naturally monotone drop-off (each step's
       count <= the previous), documentation only, that no check fires on.

    Keep every new key top-level under results/metrics/design — never inside `validity_frame` or its
    `exclusions` sub-block (D-04). Do not change any existing value. The file must still parse and still
    pass every gate at every threshold.
  </action>
  <acceptance_criteria>
    - examples/good-ANALYSIS-SPEC.yaml declares `metrics[].cohort_grain`, `design.variance_adjustment: cuped`, `design.cuped.covariate_timing: pre_experiment`, a well-behaved `results.cohort_comparisons[]` (equal sampling_rate or reweighted: true), and a monotone `results.funnel_steps[]`.
    - No new key is placed inside `validity_frame` (or its exclusions); all additions are under results/metrics/design.
    - The file still round-trips through the loader and every existing field is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>python -c "from dsx.loader import load; from dsx.spec import get, section, items; s=load('examples/good-ANALYSIS-SPEC.yaml'); assert get(s,'design.variance_adjustment')=='cuped', get(s,'design.variance_adjustment'); assert get(s,'design.cuped.covariate_timing')=='pre_experiment'; r=section(s,'results'); assert items(r,'cohort_comparisons'), 'cohort_comparisons missing'; assert items(r,'funnel_steps'), 'funnel_steps missing'; assert any(m.get('cohort_grain') for m in items(s,'metrics')), 'cohort_grain missing'; vf=s.get('validity_frame') or {}; import json; blob=json.dumps(vf); assert 'cohort_comparisons' not in blob and 'funnel_steps' not in blob and 'cuped' not in blob and 'cohort_grain' not in blob, 'new keys must not be inside validity_frame'"</automated>
  </verify>
  <done>examples/good-ANALYSIS-SPEC.yaml is extended additively with cohort_grain, a pre_experiment CUPED block, a well-behaved cohort_comparisons entry and a monotone funnel_steps list — all top-level under results/metrics/design, none inside validity_frame — and still round-trips.</done>
</task>

<task type="auto">
  <name>Task 2: Prove D-08 silence at every threshold in tests/test_good_fixture_phase15.py</name>
  <files>tests/test_good_fixture_phase15.py</files>
  <read_first>
    - dsx/cli.py:115-140 (GATE_PROFILES and GATE_THRESHOLDS — the four gate points and their thresholds to iterate)
    - dsx/cli.py:165-188 (run_checks — pass `resolve_root=str(ROOT/'examples')` so the good fixture's sibling evidence resolves and only the new fields could add a finding)
    - dsx/findings.py:137-182 (Report.at_or_above / blocks / exit_code and Severity — assert exit_code==0 at each threshold and that DSX-MET-021/DSX-EXP-070 are absent from the finding codes)
    - tests/test_explain_self_reported.py:40-55 (the good-fixture-in-tmp / phase_dir pattern, for reference on how the good fixture is exercised through the CLI engine)
  </read_first>
  <action>
    Create tests/test_good_fixture_phase15.py, a stdlib-only unittest module (import unittest, pathlib;
    `from dsx.cli import run_checks, GATE_PROFILES, GATE_THRESHOLDS`; `from dsx.findings import Severity`;
    `from dsx.loader import load`). ROOT via `Path(__file__).resolve().parent.parent`; load
    `ROOT/'examples'/'good-ANALYSIS-SPEC.yaml'` once; `resolve = str(ROOT/'examples')`. Define a local
    `codes(report)` helper.

    Write these test methods:

    1. test_good_fixture_passes_every_gate_at_every_threshold: for each point in
       ("plan","execute","verify","ship"), run `run_checks(good, GATE_PROFILES[point], None,
       gate_point=point, resolve_root=resolve)` and assert `report.exit_code(Severity.parse(
       GATE_THRESHOLDS[point])) == 0` — the extended fixture blocks at no threshold (D-08). Include the
       point name in the failure message.
    2. test_new_checks_stay_silent_on_the_good_fixture: at the strictest point ("ship"), assert neither
       `DSX-MET-021` nor `DSX-EXP-070` is in `codes(report)` — the new checks report.ok on the
       good-fixture values (trap #2).
    3. test_thin_fields_are_present_and_outside_validity_frame: assert `design.cuped.covariate_timing`
       is `pre_experiment`, `results.cohort_comparisons` and `results.funnel_steps` are non-empty, some
       metric carries `cohort_grain`, and none of these keys appears inside `validity_frame` (the D-04
       placement invariant that keeps frame_digest unchanged).

    Keep the module stdlib-only and CRLF-agnostic. Do not edit any dsx/ file. This plan mints nothing —
    add no `# D-05:` marker.
  </action>
  <acceptance_criteria>
    - tests/test_good_fixture_phase15.py exists, is stdlib-only, and defines the three methods above.
    - The threshold test asserts exit_code == 0 at all four gate points; the silence test asserts DSX-MET-021 and DSX-EXP-070 are absent at ship; the placement test asserts the new keys are outside validity_frame.
    - `python -m unittest tests.test_good_fixture_phase15` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_good_fixture_phase15 >/dev/null 2>&1 && python -m unittest tests.test_causal_verb_golden >/dev/null 2>&1 && python -c "import ast; t=ast.parse(open('tests/test_good_fixture_phase15.py',encoding='utf-8').read()); ms={f.name for f in ast.walk(t) if isinstance(f,ast.FunctionDef)}; need={'test_good_fixture_passes_every_gate_at_every_threshold','test_new_checks_stay_silent_on_the_good_fixture','test_thin_fields_are_present_and_outside_validity_frame'}; assert need<=ms, need-ms"</automated>
  </verify>
  <done>tests/test_good_fixture_phase15.py proves the extended good fixture passes every gate at every threshold, DSX-MET-021 and DSX-EXP-070 stay silent on it, and the new thin fields sit outside validity_frame — and the good-fixture-consuming causal-verb golden suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan extends an authored fixture and adds one read-only test. It introduces no runtime gate
surface, no network/auth/input, and executes nothing beyond running the existing gate engine over the
fixture in-process. No npm/pip/cargo install occurs, so T-15-SC is N/A this plan.

| Boundary | Description |
|----------|-------------|
| good fixture → gate engine | The extended fixture is run through run_checks at each gate point; the test asserts it blocks at none and that the two new checks stay silent (D-08). |
| new thin keys → frame_digest | The keys live under results/metrics/design, never validity_frame, so frame_digest is unchanged (D-04); the placement test pins this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-50 | Tampering | A new thin field accidentally landing inside validity_frame.exclusions, changing frame_digest and silently moving a downstream check | high | mitigate | D-04: the placement test asserts the new keys are absent from validity_frame; all additions are under results/metrics/design. |
| T-15-51 | Tampering | The new checks firing on the good fixture (a cohort_comparisons with unequal rates and no reweighting, or a post_treatment CUPED), breaking the D-08 clean-at-every-threshold contract | high | mitigate | trap #2: cohort_comparisons declare equal sampling_rate or reweighted: true, and the CUPED timing is pre_experiment; the silence test asserts DSX-MET-021/DSX-EXP-070 absent and exit_code 0 at all four points. |
| T-15-52 | Tampering | Replacing rather than extending the good fixture, dropping an existing field and silently loosening a passing gate | high | mitigate | D-08: additions only; the round-trip verify asserts the new keys are present while the good-fixture-consuming golden suites (causal-verb golden) stay green, proving the existing finding set is unchanged. |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs occur; the change is a fixture extension and a stdlib test. |
</threat_model>

<verification>
This plan delivers REQ-P15-03: the ANALYSIS-SPEC accepts thin cohort-grain and funnel-step fields, and
the extended good fixture still passes every gate at every threshold (D-08).

For this plan specifically:
- `python -m unittest tests.test_good_fixture_phase15` passes: exit_code 0 at plan/execute/verify/ship, DSX-MET-021/DSX-EXP-070 silent, new keys outside validity_frame.
- `python -m unittest tests.test_causal_verb_golden` stays green (the good fixture's finding set is unchanged — extended, not replaced).
- `git status --porcelain -- dsx/ references/finding-codes.md` is empty (no gate edit, no mint).
- Do NOT run `gen-finding-catalogue.py --check` or `tests.test_finding_catalogue_invariant` here — the catalogue is regenerated once in 15-06 and is intentionally stale until then.
</verification>

<success_criteria>
- The extended good fixture accepts cohort_grain, cohort_comparisons, funnel_steps and a pre_experiment CUPED block, and passes every gate at every threshold (REQ-P15-03 / D-08).
- The new DSX-MET-021 and DSX-EXP-070 checks stay silent on the good-fixture values (trap #2).
- Every new key is top-level under results/metrics/design, never inside validity_frame, so frame_digest is unchanged (D-04).
- Zero finding codes minted; no gate module edited; the good-fixture-consuming golden suites stay green (D-08).
</success_criteria>

<output>
Create `.planning/phases/15-cuped-and-bi-declaration-checks-new-codes-d-05/15-05-SUMMARY.md` when done.
</output>
