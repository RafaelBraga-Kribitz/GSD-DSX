---
phase: 15-cuped-and-bi-declaration-checks-new-codes-d-05
plan: 04
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - dsx/mathx.py
  - dsx/checks/design.py
  - tests/test_cuped.py
autonomous: true
requirements: [REQ-P15-02]
tags: [dsx, mint, DSX-EXP-070, cuped, design-check, critical, gate-path-pure, mathx-reference-arithmetic, deng-wsdm-2013, D-02, D-06, trap-5, trap-11]

must_haves:
  truths:
    - "`dsx/checks/design.py` gains `_check_cuped`, dispatched from the ALWAYS-RUN tail of `design.check()` (not the looks_like_experiment battery), keyed on `normalize(design.variance_adjustment) == \"cuped\"`, minting DSX-EXP-070 (CRITICAL): `pre_experiment` covariate_timing → report.ok; `post_treatment` OR any unrecognised/absent value → fire — so a typo or omission is never cheaper than an honest post_treatment (REQ-P15-02 / D-02)."
    - "A post-treatment-covariate CUPED spec exits 1 at `dsx gate plan` (DSX-EXP-070 is CRITICAL, and plan/execute block at CRITICAL) while the same spec with `pre_experiment` passes — proven by running the real `run_checks` engine over `GATE_PROFILES['plan']` at the `GATE_THRESHOLDS['plan']` threshold and asserting `report.exit_code(threshold)` flips 0→1 (REQ-P15-02)."
    - "The CUPED θ/ρ/variance reference arithmetic lives ONLY in `dsx/mathx.py` (`cuped_theta`, `cuped_variance_reduction`), `# D-05`-marked and tested to the WSDM identity ρ=0.5 → variance reduction ρ²=0.25 (variance ratio 0.75), and is NEVER imported by the gate-path check — the `dsx.mathx.diluted_effect`↔INT-030 precedent (D-01/D-02/D-06 / trap #5)."
    - "Severity is CRITICAL with NO edit to GATE_THRESHOLDS/GATE_PROFILES — DSX-EXP-070 blocks at plan/execute by severity alone, mirroring EXP-060/INT-030 (trap #8)."
    - "DSX-EXP-070's finding title is the FIXED plain string literal `CUPED declared with a covariate that is not pre-experiment` (AST-extractable), `where=\"spec.design.cuped.covariate_timing\"`; the detail names the actual declared value honestly and never mislabels an absent/unrecognised timing as an affirmative post_treatment accusation (trap #4/#11)."
  artifacts:
    - dsx/mathx.py
    - dsx/checks/design.py
    - tests/test_cuped.py
  key_links:
    - "`_check_cuped` imports `CUPED_COVARIATE_TIMINGS` from `..spec` (added in 15-01) exactly as design.py already imports `IDENTIFICATION_STRATEGIES` — so the recognised timing set and the dumped vocabulary cannot drift; this is why the plan is wave 2, `depends_on: [15-01]` (trap #12)."
    - "`_check_cuped` does NOT import or call `cuped_theta`/`cuped_variance_reduction` — the reference arithmetic stays in dsx/mathx.py, off the gate path (D-01/D-02). The verify asserts design.py never names those functions."
    - "The enclosing function `_check_cuped` docstring carries a `Citation:` line (Deng, Xu, Kohavi & Walker 2013, WSDM, DOI 10.1145/2433396.2433413, naming Ŷ_cv = Ȳ − θ(X̄ − E[X]), θ = Cov(Y,X)/Var(X), Var(Ŷ_cv) = Var(Ȳ)(1−ρ²)) AND a `Structural criterion:` line; tests/test_cuped.py carries a `# D-05: DSX-EXP-070` marker — the three artifacts the 15-06 D-05 build gate reads."
  prohibitions:
    - statement: "`dsx/checks/design.py` never imports or calls the CUPED math (`cuped_theta`/`cuped_variance_reduction`); the check computes nothing on the gate path (no θ/ρ/variance) — it reads the declared covariate_timing only (D-01/D-02 / trap #5)."
      status: enforced
      verification: "a text/AST scan of dsx/checks/design.py finds neither `cuped_theta` nor `cuped_variance_reduction`; the `from ..mathx import (...)` block is unchanged apart from carrying only its existing power/SRM functions."
    - statement: "No GATE_THRESHOLDS/GATE_PROFILES edit — DSX-EXP-070 is CRITICAL and blocks at plan/execute by severity alone (trap #8). The Deng 2013 citation names the WSDM formulation, NOT the Unified playbook snippet (REQ-P15-02 exclusion)."
      status: enforced
      verification: "`git status --porcelain -- dsx/cli.py` is empty for this plan; the DSX-EXP-070 report.add severity arg is the constant 'CRITICAL'; the docstring cites Deng et al. 2013 WSDM."
    - statement: "Do NOT run `python scripts/gen-finding-catalogue.py --check` or `tests.test_finding_catalogue_invariant` in this plan. Adding the DSX-EXP-070 report.add site makes the catalogue intentionally stale until it is regenerated ONCE in 15-06 (REQ-P15-07); asserting catalogue currency here would fail spuriously."
      status: enforced
      verification: "this plan's verify runs only tests.test_cuped + AST/text checks scoped to design.py and mathx.py — never the catalogue check or the invariant test."
---

<objective>
Deliver REQ-P15-02: the CUPED post-treatment-covariate gate check plus its off-gate-path reference
arithmetic. Add `_check_cuped` to the always-run tail of `dsx/checks/design.py::check()`, keyed on
`normalize(design.variance_adjustment) == "cuped"`, minting DSX-EXP-070 (CRITICAL) when the declared
`design.cuped.covariate_timing` is anything other than `pre_experiment` (post_treatment OR any
unrecognised/absent value), and calling report.ok on `pre_experiment`. Add the CUPED identity
arithmetic (`cuped_theta`, `cuped_variance_reduction`) to `dsx/mathx.py`, tested to the WSDM worked
value ρ²=25%, and NEVER imported by the check.

Purpose: EXP is design-correctness. The vocabulary question (is `cuped` a known adjustment) is owned by
DSX-SPEC-044 (now accepting cuped, 15-01); the SEMANTIC pre-experiment question — a CUPED covariate
that is not measured strictly before treatment reintroduces exactly the bias CUPED exists to remove —
is an EXP concern, and `070` is the next free EXP band. It is CRITICAL because REQ-P15-02 requires it to
exit 1 at `dsx gate plan`, where plan/execute block only at CRITICAL. It computes nothing on the gate
path: the θ/ρ/variance arithmetic is reference-only in mathx.py, the diluted_effect↔INT-030 precedent.
A typo or an absent timing must not be cheaper than an honest post_treatment (INT-010/INT-030
doctrine), so the fire covers every non-pre_experiment value, while the two-member vocabulary keeps a
valid covariate from ever being false-flagged.

Output: `_check_cuped` (DSX-EXP-070) in design.py, `cuped_theta`/`cuped_variance_reduction` in mathx.py,
and tests/test_cuped.py proving the worked value, the firing/ok behaviour, the gate-plan exit 0→1, and
gate-path purity — carrying the `# D-05: DSX-EXP-070` marker.
</objective>

<artifacts_this_phase_produces>
This plan (15-04) is the sole wave-2 plan (`depends_on: [15-01]`), so single-writer-per-wave holds
trivially. It is the sole writer of dsx/mathx.py and dsx/checks/design.py in Phase 15. It edits two
files and creates one:
- `dsx/mathx.py` (edit — add `cuped_theta`, `cuped_variance_reduction`, reference-only, `# D-05`-tested)
- `dsx/checks/design.py` (edit — add `_check_cuped`, DSX-EXP-070; import CUPED_COVARIATE_TIMINGS from ..spec)
- `tests/test_cuped.py` (new — worked value + firing/ok + gate-plan exit 0→1 + purity; carries `# D-05: DSX-EXP-070`)

It depends on 15-01 because `_check_cuped` imports `CUPED_COVARIATE_TIMINGS` and keys on
`cuped ∈ VARIANCE_ADJUSTMENTS` (trap #12). It mints exactly ONE finding code (DSX-EXP-070); the
catalogue is left intentionally stale until 15-06 regenerates it. See 15-01 for the full phase wave
structure and single-writer justification.

## Phase-level requirement coverage (union across all six Phase-15 plans)
- REQ-P15-01 — plan 15-01; REQ-P15-02 — **this plan (15-04)**; REQ-P15-03 — plan 15-05;
  REQ-P15-04 — plan 15-02; REQ-P15-05 — plan 15-03; REQ-P15-06 — plan 15-03; REQ-P15-07 — plan 15-06.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cuped-and-bi-declaration-checks-new-codes-d-05/15-CONTEXT.md
@dsx/checks/design.py
@dsx/mathx.py
@examples/good-ANALYSIS-SPEC.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cuped_theta and cuped_variance_reduction to dsx/mathx.py (reference-only, D-06)</name>
  <files>dsx/mathx.py</files>
  <read_first>
    - dsx/mathx.py:492-527 (`diluted_effect` — the exact docstring discipline to mirror: a `Citation:` block naming the paper/DOI/formula, a `Reference value:` line pinning a published worked value, and honest UNVERIFIED-locator flagging where a locator was not confirmed; this is the off-gate-path reference-arithmetic precedent)
    - dsx/mathx.py:469-489 (`design_effect` — a second example of the Citation + Reference value docstring shape for a pure scalar identity)
    - dsx/mathx.py:1-16 (module docstring: stdlib-only, math imported at top; add no scipy/numpy)
  </read_first>
  <action>
    Add two pure functions to dsx/mathx.py, in the effect-sizes / identities region beside
    `diluted_effect`:

    `cuped_theta(cov_xy: float, var_x: float) -> float` — returns the CUPED coefficient
    θ = Cov(Y, X) / Var(X). Raise ValueError when `var_x` is 0 (the coefficient is undefined for a
    degenerate covariate). Docstring: a `Citation:` line naming Deng, Xu, Kohavi & Walker (2013),
    "Improving the Sensitivity of Online Controlled Experiments by Utilizing Pre-Experiment Data", WSDM
    '13, pp.123-132, DOI 10.1145/2433396.2433413, and stating the identities Ŷ_cv = Ȳ − θ(X̄ − E[X]) and
    θ = Cov(Y,X)/Var(X); a `Reference value:` line pinning a small verifiable pair (e.g. Cov=0.5,
    Var=2.0 → θ=0.25). Follow the honest-locator discipline from diluted_effect: assert only what is
    verified.

    `cuped_variance_reduction(rho: float) -> float` — returns the fractional variance reduction ρ² for a
    pre-experiment covariate correlated ρ with the outcome; Var(Ŷ_cv) = Var(Ȳ)(1 − ρ²). Raise ValueError
    when `rho` is outside [-1, 1]. Docstring: the same Deng et al. 2013 WSDM `Citation:` line, plus a
    `Reference value:` line pinning the paper's own DERIVED identity ρ = 0.5 → variance reduction
    ρ² = 0.25 (a 25% reduction; variance ratio 1 − ρ² = 0.75). State explicitly that the ~50% Bing
    headline is EMPIRICAL (ρ ≈ 0.707), not a derived identity, and is kept as docstring context only —
    the pinned worked value is ρ²=0.25 at ρ=0.5 (D-06).

    Both functions are pure, stdlib-only (math only), and are reference arithmetic — nothing in dsx/checks/
    or dsx/frame/ imports them. Do not wire them into any check.
  </action>
  <acceptance_criteria>
    - `cuped_theta(cov, var)` returns cov/var and raises ValueError on var=0; `cuped_variance_reduction(rho)` returns rho**2 and raises ValueError outside [-1,1].
    - `cuped_variance_reduction(0.5) == 0.25` and the variance ratio `1 - 0.25 == 0.75` (the WSDM derived identity, D-06).
    - Each function's docstring carries a `Citation:` line (Deng et al. 2013 WSDM, DOI 10.1145/2433396.2433413, naming the exact formulation) and a `Reference value:` line.
    - dsx/mathx.py imports no scipy/numpy (stdlib math only).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import math, ast; from dsx.mathx import cuped_theta, cuped_variance_reduction; assert math.isclose(cuped_variance_reduction(0.5), 0.25); assert math.isclose(1-cuped_variance_reduction(0.5), 0.75); assert math.isclose(cuped_theta(0.5,2.0),0.25); t=ast.parse(open('dsx/mathx.py',encoding='utf-8').read()); fns={f.name:f for f in ast.walk(t) if isinstance(f,ast.FunctionDef)}; docs={n:(ast.get_docstring(fns[n]) or '') for n in ('cuped_theta','cuped_variance_reduction')}; assert all('Citation:' in d and 'Reference value:' in d and 'Deng' in d for d in docs.values()), docs"</automated>
  </verify>
  <done>dsx/mathx.py defines cuped_theta and cuped_variance_reduction as pure reference arithmetic with Deng-2013-WSDM Citation + Reference value docstrings, pinning ρ=0.5 → ρ²=0.25 (variance ratio 0.75), stdlib-only and imported by no check.</done>
</task>

<task type="auto">
  <name>Task 2: Add _check_cuped (DSX-EXP-070, CRITICAL) to dsx/checks/design.py</name>
  <files>dsx/checks/design.py</files>
  <read_first>
    - dsx/checks/design.py:33-85 (check(): the always-run tail lines 82-84 call `_check_multiplicity`, `_check_peeking`, `_check_identification` regardless of `looks_like_experiment` — add `_check_cuped(design, spec, report)` here so CUPED fires even when the experiment battery is skipped, D-02)
    - dsx/checks/design.py:269-309 (`_check_units` — note it reads `design.variance_adjustment` only when randomization != analysis and returns ok early when they match; adding a cuped block to a matched-unit spec does not perturb it)
    - dsx/checks/design.py:499-529 (`_check_peeking` DSX-EXP-060 CRITICAL — the CRITICAL report.add shape and the `where=` / keyword-context style to mirror)
    - dsx/checks/design.py:12-27 (imports: the `from ..mathx import (...)` block MUST NOT gain cuped functions; the `from ..spec import (...)` block gains `CUPED_COVARIATE_TIMINGS` beside `IDENTIFICATION_STRATEGIES`)
    - dsx/spec.py CUPED_COVARIATE_TIMINGS (from 15-01 — the two-member set used only to phrase an honest detail: post_treatment vs unrecognised vs absent, trap #11)
  </read_first>
  <action>
    Add `CUPED_COVARIATE_TIMINGS` to the `from ..spec import (...)` block. Do NOT add any name to the
    `from ..mathx import (...)` block. Define `_check_cuped(design: dict, spec: dict, report: Report) ->
    None` and call it from the always-run tail of `check()` (immediately after
    `_check_identification(design, spec, report)` on dsx/checks/design.py:84).

    Give `_check_cuped` a docstring carrying BOTH a `Citation:` line and a `Structural criterion:` line
    (the D-05 build gate reads the enclosing function's docstring):
    - Citation: Deng, Xu, Kohavi & Walker (2013), "Improving the Sensitivity of Online Controlled
      Experiments by Utilizing Pre-Experiment Data", WSDM '13, pp.123-132, DOI 10.1145/2433396.2433413 —
      the pre-experiment independence requirement (a CUPED covariate must be measured strictly before
      treatment). NOT the Unified Framework playbook snippet (REQ-P15-02's explicit exclusion).
    - Structural criterion: runs only when `normalize(design.variance_adjustment) == "cuped"`; fires
      CRITICAL when `design.cuped.covariate_timing` is not `pre_experiment` (post_treatment or any
      unrecognised/absent value); `pre_experiment` → report.ok. Computes nothing (no θ/ρ/variance) — the
      reference arithmetic is in dsx/mathx.py and is not imported here.

    Body: if `not design`, return. Read `adjustment = normalize(design.get("variance_adjustment", ""))`;
    if `adjustment != "cuped"`, return (the check does not run for any other adjustment — trap #11). Read
    the raw declared timing via `get(design, "cuped.covariate_timing")` and `timing =
    normalize(raw)` when not blank else "". If `timing == "pre_experiment"`, call `report.ok(...)`
    noting the CUPED covariate is declared pre-experiment, and return. Otherwise fire DSX-EXP-070 at
    severity "CRITICAL" with the 3rd positional message being the FIXED plain string literal (no
    f-string): CUPED declared with a covariate that is not pre-experiment. Build an HONEST `detail=`
    f-string that distinguishes the cases (trap #4/#11): when `timing in CUPED_COVARIATE_TIMINGS` (i.e.
    the recognised `post_treatment` value) say the covariate is declared post-treatment; when the raw
    value is blank/absent say the covariate_timing is absent; otherwise say the declared value is not a
    recognised covariate timing — naming the actual value. Never phrase an absent/unrecognised timing as
    an affirmative post_treatment accusation. Set `where="spec.design.cuped.covariate_timing"` and pass
    the declared value as a keyword such as `declared_timing=str(raw)`.

    Do NOT edit GATE_THRESHOLDS/GATE_PROFILES; CRITICAL alone makes plan/execute block.
  </action>
  <acceptance_criteria>
    - `_check_cuped(design, spec, report)` exists in dsx/checks/design.py and is called from the always-run tail of `check()` (after `_check_identification`), so it fires without the looks_like_experiment marker.
    - It runs only when `normalize(design.variance_adjustment) == "cuped"`; `pre_experiment` → report.ok; post_treatment / unrecognised / absent → DSX-EXP-070 CRITICAL with the fixed plain-string message and `where="spec.design.cuped.covariate_timing"`.
    - The enclosing function docstring carries a `Citation:` line (Deng et al. 2013 WSDM) and a `Structural criterion:` line; `CUPED_COVARIATE_TIMINGS` is imported from ..spec.
    - design.py imports/calls neither `cuped_theta` nor `cuped_variance_reduction`; no GATE_THRESHOLDS/GATE_PROFILES edit.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast; src=open('dsx/checks/design.py',encoding='utf-8').read(); assert 'cuped_theta' not in src and 'cuped_variance_reduction' not in src, 'gate check must not name the CUPED math'; t=ast.parse(src); fns={f.name:f for f in ast.walk(t) if isinstance(f,ast.FunctionDef)}; assert '_check_cuped' in fns; f=fns['_check_cuped']; d=ast.get_docstring(f) or ''; assert 'Citation:' in d and 'Structural criterion:' in d and 'Deng' in d; adds=[n for n in ast.walk(f) if isinstance(n,ast.Call) and isinstance(n.func,ast.Attribute) and n.func.attr=='add']; assert len(adds)==1; a=adds[0]; assert isinstance(a.args[0],ast.Constant) and a.args[0].value=='DSX-EXP-070'; assert isinstance(a.args[1],ast.Constant) and a.args[1].value=='CRITICAL'; assert isinstance(a.args[2],ast.Constant) and a.args[2].value=='CUPED declared with a covariate that is not pre-experiment'; body=ast.get_source_segment(src, fns['check']); assert '_check_cuped(' in body, 'must be dispatched from check()'; assert 'CUPED_COVARIATE_TIMINGS' in src" && [ -z "$(git status --porcelain -- dsx/cli.py)" ]</automated>
  </verify>
  <done>dsx/checks/design.py defines _check_cuped in the always-run tail, minting DSX-EXP-070 CRITICAL when a cuped variance_adjustment declares a non-pre_experiment covariate_timing (post_treatment/unrecognised/absent) with an honest detail and pre_experiment passing, importing the timing vocabulary but never the CUPED math, with a Deng-2013 Citation + Structural criterion docstring and no gate-threshold edit.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the worked value, firing/ok, gate-plan exit 0→1, and purity in tests/test_cuped.py</name>
  <files>tests/test_cuped.py</files>
  <read_first>
    - dsx/checks/design.py check(spec, *, strict=False) (the check entry the firing tests call: `design.check(spec)`)
    - dsx/cli.py:165-188 (run_checks signature and resolve_root semantics — pass `resolve_root=str(ROOT/'examples')` so the good fixture's sibling evidence resolves; GATE_PROFILES/GATE_THRESHOLDS live in dsx.cli)
    - dsx/findings.py:137-182 (Report.at_or_above / blocks / exit_code(threshold) and Severity — `report.exit_code(Severity.parse(GATE_THRESHOLDS['plan']))` reproduces the exact `dsx gate plan` block decision)
    - examples/good-ANALYSIS-SPEC.yaml:129-147 (design: kind experiment, randomization_unit == analysis_unit == user, so injecting variance_adjustment: cuped does not perturb _check_units; the file passes every gate, so the ONLY delta from a CUPED injection is DSX-EXP-070)
    - tests/test_reproduce_report.py (the stdlib-only behavioural-test house style: local `codes(report)` helper, ROOT resolution, inline spec dicts)
  </read_first>
  <action>
    Create tests/test_cuped.py, a stdlib-only unittest module (import unittest, copy, math, pathlib;
    `from dsx.checks import design`; `from dsx.cli import run_checks, GATE_PROFILES, GATE_THRESHOLDS`;
    `from dsx.findings import Severity`; `from dsx.loader import load`; `from dsx.mathx import cuped_theta,
    cuped_variance_reduction`). Add a top-of-file comment line `# D-05: DSX-EXP-070` (the marker the
    15-06 D-05 build gate scans for). Define ROOT via `Path(__file__).resolve().parent.parent` and a
    local `codes(report)` helper `{f.code for f in report.findings}`.

    Worked-value test (D-06): `test_cuped_variance_reduction_matches_wsdm_identity` — assert
    `math.isclose(cuped_variance_reduction(0.5), 0.25)` and `math.isclose(1 - cuped_variance_reduction(0.5),
    0.75)` and a `cuped_theta` pair (e.g. 0.5/2.0 → 0.25). This is the published-worked-value test
    REQ-P15-02 requires.

    Check-level firing tests (build a minimal spec dict with a `design` block; assert on
    `design.check(spec)`):
    - test_exp070_fires_on_post_treatment: design.variance_adjustment: cuped +
      design.cuped.covariate_timing: post_treatment → DSX-EXP-070 in codes, and its finding severity is
      CRITICAL.
    - test_exp070_fires_on_absent_timing: variance_adjustment: cuped, a cuped block with NO
      covariate_timing → DSX-EXP-070 fires (omission is not cheaper than post_treatment).
    - test_exp070_fires_on_unrecognised_timing: covariate_timing: "during_experiment" (not in the
      vocabulary) → DSX-EXP-070 fires.
    - test_exp070_silent_on_pre_experiment: covariate_timing: pre_experiment → DSX-EXP-070 NOT in codes
      (a report.ok is recorded instead).
    - test_exp070_does_not_run_without_cuped: variance_adjustment: cluster_robust (not cuped) → DSX-EXP-070
      NOT in codes (the check does not run).

    Gate-plan exit test (REQ-P15-02 "exits 1 at dsx gate plan", via the real engine): load
    examples/good-ANALYSIS-SPEC.yaml with `load`. Set `threshold = Severity.parse(GATE_THRESHOLDS["plan"])`
    and `resolve = str(ROOT / "examples")`. First assert the UNMUTATED good fixture is clean at plan:
    `run_checks(good, GATE_PROFILES["plan"], None, gate_point="plan", resolve_root=resolve).exit_code(threshold)
    == 0` and DSX-EXP-070 not present (baseline). Then `copy.deepcopy` the spec, inject
    `spec["design"]["variance_adjustment"] = "cuped"` and `spec["design"]["cuped"] =
    {"covariate": "pre_period_activation", "covariate_timing": "post_treatment", "covariate_source":
    "warehouse.fct_signups_prior"}`, and assert the plan run now has `exit_code(threshold) == 1` with
    DSX-EXP-070 present — the 0→1 flip. Finally inject `covariate_timing: pre_experiment` on another deep
    copy and assert `exit_code(threshold) == 0` with DSX-EXP-070 absent. Passing resolve_root at the
    examples dir keeps every other check clean, so the exit flip is attributable solely to DSX-EXP-070.

    Purity test: `test_gate_check_does_not_import_cuped_math` — read dsx/checks/design.py source and
    assert it does not contain `cuped_theta` or `cuped_variance_reduction` (the reference arithmetic
    stays off the gate path, D-01/D-02).

    Keep the module stdlib-only and CRLF-agnostic. Do not edit any file under dsx/ or examples/.
  </action>
  <acceptance_criteria>
    - tests/test_cuped.py exists, is stdlib-only, carries a `# D-05: DSX-EXP-070` marker, and includes the worked-value, five firing/ok, gate-plan exit, and purity tests.
    - The worked-value test pins ρ=0.5 → variance reduction 0.25 (ratio 0.75); the gate-plan test proves exit_code flips 0→1 on a post_treatment injection over the good fixture and stays 0 on pre_experiment.
    - The purity test asserts design.py names neither CUPED math function.
    - `python -m unittest tests.test_cuped` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_cuped >/dev/null 2>&1 && python -c "import ast; src=open('tests/test_cuped.py',encoding='utf-8').read(); assert '# D-05: DSX-EXP-070' in src, 'missing D-05 marker'; t=ast.parse(src); ms={f.name for f in ast.walk(t) if isinstance(f,ast.FunctionDef)}; need={'test_cuped_variance_reduction_matches_wsdm_identity','test_exp070_fires_on_post_treatment','test_exp070_fires_on_absent_timing','test_exp070_silent_on_pre_experiment','test_gate_check_does_not_import_cuped_math'}; assert need<=ms, need-ms"</automated>
  </verify>
  <done>tests/test_cuped.py is a stdlib-only unittest pinning the WSDM ρ²=0.25 worked value, DSX-EXP-070 firing on post_treatment/absent/unrecognised and silence on pre_experiment / non-cuped, the `dsx gate plan` exit_code 0→1 flip over the good fixture, and design.py's non-import of the CUPED math — carrying the `# D-05: DSX-EXP-070` marker and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan mints a CRITICAL declaration-only check on the deterministic gate path and adds pure
reference arithmetic off it. The check reads the already-loaded spec dict via existing spec helpers —
it opens no data file, imports no tabular library, computes no θ/ρ/variance, and executes nothing. The
mathx functions are pure scalars called only from tests. No network/auth/input surface beyond the
trusted spec is introduced. No npm/pip/cargo install occurs, so T-15-SC is N/A this plan.

| Boundary | Description |
|----------|-------------|
| spec.design.cuped.covariate_timing → check | The check reads one declared string and branches; it emits at most one finding and computes nothing (D-01/D-02). |
| dsx/mathx.py CUPED arithmetic → tests only | cuped_theta/cuped_variance_reduction are pure identities exercised by tests; no check imports them — the diluted_effect↔INT-030 boundary. |
| report.add → catalogue | The single DSX-EXP-070 report.add site is what 15-06's regen reads; a silent second mint is caught by the AST verify asserting exactly one report.add in _check_cuped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-40 | Tampering | The CUPED math creeping onto the gate path (design.py importing cuped_theta to "verify" the reduction), pulling computation into a declaration gate | critical | mitigate | D-01/D-02 / trap #5: the check reads covariate_timing only; the verify asserts design.py names neither CUPED math function; the test purity check re-proves it. |
| T-15-41 | Tampering | A typo or absent covariate_timing being cheaper than an honest post_treatment, letting a mislabelled CUPED slip the gate | high | mitigate | D-02 / trap #11: the fire covers every non-pre_experiment value (post_treatment, unrecognised, absent); tests pin all three firing paths and the honest detail phrasing. |
| T-15-42 | Spoofing | A false-positive on a valid covariate via a fuzzy third timing state | medium | mitigate | D-02: CUPED_COVARIATE_TIMINGS is exactly two-valued (15-01); pre_experiment is the only ok path; the check never invents a third state. |
| T-15-43 | Tampering | Downgrading below CRITICAL (or a GATE_THRESHOLDS edit) so a post-treatment CUPED no longer exits 1 at plan | high | mitigate | REQ-P15-02 / trap #8: the report.add severity arg is the constant 'CRITICAL'; the gate-plan test asserts exit_code 0→1 at the plan threshold; `git status` on dsx/cli.py is empty. |
| T-15-44 | Tampering | Citing the Unified playbook snippet instead of the primary WSDM source | medium | mitigate | REQ-P15-02: both the check docstring and the mathx docstrings cite Deng et al. 2013 WSDM (DOI 10.1145/2433396.2433413); the verify asserts 'Deng' in each docstring. |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs occur; the change is a stdlib check, two stdlib identities, and a stdlib test. |
</threat_model>

<verification>
This plan delivers REQ-P15-02: a post-treatment-covariate CUPED spec exits 1 at `dsx gate plan`
(DSX-EXP-070 CRITICAL); pre-experiment passes; the docstring cites Deng et al. 2013 WSDM naming the
formulation; and a test pins the ρ²=25% worked value (not the playbook).

For this plan specifically:
- `python -m unittest tests.test_cuped` passes: the WSDM worked value, all five firing/ok cases, the gate-plan exit_code 0→1 flip over the good fixture, and design.py's non-import of the CUPED math.
- The AST verify confirms one fixed-literal DSX-EXP-070 report.add at CRITICAL with `where="spec.design.cuped.covariate_timing"`, dispatched from the always-run tail, with a Deng-2013 Citation + Structural criterion docstring.
- `git status --porcelain -- dsx/cli.py` is empty (no GATE_THRESHOLDS/GATE_PROFILES edit).
- Do NOT run `gen-finding-catalogue.py --check` or `tests.test_finding_catalogue_invariant` here — the catalogue is intentionally stale until 15-06 regenerates it once.
</verification>

<success_criteria>
- DSX-EXP-070 (CRITICAL) fires when a cuped variance_adjustment declares a non-pre_experiment covariate_timing and passes on pre_experiment; a post-treatment spec exits 1 at `dsx gate plan` (REQ-P15-02 / D-02).
- The θ/ρ/variance reference arithmetic lives only in dsx/mathx.py, `# D-05`-tested to ρ²=0.25 at ρ=0.5, and is imported by no check (D-01/D-06 / trap #5).
- The finding title is the fixed AST-extractable literal; the detail is honest about absent/unrecognised timing (trap #4/#11); CRITICAL blocks by severity alone with no gate-threshold edit (trap #8).
- The docstring cites Deng et al. 2013 WSDM, not the Unified playbook snippet (REQ-P15-02 exclusion).
</success_criteria>

<output>
Create `.planning/phases/15-cuped-and-bi-declaration-checks-new-codes-d-05/15-04-SUMMARY.md` when done.
</output>
