---
phase: 11.3-reporting-completeness
plan: 06
subsystem: corpus-green-closing-gate
tags: [req-p11.3-07, catalogue-256, corpus-green, d-15-fixture-audit, verification-only, no-code-change]

# Dependency graph
requires:
  - phase: 11.3-01
    provides: DSX-EXP-053 + rebased DSX-EXP-051 (catalogue 248→249)
  - phase: 11.3-02
    provides: DSX-STA-012 (catalogue 249→250)
  - phase: 11.3-03
    provides: MISSINGNESS_METHODS + DSX-VAL-060 CRITICAL branch (reuse; no catalogue drift)
  - phase: 11.3-04
    provides: DSX-VAL-080 + DSX-SPEC-083 (catalogue 250→252)
  - phase: 11.3-05
    provides: DSX-CRV-010/011/012/013 family + module + fixtures (catalogue 252→256)
provides:
  - "REQ-P11.3-07: final currency verification — finding catalogue confirmed current at 256 codes against the fully-merged tree"
  - "D-15(a)-(f) fixture-presence audit recorded — every obligation on disk or exercised by a named unit test"
  - "full corpus gate green: scripts/check.sh all stages pass (1194-test suite, catalogue --check, capability manifest, gate contract good/bad/missing, determinism)"
affects: [11.3-reporting-completeness, verify-phase, phase-12-calibration]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "closing corpus-green gate: a verification-only plan that regenerates + --check-verifies the catalogue and runs the full corpus once against the fully-merged tree, changing no code (files_modified is references/finding-codes.md, which regenerated with zero diff — already current from plan 05's GREEN commit)"

key-files:
  created:
    - .planning/phases/11.3-reporting-completeness/11.3-06-SUMMARY.md
  modified: []

key-decisions:
  - "This plan authored no code. gen-finding-catalogue.py regenerated references/finding-codes.md to a byte-identical result (git diff empty) — the catalogue was already current at 256 from plan 05's dual-registry GREEN commit. --check exit 0 confirms currency; the plan's job here is the final independent verification, not a fresh mint."
  - "Every D-15 fixture obligation is satisfied either by a fixture file on disk (the five chart-review CHART-REVIEW.md variants + examples/good-CHART-REVIEW.md) or by a named unit test (missingness / multiplicity / exclusions / effect-size / single-imputation cases) — audited whole-repo by glob + grep, not asserted by hand."
  - "The prohibition (no corpus-harness expected-defect map edited to absorb a verdict change) is satisfied trivially: this plan edited nothing — tests/test_known_bad_corpus.py, tests/test_causal_verb_golden.py and their _TARGET_DEFECT_CODES / _EXPECTED_CAUGHT_DEFECTS / _INCIDENTAL_GAP_CODES maps are untouched (git diff empty)."

patterns-established:
  - "Phase-closing corpus-green plan verifies currency and runs the full gate once against the merged tree, surfacing (never swallowing) any moved verdict; here nothing moved."

requirements-completed: [REQ-P11.3-07]

coverage:
  - id: D1
    description: "The finding catalogue is current at 256 codes against the fully-merged tree: gen-finding-catalogue.py regenerates references/finding-codes.md with zero diff and --check exits 0; the header reads Total: 256 codes and all eight new codes (DSX-EXP-053, DSX-STA-012, DSX-VAL-080, DSX-SPEC-083, DSX-CRV-010/011/012/013) render under their sections."
    requirement: "REQ-P11.3-07"
    verification:
      - kind: other
        ref: "python scripts/gen-finding-catalogue.py (git diff --stat references/finding-codes.md empty) + python scripts/gen-finding-catalogue.py --check (exit 0, 'finding catalogue is current')"
        status: pass
    human_judgment: false
  - id: D2
    description: "Every D-15(a)-(f) fixture obligation is present — on disk or exercised by a named unit test: (a) examples/good-CHART-REVIEW.md + one malformed CHART-REVIEW.md per DSX-CRV-010/011/012/013 + the D-13 forbidden-input-flip fixture under tests/fixtures/chart-review/; (b) test_missingness_unknown_method_implied_fires_spec_082_high + ..._multiple_imputation_fires_neither_val_060_nor_spec_082; (c) test_exp_051_fires_with_no_family_declared + test_exp_053_fires_and_names_absent_metric; (d) test_rule_without_justification_fires_high_val_080_single_dict + test_rule_with_justification_produces_no_val_080_single_dict + test_unexpected_key_in_exclusions_fires_high_spec_083_single_dict; (e) test_unrecognised_kind_fires_medium + test_missingness_mar_with_single_imputation_fires_critical_val_060."
    requirement: "REQ-P11.3-07"
    verification:
      - kind: other
        ref: "glob tests/fixtures/chart-review/** + examples/good-CHART-REVIEW.md (6 fixture files) + grep of named tests in test_frame_val.py / test_multiplicity_family.py / test_effect_size_kind.py"
        status: pass
    human_judgment: false
  - id: D3
    description: "The full corpus re-run is green against the fully-merged tree: bash scripts/check.sh exits 0 with every stage reported green — full unit suite (Ran 1194 tests OK), catalogue --check (current), capability manifest (conformant), gate contract (good passes / bad blocks / missing errors), determinism (identical input, identical output)."
    requirement: "REQ-P11.3-07"
    verification:
      - kind: other
        ref: "bash scripts/check.sh — 'all checks passed', exit 0"
        status: pass
    human_judgment: false
  - id: D4
    description: "No corpus-harness expected-defect map was edited to absorb an unexplained verdict change — this plan authored no code; the corpus maps and golden files are untouched."
    requirement: "REQ-P11.3-07"
    verification:
      - kind: other
        ref: "git diff --stat tests/test_known_bad_corpus.py tests/test_causal_verb_golden.py references/finding-codes.md (empty)"
        status: pass
    human_judgment: false

# Metrics
duration: ~10min
completed: 2026-08-27
status: complete
---

# Phase 11.3 Plan 06: corpus-green closing gate (REQ-P11.3-07) Summary

**The terminal plan of Phase 11.3 proves the phase closed cleanly: the finding catalogue is confirmed current at 256 codes (regeneration produces zero diff, `--check` exits 0), every D-15 fixture obligation is present on disk or exercised by a named unit test, and the full corpus gate (`scripts/check.sh`) runs green against the fully-merged tree — all eight net-new codes reachable, no code changed and no expected-defect map edited.**

## Performance

- **Duration:** ~10 min (verification-only)
- **Started:** 2026-08-27
- **Completed:** 2026-08-27
- **Tasks:** 2 (catalogue currency + D-15 fixture audit; full corpus gate)
- **Files:** 0 code files changed (references/finding-codes.md regenerated byte-identical); 1 SUMMARY created

## Accomplishments

- **Task 1 — catalogue current at 256 + D-15 audit.** `python scripts/gen-finding-catalogue.py` regenerated `references/finding-codes.md` against the fully-merged tree with **zero diff** (already current from plan 05's dual-registry GREEN commit); `python scripts/gen-finding-catalogue.py --check` → "finding catalogue is current", **exit 0**. Header reads **Total: 256 codes**. All eight new codes render under their sections: `DSX-EXP-053` (HIGH, Experiment design), `DSX-STA-012` (MEDIUM, Statistical validity), `DSX-VAL-080` (HIGH, Validity frame), `DSX-SPEC-083` (HIGH, Contract structure), and the new **`DSX-CRV-010/011/012/013`** Chart-review-conformance section.
- **D-15(a)-(f) fixture audit (whole-repo glob + grep, per Task 1):** every obligation accounted for —
  - (a) conforming `examples/good-CHART-REVIEW.md` + one malformed variant per DSX-CRV code (`bad-schema-tag`, `ten-point-scale`, `missing-sentinel`, `untokenised-finding`) + the D-13 `forbidden-input-flip` boundary fixture under `tests/fixtures/chart-review/`, all exercised by `tests/test_chart_review.py`.
  - (b) unknown missingness method → `test_missingness_unknown_method_implied_fires_spec_082_high`; good `multiple_imputation` stays green → `test_mar_mechanism_with_multiple_imputation_produces_no_missingness_finding` + `test_missingness_mar_with_multiple_imputation_fires_neither_val_060_nor_spec_082`.
  - (c) no declared family, comparisons_looked_at > reported → `test_exp_051_fires_with_no_family_declared`; family < reported → `test_exp_053_fires_and_names_absent_metric`.
  - (d) exclusions with/without justification (DSX-VAL-080) → `test_rule_without_justification_fires_high_val_080_single_dict` + `test_rule_with_justification_produces_no_val_080_single_dict` (+ list variants); unexpected key (DSX-SPEC-083) → `test_unexpected_key_in_exclusions_fires_high_spec_083_single_dict` + `test_unexpected_key_in_list_of_dicts_fires_spec_083`.
  - (e) unrecognised effect_size_kind (DSX-STA-012) → `test_unrecognised_kind_fires_medium`; single_imputation under MAR (DSX-VAL-060 CRITICAL) → `test_missingness_mar_with_single_imputation_fires_critical_val_060`.
- **Task 2 — full corpus gate green.** `bash scripts/check.sh` → **all checks passed**, exit 0. Stages: unit suite `Ran 1194 tests in 42.636s ... OK`; catalogue `--check` current; capability manifest conformant (`dsx v2.0.0: 2 steps, 5 contributions, 5 gates, 9 skills, 6 agents, 13 config keys`); gate contract (good spec passes, bad spec blocks, missing spec errors); determinism (identical input, identical output).

## Task Commits

1. **Task 1: Regenerate + verify catalogue at 256, audit D-15 fixture presence** — no code diff (catalogue byte-identical); evidence recorded here and in the ledger. `--check` exit 0.
2. **Task 2: Run the full corpus gate green** — `bash scripts/check.sh` exit 0; no code diff.

**Plan metadata:** docs/tracking commit for this SUMMARY + ROADMAP/STATE/ledger (this firing).

_This is a verification-only closing plan: the "execution" IS running the two verifying gates. No RED/GREEN — the codes, fixtures and tests all landed in plans 01–05; plan 06 proves the merged tree is current and green._

## Files Created/Modified

- `.planning/phases/11.3-reporting-completeness/11.3-06-SUMMARY.md` — this summary (created).
- `references/finding-codes.md` — regenerated, **byte-identical** (no diff); already current at 256 from plan 05.
- No `dsx/` source, no test, and no corpus-harness map changed.

## Decisions Made

- **No mint, no code change.** The catalogue was already at 256; the regenerate was a no-op diff. The plan's contribution is the final independent currency + corpus verification against the fully-merged tree, exactly as its `must_haves` frame it (D-15 verified by `--check`, not by hand).
- **D-15 obligations satisfied by fixtures-or-tests, audited whole-repo.** Per Task 1's own wording, an obligation counts as present if a fixture file is on disk *or* it is exercised by a named unit test; all six are.
- **Prohibition honoured trivially.** No `_TARGET_DEFECT_CODES` / `_EXPECTED_CAUGHT_DEFECTS` / `_INCIDENTAL_GAP_CODES` edit — nothing was edited; no verdict moved.

## Deviations from Plan

None. Both tasks ran exactly as specified; the only nuance is that Task 1's regenerate produced no diff (the catalogue was already current), which is the expected steady state for a closing verification plan whose upstream plans already regenerated the catalogue.

## Issues Encountered

- The `warning: DSX-* declared twice with different text` lines printed by the generator are the **pre-existing, pinned** divergent-declaration set (S0-6c `TestCanonicalDeclarations`) — non-blocking; both `--check` and `check.sh` exit 0. Not introduced by this plan.
- Running `./bin/dsx gate verify --spec examples/good-ANALYSIS-SPEC.yaml` directly from repo root would exit 1 on the ambient `examples/DECISIONS.jsonl → DSX-PRE-041` floor; `check.sh` strips that trail in a tmpdir (`scripts/check.sh:29-33`). The authoritative corpus contract (`check.sh`) passes. Same behaviour recorded for plans 03/05 — not a regression.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **REQ-P11.3-07 complete; all seven REQ-P11.3-01..07 now have executed, gated plans. S2-3 (execute all Phase 11.3 plans) closes.**
- Next unblocked = **S2-4** (Phase 11.3 code review + auto-fix; verification `passed`; human items → HUMAN-QUEUE), then **S2-5** (`/gsd-secure-phase 11.3` + `/gsd-validate-phase 11.3`).
- **D-05 primary-source reads owed at the 11.3 UAT/ship round (non-blocking, pre-registered):** Rubin (1987) §3.1 (plan 03's single-imputation-CRITICAL DSX-VAL-060 branch) and Simmons, Nelson & Simonsohn (2011) (plan 04's DSX-VAL-080 exclusions). DSX-CRV-* needs no judgement-source citation (the schema file is its own structural criterion).

## Gate Results (as run)

- `python scripts/gen-finding-catalogue.py` → regenerated; `git diff --stat references/finding-codes.md` **empty** (byte-identical, already current).
- `python scripts/gen-finding-catalogue.py --check` → "finding catalogue is current", **exit 0**; header **Total: 256 codes**; all 8 new codes render under their sections.
- `bash scripts/check.sh` → **all checks passed**, exit 0: unit suite `Ran 1194 tests in 42.636s ... OK`; catalogue current; capability manifest conformant; gate contract good/bad/missing; determinism identical.
- `git diff --stat tests/test_known_bad_corpus.py tests/test_causal_verb_golden.py references/finding-codes.md` → **empty** (no corpus-map or catalogue edit).

---
*Phase: 11.3-reporting-completeness*
*Completed: 2026-08-27*
