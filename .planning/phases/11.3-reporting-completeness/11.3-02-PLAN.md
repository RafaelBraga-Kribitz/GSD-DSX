---
phase: 11.3-reporting-completeness
plan: 02
type: execute
wave: 2
depends_on: [11.3-01]
files_modified:
  - dsx/checks/stats.py
  - dsx/mathx.py
  - tests/test_effect_size_kind.py
  - references/finding-codes.md
autonomous: true
requirements: [REQ-P11.3-05]
must_haves:
  truths:
    - "An unrecognised effect_size_kind fires DSX-STA-012 MEDIUM instead of silently skipping the magnitude guard (D-11, ROADMAP SC5)."
    - "interpret_effect is never called on the unrecognised-kind branch, so no ValueError traceback reaches the gate path (D-01/D-11)."
    - "The recognised-kind set the guard uses is single-sourced with (or asserted equal to) interpret_effect's table keys, so the two cannot drift."
  artifacts:
    - "dsx/checks/stats.py effect-size membership guard preceding the interpret_effect call, emitting DSX-STA-012"
    - "dsx/mathx.py EFFECT_SIZE_KINDS module-level constant (or an equality-assertion test)"
    - "tests/test_effect_size_kind.py (new) covering fire, no-fire, and no-traceback"
    - "references/finding-codes.md regenerated to include DSX-STA-012"
  key_links:
    - "The guard must PRECEDE interpret_effect and route the value through normalize(); a try/except around interpret_effect is forbidden (still routes an exception through the gate path)."
  prohibitions:
    - statement: "No try/except substitute — the guard decides membership BEFORE interpret_effect is ever called, so mathx.py's ValueError-on-unknown-kind (mathx.py:302) cannot fire on the gate path."
      status: resolved
      verification: "tests/test_effect_size_kind.py feeds an unrecognised kind and asserts DSX-STA-012 fires with no exception raised."
    - statement: "A crafted mixed-case/whitespace effect_size_kind must not bypass the guard and reach interpret_effect."
      status: resolved
      verification: "A test feeds a mixed-case/whitespace kind and asserts it is treated as unrecognised (routed through normalize())."
---

<objective>
Deliver REQ-P11.3-05: mint DSX-STA-012 MEDIUM for an unrecognised effect_size_kind, fired from a
membership guard that PRECEDES the interpret_effect dispatch in dsx/checks/stats.py, so the
existing silent skip becomes a visible MEDIUM and no ValueError traceback ever reaches the gate
path. Single-source the recognised-kind set with mathx.py's interpret_effect table.

Purpose: A declared effect-size kind the tool does not recognise is currently a silent skip that
leaves the magnitude guard DSX-STA-011 effectively disabled; make the gap visible without
over-claiming that the effect is negligible.
Output: DSX-STA-012 (MEDIUM), a single-sourced recognised-kind set, a new test module, and a
regenerated catalogue.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.3-reporting-completeness/11.3-CONTEXT.md
@.planning/phases/11.3-reporting-completeness/11.3-RESEARCH.md
@.planning/phases/11.3-reporting-completeness/11.3-PATTERNS.md
</context>

## Artifacts this phase produces (whole phase, for orientation)

- Net-new finding codes (8): `DSX-EXP-053` HIGH, `DSX-STA-012` MEDIUM, `DSX-VAL-080` HIGH,
  `DSX-SPEC-083` HIGH, `DSX-CRV-010/011/012/013`. Catalogue 248 → 256.
- Reused codes: `DSX-EXP-051` (rebased), `DSX-SPEC-082` (membership row), `DSX-VAL-060` (CRITICAL branch).
- New module `dsx/checks/chart_review.py`; new functions `_check_exclusions`, the effect-size
  guard (this plan), the exclusions closed-key guard; new vocab `MISSINGNESS_METHODS`.
- New spec fields `validity_frame.exclusions.{rule, action, applied_before_split, justification}`,
  `missingness.method_implied`. New fixtures and test modules per the D-15 list.

**This plan produces:** `DSX-STA-012`; the `EFFECT_SIZE_KINDS` single-source (mathx.py);
`tests/test_effect_size_kind.py`; the catalogue row for `DSX-STA-012`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pin DSX-STA-012 fire / no-fire / no-traceback</name>
  <files>tests/test_effect_size_kind.py</files>
  <read_first>
    - dsx/checks/stats.py:277-289 (the `if kind in ("d","h","r"):` dispatch with no else — the exact
      silent-skip site) and how test_dsx.py currently exercises DSX-STA-011 (import + assertion style)
    - dsx/mathx.py:292-306 (interpret_effect; table at :295-299; `raise ValueError` at :302)
    - 11.3-CONTEXT.md D-11 (MEDIUM not LOW not HIGH; guard precedes the call; no try/except; set
      single-sourced or tested-equal) and 11.3-RESEARCH.md Pitfall 4
  </read_first>
  <behavior>
    - A results.tests entry with a significant p-value, a non-null standardized effect, and an
      effect_size_kind OUTSIDE the recognised set fires DSX-STA-012 MEDIUM and does NOT raise.
    - A recognised kind (d/h/r) does NOT fire DSX-STA-012 (and still reaches the DSX-STA-011
      magnitude guard as before).
    - A mixed-case or whitespace-padded unrecognised kind is treated as unrecognised (normalize()),
      i.e. still fires DSX-STA-012 and still does not raise.
  </behavior>
  <action>
    Create tests/test_effect_size_kind.py as a unittest.TestCase using inline spec/test dicts (a
    dedicated module keeps this plan disjoint from test_dsx.py, which Wave-1 and other plans read).
    Add tests for: unrecognised kind fires MEDIUM with no exception; recognised kind stays silent;
    a crafted mixed-case/whitespace kind is still caught. Mark methods with `# REQ-P11.3-05`. These
    MUST fail now because DSX-STA-012 does not exist and the current branch silently skips.
  </action>
  <acceptance_criteria>
    - `python -m unittest tests.test_effect_size_kind -v` runs and FAILS (RED).
    - The module asserts both fire (unrecognised) and no-raise, and covers the crafted-kind case.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_effect_size_kind -v</automated>
  </verify>
  <done>The new module fails for the right reason: DSX-STA-012 and the guard do not yet exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add the membership guard, single-source the recognised set, regenerate catalogue</name>
  <files>dsx/checks/stats.py, dsx/mathx.py, references/finding-codes.md</files>
  <read_first>
    - dsx/checks/stats.py:277-289 (dispatch site) and the local `table` in dsx/mathx.py:295-299
    - 11.3-PATTERNS.md "dsx/checks/stats.py" section (guard-before-branch shape) and the
      `report.add` shared shape
  </read_first>
  <behavior>
    - tests/test_effect_size_kind.py (Task 1) turns GREEN.
    - No new drift is introduced beyond the DSX-STA-012 row.
  </behavior>
  <action>
    In dsx/mathx.py hoist the interpret_effect table keys to a module-level constant
    `EFFECT_SIZE_KINDS = frozenset({"d", "h", "r"})` and have interpret_effect derive its recognised
    set from it (single-sourcing per D-11's option (a)). In dsx/checks/stats.py, at the dispatch
    site (:277-289), compute `kind = normalize(test.get("effect_size_kind", "d"))` then branch: if
    `kind in EFFECT_SIZE_KINDS` call interpret_effect as today; ELSE
    `report.add("DSX-STA-012", "MEDIUM", <short title: unrecognised effect_size_kind>,
    detail=<names the declared value and the recognised set>, remedy=<declare one of d/h/r>,
    where=<the results.tests path>)` and do NOT call interpret_effect. Severity literal "MEDIUM".
    Carry a D-05 `Structural criterion:` line in the guard's docstring (the recognised set is the
    interpret_effect table domain). Import EFFECT_SIZE_KINDS into stats.py so the two sets cannot
    drift. Regenerate references/finding-codes.md via `python scripts/gen-finding-catalogue.py`.
  </action>
  <acceptance_criteria>
    - `python -m unittest tests.test_effect_size_kind -v` passes (GREEN).
    - `dsx/checks/stats.py` imports `EFFECT_SIZE_KINDS` from dsx.mathx and contains
      `report.add("DSX-STA-012", "MEDIUM"`.
    - No `try`/`except` wraps the interpret_effect call.
    - `python scripts/gen-finding-catalogue.py --check` exits 0 and finding-codes.md has a
      `DSX-STA-012` row.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_effect_size_kind -v && python scripts/gen-finding-catalogue.py --check</automated>
  </verify>
  <done>DSX-STA-012 fires MEDIUM on an unrecognised kind, the recognised set is single-sourced, and the catalogue is current.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied results.tests[].effect_size_kind → stats.py | Untrusted declared kind string crosses into the magnitude dispatch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.3-03 | Tampering | A crafted `effect_size_kind` (mixed-case/whitespace) reaching interpret_effect unguarded | medium | mitigate | Route the membership test through the existing `normalize()` before comparing to EFFECT_SIZE_KINDS, so casing/whitespace cannot slip past the guard (spec.py:1231 normalize-then-compare precedent). |
| T-11.3-04 | Denial of Service | interpret_effect's `raise ValueError` (mathx.py:302) reaching the gate path | high | mitigate | The guard decides membership BEFORE the call; an unrecognised kind never calls interpret_effect. No try/except substitute (D-11). A test asserts no exception is raised on an unrecognised kind. |
| T-11.3-SC | Tampering | package installs | low | accept | No packages installed (D-01 stdlib-only). |
</threat_model>

<verification>
- `python -m unittest tests.test_effect_size_kind -v` green.
- `python scripts/gen-finding-catalogue.py --check` exits 0.
- `python -m unittest discover -s tests -q` green (wave-merge check).
</verification>

<success_criteria>
- REQ-P11.3-05: an unrecognised effect_size_kind fires DSX-STA-012 MEDIUM with no gate-path
  traceback; the recognised set is single-sourced with mathx.py; the catalogue is current.
</success_criteria>

<output>
Create `.planning/phases/11.3-reporting-completeness/11.3-02-SUMMARY.md` when done.
</output>
