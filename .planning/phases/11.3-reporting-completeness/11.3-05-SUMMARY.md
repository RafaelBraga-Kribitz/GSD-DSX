---
phase: 11.3-reporting-completeness
plan: 05
subsystem: chart-review-conformance
tags: [dsx-crv-010, dsx-crv-011, dsx-crv-012, dsx-crv-013, chart-review, report-acceptance-precondition, degrade-to-empty, dual-registry, d-13-boundary, tdd]

# Dependency graph
requires:
  - phase: 11.3-04
    provides: catalogue at 252 + the dual-registry (PREFIX_GROUPS + _D05_ALLOWLIST_PREFIXES) editing pattern this plan extends for a brand-new family
  - phase: 7-validity-frame
    provides: dsx/checks/figures.py degrade-to-empty artifact-reader template + tests/test_frame_boundary.py::TestFrameParadigmReadBoundary forbidden-input boundary shape
provides:
  - "DSX-CRV-010 HIGH — CHART-REVIEW.md frontmatter schema tag is not dsx-chart-review-v1"
  - "DSX-CRV-011 MEDIUM — the forbidden free-form ten-point-scale ('X/10') substring is present"
  - "DSX-CRV-012 HIGH — the file does not end with the terminal '## CHART AUDIT COMPLETE' sentinel"
  - "DSX-CRV-013 MEDIUM — a finding line carries neither a DSX- code nor UNMAPPED"
  - "new module dsx/checks/chart_review.py: structural-only reader, degrades to ok on missing/undecodable/unparseable (never a gate-path traceback, D-01/T-11.3-11)"
  - "DSX-CRV-* routed as a report-acceptance PRECONDITION in references/chart-review-schema.md, NOT folded into Gate D's DSX-FIG-* wildcard (D-14/D-12 circularity avoided)"
  - "chart_review wired into GATE_PROFILES verify + ship (not plan/execute) with an explicit phase_dir-passing run_checks branch"
affects: [11.3-reporting-completeness, verify-phase, ship-phase, finding-catalogue]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "structural-only artifact reader cloned from figures.check — reads frontmatter schema tag + body text ONLY, never scores.*/gates.A-D/final_assessment/verdict prose (D-13)"
    - "brand-new finding family requires BOTH catalogue registries in the same commit as the module: PREFIX_GROUPS (rows render) + _D05_ALLOWLIST_PREFIXES (citations enforced) — the two silent-failure omissions (RESEARCH Pitfalls 2, 3)"
    - "report-acceptance precondition: a family that gates the validity of the report artifact itself, evaluated BEFORE the Gate A-D proxies inside it are trusted, kept out of the Gate-D wildcard it would otherwise report on"

key-files:
  created:
    - dsx/checks/chart_review.py
    - tests/test_chart_review.py
    - examples/good-CHART-REVIEW.md
    - tests/fixtures/chart-review/bad-schema-tag/CHART-REVIEW.md
    - tests/fixtures/chart-review/ten-point-scale/CHART-REVIEW.md
    - tests/fixtures/chart-review/missing-sentinel/CHART-REVIEW.md
    - tests/fixtures/chart-review/untokenised-finding/CHART-REVIEW.md
    - tests/fixtures/chart-review/forbidden-input-flip/CHART-REVIEW.md
  modified:
    - dsx/cli.py
    - scripts/gen-finding-catalogue.py
    - tests/test_gen_finding_catalogue.py
    - references/finding-codes.md
    - references/chart-review-schema.md

key-decisions:
  - "The family carries the DSX-CRV prefix (NOT DSX-FIG) — the schema's Gate-D proxy wildcards DSX-FIG-* at CRITICAL/HIGH, so minting a DSX-FIG-* code for the report's own structural defects would sweep the report into the very gate it reports on (D-12). The D-14 schema edit routes DSX-CRV-* as a report-acceptance precondition."
  - "The four codes read STRUCTURE ONLY (frontmatter schema tag, the forbidden 'X/10' substring, the terminal sentinel, the DSX-/UNMAPPED token on finding lines) — never final_assessment, scores.*, agent-transcribed gates.A-D, or verdict prose (D-13). The forbidden-input names appear in chart_review.py only inside docstrings/comments documenting the prohibition, never as field reads."
  - "check() mirrors figures.check's degrade-to-empty contract: a missing, undecodable, or unparseable-frontmatter CHART-REVIEW.md returns report.ok(...) and never raises on the gate path (D-01/T-11.3-11)."
  - "chart_review joins GATE_PROFILES verify + ship ONLY (a post-execution artifact, mirroring figures/narrative/claims/smells) — NOT plan/execute."

patterns-established:
  - "Report-acceptance precondition family: gate the validity of a report artifact as an artifact, kept structurally separate from the analytical gates the report narrates."

requirements-completed: [REQ-P11.3-06]

coverage:
  - id: D1
    description: "The four structural codes fire/stay-silent exactly: DSX-CRV-010 HIGH on a wrong schema tag, DSX-CRV-011 MEDIUM on the forbidden X/10 substring, DSX-CRV-012 HIGH on a missing terminal sentinel, DSX-CRV-013 MEDIUM on an untokenised finding line; the conforming good fixture fires none of the four."
    requirement: "REQ-P11.3-06"
    verification:
      - kind: unit
        ref: "tests/test_chart_review.py#TestChartReview (9 fire/no-fire cases + good-fixture-fires-none)"
        status: pass
    human_judgment: false
  - id: D2
    description: "D-13 boundary — a structurally conforming CHART-REVIEW.md carrying a deliberately wrong scores/gates/final_assessment block fires NONE of the four; flipping only a forbidden-input field never changes any verdict (the codes do not read judgement content)."
    requirement: "REQ-P11.3-06"
    verification:
      - kind: unit
        ref: "tests/test_chart_review.py#TestChartReviewD13Boundary (forbidden-input flip fires none + matches good-fixture verdict exactly)"
        status: pass
    human_judgment: false
  - id: D3
    description: "A missing, undecodable, or unparseable-frontmatter/truncated CHART-REVIEW.md returns a report WITHOUT raising (degrade-to-empty, never a gate-path traceback)."
    requirement: "REQ-P11.3-06"
    verification:
      - kind: unit
        ref: "tests/test_chart_review.py#TestChartReviewDegradesSafely (missing / no-phase-dir / undecodable / truncated / unparseable-yaml no-raise)"
        status: pass
    human_judgment: false
  - id: D4
    description: "The DSX-CRV family is registered in BOTH catalogue registries (PREFIX_GROUPS renders the rows; _D05_ALLOWLIST_PREFIXES enforces citations) and finding-codes.md regenerated 252 -> 256 with the four rows under a DSX-CRV section."
    requirement: "REQ-P11.3-06"
    verification:
      - kind: unit
        ref: "tests/test_gen_finding_catalogue.py#test_dsx_crv_prefix_group_registered + test_dsx_crv_prefix_in_d05_allowlist"
        status: pass
      - kind: other
        ref: "python scripts/gen-finding-catalogue.py --check (exit 0; 256 codes; 4 DSX-CRV rows rendered)"
        status: pass
    human_judgment: false
  - id: D5
    description: "chart_review joins verify + ship (not plan/execute) and the canonical gate contract is unchanged: good spec passes, bad spec blocks at both verify and ship (chart_review degrades to ok on examples/ which has no convention-named CHART-REVIEW.md in the gate tmpdir)."
    requirement: "REQ-P11.3-06"
    verification:
      - kind: other
        ref: "bash scripts/check.sh — 'gate contract: good spec passes, bad spec blocks, missing spec errors' PASS"
        status: pass
    human_judgment: false

# Metrics
duration: ~35min (across two firings — RED+GREEN one firing, Task-3 wiring + gate + close the next)
completed: 2026-08-27
status: complete
---

# Phase 11.3 Plan 05: chart-review structural conformance (DSX-CRV family) Summary

**A new deterministic checks-layer module `dsx/checks/chart_review.py` validates CHART-REVIEW.md's structure with four DSX-CRV-* codes (schema tag, forbidden X/10 scale, terminal sentinel, tokenised finding lines) reading structure only — never any stochastic agent verdict (D-13) — degrading safely to an empty report on a missing/undecodable file, registered in both catalogue registries and routed in the schema as a report-acceptance precondition rather than into the Gate-D wildcard it would otherwise report on (D-14/D-12).**

## Performance

- **Duration:** ~35 min across two firings (RED + GREEN in one firing; Task-3 wiring, wave-merge gate, and close in the next after a mid-Task-3 stop — see Deviations)
- **Started:** 2026-08-27
- **Completed:** 2026-08-27
- **Tasks:** 3 (RED, GREEN, wire+schema)
- **Files:** 8 created (module + test + good fixture + 5 malformed/boundary fixtures), 5 modified (cli.py, gen-finding-catalogue.py, test_gen_finding_catalogue.py, finding-codes.md, chart-review-schema.md)

## Accomplishments
- `dsx/checks/chart_review.py` (new module) — `check(spec, phase_dir=None, *, strict=False) -> Report` named `"chart_review"`, mirroring `figures.check`: resolves CHART-REVIEW.md under the phase_dir/cwd roots, `report.ok(...)` and returns on missing/undecodable/unparseable (never raises). Emits, using literal severities: `DSX-CRV-010` HIGH (`:134`, schema tag ≠ `dsx-chart-review-v1`), `DSX-CRV-011` MEDIUM (`:166`, forbidden `X/10` substring present), `DSX-CRV-012` HIGH (`:197`, missing terminal `## CHART AUDIT COMPLETE` sentinel), `DSX-CRV-013` MEDIUM (`:231`, finding line lacking a `DSX-`/`UNMAPPED` token). Each emit site carries a `Structural criterion:` docstring pointing at the schema rule it enforces.
- `scripts/gen-finding-catalogue.py` dual-registry, same commit as the module: `PREFIX_GROUPS` gains `("DSX-CRV", "Chart review conformance", …)` (rows render) AND `_D05_ALLOWLIST_PREFIXES` gains `"DSX-CRV-"` (citations enforced) — both with a dated Phase-11.3/D-12 comment. Catalogue regenerated 252 → **256**.
- `dsx/cli.py` four-site registration mirroring figures: import (alphabetically before `claims`); `CHECKS["chart_review"]`; an explicit `elif name == "chart_review":` run_checks branch passing `root`; `chart_review` added to `GATE_PROFILES["verify"]` and `["ship"]` tuples only (not plan/execute).
- `references/chart-review-schema.md` D-14 edit: a "Report-acceptance precondition" table routing `DSX-CRV-*` as a gate on the report's validity as an artifact, explicitly NOT folded into Gate D's `DSX-FIG-*` wildcard (with the circularity rationale inline).
- Fixtures: conforming `examples/good-CHART-REVIEW.md`; one malformed variant per code + a forbidden-input-flip fixture under `tests/fixtures/chart-review/`. `tests/test_chart_review.py` (16 tests, 3 classes): fire/no-fire per code, the D-13 boundary pair, and the degrade-safely no-raise set.

## Task Commits

1. **Task 1: RED — CHART-REVIEW fixtures + DSX-CRV-010/011/012/013 fire/no-fire + D-13 boundary** - `f33b5b9` (test)
2. **Task 2: GREEN — chart_review.py (four codes, degrade-to-empty) + catalogue dual-registry + regenerate** - `56c8fc9` (feat)
3. **Task 3: wire chart_review at verify/ship (cli.py) + route DSX-CRV-* as a report-acceptance precondition (D-14 schema)** - `<this firing's feat commit>`

**Plan metadata:** (docs commit for this SUMMARY — see final commit)

_TDD: RED (import/attribute error on the missing module — 16 tests fail for the right reason) → GREEN (all pass) → wire (gate contract unchanged)._

## Files Created/Modified
- `dsx/checks/chart_review.py` — new structural-only reader, four DSX-CRV-* emit sites, degrade-to-empty.
- `dsx/cli.py` — import + CHECKS + run_checks branch + GATE_PROFILES verify/ship (4 sites).
- `scripts/gen-finding-catalogue.py` — `PREFIX_GROUPS` + `_D05_ALLOWLIST_PREFIXES` DSX-CRV entries.
- `references/finding-codes.md` — regenerated; four DSX-CRV rows (252 → 256).
- `references/chart-review-schema.md` — D-14 report-acceptance precondition table for DSX-CRV-*.
- `tests/test_chart_review.py` — 16 tests, 3 classes (fire/no-fire, D-13 boundary, degrade-safely).
- `tests/test_gen_finding_catalogue.py` — DSX-CRV `PREFIX_GROUPS` + `_D05_ALLOWLIST_PREFIXES` membership assertions.
- 5 fixtures under `tests/fixtures/chart-review/` + `examples/good-CHART-REVIEW.md`.

## Decisions Made
- DSX-CRV, not DSX-FIG (D-12 circularity): the schema Gate-D proxy wildcards `DSX-FIG-*`, so the report's own structural defects get their own prefix and are routed as a precondition (D-14), never into the gate the report narrates.
- Structure-only reads (D-13): the forbidden-input field names appear in the module only inside docstrings/comments documenting the prohibition; a boundary test proves flipping only a scores/gates/final_assessment field changes no verdict.
- verify + ship only (not plan/execute): a post-execution artifact, mirroring figures/narrative/claims/smells.

## Deviations from Plan

**1. [Process — mid-unit death recovered by a later firing, no scope change] Task 3 was left uncommitted by an earlier firing.**
- **Found during:** the reconcile step of the closing firing (2026-08-27T07:xxZ). The RED (`f33b5b9`) and GREEN (`56c8fc9`) commits were on the branch **unpushed** (2 ahead of origin), and Task 3's edits (`dsx/cli.py` + `references/chart-review-schema.md`) sat **uncommitted** in the working tree, with no SUMMARY, no STATE/ROADMAP/ledger update. The prior firing died after the GREEN commit, mid-Task-3, before running the wave-merge gate — which is exactly where the gate matters, since Task 3 is what puts chart_review into the verify/ship profiles.
- **Resolution:** the closing firing independently verified the committed + uncommitted state was genuinely green (`bash scripts/check.sh` → all checks passed, including the **gate contract**), then committed Task 3, wrote this SUMMARY, updated tracking, and pushed. No code was changed during recovery — the uncommitted edits matched Task 3 exactly.

**2. [Clarification, not a defect] Running `./bin/dsx gate verify --spec examples/good-ANALYSIS-SPEC.yaml` directly from repo root exits 1.**
- **Detail:** it fires `DSX-CRV`-unrelated `DSX-PRE-041` (Phase 11.2's uncleared-amendment floor) on the ambient, ever-growing `examples/DECISIONS.jsonl`. `check.sh` copies `examples/` to a tmpdir and deletes `DECISIONS.jsonl` before the contract (scripts/check.sh:29-33) precisely for this reason. The authoritative gate contract (`check.sh`) passes; chart_review never fired on the good spec. This is the same ambient-trail behaviour recorded for Phase 11.2 (S1-3 Wave 2), not a regression.

---

**Total deviations:** 0 auto-fixed bugs; 1 process recovery + 1 environment clarification.
**Impact on plan:** None on behaviour. All hard constraints honoured: severities HIGH/MEDIUM/HIGH/MEDIUM; `dsx/findings.py` untouched; DSX-CRV prefix (not DSX-FIG); both registries in the module's commit; verify/ship only; catalogue 252 → 256.

## Issues Encountered
- None beyond Deviation 1 (mid-unit death) and Deviation 2 (ambient-trail gate artifact), both resolved/clarified above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- REQ-P11.3-06 complete. Remaining: Wave 5 = plan 06 (REQ-P11.3-07 corpus-green, MUST run last per D-06) closes S2-3.
- D-05 primary-source reads for the phase (Rubin 1987 §3.1 for plan 03's DSX-VAL-060 branch; Simmons 2011 for plan 04's DSX-VAL-080) remain owed at the 11.3 UAT/ship round (non-blocking, pre-registered in HUMAN-QUEUE HQ-4-adjacent). DSX-CRV-* needs no judgement-source citation — the schema file is its own structural criterion.

## Gate Results (as run)
- `python -m unittest tests.test_chart_review` → 16 tests OK.
- `python -m unittest tests.test_chart_review tests.test_gen_finding_catalogue` → 60 tests OK.
- `python scripts/gen-finding-catalogue.py --check` → exit 0; finding-codes.md has DSX-CRV-010/011/012/013 (256 codes).
- `python -m unittest discover -s tests -q` (in check.sh) → **1194 tests OK** (was 1176; +18 = 16 chart_review + 2 catalogue-registry assertions).
- `bash scripts/check.sh` → **all checks passed** (catalogue current; capability manifest conformant; gate contract good passes / bad blocks / missing errors; determinism).
- Emit sites: DSX-CRV-010 `dsx/checks/chart_review.py:134`; DSX-CRV-011 `:166`; DSX-CRV-012 `:197`; DSX-CRV-013 `:231`.

---
*Phase: 11.3-reporting-completeness*
*Completed: 2026-08-27*
