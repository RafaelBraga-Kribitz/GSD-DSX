---
phase: 11.3-reporting-completeness
plan: 03
type: execute
wave: 2
depends_on: [11.3-01]
files_modified:
  - dsx/spec.py
  - dsx/frame/val.py
  - tests/test_frame_val.py
autonomous: true
requirements: [REQ-P11.3-03]
must_haves:
  truths:
    - "missingness.method_implied is a closed vocabulary in the frame-membership table; an unrecognised method fires DSX-SPEC-082 HIGH (D-04, ROADMAP SC3)."
    - "single_imputation (and mean/median/mode/LOCF as-if-observed) under a declared MAR mechanism fires DSX-VAL-060 CRITICAL and blocks at plan (D-05, ROADMAP SC3)."
    - "examples/good-ANALYSIS-SPEC.yaml (declares multiple_imputation) still exits 0 — the vocab neither over- nor under-fires."
  artifacts:
    - "dsx/spec.py MISSINGNESS_METHODS constant (7 members) + the ('missingness','method_implied',MISSINGNESS_METHODS) membership row"
    - "dsx/frame/val.py _MISSINGNESS_METHOD_VALIDITY per-method severity split + the third DSX-VAL-060 branch (CRITICAL) carrying its own Rubin (1987) §3.1 citation"
    - "tests/test_frame_val.py cases for the unknown method, the single_imputation-CRITICAL block, and the good-fixture-stays-green regression"
  key_links:
    - "The new CRITICAL branch MUST reuse the exact existing DSX-VAL-060 title 'missingness mechanism paired with a method it does not license' with severity literal CRITICAL, so the rendered catalogue row and the _CANONICAL_DECLARATIONS pin do NOT drift — only the remedy/detail (Rubin 1987 §3.1) differ. This plan does NOT regenerate references/finding-codes.md."
  prohibitions:
    - statement: "The single-imputation-CRITICAL branch must NOT reuse the White & Carlin (2010) complete-case citation (val.py:158-165) — it concerns bias, not the standard-error fabrication that makes single imputation strictly worse; it gets its own Rubin (1987) §3.1 anchor (D-06)."
      status: resolved
      verification: "The new branch's remedy/docstring names Rubin (1987) §3.1 and the variance decomposition; a test asserts single_imputation-under-MAR fires CRITICAL (not HIGH)."
    - statement: "MISSINGNESS_METHODS must include multiple_imputation (good fixture declares it) and single_imputation (else the block path is unreachable); it must not be silently shrunk below the 7 locked members."
      status: resolved
      verification: "A test asserts multiple_imputation is accepted (no DSX-SPEC-082) and an omitted/unknown method fires DSX-SPEC-082; the good fixture gate stays green."
---

<objective>
Deliver REQ-P11.3-03: register missingness.method_implied as a closed vocabulary in the existing
frame-membership table (reusing DSX-SPEC-082, minting no code), and split
_MISSINGNESS_METHOD_VALIDITY so that single imputation treated as if observed, under a declared
missing-at-random mechanism, fires DSX-VAL-060 at CRITICAL and blocks at plan.

Purpose: single imputation fabricates precision — treating imputed values as observed sets n to
the full sample and drops the missing-data variance component, producing anti-conservative
intervals. That is the gate's north-star sin: a structural defect no downstream evidence rescues.
Complete-case under MAR only sacrifices power while reporting honest uncertainty, so it stays HIGH.
Output: MISSINGNESS_METHODS (7 members) + the membership row; the per-method severity split with
the new CRITICAL branch and its own Rubin (1987) §3.1 citation; the tests; no catalogue change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.3-reporting-completeness/11.3-CONTEXT.md
@.planning/phases/11.3-reporting-completeness/11.3-RESEARCH.md
@.planning/phases/11.3-reporting-completeness/11.3-PATTERNS.md
@.planning/phases/07-validity-frame-checks-dsx-val/07-CONTEXT.md
</context>

## Artifacts this phase produces (whole phase, for orientation)

- Net-new finding codes (8): `DSX-EXP-053`, `DSX-STA-012`, `DSX-VAL-080`, `DSX-SPEC-083`,
  `DSX-CRV-010/011/012/013`. Catalogue 248 → 256.
- Reused codes: `DSX-EXP-051` (rebased), `DSX-SPEC-082` (new membership row — THIS plan),
  `DSX-VAL-060` (new CRITICAL branch — THIS plan).
- New module `dsx/checks/chart_review.py`; new functions `_check_exclusions`, the effect-size
  guard, the exclusions closed-key guard; new vocab `MISSINGNESS_METHODS` (THIS plan).
- New spec fields `validity_frame.exclusions.{...}`, `missingness.method_implied` (THIS plan).

**This plan produces:** `MISSINGNESS_METHODS`; the method_implied membership row (reuses
`DSX-SPEC-082`); the per-method severity split with the third `DSX-VAL-060` CRITICAL branch;
the frame-val tests. It mints NO new code and does NOT touch references/finding-codes.md.

**Out of scope (D-07, deferred):** missingness-rate reconciliation (declared `missingness.rate`
vs the profile's actual null rates) is NOT shipped in 11.3 — `missingness.rate` stays read by no
check, as today. This closes the ROADMAP open item; the concrete defect it targeted is already
covered by `DSX-DQ-030` (profile null_rate vs assertions.max_null_rate) plus `DSX-VAL-060`. Do NOT
add any rate-reconciliation logic here (it would false-positive on the good fixture and force a
frame-layer check to read the profile — a D-11 boundary breach). The four-part entry condition for
a future backlog item lives in 11.3-CONTEXT.md D-07.

<assumption_delta_decision>
- Now-primary noun: **per-method severity within a missingness mechanism** (was: one severity per
  mechanism in `_MISSINGNESS_METHOD_VALIDITY`).
- Decision: **promote** — re-key the MAR deny set more granularly into a HIGH subset
  `{complete_case, available_case}` (unchanged behaviour) and a new CRITICAL subset
  `{single_imputation}` plus its as-if-observed instances. The old per-mechanism severity becomes
  the default for the un-split members; this is NOT an add-alongside second table.
- Rationale: single imputation understates variance (Rubin's `T = W̄ + (1+1/m)·B` with `B` forced
  to 0) in the anti-conservative direction, which complete-case does not; the severity must be
  keyed at method granularity, not mechanism granularity.
- Suggested (optional) invariant test: every declared missingness method resolves to exactly one
  severity, so a future singular reversion (collapsing back to one-per-mechanism) goes red.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pin the closed vocab, the single-imputation CRITICAL block, and the good-fixture regression</name>
  <files>tests/test_frame_val.py</files>
  <read_first>
    - dsx/spec.py:1116-1126 (_VALIDITY_FRAME_MEMBERSHIP tuple) and :1220-1239 (the DSX-SPEC-082
      membership loop); dsx/spec.py near :442 (MISSINGNESS_MECHANISMS — where MISSINGNESS_METHODS
      belongs)
    - dsx/frame/val.py:187-198 (_MISSINGNESS_METHOD_VALIDITY current two-branch table) and
      :1145-1178 (the DSX-VAL-060 HIGH/CRITICAL emit; the literal-severity comment at :1140-1144)
    - tests/test_frame_val.py TestValSamplingMissingnessMeasurement (existing DSX-VAL-060 tests —
      copy the class/method convention and the `# D-05: DSX-VAL-060` marker)
    - examples/good-ANALYSIS-SPEC.yaml:348-351 (missingness.mechanism: MAR, method_implied:
      multiple_imputation) — the regression that must stay green
    - 11.3-CONTEXT.md D-04, D-05, D-06 and 11.3-PATTERNS.md "dsx/frame/val.py" section
  </read_first>
  <behavior>
    - method_implied = single_imputation with mechanism MAR fires DSX-VAL-060 CRITICAL.
    - method_implied = complete_case (or available_case) with mechanism MAR fires DSX-VAL-060 HIGH
      (unchanged behaviour).
    - method_implied = multiple_imputation with mechanism MAR does NOT fire DSX-VAL-060 and does
      NOT fire DSX-SPEC-082 (recognised + licensed).
    - method_implied = an unknown string (e.g. "made_up_method") fires DSX-SPEC-082 HIGH
      (unrecognised), proving the closed vocab.
    - (Optional invariant) every member of MISSINGNESS_METHODS resolves to exactly one severity in
      the split table.
  </behavior>
  <action>
    Add unittest cases to tests/test_frame_val.py using inline spec dicts (the module's convention —
    no YAML files under examples/known-bad/). Cover each behavior clause. Assert the good-fixture
    mechanism/method pair (MAR + multiple_imputation) produces neither DSX-VAL-060 nor DSX-SPEC-082.
    Assert single_imputation-under-MAR fires DSX-VAL-060 at CRITICAL specifically (not HIGH). Mark
    with `# REQ-P11.3-03`. These MUST fail now: MISSINGNESS_METHODS and the CRITICAL single_imputation
    branch do not yet exist.
  </action>
  <acceptance_criteria>
    - `python -m unittest tests.test_frame_val -v` runs and FAILS (RED) on the new method_implied
      / single_imputation cases.
    - The new cases assert CRITICAL for single_imputation and HIGH for complete_case, both under MAR.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_frame_val -v</automated>
  </verify>
  <done>The new cases fail for the right reason: the closed vocab and the CRITICAL split are unimplemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add MISSINGNESS_METHODS + membership row (D-04) and the per-method severity split (D-05/D-06)</name>
  <files>dsx/spec.py, dsx/frame/val.py</files>
  <read_first>
    - dsx/spec.py:1116-1126 and the surrounding vocabulary constants near :442
    - dsx/frame/val.py:187-198 and :1074-1178 (_check_missingness in full, incl. the White & Carlin
      remedy at the HIGH branch and the literal-severity constraint comment at :1140-1144)
    - 11.3-CONTEXT.md D-04 (7-member locked list), D-05 (per-method split, CRITICAL for
      single_imputation-as-if-observed under MAR), D-06 (own Rubin 1987 §3.1 citation, NOT White & Carlin)
  </read_first>
  <behavior>
    - Task 1's new cases turn GREEN; existing DSX-VAL-060 HIGH/CRITICAL cases stay GREEN.
    - references/finding-codes.md needs no regeneration (no new code, same catalogue row).
  </behavior>
  <action>
    In dsx/spec.py add `MISSINGNESS_METHODS` as a module-level constant near MISSINGNESS_MECHANISMS,
    with EXACTLY the 7 locked members: multiple_imputation, single_imputation, complete_case,
    available_case, mechanism_model, selection_model, pattern_mixture_model (do not shrink). Add the
    row `("missingness", "method_implied", MISSINGNESS_METHODS)` to _VALIDITY_FRAME_MEMBERSHIP
    (:1116-1126). This reuses the existing DSX-SPEC-082 loop — add NO new report.add call site.
    In dsx/frame/val.py split _MISSINGNESS_METHOD_VALIDITY (:187-198) so the MAR deny set is keyed
    per method: HIGH subset {complete_case, available_case}; new CRITICAL subset {single_imputation}
    (and its as-if-observed mean/median/mode/LOCF instances if the table encodes them). Add a THIRD
    branch to the DSX-VAL-060 emit (:1145-1178): when the method is in the CRITICAL single-imputation
    subset under MAR, emit `report.add("DSX-VAL-060", "CRITICAL", "missingness mechanism paired with
    a method it does not license", detail=..., remedy=<names the escape: declare multiple_imputation
    or an explicitly variance-propagating method>, where="spec.validity_frame.missingness")`. The
    title string and the "CRITICAL" literal MUST match the existing CRITICAL branch verbatim so the
    catalogue and the _CANONICAL_DECLARATIONS pin do not drift; ONLY remedy/detail differ. Add a
    D-05 docstring line citing Rubin (1987) §3.1 (`T = W̄ + (1+1/m)·B`, single-imputation variance
    understatement) — do NOT reuse the White & Carlin (2010) complete-case citation. Add the
    optional invariant test that each MISSINGNESS_METHODS member resolves to exactly one severity.
  </action>
  <acceptance_criteria>
    - `python -m unittest tests.test_frame_val -v` passes (GREEN), including the single_imputation
      CRITICAL case and the multiple_imputation no-fire case.
    - `dsx/spec.py` defines MISSINGNESS_METHODS with all 7 members and the new membership row; no new
      report.add site was added for the membership check.
    - `dsx/frame/val.py` contains a third DSX-VAL-060 emit whose remedy names Rubin (1987) §3.1 and
      does not repeat the White & Carlin remedy text.
    - `python scripts/gen-finding-catalogue.py --check` exits 0 WITHOUT regenerating
      references/finding-codes.md (no drift — proves the title/severity reuse is correct).
    - `./bin/dsx gate plan --spec examples/good-ANALYSIS-SPEC.yaml` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_frame_val -v && python scripts/gen-finding-catalogue.py --check</automated>
  </verify>
  <done>The closed vocab types method_implied, single_imputation-under-MAR blocks at plan at CRITICAL with its own citation, the good fixture stays green, and no catalogue drift is introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied validity_frame.missingness → spec.py membership + frame/val.py check | Untrusted declared mechanism/method crosses into spec-only frame checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.3-05 | Denial of Service | _check_missingness / membership loop on a malformed missingness block | low | mitigate | The membership loop already guards `isinstance(block, dict)` and `is_blank(value)`; the frame check degrades to an empty report on a non-dict validity_frame (val.py:204-209, T-7-01). Assert no traceback on a malformed block. |
| T-11.3-06 | Tampering | A method value crafted (case/whitespace) to dodge the closed-vocab membership | low | mitigate | Membership compares normalize(value) against {normalize(k) for k in vocab} (spec.py:1231), so casing/whitespace resolve consistently. |
| T-11.3-07 | Spoofing | A frame check reading inference.paradigm or the profile to soften the block | medium | mitigate | The split reads only spec.validity_frame.missingness; tests/test_frame_boundary.py forbids dsx.frame importing dsx.checks and reading inference.paradigm (D-11) — the existing boundary test stays green. |
| T-11.3-SC | Tampering | package installs | low | accept | No packages installed (D-01 stdlib-only). |
</threat_model>

<verification>
- `python -m unittest tests.test_frame_val -v` green (new cases + existing DSX-VAL-060 cases).
- `python -m unittest tests.test_frame_boundary -v` green (D-11 boundary unbroken).
- `python scripts/gen-finding-catalogue.py --check` exits 0 with NO regeneration of finding-codes.md.
- `python -m unittest discover -s tests -q` green (wave-merge check).
</verification>

<success_criteria>
- REQ-P11.3-03: method_implied is a closed vocabulary firing DSX-SPEC-082 on an unknown method;
  single_imputation-under-MAR blocks at plan under DSX-VAL-060 CRITICAL with its own Rubin (1987)
  §3.1 citation; the good fixture stays green; no catalogue drift.
</success_criteria>

<output>
Create `.planning/phases/11.3-reporting-completeness/11.3-03-SUMMARY.md` when done.
</output>
