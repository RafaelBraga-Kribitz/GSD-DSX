---
phase: 12-calibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_known_bad_corpus.py
  - examples/known-bad/
  - tests/test_causal_verb_golden.py
autonomous: true
requirements: [REQ-P12-01, REQ-P12-02]
must_haves:
  truths:
    - "The known-bad corpus contains at least one retracted-paper+postmortem case, at least one documented p-hacking case, and at least one operator-known-answer case, each a spec+postmortem pair (D-01)."
    - "Each new miss/backlog-promotion case carries a `<slug>-ATTRIBUTION.yaml` sidecar authored in the same change that adds the case, never retrofitted (REQ-P12-02 ordering constraint, D-06)."
    - "The three coverage classes are asserted by class-presence over glob-discovered slugs, not by a fixed count and not by a hardcoded slug list (D-01/D-02)."
    - "Every new `*-ANALYSIS-SPEC.yaml` under examples/ has a matching `_GOLDEN_SHIP_FINDINGS` entry so the whole-examples-tree golden test stays green (RESEARCH Pitfall 2)."
  artifacts:
    - "tests/test_known_bad_corpus.py::TestKnownBadCorpus.test_corpus_includes_full_coverage_classes (new coverage predicate, D-01)"
    - "examples/known-bad/<retracted-slug>-ANALYSIS-SPEC.yaml + -POSTMORTEM.md"
    - "examples/known-bad/<p-hacking-slug>-ANALYSIS-SPEC.yaml + -POSTMORTEM.md"
    - "examples/known-bad/<operator-known-slug>-ANALYSIS-SPEC.yaml + -POSTMORTEM.md"
    - "examples/known-bad/<slug>-ATTRIBUTION.yaml sidecars for miss/promotion cases"
    - "New _GOLDEN_SHIP_FINDINGS entries in tests/test_causal_verb_golden.py (one per new spec)"
  key_links:
    - "Coverage predicate reads slugs via the existing _slugs(...) glob helper (tests/test_known_bad_corpus.py:544) — never a hardcoded list."
    - "Every new spec's committed CRITICAL/HIGH finding-set is measured via the golden test's own fresh-tempdir helper, not guessed."
  prohibitions:
    - statement: "No fixture is reverse-engineered to trip a §6.5 threshold; cases are sourced from the real known-bad population and tagged as they are added (source-before-count, D-02)."
      status: must-hold
      verification: "Coverage predicate asserts class-presence, not a target count; no slug appears in a hardcoded list in the harness (grep the predicate for literal slug strings — there are none)."
    - statement: "The attribution tag never lives in the ANALYSIS-SPEC (no top-level spec key), never in postmortem prose, and never in a harness-side map — it lives only in the per-slug sidecar file (D-06)."
      status: must-hold
      verification: "Sidecar is a separate examples/known-bad/<slug>-ATTRIBUTION.yaml; grep the new specs' validity_frame/inference blocks — absent_code/promotes_backlog_item keys do not appear there; frame_digest of each new spec is unaffected by sidecar presence."
    - statement: "No DSX-* finding code is minted and nothing is added to GATE_PROFILES or the CHECKS-as-check registry (D-18)."
      status: must-hold
      verification: "Sidecars only REFERENCE existing-or-named-backlog codes; python -m unittest tests.test_finding_catalogue_invariant stays green (authored in plan 12-06); git diff shows no edit to dsx/cli.py GATE_PROFILES (dsx/cli.py:115-131)."
---

<objective>
Extend the known-bad corpus to full size by class-coverage (D-01) and instrument each new miss/promotion case with its catch-attribution sidecar in the same change (REQ-P12-02 ordering). This is the acquisition step of the measurement pipeline: source real cases, tag them as they land, and define "full" as falsifiable-by-class-present rather than by an arbitrary count.

Purpose: Give the downstream rate/FPR/friction harness (plan 12-05/12-06) and the §6.5 re-evaluation (plan 12-07) a corpus whose §6.5 entry conditions are machine-countable off honestly-sourced cases.
Output: New spec+postmortem pairs across three coverage classes, their `<slug>-ATTRIBUTION.yaml` sidecars, a class-presence coverage predicate, and lockstep golden-file entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-calibration/12-CONTEXT.md
@.planning/phases/12-calibration/12-RESEARCH.md
@.planning/phases/12-calibration/12-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols/files this plan creates (read by the plan-review source-grounding pass — exclude these from drift verification):
- `tests/test_known_bad_corpus.py::TestKnownBadCorpus.test_corpus_includes_full_coverage_classes` — new coverage predicate (D-01).
- `examples/known-bad/<retracted-slug>-ANALYSIS-SPEC.yaml`, `-POSTMORTEM.md` — new retracted-paper case.
- `examples/known-bad/<p-hacking-slug>-ANALYSIS-SPEC.yaml`, `-POSTMORTEM.md` — new p-hacking case.
- `examples/known-bad/<operator-known-slug>-ANALYSIS-SPEC.yaml`, `-POSTMORTEM.md` — new operator-known-answer case.
- `examples/known-bad/<slug>-ATTRIBUTION.yaml` — new sidecar file kind (schema: `absent_code`, `promotes_backlog_item`, `rationale?`, `kind?`), one per miss/promotion case.
- New `_GOLDEN_SHIP_FINDINGS` dict entries in `tests/test_causal_verb_golden.py` — one per new spec.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coverage predicate (RED) then source the three coverage classes (GREEN)</name>
  <files>tests/test_known_bad_corpus.py, examples/known-bad/</files>
  <read_first>
    - tests/test_known_bad_corpus.py:544-643 — `_slugs`, the `≥3` floor `test_corpus_holds_at_least_three_pairs` (:627-632), the pairing test `test_every_spec_has_a_sibling_postmortem_and_vice_versa` (:618-625), and the exact analog to copy: `test_corpus_includes_an_interference_case_and_a_bayesian_continuous_case` (:634-643).
    - .planning/phases/12-calibration/12-PATTERNS.md — "coverage predicates (D-01)" section (the class-presence idiom).
    - An existing pair for shape, e.g. examples/known-bad/weak-identification-mmm-ANALYSIS-SPEC.yaml + -POSTMORTEM.md.
  </read_first>
  <behavior>
    - RED: test_corpus_includes_full_coverage_classes fails on today's corpus because no slug marks a retracted-paper, p-hacking, or operator-known-answer class.
    - Test asserts, over `_slugs(f"*{SPEC_SUFFIX}", SPEC_SUFFIX)`: any slug contains a retracted-paper marker; any slug contains a p-hacking marker; any slug contains an operator-known-answer marker. Each assertion prints the sorted slug set on failure.
    - GREEN: after the three cases are added, the predicate passes and the existing `≥3` floor and pairing tests still pass.
  </behavior>
  <action>
    Add `test_corpus_includes_full_coverage_classes` to TestKnownBadCorpus, copying the `_slugs(...)` + `assertTrue(any(marker in slug ...))` idiom from `test_corpus_includes_an_interference_case_and_a_bayesian_continuous_case`. Assert three class markers by substring over glob-discovered slugs: a retracted-paper marker (e.g. "retract"), a p-hacking marker (e.g. "p-hack" or "phack"), and an operator-known-answer marker (e.g. "operator-known"). Do NOT hardcode any full slug; assert class presence only, matching D-01's "full is falsifiable by class present, not by an arbitrary count."

    Then source at least one real case per class from the genuine known-bad population (D-02, source-before-count — never invent a case to hit a threshold): one retracted paper carrying a published post-mortem/retraction notice; one documented p-hacking / garden-of-forking-paths case; one of the operator's own prior analyses whose answer is now known. Commit each as a `<slug>-ANALYSIS-SPEC.yaml` + `<slug>-POSTMORTEM.md` pair under examples/known-bad/, with a slug carrying the class marker the predicate checks. Give each spec the full validity_frame/inference shape used by existing pairs so it only fires its intended defect. Carry each case's citation plus an honestly-flagged, unverified locator in the postmortem — do NOT invent a page/section locator; the verbatim-quote D-05 human read is pre-registered for the Phase-12 UAT/ship round (CONTEXT deferred note), not owed now.
  </action>
  <acceptance_criteria>
    - source: `test_corpus_includes_full_coverage_classes` exists in tests/test_known_bad_corpus.py and uses `_slugs(...)`; it contains no literal full-slug string.
    - behavior: `python -m unittest tests.test_known_bad_corpus.TestKnownBadCorpus.test_corpus_includes_full_coverage_classes -v` passes AND `...test_corpus_holds_at_least_three_pairs` and `...test_every_spec_has_a_sibling_postmortem_and_vice_versa` still pass.
    - source: at least three new spec+postmortem pairs exist under examples/known-bad/, one per coverage class, each slug carrying the class marker.
    - source: each new POSTMORTEM.md names a primary source and flags its locator as unverified (no fabricated page/section).
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_known_bad_corpus.TestKnownBadCorpus.test_corpus_includes_full_coverage_classes tests.test_known_bad_corpus.TestKnownBadCorpus.test_corpus_holds_at_least_three_pairs tests.test_known_bad_corpus.TestKnownBadCorpus.test_every_spec_has_a_sibling_postmortem_and_vice_versa -v</automated>
  </verify>
  <done>The coverage predicate is class-presence over glob-discovered slugs, the three new pairs make it pass, and the floor + pairing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Author the attribution sidecars and land the golden-file entries in lockstep</name>
  <files>examples/known-bad/, tests/test_causal_verb_golden.py</files>
  <read_first>
    - .planning/phases/12-calibration/12-CONTEXT.md — D-06/D-07 (sidecar carrier + schema) and the placement precedent correction (cite 11.2 D-08 / dsx/decisions.py:99-121, NOT 11.3 D-08 — RESEARCH Pitfall 5).
    - tests/test_causal_verb_golden.py:58-142 — SPEC_GLOB, EXAMPLES_DIR, the recursive whole-examples glob, `_GOLDEN_SHIP_FINDINGS`, and the fresh-tempdir `_ship_findings` helper that measures a spec's committed CRITICAL/HIGH set.
    - brief.md:369-379 — the nine §6.5 rows, to pick the real item id each sidecar's `promotes_backlog_item` names.
  </read_first>
  <action>
    For each new case that is a miss or a backlog-promotion case (a pure-catch case gets no sidecar, per D-03), author `examples/known-bad/<slug>-ATTRIBUTION.yaml` with the D-07 schema: `absent_code` (required — the currently-ABSENT code that WOULD have caught the miss, opposite polarity to _TARGET_DEFECT_CODES; a named §6.5 backlog code such as DSX-PAR-020/-021/-030 or DSX-ADM-* is legitimate and is not minting), `promotes_backlog_item` (required — the real §6.5 item id this case's absence promotes), `rationale` (optional one line), `kind` (optional, default "miss"; use "caught" only for a case whose named code DOES fire CRITICAL live). Keep every field OUT of the spec file so it never perturbs frame_digest (D-06; precedent = 11.2 D-08 spec_id placement). Parse discipline for any code comment/doc: cite 11.2 D-08, not 11.3 D-08.

    In the SAME change, measure each new spec's ship-time CRITICAL/HIGH finding set with the golden test's own fresh-tempdir `_ship_findings` helper and add one `_GOLDEN_SHIP_FINDINGS` entry per new spec (RESEARCH Pitfall 2 — the whole-examples recursive glob hard-fails the moment a new spec lands without its golden entry). Measure the set; never guess it.
  </action>
  <acceptance_criteria>
    - source: every new miss/promotion case has an examples/known-bad/<slug>-ATTRIBUTION.yaml with `absent_code` and `promotes_backlog_item` present; pure-catch cases have none.
    - source: no new spec's validity_frame or inference block contains an `absent_code` or `promotes_backlog_item` key (sidecar placement, D-06).
    - behavior: `python -m unittest tests.test_causal_verb_golden -v` passes with the new `_GOLDEN_SHIP_FINDINGS` entries (test_golden_keys_match_the_examples_tree_on_disk green).
    - source: each sidecar's `promotes_backlog_item` names one of the nine real §6.5 rows.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_causal_verb_golden -v</automated>
  </verify>
  <done>Each miss/promotion case has a schema-correct sidecar living outside the spec, and every new spec has a measured golden-file entry so the whole-examples golden test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture author → harness | Hand-authored sidecar YAML and postmortem prose become machine-countable §6.5 evidence |
| fixture set → §6.5 count | Corpus composition drives which backlog items are promotable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-02 | Tampering | new corpus cases / coverage predicate | high | mitigate | Coverage predicate asserts class-presence not count; no hardcoded slugs; D-02 source-before-count enforced by review that each case cites a real primary source (test_corpus_includes_full_coverage_classes, tests/test_known_bad_corpus.py) |
| T-12-07 | Tampering | `<slug>-ATTRIBUTION.yaml` naming a hallucinated/misspelled code | high | mitigate | absent_code validated against the closed catalogue∪backlog union by the sibling-integrity test authored in plan 12-03 (tests/test_known_bad_corpus.py); this plan authors only schema-shaped sidecars |
| T-12-01 | Tampering | golden-file entries | medium | mitigate | Every `_GOLDEN_SHIP_FINDINGS` entry measured live via the golden test's fresh-tempdir `_ship_findings`, never lifted (tests/test_causal_verb_golden.py:82-142) |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new third-party packages this phase (D-01 hermeticity, 12-RESEARCH Package Legitimacy Audit); no install surface |
</threat_model>

<verification>
- `python -m unittest tests.test_known_bad_corpus -v` green (coverage predicate + floor + pairing).
- `python -m unittest tests.test_causal_verb_golden -v` green (golden entries in lockstep).
- Manual: each new spec's frame_digest is independent of its sidecar (sidecar is not in the spec).
</verification>

<success_criteria>
Three coverage classes present by class-presence predicate; each miss/promotion case carries a schema-correct sidecar authored in the same change; golden test green; zero codes minted.
</success_criteria>

<output>
Create `.planning/phases/12-calibration/12-01-SUMMARY.md` when done.
</output>
