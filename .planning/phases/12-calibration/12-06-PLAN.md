---
phase: 12-calibration
plan: 06
type: tdd
wave: 4
depends_on: [12-05]
files_modified:
  - tests/test_known_bad_corpus.py
  - tests/test_finding_catalogue_invariant.py
autonomous: true
requirements: [REQ-P12-02]
must_haves:
  truths:
    - "The harness reports a per-family friction column = live(ship-blocking findings) − live(own-target codes), expressed as a rate over non-target in-profile (fixture × gate-point) cells, reported RAW and NET (D-11)."
    - "Friction is guarded three ways: synthetic arithmetic proof (net = raw − own on a fabricated finding dict, filesystem-independent), live-source proof (same _gate_findings set as the golden test), and incidental→own relabel closure (D-11 a/b/c)."
    - "Every _TARGET_DEFECT_CODES entry is positively verified firing CRITICAL live AND named in that fixture's postmortem/attribution, so a code cannot be demoted out of friction without publicly declaring it the intended defect (D-11c)."
    - "The finding-code catalogue count remains exactly 256 after Phase 12, and nothing is added to GATE_PROFILES (D-18)."
  artifacts:
    - "tests/test_known_bad_corpus.py::TestFrictionArithmetic (synthetic arithmetic proof, filesystem-independent)"
    - "tests/test_known_bad_corpus.py::TestKnownBadCorpus.test_friction_uses_the_same_live_findings_as_golden (live-source proof)"
    - "tests/test_known_bad_corpus.py::TestKnownBadCorpus.test_target_defect_codes_fire_and_are_named (incidental→own relabel closure)"
    - "tests/test_finding_catalogue_invariant.py::TestCatalogueInvariant.test_finding_catalogue_stays_at_256_codes (D-18)"
  key_links:
    - "The friction function is pure arithmetic (raw − own) tested in isolation, then wired to the live _gate_findings set the golden test already consumes."
    - "The relabel-closure guard cross-references _TARGET_DEFECT_CODES against live CRITICAL firing and against each fixture's postmortem/attribution text."
  prohibitions:
    - statement: "Friction is never reported net-only; RAW and NET are both reported so a fixture over-blocking on unrelated codes cannot look clean by attributing two of them to itself (D-11)."
      status: must-hold
      verification: "TestFrictionArithmetic asserts both raw and net are surfaced; net = raw − own on a fabricated finding dict."
    - statement: "A code is never demoted incidental→own to shrink friction without being positively verified firing CRITICAL live and named in the fixture's postmortem/attribution (D-11c)."
      status: must-hold
      verification: "test_target_defect_codes_fire_and_are_named iterates every _TARGET_DEFECT_CODES entry, asserts it fires CRITICAL live and is named in that slug's postmortem or attribution."
    - statement: "No DSX-* finding code is minted; the catalogue count stays exactly 256 and GATE_PROFILES is unchanged (D-18)."
      status: must-hold
      verification: "test_finding_catalogue_stays_at_256_codes asserts the catalogue Total is 256; git diff shows GATE_PROFILES (dsx/cli.py:115-131) unchanged."
    - statement: "The friction number is never lifted from a stale ledger; it consumes the same live _gate_findings set as the golden test (D-09/D-11b)."
      status: must-hold
      verification: "test_friction_uses_the_same_live_findings_as_golden proves a hardcoded/lifted number breaks the equality with the live set."
---

<objective>
Add the per-family friction column (over-blocking beyond a fixture's own target) as a live-computed rate reported RAW and NET, guarded three ways, and lock the finding-code catalogue at 256. Friction is the honesty check on the catch rate: a gate that catches everything by blocking everything has high friction, and net-only reporting would hide it, so both numbers ship and the relabel path is closed.

Purpose: Complete REQ-P12-02's friction requirement and pin D-18's zero-mint invariant.
Output: The friction arithmetic proof, the live-source proof, the relabel-closure guard, and the catalogue-invariant test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-calibration/12-CONTEXT.md
@.planning/phases/12-calibration/12-RESEARCH.md
@.planning/phases/12-calibration/12-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- `tests/test_known_bad_corpus.py::TestFrictionArithmetic` — filesystem-independent synthetic proof that net = raw − own.
- `tests/test_known_bad_corpus.py::TestKnownBadCorpus.test_friction_uses_the_same_live_findings_as_golden` — live-source proof.
- `tests/test_known_bad_corpus.py::TestKnownBadCorpus.test_target_defect_codes_fire_and_are_named` — incidental→own relabel closure over every _TARGET_DEFECT_CODES entry.
- A pure friction helper computing raw/net over non-target in-profile cells.
- `tests/test_finding_catalogue_invariant.py::TestCatalogueInvariant.test_finding_catalogue_stays_at_256_codes` — D-18 catalogue invariant.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Friction column — synthetic arithmetic proof, live-source proof, relabel closure (RED→GREEN)</name>
  <files>tests/test_known_bad_corpus.py</files>
  <read_first>
    - tests/test_known_bad_corpus.py:172-306 — `_TARGET_DEFECT_CODES` (the own-target map) and `_classify_target_defect`.
    - tests/test_known_bad_corpus.py:555-616 — `_gate_findings` (the single live source both friction and the golden test must share).
    - tests/test_known_bad_corpus.py:64-88 — `_INCIDENTAL_GAP_CODES` allowlist-guard shape (the precedent for asserting a set names no slug's own target code).
    - .planning/phases/12-calibration/12-CONTEXT.md — D-11 (raw and net, per-family rate over non-target in-profile cells, three guards a/b/c).
    - .planning/phases/12-calibration/12-PATTERNS.md — the D-11 three-guard structure note.
  </read_first>
  <behavior>
    - Guard (a) synthetic arithmetic: over a fabricated findings dict (no filesystem), net = raw − own per family; both raw and net are returned.
    - Guard (b) live-source: friction consumes the same self._gate_findings(slug, "ship") set the golden test uses; a hardcoded/lifted number breaks the equality.
    - Guard (c) relabel closure: every _TARGET_DEFECT_CODES entry fires CRITICAL live AND is named in that slug's postmortem or attribution text, so a code cannot be quietly moved from friction into the own-target map.
    - Friction is expressed as a per-family rate over non-target in-profile (fixture × gate-point) cells, not a bare count.
  </behavior>
  <action>
    Write a pure friction helper: given a family's ship-blocking findings and that fixture's own-target codes, return raw = count of ship-blocking findings and net = raw − (ship-blocking findings that are own-target), as a per-family rate over non-target in-profile (fixture × gate-point) cells. Add TestFrictionArithmetic (guard a): a filesystem-independent unit test over a fabricated finding dict asserting net = raw − own and that BOTH raw and net are surfaced (net-only is forbidden). Add test_friction_uses_the_same_live_findings_as_golden (guard b): compute friction from self._gate_findings and assert it equals the value derived from the same live set the golden test consumes — proving a lifted/hardcoded number would break. Add test_target_defect_codes_fire_and_are_named (guard c): for every entry in _TARGET_DEFECT_CODES, assert the code fires CRITICAL in a live _gate_findings run for that slug/point AND appears in the slug's POSTMORTEM.md or ATTRIBUTION.yaml — closing the incidental→own relabel path.
  </action>
  <acceptance_criteria>
    - source: the friction helper returns both raw and net and expresses friction as a rate over non-target in-profile cells.
    - behavior: `python -m unittest tests.test_known_bad_corpus.TestFrictionArithmetic -v` passes (filesystem-independent, net = raw − own).
    - behavior: `python -m unittest tests.test_known_bad_corpus.TestKnownBadCorpus.test_friction_uses_the_same_live_findings_as_golden tests.test_known_bad_corpus.TestKnownBadCorpus.test_target_defect_codes_fire_and_are_named -v` passes.
    - source: guard (c) iterates every _TARGET_DEFECT_CODES entry and checks both live CRITICAL firing and postmortem/attribution naming.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_known_bad_corpus.TestFrictionArithmetic tests.test_known_bad_corpus.TestKnownBadCorpus.test_friction_uses_the_same_live_findings_as_golden tests.test_known_bad_corpus.TestKnownBadCorpus.test_target_defect_codes_fire_and_are_named -v</automated>
  </verify>
  <done>Friction reports raw and net as a live rate, proven arithmetically and against the live set, with the relabel path closed for every own-target code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Catalogue-invariant test — the code count stays 256 (RED→GREEN)</name>
  <files>tests/test_finding_catalogue_invariant.py</files>
  <read_first>
    - references/finding-codes.md:16 — the `**Total: 256 codes.**` line.
    - scripts/gen-finding-catalogue.py:423-425 — the `--check` / `--write` argparse surface and the row-collection function.
    - tests/test_known_bad_corpus.py:627-632 — `test_corpus_holds_at_least_three_pairs`, the "count something, assert a fixed bound" shape to copy.
    - .planning/phases/12-calibration/12-CONTEXT.md — D-18 (zero codes minted, catalogue stays 256, nothing in GATE_PROFILES).
  </read_first>
  <behavior>
    - test_finding_catalogue_stays_at_256_codes reads the catalogue Total (or enumerates rows via gen-finding-catalogue's collection function) and asserts the count is exactly 256.
    - The test tolerates CRLF: it normalizes with whitespace-collapse, never a line-anchored regex (this checkout is CRLF).
  </behavior>
  <action>
    Create tests/test_finding_catalogue_invariant.py with TestCatalogueInvariant.test_finding_catalogue_stays_at_256_codes. Read references/finding-codes.md, normalize with whitespace-collapse (never a line-anchored `\n` regex — use `\r?\n`-tolerant handling per repo CRLF discipline), and assert the catalogue Total is exactly 256; OR invoke scripts/gen-finding-catalogue.py's row-collection function directly and assert `len(rows) == 256`. Model the "count and assert a fixed value" shape on test_corpus_holds_at_least_three_pairs. This test is the D-18 pin: Phase 12 mints no code, so the count must not move.
  </action>
  <acceptance_criteria>
    - source: tests/test_finding_catalogue_invariant.py exists and asserts the catalogue count is exactly 256; it uses CRLF-tolerant normalization (no bare `\n` line-anchored regex).
    - behavior: `python -m unittest tests.test_finding_catalogue_invariant -v` passes.
    - behavior: `python scripts/gen-finding-catalogue.py --check` passes (catalogue current).
    - source: git diff shows no change to dsx/cli.py CHECKS (:64-85) or GATE_PROFILES (:115-131).
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_finding_catalogue_invariant -v && python scripts/gen-finding-catalogue.py --check</automated>
  </verify>
  <done>The catalogue-invariant test asserts exactly 256 codes, passes, and the generator --check confirms the catalogue is current and unminted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| own-target map ↔ friction | Relabelling a code incidental→own shrinks reported over-blocking without fixing it |
| catalogue count → D-18 invariant | A silently minted code would break the phase's zero-mint contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04 | Tampering | incidental→own relabel to shrink friction | high | mitigate | test_target_defect_codes_fire_and_are_named: every _TARGET_DEFECT_CODES entry must fire CRITICAL live AND be named in the fixture's postmortem/attribution (tests/test_known_bad_corpus.py:172-306) |
| T-12-01 | Tampering | net-only laundering / lifted friction number | high | mitigate | RAW and NET both reported (TestFrictionArithmetic); live-source proof ties friction to the golden test's _gate_findings set (:555-616) |
| T-12-12 | Tampering | silent code mint breaking D-18 | high | mitigate | test_finding_catalogue_stays_at_256_codes + gen-finding-catalogue --check (references/finding-codes.md:16); GATE_PROFILES unchanged |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages (D-01); no install surface |
</threat_model>

<verification>
- `python -m unittest tests.test_known_bad_corpus -v` green (all three friction guards).
- `python -m unittest tests.test_finding_catalogue_invariant -v` green.
- `python scripts/gen-finding-catalogue.py --check` passes.
</verification>

<success_criteria>
Friction ships RAW and NET as a live rate with all three D-11 guards, the incidental→own relabel path is closed, and the catalogue is pinned at 256 with zero codes minted.
</success_criteria>

<output>
Create `.planning/phases/12-calibration/12-06-SUMMARY.md` when done.
</output>
