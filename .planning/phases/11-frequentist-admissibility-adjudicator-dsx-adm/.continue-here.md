---
context: phase
phase: 11-frequentist-admissibility-adjudicator-dsx-adm
task: null
total_tasks: null
status: in_progress
paused_during: planning
last_updated: 2026-08-20T15:21:24.870Z
---

# Resume here — Phase 11 planning is half-finished

**Nothing in `dsx/` has been touched.** This phase is still being planned. The pause happened
because the Anthropic session limit was reached partway through the planner's run, not because
anything went wrong with the work.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Replanning from scratch | `/gsd-plan-phase 11` offers "Replan from scratch" at its existing-plans prompt. Taking it would discard three complete, well-grounded plans and repeat roughly an hour of agent work. | advisory | At the prompt, choose **"Add more plans"**. The three existing plans are valid and were written against the same CONTEXT.md and RESEARCH.md the new ones will use. |
| Leaving the new estimand field blank on the known-bad fixtures | RESEARCH.md verified that `tests/test_known_bad_corpus.py` currently expects `post-hoc-procedure-switch` to clear `dsx gate plan`. If the new `validity_frame.estimand.type` field is added but left blank there, `DSX-ADM-020` fires spuriously at `plan` and that test breaks. | advisory | Any plan that introduces the field must populate it on **all nine** committed example specs, not only the good fixture. `11-02-PLAN.md` already lists all nine in `files_modified` — check it covers them before adding more. |

<current_state>
`/gsd-discuss-phase 11` ran to completion and auto-advanced into `/gsd-plan-phase 11 --auto`
(the project has `auto_advance: true`). Research, validation strategy and pattern mapping all
finished and are on disk. The `gsd-planner` agent then wrote three plans and was terminated by the
session limit before finishing the set.

**The plan set is incomplete.** Written plans cover only REQ-P11-01, REQ-P11-04, REQ-P11-05 and
REQ-P11-06. **REQ-P11-02 and REQ-P11-03 have no plan**, and no plan yet creates either headline
artifact — `references/families.yaml` or `dsx/frame/admissibility.py`.

The `gsd-plan-checker` gate has **not** run. `plan_checker_enabled` is `true`, so it must run over
the completed set before execution.
</current_state>

<completed_work>

Committed:
- `0b21ae1` — `11-CONTEXT.md` (29 decisions) and `11-DISCUSSION-LOG.md`
- `c954158` — STATE.md session record
- `2ce095b` — `11-RESEARCH.md` (1033 lines) and `11-VALIDATION.md`
- `2271c73` — `11-PATTERNS.md`, the three partial plans, and this handoff (the pause commit)

On disk, structurally complete (frontmatter, `must_haves`, `<threat_model>`, prohibitions and
"Artifacts this phase produces" all present in each):
- `11-01-PLAN.md` — wave 1, 4 tasks — requirement and roadmap amendments (D-02), the
  `references/test-selection.md` Fisher correction (D-27), the two `brief.md` §7 locator
  corrections (D-29). Requirements: REQ-P11-01, REQ-P11-06.
- `11-02-PLAN.md` — wave 1, 3 tasks, `type: tdd` — the `ESTIMAND_TYPES` vocabulary and the new
  optional `validity_frame.estimand.type` field, populated across all nine committed specs.
  Requirements: REQ-P11-01, REQ-P11-04.
- `11-03-PLAN.md` — wave 1, 3 tasks, `type: tdd` — the reverse-direction import scanner (D-04a)
  and `applies_to_frequentist_admissibility(spec)` in `dsx/frame/paradigm.py` (D-22).
  Requirements: REQ-P11-04, REQ-P11-05.
</completed_work>

<remaining_work>

Planning:
1. Plans for **REQ-P11-02** (named tests resolve as aliases into families, exact-match only) and
   **REQ-P11-03** (ranked admissible set naming assumptions bought and charged).
2. A plan creating `references/families.yaml` — roughly 14 entries with citations and
   `locator_status` per D-09. RESEARCH.md holds a verified worked example that round-trips through
   both loader paths; use it rather than inventing a schema.
3. A plan creating `dsx/frame/admissibility.py` — the adjudicator, the ranking rule table (D-12
   through D-15), and the `no_admissible_procedure` refusal branch (D-16 through D-18).
4. A plan for the wiring: `cmd_recommend` composition (D-04), `run_checks` registration and
   `GATE_PROFILES` (D-20), and the new `DSX-ADM-010`/`-020` rows in `references/finding-codes.md`.
5. A plan for `scripts/gen-finding-catalogue.py` — the new YAML-citation function, the `sys.path`
   edit, and adding `"DSX-ADM-"` to `_D05_ALLOWLIST_PREFIXES` (D-23, D-24, D-25).
6. **Wave assignment.** All three existing plans declare `wave: 1` with `depends_on: []`. The new
   plans depend on the estimand vocabulary from `11-02` and the paradigm helper from `11-03`, so
   they belong in waves 2 and later.
7. Run `gsd-plan-checker` over the full set.

Then: `/gsd-execute-phase 11`.
</remaining_work>

<decisions_made>

Three went to the user as decision blocks during discuss; all three are recorded in
`11-CONTEXT.md` with their rationale and the rejected alternatives.

- **Ontology sized to committed evidence (~10–14 families), not the roadmap's 25–35.** The corpus
  supplies six distinct procedure labels, and Phase 12 — which grows the corpus — comes after this
  phase, so the count and the traceability criterion cannot both hold. The plan amends
  `REQUIREMENTS.md` and `ROADMAP.md` rather than under-delivering silently. (D-01, D-02)
- **Frequentist-only routing decided outside the adjudicator**, via a helper in
  `dsx/frame/paradigm.py` called from `run_checks`. The alternative would make an honest
  `paradigm: bayesian` declaration draw a CRITICAL block, making misdeclaration cheaper than
  honesty. (D-22)
- **The alias table has exactly one consumer.** `dsx/checks/stats.py` and `dsx/frame/prereg.py`
  are not rewired, because changing a shipped firing set can silently invalidate suppressions
  operators have already written. (D-03)

Made by the planner within the constraint CONTEXT.md set:

- **The estimand axis is a new optional `validity_frame.estimand.type` field** with an
  `ESTIMAND_TYPES` vocabulary in `dsx/spec.py`. RESEARCH.md ruled out the alternative by direct
  read: `examples/known-bad/weak-identification-mmm-ANALYSIS-SPEC.yaml` has no `analysis:` block
  at all, so keying on `analysis.outcome_type` would not reach it.
</decisions_made>

<blockers>
- **Session limit reached at ~15:20 Europe/Vienna, mid-planner-run.** External, not technical.
  Nothing is corrupted. Resume when it resets.
</blockers>

## Required Reading (in order)

1. `.planning/phases/11-frequentist-admissibility-adjudicator-dsx-adm/11-CONTEXT.md` — 29 locked
   decisions D-01…D-29 plus the Claude's Discretion list. **Settled; plan how to implement them,
   do not reopen them.** Also records eight places where the planning documents contradict the
   shipped code.
2. `.planning/phases/11-frequentist-admissibility-adjudicator-dsx-adm/11-RESEARCH.md` — the
   verified `families.yaml` schema, the estimand-axis finding, the exact `cmd_recommend` merge
   shape, and the list of which existing tests go red.
3. `.planning/phases/11-frequentist-admissibility-adjudicator-dsx-adm/11-PATTERNS.md` — analog
   files and code excerpts; `dsx/frame/prereg.py` is the strongest analog for the new module.
4. The three existing `11-0*-PLAN.md` files — so the new plans do not duplicate or contradict them.
5. `.planning/phases/11-frequentist-admissibility-adjudicator-dsx-adm/11-VALIDATION.md` — the
   requirement-to-test map and Wave 0 list the new plans must satisfy.
6. `.claude/CLAUDE.md` — Windows/PowerShell, CRLF (`\r?\n` in every line-anchored regex),
   verification before claiming, single-writer discipline on shared tracking files.

## Infrastructure State

- **Test baseline, measured 2026-08-20 before any Phase 11 work:**
  `python -m unittest discover -s tests` → **640 tests, OK, ~18 s**.
  `python -m unittest tests.test_frame_boundary` → 8 tests, OK, ~0.2 s.
  `python scripts/gen-finding-catalogue.py --check` → exit 0, "finding catalogue is current".
- Four **pre-existing** "declared twice with different text" warnings (`DSX-SPEC-070` twice,
  `DSX-VAL-021`, `DSX-VAL-060`) are unrelated to this phase. Do not plan work to fix them, and do
  not let an acceptance criterion assume warning-free output.
- Python 3.12.10. No background processes were started. No async jobs.

## Spec-less probe state (already computed — do not re-run)

This phase has no SPEC file, so the spec-less probe fallback fired. The edge probe surfaced
**11 applicable edges, all unresolved**, of which **3 are `unclassified`** (REQ-P11-01,
REQ-P11-02, REQ-P11-05) and must stay unresolved as flagged assumptions rather than being
auto-backstopped. The full report is in the planner prompt history; re-running
`node ~/.claude/gsd-core/bin/lib/edge-probe.cjs` against the six requirement texts reproduces it.
The three optional plan:pre detectors — API coverage, assumption-delta, schema-push — all returned
`detected: false`, and the dsx `<dsx_planner_contract>` does **not** apply (this repository builds
the dsx tool; the phase produces no number, model or chart, which is the capability's own skip
condition). Do not re-raise any of them.

<context>
The hard thinking is done and it is unusually well grounded: CONTEXT.md cites file and line for
almost every claim, RESEARCH.md verified the YAML schema by actually round-tripping it through
both parser paths in-session rather than reasoning about it, and the citation spine was researched
against primary sources with unverified locators honestly marked rather than guessed.

What is left is mechanical — turning the rest of that into plans. The three written plans are all
foundation work and correctly sit in wave 1 with no dependencies. The plans that build the
adjudicator itself are the missing ones, and they belong in later waves because they need the
estimand vocabulary from `11-02` and the paradigm helper from `11-03`.

Resume with `/gsd-plan-phase 11`, choose **"Add more plans"**, and point the planner at the
requirement gap (REQ-P11-02, REQ-P11-03) and the two unbuilt artifacts.
</context>
