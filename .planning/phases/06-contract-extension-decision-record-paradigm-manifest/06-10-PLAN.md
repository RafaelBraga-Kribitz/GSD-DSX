---
phase: 06-contract-extension-decision-record-paradigm-manifest
plan: 10
type: execute
wave: 6
depends_on: ["06-09", "06-08", "06-04"]
files_modified:
  - dsx/__init__.py
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - capabilities/dsx/capability.json
  - examples/good-ANALYSIS-SPEC.yaml
  - templates/ANALYSIS-SPEC.yaml
  - references/finding-codes.md
autonomous: true
requirements: [REQ-P6-12, REQ-P6-16]

must_haves:
  truths:
    - "`dsx --version` reports 2.0.0, and every committed version declaration in the package, the plugin manifests and the capability manifest agrees with it"
    - "`python3 scripts/gen-finding-catalogue.py --check` exits 0, covering both catalogue staleness and the D-05 citation, reference-value and test-linkage enforcement over every new code"
    - "`references/finding-codes.md` contains all five new `DSX-SPEC-08x` codes and `DSX-PAR-001`, each under a rendered prefix group"
    - "`dsx gate` exits 0 on `examples/good-ANALYSIS-SPEC.yaml` at plan, execute, verify and ship, and 1 on `examples/bad-ANALYSIS-SPEC.yaml` at plan and at ship"
    - "The three D-08 fixture tests and the `_run` harness in `tests/test_dsx.py` are byte-for-byte identical to their pre-phase content"
    - "No spec committed in `examples/` or `templates/` declares a `repro_lock.dsx_version` that disagrees with the package version"
  artifacts:
    - path: "dsx/__init__.py"
      provides: "The package version of record"
      contains: "2.0.0"
    - path: "references/finding-codes.md"
      provides: "The regenerated catalogue for the v2.0.0 code set"
  key_links:
    - from: "dsx/__init__.py::__version__"
      to: "dsx/checks/repro.py"
      via: "DSX-REP-053 compares repro_lock.dsx_version against the package version; the shipped specs must agree"
      pattern: "dsx_version"
    - from: "scripts/gen-finding-catalogue.py --check"
      to: "references/finding-codes.md"
      via: "the committed catalogue must be byte-identical to the rendered one"
      pattern: "finding-codes"
  prohibitions: []
---

<objective>
Close the phase: bump every version declaration to 2.0.0, regenerate the finding catalogue, and run
the full phase gate that proves every ROADMAP success criterion holds together rather than
plan-by-plan (REQ-P6-16, REQ-P6-12).

Purpose: v2.0.0 rather than v1.6.0 because `validity_frame:` is now required from `plan` at
CRITICAL, so a spec written against v1.x begins blocking — a breaking contract change. The version
number is the signal a user reads before upgrading, and the migration path plan 06-04 documented is
what makes the break survivable.

Output: consistent versions, a current catalogue, and the six gate invocations run as one closing
check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-CONTEXT.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-RESEARCH.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-04-SUMMARY.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-08-SUMMARY.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-09-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Every symbol below is CREATED by Phase 6. Drift/source-grounding passes must treat these as new,
not as pre-existing references.

**New modules and packages**
- `dsx/decisions.py` — top-level peer to `dsx/findings.py`. NOT `dsx/checks/decision.py`, which is
  the pre-existing `DSX-DEC-*` replay module and is untouched this phase.
- `dsx/frame/` package: `dsx/frame/__init__.py`, `dsx/frame/paradigm.py`

**New classes / functions / module constants**
- `dsx.decisions.DecisionRecord`, `dsx.decisions.InvocationHeader`
- `dsx.decisions.append()`, `read_all()`, `next_invocation_id()`, `frame_digest()`,
  `decisions_path()`, `collect_from_report()`
- `dsx.decisions.DECISION_LAYERS`, `dsx.decisions.RECORD_TYPES`
- `dsx.frame.paradigm.check()`, `_PARADIGM_INDEPENDENT`, `_PARADIGM_CONDITIONAL`, `_NOT_SHIPPED`
- `dsx.spec._validate_validity_frame_shape()`, `dsx.spec._validate_inference_shape()`
- `dsx.spec._VOCABULARIES`, `_VALIDITY_FRAME_ALWAYS_REQUIRED`, `_VALIDITY_FRAME_CAUSAL_REQUIRED`,
  `_INFERENCE_FIELDS`
- `dsx.cli.cmd_explain()`, `dsx.cli._render_decision_trail()`
- `scripts/gen-finding-catalogue.py`: `check_d05()`, `_resolve_docstrings()`,
  `_collect_test_markers()`, `_D05_ALLOWLIST_PREFIXES`, `_CITATION_RE`, `_REFVALUE_RE`,
  `_TEST_MARKER_RE`
- `tests/test_frame_boundary.py`: `_scan_source_for_checks_imports()`, `_package_for()`

**New CLI surface**
- subcommand `dsx explain` with `--spec`, `--phase-dir`, `--invocation`, `--json`, `--verbose`
  (deliberately NO `--block-on`)
- `dsx.cli.add_common()` gains `include_block_on: bool = True`
- `CHECKS["paradigm"]`; `"paradigm"` added to all four `GATE_PROFILES` tuples

**New finding codes**
- `DSX-SPEC-080` validity_frame block absent (CRITICAL)
- `DSX-SPEC-081` required validity_frame sub-block missing (CRITICAL)
- `DSX-SPEC-082` validity_frame sub-field outside its closed vocabulary (HIGH)
- `DSX-SPEC-085` inference sub-field outside its closed vocabulary (HIGH)
- `DSX-SPEC-086` a removed-by-M-02 inference field is declared (HIGH)
- `DSX-PAR-001` paradigm manifest (INFO)
- `DSX-PAR-999` — test-fixture-only code, lives under `tests/fixtures/d05/`, never under `dsx/`

**New vocabulary constants in `dsx/spec.py`**
- `IDENTIFICATION_STRENGTHS`, `CONSTRAINT_SOURCES`, `DEPENDENCE_STRUCTURES`,
  `INTERFERENCE_RISKS`, `INTERFERENCE_MITIGATIONS`, `MISSINGNESS_MECHANISMS`,
  `ANALYSIS_POPULATIONS`, `DECLARATION_POINTS`, `PARADIGMS`, `PARADIGM_JUSTIFICATIONS`
- `PEEKING_POLICIES` gains the member `uncontrolled_continuous` (existing dict, one new entry)
- `VARIANCE_ADJUSTMENTS` is REUSED unchanged for `dependence.method_family_required` (M-09)

**New spec contract surface (`ANALYSIS-SPEC.yaml`)**
- `validity_frame:` with `estimand`, `units`, `identification`, `dependence`, `interference`,
  `triggering`, `stability`, `sampling_frame`, `missingness`, `measurement`
- `inference:` with `paradigm`, `paradigm_justification`, `declared_at`, `primary_procedure`,
  `alpha_spending`, `fallback_rule` (M-02 removes the stopping-rule field; the concept is read
  from the existing `design.peeking_policy`)

**New files**
- `tests/test_frame_boundary.py`, `tests/test_decisions.py`,
  `tests/test_gen_finding_catalogue.py`, `tests/test_known_bad_corpus.py`
- `tests/fixtures/d05/bad_check.py`
- `examples/known-bad/interference-shared-budget-ANALYSIS-SPEC.yaml` + `-POSTMORTEM.md`
- `examples/known-bad/frequentist-uncontrolled-continuous-ANALYSIS-SPEC.yaml` + `-POSTMORTEM.md`
- `examples/known-bad/bayesian-continuous-monitoring-ANALYSIS-SPEC.yaml` + `-POSTMORTEM.md`
- `.planning/REVERSALS.md`
- `DECISIONS.jsonl` — runtime artifact written beside the resolved spec; not committed
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Bump every version declaration to 2.0.0 (REQ-P6-16)</name>
  <files>dsx/__init__.py, .claude-plugin/plugin.json, .claude-plugin/marketplace.json, capabilities/dsx/capability.json, examples/good-ANALYSIS-SPEC.yaml, templates/ANALYSIS-SPEC.yaml</files>
  <read_first>
    - `dsx/__init__.py` — `__version__` at line 9, the single source `dsx --version` reads
    - `.claude-plugin/plugin.json` line 4 and `.claude-plugin/marketplace.json` line 9 — the two
      plugin manifests
    - `capabilities/dsx/capability.json` line 4 — the capability manifest
    - `dsx/checks/repro.py` lines 256-280 — `DSX-REP-053`, which compares
      `reproducibility.repro_lock.dsx_version` against the package version at MEDIUM severity
    - `examples/good-ANALYSIS-SPEC.yaml` around line 211 and `templates/ANALYSIS-SPEC.yaml` around
      line 203 — the two committed `dsx_version` declarations, currently naming older versions
    - `scripts/validate-capability.py` — the existing capability-manifest validator
  </read_first>
  <action>
    Set the version to `2.0.0` in all four declaration sites: `dsx/__init__.py`'s `__version__`,
    `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` and
    `capabilities/dsx/capability.json`. Use scoped edits to the version field only; do not rewrite
    any manifest wholesale.

    Update the two committed `repro_lock.dsx_version` declarations to `2.0.0` as well —
    `examples/good-ANALYSIS-SPEC.yaml` currently names 1.5.0 and `templates/ANALYSIS-SPEC.yaml`
    names 1.4.0. `DSX-REP-053` fires at MEDIUM on a mismatch, which is below the verify and ship
    threshold and therefore does not block the good fixture, but shipping a canonical fixture that
    emits a finding about the very release it ships in is the kind of drift that gets normalised.
    The template's declaration was already stale before this phase; fix it here rather than
    carrying it further.

    Run `python3 scripts/validate-capability.py` and confirm it exits 0, so a manifest edit that
    breaks the capability contract surfaces now rather than at install time.

    Do not touch `spec_version` anywhere. That is the ANALYSIS-SPEC contract version enforced by
    `DSX-SPEC-002`, it is independent of the package version, and no REQ-P6-* changes it — the new
    blocks are additive to `spec_version: 1`.
  </action>
  <acceptance_criteria>
    - `python3 -m dsx --version` prints `dsx 2.0.0`
    - `python3 -c "import json,pathlib,dsx; v=dsx.__version__; assert v=='2.0.0', v; assert json.loads(pathlib.Path('.claude-plugin/plugin.json').read_text(encoding='utf-8'))['version']==v; assert json.loads(pathlib.Path('capabilities/dsx/capability.json').read_text(encoding='utf-8'))['version']==v; m=json.loads(pathlib.Path('.claude-plugin/marketplace.json').read_text(encoding='utf-8')); assert v in json.dumps(m)"` exits 0
    - `python3 -c "import pathlib,dsx; from dsx.loader import load
for p in ('examples/good-ANALYSIS-SPEC.yaml','templates/ANALYSIS-SPEC.yaml'):
    lock=(load(p).get('reproducibility') or {}).get('repro_lock') or {}
    assert str(lock.get('dsx_version'))==dsx.__version__, (p, lock.get('dsx_version'))"` exits 0
    - `python3 -c "from dsx.loader import load; from dsx.spec import SPEC_VERSION; assert load('examples/good-ANALYSIS-SPEC.yaml')['spec_version']==SPEC_VERSION"` exits 0
    - `python3 scripts/validate-capability.py` exits 0
    - `python3 -m unittest discover -s tests` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s tests && python3 scripts/validate-capability.py && python3 -m dsx --version</automated>
  </verify>
  <done>All four version declarations and both committed `repro_lock.dsx_version` values read 2.0.0, the capability manifest validates, and `spec_version` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the catalogue and run the closing phase gate (REQ-P6-16, REQ-P6-12)</name>
  <files>references/finding-codes.md</files>
  <read_first>
    - `scripts/gen-finding-catalogue.py` as extended by plan 06-03 — `--write`, `--check`, and the
      D-05 enforcement that `--check` now also runs
    - `references/finding-codes.md` — the committed catalogue, its total-codes line and its prefix
      group headings
    - `.planning/ROADMAP.md` lines 112-139 — the five Phase 6 success criteria this task verifies
      as a set
    - `.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-VALIDATION.md`
      — the phase-gate command list under "Sampling Rate"
  </read_first>
  <action>
    Run `python3 scripts/gen-finding-catalogue.py --write` and commit the regenerated
    `references/finding-codes.md`. Confirm it renders all five new `DSX-SPEC-08x` codes under the
    existing contract-structure group and `DSX-PAR-001` under the paradigm group plan 06-03 added to
    `PREFIX_GROUPS`, and that the total-codes line matches the number of rows actually rendered — a
    code whose prefix has no group is counted in the total but silently omitted from every table,
    which is the failure the group addition and its test exist to prevent.

    Then run the closing phase gate as one block and record every result in the plan summary. This
    is not a repetition of the per-task checks: each earlier plan verified its own slice, and this
    is the first point at which every slice is present at once.
      - `python3 -m unittest discover -s tests` exits 0, with a test count strictly greater than the
        160 the suite carried before this phase
      - `python3 scripts/gen-finding-catalogue.py --check` exits 0, covering both catalogue
        staleness and the D-05 enforcement across every allow-listed code
      - `python3 -m dsx gate {plan,execute,verify,ship} --spec examples/good-ANALYSIS-SPEC.yaml`
        each exit 0
      - `python3 -m dsx gate plan --spec examples/bad-ANALYSIS-SPEC.yaml` exits 1
      - `python3 -m dsx gate ship --spec examples/bad-ANALYSIS-SPEC.yaml` exits 1
      - `python3 -m dsx validate --spec examples/known-bad/<each of the three>-ANALYSIS-SPEC.yaml`
        each exit 0
      - `python3 -m dsx explain --spec /nonexistent/spec.yaml` exits 0
      - `python3 -m dsx vocab` exits 0 and its JSON carries every new vocabulary
      - `git diff -U0 tests/test_dsx.py` shows no deletion or modification inside the original
        804-839 range, which is REQ-P6-12's "unedited" half restated at phase close
      - `git status --porcelain` reports no untracked `DECISIONS.jsonl` anywhere

    Record in the plan summary which ROADMAP success criteria are covered by an automated check and
    which rest on the human-check reviews from plans 06-04 and 06-08, so `/gsd-verify-work` inherits
    an honest picture rather than a uniform green.
  </action>
  <acceptance_criteria>
    - `python3 scripts/gen-finding-catalogue.py --check` exits 0
    - `python3 -c "import pathlib,re; t=pathlib.Path('references/finding-codes.md').read_text(encoding='utf-8'); need=['DSX-SPEC-080','DSX-SPEC-081','DSX-SPEC-082','DSX-SPEC-085','DSX-SPEC-086','DSX-PAR-001']; missing=[c for c in need if c not in t]; assert not missing, missing; total=int(re.search(r'\*\*Total: (\d+) codes', t).group(1)); rendered=len(re.findall(r'^\| `DSX-', t, re.M)); assert total==rendered, (total, rendered)"` exits 0
    - `python3 -m unittest discover -s tests` exits 0 and reports strictly more than 160 tests
    - `python3 -m dsx gate plan --spec examples/good-ANALYSIS-SPEC.yaml` exits 0, and the same for
      `execute`, `verify` and `ship`
    - `python3 -m dsx gate plan --spec examples/bad-ANALYSIS-SPEC.yaml` exits 1, and the same for `ship`
    - `python3 -c "import pathlib,io,contextlib,dsx.cli as c; ok=True
for p in sorted(pathlib.Path('examples/known-bad').glob('*-ANALYSIS-SPEC.yaml')):
    with contextlib.redirect_stdout(io.StringIO()), contextlib.redirect_stderr(io.StringIO()): rc=c.main(['validate','--spec',str(p)])
    assert rc==0, (p, rc)"` exits 0
    - `python3 -m dsx explain --spec /nonexistent/spec.yaml` exits 0
    - `python3 -c "import json,subprocess,sys; out=subprocess.run([sys.executable,'-m','dsx','vocab'],capture_output=True,text=True); assert out.returncode==0; v=json.loads(out.stdout); need=['identification_strengths','constraint_sources','dependence_structures','interference_risks','interference_mitigations','missingness_mechanisms','analysis_populations','declaration_points','paradigms','paradigm_justifications','peeking_policies']; missing=[k for k in need if k not in v]; assert not missing, missing; assert isinstance(v['peeking_policies'], dict)"` exits 0
    - `git diff -U0 tests/test_dsx.py` shows no deletion or modification inside the original 804-839 range
    - `git status --porcelain | grep DECISIONS.jsonl` returns no output
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s tests && python3 scripts/gen-finding-catalogue.py --check && for p in plan execute verify ship; do python3 -m dsx gate $p --spec examples/good-ANALYSIS-SPEC.yaml || exit 1; done && python3 -m dsx explain --spec /nonexistent/spec.yaml && python3 -m dsx vocab > /dev/null</automated>
  </verify>
  <done>The catalogue is current and passes both halves of `--check`, its rendered row count matches its own total, and every gate invocation in the closing phase gate behaves as the ROADMAP success criteria require.</done>
</task>

</tasks>

<flagged_assumptions>
Spec-less edge-probe rows that remain `unresolved` and are surfaced here rather than silently
dropped or auto-backstopped:

- **REQ-P6-16 (unclassified, unresolved).** The probe surfaced no boundary/precision/adjacency/
  empty/ordering shape for "package version is 2.0.0 and the finding catalogue is regenerated".
  Residual risk: `2.0.0` asserts a breaking contract change, and nothing mechanical verifies that
  the break is actually the one the version claims. The catalogue check proves the code list is
  current and the gate invocations prove the fixtures behave; neither proves that a real pre-v2.0.0
  spec in a user's repository degrades the way the README says it will. The `suppressions[]`
  migration path documented in plan 06-04 is the mitigation, and the first real upgrade is the
  first real test of it.
</flagged_assumptions>

<edge_coverage_accounting>
Phase-wide spec-less edge probe: **27 applicable rows, 0 resolved on arrival.**
Resolution across the whole plan set: **17 authored into `must_haves`** (15 `covered` plain-string
truths + 2 `backstop` structured markers) **+ 10 surfaced as flagged assumptions** = **27**.
No row was dismissed and no `unclassified` row was auto-backstopped.

This plan carries **0** of the 17 authored rows and **1** of the 10 flagged assumptions (REQ-P6-16).

Phase-wide distribution, for the checker's arithmetic:

| Plan | Authored truths | Flagged assumptions |
|------|-----------------|---------------------|
| 06-01 | 3 (REQ-P6-06) | 2 (REQ-P6-01, REQ-P6-05) |
| 06-02 | 0 | 1 (REQ-P6-07) |
| 06-03 | 0 | 1 (REQ-P6-11) |
| 06-04 | 2 backstop (REQ-P6-15) | 1 (REQ-P6-14) |
| 06-05 | 7 (REQ-P6-02, REQ-P6-12) | 0 |
| 06-06 | 0 | 2 (REQ-P6-03, REQ-P6-04) |
| 06-07 | 2 (REQ-P6-09) | 1 (REQ-P6-10) |
| 06-08 | 3 (REQ-P6-13) | 0 |
| 06-09 | 0 | 1 (REQ-P6-08) |
| 06-10 | 0 | 1 (REQ-P6-16) |
| **Total** | **17** | **10** |

17 + 10 = 27. No silent drops.
</edge_coverage_accounting>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published version number -> a user deciding whether to upgrade | The version is the only signal a user reads before pulling a breaking contract change; a version that understates the break is a trust failure, not a cosmetic one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6-20 | Spoofing | version declarations across four manifests | medium | mitigate | Four independent declaration sites can drift, so an install could advertise one version while the package reports another. Mitigated by asserting all four against `dsx.__version__` in one acceptance criterion, by running `scripts/validate-capability.py`, and by pinning the two committed `repro_lock.dsx_version` values to the same source. |
| T-6-21 | Repudiation | the finding catalogue | medium | mitigate | A stale catalogue makes a suppression written against it invalid and hides a shipped code from every reader. Mitigated by `--check` in the closing gate, and by the rendered-rows-equals-declared-total assertion, which catches the specific failure of a code whose prefix has no `PREFIX_GROUPS` entry being counted but never rendered. |
| T-6-SC | Tampering | package-manager installs | low | accept | Zero third-party installs ship in this phase (D-01; `06-RESEARCH.md` Package Legitimacy Audit records the gate as not applicable). This plan edits version strings, two YAML values and one generated file. |
</threat_model>

<verification>
Closing phase gate — every command below must pass before `/gsd-verify-work`:

- `python3 -m unittest discover -s tests` exits 0 with strictly more than 160 tests
- `python3 scripts/gen-finding-catalogue.py --check` exits 0
- `python3 scripts/validate-capability.py` exits 0
- `python3 -m dsx --version` prints `dsx 2.0.0`
- `python3 -m dsx gate {plan,execute,verify,ship} --spec examples/good-ANALYSIS-SPEC.yaml` all exit 0
- `python3 -m dsx gate plan --spec examples/bad-ANALYSIS-SPEC.yaml` exits 1
- `python3 -m dsx gate ship --spec examples/bad-ANALYSIS-SPEC.yaml` exits 1
- `python3 -m dsx validate --spec examples/known-bad/<each>-ANALYSIS-SPEC.yaml` exits 0 for all three
- `python3 -m dsx explain --spec /nonexistent/spec.yaml` exits 0
- `python3 -m dsx vocab` exits 0 with every new vocabulary present
- `python3 -m unittest tests.test_frame_boundary -v` exits 0 with no skipped tests
- `git diff -U0 tests/test_dsx.py` shows no deletion or modification inside the original 804-839 range
- `git status --porcelain` reports no untracked `DECISIONS.jsonl`
- `git diff --stat dsx/checks/` reports no changes across the whole phase (D-13, M-01)
</verification>

<success_criteria>
- REQ-P6-16: the package version is 2.0.0 across every declaration site and the finding catalogue
  is regenerated and current
- REQ-P6-12: the good fixture still passes every gate at every threshold, the bad fixture is still
  blocked at plan and ship, and the three D-08 tests plus the `_run` harness remain unedited —
  re-verified at phase close with every plan's work present at once
- The closing summary records which ROADMAP success criteria are covered by an automated check and
  which rest on the human-check reviews from plans 06-04 and 06-08, so `/gsd-verify-work` inherits
  an honest picture rather than a uniform green
</success_criteria>

<output>
Create `.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-10-SUMMARY.md` when done
</output>