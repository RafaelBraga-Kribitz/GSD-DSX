---
phase: 06-contract-extension-decision-record-paradigm-manifest
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REVERSALS.md
  - README.md
  - .planning/PROJECT.md
autonomous: true
requirements: [REQ-P6-14, REQ-P6-15]

must_haves:
  truths:
    - statement: "README.md contains a section documenting `suppressions[]` as the pre-v2.0.0 migration path, naming the ADR/SPEC `authority` requirement, reachable from the README's own upgrade/migration heading"
      verification: backstop
    - statement: "README.md states the known limit as a claim about the tool — the gate checks declarations against declarations, and a frame that lies passes — and states the two tiers of D-05 evidentiary rigour, with the new v2.0.0 codes carrying a citation, a reference value or named structural criterion and a linked test, and the pre-existing codes allow-listed"
      verification: backstop
    - "`.planning/REVERSALS.md` exists and carries a reversal-record template with the decision reversed, the new evidence, what would have made the original correct, and what did not change"
    - "`.planning/REVERSALS.md` defines the `SELF-001` convention — a reversal filed with no new evidence is itself a finding — rather than merely naming it"
    - "`.planning/PROJECT.md`'s version rationale no longer states that the frame block becomes required at verify/ship; it states required from plan, matching the CRITICAL severity D-10 chose"
  artifacts:
    - path: ".planning/REVERSALS.md"
      provides: "The D-14 reversal-record template, the SELF-001 convention, and REV-001 transcribed from brief.md"
      contains: "SELF-001"
    - path: "README.md"
      provides: "suppressions[] migration path, the known limit, and the two tiers of D-05 rigour"
    - path: ".planning/PROJECT.md"
      provides: "Version rationale corrected to match the gate point D-10 selected"
  key_links:
    - from: "README.md migration section"
      to: "dsx/suppressions.py"
      via: "documents the already-shipped v1.5.0 mechanism; zero new code — M-07's grandfather path"
      pattern: "authority"
    - from: "README.md D-05 tiers section"
      to: "scripts/gen-finding-catalogue.py"
      via: "names --check as the mechanism enforcing the citation and test-linkage convention"
      pattern: "gen-finding-catalogue"
  prohibitions:
    - statement: "MUST NOT document `suppressions[]` as a way to make the new frame findings go away. It is a migration path with an authority requirement, and the documentation must say so plainly: a suppression without a resolvable ADR or SPEC reference is a silent bypass of the tool's central value, not a supported workflow. The README must not soften the authority requirement into a suggestion, and must not present suppression as an alternative to filling the frame."
      status: unverified
      verification: judgment
---

<objective>
Write the two documentation deliverables and the one planning-artifact correction this phase owes:
`.planning/REVERSALS.md` with the D-14 template and the `SELF-001` convention (REQ-P6-14, M-05),
README coverage of the `suppressions[]` migration path and the known limit (REQ-P6-15, M-07,
brief section 8), and the `PROJECT.md` version-rationale amendment D-10 requires.

Purpose: `validity_frame:` becomes CRITICAL from plan onward, which means every pre-v2.0.0 spec
starts blocking. The migration path exists (v1.5.0's `suppressions[]`, with its ADR/SPEC authority
requirement) but is undocumented as a migration path. And the tool's central honesty caveat — the
gate checks declarations against declarations, so a frame that lies passes — must be stated by the
tool itself rather than discovered by a user.

Output: three files, no code. This plan has zero dependencies and runs in parallel with the code
plans in wave 1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-CONTEXT.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Every symbol below is CREATED by Phase 6. Drift/source-grounding passes must treat these as new,
not as pre-existing references.

**New modules and packages**
- `dsx/decisions.py` — top-level peer to `dsx/findings.py`. NOT `dsx/checks/decision.py`, which is
  the pre-existing `DSX-DEC-*` replay module and is untouched this phase.
- `dsx/frame/` package: `dsx/frame/__init__.py`, `dsx/frame/paradigm.py`

**New classes / functions / module constants**
- `dsx.decisions.DecisionRecord`, `dsx.decisions.InvocationHeader`
- `dsx.decisions.append()`, `read_all()`, `next_invocation_id()`, `frame_digest()`,
  `decisions_path()`, `collect_from_report()`
- `dsx.decisions.DECISION_LAYERS`, `dsx.decisions.RECORD_TYPES`
- `dsx.frame.paradigm.check()`, `_PARADIGM_INDEPENDENT`, `_PARADIGM_CONDITIONAL`, `_NOT_SHIPPED`
- `dsx.spec._validate_validity_frame_shape()`, `dsx.spec._validate_inference_shape()`
- `dsx.spec._VOCABULARIES`, `_VALIDITY_FRAME_ALWAYS_REQUIRED`, `_VALIDITY_FRAME_CAUSAL_REQUIRED`,
  `_INFERENCE_FIELDS`
- `dsx.cli.cmd_explain()`, `dsx.cli._render_decision_trail()`
- `scripts/gen-finding-catalogue.py`: `check_d05()`, `_resolve_docstrings()`,
  `_collect_test_markers()`, `_D05_ALLOWLIST_PREFIXES`, `_CITATION_RE`, `_REFVALUE_RE`,
  `_TEST_MARKER_RE`
- `tests/test_frame_boundary.py`: `_scan_source_for_checks_imports()`, `_package_for()`

**New CLI surface**
- subcommand `dsx explain` with `--spec`, `--phase-dir`, `--invocation`, `--json`, `--verbose`
  (deliberately NO `--block-on`)
- `dsx.cli.add_common()` gains `include_block_on: bool = True`
- `CHECKS["paradigm"]`; `"paradigm"` added to all four `GATE_PROFILES` tuples

**New finding codes**
- `DSX-SPEC-080` validity_frame block absent (CRITICAL)
- `DSX-SPEC-081` required validity_frame sub-block missing (CRITICAL)
- `DSX-SPEC-082` validity_frame sub-field outside its closed vocabulary (HIGH)
- `DSX-SPEC-085` inference sub-field outside its closed vocabulary (HIGH)
- `DSX-SPEC-086` a removed-by-M-02 inference field is declared (HIGH)
- `DSX-PAR-001` paradigm manifest (INFO)
- `DSX-PAR-999` — test-fixture-only code, lives under `tests/fixtures/d05/`, never under `dsx/`

**New vocabulary constants in `dsx/spec.py`**
- `IDENTIFICATION_STRENGTHS`, `CONSTRAINT_SOURCES`, `DEPENDENCE_STRUCTURES`,
  `INTERFERENCE_RISKS`, `INTERFERENCE_MITIGATIONS`, `MISSINGNESS_MECHANISMS`,
  `ANALYSIS_POPULATIONS`, `DECLARATION_POINTS`, `PARADIGMS`, `PARADIGM_JUSTIFICATIONS`
- `PEEKING_POLICIES` gains the member `uncontrolled_continuous` (existing dict, one new entry)
- `VARIANCE_ADJUSTMENTS` is REUSED unchanged for `dependence.method_family_required` (M-09)

**New spec contract surface (`ANALYSIS-SPEC.yaml`)**
- `validity_frame:` with `estimand`, `units`, `identification`, `dependence`, `interference`,
  `triggering`, `stability`, `sampling_frame`, `missingness`, `measurement`
- `inference:` with `paradigm`, `paradigm_justification`, `declared_at`, `primary_procedure`,
  `alpha_spending`, `fallback_rule` (M-02 removes the stopping-rule field; the concept is read
  from the existing `design.peeking_policy`)

**New files**
- `tests/test_frame_boundary.py`, `tests/test_decisions.py`,
  `tests/test_gen_finding_catalogue.py`, `tests/test_known_bad_corpus.py`
- `tests/fixtures/d05/bad_check.py`
- `examples/known-bad/interference-shared-budget-ANALYSIS-SPEC.yaml` + `-POSTMORTEM.md`
- `examples/known-bad/frequentist-uncontrolled-continuous-ANALYSIS-SPEC.yaml` + `-POSTMORTEM.md`
- `examples/known-bad/bayesian-continuous-monitoring-ANALYSIS-SPEC.yaml` + `-POSTMORTEM.md`
- `.planning/REVERSALS.md`
- `DECISIONS.jsonl` — runtime artifact written beside the resolved spec; not committed
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create .planning/REVERSALS.md with the D-14 template and the SELF-001 convention (REQ-P6-14)</name>
  <files>.planning/REVERSALS.md</files>
  <read_first>
    - `brief.md` line 110 — D-14's full text, which defines the three required fields and states
      that a reversal with no new evidence is itself a finding logged as `SELF-001`
    - `brief.md` lines 382-392 — the worked `REV-001` record, the only existing instance of the
      form, showing the four headings actually used: Reversed / New evidence / What would have made
      the original correct / What did not change
    - `brief.md` lines 424-431 — section 6.6 item 3, which records that `SELF-001` has no
      enforcement mechanism yet
    - `.planning/PROJECT.md` lines 138-150 — the D-table and M-table entries a reversal record
      would reference by id
  </read_first>
  <action>
    Create `.planning/REVERSALS.md`. The file has three parts.

    First, a short preamble stating the scope: reversing any decision in `brief.md` section 4's
    D-table or `PROJECT.md`'s M-table requires a record here, and the record is written before or
    with the change that reverses it, never after.

    Second, the reusable template, presented as a fenced markdown block a future author copies. Its
    id scheme is `REV-NNN`, allocated sequentially and never reused. Required fields, taken
    verbatim from D-14 and the REV-001 instance: the record id and date; **Reversed** — which
    decision, named by its id and one-line restatement; **New evidence** — what became known that
    was not known when the original decision was made; **What would have made the original
    correct** — the condition under which the reversed decision would still stand, which is the
    field that makes the reversal falsifiable rather than a mood; **What did not change** — the
    scope boundary, so a reversal of one item is not read as a reversal of its neighbours.

    Third, the `SELF-001` convention, defined rather than merely named: `SELF-001` is the finding
    logged in this file when a reversal is filed whose **New evidence** field is empty or restates
    the original reasoning. It is a convention for v2.0.0, not a gate check (M-05) — `dsx` does not
    adjudicate planning documents, and `brief.md` section 6.6 item 3 records that it has no
    enforcement mechanism. State that plainly, including what filing one looks like: a `SELF-001`
    line inside the offending `REV-NNN` record naming which field was empty.

    Then transcribe `brief.md`'s existing REV-001 (the reversal of the blanket deferral of the
    prior family under D-12a) as the first real entry, so the file ships with a worked example
    rather than an empty template.
  </action>
  <acceptance_criteria>
    - `.planning/REVERSALS.md` exists and is non-empty
    - The file contains the four D-14 field headings: `Reversed`, `New evidence`, `What would have
      made the original correct`, `What did not change`
    - The file contains `SELF-001` together with a definition sentence stating that a reversal with
      no new evidence is itself a finding — not merely a mention of the identifier
    - The file contains an entry identified as `REV-001` naming the D-12a prior-family deferral
    - `python3 -c "import pathlib,sys; t=pathlib.Path('.planning/REVERSALS.md').read_text(encoding='utf-8'); need=['REV-001','SELF-001','Reversed','New evidence','What would have made the original correct','What did not change']; missing=[n for n in need if n not in t]; assert not missing, missing"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import pathlib; t=pathlib.Path('.planning/REVERSALS.md').read_text(encoding='utf-8'); need=['REV-001','SELF-001','Reversed','New evidence','What would have made the original correct','What did not change']; missing=[n for n in need if n not in t]; assert not missing, missing; assert len(t) > 800"</automated>
    <human-check>Read the file. Confirm the template has every D-14 field, that `SELF-001` is defined rather than mentioned, and that a future author could copy the template and fill it without consulting brief.md.</human-check>
  </verify>
  <done>`.planning/REVERSALS.md` exists with a copyable D-14 reversal template, a defined `SELF-001` convention, and REV-001 transcribed as a worked entry.</done>
</task>

<task type="auto">
  <name>Task 2: Document the migration path, the known limit and the two tiers of D-05 rigour (REQ-P6-15)</name>
  <files>README.md, .planning/PROJECT.md</files>
  <read_first>
    - `README.md` lines 94-136 (`## The contract`), 206-224 (`### Finding suppressions`), and
      288-317 (`## Design notes`, including the existing "a spec can lie" sentence the new section
      must sharpen rather than duplicate)
    - `brief.md` lines 453-458 — section 8, the known limit in the author's own words
    - `.planning/PROJECT.md` lines 77-80 — the version rationale sentence D-10 requires amending
    - `.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-CONTEXT.md`
      decisions D-10 (severity is the gate point), D-20 (two tiers of evidentiary rigour) and
      D-23 (the `# D-05: <CODE>` test-linkage convention that binds Phases 7 through 12)
  </read_first>
  <action>
    Make three README additions.

    (1) A migration section for pre-v2.0.0 specs, placed under `## The contract` where a reader
    arrives after seeing a spec start to block. It states: `validity_frame:` is required from
    `plan` onward at CRITICAL severity, so a spec written against v1.x begins blocking on upgrade;
    the supported path is to fill the frame; the supported *interim* path is the existing
    `suppressions[]` mechanism, which requires a `reason` and an `authority` naming a real ADR or
    SPEC file, applies after checks and before the blocking threshold, and aborts the run with
    exit 2 on an unknown code. Say plainly that a suppression is an attributable, dated decision
    with a named authority, not a way to make the finding go away — and that a suppression with no
    resolvable authority reference already produces `DSX-SPEC-070`. Cross-reference the existing
    `### Finding suppressions` section rather than duplicating its YAML example.

    (2) A `## Known limits` section, near `## Design notes`. State brief section 8's limit as a
    claim about this tool: the gate checks declarations against declarations, so **a frame that
    lies passes**; the insurance against a bad question is still a human who knows the domain
    reading the frame before the data is touched; what changes is that the review becomes cheap,
    structured and repeatable, so it actually happens. Do not soften it and do not bury it inside
    the existing design-notes paragraph about `dsx-ml-integrity-auditor` — that paragraph is about
    misdeclared *pipelines*; this is about a frame that is internally coherent and still false.

    (3) A subsection stating the two tiers of evidentiary rigour, so a reader does not assume
    uniformity across the catalogue. Tier one: every finding code introduced in v2.0.0 carries a
    `Citation:` line naming author, year, work and the exact formulation, plus a
    `Reference value:` or `Structural criterion:` line, plus a `# D-05: <CODE>` marker comment in
    `tests/` linking it to a test — all three enforced mechanically by
    `python3 scripts/gen-finding-catalogue.py --check`. Tier two: the pre-existing finding codes
    predate the rule and are carried on a finite allow-list inside that script; many are structural
    (contract shape, SQL fan-out) with no primary statistical source, and forcing a citation there
    would manufacture exactly the fake authority the rule exists to prevent. State that the
    allow-list is designed to shrink. State the `# D-05: <CODE>` convention explicitly, because it
    binds every later phase from the moment it lands.

    Then amend `.planning/PROJECT.md`'s version rationale (lines 77-80). It currently names the
    later pair of gate points as where the frame block becomes required; rewrite that clause to say
    the block is required from `plan`, since D-10 set the severity to CRITICAL and CRITICAL is the
    plan/execute threshold. Replace the clause outright — do not leave the superseded wording
    behind as a parenthetical or a struck-through note, because the acceptance criterion asserts
    the old phrasing is gone from the file. Use a scoped edit to that sentence only. This is not a D-14 reversal — the version rationale sits
    outside both the brief section 4 D-table and the PROJECT.md M-table — but it must not be left
    contradicting the gate, so do not file a `REV-NNN` record for it.
  </action>
  <acceptance_criteria>
    - `README.md` contains a heading `## Known limits`
    - `README.md` contains the phrase `a frame that lies passes`
    - `README.md` names `suppressions[]` together with `authority` in a migration context, and
      names `DSX-SPEC-070`
    - `README.md` names `scripts/gen-finding-catalogue.py`, the `Citation:` marker, the
      `Reference value:`/`Structural criterion:` marker, and the `# D-05:` test-linkage convention
    - `python3 -c "import pathlib; t=pathlib.Path('README.md').read_text(encoding='utf-8'); need=['## Known limits','a frame that lies passes','suppressions[]','authority','DSX-SPEC-070','gen-finding-catalogue.py','Citation:','Structural criterion:','# D-05:']; missing=[n for n in need if n not in t]; assert not missing, missing"` exits 0
    - `python3 -c "import pathlib; t=pathlib.Path('.planning/PROJECT.md').read_text(encoding='utf-8'); assert 'required at verify/ship' not in t; assert 'required from plan' in t"` exits 0
    - `git diff --stat .planning/PROJECT.md` shows a change confined to the version-rationale lines
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import pathlib; t=pathlib.Path('README.md').read_text(encoding='utf-8'); need=['## Known limits','a frame that lies passes','suppressions[]','authority','DSX-SPEC-070','gen-finding-catalogue.py','Citation:','Structural criterion:','# D-05:']; missing=[n for n in need if n not in t]; assert not missing, missing; p=pathlib.Path('.planning/PROJECT.md').read_text(encoding='utf-8'); assert 'required at verify/ship' not in p and 'required from plan' in p"</automated>
    <human-check>Read the three README additions. Confirm the migration section states the authority requirement as a requirement and not a suggestion, that the known limit is stated plainly rather than softened or buried, and that the two D-05 tiers are legible to a reader who has not read brief.md.</human-check>
  </verify>
  <done>The README documents the `suppressions[]` migration path with its authority requirement, states the known limit that a frame which lies passes, and states the two tiers of D-05 evidentiary rigour with the mechanism that enforces tier one; PROJECT.md's version rationale matches the gate point D-10 chose.</done>
</task>

</tasks>

<flagged_assumptions>
Spec-less edge-probe rows that remain `unresolved` and are surfaced here rather than silently
dropped or auto-backstopped:

- **REQ-P6-14 (unclassified, unresolved).** The probe surfaced no boundary/precision/adjacency/
  empty/ordering shape for "`.planning/REVERSALS.md` exists with the D-14 template and the
  `SELF-001` convention documented". Residual risk: `SELF-001` has no enforcement mechanism (M-05
  keeps it a convention for v2.0.0; `brief.md` section 6.6 item 3 records the gap). Nothing in this
  phase detects a reversal filed with an empty **New evidence** field. The mechanical criteria
  above confirm the file exists and defines the convention; they cannot confirm the convention is
  followed, and by construction for v2.0.0 nothing can.
</flagged_assumptions>

<edge_coverage_accounting>
Phase-wide spec-less edge probe: **27 applicable rows, 0 resolved on arrival.**
Resolution across the whole plan set: **17 authored into `must_haves`** (15 `covered` plain-string
truths + 2 `backstop` structured markers) **+ 10 surfaced as flagged assumptions** = **27**.
No row was dismissed and no `unclassified` row was auto-backstopped.

This plan carries **2** of the 17 authored rows — both REQ-P6-15 rows (boundary, precision),
authored as `verification: backstop` structured markers because README prose quality is a
judgement the verifier cannot confirm with explicit evidence; an unconfirmed backstop abstains to
`human_needed` rather than passing silently. It carries **1** of the 10 flagged assumptions
(REQ-P6-14).
</edge_coverage_accounting>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This plan writes documentation and one planning-artifact correction. No code, no input parsing, no new execution path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6-09 | Repudiation | `suppressions[]` documented as a migration path | medium | mitigate | Documenting suppression as the upgrade route risks normalising it into a silent bypass of the frame requirement, defeating the milestone's core value. Mitigated by requiring the README to state the `authority` requirement as a requirement, to name `DSX-SPEC-070` as the finding for a suppression missing `reason`/`authority`, and to present filling the frame as the supported path with suppression as the attributable interim one. Recorded as a prohibition in `must_haves.prohibitions`. |
| T-6-10 | Repudiation | `SELF-001` convention | low | accept | An evidence-free reversal is undetectable in v2.0.0 by design (M-05: enforcement is a planning-process concern, not a gate concern; a subcommand adjudicating planning documents sits outside the gate path). Accepted with the residual gap recorded as the REQ-P6-14 flagged assumption. |
| T-6-SC | Tampering | package-manager installs | low | accept | Zero third-party installs ship in this phase (D-01; `06-RESEARCH.md` Package Legitimacy Audit records the gate as not applicable). This plan installs nothing and adds no import. |
</threat_model>

<verification>
- `python3 -m unittest discover -s tests` exits 0 (this plan touches no code; the suite must be
  unaffected)
- `git diff --stat dsx/ tests/ scripts/ examples/ templates/` reports no changes
- Both `<human-check>` reviews recorded in the plan summary
</verification>

<success_criteria>
- REQ-P6-14: `.planning/REVERSALS.md` exists with the D-14 reversal-record template and the
  `SELF-001` convention documented, plus REV-001 as a worked entry
- REQ-P6-15: the README documents `suppressions[]` with its authority requirement as the migration
  path for pre-v2.0.0 specs, and states the known limit that a frame which lies passes
- The README additionally states the two tiers of D-05 evidentiary rigour and the
  `# D-05: <CODE>` test-linkage convention that binds Phases 7 through 12 (D-20, D-23)
- `.planning/PROJECT.md`'s version rationale no longer contradicts D-10's chosen gate point
</success_criteria>

<output>
Create `.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-04-SUMMARY.md` when done
</output>
