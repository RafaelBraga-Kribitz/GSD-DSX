---
phase: 06-contract-extension-decision-record-paradigm-manifest
plan: 08
type: execute
wave: 4
depends_on: ["06-06"]
files_modified:
  - examples/known-bad/interference-shared-budget-ANALYSIS-SPEC.yaml
  - examples/known-bad/interference-shared-budget-POSTMORTEM.md
  - examples/known-bad/frequentist-uncontrolled-continuous-ANALYSIS-SPEC.yaml
  - examples/known-bad/frequentist-uncontrolled-continuous-POSTMORTEM.md
  - examples/known-bad/bayesian-continuous-monitoring-ANALYSIS-SPEC.yaml
  - examples/known-bad/bayesian-continuous-monitoring-POSTMORTEM.md
  - tests/test_known_bad_corpus.py
autonomous: true
requirements: [REQ-P6-13]

must_haves:
  truths:
    - "Each `examples/known-bad/*-ANALYSIS-SPEC.yaml` has a sibling `*-POSTMORTEM.md` in the same directory, and every spec has exactly one post-mortem and every post-mortem exactly one spec"
    - "`examples/known-bad/` contains at least three spec and post-mortem pairs, including at least one interference case and at least one Bayesian continuous-monitoring case"
    - "Each known-bad fixture parses and `dsx validate --spec <fixture>` exits 0 — the defect each encodes is semantic and is adjudicated by the phase that ships its code, so no structural finding fires in this phase"
    - "The Bayesian continuous-monitoring post-mortem states that the fixture encodes the prior-averaged Ville bound formulation and not the point-null / law-of-iterated-logarithm formulation, and names the theorem its reference value traces to"
    - "The frequentist uncontrolled-continuous fixture declares the peeking policy denoting continuous monitoring with no sequential correction, and its post-mortem's reference value agrees with `dsx.mathx.inflation_from_peeking` at the declared number of looks"
    - "Each post-mortem names the currently-absent finding code that would have caught the case"
  artifacts:
    - path: "examples/known-bad/interference-shared-budget-ANALYSIS-SPEC.yaml"
      provides: "A structurally valid spec encoding a documented shared-resource interference failure"
    - path: "examples/known-bad/frequentist-uncontrolled-continuous-ANALYSIS-SPEC.yaml"
      provides: "The frequentist half of the Phase 9 atomic pair's target (D-06)"
    - path: "examples/known-bad/bayesian-continuous-monitoring-ANALYSIS-SPEC.yaml"
      provides: "The spec asserting that a weakly informative prior controls false positives while peeking"
    - path: "tests/test_known_bad_corpus.py"
      provides: "Pairing, minimum-composition and structural-validity tests over the corpus directory"
  key_links:
    - from: "examples/known-bad/frequentist-uncontrolled-continuous-ANALYSIS-SPEC.yaml"
      to: "dsx/spec.py::PEEKING_POLICIES"
      via: "design.peeking_policy declares the member added by plan 06-01; without it the spec would trip DSX-SPEC-042 at HIGH"
      pattern: "uncontrolled_continuous"
    - from: "examples/known-bad/*-POSTMORTEM.md"
      to: "brief.md section 7 reference list"
      via: "every post-mortem's source is a primary work from the anchored list; vendor blogs are inadmissible under D-05 in either direction"
      pattern: "\\(20[0-9]{2}\\)"
  prohibitions:
    - statement: "MUST NOT present a synthetic, invented or reconstructed-from-memory case as a real documented failure. Every post-mortem in this corpus must trace to a published primary work, a documented retraction, or a named prior project whose outcome is now known — never to a plausible narrative written to fit the fixture. Phase 12 reports a measured catch rate over this corpus; a fabricated case makes that number a fiction while making it look like evidence, and vendor blogs, Medium posts and tool marketing are inadmissible under D-05 in either direction."
      status: unverified
      verification: judgment
---

<objective>
Commit the known-bad seed corpus: three real failure cases as spec-and-post-mortem pairs, each
structurally valid so it parses today and blocks later when the phase that owns its code ships
(REQ-P6-13, D-06, brief section 6.5's M1 fixture note).

Purpose: brief M1 pulls this forward from M5 deliberately — the fixtures cost little now and they
make every later check answerable to a real failure rather than to a model of failure. D-06 adds
one beyond the floor: both halves of Phase 9's atomic pair get a target here, so a half-shipped
Phase 9 is harder to fudge.

Output: `examples/known-bad/` with three pairs and a dedicated test module. This plan runs in
parallel with plan 06-07 — the file sets are disjoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-CONTEXT.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-RESEARCH.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-05-SUMMARY.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-06-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Every symbol below is CREATED by Phase 6. Drift/source-grounding passes must treat these as new,
not as pre-existing references.

**New modules and packages**
- `dsx/decisions.py` — top-level peer to `dsx/findings.py`. NOT `dsx/checks/decision.py`, which is
  the pre-existing `DSX-DEC-*` replay module and is untouched this phase.
- `dsx/frame/` package: `dsx/frame/__init__.py`, `dsx/frame/paradigm.py`

**New classes / functions / module constants**
- `dsx.decisions.DecisionRecord`, `dsx.decisions.InvocationHeader`
- `dsx.decisions.append()`, `read_all()`, `next_invocation_id()`, `frame_digest()`,
  `decisions_path()`, `collect_from_report()`
- `dsx.decisions.DECISION_LAYERS`, `dsx.decisions.RECORD_TYPES`
- `dsx.frame.paradigm.check()`, `_PARADIGM_INDEPENDENT`, `_PARADIGM_CONDITIONAL`, `_NOT_SHIPPED`
- `dsx.spec._validate_validity_frame_shape()`, `dsx.spec._validate_inference_shape()`
- `dsx.spec._VOCABULARIES`, `_VALIDITY_FRAME_ALWAYS_REQUIRED`, `_VALIDITY_FRAME_CAUSAL_REQUIRED`,
  `_INFERENCE_FIELDS`
- `dsx.cli.cmd_explain()`, `dsx.cli._render_decision_trail()`
- `scripts/gen-finding-catalogue.py`: `check_d05()`, `_resolve_docstrings()`,
  `_collect_test_markers()`, `_D05_ALLOWLIST_PREFIXES`, `_CITATION_RE`, `_REFVALUE_RE`,
  `_TEST_MARKER_RE`
- `tests/test_frame_boundary.py`: `_scan_source_for_checks_imports()`, `_package_for()`

**New CLI surface**
- subcommand `dsx explain` with `--spec`, `--phase-dir`, `--invocation`, `--json`, `--verbose`
  (deliberately NO `--block-on`)
- `dsx.cli.add_common()` gains `include_block_on: bool = True`
- `CHECKS["paradigm"]`; `"paradigm"` added to all four `GATE_PROFILES` tuples

**New finding codes**
- `DSX-SPEC-080` validity_frame block absent (CRITICAL)
- `DSX-SPEC-081` required validity_frame sub-block missing (CRITICAL)
- `DSX-SPEC-082` validity_frame sub-field outside its closed vocabulary (HIGH)
- `DSX-SPEC-085` inference sub-field outside its closed vocabulary (HIGH)
- `DSX-SPEC-086` a removed-by-M-02 inference field is declared (HIGH)
- `DSX-PAR-001` paradigm manifest (INFO)
- `DSX-PAR-999` — test-fixture-only code, lives under `tests/fixtures/d05/`, never under `dsx/`

**New vocabulary constants in `dsx/spec.py`**
- `IDENTIFICATION_STRENGTHS`, `CONSTRAINT_SOURCES`, `DEPENDENCE_STRUCTURES`,
  `INTERFERENCE_RISKS`, `INTERFERENCE_MITIGATIONS`, `MISSINGNESS_MECHANISMS`,
  `ANALYSIS_POPULATIONS`, `DECLARATION_POINTS`, `PARADIGMS`, `PARADIGM_JUSTIFICATIONS`
- `PEEKING_POLICIES` gains the member `uncontrolled_continuous` (existing dict, one new entry)
- `VARIANCE_ADJUSTMENTS` is REUSED unchanged for `dependence.method_family_required` (M-09)

**New spec contract surface (`ANALYSIS-SPEC.yaml`)**
- `validity_frame:` with `estimand`, `units`, `identification`, `dependence`, `interference`,
  `triggering`, `stability`, `sampling_frame`, `missingness`, `measurement`
- `inference:` with `paradigm`, `paradigm_justification`, `declared_at`, `primary_procedure`,
  `alpha_spending`, `fallback_rule` (M-02 removes the stopping-rule field; the concept is read
  from the existing `design.peeking_policy`)

**New files**
- `tests/test_frame_boundary.py`, `tests/test_decisions.py`,
  `tests/test_gen_finding_catalogue.py`, `tests/test_known_bad_corpus.py`
- `tests/fixtures/d05/bad_check.py`
- `examples/known-bad/interference-shared-budget-ANALYSIS-SPEC.yaml` + `-POSTMORTEM.md`
- `examples/known-bad/frequentist-uncontrolled-continuous-ANALYSIS-SPEC.yaml` + `-POSTMORTEM.md`
- `examples/known-bad/bayesian-continuous-monitoring-ANALYSIS-SPEC.yaml` + `-POSTMORTEM.md`
- `.planning/REVERSALS.md`
- `DECISIONS.jsonl` — runtime artifact written beside the resolved spec; not committed
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The interference case (REQ-P6-13)</name>
  <files>examples/known-bad/interference-shared-budget-ANALYSIS-SPEC.yaml, examples/known-bad/interference-shared-budget-POSTMORTEM.md</files>
  <read_first>
    - `examples/good-ANALYSIS-SPEC.yaml` as extended by plan 06-05 — the complete shape a
      structurally valid spec now has, including all ten `validity_frame` sub-blocks
    - `dsx/spec.py` `REQUIRED_TOP_LEVEL` (line 197) and `validate_structure` (200-256) — the
      CRITICAL findings a structurally valid fixture must avoid
    - `dsx/spec.py` `INTERFERENCE_RISKS` and `INTERFERENCE_MITIGATIONS` as landed by plan 06-01 —
      the legal members
    - `brief.md` lines 432-449 (section 7) — the anchored primary-source list; and lines 307-313
      (M2b) for what `DSX-INT-*` will eventually adjudicate
    - `.planning/ROADMAP.md` lines 210-215 — Phase 8 Success Criterion 1, which names
      `examples/known-bad/interference-shared-budget-ANALYSIS-SPEC.yaml` and `DSX-INT-010` by name,
      so the filename created here is the one Phase 8 will assert against
  </read_first>
  <action>
    Create `examples/known-bad/interference-shared-budget-ANALYSIS-SPEC.yaml` encoding a shared-
    advertising-budget interference failure: two arms bidding into one budget, so lift in treatment
    cannibalises control and the measured effect is not the effect of the treatment. The spec is
    **structurally complete and semantically wrong**: `question_type: causal`, `design.kind:
    experiment`, all ten `validity_frame` sub-blocks present with legal vocabulary members, and
    `interference.risk: shared_budget` declared with `mitigation: none` and an empty
    `residual_note`. That combination is the defect — a declared risk other than `none` with
    neither a mitigation nor an explicit residual note — and it is what `DSX-INT-010` will block on
    in Phase 8. Nothing in this phase blocks it, which is correct: Phase 6 checks shape.

    Open the file with a header comment, following the convention `examples/bad-ANALYSIS-SPEC.yaml`
    already uses, naming the defect and the code that will catch it.

    Create `examples/known-bad/interference-shared-budget-POSTMORTEM.md` with four sections:
      - **What was concluded** — the wrong answer the analysis produced
      - **Why it was wrong** — the interference mechanism, stated so a reader who does not know the
        term understands the cannibalisation
      - **Source** — a primary work from `brief.md` section 7's anchored list, naming the exact
        chapter: Kohavi, Tang & Xu (2020), *Trustworthy Online Controlled Experiments*, for the
        shared-resource interference pattern, and Imbens & Rubin (2015), *Causal Inference for
        Statistics, Social, and Biomedical Sciences*, for the SUTVA statement the pattern violates.
        Vendor blogs, Medium posts and tool marketing are inadmissible under D-05 in either
        direction — they cannot justify a check and cannot excuse skipping one. **If you cannot
        verify the exact chapter, escalate in the plan summary rather than inventing a locator.**
      - **Which absent code would have caught it** — name `DSX-INT-010` and the phase that ships
        it. This line is deliberately machine-adjacent: Phase 12 REQ-P12-02 turns catch attribution
        into structured tags, and starting the convention now means the corpus is not retrofitted.
  </action>
  <acceptance_criteria>
    - `python3 -m dsx validate --spec examples/known-bad/interference-shared-budget-ANALYSIS-SPEC.yaml` exits 0
    - `python3 -c "from dsx.loader import load; s=load('examples/known-bad/interference-shared-budget-ANALYSIS-SPEC.yaml'); vf=s['validity_frame']; need=['estimand','units','identification','dependence','interference','triggering','stability','sampling_frame','missingness','measurement']; missing=[k for k in need if not isinstance(vf.get(k), dict)]; assert not missing, missing; assert vf['interference']['risk']=='shared_budget'; assert vf['interference']['mitigation']=='none'; assert not str(vf['interference'].get('residual_note') or '').strip()"` exits 0
    - `python3 -c "import pathlib; t=pathlib.Path('examples/known-bad/interference-shared-budget-POSTMORTEM.md').read_text(encoding='utf-8'); need=['DSX-INT-010','Kohavi','Imbens','Rubin']; missing=[n for n in need if n not in t]; assert not missing, missing; assert len(t) > 600"` exits 0
    - `python3 -m unittest discover -s tests` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python3 -m dsx validate --spec examples/known-bad/interference-shared-budget-ANALYSIS-SPEC.yaml && python3 -m unittest discover -s tests</automated>
    <human-check>Read the post-mortem. Confirm it names a real documented failure pattern with a verifiable primary source and chapter, not a synthetic narrative written to fit the fixture.</human-check>
  </verify>
  <done>A structurally valid spec encoding a declared shared-budget interference risk with no mitigation and no residual note, paired with a post-mortem sourced to two primary works and naming the absent code that would have caught it.</done>
</task>

<task type="auto">
  <name>Task 2: Both halves of the Phase 9 monitoring pair (REQ-P6-13, D-06)</name>
  <files>examples/known-bad/frequentist-uncontrolled-continuous-ANALYSIS-SPEC.yaml, examples/known-bad/frequentist-uncontrolled-continuous-POSTMORTEM.md, examples/known-bad/bayesian-continuous-monitoring-ANALYSIS-SPEC.yaml, examples/known-bad/bayesian-continuous-monitoring-POSTMORTEM.md</files>
  <read_first>
    - `dsx/mathx.py` lines 411-432 (`inflation_from_peeking`) — the tabulated Type-I inflation
      anchors already in this codebase (1 look 0.05, 2 looks 0.083, 3 looks 0.107, 5 looks 0.142,
      10 looks 0.193), which the frequentist post-mortem's reference value must agree with
    - `dsx/checks/design.py` lines 444-461 (`_check_peeking` / `DSX-EXP-060`) — confirm its trigger
      is the literal tuple, so the new peeking-policy member falls straight through and this
      fixture does not double-fire (M-01, D-08)
    - `dsx/spec.py` `PEEKING_POLICIES` and `PARADIGMS` as landed by plan 06-01
    - `brief.md` lines 217-227 (section 5.3) and 394-413 (the M1 fixture note) — the formulation
      trap, stated in the author's own words: the point-null and prior-averaged setups give
      different numbers and a fixture built against one and checked against the other looks like an
      implementation bug for a day
    - `.planning/ROADMAP.md` lines 257-265 — Phase 9 Success Criteria 2 and 3, which name
      `examples/known-bad/bayesian-continuous-monitoring-ANALYSIS-SPEC.yaml`, `DSX-PAR-011`, the
      bound `1/(K+1)`, `K = 19` and `0.05`
  </read_first>
  <action>
    Create the frequentist half:
    `examples/known-bad/frequentist-uncontrolled-continuous-ANALYSIS-SPEC.yaml`. A structurally
    complete causal experiment spec with all ten `validity_frame` sub-blocks,
    `inference.paradigm: frequentist`, `alpha_spending: null`, no sequential method named in
    `primary_procedure`, and `design.peeking_policy` set to the member plan 06-01 added for
    continuous monitoring with no sequential correction. Declare the number of interim looks in the
    existing `results.interim_looks` field so the inflation figure in the post-mortem is grounded in
    the spec's own declaration. Use 5 looks, because `inflation_from_peeking(5)` returns exactly
    `0.142` and that number is already unit-tested in this codebase. This fixture exists one beyond
    REQ-P6-13's floor because D-06 wants both halves of Phase 9's atomic pair to have a committed
    target before the pair is written.

    Its post-mortem uses the same four sections as Task 1. **Source:** Armitage, McPherson & Rowe
    (1969), "Repeated significance tests on accumulating data", *Journal of the Royal Statistical
    Society Series A* — the published Type-I inflation under repeated naive testing, which is the
    result `inflation_from_peeking` tabulates. State the reference value explicitly: at a nominal
    alpha of 0.05 with 5 interim looks, the true Type-I error is approximately 0.142. **Which absent
    code would have caught it:** `DSX-PAR-010`, Phase 9. Note in the post-mortem that
    `DSX-EXP-060` does not fire on this spec and is not supposed to — its trigger is a literal
    tuple disjoint from this policy value by construction (M-01), and the parametrised disjointness
    test from plan 06-01 is what pins that.

    Create the Bayesian half:
    `examples/known-bad/bayesian-continuous-monitoring-ANALYSIS-SPEC.yaml`. The same structural
    completeness, with `inference.paradigm: bayesian`, the same continuous-monitoring peeking
    policy, no threshold calibration declared, and a claim or limitation in the spec asserting that
    a weakly informative prior controls false positives while peeking. That assertion is the defect,
    and brief section 6.5 names it the single most load-bearing misconception in this domain: the
    posterior is valid under any stopping rule; the error rate of a decision procedure built on it
    is not. A weak prior delays threshold crossing slightly and does not control the rate.

    Its post-mortem carries one extra, mandatory section beyond the four: **Which formulation this
    fixture encodes.** State explicitly that the reference value is the **prior-averaged** bound —
    Ville's martingale inequality caps the probability of ever crossing a posterior-odds threshold
    `k` at roughly `1/k`, so at the `P(B>A) > 0.95` decision threshold `K = 19` and the bound is
    `0.05` — and that it is **not** the point-null / law-of-iterated-logarithm formulation, under
    which the error rate grows without a useful ceiling over an unbounded horizon. Both are correct
    statements about different quantities. **Source:** Deng, Lu & Chen (2016), "Continuous
    Monitoring of A/B Tests without Pain", Theorem 1. **Which absent code would have caught it:**
    `DSX-PAR-011`, Phase 9. Add the same formulation note as a comment in the spec file itself, so
    a future reader hitting a number mismatch reads it as a formulation question in five minutes
    rather than an implementation bug for a day.

    Do not write the `DSX-PAR-011` simulation here. It lives under `tests/`, seeded and
    reproducible, and never on the gate path (D-02, REQ-P9-07) — Phase 9's work, not this phase's.
  </action>
  <acceptance_criteria>
    - `python3 -m dsx validate --spec examples/known-bad/frequentist-uncontrolled-continuous-ANALYSIS-SPEC.yaml` exits 0
    - `python3 -m dsx validate --spec examples/known-bad/bayesian-continuous-monitoring-ANALYSIS-SPEC.yaml` exits 0
    - `python3 -c "from dsx.loader import load; from dsx.spec import PEEKING_POLICIES; f=load('examples/known-bad/frequentist-uncontrolled-continuous-ANALYSIS-SPEC.yaml'); b=load('examples/known-bad/bayesian-continuous-monitoring-ANALYSIS-SPEC.yaml'); assert f['inference']['paradigm']=='frequentist'; assert b['inference']['paradigm']=='bayesian'; assert f['design']['peeking_policy'] in PEEKING_POLICIES and f['design']['peeking_policy'] not in ('fixed_horizon','always_valid'); assert b['design']['peeking_policy']==f['design']['peeking_policy']; assert f['inference']['alpha_spending'] is None"` exits 0
    - `python3 -c "from dsx.loader import load; from dsx.mathx import inflation_from_peeking as inf; f=load('examples/known-bad/frequentist-uncontrolled-continuous-ANALYSIS-SPEC.yaml'); looks=f['results']['interim_looks']; assert abs(inf(looks) - 0.142) < 1e-9, (looks, inf(looks))"` exits 0
    - `python3 -c "import pathlib; t=pathlib.Path('examples/known-bad/frequentist-uncontrolled-continuous-POSTMORTEM.md').read_text(encoding='utf-8'); need=['DSX-PAR-010','Armitage','1969','0.142','DSX-EXP-060']; missing=[n for n in need if n not in t]; assert not missing, missing"` exits 0
    - `python3 -c "import pathlib; t=pathlib.Path('examples/known-bad/bayesian-continuous-monitoring-POSTMORTEM.md').read_text(encoding='utf-8'); need=['DSX-PAR-011','Deng','2016','Theorem 1','prior-averaged','Ville','19','0.05']; missing=[n for n in need if n not in t]; assert not missing, missing"` exits 0
    - `python3 -c "import pathlib; t=pathlib.Path('examples/known-bad/bayesian-continuous-monitoring-ANALYSIS-SPEC.yaml').read_text(encoding='utf-8'); assert 'prior-averaged' in t or 'prior averaged' in t"` exits 0
    - `python3 -m unittest discover -s tests` exits 0
  </acceptance_criteria>
  <verify>
    <automated>for f in frequentist-uncontrolled-continuous bayesian-continuous-monitoring; do python3 -m dsx validate --spec examples/known-bad/$f-ANALYSIS-SPEC.yaml || exit 1; done && python3 -m unittest discover -s tests</automated>
    <human-check>Read both post-mortems. Confirm the Bayesian one states the prior-averaged formulation unambiguously and does not conflate it with the point-null result, and that both cite verifiable primary works rather than secondary summaries.</human-check>
  </verify>
  <done>Both halves of the Phase 9 atomic pair have a committed, structurally valid target with a sourced post-mortem; the frequentist reference value agrees with the table already in `dsx/mathx.py`; the Bayesian one states which of the two formulations it encodes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Corpus invariants as a dedicated test module (REQ-P6-13)</name>
  <files>tests/test_known_bad_corpus.py</files>
  <read_first>
    - `tests/test_dsx.py` lines 804-812 — the `TestCLI` `_run` harness idiom this module reuses in
      spirit (capture stdout/stderr, call `cli.main(argv)`, assert on the exit code)
    - `examples/known-bad/` as created in Tasks 1 and 2
    - `.planning/ROADMAP.md` lines 134-139 — Success Criterion 5, which is what these invariants
      encode
  </read_first>
  <behavior>
    - Every `*-ANALYSIS-SPEC.yaml` under `examples/known-bad/` has a sibling `*-POSTMORTEM.md`
      derived from the same slug
    - Every `*-POSTMORTEM.md` under `examples/known-bad/` has a sibling `*-ANALYSIS-SPEC.yaml` —
      the pairing is checked in both directions, so neither an orphan spec nor an orphan post-mortem
      passes
    - The directory holds at least three pairs
    - At least one slug names interference and at least one names Bayesian continuous monitoring
    - Every spec in the directory loads without raising
    - `dsx validate --spec <each spec>` exits 0
    - Every post-mortem names at least one finding code matching the `DSX-<LETTERS>-<digits>` shape
      — the catch-attribution line Phase 12 will formalise
  </behavior>
  <action>
    Create `tests/test_known_bad_corpus.py` with a `TestKnownBadCorpus(unittest.TestCase)` class
    covering every behaviour above. Discover the corpus by globbing the directory rather than
    listing filenames, so Phase 12 can extend the corpus to full size without editing this module —
    which is the whole reason this lives in its own file rather than as methods inside
    `tests/test_dsx.py`.

    Derive each pair's slug by stripping the `-ANALYSIS-SPEC.yaml` and `-POSTMORTEM.md` suffixes and
    comparing the two slug sets for equality, which catches an orphan in either direction with one
    assertion and a readable failure message naming the unmatched slugs.

    For the composition assertions, match on the slug, not on file content: at least one slug
    contains `interference`, at least one contains both `bayesian` and `continuous`. Assert at least
    three pairs, not exactly three — Phase 12 grows this corpus and an exact count would make that
    growth break this test.

    For the structural-validity assertion, invoke the CLI in-process the way `TestCLI` does, with
    `redirect_stdout`/`redirect_stderr` around `cli.main(["validate", "--spec", str(path)])`, and
    assert exit code 0 with the captured stderr in the failure message. Do not assert that any
    specific finding code fires: the defect each fixture encodes is semantic, and the code-specific
    block assertions land with the phase that ships each code (ROADMAP Success Criterion 5).
  </action>
  <acceptance_criteria>
    - `python3 -m unittest tests.test_known_bad_corpus -v` exits 0 and reports at least 6 tests
    - `python3 -c "import sys; sys.path.insert(0,'tests'); import test_known_bad_corpus as m; import inspect; src=inspect.getsource(m); assert 'glob' in src or 'rglob' in src, 'corpus must be discovered by globbing, not by a hardcoded filename list'"` exits 0
    - `python3 -c "import pathlib; d=pathlib.Path('examples/known-bad'); specs={p.name[:-len('-ANALYSIS-SPEC.yaml')] for p in d.glob('*-ANALYSIS-SPEC.yaml')}; pms={p.name[:-len('-POSTMORTEM.md')] for p in d.glob('*-POSTMORTEM.md')}; assert specs==pms, specs ^ pms; assert len(specs)>=3, specs"` exits 0
    - `python3 -m unittest discover -s tests` exits 0
    - `python3 scripts/gen-finding-catalogue.py --check` exits 0 — this plan adds no finding code,
      so the catalogue must be unchanged
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest tests.test_known_bad_corpus -v && python3 -m unittest discover -s tests && python3 scripts/gen-finding-catalogue.py --check</automated>
  </verify>
  <done>A dedicated test module discovers the corpus by glob, enforces two-directional pairing, a minimum composition including the interference and Bayesian cases, structural validity of every spec, and a catch-attribution code in every post-mortem — all without a hardcoded filename list, so Phase 12 can grow the corpus without editing it.</done>
</task>

</tasks>

<flagged_assumptions>
No `unresolved` spec-less edge-probe rows are assigned to this plan. All three rows this plan owns
(REQ-P6-13: adjacency, empty, ordering) are resolved `covered` and authored as plain-string truths
in `must_haves.truths`.

One discretionary choice is recorded here rather than assumed silently. `06-CONTEXT.md` delegates
*which* real analyses the corpus encodes to the researcher and planner; D-06 pins two of the three
(the frequentist and Bayesian uncontrolled-continuous cases) and REQ-P6-13 requires at least one
interference case. The interference case is the shared-advertising-budget cannibalisation pattern,
chosen because Phase 8's ROADMAP Success Criterion 1 already names
`examples/known-bad/interference-shared-budget-ANALYSIS-SPEC.yaml` and `DSX-INT-010` by name, so
this filename is the one Phase 8 will assert against and choosing any other case would force a
ROADMAP edit.
</flagged_assumptions>

<edge_coverage_accounting>
Phase-wide spec-less edge probe: **27 applicable rows, 0 resolved on arrival.**
Resolution across the whole plan set: **17 authored into `must_haves`** (15 `covered` plain-string
truths + 2 `backstop` structured markers) **+ 10 surfaced as flagged assumptions** = **27**.
No row was dismissed and no `unclassified` row was auto-backstopped.

This plan carries **3** of the 17 authored rows (REQ-P6-13: adjacency, empty, ordering) and **0**
of the 10 flagged assumptions.
</edge_coverage_accounting>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture specs -> `dsx.loader.load()` | Repository-controlled input; the corpus is evidence, and its trust property is provenance rather than parsing safety |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6-16 | Spoofing | post-mortem provenance | high | mitigate | A fabricated case presented as a real documented failure corrupts Phase 12's measured catch rate while making it look like evidence — the same laundering failure D-05 targets, one level up. Mitigated by requiring a primary work from `brief.md` section 7's anchored list with an exact chapter or theorem, by the mechanical author/year assertions in the acceptance criteria, by the instruction to escalate rather than invent a locator, and by a human-check review of each post-mortem. Recorded as a prohibition in `must_haves.prohibitions`. |
| T-6-17 | Tampering | the Bayesian fixture's formulation | high | mitigate | A fixture built against the point-null formulation and later checked against the prior-averaged bound reads as an implementation bug for a day rather than a formulation question in five minutes (brief section 6.5). Mitigated by requiring the formulation to be stated in the post-mortem AND commented in the spec file, and by asserting both mechanically. |
| T-6-18 | Tampering | `DSX-EXP-060` double-firing | medium | mitigate | The frequentist fixture declares a peeking policy adjacent to what `DSX-EXP-060` already covers; a future widening of `_check_peeking` would make Phase 9's `DSX-PAR-010` double-fire on it. Mitigated by M-01 keeping `_check_peeking` untouched and by the parametrised disjointness test landed in plan 06-01, which pins the property rather than the current code. |
| T-6-SC | Tampering | package-manager installs | low | accept | Zero third-party installs ship in this phase (D-01; `06-RESEARCH.md` Package Legitimacy Audit records the gate as not applicable). This plan adds YAML, markdown and one test module. |
</threat_model>

<verification>
- `python3 -m unittest discover -s tests` exits 0
- `python3 -m dsx validate --spec examples/known-bad/<each>-ANALYSIS-SPEC.yaml` exits 0 for all three
- `python3 scripts/gen-finding-catalogue.py --check` exits 0
- `python3 -m dsx gate {plan,execute,verify,ship} --spec examples/good-ANALYSIS-SPEC.yaml` all exit 0
- `git diff --stat dsx/ scripts/ examples/good-ANALYSIS-SPEC.yaml examples/bad-ANALYSIS-SPEC.yaml`
  reports no changes
- Both `<human-check>` reviews recorded in the plan summary
</verification>

<success_criteria>
- REQ-P6-13: at least three real known-bad analyses are committed as fixtures with documented
  post-mortems, including at least one interference case and one Bayesian continuous-monitoring case
- D-06: both halves of Phase 9's atomic pair have a committed target, so a half-shipped Phase 9 is
  harder to fudge
- Every fixture passes `dsx validate` structurally; no code-specific block assertion is made here,
  because each lands with the phase that ships its code (ROADMAP Success Criterion 5)
- Each post-mortem names the currently-absent code that would have caught its case, seeding
  Phase 12's structured catch attribution (D-13, REQ-P12-02)
</success_criteria>

<output>
Create `.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-08-SUMMARY.md` when done
</output>
