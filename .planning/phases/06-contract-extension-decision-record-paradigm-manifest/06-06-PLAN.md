---
phase: 06-contract-extension-decision-record-paradigm-manifest
plan: 06
type: execute
wave: 3
depends_on: ["06-05", "06-02", "06-03"]
files_modified:
  - dsx/spec.py
  - tests/test_dsx.py
  - references/finding-codes.md
autonomous: true
requirements: [REQ-P6-02, REQ-P6-03, REQ-P6-04]

must_haves:
  truths:
    - "A causal spec with no `validity_frame:` block at all produces exactly one CRITICAL finding whose detail itemises every missing required sub-block, and blocks at `dsx gate plan`"
    - "A spec with a `validity_frame:` block present but required sub-blocks missing produces one CRITICAL finding per missing sub-block, each naming that sub-block in `where`"
    - "A descriptive-question spec that omits `identification`, `interference`, `triggering` and `stability` entirely exits 0, while a causal spec omitting the same four blocks"
    - "A `validity_frame` sub-field whose declared value is outside its closed vocabulary produces a HIGH finding naming the allowed values"
    - "An `inference` sub-field whose declared value is outside its closed vocabulary produces a HIGH finding, and declaring the field M-02 removed produces a HIGH finding redirecting to `design.peeking_policy`"
    - "Each new `DSX-SPEC-08x` code resolves to a docstring carrying a `Citation:` line and a `Structural criterion:` line, and to a `# D-05:` marker under `tests/`, so `scripts/gen-finding-catalogue.py --check` exits 0"
    - "Both new validators append `layer: deterministic` decision records to the report context, and the fifteen pre-existing check modules under `dsx/checks/` are unchanged"
  artifacts:
    - path: "dsx/spec.py"
      provides: "_validate_validity_frame_shape and _validate_inference_shape under DSX-SPEC-08x"
      contains: "_validate_validity_frame_shape"
    - path: "tests/test_dsx.py"
      provides: "Requiredness, aggregation, membership and D-05-marker tests for the new codes"
    - path: "references/finding-codes.md"
      provides: "Regenerated catalogue including the new DSX-SPEC-08x codes"
  key_links:
    - from: "dsx/spec.py::validate_structure"
      to: "dsx/spec.py::_validate_validity_frame_shape"
      via: "added to the same call chain as the five existing _validate_* calls; no CHECKS or GATE_PROFILES change (spec is already in all four profiles)"
      pattern: "_validate_validity_frame_shape"
    - from: "dsx/spec.py new validators"
      to: "dsx/decisions.py::DecisionRecord"
      via: "records appended as dicts onto report.context['decisions']; the CLI assigns ids and writes the file"
      pattern: "decisions"
  prohibitions: []
---

<objective>
Land the two structural shape validators and their finding codes: `_validate_validity_frame_shape`
and `_validate_inference_shape` in `dsx/spec.py` under `DSX-SPEC-08x` (REQ-P6-02, REQ-P6-03,
REQ-P6-04, D-09, D-10, D-11).

Purpose: this is where the contract becomes enforceable. Severity is the gate point here —
CRITICAL blocks from `plan` onward, which serves the milestone's core value literally: a frame
first demanded at verify is a frame filled after the data was touched. The requiredness gating and
the aggregate-versus-per-sub-block finding granularity have no precedent in this codebase; budget
this as new design work, not a copy of `_validate_design_shape`.

Output: two validators, five finding codes, their D-05 markers, decision-record emission, and the
tests that pin every branch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-CONTEXT.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-RESEARCH.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-PATTERNS.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-02-SUMMARY.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-03-SUMMARY.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Every symbol below is CREATED by Phase 6. Drift/source-grounding passes must treat these as new,
not as pre-existing references.

**New modules and packages**
- `dsx/decisions.py` — top-level peer to `dsx/findings.py`. NOT `dsx/checks/decision.py`, which is
  the pre-existing `DSX-DEC-*` replay module and is untouched this phase.
- `dsx/frame/` package: `dsx/frame/__init__.py`, `dsx/frame/paradigm.py`

**New classes / functions / module constants**
- `dsx.decisions.DecisionRecord`, `dsx.decisions.InvocationHeader`
- `dsx.decisions.append()`, `read_all()`, `next_invocation_id()`, `frame_digest()`,
  `decisions_path()`, `collect_from_report()`
- `dsx.decisions.DECISION_LAYERS`, `dsx.decisions.RECORD_TYPES`
- `dsx.frame.paradigm.check()`, `_PARADIGM_INDEPENDENT`, `_PARADIGM_CONDITIONAL`, `_NOT_SHIPPED`
- `dsx.spec._validate_validity_frame_shape()`, `dsx.spec._validate_inference_shape()`
- `dsx.spec._VOCABULARIES`, `_VALIDITY_FRAME_ALWAYS_REQUIRED`, `_VALIDITY_FRAME_CAUSAL_REQUIRED`,
  `_INFERENCE_FIELDS`
- `dsx.cli.cmd_explain()`, `dsx.cli._render_decision_trail()`
- `scripts/gen-finding-catalogue.py`: `check_d05()`, `_resolve_docstrings()`,
  `_collect_test_markers()`, `_D05_ALLOWLIST_PREFIXES`, `_CITATION_RE`, `_REFVALUE_RE`,
  `_TEST_MARKER_RE`
- `tests/test_frame_boundary.py`: `_scan_source_for_checks_imports()`, `_package_for()`

**New CLI surface**
- subcommand `dsx explain` with `--spec`, `--phase-dir`, `--invocation`, `--json`, `--verbose`
  (deliberately NO `--block-on`)
- `dsx.cli.add_common()` gains `include_block_on: bool = True`
- `CHECKS["paradigm"]`; `"paradigm"` added to all four `GATE_PROFILES` tuples

**New finding codes**
- `DSX-SPEC-080` validity_frame block absent (CRITICAL)
- `DSX-SPEC-081` required validity_frame sub-block missing (CRITICAL)
- `DSX-SPEC-082` validity_frame sub-field outside its closed vocabulary (HIGH)
- `DSX-SPEC-085` inference sub-field outside its closed vocabulary (HIGH)
- `DSX-SPEC-086` a removed-by-M-02 inference field is declared (HIGH)
- `DSX-PAR-001` paradigm manifest (INFO)
- `DSX-PAR-999` — test-fixture-only code, lives under `tests/fixtures/d05/`, never under `dsx/`

**New vocabulary constants in `dsx/spec.py`**
- `IDENTIFICATION_STRENGTHS`, `CONSTRAINT_SOURCES`, `DEPENDENCE_STRUCTURES`,
  `INTERFERENCE_RISKS`, `INTERFERENCE_MITIGATIONS`, `MISSINGNESS_MECHANISMS`,
  `ANALYSIS_POPULATIONS`, `DECLARATION_POINTS`, `PARADIGMS`, `PARADIGM_JUSTIFICATIONS`
- `PEEKING_POLICIES` gains the member `uncontrolled_continuous` (existing dict, one new entry)
- `VARIANCE_ADJUSTMENTS` is REUSED unchanged for `dependence.method_family_required` (M-09)

**New spec contract surface (`ANALYSIS-SPEC.yaml`)**
- `validity_frame:` with `estimand`, `units`, `identification`, `dependence`, `interference`,
  `triggering`, `stability`, `sampling_frame`, `missingness`, `measurement`
- `inference:` with `paradigm`, `paradigm_justification`, `declared_at`, `primary_procedure`,
  `alpha_spending`, `fallback_rule` (M-02 removes the stopping-rule field; the concept is read
  from the existing `design.peeking_policy`)

**New files**
- `tests/test_frame_boundary.py`, `tests/test_decisions.py`,
  `tests/test_gen_finding_catalogue.py`, `tests/test_known_bad_corpus.py`
- `tests/fixtures/d05/bad_check.py`
- `examples/known-bad/interference-shared-budget-ANALYSIS-SPEC.yaml` + `-POSTMORTEM.md`
- `examples/known-bad/frequentist-uncontrolled-continuous-ANALYSIS-SPEC.yaml` + `-POSTMORTEM.md`
- `examples/known-bad/bayesian-continuous-monitoring-ANALYSIS-SPEC.yaml` + `-POSTMORTEM.md`
- `.planning/REVERSALS.md`
- `DECISIONS.jsonl` — runtime artifact written beside the resolved spec; not committed
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: validity_frame requiredness, aggregation and membership (REQ-P6-02, REQ-P6-03)</name>
  <files>dsx/spec.py, tests/test_dsx.py</files>
  <read_first>
    - `dsx/spec.py` lines 200-256 (`validate_structure`, the dispatch point) and 422-479
      (`_validate_design_shape`, the shape idiom to mirror: read the section, `normalize`, test
      membership, `report.add` with `detail`/`remedy`/`where`)
    - `dsx/spec.py` lines 143-198 (`get`, `section`, `items`, `is_blank`, `as_number`, `normalize`)
      — the helper set the new validators use
    - `dsx/checks/claims.py` lines 496-516 (`_check_limitations_required` / `DSX-CLM-080`) — the
      closest existing question_type-gated requiredness shape, and note the two differences: it
      gates one flat field, and it runs only under `strict` at verify/ship, which the new validator
      must NOT do
    - `dsx/checks/design.py` lines 477-507 (`_check_identification` / `DSX-CAU-010`) — the existing
      precedent for CRITICAL requiredness gated on `question_type in ("causal", "prescriptive")`
    - `.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-PATTERNS.md`
      section on the validators, including the explicit note that the RESEARCH.md sketch shows one
      aggregate `report.add` for the block-present branch where D-11 requires one per sub-block
    - `.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-CONTEXT.md`
      locked decision R-01
  </read_first>
  <behavior>
    - A causal spec with no `validity_frame` key produces exactly one `DSX-SPEC-080` at CRITICAL
      whose `detail` names all ten required sub-blocks
    - A descriptive spec with no `validity_frame` key produces exactly one `DSX-SPEC-080` whose
      `detail` names the six always-required sub-blocks and none of the causal-only four
    - A causal spec whose `validity_frame` is present but missing three required sub-blocks
      produces exactly three `DSX-SPEC-081` findings at CRITICAL, each with
      `where == "spec.validity_frame.<name>"`
    - A descriptive spec carrying only the six always-required sub-blocks produces no
      `DSX-SPEC-080` and no `DSX-SPEC-081`
    - A spec with `question_type: descriptive` and `design.kind: experiment` and no `interference`
      sub-block produces a `DSX-SPEC-081` for `interference`
    - A sub-block value outside its closed vocabulary produces a `DSX-SPEC-082` at HIGH whose
      `detail` lists the allowed members and whose `where` names the dotted path
    - A `validity_frame` present as a non-dict (a string, a list, an empty dict) is treated as
      absent and produces `DSX-SPEC-080`, not a crash
    - The extended `examples/good-ANALYSIS-SPEC.yaml` produces none of the three codes
  </behavior>
  <action>
    Add two module-level tuples beside the existing vocabulary constants:
      - `_VALIDITY_FRAME_ALWAYS_REQUIRED = ("estimand", "units", "measurement", "dependence",
        "sampling_frame", "missingness")`
      - `_VALIDITY_FRAME_CAUSAL_REQUIRED = ("identification", "interference", "triggering",
        "stability")`
    These lists are locked by decision R-01 and are not the planner's to re-derive. R-01 diverges
    deliberately from `research/PITFALLS.md` Pitfall 2, which placed `interference` in a
    conditional tier and `identification` in the causal-only tier: REQ-P6-03 and ROADMAP Success
    Criterion 2 are the binding sources and both put `interference` in the causal-only list. Carry
    that note as an inline comment so a later reader does not "fix" it back.

    Add `_validate_validity_frame_shape(spec, report) -> None` and call it from
    `validate_structure()` alongside the five existing `_validate_*` calls. No `CHECKS` entry and
    no `GATE_PROFILES` edit: `spec` is already in all four profiles, so severity alone selects the
    gate point (D-09).

    Requiredness gating: compute
    `needs_causal_block = normalize(spec.get("question_type","")) in ("causal", "prescriptive") or
    normalize(get(spec, "design.kind", "")) == "experiment"`. `prescriptive` is included because
    `QUESTION_TYPES` documents it as requiring a causal estimate plus a decision rule, and because
    `_check_identification` already gates its own causal requiredness on exactly
    `("causal", "prescriptive")` — reusing that predicate keeps one rule in the codebase rather
    than two. `QUESTION_TYPES` has no `experimental` member, so the experimental half of R-01 is
    expressed through `design.kind`. Record this predicate choice in the plan summary; it is the
    one part of R-01 the locked decision did not pin.

    Finding granularity, per D-11:
      - block entirely absent, or present but not a non-empty dict -> **one** `DSX-SPEC-080` at
        CRITICAL, `detail` itemising every required sub-block, `where="spec.validity_frame"`,
        `remedy` pointing at `templates/ANALYSIS-SPEC.yaml`. Then return.
      - block present, required sub-blocks missing -> **one `DSX-SPEC-081` per missing sub-block**
        at CRITICAL, each with `where=f"spec.validity_frame.{name}"` and a `remedy` naming what
        that sub-block must declare. Do not collapse these into one aggregate call — the RESEARCH
        sketch shows a single call here and is wrong on this point; a legacy spec is grandfathered
        with one `suppressions[]` entry in the absent case, and once migration starts the findings
        get specific.
      - all required sub-blocks present -> `report.ok("validity_frame required sub-blocks present")`
    Then, independently of requiredness, run per-field membership checks on whichever sub-blocks
    are present, mirroring `_validate_design_shape`'s per-field idiom: `normalize` the declared
    value, test membership, and emit `DSX-SPEC-082` at HIGH with `detail` listing the sorted
    allowed members. Fields and their vocabularies: `identification.strength` ->
    `IDENTIFICATION_STRENGTHS`; `identification.constraint_source` -> `CONSTRAINT_SOURCES`;
    `dependence.structure` -> `DEPENDENCE_STRUCTURES`; `dependence.method_family_required` ->
    `VARIANCE_ADJUSTMENTS` (M-09, reused, no parallel set); `interference.risk` ->
    `INTERFERENCE_RISKS`; `interference.mitigation` -> `INTERFERENCE_MITIGATIONS`;
    `triggering.analysis_population` -> `ANALYSIS_POPULATIONS`; `missingness.mechanism` ->
    `MISSINGNESS_MECHANISMS`. A blank or absent sub-field is not a membership error — Phase 7 owns
    content completeness; this validator owns shape.

    D-05 markers, mandatory because plan 06-03's allow-list covers `DSX-SPEC-08`: give the function
    a docstring carrying a `Citation:` line naming author, year, work and the exact formulation,
    and a `Structural criterion:` line stating that the check is a presence-and-membership test
    rather than a numeric one. Anchor the citation in `brief.md` section 7's reference list —
    Hernán & Robins (2020), *Causal Inference: What If*, for the estimand specification the frame
    encodes, and Little & Rubin (2019) / Imbens & Rubin (2015) for the missingness and interference
    vocabularies the membership half enforces. Name the exact chapter. **If you cannot verify the
    exact chapter of an anchored work, stop and escalate in the plan summary rather than inventing
    a reference** — a fabricated but well-formed citation is precisely the failure D-05 exists to
    prevent, and vendor blogs and Medium posts are inadmissible in either direction.

    Add the eight behaviours above as tests in `tests/test_dsx.py`'s `TestSpecStructure`, building
    minimal spec dicts inline and calling `validate_structure(spec)` directly. Put a
    `# D-05: DSX-SPEC-080`, `# D-05: DSX-SPEC-081` and `# D-05: DSX-SPEC-082` marker comment on the
    tests that assert each code, so plan 06-03's test-linkage check resolves. Do not modify
    `tests/test_dsx.py` lines 804-839.
  </action>
  <acceptance_criteria>
    - `python3 -c "from dsx.spec import validate_structure as v; r=v({'spec_version':1,'title':'t','question_type':'causal','decision':{'owner':'x'}}); c=[f for f in r.findings if f.code=='DSX-SPEC-080']; assert len(c)==1; d=c[0].detail; assert all(k in d for k in ('estimand','units','measurement','dependence','sampling_frame','missingness','identification','interference','triggering','stability'))"` exits 0
    - `python3 -c "from dsx.spec import validate_structure as v; r=v({'spec_version':1,'title':'t','question_type':'descriptive','decision':{'owner':'x'}}); c=[f for f in r.findings if f.code=='DSX-SPEC-080']; assert len(c)==1; d=c[0].detail; assert 'interference' not in d and 'triggering' not in d and 'stability' not in d and 'identification' not in d"` exits 0
    - `python3 -c "from dsx.spec import validate_structure as v; base={'spec_version':1,'title':'t','question_type':'causal','decision':{'owner':'x'},'validity_frame':{k:{'a':1} for k in ('estimand','units','measurement','dependence','sampling_frame','missingness','identification')}}; r=v(base); f=[x for x in r.findings if x.code=='DSX-SPEC-081']; assert len(f)==3, [x.where for x in f]; assert {x.where for x in f}=={'spec.validity_frame.interference','spec.validity_frame.triggering','spec.validity_frame.stability'}"` exits 0
    - `python3 -c "from dsx.spec import validate_structure as v; s={'spec_version':1,'title':'t','question_type':'descriptive','decision':{'owner':'x'},'validity_frame':{k:{'a':1} for k in ('estimand','units','measurement','dependence','sampling_frame','missingness')}}; r=v(s); assert not [x for x in r.findings if x.code in ('DSX-SPEC-080','DSX-SPEC-081')]"` exits 0
    - `python3 -c "from dsx.spec import validate_structure as v; s={'spec_version':1,'title':'t','question_type':'descriptive','decision':{'owner':'x'},'validity_frame':{**{k:{'a':1} for k in ('estimand','units','measurement','sampling_frame','missingness')},'dependence':{'structure':'not_a_member'}}}; r=v(s); f=[x for x in r.findings if x.code=='DSX-SPEC-082']; assert len(f)==1 and f[0].severity.label=='HIGH' and f[0].where=='spec.validity_frame.dependence.structure'"` exits 0
    - `python3 -c "from dsx.spec import validate_structure as v; [v({'spec_version':1,'title':'t','question_type':'causal','decision':{'owner':'x'},'validity_frame':b}) for b in ('a string', [], {}, None)]"` exits 0 with no exception
    - `python3 -m dsx gate plan --spec examples/good-ANALYSIS-SPEC.yaml` exits 0, and the same for `execute`, `verify` and `ship`
    - `python3 -m dsx gate plan --spec examples/bad-ANALYSIS-SPEC.yaml` exits 1
    - `python3 -m unittest discover -s tests` exits 0
    - `git diff --stat dsx/checks/` reports no changes
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s tests && for p in plan execute verify ship; do python3 -m dsx gate $p --spec examples/good-ANALYSIS-SPEC.yaml || exit 1; done</automated>
  </verify>
  <done>`validity_frame` requiredness is gated by question_type per R-01, absence yields one itemised CRITICAL finding and a present-but-incomplete block yields one CRITICAL per sub-block, membership violations yield HIGH findings naming the allowed values, and the good fixture still passes every gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: inference block shape and the removed-field redirect (REQ-P6-04)</name>
  <files>dsx/spec.py, tests/test_dsx.py</files>
  <read_first>
    - `dsx/spec.py` `_validate_design_shape` (422-479) again, and `_validate_validity_frame_shape`
      as written in Task 1 — the sibling this function mirrors
    - `dsx/spec.py` new vocabulary constants `PARADIGMS`, `PARADIGM_JUSTIFICATIONS` and
      `DECLARATION_POINTS` as landed by plan 06-01
    - `brief.md` lines 188-215 (section 5.2) — the six fields that survive, and the field M-02
      removed
    - `.planning/REQUIREMENTS.md` line 80 (REQ-P6-04) — the literal six-field list, and the clause
      placing the stopping-rule concept in the existing `design.peeking_policy`
  </read_first>
  <behavior>
    - An absent `inference:` block produces no finding — the block is optional at the structural
      layer, like `design:`
    - `inference.paradigm` outside `PARADIGMS` produces one `DSX-SPEC-085` at HIGH naming the
      allowed members, with `where == "spec.inference.paradigm"`
    - `inference.paradigm_justification` outside `PARADIGM_JUSTIFICATIONS` produces one
      `DSX-SPEC-085` at HIGH
    - `inference.declared_at` outside `DECLARATION_POINTS` produces one `DSX-SPEC-085` at HIGH
    - Declaring the field M-02 removed produces exactly one `DSX-SPEC-086` at HIGH whose `remedy`
      names `design.peeking_policy` as where the concept now lives
    - A clean `inference:` block matching the extended good fixture produces neither code
    - `_INFERENCE_FIELDS` contains exactly the six REQ-P6-04 field names
  </behavior>
  <action>
    Add `_INFERENCE_FIELDS = ("paradigm", "paradigm_justification", "declared_at",
    "primary_procedure", "alpha_spending", "fallback_rule")` as a module-level tuple — the
    machine-readable statement of REQ-P6-04's field list, which the round-trip test in plan 06-05
    and the catalogue can both read.

    Add `_validate_inference_shape(spec, report) -> None`, registered in `validate_structure()`'s
    call chain immediately after the validity-frame validator. Return early when the section is
    absent or is not a dict: an absent optional section is not itself an error, which is
    `_validate_design_shape`'s established behaviour and the right one here (no REQ-P6-* makes the
    `inference:` block required, and REQ-P6-03's requiredness gating is scoped to `validity_frame`
    only).

    Emit `DSX-SPEC-085` at HIGH for each of the three vocabulary-typed fields whose declared,
    non-blank value falls outside its constant, with `detail` listing the sorted allowed members
    and `where` naming the dotted path. `primary_procedure`, `alpha_spending` and `fallback_rule`
    are free-form at this layer — Phase 10 parses `fallback_rule` and Phase 9 reads
    `alpha_spending`; neither is this phase's business.

    Emit `DSX-SPEC-086` at HIGH when the `inference:` block declares the field M-02 removed. HIGH,
    not CRITICAL: it does not invalidate the analysis, but a field name the tool silently ignores
    is worse than one it rejects, and an operator who copies brief section 5.2 verbatim gets a
    finding that redirects them rather than silence. The `remedy` names `design.peeking_policy` as
    the single source for the stopping-rule concept (M-02), and the `detail` states that no
    equivalent field exists under `inference:`.

    D-05 markers on this function's docstring, same rules as Task 1: a `Citation:` line and a
    `Structural criterion:` line. Anchor the citation in Deng, Lu & Chen (2016), "Continuous
    Monitoring of A/B Tests without Pain" — the primary source establishing that a decision
    procedure's error rate depends on the declared paradigm, which is why the paradigm must be
    declared rather than assumed. Name the exact section or theorem. Do not fabricate; escalate in
    the summary if the exact locator cannot be verified.

    Add the seven behaviours above as tests in `TestSpecStructure`, with `# D-05: DSX-SPEC-085` and
    `# D-05: DSX-SPEC-086` marker comments on the tests asserting each code. The `DSX-SPEC-086`
    test builds a synthetic spec dict inline — it deliberately does not use
    `examples/bad-ANALYSIS-SPEC.yaml`, because D-07 fences that fixture off from the peeking axis.
  </action>
  <acceptance_criteria>
    - `python3 -c "from dsx.spec import _INFERENCE_FIELDS as F; assert F == ('paradigm','paradigm_justification','declared_at','primary_procedure','alpha_spending','fallback_rule'), F"` exits 0
    - `python3 -c "from dsx.spec import validate_structure as v; base={'spec_version':1,'title':'t','question_type':'descriptive','decision':{'owner':'x'},'validity_frame':{k:{'a':1} for k in ('estimand','units','measurement','dependence','sampling_frame','missingness')}}; r=v(dict(base, inference={'paradigm':'freqentist','paradigm_justification':'nope','declared_at':'whenever'})); f=[x for x in r.findings if x.code=='DSX-SPEC-085']; assert len(f)==3, [x.where for x in f]; assert all(x.severity.label=='HIGH' for x in f)"` exits 0
    - `python3 -c "from dsx.spec import validate_structure as v; base={'spec_version':1,'title':'t','question_type':'descriptive','decision':{'owner':'x'},'validity_frame':{k:{'a':1} for k in ('estimand','units','measurement','dependence','sampling_frame','missingness')}}; r=v(dict(base, inference={'paradigm':'frequentist','stopping_rule':'fixed_horizon'})); f=[x for x in r.findings if x.code=='DSX-SPEC-086']; assert len(f)==1; assert 'design.peeking_policy' in f[0].remedy"` exits 0
    - `python3 -c "from dsx.spec import validate_structure as v; base={'spec_version':1,'title':'t','question_type':'descriptive','decision':{'owner':'x'},'validity_frame':{k:{'a':1} for k in ('estimand','units','measurement','dependence','sampling_frame','missingness')}}; assert not [x for x in v(base).findings if x.code in ('DSX-SPEC-085','DSX-SPEC-086')]"` exits 0
    - `python3 -m dsx gate ship --spec examples/good-ANALYSIS-SPEC.yaml` exits 0
    - `python3 -m unittest discover -s tests` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s tests && python3 -m dsx gate ship --spec examples/good-ANALYSIS-SPEC.yaml</automated>
  </verify>
  <done>The `inference:` block is shape-validated against its three closed vocabularies, the field M-02 removed produces a redirect finding rather than silence, and `_INFERENCE_FIELDS` is the machine-readable statement of REQ-P6-04's field list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emit decision records from the structural adjudications and regenerate the catalogue (REQ-P6-02, REQ-P6-03, REQ-P6-04)</name>
  <files>dsx/spec.py, tests/test_dsx.py, references/finding-codes.md</files>
  <read_first>
    - `dsx/decisions.py` as landed by plan 06-02 — `DecisionRecord`'s field list and
      `collect_from_report`'s expectation that producers append plain dicts onto
      `report.context["decisions"]`
    - `dsx/findings.py` lines 91-100 (`Report.context`) and 202-207 (`merge`)
    - `brief.md` lines 246-266 (section 5.5) — the record shape, and why `counterfactual` is the
      field that teaches
    - `.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-CONTEXT.md`
      decisions D-13 (only the new surface emits records; the fifteen existing check modules are
      untouched) and D-19 (`layer: deterministic` only)
  </read_first>
  <behavior>
    - `validate_structure(spec)` for a spec carrying a `validity_frame` leaves exactly one
      requiredness decision record in `report.context["decisions"]`
    - That record's `layer` is `deterministic`, its `inputs` name `question_type` and
      `design.kind`, its `rule` names the requiredness rule, and its `counterfactual` states what
      would have to differ for the causal-only four not to be required
    - `validate_structure(spec)` for a spec carrying an `inference` block leaves a second record
      naming the declared paradigm
    - Records carry empty `id` and `invocation_id` placeholders — the CLI assigns them at write time
    - `collect_from_report(merge("x", [report]))` returns both records in order
    - No module under `dsx/checks/` appends to `report.context["decisions"]`
  </behavior>
  <action>
    Have both new validators append one `DecisionRecord(...).to_dict()` each onto
    `report.context.setdefault("decisions", [])`. Construct the records with `id=""` and
    `invocation_id=""`; the CLI assigns the sequential `DEC-NNN` id and the invocation id at write
    time via `dataclasses.replace` (plan 06-09), because a pure validator does not know the
    invocation's record sequence and must not perform file I/O. Import `DecisionRecord` inside the
    function body, not at module scope, to keep `dsx/spec.py`'s import surface unchanged for the
    fifteen check modules that import from it.

    The requiredness record: `layer="deterministic"`, `choice` naming the computed required
    sub-block list, `inputs=["question_type", "design.kind"]`, `rule` restating the R-01 gating
    rule, `citation` matching the validator docstring's `Citation:` line, and `counterfactual`
    stating that a descriptive, non-experimental question would not require `identification`,
    `interference`, `triggering` or `stability`. `alternatives_rejected` and `confidence` stay at
    their defaults — those are stochastic-entry fields (brief section 5.5) and the gate emits
    `layer: deterministic` only (D-19).

    The inference record: `choice` naming the declared paradigm (or that none was declared),
    `inputs=["inference.paradigm"]`, `rule` naming the closed vocabulary, and a `counterfactual`
    stating which check families the other paradigm would have selected — the same information
    `DSX-PAR-001` renders, recorded at the point the paradigm is read.

    Do not touch any module under `dsx/checks/` (D-13). Their emitting records is each family's own
    work when it ships, which is how Phases 7 through 12 inherit the pattern, and keeping the
    Phase 6 diff off the v1.5.0 surface is what makes D-08's two exit-code tests provably unaffected.

    Regenerate the finding catalogue: run `python3 scripts/gen-finding-catalogue.py --write` and
    commit the updated `references/finding-codes.md`, so
    `python3 scripts/gen-finding-catalogue.py --check` — which now also runs the D-05 enforcement
    from plan 06-03 over the new `DSX-SPEC-08` codes — exits 0 at the end of this wave rather than
    only at the end of the phase.

    Add the six behaviours above as tests in `TestSpecStructure`. The last one is a guard test that
    reads every `dsx/checks/*.py` source and asserts none of them appends to a decisions list, so
    D-13's boundary is mechanical rather than a convention.
  </action>
  <acceptance_criteria>
    - `python3 -c "from dsx.spec import validate_structure as v; s={'spec_version':1,'title':'t','question_type':'causal','decision':{'owner':'x'},'validity_frame':{k:{'a':1} for k in ('estimand','units','measurement','dependence','sampling_frame','missingness','identification','interference','triggering','stability')},'inference':{'paradigm':'bayesian'}}; r=v(s); d=r.context['decisions']; assert len(d)==2, d; assert all(x['layer']=='deterministic' for x in d); assert all(x['id']=='' and x['invocation_id']=='' for x in d); assert d[0]['counterfactual'].strip() and d[0]['rule'].strip()"` exits 0
    - `python3 -c "from dsx.spec import validate_structure as v; from dsx.findings import merge; from dsx.decisions import collect_from_report; s={'spec_version':1,'title':'t','question_type':'causal','decision':{'owner':'x'},'validity_frame':{k:{'a':1} for k in ('estimand','units','measurement','dependence','sampling_frame','missingness','identification','interference','triggering','stability')},'inference':{'paradigm':'bayesian'}}; m=merge('x',[v(s)]); assert len(collect_from_report(m))==2"` exits 0
    - `python3 scripts/gen-finding-catalogue.py --check` exits 0
    - `python3 -c "import importlib.util; spec=importlib.util.spec_from_file_location('g','scripts/gen-finding-catalogue.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); codes={r[0] for r in m.collect()}; need={'DSX-SPEC-080','DSX-SPEC-081','DSX-SPEC-082','DSX-SPEC-085','DSX-SPEC-086'}; assert need <= codes, need - codes; assert m.check_d05(m.collect(), m.ROOT/'dsx', m.ROOT/'tests') == []"` exits 0
    - `python3 -c "import pathlib; t=pathlib.Path('references/finding-codes.md').read_text(encoding='utf-8'); assert all(c in t for c in ('DSX-SPEC-080','DSX-SPEC-081','DSX-SPEC-082','DSX-SPEC-085','DSX-SPEC-086'))"` exits 0
    - `git diff --stat dsx/checks/` reports no changes
    - `python3 -m unittest discover -s tests` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s tests && python3 scripts/gen-finding-catalogue.py --check</automated>
  </verify>
  <done>Both structural adjudications leave `layer: deterministic` decision records on the report context with the id fields left for the CLI to assign, no existing check module emits records, and the regenerated catalogue passes both the staleness check and the D-05 enforcement for all five new codes.</done>
</task>

</tasks>

<flagged_assumptions>
Spec-less edge-probe rows that remain `unresolved` and are surfaced here rather than silently
dropped or auto-backstopped:

- **REQ-P6-03 (unclassified, unresolved).** R-01 locks *which* sub-blocks sit in the always-required
  and causal-only lists, but does not pin the predicate that decides whether a spec is
  "causal/experimental". `QUESTION_TYPES` has no `experimental` member, so this plan uses
  `question_type in ("causal", "prescriptive") or design.kind == "experiment"`, reusing
  `_check_identification`'s existing predicate. Residual risk: a `predictive` question with a
  causal decision rule slips past the causal-only four. `DSX-CLM-080` uses a wider set
  (`causal`, `prescriptive`, `predictive`) for its own requiredness, so the two rules are not
  identical, and whether they should be is a modelling call best made against real specs.
- **REQ-P6-04 (unclassified, unresolved).** The probe surfaced no shape for the `inference:` block.
  Residual risk: `declared_at` is an operator self-declaration that nothing can verify —
  `DECLARATION_POINTS` checks that the declared value is one of two legal strings, not that the
  declaration is true. Phase 10 REQ-P10-02 documents this limit rather than closing it, and no
  check in any phase can close it.
</flagged_assumptions>

<edge_coverage_accounting>
Phase-wide spec-less edge probe: **27 applicable rows, 0 resolved on arrival.**
Resolution across the whole plan set: **17 authored into `must_haves`** (15 `covered` plain-string
truths + 2 `backstop` structured markers) **+ 10 surfaced as flagged assumptions** = **27**.
No row was dismissed and no `unclassified` row was auto-backstopped.

This plan carries **0** of the 17 authored rows and **2** of the 10 flagged assumptions
(REQ-P6-03, REQ-P6-04).
</edge_coverage_accounting>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-authored `ANALYSIS-SPEC.yaml` -> `validate_structure()` | The untrusted-input boundary these validators sit on; malformed or hostile shapes must produce findings, never exceptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6-12 | Denial of Service | `_validate_validity_frame_shape` | medium | mitigate | A `validity_frame` declared as a string, a list, `null` or an empty dict must degrade to `DSX-SPEC-080` rather than raising, because an unhandled exception in a structural validator turns a block (exit 1) into a could-not-run (exit 2) and changes the gate's route. ASVS V5. Asserted by the malformed-shape acceptance criterion covering all four non-dict cases. |
| T-6-07 | Spoofing | `Citation:` lines on the new codes | medium | mitigate | A fabricated but well-formed citation would launder model knowledge into a blocking gate. Mitigated by anchoring each citation in `brief.md` section 7's reference list, by requiring the exact chapter or theorem, by the explicit instruction to escalate rather than invent when a locator cannot be verified, and by the `Structural criterion:` half plus the `# D-05:` linked test that plan 06-03's `--check` enforces. |
| T-6-13 | Tampering | the D-13 boundary | medium | mitigate | Emitting decision records from an existing check module would put the Phase 6 diff on the v1.5.0 surface and break the argument that D-08's two exit-code tests are provably unaffected. Mitigated by the source-level guard test asserting no `dsx/checks/*.py` module appends to a decisions list, plus a `git diff --stat dsx/checks/` criterion on every task. |
| T-6-SC | Tampering | package-manager installs | low | accept | Zero third-party installs ship in this phase (D-01; `06-RESEARCH.md` Package Legitimacy Audit records the gate as not applicable). |
</threat_model>

<verification>
- `python3 -m unittest discover -s tests` exits 0
- `python3 scripts/gen-finding-catalogue.py --check` exits 0 (both the staleness half and the D-05
  half)
- `python3 -m dsx gate {plan,execute,verify,ship} --spec examples/good-ANALYSIS-SPEC.yaml` all exit 0
- `python3 -m dsx gate plan --spec examples/bad-ANALYSIS-SPEC.yaml` exits 1 with `DSX-SPEC-081` in
  the output
- `python3 -m dsx gate ship --spec examples/bad-ANALYSIS-SPEC.yaml` exits 1
- `git diff --stat dsx/checks/ dsx/cli.py` reports no changes
- `git diff -U0 tests/test_dsx.py` shows no deletion or modification inside the original 804-839 range
</verification>

<success_criteria>
- REQ-P6-02: the `validity_frame:` block's ten sub-blocks are shape-validated against their closed
  vocabularies
- REQ-P6-03: sub-block requiredness is gated by question_type per locked decision R-01 — six always
  required, four required only for causal/experimental — with absence producing one itemised
  CRITICAL finding and a present-but-incomplete block producing one CRITICAL per sub-block (D-11)
- REQ-P6-04: the `inference:` block's three vocabulary-typed fields are validated, and declaring the
  field M-02 removed produces a redirect to `design.peeking_policy` rather than silence
- A descriptive-question spec omitting `interference`/`triggering`/`stability` entirely exits 0
  while a causal spec omitting them blocks (ROADMAP Success Criterion 2)
- Every new `DSX-SPEC-08x` code satisfies both halves of D-05 mechanically, proven by
  `gen-finding-catalogue.py --check` exiting 0
</success_criteria>

<output>
Create `.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-06-SUMMARY.md` when done
</output>
