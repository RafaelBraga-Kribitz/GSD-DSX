---
phase: 30
plan: 30-02
type: execute
wave: 2
depends_on: [30-01]
requirements: [REQ-P30-01, REQ-P30-02, REQ-P30-03]
files_modified:
  - brief.md
  - docs/literature/the-ai-data-scientist.md
  - tests/test_literature_corpus_count_agreement.py
autonomous: true

must_haves:
  truths:
    - "brief.md §6.5's calibration-backdrop paragraph reads the Phase-30 numbers (re-measured headline pair, FPR over /15, 5-miss partition) with a dated Phase-30 re-evaluation note; the item-7/8/9 entry rows and re-evaluation bullets are verified consistent with the re-baseline (item 7 = DSX-ML-034 miss, item 8 = DSX-CLM-034 miss, item 9 = DSX-COH-041 target catch)."
    - "docs/literature/the-ai-data-scientist.md lists items 7/8/9 as Satisfied (code+fixture named, one line each) out of the 'What is deferred' table; mapping-table row 15 reads 42 known-bad / 15 good-control dated to the Phase-30 re-baseline; the deferred status cells for rows 7, 8 and 12 are flipped to present."
    - "A lightweight agreement test pins the literature row-15 corpus counts to the live example globs and passes on the real 3.12.10 interpreter."
    - "On the FINAL tree from a clean tree on the real 3.12.10 interpreter: scripts/gen-finding-catalogue.py --check exits 0 with set-identity 279 -> 279 (added={} removed={}); the frozen count-pin/snapshot tests and doc/code agreement tests are green; node install.mjs --check passes; scripts/check.sh is green; the full suite is green."
  artifacts:
    - "brief.md (refreshed §6.5 calibration backdrop + dated Phase-30 re-evaluation note)"
    - "docs/literature/the-ai-data-scientist.md (items 7/8/9 Satisfied; row-15 counts 42/15; status cells flipped)"
    - "tests/test_literature_corpus_count_agreement.py (new agreement gate, REQ-P30-02 gap closure)"
  key_links:
    - "literature row-15 known-bad count == len(glob(examples/known-bad/*-ANALYSIS-SPEC.yaml)) and good-control count == len(glob(examples/good-corpus/*-ANALYSIS-SPEC.yaml)) — pinned by the new test."
    - "brief §6.5 backdrop numbers == 30-READOUT.md measured numbers (from plan 30-01)."
    - "catalogue set-identity 279 -> 279 (zero mint, REQ-P30-03)."
---

<objective>
Refresh the two stale calibration records to the Phase-30 measured re-baseline, close the
one genuine REQ-P30-02 audit-prerequisite gap with a single lightweight agreement test, and
run the full milestone-audit verification battery on the final tree.

Purpose: The durable records must cite the terminal re-baseline (from plan 30-01), not the
Phase-12 numbers, under the D-13 measured-evidence rule (code + fixture named, never an
estimate). REQ-P30-02 confirms the tree is audit-ready; REQ-P30-03 confirms zero mint.

Output:
- brief.md §6.5 refreshed (calibration backdrop + item-7/8/9 verification)
- docs/literature/the-ai-data-scientist.md updated (items 7/8/9 Satisfied; counts 42/15)
- tests/test_literature_corpus_count_agreement.py (the ONE new agreement gate)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-calibration-rebaseline/30-CONTEXT.md
@.planning/phases/30-calibration-rebaseline/30-READOUT.md
@brief.md
@docs/literature/the-ai-data-scientist.md
@tests/test_doc_code_agreement.py

Interpreter (bind once): REAL 3.12.10 =
C:\Users\Benutzer1\AppData\Local\Programs\Python\Python312\python.exe. NEVER use `python3`
(a stub that skips the matplotlib determinism test).

Grep-anchor, do NOT hard-code line numbers (files may shift a few lines before this runs).
Current anchors observed at plan time:
- brief.md: item-7/8/9 entry rows near :376-378 (already "Satisfied (Phase 27/28/29)"); the
  Phase-12 re-evaluation bullets near :421-437; the STALE calibration-backdrop paragraph
  near :439-445 (reads "(miss-rate 1.0, FPR 0.0) ... twelve-spec good-control corpus (plan
  12-05, 0/12) ... a 3/3 miss").
- docs/literature/the-ai-data-scientist.md: mapping-table rows near :35 (row 7 "provenance
  list deferred"), :38 (row 8 "residual deferred"), :40 (row 12 subgroup "gate deferred"),
  :43 (row 15 "As of 2026-09-06: 39 known-bad specs, 15 good-control specs"); the "What is
  deferred" section header near :74, its "As of 2026-09-06 none ... is met" line near :79,
  and its three-row table near :83-85 (item 7, item 8, item 9).

Known tree/interpreter HAZARDS (handle before the verification battery):
- A stray root DECISIONS.jsonl false-fails two `explain` tests — delete it first (untracked
  cleanup, not a tracked-file edit).
- The bare `python3` stub reports the matplotlib determinism test as SKIPPED — the
  full-suite run must use the real 3.12.10 interpreter so that test actually runs.
- scripts/check.sh invokes `python3` internally; run it as the canonical gate AND run the
  full unittest suite separately on the real interpreter to exercise the matplotlib test.
</context>

<constraints>
SINGLE-WRITER PROHIBITION. This plan MUST NOT modify REQUIREMENTS.md, .planning/STATE.md,
or .planning/ROADMAP.md. Those tracking files are orchestrator-only; a subagent never
edits them. They may be read but never appear in files_modified.

FROZEN SURFACES (must stay byte-identical). dsx/checks/dq.py, dsx/cli.py,
dsx/checks/viz.py, every fixture under examples/known-bad/ and examples/good-corpus/, the
finding catalogue references/finding-codes.md, and the chart corpus. This plan edits ONLY
record files (brief.md, docs/literature/...) and adds ONE test file. No fixture, check, or
catalogue edit.

ZERO MINT. This plan mints no finding codes. REQ-P30-03 is confirmed here by the
set-identity gate (279 -> 279).

INSTALLER SCOPE. The edited docs live under brief.md and docs/ — NEITHER is under the
installer's tracked trees (skills/, agents/, templates/, references/), and the new test
lives under tests/. So no installer re-generation is required. `node install.mjs --check`
is run only as a read-only verification that nothing under the installer's trees drifted;
the mutating `node install.mjs` is NOT run.
</constraints>

<decision_coverage>
- GA-3 (the two required doc rewrites, D-13 measured-evidence rule): both rewrites are
  D-13-honest — each promoted item names its code and fixture and cites its measured
  LIVE-MISS / TARGET evidence (item 7 -> DSX-ML-034 / feature-origin-only-leak miss; item 8
  -> DSX-CLM-034 / magnitude-without-computed-effect miss; item 9 -> DSX-COH-041 /
  subgroup-harm-without-disposition target). No fixture/check/catalogue edit.
- GA-4 (REQ-P30-02 verify-not-build; close exactly the one genuine gap):
  DECISION = (a). A search at plan time confirmed NO existing test asserts the literature
  record's corpus counts track the live corpus glob (test_doc_code_agreement covers
  decision-table/membership only; test_agreement_completeness_gate's "deferred" is an
  unrelated DSX-STA-063 docstring; test_selection_heuristic_docs is unrelated). The gap is
  genuine, so it is closed with ONE lightweight, non-speculative agreement test that pins
  the row-15 corpus counts to len(glob(...)) and NO more — this is exactly what let the doc
  drift stale (39 vs live 42) and is the smaller, provable claim (brief). No speculative
  test; the rest of REQ-P30-02 is verification of existing gates.
- D-06 / REQ-P30-03 (zero mint): the set-identity gate confirms 279 -> 279, added={}
  removed={}. Re-measure the live count at execution — do not assume 279 has held.
- D-10 / D-13 framing carried into brief §6.5: the backdrop refresh states the pair
  (miss-rate, FPR /15), the 5-miss partition, and the construction-invariant reading; item
  9 is a PRESENT/DETECTED catch, items 7/8 attributed misses.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Refresh brief.md §6.5 to the Phase-30 re-baseline (grep-anchored)</name>
  <files>brief.md</files>
  <action>
Read the Phase-30 numbers from .planning/phases/30-calibration-rebaseline/30-READOUT.md
(dependency 30-01). Grep-anchor every edit; do not hard-code line numbers.

(1) VERIFY the item-7/8/9 entry rows (grep the "Feature-provenance per-feature list",
"Magnitude-without-computed-effect residual", and "Subgroup-harm declaration" rows) already
match the measured re-baseline: item 7 Satisfied as an attributed miss via DSX-ML-034 on
feature-origin-only-leak; item 8 Satisfied as an attributed miss via DSX-CLM-034 on
magnitude-without-computed-effect; item 9 Satisfied as a PRESENT/DETECTED catch via
DSX-COH-041 on subgroup-harm-without-disposition. If any row contradicts the re-baseline,
correct it to the measured disposition; if consistent, leave byte-identical (record in the
summary that they were verified, not rewritten).

(2) REFRESH the stale calibration-backdrop paragraph (grep the paragraph containing
"twelve-spec good-control corpus" / "0/12" / "3/3 miss"). Replace its Phase-12 numbers with
the Phase-30 measured numbers from the readout: the re-measured headline pair, the FPR over
the FIFTEEN-spec good-control corpus, and the FIVE-case miss partition. Preserve the dated
Phase-12 re-evaluation section as history: add a dated "Phase 30 re-evaluation (terminal
re-baseline)" note so the durable record shows the terminal state and its date, rather than
silently overwriting the 2026-08-27 record. The refreshed paragraph must carry the D-10/F3
framing (the miss-rate is a construction invariant of a curated partition; the headline is
the pair; the FPR is bounded, not a point estimate) and must name the two new misses
(DSX-ML-034, DSX-CLM-034) and the one new target (DSX-COH-041).

After editing, grep the refreshed backdrop passage to confirm NO stale Phase-12 calibration
tokens survive in it (the "twelve-spec", "0/12", and "3/3 miss" phrasings must be gone from
the current-state backdrop; they may remain only inside the clearly dated 2026-08-27
historical block).
  </action>
  <verify>
    <automated>& "C:\Users\Benutzer1\AppData\Local\Programs\Python\Python312\python.exe" -c "import re,sys; t=open('brief.md',encoding='utf-8').read(); ok = ('DSX-ML-034' in t) and ('DSX-CLM-034' in t) and ('DSX-COH-041' in t) and re.search(r'Phase\s*30\s*re-evaluation', t, re.I) and re.search(r'(fifteen|15)[- ]spec', t, re.I); sys.exit(0 if ok else 1)"</automated>
  </verify>
  <done>brief.md §6.5 has a dated "Phase 30 re-evaluation" note; the current-state
calibration backdrop reads the Phase-30 numbers (headline pair, FPR over /15, 5-miss
partition) and names DSX-ML-034, DSX-CLM-034, DSX-COH-041; the item-7/8/9 entry rows are
verified consistent with the re-baseline; no stale "twelve-spec / 0/12 / 3/3 miss" phrasing
remains outside the dated historical block.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/literature/the-ai-data-scientist.md (items 7/8/9 Satisfied; counts 42/15)</name>
  <files>docs/literature/the-ai-data-scientist.md</files>
  <action>
Grep-anchor every edit; do not hard-code line numbers.

(1) "What is deferred" table: move items 7, 8 and 9 OUT of the deferred table into a
Satisfied disposition, one line each, code + fixture named and its measured evidence cited:
item 7 -> DSX-ML-034 / feature-origin-only-leak (attributed LIVE MISS); item 8 ->
DSX-CLM-034 / magnitude-without-computed-effect (attributed LIVE MISS); item 9 ->
DSX-COH-041 / subgroup-harm-without-disposition (PRESENT/DETECTED catch, fires CRITICAL at
plan/verify/ship). Update the section's "As of 2026-09-06 none of the three conditions is
met" line to reflect that all three are now satisfied at the Phase-30 re-baseline (dated).

(2) Mapping-table row 15 (grep the "known-bad specs" / "good-control specs" corpus-count
row): change 39 -> 42 known-bad; good-control stays 15; date the count to the Phase-30
re-baseline (replace "As of 2026-09-06" with the Phase-30 date).

(3) Flip the deferred status cells: row 7 ("provenance list deferred") -> present as an
attributed miss via DSX-ML-034; row 8 ("residual deferred") -> present as an attributed
miss via DSX-CLM-034; row 12 (subgroup "gate deferred") -> present as a catch via
DSX-COH-041. Confirm the item-9 CTA row's reference is consistent (it already reads
"Present, stronger").

Keep every promotion D-13-honest (code + fixture named, measured evidence) — never a
promotion on an estimate. Edit ONLY this record file; touch no fixture, check, or catalogue.
  </action>
  <verify>
    <automated>& "C:\Users\Benutzer1\AppData\Local\Programs\Python\Python312\python.exe" -c "import re,sys; t=open('docs/literature/the-ai-data-scientist.md',encoding='utf-8').read(); ok = re.search(r'42\s+known-bad specs', t) and ('DSX-ML-034' in t) and ('DSX-CLM-034' in t) and ('DSX-COH-041' in t) and (not re.search(r'39\s+known-bad specs', t)); sys.exit(0 if ok else 1)"</automated>
  </verify>
  <done>The literature doc lists items 7/8/9 as Satisfied with code + fixture named; row 15
reads "42 known-bad specs, 15 good-control specs" dated to the Phase-30 re-baseline; the
"39 known-bad specs" string is gone; the deferred status cells for rows 7, 8 and 12 are
flipped to present; the "none ... is met" line is updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close the REQ-P30-02 gap with ONE lightweight corpus-count agreement test</name>
  <files>tests/test_literature_corpus_count_agreement.py</files>
  <behavior>
    - The test extracts the known-bad corpus count integer from the literature doc's
      mapping-table row 15 with a CRLF-tolerant regex (r'(\d+)\s+known-bad specs') and
      asserts it equals len(list((ROOT/'examples'/'known-bad').glob('*-ANALYSIS-SPEC.yaml'))).
    - The test extracts the good-control count integer (r'(\d+)\s+good-control specs') and
      asserts it equals len(list((ROOT/'examples'/'good-corpus').glob('*-ANALYSIS-SPEC.yaml'))).
    - It asserts AGREEMENT (doc integer == live glob count), never a hardcoded expected
      number, so it cannot under-pin to a stale literal and cannot over-fire on formatting.
    - It fails if the row-15 counts drift from the live corpus (the exact silent drift that
      made the doc read 39 while the live count grew to 42).
  </behavior>
  <action>
Create tests/test_literature_corpus_count_agreement.py: a single unittest.TestCase that
resolves ROOT from the test file location, reads docs/literature/the-ai-data-scientist.md
as UTF-8, extracts the row-15 known-bad and good-control count integers via CRLF-tolerant
regexes, and asserts each equals the live glob count of the corresponding ANALYSIS-SPEC
fixtures. Do NOT assert any fixed number — assert doc-vs-live agreement only. Do NOT assert
the deferred-table membership, dispositions, or any other claim (that would exceed the one
genuine gap; GA-4 "close exactly this gap and no more"). Keep the file to the two agreement
assertions plus their extraction. This is off-gate-path repo-integrity work, mints no code,
and is the ONLY new file this phase authors (authorised by GA-4 / CONTEXT §0).
  </action>
  <verify>
    <automated>& "C:\Users\Benutzer1\AppData\Local\Programs\Python\Python312\python.exe" -m unittest tests.test_literature_corpus_count_agreement -v</automated>
  </verify>
  <done>The test passes on the real 3.12.10 interpreter: the literature row-15 known-bad
count equals the live examples/known-bad glob count (42) and the good-control count equals
the live examples/good-corpus glob count (15). The test asserts agreement, not a hardcoded
number, and pins nothing beyond the row-15 counts.</done>
</task>

<task type="auto">
  <name>Task 4: Run the REQ-P30-02 audit battery + REQ-P30-03 zero-mint on the final tree</name>
  <files>tests/test_literature_corpus_count_agreement.py</files>
  <action>
On the FINAL tree, on the REAL 3.12.10 interpreter, from a CLEAN tree. First handle the
hazards: delete any stray root DECISIONS.jsonl (untracked) so the two `explain` tests do not
false-fail; confirm `git status` shows only this phase's intended edits.

Run and require green, in order:
(1) Catalogue current + zero-mint set-identity (REQ-P30-03): scripts/gen-finding-catalogue.py
    --check must exit 0 and report the live count; confirm set-identity 279 -> 279,
    added={} removed={}. Re-measure the live count — do not assume 279 has held.
(2) Frozen count-pin / snapshot tests unmutated: test_finding_catalogue_invariant,
    test_phase20_zero_mint_close, test_p19_categorical_rows, and the example-profile digests.
(3) Doc/code agreement tests green: test_doc_code_agreement, test_selection_heuristic_docs,
    and the new test_literature_corpus_count_agreement.
(4) node install.mjs --check passes (read-only verification; nothing under the installer's
    trees changed — do NOT run the mutating install).
(5) scripts/check.sh green (the canonical gate).
(6) Full unittest suite green on the real interpreter (so the matplotlib determinism test
    actually runs rather than skipping under the python3 stub).

If any gate fails, STOP and report which and why. A frozen-surface diff, a catalogue count
!= 279, or a snapshot mutation is a D-13/D-14 finding, not something to "fix" by reshaping.
  </action>
  <verify>
    <automated>& "C:\Users\Benutzer1\AppData\Local\Programs\Python\Python312\python.exe" scripts/gen-finding-catalogue.py --check && & "C:\Users\Benutzer1\AppData\Local\Programs\Python\Python312\python.exe" -m unittest discover -s tests -q && node install.mjs --check</automated>
  </verify>
  <done>On the real 3.12.10 interpreter from a clean tree: scripts/gen-finding-catalogue.py
--check exits 0 and reports the live count with set-identity 279 -> 279 (added={}
removed={}); the frozen count-pin/snapshot tests (test_finding_catalogue_invariant,
test_phase20_zero_mint_close, test_p19_categorical_rows, example-profile digests) are green;
the doc/code agreement tests (test_doc_code_agreement, test_selection_heuristic_docs,
test_literature_corpus_count_agreement) are green; node install.mjs --check passes;
scripts/check.sh is green; the full unittest suite is green with the matplotlib determinism
test RUN (not skipped). git status shows only this phase's intended edits (brief.md,
docs/literature/the-ai-data-scientist.md, the new test); no frozen surface diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 30-READOUT.md -> record docs (transcription) | Measured numbers cross into brief.md / the literature doc; a number not matching the re-baseline silently corrupts the durable record. |
| literature doc corpus counts -> live corpus | A prose count that drifts from the live glob (the exact 39-vs-42 staleness) is a silent-wrongness surface the new test closes. |
| final tree -> milestone audit | An accidental catalogue/fixture/frozen-code mutation would pass unnoticed without the count-pin/snapshot/set-identity gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-06 | Tampering (misleading record) | brief.md §6.5 backdrop | high | mitigate | The backdrop numbers are read from 30-READOUT.md (plan 30-01); Task-1 verify asserts DSX-ML-034/DSX-CLM-034/DSX-COH-041 present, a dated Phase-30 note, and the /15 FPR phrasing; the dated Phase-12 record is preserved as history, not overwritten. |
| T-30-07 | Tampering (stale claim) | literature doc row 15 + deferred table | high | mitigate | Task-2 verify asserts "42 known-bad specs" present and "39 known-bad specs" gone; the new agreement test pins the row-15 counts to the live globs so future drift fails a gate. |
| T-30-08 | Elevation (gap-test over/under-pinning) | test_literature_corpus_count_agreement | medium | mitigate | The test asserts doc==live agreement (no hardcoded number), so it cannot under-pin to a stale literal; a CRLF-tolerant regex avoids over-firing on line endings; it pins nothing beyond the row-15 counts (GA-4 "and no more"). |
| T-30-09 | Tampering | catalogue / fixtures / dq.py / viz.py (frozen) | critical | mitigate | gen-finding-catalogue.py --check set-identity 279 -> 279; frozen count-pin/snapshot tests green; git status clean on frozen surfaces; a diff is a D-13/D-14 finding, STOP (not a reshape). |
| T-30-10 | Spoofing (wrong interpreter / dirty tree) | audit battery | high | mitigate | Real 3.12.10 interpreter only; stray root DECISIONS.jsonl deleted before the explain tests; the full suite run on the real interpreter so the matplotlib test runs rather than skips under the python3 stub. |
| T-30-11 | Tampering (zero-mint violation) | finding catalogue | critical | mitigate | REQ-P30-03 set-identity 279 -> 279 added={} removed={}; the live count re-measured at execution, not assumed. |
</threat_model>

<verification>
All on the REAL 3.12.10 interpreter, from a clean tree (stray root DECISIONS.jsonl deleted):
1. brief.md §6.5 backdrop refreshed to the Phase-30 numbers with a dated note; item-7/8/9
   rows verified (Task 1).
2. literature doc: items 7/8/9 Satisfied, row-15 counts 42/15, status cells flipped (Task 2).
3. test_literature_corpus_count_agreement passes (Task 3).
4. gen-finding-catalogue.py --check exit 0, set-identity 279 -> 279; frozen
   snapshots/count-pins green; doc/code agreement tests green; node install.mjs --check
   passes; scripts/check.sh green; full suite green with matplotlib test run (Task 4).
5. git status shows only brief.md, docs/literature/the-ai-data-scientist.md, and the new
   test file; no frozen-surface diff.
</verification>

<success_criteria>
- brief.md §6.5 reads the terminal re-baseline (headline pair, FPR /15, 5-miss partition)
  with a dated Phase-30 re-evaluation note; the Phase-12 dated record is preserved.
- docs/literature/the-ai-data-scientist.md promotes items 7/8/9 to Satisfied (code+fixture
  named), reads 42 known-bad / 15 good-control, and flips the deferred status cells.
- The one lightweight agreement test closes exactly the REQ-P30-02 gap and passes.
- REQ-P30-02 battery green on the final tree; REQ-P30-03 set-identity 279 -> 279.
- Zero codes minted; no frozen surface changed; no tracking file edited.
</success_criteria>

<output>
Create .planning/phases/30-calibration-rebaseline/30-02-SUMMARY.md when done, recording the
refreshed brief/literature numbers, the gap decision (a) and the test's assertions, the
measured catalogue count (set-identity result), and confirmation that git status showed
only the three intended edits with no frozen-surface diff.
</output>