---
phase: 07-validity-frame-checks-dsx-val
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - brief.md
  - .planning/research/FEATURES.md
autonomous: true
requirements: [REQ-P7-02, REQ-P7-05]

must_haves:
  truths:
    - "Every primary source a Phase 7 docstring cites is listed in brief.md section 7 before the citing code lands (D-17, threat T-6-07)."
    - "brief.md section 7 pins an edition for Lohr and for Little and Rubin, because section numbers differ across editions and an unpinned section locator is not checkable (D-17)."
    - "Every locator that research could not confirm is labelled unverified in the reference list itself, not only in the planning documents (D-05)."
    - "The research file no longer asserts the retired design-effect illustration and no longer attributes it to sources that do not print it (D-10)."
    - "The research file states in its own words that the retired value was an unsourced computed illustration, so a later reader learns why it changed rather than only that it changed (D-10)."
    - statement: "The six newly added section-7 entries name real publications with correctly attributed author, year, title and venue."
      verification: backstop
  artifacts:
    - brief.md (section 7 extended with six sources, two pinned editions, and explicit unverified-locator labels)
    - .planning/research/FEATURES.md (design-effect worked example corrected)
  key_links:
    - "brief.md section 7 is the anchor the plan 07-03 through 07-06 docstrings cite against — a docstring citing a source absent from section 7 defeats the anchoring rule section 7 states in its own opening line."
    - "The corrected value in the research file must match the reference-value line written into dsx/mathx.py::design_effect in plan 07-01, or the two disagree and a later reader cannot tell which is right."
  prohibitions:
    - statement: "No reference entry or research prose written in this plan presents a project-defined convention as a published result."
      status: active
      verification: human
    - statement: "No citation locator is written that was not actually verified; an unconfirmed locator is labelled unverified rather than invented."
      status: active
      verification: human
    - statement: "No check trigger is narrowed and no severity lowered in order to make a fixture, the template or an existing test pass."
      status: active
      verification: human
    - statement: "The unpublished design-effect illustration that decision D-10 retires is not reintroduced, and the false attribution that carried it is removed rather than softened."
      status: active
      verification: human
---

<objective>
Extend the project's single citation anchor with the six sources Phase 7 is about to cite, pin
the two editions that are currently unpinned, and correct the one unpublished number the
research corpus still carries.

Purpose: decision D-17 requires the reference list to be extended before Phase 7 code lands,
because `brief.md` section 7 states in its own opening line that citations are anchored there
rather than sprawling. Decision D-10 requires the retired design-effect illustration to be
corrected in `.planning/research/FEATURES.md` as well as avoided in code, because otherwise the
next agent to read that file reintroduces it. This plan carries no code and no test, which is why
it runs in parallel with plan 07-01 rather than behind it.

Output: a section 7 that lists every source a Phase 7 docstring will cite, and a research file
that no longer prints an unsourced number as a published one.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-validity-frame-checks-dsx-val/07-CONTEXT.md
@.planning/phases/07-validity-frame-checks-dsx-val/07-PATTERNS.md
@.claude/CLAUDE.md
@.claude/skills/plain-language/SKILL.md
@.claude/skills/decision-format/SKILL.md
</context>

<shell_note>
Windows with PowerShell. Use `python3` for the interpreter and `python3 -m dsx.cli` for the
command-line interface — the `dsx` console script is not on PATH. The repository checks out
CRLF; any regular expression matching a line start or end must use `\r?\n`.
</shell_note>

<tasks>

<task type="auto">
  <name>Task 1: Extend brief.md section 7 with the six Phase 7 sources and pin two editions</name>

  <files>brief.md</files>

  <read_first>
    - `brief.md` lines 430-452, section 7 in full — the flowing-paragraph convention, one sentence per source, title in italics, a parenthetical naming what the source anchors
    - `.planning/phases/07-validity-frame-checks-dsx-val/07-CONTEXT.md`, the table headed "The D-05 citation ledger — use these verbatim", and the two paragraphs beneath it headed "Two honesty disclosures" and "Two locators that are UNVERIFIED"
    - `.planning/phases/07-validity-frame-checks-dsx-val/07-PATTERNS.md`, the section headed "brief.md section 7"
    - `dsx/frame/paradigm.py` lines 61-77 — how this project writes a citation that carries an unverified locator
  </read_first>

  <action>
    Edit `brief.md` section 7 in place. Keep the existing flowing-paragraph style exactly: plain
    sentences, one source per sentence, the title in italics, a parenthetical naming the finding
    codes or the concepts the source anchors. Do not convert the paragraph into a bulleted or
    tabular list. The two existing sentences that show the intended shape are the Gelman, Simpson
    and Betancourt sentence and the Deng, Lu and Chen sentence at the end of the current
    paragraph.

    Add these six sources, taking author, year, title, venue and locator verbatim from the
    citation ledger in `07-CONTEXT.md` rather than re-sourcing them.

    International Council for Harmonisation (2019), the E9(R1) addendum on estimands and
    sensitivity analysis, document reference EMA/CHMP/ICH/436221/2017 at Step 5, sections A.3.2
    and A.3.3, anchoring `DSX-VAL-010`.

    Hernan and Robins (2016), American Journal of Epidemiology volume 183 issue 8, pages 758 to
    764, Table 1, anchoring `DSX-VAL-010` alongside the addendum. Hernan and Robins, Causal
    Inference: What If, is already listed; this is a second, different publication by the same
    authors and both are needed.

    Popper (1959, 2002 reissue), The Logic of Scientific Discovery, Part I Chapter 1 section 6 on
    falsifiability as a criterion of demarcation, pages 17 to 18, anchoring `DSX-VAL-011`.

    Kish (1965), Survey Sampling, page 258 for the design-effect definition and pages 161 to 162
    for the intraclass correlation, anchoring `DSX-VAL-020`. State in the sentence itself that a
    section number inside Kish for the formula is unverified and that only the page numbers were
    confirmed.

    Higgins, Eldridge and Li (2024), the Cochrane Handbook for Systematic Reviews of
    Interventions version 6.5, sections 23.1.4 and 23.1.4.1, anchoring `DSX-VAL-020` and carrying
    its published worked value.

    Cronbach and Meehl (1955), "Construct Validity in Psychological Tests", Psychological
    Bulletin volume 52 issue 4, pages 281 to 302, the nomological net discussion at page 290,
    anchoring `DSX-VAL-070`.

    Then pin the two unpinned editions. The Lohr entry becomes Lohr (2021), Sampling: Design and
    Analysis, third edition, with Chapter 1 sections 1.2, 1.3 and 1.3.4 and Chapter 16 section
    16.1 named as locators, anchoring `DSX-VAL-050`. The Little and Rubin entry becomes Little
    and Rubin (2019), Statistical Analysis with Missing Data, third edition, Chapter 3 section
    3.2, anchoring `DSX-VAL-060`, and gains a companion sentence for White and Carlin (2010),
    Statistics in Medicine volume 29 issue 28, pages 2920 to 2931, digital object identifier
    10.1002/sim.3944.

    Extend the existing Gelman, Simpson and Betancourt sentence with its two locators, section
    3.3 and section 1.2, cited by number and by title together, and add a clause stating that
    whether the typeset journal version uses the same section numbers as the preprint is
    unverified. Citing by number and title together is what lets the locator survive either
    version.

    Extend the existing Gelman and Hill entry with the year 2007 and the publisher, and state
    that the exact chapter locator is unverified. Add Cameron and Miller (2015), "A
    Practitioner's Guide to Cluster-Robust Inference", Journal of Human Resources volume 50 issue
    2, pages 317 to 372, anchoring `DSX-VAL-030`, with its section locator also marked
    unverified.

    Do not add Conley (1999). Only training-knowledge attribution was available for it, and this
    project does not list a source it has not confirmed. Do not attach a section number to Mayo
    (2018) anywhere; only its Tour structure was confirmable.

    Write every added sentence in plain language and expand any abbreviation on first use inside
    the paragraph. Write "intraclass correlation" in full before any short form of it, and write
    "digital object identifier" before the identifier itself.
  </action>

  <verify>
    <automated>python3 scripts/check_brief_refs.py</automated>
  </verify>

  <acceptance_criteria>
    - Section 7 of `brief.md` contains each of these strings: `E9(R1)`, `Hernan and Robins (2016)`, `Popper`, `Kish (1965)`, `Cochrane Handbook`, `Cronbach and Meehl (1955)`, `Lohr (2021)`, `Little and Rubin (2019)`, `White and Carlin (2010)`, `Cameron and Miller (2015)`
    - Section 7 of `brief.md` contains the word `unverified` at least three times, one each for the Kish section locator, the Gelman/Simpson/Betancourt typeset-version caveat, and the Gelman and Hill chapter locator
    - Section 7 of `brief.md` contains `3rd ed.` or `third edition` for both Lohr and Little and Rubin
    - Section 7 of `brief.md` does not contain the string `Conley`
    - `python3 -m unittest discover -s tests` exits 0 — `tests/test_known_bad_corpus.py` scans planning and corpus prose for retired over-claims and must stay green
    - The verification helper script named in `<verify>` is created by this task if it does not already exist, is placed under `scripts/`, reads section 7, asserts every criterion above, prints what it checked, and exits non-zero on any failure
  </acceptance_criteria>

  <done>
    `brief.md` section 7 names every source a Phase 7 docstring will cite, pins the Lohr and
    Little and Rubin editions, carries three explicit unverified-locator labels, does not name
    Conley, and the helper script asserting all of that exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the unsourced design-effect worked example in the research file</name>

  <files>.planning/research/FEATURES.md</files>

  <read_first>
    - `.planning/research/FEATURES.md` lines 36-60 — the passage headed "Worked published example" and the paragraph after it, which reasons from the retired value onward
    - `.planning/phases/07-validity-frame-checks-dsx-val/07-CONTEXT.md` decision D-10 and the first bullet of the `<specifics>` section
    - `.planning/phases/07-validity-frame-checks-dsx-val/07-PATTERNS.md`, the section headed ".planning/research/FEATURES.md"
    - `dsx/mathx.py::design_effect` as written by plan 07-01, so the corrected number and its citation match the shipped docstring word for word
  </read_first>

  <action>
    The passage headed "Worked published example" asserts a design-effect value computed from an
    intraclass correlation of 0.05 and a cluster size of 50, and attributes it to Donner and Klar
    (2000) and to Cochrane Handbook section 16.3.4. Research established that no such worked
    value is printed in either source. The arithmetic is correct and the attribution is not, which
    is the exact failure the citation discipline exists to catch — a plausible number, correct
    working, credited to texts that do not print it.

    <!-- planner-discipline-allow: 3.45 -->

    Replace the passage, keeping the existing shape — a bold lead-in label, then the claim, then
    the citation inline. The replacement states the Cochrane Handbook's own worked example: an
    intraclass correlation of 0.02 and an average cluster size of 29.8, giving a design effect of
    1.576, cited to Higgins, Eldridge and Li (2024), Cochrane Handbook for Systematic Reviews of
    Interventions version 6.5, sections 23.1.4 and 23.1.4.1, where the formula and the value
    appear in the same freely accessible versioned subsection. Recompute the accompanying
    interval-narrowness figure from 1.576 rather than leaving the old one in place: the square
    root of 1.576 is approximately 1.26, so the naive interval is about 1.26 times too narrow at
    that design effect, not the figure the passage currently gives.

    Immediately after the replacement, add two or three plain sentences recording what changed
    and why: that the previous worked value was an unsourced computed illustration, that its
    arithmetic was right but no cited text prints it, and that it was removed under decision D-10
    rather than corrected in place, so a later reader who has seen it elsewhere knows it was
    examined and rejected rather than overlooked. Name the value being retired in that note, once,
    so the note is actually useful to someone who encounters the old number in an older document.

    The paragraph that follows the replaced passage reasons onward from the retired value toward
    the brief's "3 to 10 times too narrow" framing, using further computed illustrations of its
    own. Leave that reasoning intact but relabel it plainly as arithmetic illustration rather than
    published example, so no sentence in the file claims publication for a number that has none.

    Optionally add the second genuinely published value as a cross-check: the United Nations
    household survey handbook, Chapter 6 paragraph 72, gives an intraclass correlation of 0.05
    and a cluster size of 17, yielding 1.80. Add it only if the executor can name the handbook's
    full title and year; otherwise omit it, because an unnameable source is not a source.
  </action>

  <verify>
    <automated>python3 -m unittest tests.test_known_bad_corpus -v</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/research/FEATURES.md` contains the string `1.576` and the string `29.8`
    - `.planning/research/FEATURES.md` contains the string `Higgins, Eldridge` and the string `23.1.4.1`
    - `.planning/research/FEATURES.md` no longer contains the string `Donner & Klar` as an attribution for a worked design-effect value
    - `.planning/research/FEATURES.md` no longer contains the string `16.3.4`
    - The file contains a sentence naming decision `D-10` and explaining that the retired value was an unsourced computed illustration
    - `python3 -c "import pathlib; t=pathlib.Path('.planning/research/FEATURES.md').read_text(encoding='utf-8'); assert 'Worked published example' not in t or '1.576' in t.split('Worked published example')[1][:600]"` exits 0
    - `python3 -m unittest discover -s tests` exits 0
  </acceptance_criteria>

  <done>
    The research file's worked example is the Cochrane published one, the retired value appears
    only inside an explicit note saying it was retired and why, no sentence in the file claims
    publication for a computed illustration, and the full suite is green.
  </done>
</task>

</tasks>

<threat_model>
Application Security Verification Standard level 1. Blocking severity: high. This plan writes only
Markdown prose in the repository's own documentation tree. It adds no code path, reads no
untrusted input, and executes nothing at gate time. The threat surface here is honesty, not
memory safety, and it is the surface this whole project exists to defend.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning prose to the project's citation anchor | A locator crosses from a research document into `brief.md` section 7, which every later docstring is written against and which a reader treats as verified. |
| retired claim to future reader | A number removed here can be reintroduced by any later agent that reads the older passage, which is why the removal carries an explanatory note rather than a silent edit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7-07 | Spoofing | `brief.md` section 7 entries | high | mitigate | Every locator research could not confirm carries the word `unverified` in the entry itself; an acceptance criterion counts at least three such labels; Conley (1999) is excluded because only training-knowledge attribution existed for it |
| T-7-08 | Tampering | `.planning/research/FEATURES.md` worked example | medium | mitigate | The retired value is removed together with its false attribution, and a note records that it was examined and rejected, so a later reader cannot restore it believing it was merely omitted |
| T-7-09 | Repudiation | correspondence between the research file and the shipped docstring | low | mitigate | Task 2 reads `dsx/mathx.py::design_effect` as written by plan 07-01 and matches the number and citation word for word |
| T-7-SC | Tampering | package-manager installs | low | accept | This phase adds zero external dependencies — `07-RESEARCH.md` section "Package Legitimacy Audit" states it and this plan installs nothing. No legitimacy checkpoint applies. |
</threat_model>

## Flagged assumptions (spec-less edge probe)

This phase has no SPEC file, so the deterministic edge probe ran as a fallback. It returned nine
applicable rows and classified every one of them `unclassified`, leaving all nine unresolved. An
`unclassified` row is never auto-resolved and never dropped, so each is surfaced explicitly across
the phase's plans.

The probe classifying nine of nine rows as unclassified is a classification miss, not evidence
that this phase is edge-free. Its categories are shaped for feature work, and this phase writes
gate checks that adjudicate other people's declarations.

Rows this plan touches:

| Probe row | Status | How it is handled here |
|---|---|---|
| REQ-P7-02 | unresolved, flagged | The citation half of the requirement — the design-effect formula's source and its published worked value — is anchored in section 7 and corrected in the research file. The arithmetic half is covered by plan 07-01 and the finding text by plan 07-04. |
| REQ-P7-05 | unresolved, flagged | The Gelman, Simpson and Betancourt entry gains both section locators and the typeset-version caveat, which is what plan 07-05's docstring cites against. |

## Artifacts this phase produces

This plan creates no code symbol. It edits two prose files and may create one small verification
helper under `scripts/`. The phase-wide artifact list is carried in full in `07-01-PLAN.md`; the
rows relevant here are:

| Artifact | Kind | Created by |
|---|---|---|
| `brief.md` section 7, extended with six sources and two pinned editions | reference list | 07-02 |
| `.planning/research/FEATURES.md` corrected design-effect passage | research prose | 07-02 |
| `scripts/check_brief_refs.py` | verification helper | 07-02 |

<verification>
- `python3 -m unittest discover -s tests` exits 0
- `python3 scripts/gen-finding-catalogue.py --check` exits 0
- `git diff --stat` names only `brief.md`, `.planning/research/FEATURES.md` and the new helper script
- No file under `dsx/` is touched by this plan
</verification>

<success_criteria>
Section 7 of `brief.md` lists every source a Phase 7 docstring will cite, pins the two editions
that were unpinned, and labels three locators as unverified. The research file's worked example is
the published one, the retired value survives only inside an explicit note explaining its removal,
and the full test suite is green.
</success_criteria>

<output>
Create `.planning/phases/07-validity-frame-checks-dsx-val/07-02-SUMMARY.md` when done.
</output>
