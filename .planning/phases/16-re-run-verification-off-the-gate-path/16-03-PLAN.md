---
phase: 16-re-run-verification-off-the-gate-path
plan: 03
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - examples/known-bad/garden-of-forking-paths-p-hacking-ATTRIBUTION.yaml
  - examples/known-bad/operator-known-answer-selective-exclusion-ATTRIBUTION.yaml
  - examples/known-bad/retracted-fabricated-field-experiment-ATTRIBUTION.yaml
  - tests/test_known_bad_corpus.py
autonomous: true
requirements: [REQ-P16-03]
tags: [dsx, corpus, protocol_adherence, attribution-sidecar, calibration-pinned, additive]

must_haves:
  truths:
    - "Each of the three remaining Phase-12 `*-ATTRIBUTION.yaml` sidecars carries a `protocol_adherence` field, so a skipped `dsx-reproduce` run is machine-countable (REQ-P16-03 / D-10) — the field lives on the SIDECAR, never on any `ANALYSIS-SPEC.yaml`."
    - "`protocol_adherence` is additive and does not move calibration: per-fixture gate-finding sets stay byte-unchanged, the ABSENT-partition denominator and good-corpus count are unchanged, and `_headline` still returns the exact `(0.25, 0.3)` pair — the field is a third, independently-reported statistic accepted-but-ignored exactly as `present` is (D-10)."
    - "A new corpus assertion proves protocol_adherence is accepted-but-ignored: it is in a closed vocabulary on every sidecar, it is NOT a parameter of `_headline` (not wired into the calibration path), and skipped-skill cases are countable — while `test_headline_is_the_absent_miss_rate_and_fpr_pair` and the invariance test stay UNEDITED (D-10)."
    - "The existing sidecar schema test still passes: adding a fourth key does not perturb `absent_code`/`promotes_backlog_item`/`kind`, and every sidecar still gates its sibling spec identically (D-10)."
  artifacts:
    - examples/known-bad/garden-of-forking-paths-p-hacking-ATTRIBUTION.yaml
    - examples/known-bad/operator-known-answer-selective-exclusion-ATTRIBUTION.yaml
    - examples/known-bad/retracted-fabricated-field-experiment-ATTRIBUTION.yaml
    - tests/test_known_bad_corpus.py
  key_links:
    - "The sidecar lives OUTSIDE the ANALYSIS-SPEC (dsx/decisions.py placement precedent 11.2 D-08), so adding `protocol_adherence` cannot perturb frame_digest or any gate finding — the D-10(a) byte-unchanged-gate-findings pin holds by construction and is re-proven by running the full corpus suite (D-10)."
    - "`test_attribution_sidecars_reference_valid_codes_and_items` reads ONLY `absent_code`/`promotes_backlog_item`/`kind` (test_known_bad_corpus.py:1422-1444) — the sidecar schema is open, so a fourth key is accepted-but-ignored by the existing test; the new key is proven ignored by asserting it is not in `_headline.__code__.co_varnames`."
  prohibitions:
    - statement: "`protocol_adherence` is NEVER added to any ANALYSIS-SPEC.yaml — only to the *-ATTRIBUTION.yaml sidecars. A spec key could change gate findings or trip an unknown-key schema check; a sidecar key cannot."
      status: enforced
      verification: "`git status --porcelain -- examples/known-bad/*ANALYSIS-SPEC.yaml` is empty for this plan; the diff touches only the three *-ATTRIBUTION.yaml files and tests/test_known_bad_corpus.py."
    - statement: "The two calibration-anchor tests are not edited: test_headline_is_the_absent_miss_rate_and_fpr_pair and test_headline_is_invariant_to_adding_a_target_present_case stay byte-unchanged; `_headline` and `_INCIDENTAL_GAP_CODES`/denominator logic are not touched."
      status: enforced
      verification: "`_headline((2,5),(1,4),(3,10))` still returns (0.25,0.3); the new test is additive; git diff shows no edit to those two methods or to _headline."
    - statement: "No finding code is minted; the catalogue stays at the 258 baseline established by 16-01."
      status: enforced
      verification: "gen-finding-catalogue.py --check stays exit 0 at 258; this plan adds no report.add call site (it edits YAML sidecars and a test)."
---

<objective>
Deliver REQ-P16-03: add a `protocol_adherence` field to the three remaining Phase-12
`*-ATTRIBUTION.yaml` sidecars so "the agent skipped the reproduce skill" is machine-countable, and
add one corpus assertion that the field is additive and accepted-but-ignored — provably NOT moving
catch rate, false-positive rate, or `_headline`.

Purpose: Phase 16 makes skipped-skill failures countable without letting them touch calibration. The
field records, per known-bad case, whether the `dsx-reproduce` protocol was followed — a third
statistic reported beside catch rate and FPR, never inside them. It lives on the sidecar (outside the
ANALYSIS-SPEC) precisely so it cannot perturb frame_digest or any gate finding (D-10), the same
placement discipline the existing `absent_code`/`promotes_backlog_item` tags use. The new test pins
the additivity: the field is in a closed vocabulary, it is not a parameter of `_headline`, and the
headline pair stays exactly (0.25, 0.3).

Output: `protocol_adherence` on all three sidecars + `test_protocol_adherence_is_additive_and_ignored`
in `tests/test_known_bad_corpus.py`.
</objective>

<artifacts_this_phase_produces>
This plan (16-03) runs in wave 2, `depends_on: [16-01]` (so any corpus re-check runs against the 258
baseline). It edits four files:
- the three `examples/known-bad/*-ATTRIBUTION.yaml` sidecars (add `protocol_adherence`)
- `tests/test_known_bad_corpus.py` (add one additive accepted-but-ignored assertion)

It mints zero finding codes and its `files_modified` are disjoint from the other wave-2 plans (16-02
owns skills/+templates/+capabilities/; 16-04 owns tests/test_no_entrypoint_execution.py — a DIFFERENT
tests/ file). This plan is the SOLE editor of tests/test_known_bad_corpus.py, satisfying
single-writer-per-wave.

## Phase-level goal-backward coverage (union across all four Phase-16 plans)
- Success Criterion 1 (REQ-P16-01) — plan 16-02: skill `dsx-reproduce` + template + capability registration.
- Success Criterion 2 (REQ-P16-02) — plan 16-01: DSX-REP-060/061 gate check + 258 catalogue mint + invariant rebaseline.
- Success Criterion 3 (REQ-P16-03) — **this plan (16-03)**: `protocol_adherence` on the three ATTRIBUTION sidecars + accepted-but-ignored corpus assertion (calibration pinned).
- Success Criterion 4 (REQ-P16-04) — plan 16-04: static AST no-entrypoint-execution guard with a positive control.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-re-run-verification-off-the-gate-path/16-CONTEXT.md
@examples/known-bad/garden-of-forking-paths-p-hacking-ATTRIBUTION.yaml
@tests/test_known_bad_corpus.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add protocol_adherence to the three *-ATTRIBUTION.yaml sidecars</name>
  <files>examples/known-bad/garden-of-forking-paths-p-hacking-ATTRIBUTION.yaml, examples/known-bad/operator-known-answer-selective-exclusion-ATTRIBUTION.yaml, examples/known-bad/retracted-fabricated-field-experiment-ATTRIBUTION.yaml</files>
  <read_first>
    - examples/known-bad/garden-of-forking-paths-p-hacking-ATTRIBUTION.yaml (the sidecar shape: a schema comment then `absent_code`/`promotes_backlog_item`/`kind`/`rationale` keys — add `protocol_adherence` beside them, leave the existing keys byte-unchanged)
    - examples/known-bad/operator-known-answer-selective-exclusion-ATTRIBUTION.yaml (same shape)
    - examples/known-bad/retracted-fabricated-field-experiment-ATTRIBUTION.yaml (same shape)
    - tests/test_known_bad_corpus.py:1422-1444 (the existing schema test reads only absent_code/promotes_backlog_item/kind — an extra key is accepted-but-ignored, so adding one is safe)
    - .planning/phases/16-re-run-verification-off-the-gate-path/16-CONTEXT.md (D-10: field lives on the sidecar not the spec; D-11: the skipped state ties to the report SKIPPED status)
  </read_first>
  <action>
    In each of the three `*-ATTRIBUTION.yaml` sidecars, add a single new top-level key
    `protocol_adherence` with the value `skipped`, plus a one-line comment stating it records whether
    the `dsx-reproduce` protocol was followed for this hand-authored fixture and that its vocabulary is
    `adhered` / `skipped` / `not_applicable` (all three are honest hand-authored misses that never ran
    the reproduce skill, so `skipped` is correct). Extend the schema comment block at the top of each
    file to name `protocol_adherence?` as an optional additive field (REQ-P16-03 / D-10), noting it is
    accepted-but-ignored by calibration — reported beside, never inside, catch rate and FPR.

    Do NOT change `absent_code`, `promotes_backlog_item`, `kind`, or `rationale` in any sidecar. Do NOT
    add this key to any `ANALYSIS-SPEC.yaml` — sidecars only (D-10). Use the same value across all
    three so the skipped-skill count is unambiguous.
  </action>
  <acceptance_criteria>
    - All three *-ATTRIBUTION.yaml sidecars contain a `protocol_adherence` key with value `skipped`.
    - The existing `absent_code`/`promotes_backlog_item`/`kind`/`rationale` values are unchanged in every sidecar.
    - No ANALYSIS-SPEC.yaml is edited; the schema comment in each sidecar names protocol_adherence as an optional additive field.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import glob; from dsx.loader import load; ps=glob.glob('examples/known-bad/*-ATTRIBUTION.yaml'); assert len(ps)==3, ps; c=[load(p) for p in ps]; assert all(d.get('protocol_adherence')=='skipped' for d in c), [d.get('protocol_adherence') for d in c]; assert all(d.get('absent_code') for d in c)" && [ -z "$(git status --porcelain -- examples/known-bad/*ANALYSIS-SPEC.yaml)" ]</automated>
  </verify>
  <done>All three attribution sidecars carry protocol_adherence: skipped with an updated schema comment, their existing keys are byte-unchanged, and no ANALYSIS-SPEC.yaml was touched.</done>
</task>

<task type="auto">
  <name>Task 2: Add test_protocol_adherence_is_additive_and_ignored to tests/test_known_bad_corpus.py</name>
  <files>tests/test_known_bad_corpus.py</files>
  <read_first>
    - tests/test_known_bad_corpus.py:701-714 (_headline: `present` is accepted for signature symmetry but MUST NOT influence output — the exact accepted-but-ignored pattern protocol_adherence mirrors)
    - tests/test_known_bad_corpus.py:1371-1444 (test_attribution_sidecars_reference_valid_codes_and_items: the sidecar-loading idiom via self._attribution_paths() + dsx.loader.load, and the `kind` open-schema precedent)
    - tests/test_known_bad_corpus.py:1919-1931 (the two anchor tests test_headline_is_the_absent_miss_rate_and_fpr_pair and test_headline_is_invariant_to_adding_a_target_present_case — MUST stay unedited; the new test sits beside them, additive)
    - tests/test_known_bad_corpus.py:34-37 (CORPUS_DIR, ATTRIBUTION_SUFFIX — reuse; do not re-glob by hand)
  </read_first>
  <action>
    Add ONE new test method to the corpus TestCase, `test_protocol_adherence_is_additive_and_ignored`,
    that proves REQ-P16-03's additivity. It must:

    (1) Load every `*-ATTRIBUTION.yaml` via `self._attribution_paths()` + `dsx.loader.load`, assert
    each carries `protocol_adherence`, and assert its value is in the closed vocabulary
    `{"adhered", "skipped", "not_applicable"}` — so a typo or an open-ended value fails here.

    (2) Prove the field is NOT wired into the calibration headline: assert `"protocol_adherence" not
    in _headline.__code__.co_varnames` (the field is neither a parameter nor a local of `_headline`),
    the machine-checkable form of "accepted-but-ignored exactly as `present` is". Also re-assert, in
    this test, that `_headline((2, 5), (1, 4), (3, 10)) == (0.25, 0.3)` to pin that the field's
    introduction did not move the headline pair (a local re-pin; the standalone anchor test stays
    untouched).

    (3) Prove skipped-skill cases are countable: compute the number of sidecars whose
    `protocol_adherence == "skipped"` and assert it is at least 1 (today all three) — the machine-count
    REQ-P16-03 asks for.

    Do NOT edit `_headline`, `test_headline_is_the_absent_miss_rate_and_fpr_pair`,
    `test_headline_is_invariant_to_adding_a_target_present_case`, `_INCIDENTAL_GAP_CODES`, or any
    denominator/partition helper. Add no new module-level import beyond what the file already has
    (co_varnames needs none). Keep the assertion messages CRLF-agnostic (no line-anchored parsing).
  </action>
  <acceptance_criteria>
    - `test_protocol_adherence_is_additive_and_ignored` exists and asserts: every sidecar's protocol_adherence is in the closed vocabulary; `"protocol_adherence"` is not in `_headline.__code__.co_varnames`; `_headline((2,5),(1,4),(3,10)) == (0.25,0.3)`; and the skipped count is >= 1.
    - The two headline anchor tests and `_headline` are byte-unchanged.
    - `python -m unittest tests.test_known_bad_corpus` exits 0 (full corpus suite green — calibration unchanged, gate findings byte-unchanged).
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_known_bad_corpus >/dev/null 2>&1 && python -c "import ast; t=ast.parse(open('tests/test_known_bad_corpus.py',encoding='utf-8').read()); ms={f.name for f in ast.walk(t) if isinstance(f,ast.FunctionDef)}; assert 'test_protocol_adherence_is_additive_and_ignored' in ms; assert {'test_headline_is_the_absent_miss_rate_and_fpr_pair','test_headline_is_invariant_to_adding_a_target_present_case'} <= ms"</automated>
  </verify>
  <done>tests/test_known_bad_corpus.py gains one additive test proving protocol_adherence is in a closed vocabulary, is not a variable of _headline, leaves the (0.25,0.3) headline pair intact, and makes skipped-skill cases countable — with the two headline anchor tests unedited and the full corpus suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan edits three known-bad-fixture sidecars and adds one read-only test assertion. The sidecars
live outside the ANALYSIS-SPEC and are read only by the corpus test harness (via dsx.loader.load at
test time) — never by a runtime gate — so no gate finding, no frame_digest, and no calibration number
can move. There is no network/auth/input surface. No npm/pip/cargo install occurs, so T-16-SC is N/A.

| Boundary | Description |
|----------|-------------|
| sidecar → corpus test harness | The harness loads the sidecar at test time; the added key is read only by the new additive assertion. No runtime gate reads the sidecar (placement precedent 11.2 D-08). |
| protocol_adherence → calibration | The field is reported beside catch rate/FPR, never inside them; the new test pins that it is not a `_headline` variable (D-10). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-20 | Tampering | `protocol_adherence` silently entering the catch-rate/FPR denominators or `_headline`, moving calibration | high | mitigate | D-10: the field is a sidecar key, not a `_headline` parameter; test_protocol_adherence_is_additive_and_ignored asserts it is absent from `_headline.__code__.co_varnames` and that the headline pair stays (0.25,0.3); the anchor tests are unedited. |
| T-16-21 | Tampering | The field being placed on an ANALYSIS-SPEC.yaml, changing gate findings or tripping an unknown-key schema check | high | mitigate | D-10: the field goes on the sidecar only; the verify asserts `git status` on examples/known-bad/*ANALYSIS-SPEC.yaml is empty; the full corpus gate suite stays green (per-fixture findings byte-unchanged). |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs occur; the deliverables are YAML sidecar edits and a stdlib test assertion. |
</threat_model>

<verification>
This plan delivers Success Criterion 3 (REQ-P16-03): remaining Phase-12 corpus cases carry a
`protocol_adherence` field so skipped-skill failures are countable; it extends REQ-P12-02 and does
NOT replace catch rate or false-positive rate.

For this plan specifically:
- All three sidecars carry `protocol_adherence: skipped`; no ANALYSIS-SPEC.yaml is edited.
- `test_protocol_adherence_is_additive_and_ignored` passes: closed vocabulary, not a `_headline` variable, headline pair still (0.25, 0.3), skipped count >= 1.
- The two headline anchor tests and `_headline` are byte-unchanged; the full corpus suite is green (gate findings and calibration unchanged).
- `gen-finding-catalogue.py --check` stays exit 0 at 258 (no code minted).
</verification>

<success_criteria>
- `protocol_adherence` is present on all three ATTRIBUTION sidecars and machine-countable (REQ-P16-03 / D-10).
- The field is additive: gate-finding sets byte-unchanged, denominators unchanged, `_headline` returns (0.25, 0.3), and the field is not a `_headline` variable (D-10).
- The two headline anchor tests stay unedited; the field is never added to an ANALYSIS-SPEC.
- Zero finding codes minted; catalogue stays at 258.
</success_criteria>

<output>
Create `.planning/phases/16-re-run-verification-off-the-gate-path/16-03-SUMMARY.md` when done.
</output>
