---
quick_id: 260821-d6h
description: Deepen dsx-explore-data into an insight-driving EDA protocol (branches, ledgers, spec reconciliation, EDA.md template)
created: 2026-08-21
must_haves:
  truths:
    - The skill states the trust core (steps 1-4) plus grain/dependence, joins, time integrity, robust summaries, concentration, outlier taxonomy, pathology sweep and base rate as named sub-steps.
    - Six question-type branches exist, each with named output tables and a meets/falls_short/re-scope verdict.
    - Stop-and-re-scope fires from four deterministic sites, never from judgement.
    - Every spec field the protocol writes into exists verbatim in templates/ANALYSIS-SPEC.yaml.
    - dsx/ is untouched; no new DSX-* finding codes; no gate reads EDA.md.
  artifacts:
    - skills/dsx-explore-data/SKILL.md (rewritten)
    - templates/EDA.md (new)
    - capabilities/dsx/fragments/executor.md (one EDA read line)
    - C:/Users/Benutzer1/.claude/skills/dsx-explore-data/SKILL.md (synced copy)
    - .planning/seeds/SEED-001-deepen-dsx-explore-data-eda-protocol.md (deferred set recorded)
  key_links:
    - templates/ANALYSIS-SPEC.yaml
    - references/question-taxonomy.md
    - references/data-quality-assertions.md
    - brief.md
---

# Quick Task 260821-d6h: Deepen the dsx-explore-data EDA protocol

The protocol is a strong data-trust checklist whose steps 5-6 name vague verbs
("correlations", "the headline number") that no agent can execute the same way twice.
This task keeps the trust core and replaces everything around it with named outputs,
closed vocabularies and deterministic stop rules — agent protocol only, gates untouched.

Design is fully adjudicated: a 10-agent research workflow produced catalog E-01..E-33;
three adversarial critics found 25 defects; each correction was verified against the live
repo (gate code, spec template, CLI) before acceptance. Locked decisions are in
`260821-d6h-CONTEXT.md`.

## Task 1 — Rewrite the skill

**files:** `skills/dsx-explore-data/SKILL.md`

**action:** Rewrite the protocol: setup and contract (profile precedence, ledgers open,
column scoping, no-spec rule); trust core 1-4 with sub-steps 1a grain/dependence, 1b joins,
2 missingness-mechanism decision table, 3a time integrity, 4a robust summaries,
4b concentration, 4c outlier taxonomy, 4d pathologies and impossible pairs, 4e base rate;
a post-trust-core checkpoint; six question-type branches with verdicts; optional funnel
routines with composition rules; segments with computed ranking and candidate handshake;
second-order look; spec reconciliation; findings / searched-not-found / comparisons
registers; single spec-write moment with `dsx validate`; rerun contract; lifecycle rules.

**verify:** `grep` the file for each of: `Spec reconciliation`, `Findings ledger`,
`Comparisons ledger`, `Searched, not found`, `Second-order look`, `Branch: `, `verdict:`,
`method_family_required`, `multiplicity.family`. No occurrence of a new `DSX-` code.

**done:** The six trust steps, the stop rule, the six branches, the three registers, the
second-order look and the spec reconciliation are all present with named outputs and skip
conditions.

## Task 2 — Add the EDA.md template and wire the executor

**files:** `templates/EDA.md`, `capabilities/dsx/fragments/executor.md`

**action:** Write `templates/EDA.md` with the YAML front-matter contract (the facts
downstream steps read) and the fixed headings in protocol order, plus a CRLF-tolerance
authoring note for any front-matter parser. Add one line to the executor fragment telling
the executor to read EDA.md front-matter when present and seed
`results.comparisons_looked_at` from its count.

**verify:** `templates/EDA.md` exists and contains the front-matter keys and every heading
the skill names; the executor fragment contains the EDA read line.

**done:** Both files written; headings and front-matter keys match the skill one-for-one.

## Task 3 — Sync the user copy, record the deferred set, verify green

**files:** `C:/Users/Benutzer1/.claude/skills/dsx-explore-data/SKILL.md`,
`.planning/seeds/SEED-001-deepen-dsx-explore-data-eda-protocol.md`

**action:** Copy the repo skill over the user-level copy (which currently lacks even the
split-first paragraph). Update SEED-001 with the six deferred enhancements (E-26..E-31)
and their entry conditions. Run `python -m unittest` and confirm it stays green.

**verify:** `fc`/diff shows the two SKILL.md copies identical; SEED-001 lists the deferred
set; `python -m unittest discover -s tests` exits 0; `git status` shows `dsx/` unchanged.

**done:** Copies in sync, deferred set recorded, test suite green, no `dsx/` diff.
