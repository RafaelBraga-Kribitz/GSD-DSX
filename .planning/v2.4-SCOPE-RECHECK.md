# v2.4 Scope Recheck — inherited scope vs the live tree

**Unit:** S0-2. **Run:** 2026-09-03 (autonomous ceremony firing).
**Purpose:** the v2.4 scope was researched 2026-08-29 alongside v2.3 and reused at
this milestone's open. Before any phase plans on it, this file re-verifies its key
premises against the **live** tree — the ledger/scope are a claim, the code is the
fact. Verdicts: **still-valid** (execution needed), **already-satisfied** (built,
drop or shrink), **contradicted** (premise wrong, re-scope).

## Method

- Read live `dsx/checks/viz.py` (last touched `edf862c`, 2026-08-10 — untouched
  since before v2.3, as the scope claims), `dsx/spec.py` `CHART_CAPABILITIES`,
  `dsx/checks/smells.py` mark sets, and `scripts/gen-input-types.py` IT001–IT040
  inventory (the invariant's "input-type extras").
- Catalogue count re-measured three independent ways, not assumed.
- Phase 22/23/24 target artifacts probed for existence.

## Premise 1 — the orphan list matches the live tree

REQ-P21-01 names 12 orphans. An orphan is a mark named somewhere in the vocabulary
that lacks a **relationship** home (`RELATIONSHIP_CHARTS`, viz.py) AND/OR a
**capability** home (`CHART_CAPABILITIES`, spec.py). Live verdict per mark:

| Mark | Relationship home | Capability home | Named in | Orphan? |
|---|---|---|---|---|
| histogram | distribution | — | RELATIONSHIP_CHARTS, LENGTH_ENCODED | yes (no capability) |
| density | distribution | — | RELATIONSHIP_CHARTS, smells DENSITY_MARKS | yes (no capability) |
| ecdf | distribution | — | RELATIONSHIP_CHARTS | yes (no capability) |
| strip | distribution | — | RELATIONSHIP_CHARTS | yes (no capability) |
| diverging_bar | deviation | — | RELATIONSHIP_CHARTS, LENGTH_ENCODED | yes (no capability) |
| waterfall | deviation | — | RELATIONSHIP_CHARTS, LENGTH_ENCODED | yes (no capability) |
| dumbbell | deviation | — | RELATIONSHIP_CHARTS | yes (no capability) |
| bump | ranking | — | RELATIONSHIP_CHARTS | yes (no capability) |
| sankey | flow | — | RELATIONSHIP_CHARTS | yes (no capability) |
| kde | — | — | smells DENSITY_MARKS only | yes (double orphan) |
| population_pyramid | — | — | IT011 inventory (gen-input-types.py:78) | yes (double orphan) |
| butterfly | — | — | IT011 inventory (gen-input-types.py:78) | yes (double orphan) |

Confirmed: none of the 12 appear in any `CHART_CAPABILITIES` value; the nine
distribution/deviation/ranking/flow marks have a relationship home only; `kde`,
`population_pyramid`, `butterfly` have neither and are reachable only via the
smells set / the IT011 input-type inventory respectively. **The scope's orphan
list is exact against the live tree.** Verdict: **still-valid.**

## Premise 2 — BANNED_TYPES still the same five

Live `dsx/checks/viz.py:36–42` `BANNED_TYPES` = `{3d_bar, 3d_pie, 3d_line, radar,
dual_axis_line}` — exactly five, unchanged. They are a flat `dict[str,str]`
(mark → distortion reason) routed to `DSX-VIZ-001`; there is **no** refusal-entry
structure cross-referencing a banning code + perception citation yet, so REQ-P21-02
is unbuilt. Verdict: **still-valid.**

## Premise 3 — live catalogue count (do NOT assume 275)

Re-measured three independent ways, all agree on **275**:

1. `tests/test_finding_catalogue_invariant.py` — `2 passed` (two independent
   readings of the generated artifact — declared Total line vs enumerated
   `DSX-*` rows — must both equal 275).
2. Declared Total line, `references/finding-codes.md:16` — `**Total: 275 codes.**`
3. `grep -oE '(DSX-[A-Z]+-[0-9]+|SQL-[0-9]+)' references/finding-codes.md | sort -u
   | wc -l` → **275**.

The 275-code baseline holds. This is the set-identity baseline for REQ-P21-03's
zero-new-codes diff and the floor for REQ-P22-05 / D-06 numbering. Verdict:
**still-valid.**

## Per-requirement verdicts (all 16)

| Req | Verdict | Live-tree evidence |
|---|---|---|
| REQ-P21-01 (every-mark-has-a-home invariant + home the 12 orphans) | still-valid | orphan table above; no such invariant test exists |
| REQ-P21-02 (banned types → first-class refusal entries) | still-valid | BANNED_TYPES is a flat reason-dict, no refusal/citation structure |
| REQ-P21-03 (zero new codes, set-identity diff vs 275) | still-valid | baseline re-measured = 275 |
| REQ-P22-01 (merged catalog ~80, 3 axes + citation) | still-valid | no catalog artifact exists |
| REQ-P22-02 (uncertainty function family) | still-valid | no `uncertainty` relationship key; no fan_chart/quantile_dot/half_eye/gradient-CI vocab in dsx/ |
| REQ-P22-03 (facet_by orthogonal declaration) | still-valid | no `facet_by` anywhere in dsx/ |
| REQ-P22-04 (5-layer question→chart heuristic edits) | still-valid | references/question-taxonomy.md + chart-selection.md exist (edit targets), heuristic unbuilt |
| REQ-P22-05 (gate extensions + Cleveland–McGill structural criterion) | still-valid | baseline 275; no perceptual-rank ordering test |
| REQ-P23-01 (styles/*.mplstyle set + license headers) | still-valid | no `styles/` dir, no `.mplstyle` files |
| REQ-P23-02 (templates/dsx_plotstyle.py helper) | still-valid | file absent |
| REQ-P23-03 (determinism recipe + double-render hash test) | still-valid | no helper, no such test |
| REQ-P23-04 (per-chart-type snippet catalog) | still-valid | no snippet catalog artifact |
| REQ-P23-05 (WCAG AA palettes + per-palette citations) | still-valid | no style files to carry them |
| REQ-P24-01 (end-to-end portfolio exemplar) | still-valid | no exemplar artifact |
| REQ-P24-02 (known-bad fixtures + catch-rate/FPR re-baseline) | still-valid | depends on P22 codes not yet minted |
| REQ-P24-03 (milestone audit prerequisites) | still-valid | terminal; depends on P21–23 |

**No requirement is already-satisfied; none is contradicted.** The inherited scope
is sound and needs execution, not re-scoping — consistent with the brief's
standing finding. Phase 21 (orphan homing + refusal entries) is unblocked and
first in stage order.
