# v2.3 Scope recheck — inherited scope vs. the live tree

**Unit:** S0-2. **Date:** 2026-08-29. **Verdict in one line:** the inherited
scope (`.planning/research/V2.3-V2.4-SCOPE.md`, `REQUIREMENTS.md`) holds against
the live tree. All 22 requirements are **still-valid** (unsatisfied and
uncontradicted); none are already-satisfied, none contradicted. The tree is
exactly the pre-milestone state the scope assumed — this milestone needs
execution, not re-scoping.

Method: the brief requires the ledger's claims be reconciled against the repo
before planning on them. This re-confirms the five load-bearing premises the
scope rests on at their live locators, re-measures the catalogue count rather
than trusting the number in the scope prose, then renders a per-requirement
verdict.

## Load-bearing premises (re-confirmed at live locators)

| # | Premise | Locator | Verdict |
|---|---|---|---|
| 1 | `recommend_test` small-expected-cell fallback emits `fisher_exact`, not `boschloo_exact` | `dsx/checks/stats.py:65` — alternatives `["fisher_exact (any expected cell < 5)", "chi_square", "bootstrap"]` in the two-proportion branch | CONFIRMED |
| 2 | `boschloo_exact` absent from `NONPARAMETRIC_TESTS` | `dsx/checks/stats.py:23–26` — set lists `fisher_exact`, `mcnemar`, `chi_square`, … no `boschloo_exact` | CONFIRMED |
| 3 | Doc side of the divergence still names Boschloo (so doc and code genuinely disagree) | `references/test-selection.md:10` (table cell "Boschloo's exact test if any expected cell < 5") + footnote `[^1]` lines 26–30 citing Lydersen, Fagerland & Laake (2009) | CONFIRMED |
| 4 | `QUESTION_TYPES` is still the closed 5-key list (so `estimand_kind` is a genuinely new, additive vocabulary) | `dsx/spec.py:22–28` — `descriptive`, `diagnostic`, `predictive`, `causal`, `prescriptive`; `estimand_kind` absent from `dsx/`, `references/`, `templates/` | CONFIRMED |
| 5 | `recommend_test`'s `time_to_event` fallthrough is unconditional (bare final return, no guard) | `dsx/checks/stats.py:128–129` — `return _rec("log_rank", …)` after all four explicit outcome branches; no `if outcome == "time_to_event"` guard | CONFIRMED |
| 6 | Live catalogue count — **re-measured, not assumed** | `scripts/gen-finding-catalogue.py` (print mode) reports **`Total: 260 codes`**, identical to the checked-in `references/finding-codes.md` header. No drift. | **260** |

The scope prose said "do NOT assume 260"; the live count *is* 260, now measured
rather than assumed. **260 is the REQ-P17-05 zero-new-codes baseline** (set-identity
diff reference for phase-end verification).

Extension targets named by the requirements all exist and are untouched:
`tests/test_no_shapiro_autoswitch.py` (the REQ-P18-06/P20-03 no-autoswitch
template), `tests/test_known_bad_corpus.py` (REQ-P20-01), `dsx/mathx.py` and
`templates/APA-TABLE-research.md` (REQ-P18-05 wiring). No `17-CONTEXT.md` or any
phase directory exists yet (`.planning/phases/` holds only `.gitkeep`).

## Per-requirement verdict

Legend: **still-valid** = unsatisfied and uncontradicted, work remains as scoped.

### Phase 17 — foundation

| Req | Verdict | Evidence / rationale |
|---|---|---|
| REQ-P17-01 Boschloo reconciliation | still-valid | Divergence live: doc says Boschloo (`test-selection.md:10`+`[^1]`), code emits `fisher_exact` (`stats.py:65`), `boschloo_exact` not in `NONPARAMETRIC_TESTS` (`stats.py:23–26`); no regression test binds them (proven by the divergence surviving). |
| REQ-P17-02 `estimand_kind` closed vocabulary | still-valid | `estimand_kind` absent from `dsx/`/`references/`/`templates/`. `QUESTION_TYPES` remains the closed 5-key list — the new vocabulary would be additive, not a rename. |
| REQ-P17-03 D-12a disposition table | still-valid | No `17-CONTEXT.md`; no disposition table recorded for any Phase 18/19 gate check. |
| REQ-P17-04 D-06 range pre-allocation + fallthrough guard | still-valid | Catalogue re-measured = 260. `time_to_event` fallthrough unconditional (`stats.py:128–129`); no test pins its position; no range-allocation note committed. |
| REQ-P17-05 Zero new codes this phase | still-valid (invariant) | Baseline recorded here = **260 codes**; set-identity diff is the phase-end gate, nothing added yet. |

### Phase 18 — correlation, association, agreement

| Req | Verdict | Evidence / rationale |
|---|---|---|
| REQ-P18-01 correlation rows (Pearson/Spearman/Kendall/point-biserial/phi) | still-valid | No `estimand_kind`-keyed correlation rows exist (the key itself is absent); no distance/partial pointer rows. |
| REQ-P18-02 agreement/reliability rows (kappa family, ICC, Bland-Altman, ω pointer) | still-valid | None of these rows present in `stats.py` / `test-selection.md`. |
| REQ-P18-03 gate: correlation scale/kind match | still-valid | No DSX-STA code enforcing Pearson-on-ordinal or agreement-estimand blocking. |
| REQ-P18-04 gate: agreement declaration completeness | still-valid | No ICC-triple / weighted-kappa-weights / kappa-companion gate present. |
| REQ-P18-05 effect-size bands in `mathx.py` + APA wiring | still-valid | `mathx.py` and `APA-TABLE-research.md` exist as targets; Kendall's W / kappa / ICC bands not added. |
| REQ-P18-06 no-autoswitch invariant extends to this category | still-valid | Template `test_no_shapiro_autoswitch.py` present; no correlation-category extension yet. |

### Phase 19 — RM, trend, categorical, resampling, post-hoc

| Req | Verdict | Evidence / rationale |
|---|---|---|
| REQ-P19-01 RM rows + unconditional Greenhouse-Geisser; two-stage-sphericity gate | still-valid | No RM-ANOVA/Friedman/Cochran-Q/Page rows; no sphericity gate. |
| REQ-P19-02 trend rows + declared-field gates | still-valid | No Cochran-Armitage/Jonckheere/Mann-Kendall rows or gates. |
| REQ-P19-03 categorical rows (N-1 chi-square, CMH, FFH, G-test, GOF, log-linear) | still-valid | None present; Yates not yet deprecated-row. |
| REQ-P19-04 resampling rows + seed/B/unit/method quadruple gate | still-valid | No permutation/bootstrap/BCa rows or resampling-declaration gate. |
| REQ-P19-05 post-hoc rows + omnibus-family match gate | still-valid | No Games-Howell/Tukey/Dunnett/Dunn/Nemenyi/Scheffé rows; no SNK/LSD deprecation. |
| REQ-P19-06 negative gates (variance-as-precondition; observed-power ban) | still-valid | No such blocking codes; MDE-sensitivity substitute row absent. |
| REQ-P19-07 proportion/count extras (Wilson default, CP, RD/RR/OR, NNT, offset gate) | still-valid | No Wilson-default gate or interval-method rows; count-offset gate absent. |

### Phase 20 — calibration and reporting close

| Req | Verdict | Evidence / rationale |
|---|---|---|
| REQ-P20-01 known-bad fixtures per new code; catch-rate/FPR re-baseline | still-valid | `test_known_bad_corpus.py` present as extension target; no new-code fixtures (no new codes yet). |
| REQ-P20-02 good fixture additive; allowlist extended | still-valid | No new codes to add to the D-05 allowlist; good fixture unchanged. |
| REQ-P20-03 no-autoswitch covers every new category; fallthrough test green | still-valid | Fallthrough currently unguarded (REQ-P17-04); coverage not yet extended. |
| REQ-P20-04 doc/code agreement test binds `test-selection.md`↔`recommend_test` | still-valid | No agreement/mirror test exists — proven by the live Boschloo divergence surviving unflagged. |

## Conclusion

Scope confirmed against the live tree. No requirement is already-satisfied or
contradicted; no re-scoping needed. Proceed to the S0 D-05 evidence packs (S0-3,
S0-4) and then Phase 17 (S1).
