---
phase: 30
plan: 30-01
type: execute
wave: 1
depends_on: []
requirements: [REQ-P30-01, REQ-P30-03]
files_modified:
  - .planning/phases/30-calibration-rebaseline/_measure_readout.py
  - .planning/phases/30-calibration-rebaseline/30-READOUT.md
autonomous: true

must_haves:
  truths:
    - "Running _measure_readout.py on the real 3.12.10 interpreter prints, LIVE over the 42 known-bad + 15 good-control corpus, the headline pair (miss-rate, FPR), the stratified PRESENT/ABSENT partitions with independent denominators, the FPR over 15 good-control specs, and per-family friction."
    - "30-READOUT.md transcribes every number from that live JSON (never an estimate), states the FPR as a one-sided 95% upper bound (not a point estimate), and frames the 1.0 miss-rate as a construction invariant whose evidential content is the per-case fires_at_any_severity:false confirmations for DSX-ML-034 and DSX-CLM-034 (D-10 / 12-READOUT F3)."
    - "The durable reproducer test_stratified_catch_rate_and_fpr_report passes over the grown corpus and asserts the 5-case ABSENT partition >= floor 3."
    - "No frozen surface (dsx/, examples/, references/) changed during measurement; the companion is read-only and shells dsx gate in a fresh tempdir off the gate path."
  artifacts:
    - ".planning/phases/30-calibration-rebaseline/_measure_readout.py"
    - ".planning/phases/30-calibration-rebaseline/30-READOUT.md"
  key_links:
    - "30-READOUT.md numbers == _measure_readout.py JSON output == test_stratified_catch_rate_and_fpr_report assertions (one source of truth, three views)."
    - "companion ABSENT loop excludes the kind: target sidecar via the kind != 'miss' guard; PRESENT loop iterates the same _CRITICAL_THRESHOLD_POINTS axis the reproducer uses (test line 1984), so DSX-COH-041 is counted at its plan cell exactly as the reproducer counts it."
---

<objective>
Re-baseline the stratified calibration over the grown corpus (42 known-bad + 15
good-control) and record it in a committed 30-READOUT.md mirroring 12-READOUT.md.
Measurement runs LIVE off the gate path through a read-only _measure_readout.py
companion copied forward from the Phase-12 template; the durable reproducer of record
is the existing unittest test_stratified_catch_rate_and_fpr_report, not the companion.

Purpose: Phase 30 MEASURES an already-built corpus (Phases 27/28/29 wired the three
v2.6 evidence cases and gate-verified them). This plan produces the terminal
re-baselined headline pair + strata + friction so the durable record and the two doc
refreshes (plan 30-02) can cite measured evidence, never an estimate.

Output:
- .planning/phases/30-calibration-rebaseline/_measure_readout.py (read-only companion)
- .planning/phases/30-calibration-rebaseline/30-READOUT.md (the recorded readout, with a
  placeholder for the adversarial Statistician review that lands downstream at S6-3/S6-4)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-calibration-rebaseline/30-CONTEXT.md
@.planning/milestones/v2.0.0-phases/12-calibration/12-READOUT.md
@.planning/milestones/v2.0.0-phases/12-calibration/_measure_readout.py
@tests/test_known_bad_corpus.py

Interpreter (bind once, use everywhere in this plan):
- REAL 3.12.10 = C:\Users\Benutzer1\AppData\Local\Programs\Python\Python312\python.exe
- A bare `python3` on this machine is a STUB that reports the matplotlib determinism test
  as SKIPPED and can misreport measurement. NEVER use `python3` for any step here.

Harness API delta the companion must respect (determined at plan time by diffing the
Phase-12 template against the current tests/test_known_bad_corpus.py):
- The Phase-12 template copies forward CLEANLY. The ABSENT loop already skips the new
  kind: target sidecar via `if data.get("kind", "miss") != "miss": continue` (template
  line 59), matching the reproducer at test line 2004.
- The PRESENT loop iterates `_CRITICAL_THRESHOLD_POINTS` which is still ("plan",
  "execute") (tests line 57). The reproducer's own PRESENT loop iterates that same axis
  (test line 1984). subgroup-harm-without-disposition declares DSX-COH-041 at
  plan/verify/ship in _TARGET_DEFECT_CODES (tests lines 335-338); on the plan/execute
  axis only its `plan` cell is a PRESENT-partition cell, counted exactly as the
  reproducer counts it. Do NOT widen the PRESENT axis — that would diverge from the
  durable reproducer.
- Signatures unchanged from the template's calls: _headline(present, absent, fpr),
  _friction(blocking, own), _friction_rate(total, cells), _false_positive_findings(
  findings, noise), _non_target_in_profile_cells(effective, slugs, points),
  _classify_target_defect(slug, point, exit_code, findings, target_map) (it gained
  optional severity=/incidental= with backward-compatible defaults). _gate_findings
  gained an optional block_on=None param, so the template's 2-arg calls are unchanged.
</context>

<constraints>
SINGLE-WRITER PROHIBITION. This plan MUST NOT modify REQUIREMENTS.md, .planning/STATE.md,
or .planning/ROADMAP.md. Those tracking files are orchestrator-only; a subagent never
edits them. They may be read but never appear in files_modified.

FROZEN SURFACES (must stay byte-identical). dsx/checks/dq.py, dsx/cli.py,
dsx/checks/viz.py, the chart corpus, every fixture under examples/known-bad/ and
examples/good-corpus/, and references/finding-codes.md. This plan READS the corpus; it
never reshapes it. Reshaping a frozen case to move a number is a D-13 violation.

ZERO MINT. This plan mints no finding codes. The measurement routes entirely to existing
codes.
</constraints>

<decision_coverage>
- GA-1 (persona-round mode + readout shape): this plan delivers the committed
  30-READOUT.md mirroring 12-READOUT.md (headline pair -> stratified PRESENT/ABSENT ->
  FPR /15 -> per-family friction -> limits -> adversarial Statistician review placeholder),
  whose durable reproducer is the EXISTING test_stratified_catch_rate_and_fpr_report,
  numbers extracted by the read-only _measure_readout.py companion. The heavy adversarial
  Statistician review is deferred to the readout recording (S6-3/S6-4), NOT executed here.
- GA-2 (classification is FORCED, not re-decided): the companion CONFIRMS the committed
  wiring live. ABSENT partition = 5 miss sidecars >= floor 3; PRESENT partition gains the
  DSX-COH-041 plan cell (target). The readout states the 1.0 miss-rate is a construction
  invariant (curated misses), evidential content = the per-case fires_at_any_severity:false
  confirmations for the two NEW misses DSX-ML-034 and DSX-CLM-034; the headline is the
  PAIR, never catch-rate alone.
- GA-4 (verify-not-build): this plan builds only the .planning/ companion + readout; the
  reproducer already exists and is not re-authored.
- D-06 / REQ-P30-03 (zero mint): this plan records the live catalogue count as readout
  context only; the authoritative set-identity 279 -> 279 gate runs in plan 30-02. The
  readout must state the measured live count, not assume 279 has held.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Copy _measure_readout.py forward, adapt for the current harness, run it LIVE</name>
  <files>.planning/phases/30-calibration-rebaseline/_measure_readout.py</files>
  <action>
Copy .planning/milestones/v2.0.0-phases/12-calibration/_measure_readout.py to
.planning/phases/30-calibration-rebaseline/_measure_readout.py. Fix the ROOT path so it
resolves the repo root from the NEW location (the template uses
`Path(__file__).resolve().parents[3]` because it sat three dirs below root under
.planning/milestones/v2.0.0-phases/12-calibration/; the new file sits at
.planning/phases/30-calibration-rebaseline/, which is also three levels below root, so
parents[3] is still correct — VERIFY by asserting `(ROOT / "tests" /
"test_known_bad_corpus.py").exists()` at import and adjust the parents index only if that
assertion fails). Keep the companion READ-ONLY and OFF the gate path: it imports the live
harness functions and shells `dsx gate` in a fresh tempdir exactly as the template does;
it computes nothing on the gate path (D-01/D-02).

The template body copies forward without behavioural change: the ABSENT loop's
`if data.get("kind", "miss") != "miss": continue` already excludes the new kind: target
sidecar, and the PRESENT loop's _CRITICAL_THRESHOLD_POINTS iteration already matches the
durable reproducer's PRESENT axis. Do NOT widen the PRESENT axis and do NOT alter the
friction cell axis — both must stay identical to the reproducer so the printed numbers
equal the numbers the reproducer asserts.

ADD ONE supplementary read-only block (the honest inverse of the ABSENT loop's
fires_at_any_severity detail): for the single kind: target sidecar
(subgroup-harm-without-disposition), measure DSX-COH-041's severity at all four gate
points (plan, execute, verify, ship) and record per-point severity into the JSON under a
new key `out["target"]`, WITHOUT touching the PRESENT-partition denominator. This lets the
readout cite the full plan/verify/ship CRITICAL catch profile as the target's evidential
content, mirroring how the two new misses cite fires_at_any_severity:false. Mint no new
gate calls beyond these four read-only tempdir gates for the one target slug.

Run the companion on the REAL 3.12.10 interpreter and capture the full JSON to inspect.
Confirm the measured shape matches the forced construction facts (GA-2): headline
miss_rate == 1.0 over an ABSENT denom of 5; headline fpr == 0.0 over an FPR denom of 15;
absent_floor == 3; the two NEW misses (absent_code DSX-ML-034 on feature-origin-only-leak,
DSX-CLM-034 on magnitude-without-computed-effect) each show fires_at_any_severity:false;
the target block shows DSX-COH-041 CRITICAL at plan/verify/ship. If ANY of these diverges,
STOP and report — a divergence means a frozen surface moved (a D-13/D-14 finding), not a
number to record.
  </action>
  <verify>
    <automated>& "C:\Users\Benutzer1\AppData\Local\Programs\Python\Python312\python.exe" .planning/phases/30-calibration-rebaseline/_measure_readout.py</automated>
  </verify>
  <done>The companion runs on the real 3.12.10 interpreter and prints valid JSON with:
headline.miss_rate == 1.0, absent.denom == 5, absent.misses == 5; headline.fpr == 0.0,
fpr.denom == 15; absent_floor == 3; the DSX-ML-034 and DSX-CLM-034 cases each show
fires_at_any_severity == false; out["target"] shows DSX-COH-041 at CRITICAL for plan,
verify and ship. `git status --porcelain dsx/ examples/ references/` is empty after the
run.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the durable reproducer still holds over the grown corpus</name>
  <files>.planning/phases/30-calibration-rebaseline/_measure_readout.py</files>
  <action>
Run the durable reproducer of record on the REAL 3.12.10 interpreter:
test_known_bad_corpus.TestKnownBadCorpus.test_stratified_catch_rate_and_fpr_report. It
must PASS over the grown corpus, asserting independent PRESENT/ABSENT denominators, the
5-case ABSENT partition >= floor 3, the headline miss-rate depends on the ABSENT partition
alone, and the target-present-invariance proof (injecting a synthetic easy catch cannot
move the headline). This is the reproducer, not the companion — the readout's frontmatter
names this test as reproducing_gate. Do not author a new reproducer. If it fails, STOP and
report: a failure means the corpus or a frozen surface diverged from the wiring the readout
would record.
  </action>
  <verify>
    <automated>& "C:\Users\Benutzer1\AppData\Local\Programs\Python\Python312\python.exe" -m unittest tests.test_known_bad_corpus.TestKnownBadCorpus.test_stratified_catch_rate_and_fpr_report -v</automated>
  </verify>
  <done>The reproducer passes (OK) on the real 3.12.10 interpreter over the 42+15 corpus;
its assertions on the 5-case ABSENT floor and the invariance proof hold.</done>
</task>

<task type="auto">
  <name>Task 3: Write 30-READOUT.md from the measured numbers (never an estimate)</name>
  <files>.planning/phases/30-calibration-rebaseline/30-READOUT.md</files>
  <action>
Author 30-READOUT.md mirroring the section shape of 12-READOUT.md, transcribing ONLY the
numbers printed by the Task-1 companion run (never a typed-in estimate). Sections:

Frontmatter: phase: 30; artifact: calibration-readout; requirement: REQ-P30-01 (+
REQ-P30-03 zero-mint context); measured: <the date of the live run>; reproducing_gate:
the exact command from Task 2; statistician_review: `pending — adversarial dsx-statistician
review lands at S6-3/S6-4 (GA-1), against these measured numbers`.

Section 1 — Headline pair (miss-rate, FPR): a two-row table with the LIVE values. Carry
the D-10 framing verbatim in spirit: the headline is the PAIR, never catch-rate alone.
Carry the 12-READOUT F3 caveat: the ABSENT partition is curated to misses (each sidecar
kind: miss), so miss-rate 1.0 = 5/5 is a CONSTRUCTION INVARIANT carrying no sampling
information about a miss propensity; the evidential content is the per-case
fires_at_any_severity:false confirmations for the two NEW misses DSX-ML-034
(feature-origin-only-leak) and DSX-CLM-034 (magnitude-without-computed-effect), each
genuinely silent at every severity across all four gate points.

Section 2 — Stratified PRESENT vs ABSENT (D-10): 2a PRESENT partition, one row per
(fixture x gate-point) cell the effective target map expects, live-verified CAUGHT,
including the new DSX-COH-041 target cell at plan. Note explicitly, from out["target"],
that DSX-COH-041 additionally fires CRITICAL at verify and ship (the full catch profile),
framed as the honest inverse of the two misses. 2b ABSENT partition, one row per miss
sidecar (5 rows), each with its nearest silent shipped code, the brief §6.5 item it
promotes, missed-at-CRITICAL, and Fires-at-any-severity-anywhere (all five: no). Name the
two NEW misses as this milestone's additions.

Section 3 — FPR over the good-control corpus: 0 / 15. State the one-sided 95% upper
confidence bound, NOT a point estimate of a near-zero rate: Clopper-Pearson
1 - 0.05^(1/15) is approximately 0.181; rule-of-three approximately 3/15 = 0.20. A true
false-positive rate as high as roughly 1-in-5 on well-formed input is fully consistent
with observing 0/15. Note the denominator moved from 12 (Phase 12) to 15.

Section 4 — Friction per family (raw and net, never net-only): the corpus totals and the
per-cell raw/net rates from out["friction"], with the per-family table in a collapsible
block. Report RAW and NET so the over-block stays visible (D-11).

Section — Limits and honest caveats: small denominators with explicit bounds (FPR n=15 is
bounded, not estimated; NO interval on the curated miss-rate); the corpus is not a random
sample; 0.0 FPR is a clean-by-construction result on 15 specs the harness certifies clean.

Section — Statistician adversarial review: a PLACEHOLDER heading only, marked `to be
completed at S6-3/S6-4 (RECORD-WITH-AMENDMENTS gate)`. Do NOT write the review here — GA-1
defers it to the readout recording against measured output.

Every quantity in the readout MUST equal a value in the Task-1 JSON. Do not hardcode a
number the companion did not print.
  </action>
  <verify>
    <automated>& "C:\Users\Benutzer1\AppData\Local\Programs\Python\Python312\python.exe" -c "import re,sys; t=open(r'.planning/phases/30-calibration-rebaseline/30-READOUT.md',encoding='utf-8').read(); miss=re.search(r'5\s*/\s*5',t); fpr=re.search(r'0\s*/\s*15',t); bound=re.search(r'0\.18',t); sys.exit(0 if (miss and fpr and bound) else 1)"</automated>
  </verify>
  <done>30-READOUT.md exists with all sections; the headline shows the measured pair
(miss-rate 5/5, FPR 0/15); Section 3 states the one-sided upper bound (~0.181), not a
point estimate; the two new misses DSX-ML-034 and DSX-CLM-034 appear with
fires_at_any_severity:false; the DSX-COH-041 target cell appears CAUGHT with the
plan/verify/ship profile; the Statistician review section is a marked placeholder;
frontmatter names test_stratified_catch_rate_and_fpr_report as reproducing_gate. No number
in the readout is absent from the Task-1 JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus fixtures -> companion (read) | The companion reads frozen fixtures and shells `dsx gate` in a tempdir; it must never write back into examples/. |
| measured JSON -> 30-READOUT.md (transcription) | Numbers cross from the live run into the durable record; an estimate typed here silently corrupts the baseline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Tampering | 30-READOUT.md numbers | high | mitigate | Every readout number must equal a Task-1 JSON value; the reproducer (Task 2) independently asserts the partitions + floor; the Task-3 verify greps for the measured 5/5, 0/15, and the ~0.181 bound. |
| T-30-02 | Information disclosure (misleading record) | FPR reporting | high | mitigate | Section 3 states the one-sided 95% upper bound (~0.181), never a point estimate of a ~0 rate; the miss-rate carries the construction-invariant caveat and NO interval (D-10 / F3). |
| T-30-03 | Tampering | dsx/, examples/, references/ (frozen) | critical | mitigate | Companion is read-only, off gate path, tempdir-per-call; Task-1 done-check requires `git status --porcelain dsx/ examples/ references/` empty; any diff is a D-13/D-14 finding, STOP. |
| T-30-04 | Spoofing (wrong interpreter) | measurement run | medium | mitigate | All steps run on the REAL 3.12.10 interpreter; the bare `python3` stub is forbidden (it skips the matplotlib determinism test and can misreport). |
| T-30-05 | Tampering (silent drop/double-count) | kind: target handling | high | mitigate | Companion ABSENT loop excludes kind: target via the kind guard; the supplementary target block measures DSX-COH-041 without touching the PRESENT denominator; the reproducer's invariance proof confirms an added catch cannot move the headline. |
</threat_model>

<verification>
All on the REAL 3.12.10 interpreter, from a clean tree:
1. Companion runs and prints valid JSON with the forced construction facts (Task 1).
2. `git status --porcelain dsx/ examples/ references/` is empty after the companion run.
3. test_stratified_catch_rate_and_fpr_report passes (Task 2).
4. 30-READOUT.md transcribes only measured numbers; Task-3 grep gate passes.
</verification>

<success_criteria>
- _measure_readout.py exists at the phase path, is read-only/off-gate-path, and prints the
  live headline pair, strata, FPR /15, friction, and the target block.
- 30-READOUT.md records the terminal re-baseline mirroring 12-READOUT.md, every number
  measured, the FPR as an upper bound, the miss-rate as a construction invariant, with the
  Statistician review as a marked placeholder.
- The durable reproducer passes over the 42+15 corpus.
- No frozen surface changed; zero codes minted.
</success_criteria>

<output>
Create .planning/phases/30-calibration-rebaseline/30-01-SUMMARY.md when done, recording
the measured headline pair, the ABSENT/PRESENT/FPR denominators, the target catch profile,
the live catalogue count observed, and confirmation that git status on dsx/ examples/
references/ was clean.
</output>
