---
phase: 25-hermetic-profile-depth
plan: 02
type: tdd
wave: 2
depends_on: ["25-01"]
files_modified:
  - dsx/profiler.py
  - tests/fixtures/profiler/
  - tests/test_profiler_hermetic.py
autonomous: true
requirements: [REQ-P25-01, REQ-P25-02]
must_haves:
  truths:
    - "[D-02] a new `_extract_hour(value) -> int|None` returns None when the time token carries no hour group; `_parse_date` and the `dates: list[date]` accumulator are left byte-for-byte unchanged so time.min/time.max/time.max_gap_days stay frozen (25-RESEARCH.md Pitfall 2)."
    - "[D-02] share_at_hour_00 denominator is the count of time-bearing rows (rows whose value carries a time token); it is null when no row carries one — a date-only column never reads 1.0."
    - "[edge REQ-P25-01/boundary] share_at_hour_00 fixtures: all-midnight → 1.0; half-midnight → 0.5; all date-only → null; 2 date-only + 3 timed (1 midnight) → 0.3333333333333333."
    - "[D-02] rows_per_day {min, median, max} counts per calendar day (day-grain), appended under the existing `time:` block after max_gap_days."
    - "[D-02] first_period_ratio / last_period_ratio are computed at ISO-week grain over populated weeks (last = rows(last populated week) / mean(previous 4 populated weeks); first symmetric on the next 4); fewer than 5 populated weeks → both null."
    - "[edge REQ-P25-01/empty] edge-ratio fixture W1..W6 populated counts [2,10,10,10,10,1] → first_period_ratio=0.2, last_period_ratio=0.1; a fixture with <=4 populated weeks → both null."
    - "[D-02] the `unit` block is a NEW top-level block emitted only when the unit column is supplied, omitted entirely (never null) when absent (D-01)."
    - "[edge REQ-P25-01/empty] rows_per_unit {p50, p95, max}: fewer than 2 distinct units → p50/p95 null, max defined for >=1 unit."
    - "[edge REQ-P25-01/ordering] largest_unit_share tie-break is count desc then unit-string asc (does not change the value); rows_per_unit uses inclusive/type-7 quantiles over per-unit counts."
    - "[edge REQ-P25-02/empty] unit_counts fixture [1,2,3,4,100] → rows_per_unit.p50=3, rows_per_unit.max=100, largest_unit_share=0.9090909090909091; rows_per_unit.p95 pinned from an actual run on the real interpreter."
    - "[edge REQ-P25-02/ordering] every new time and unit statistic is byte-identical across two runs and across a shuffled row order (isocalendar bucketing + quantiles over sorted input are order-independent)."
  artifacts:
    - dsx/profiler.py::_extract_hour
    - dsx/profiler.py (time-block extension; unit block; profile_csv gains a `unit` keyword parameter)
    - tests/fixtures/profiler/time_hour_all_midnight.csv
    - tests/fixtures/profiler/time_hour_half.csv
    - tests/fixtures/profiler/time_hour_date_only.csv
    - tests/fixtures/profiler/time_hour_mixed.csv
    - tests/fixtures/profiler/time_rows_per_day.csv
    - tests/fixtures/profiler/time_edge_ratio.csv
    - tests/fixtures/profiler/time_edge_ratio_short.csv
    - tests/fixtures/profiler/unit_counts.csv
  key_links:
    - "hour retention uses a SEPARATE accumulator (e.g. hour_of_time_bearing_rows: list[int]); it must never append to the frozen `dates` list — the pre-existing-key golden from 25-01 is the trip-wire."
    - "new time keys append AFTER time.max_gap_days; the unit block appends AFTER sentinels_found — never interleaved, preserving _dump insertion-order byte-stability."
  prohibitions:
    - "No new finding code is minted; no gate module changes."
    - "No pandas / numpy / scipy or any non-stdlib import is added."
    - "No change to _parse_date's signature or the `dates` accumulator (would perturb the frozen time.* keys)."
    - "No manual //7 ISO-week arithmetic — use date.isocalendar() (25-RESEARCH.md Don't Hand-Roll)."
    - "No edits to REQUIREMENTS.md, STATE.md, or ROADMAP.md."
---

<objective>
Extend `profile_csv` with the time-integrity numbers (rows_per_day, ISO-week edge-period ratios,
share_at_hour_00) and the per-unit numbers (rows_per_unit, largest_unit_share) the explore
protocol's steps 1a and 3a currently ask the agent to compute — additively, without touching the
three frozen `time.*` keys, and without a clock (D-02 hermeticity). `profile_csv` gains a `unit`
keyword parameter here; the CLI flags that feed it land in 25-03.

Purpose: complete the time and unit halves of the trust core so EDA.md copies them (REQ-P25-01,
REQ-P25-02).
Output: additive keys under `time:` and a new top-level `unit:` block, plus hermetic
reference-value fixtures for every 25-CONTEXT.md time/unit row.

D-01 (append order, unit block omitted when flag absent), D-02 (ISO-week grain, hour retention
via a separate accumulator, inclusive/type-7 quantiles, null-not-zero) are the binding decisions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-hermetic-profile-depth/25-CONTEXT.md
@.planning/phases/25-hermetic-profile-depth/25-RESEARCH.md
@dsx/profiler.py
@.planning/phases/25-hermetic-profile-depth/25-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New function: `_extract_hour(value: str) -> int | None` (dsx/profiler.py).
`profile_csv` gains a `unit: str | None = None` keyword parameter (unknown header → CheckError,
matching the existing --pk/--time validation at dsx/profiler.py L123-127).

New keys appended under `time:` (after max_gap_days): `rows_per_day: {min, median, max}`,
`first_period_ratio`, `last_period_ratio`, `share_at_hour_00`.
New top-level `unit:` block (after sentinels_found, omitted when unit absent):
`rows_per_unit: {p50, p95, max}`, `largest_unit_share`.

New fixture CSVs: time_hour_all_midnight.csv, time_hour_half.csv, time_hour_date_only.csv,
time_hour_mixed.csv, time_rows_per_day.csv, time_edge_ratio.csv, time_edge_ratio_short.csv,
unit_counts.csv.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Time block — hour retention, share_at_hour_00, rows_per_day, ISO-week edge ratios</name>
  <files>dsx/profiler.py, tests/test_profiler_hermetic.py, tests/fixtures/profiler/time_hour_all_midnight.csv, tests/fixtures/profiler/time_hour_half.csv, tests/fixtures/profiler/time_hour_date_only.csv, tests/fixtures/profiler/time_hour_mixed.csv, tests/fixtures/profiler/time_rows_per_day.csv, tests/fixtures/profiler/time_edge_ratio.csv, tests/fixtures/profiler/time_edge_ratio_short.csv</files>
  <read_first>
    - dsx/profiler.py — `_DATE_RE` (L23-25, groups 4-6 capture H:M:S), `_parse_date` (L71-79), the time_column branch in the loop (L158-161), and the time_block assembly (L178-193).
    - .planning/phases/25-hermetic-profile-depth/25-CONTEXT.md — D-02 "Time block" vocabulary, the persona-round resolutions (ISO-week grain, share_at_hour_00 null on date-only), and the time reference-value rows.
    - .planning/phases/25-hermetic-profile-depth/25-RESEARCH.md — the `_extract_hour` code example, Pitfall 2 (do not touch the `dates` accumulator), and the isocalendar Don't-Hand-Roll row.
  </read_first>
  <behavior>
    - time_hour_all_midnight.csv (10 rows, all `...T00:00:00`) → share_at_hour_00 = 1.0.
    - time_hour_half.csv (5 × `00:00` + 5 × `13:00`) → share_at_hour_00 = 0.5.
    - time_hour_date_only.csv (all date-only) → share_at_hour_00 = null.
    - time_hour_mixed.csv (2 date-only + 3 timed, 1 at midnight) → share_at_hour_00 = 0.3333333333333333.
    - time_rows_per_day.csv (day counts 1,3,5,7,9) → rows_per_day {min:1, median:5, max:9}.
    - time_edge_ratio.csv (ISO-week populated counts W1..W6 = 2,10,10,10,10,1) → first_period_ratio=0.2, last_period_ratio=0.1.
    - time_edge_ratio_short.csv (<=4 populated weeks) → first_period_ratio=null, last_period_ratio=null.
    - The pre-existing time.min / time.max / time.max_gap_days are unchanged for every fixture (25-01 golden still green).
  </behavior>
  <action>
    RED first: add TestTimeBlock to tests/test_profiler_hermetic.py asserting the values above; write the seven fixtures with the exact day/week distributions named. GREEN: add `_extract_hour(value)` per 25-RESEARCH.md — re-match `_DATE_RE`, return None when the match is absent or group(4) (the hour) is None, else int(group(4)); do NOT change `_parse_date` or the `dates` list. In the loop's time_column branch, additively populate a separate `hour_of_time_bearing_rows: list[int]` (append the hour only when `_extract_hour` is not None), a `day_counts: Counter[str]` keyed by the parsed date's isoformat, and an `iso_week_counts: Counter[tuple[int,int]]` keyed by `parsed.isocalendar()[:2]`. After the loop, append to time_block (after max_gap_days, in this order): rows_per_day = {min, median, max} of the day_counts values via statistics (median via statistics.median), or all null when no dated rows; first_period_ratio / last_period_ratio at ISO-week grain over populated weeks sorted by (iso_year, iso_week) — last = rows(last populated week) / mean(previous 4 populated weeks), first = rows(first populated week) / mean(next 4 populated weeks), both null when fewer than 5 populated weeks; share_at_hour_00 = count of 0s in hour_of_time_bearing_rows / len(hour_of_time_bearing_rows), None when that list is empty. Emit Python None for every undefined value.
  </action>
  <verify>
    <automated>"C:/Users/Benutzer1/AppData/Local/Programs/Python/Python312/python.exe" -m unittest tests.test_profiler_hermetic.TestTimeBlock tests.test_profiler_hermetic.TestProfilerDeterminism -v</automated>
  </verify>
  <acceptance_criteria>
    - The command above exits 0.
    - share_at_hour_00 equals 1.0 / 0.5 / null / 0.3333333333333333 for the four hour fixtures respectively.
    - rows_per_day for time_rows_per_day.csv = {min:1, median:5, max:9}.
    - first/last_period_ratio for time_edge_ratio.csv = 0.2 / 0.1; both null for time_edge_ratio_short.csv.
    - The 25-01 pre-existing-key golden and determinism tests still pass (time.min/max/max_gap_days unchanged).
    - New time keys render after max_gap_days inside the `time:` block.
  </acceptance_criteria>
  <done>Every D-02 time number is computed hermetically and the frozen time.* keys are provably untouched.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Unit block (rows_per_unit p50/p95/max, largest_unit_share) + profile_csv unit parameter</name>
  <files>dsx/profiler.py, tests/test_profiler_hermetic.py, tests/fixtures/profiler/unit_counts.csv</files>
  <read_first>
    - dsx/profiler.py — the return dict assembly (L197-209) to see where a top-level block appends after sentinels_found; the unknown-column CheckError pattern (L123-127).
    - .planning/phases/25-hermetic-profile-depth/25-CONTEXT.md — D-02 "--unit block" vocabulary and the rows_per_unit reference row; D-01 (top-level block omitted when flag absent).
    - .planning/phases/25-hermetic-profile-depth/25-RESEARCH.md — Pattern 2 (tie-break) for largest_unit_share.
  </read_first>
  <behavior>
    - profile_csv(unit_counts.csv, unit="unit_id") where unit_id has per-unit row counts 1,2,3,4,100 → unit.rows_per_unit.p50=3, unit.rows_per_unit.max=100, unit.largest_unit_share=0.9090909090909091 (100/110); unit.rows_per_unit.p95 = the exact type-7 interpolation, pinned from a real run.
    - profile_csv(unit_counts.csv) with no unit argument → no `unit` key in the output at all (omitted, not null).
    - A unit column with a single distinct unit → rows_per_unit.p50=null, rows_per_unit.p95=null, rows_per_unit.max defined; unknown unit column name → CheckError (exit 2 at the CLI).
  </behavior>
  <action>
    RED first: add TestUnitBlock asserting the above; write unit_counts.csv (a `unit_id` column whose values repeat to give per-unit counts 1,2,3,4,100 — 110 rows total). Include the omitted-when-absent assertion and the unknown-column CheckError assertion. Determine the exact p95 repr by running profile_csv on the fixture on the real interpreter and pin that literal in the test. GREEN: add a `unit: str | None = None` keyword parameter to profile_csv; validate it against the header exactly like time_column (raise CheckError(f"unit column {unit!r} not in CSV header") when unknown). Accumulate `unit_counts: Counter[str]` keyed by the stripped non-null unit value inside the existing loop. After the loop, when unit is provided, build a top-level `unit` block and append it after sentinels_found in the return dict: rows_per_unit = {p50, p95, max} where p50/p95 come from statistics.quantiles(sorted(per-unit counts), n=100, method="inclusive") selecting the 50th and 95th cut (or the documented single/empty-distinct-unit null rules), max = max per-unit count; largest_unit_share = max count / total rows using an explicit sorted((-count, unit_string)) to pick the winner deterministically. Omit the `unit` key entirely when unit is None.
  </action>
  <verify>
    <automated>"C:/Users/Benutzer1/AppData/Local/Programs/Python/Python312/python.exe" -m unittest tests.test_profiler_hermetic.TestUnitBlock -v</automated>
  </verify>
  <acceptance_criteria>
    - The command above exits 0.
    - unit.rows_per_unit.p50=3, unit.rows_per_unit.max=100, unit.largest_unit_share=0.9090909090909091 for unit_counts.csv.
    - Output has no top-level `unit` key when profile_csv is called without a unit argument.
    - A single-distinct-unit column yields rows_per_unit.p50=null and p95=null with max defined.
    - The `unit` block, when present, renders after sentinels_found.
  </acceptance_criteria>
  <done>The unit block computes rows_per_unit and largest_unit_share hermetically and is omitted when no unit is declared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CSV file → profile_csv | Extract bytes plus a caller-supplied unit column name; no network/auth/eval. The unit name only selects a dict key. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-03 | Tampering | unit column-name argument | low | mitigate | Unknown unit header raises CheckError (exit 2), matching the existing --pk/--time validation; the name indexes a header dict, never eval or a filesystem path. |
| T-25-01 | Denial of Service (local) | hour/day/week/unit Counters | low | mitigate | All new accumulators grow inside the single existing pass; hour retention uses a parallel list, never a second file read; no per-row unbounded structure. |
| T-25-04 | Information disclosure (determinism) | ISO-week bucketing, largest_unit_share tie-break | medium | mitigate | isocalendar() is locale-free and deterministic; largest_unit_share uses explicit sorted((-count, unit)); Task 1/25-01 determinism tests cover byte-identity. |
| T-25-SC | Tampering (supply chain) | package installs | n/a | accept | Zero external packages this phase (stdlib-only per D-01); no install surface. |
</threat_model>

<verification>
- `"C:/Users/Benutzer1/AppData/Local/Programs/Python/Python312/python.exe" -m unittest tests.test_profiler_hermetic -v` green (all classes, including 25-01's).
- Full suite after wave merge: `"C:/Users/Benutzer1/AppData/Local/Programs/Python/Python312/python.exe" -m unittest discover -s tests -q`.
- 25-01 pre-existing-key golden still green (time.* untouched).
</verification>

<success_criteria>
- rows_per_day, first/last_period_ratio, share_at_hour_00 appended under time:; unit block appended top-level and omitted when absent.
- Every 25-CONTEXT.md time and unit reference value asserted and green.
- Frozen time.min/max/max_gap_days unchanged; zero new finding codes; no non-stdlib import.
</success_criteria>

<output>
Create `.planning/phases/25-hermetic-profile-depth/25-02-SUMMARY.md` when done.
</output>
