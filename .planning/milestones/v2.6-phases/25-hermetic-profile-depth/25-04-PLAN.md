---
phase: 25-hermetic-profile-depth
plan: 04
type: execute
wave: 4
depends_on: ["25-03"]
files_modified:
  - templates/DATA-PROFILE.yaml
  - references/data-quality-assertions.md
  - skills/dsx-explore-data/SKILL.md
  - tests/test_profiler_hermetic.py
autonomous: true
requirements: [REQ-P25-01, REQ-P25-02, REQ-P25-03]
must_haves:
  truths:
    - "[REQ-P25-03] templates/DATA-PROFILE.yaml, references/data-quality-assertions.md, and skills/dsx-explore-data/SKILL.md steps 1a/3a/4a/4b/4e/4f say 'copied from the profile' wherever the profiler now supplies the number."
    - "[REQ-P25-03] the named exclusions (null cross-tabs, outlier taxonomy, impossible pairs, robust metric recomputes, staleness) remain agent-computed — the docs still ask the agent to compute exactly those and nothing the profiler now produces."
    - "[edge REQ-P25-03/empty + frozen-vocab] a DQ-gate test proves dsx check dq returns the identical verdict and finding-code set for a profile with every Phase-25 key populated versus the same profile with those keys stripped (the gate reads only its six known keys via .get())."
    - "[edge REQ-P25-03/adjacency] the new profile keys sit adjacent to the six keys the gate reads but never collide with them: dq.py resolves each of its keys via .get() on a named path, so an added numeric/categorical/unit/target key next to a read key is passed over, not merged into the verdict."
    - "[edge REQ-P25-03/precision + frozen] dsx/checks/dq.py, the data[].assertions vocabulary, and the gate profiles are byte-unchanged: git diff --stat -- dsx/checks/dq.py is empty for the phase."
    - "[edge REQ-P25-03/boundary] the finding catalogue is set-identity 276 -> 276: gen-finding-catalogue.py --check exits 0 and tests.test_finding_catalogue_invariant is green with no edit."
    - "[D-04 guard #4] examples/good-DATA-PROFILE.yaml and examples/bad-DATA-PROFILE.yaml are byte-invariant — a committed-digest test pins their sha256 so any future edit fails the suite (read as bytes, CRLF-safe)."
    - "[edge REQ-P25-03/ordering] node install.mjs re-syncs the installed copies of dsx/, templates/, references/ and node install.mjs --check then passes (installed copy == repo copy)."
    - "[REQ-P25-02 D-04 ratification] the D-04 operationalization of 'the committed example profiles regenerate identically' as (example bytes invariant) + (producer invariance proven on fixtures dsx profile can actually produce) is recorded as ratified here, per 25-RESEARCH.md's reasoned PASS."
  artifacts:
    - tests/test_profiler_hermetic.py::TestDocRipple
    - tests/test_profiler_hermetic.py::TestDQGateIgnoresNewKeys
    - tests/test_profiler_hermetic.py::TestExampleProfilesByteInvariant
  key_links:
    - "install.mjs projects dsx/, templates/, references/ (and more) into the host overlay; because 25-01..25-03 changed dsx/ and this plan changes templates/ and references/, the re-sync + --check must run AFTER all code and doc edits land — hence wave 4."
    - "the DQ-gate-ignores test exercises the real dq.check path (spec + profile in a temp dir) — a code read of dq.py's .get() usage is necessary but the CONTEXT frozen-vocabulary note requires the test, not the assumption."
  prohibitions:
    - "No new finding code is minted; dsx/checks/dq.py, the data[].assertions vocabulary, and the gate profiles are NOT edited."
    - "No gate reads a new profile key — the additive keys stay producer-only this milestone."
    - "The profiler must NOT be made to produce the named exclusions (null cross-tabs, outlier taxonomy, impossible pairs, robust metric recomputes, staleness); those stay agent-side."
    - "No edits to REQUIREMENTS.md, STATE.md, or ROADMAP.md."
    - "examples/good-DATA-PROFILE.yaml and examples/bad-DATA-PROFILE.yaml are NOT edited (kept byte-invariant in git)."
---

<objective>
Ripple the three documentation surfaces so they say "copied from the profile" where the profiler
now supplies the number, prove the DQ gate structurally ignores every new key, pin the example
profiles byte-invariant, and re-sync the installer. This is the phase's producer-to-consumer seam
and its zero-drift close-out (REQ-P25-01, REQ-P25-02, REQ-P25-03).

Purpose: EDA.md and the assertions reference now point at the profiler for the trust core, while
the gate path, the assertion vocabulary, and the finding catalogue are proven unchanged.
Output: edited docs; three guard tests (doc ripple, DQ-gate-ignores-new-keys, example byte
invariance); installer re-sync and the phase-end frozen-file / catalogue proofs.

D-01 (additive, no gate reads a new key), D-04 (example byte-invariance ratified; the DQ-ignores
guard) are the binding decisions; REQ-P25-03's named exclusions stay agent-computed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-hermetic-profile-depth/25-CONTEXT.md
@.planning/phases/25-hermetic-profile-depth/25-RESEARCH.md
@templates/DATA-PROFILE.yaml
@references/data-quality-assertions.md
@dsx/checks/dq.py
@.planning/phases/25-hermetic-profile-depth/25-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Edited docs: templates/DATA-PROFILE.yaml (new keys documented as inert comments matching the
existing commented-column style), references/data-quality-assertions.md (the new produced keys
noted), skills/dsx-explore-data/SKILL.md (steps 1a/3a/4a/4b/4e/4f marked "copied from the
profile"; named exclusions kept agent-side).

New guard tests in tests/test_profiler_hermetic.py: TestDocRipple, TestDQGateIgnoresNewKeys,
TestExampleProfilesByteInvariant.

No new runtime symbols. No edits to dsx/. Installer overlay re-synced (not a tracked file).
</artifacts_this_phase_produces>

<tasks>

<task type="execute">
  <name>Task 1: Doc ripple — templates, assertions reference, explore skill steps say "copied from the profile"</name>
  <files>templates/DATA-PROFILE.yaml, references/data-quality-assertions.md, skills/dsx-explore-data/SKILL.md, tests/test_profiler_hermetic.py</files>
  <read_first>
    - templates/DATA-PROFILE.yaml (whole file — note the commented-column style at L19-23).
    - references/data-quality-assertions.md (header + preferred-production-path L1-27).
    - skills/dsx-explore-data/SKILL.md — steps 1a (L83-87), 3a (L159-175), 4a (L181-185), 4b (L200-202), 4e (L296-300), 4f (L308-317).
    - .planning/phases/25-hermetic-profile-depth/25-CONTEXT.md — the "Ripple (docs only)" scope line and the Named exclusions list.
  </read_first>
  <action>
    In templates/DATA-PROFILE.yaml, add the new keys as inert comments (do not add live keys that could change the scaffold's parsed shape): under the commented column example show numeric (min/q1/median/q3/max/mean/sd/n_zero/n_negative/n) and categorical (share_top1/share_top10/rare_share/n_singleton); under the time block show rows_per_day/first_period_ratio/last_period_ratio/share_at_hour_00; add commented unit (rows_per_unit p50/p95/max, largest_unit_share) and target (overall, weekly of week/n/base_rate, weekly_range, verdict) example blocks noting each is omitted when the flag is absent. In references/data-quality-assertions.md, add a short subsection stating these keys are now produced by dsx profile (with --unit/--target) and remain producer-only (no gate reads them this milestone). In skills/dsx-explore-data/SKILL.md, edit steps 1a/3a/4a/4b/4e/4f to say the specific numbers are "copied from the profile" — 1a rows_per_unit and largest_unit_share; 3a share_at_hour_00 and the first/last edge-period ratios; 4a the five-number summary, mean, sd, and the zero/negative counts; 4b the concentration shares (share_top1/share_top10); 4e rare_share and n_singleton; 4f the weekly base rate (overall, weekly_range, verdict) — and keep the named exclusions (null cross-tabs, outlier taxonomy, impossible pairs, robust metric recomputes, staleness) explicitly as what the agent still computes. Add TestDocRipple to tests/test_profiler_hermetic.py: read each of the three files as text and assert (a) every new key token appears in templates/DATA-PROFILE.yaml, and (b) the phrase "copied from the profile" appears in skills/dsx-explore-data/SKILL.md; keep the assertions substring-based so CRLF line endings do not matter.
  </action>
  <verify>
    <automated>"C:/Users/Benutzer1/AppData/Local/Programs/Python/Python312/python.exe" -m unittest tests.test_profiler_hermetic.TestDocRipple -v</automated>
  </verify>
  <acceptance_criteria>
    - The command above exits 0.
    - Every new key token (numeric, categorical, rows_per_day, first_period_ratio, last_period_ratio, share_at_hour_00, rows_per_unit, largest_unit_share, weekly_range, verdict, base_rate) appears in templates/DATA-PROFILE.yaml.
    - "copied from the profile" appears in skills/dsx-explore-data/SKILL.md within steps 1a/3a/4a/4b/4e/4f.
    - The named exclusions still read as agent-computed in the skill; the profiler is not asked to produce them.
    - templates/DATA-PROFILE.yaml still parses as a scaffold (the full unittest suite stays green).
  </acceptance_criteria>
  <done>The three doc surfaces point at the profiler for the trust core and keep the named exclusions agent-side.</done>
</task>

<task type="execute">
  <name>Task 2: Whole-vocabulary guards — DQ gate ignores new keys, example profiles byte-invariant</name>
  <files>tests/test_profiler_hermetic.py</files>
  <read_first>
    - dsx/checks/dq.py — the six .get() field accesses (row_count, primary_key_unique, duplicate_rate, columns.<col>.null_rate, time.max_gap_days, sentinels_found, computed_by); confirm there is no unknown-key rejection branch.
    - tests/test_dsx.py — TestDQAndCoherence.test_dq_blocks_bad_profile / test_dq_passes_good_profile (L3136-3152) for the dq.check(spec, dir) invocation shape.
    - examples/good-DATA-PROFILE.yaml, examples/bad-DATA-PROFILE.yaml (both whole files) and examples/good-ANALYSIS-SPEC.yaml / bad-ANALYSIS-SPEC.yaml for the spec that points at them.
    - .planning/phases/25-hermetic-profile-depth/25-CONTEXT.md — the frozen-vocabulary risk note and D-04 guard #4.
  </read_first>
  <action>
    Add TestDQGateIgnoresNewKeys: in a TemporaryDirectory, copy examples/ (the spec + both profiles); to one copy of the good profile append a full set of Phase-25 keys (a columns.<col>.numeric and .categorical sub-map, top-level unit and target blocks) after the existing keys; leave a second copy unchanged; run dq.check(load(spec), tmp_dir) against each and assert the two reports have the identical set of finding codes and the identical blocks(Severity.HIGH) verdict — proving the gate reads only its six known keys and never notices the new ones. Add TestExampleProfilesByteInvariant: read examples/good-DATA-PROFILE.yaml and examples/bad-DATA-PROFILE.yaml as bytes, compute sha256, and assert each equals a pinned expected digest literal captured from the current committed files (so any future edit to either file fails the suite). Do the digest over raw bytes so CRLF is part of the pinned value.
  </action>
  <verify>
    <automated>"C:/Users/Benutzer1/AppData/Local/Programs/Python/Python312/python.exe" -m unittest tests.test_profiler_hermetic.TestDQGateIgnoresNewKeys tests.test_profiler_hermetic.TestExampleProfilesByteInvariant -v</automated>
  </verify>
  <acceptance_criteria>
    - The command above exits 0.
    - The DQ gate produces the identical finding-code set and HIGH-block verdict with and without the Phase-25 keys present.
    - The two example-profile sha256 digests equal their pinned literals; editing either file would fail the test.
  </acceptance_criteria>
  <done>The gate is proven to ignore every new key, and the example profiles are pinned byte-invariant.</done>
</task>

<task type="execute">
  <name>Task 3: Installer re-sync + phase-end frozen-file and catalogue proofs</name>
  <files>(runs commands; installer writes the host overlay, no tracked-source edits)</files>
  <read_first>
    - install.mjs — the CAPABILITY_PAYLOAD list (L39-47) showing dsx/, references/, templates/ are all projected.
    - scripts/gen-finding-catalogue.py (header docstring L1-12) and tests/test_finding_catalogue_invariant.py (the 276 set-identity, L44-52 / L116-156).
    - .planning/phases/25-hermetic-profile-depth/25-RESEARCH.md — the catalogue 276->276 mechanism and the stray-DECISIONS.jsonl false-fail note; ./.claude/CLAUDE.md push discipline.
  </read_first>
  <action>
    Run node install.mjs to re-sync the installed copies after this phase's dsx/ and doc edits, then run node install.mjs --check and require it to pass (the v2.5.0 real-drift gate). Prove the frozen surface: run git diff --stat -- dsx/checks/dq.py and require empty output for the phase; run the finding-catalogue check on the real interpreter (gen-finding-catalogue.py --check) and require exit 0 with no change to references/finding-codes.md; run tests.test_finding_catalogue_invariant and require green (276 -> 276, no edit). Run the full unittest suite on the real interpreter. If exactly the two explain tests fail, remove any stray DECISIONS.jsonl (root, examples/, examples/known-bad/, templates/) and re-run before treating it as a regression. Record in the SUMMARY that D-04's REQ-P25-02 operationalization is ratified (example bytes invariant + producer invariance on fixtures), per 25-RESEARCH.md's reasoned PASS, and note HQ-40 row 40a (the H&F type-number primary read) remains a non-blocking pending item.
  </action>
  <verify>
    <automated>node install.mjs && node install.mjs --check && "C:/Users/Benutzer1/AppData/Local/Programs/Python/Python312/python.exe" scripts/gen-finding-catalogue.py --check && "C:/Users/Benutzer1/AppData/Local/Programs/Python/Python312/python.exe" -m unittest tests.test_finding_catalogue_invariant -q</automated>
  </verify>
  <acceptance_criteria>
    - The command above exits 0 (installer re-sync + --check pass; catalogue --check exit 0; invariant test green).
    - git diff --stat -- dsx/checks/dq.py is empty for the phase.
    - references/finding-codes.md is unchanged; the catalogue is set-identity 276 -> 276.
    - The full unittest suite is green on the real interpreter (after clearing any stray DECISIONS.jsonl if exactly the two explain tests false-fail).
  </acceptance_criteria>
  <done>The installed copies match the repo, the frozen gate surface is proven unchanged, and the catalogue is still 276.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files -> host overlay (install.mjs) | The installer copies dsx/, templates/, references/ into the runtime overlay; the only trust concern is drift between repo and installed copy, which --check adjudicates. |
| profile file -> dq.check | The gate parses an arbitrary-nesting YAML profile with no schema; new keys must be provably inert to the verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-06 | Tampering (silent drift) | installed overlay vs repo | medium | mitigate | node install.mjs re-sync followed by node install.mjs --check (the v2.5.0 real gate) proves the installed copy matches the repo after the dsx/ and doc edits. |
| T-25-07 | Elevation (scope creep) | new profile keys reaching a gate | high | mitigate | TestDQGateIgnoresNewKeys proves identical verdict with/without the new keys; dsx/checks/dq.py is byte-unchanged (git diff empty); catalogue 276 -> 276 — the profiler stays a producer, never a gate (D-01/D-02). |
| T-25-SC | Tampering (supply chain) | package installs | n/a | accept | Zero external packages this phase (stdlib-only per D-01); the installer copies existing repo files only, no registry fetch. |
</threat_model>

<verification>
- `"C:/Users/Benutzer1/AppData/Local/Programs/Python/Python312/python.exe" -m unittest tests.test_profiler_hermetic -v` green (all classes across the phase).
- Full suite: `"C:/Users/Benutzer1/AppData/Local/Programs/Python/Python312/python.exe" -m unittest discover -s tests -q` green on the real interpreter.
- `node install.mjs && node install.mjs --check` passes.
- `git diff --stat -- dsx/checks/dq.py` empty; `gen-finding-catalogue.py --check` exit 0; catalogue 276 -> 276.
</verification>

<success_criteria>
- The three doc surfaces say "copied from the profile" for the numbers the profiler now supplies; named exclusions stay agent-side.
- The DQ gate provably ignores every new key; dq.py, the assertion vocabulary, and gate profiles are byte-unchanged.
- Example profiles pinned byte-invariant; catalogue set-identity 276 -> 276; node install.mjs --check passes.
- Zero new finding codes; no non-stdlib import anywhere in the phase.
</success_criteria>

<output>
Create `.planning/phases/25-hermetic-profile-depth/25-04-SUMMARY.md` when done.
</output>