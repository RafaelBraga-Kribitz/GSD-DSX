---
phase: 27-evidence-case-feature-origin-only-leak
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - dsx/checks/ml.py
  - tests/test_ml_feature_provenance.py
  - scripts/gen-finding-catalogue.py
  - references/finding-codes.md
  - tests/test_finding_catalogue_invariant.py
  - tests/test_phase20_zero_mint_close.py
autonomous: true
requirements: [REQ-P27-02]
must_haves:
  truths:
    - "Running `dsx validate` on any existing spec that declares no model.feature_provenance block emits no DSX-ML-034 finding — the check returns early (silent-when-absent; the REQ-P27-02 empty/absent boundary)."
    - "A feature declared available_at: after_prediction fires DSX-ML-034 at CRITICAL; available_at: unknown with no waiver fires DSX-ML-034 at HIGH; before_prediction and at_prediction fire nothing (the REQ-P27-02 threshold and one-step-either-side)."
    - "available_at values are compared after normalize(), so case and whitespace variants of the closed vocabulary resolve identically (encoding/equality boundary — whose definition of equality applies)."
    - "The catalogue declares exactly one DSX-ML-034 row at a deterministic severity (CRITICAL), and regenerating the catalogue twice yields byte-identical output (adjacency + ordering: two report.add sites for one code collide into one row whose surviving severity is a stable tie-break)."
    - "The catalogue total moves 276 → 277 in lockstep across references/finding-codes.md, _EXPECTED_TOTAL, and the phase-20 pin (boundary/precision: the three count pins move together with no off-by-one)."
    - "DSX-ML-034 slots between DSX-ML-033 and DSX-ML-040 in the rendered catalogue because collect() sorts rows by code string (ordering is specified and stable)."
  artifacts:
    - dsx/checks/ml.py::_check_feature_provenance
    - references/finding-codes.md (DSX-ML-034 row; Total 277)
    - tests/test_ml_feature_provenance.py
  key_links:
    - "check() dispatch in dsx/checks/ml.py calls _check_feature_provenance in the model-present block (alongside _check_features)."
    - "DSX-ML-034 is named in _D05_ALLOWLIST_CODES (scripts/gen-finding-catalogue.py) so the D-05 docstring gate enforces its Citation and Structural criterion lines and the linked test marker."
  prohibitions:
    - "The `# D-05: DSX-ML-034` marker and the _check_feature_provenance docstring must not claim a first-hand read of the ACM PDF; both must describe the Kaufman citation as secondary-corroborated, primary PDF paywalled."
    - "dsx/checks/dq.py must not be modified in this phase (byte-frozen); the only dsx/ edit is dsx/checks/ml.py."
---

<objective>
Mint DSX-ML-034 — a declaration-only feature-provenance check — and wire it into the finding
catalogue. This is the REQ-P27-02 deliverable: on the measured LIVE MISS (27-MEASUREMENT.md),
ship `_check_feature_provenance` in `dsx/checks/ml.py` that reads a declared
`model.feature_provenance[]` block and fires DSX-ML-034 CRITICAL when a feature is declared
`available_at: after_prediction` and HIGH when `available_at: unknown` without a waiver, per the
FROZEN §S3-1-CLOSE severity map (27-CONTEXT.md:206-209). The check buys attribution, not
detection — a spec that omits or lies in the block still passes (the standing "a frame that lies
passes" limit). D-06 places the code in the leakage-features 03x family (next-free slot 034,
D-27-03). D-05 cites Kaufman, Rosset, Perlich & Stitelman (2012), ACM TKDD 6(4) Article 15,
described honestly as secondary-corroborated with the primary PDF paywalled (§S3-1-CLOSE binding
instruction). Measure-then-mint (D-13) is already satisfied by 27-MEASUREMENT.md.

Purpose: close the LIVE MISS with a cited check so the feature-origin leak is at least
attributable through a declaration.
Output: `_check_feature_provenance` + DSX-ML-034 in ml.py; a new unit test carrying the
`# D-05: DSX-ML-034` marker; the regenerated catalogue at 277; the two catalogue count pins moved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Frozen contract + measured verdict (do NOT re-open D-27-01/02; guardrail 1)
@.planning/phases/27-evidence-case-feature-origin-only-leak/27-CONTEXT.md
@.planning/phases/27-evidence-case-feature-origin-only-leak/27-MEASUREMENT.md
@.planning/phases/27-evidence-case-feature-origin-only-leak/27-RESEARCH.md

# The file being modified + the emit/citation idioms to mirror
@dsx/checks/ml.py
@scripts/gen-finding-catalogue.py
@references/finding-codes.md
</context>

<artifacts>
## Artifacts this phase produces (this plan's share)

- `_check_feature_provenance` — new function in `dsx/checks/ml.py`, called from `check()`'s
  model-present dispatch block.
- `DSX-ML-034` — the minted finding code (two literal-severity `report.add` sites: CRITICAL for
  `after_prediction`, HIGH for `unknown`-no-waiver).
- `model.feature_provenance[]` spec block — additive/optional, `{feature, source, available_at,
  derived_from}` with `available_at ∈ before_prediction | at_prediction | after_prediction |
  unknown`. NO `dsx/spec.py` change: `_validate_model_shape` already accepts unknown model keys
  (27-RESEARCH.md §D), so the block validates silently and existing specs without it still pass.
- `tests/test_ml_feature_provenance.py` — new unit test carrying the `# D-05: DSX-ML-034` marker.
- The `DSX-ML-034` row in `references/finding-codes.md` (catalogue total 276 → 277).

(Plan 27-02 produces the four `examples/known-bad/feature-origin-only-leak-*` fixture files.)
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): failing unit test for the feature-provenance check + D-05 marker</name>
  <files>tests/test_ml_feature_provenance.py</files>
  <read_first>
    - dsx/checks/ml.py (the `check()` dispatcher :122-156; `_check_features` emit idiom :473-527; the `normalize()` helper usage)
    - tests/test_dsx.py (existing `# D-05: DSX-ML-*` marker precedents at :1223, :1334, :1521 — copy the marker shape)
    - .planning/phases/27-evidence-case-feature-origin-only-leak/27-CONTEXT.md §S3-1-CLOSE (severity map, binding marker-honesty instruction)
  </read_first>
  <behavior>
    - after_prediction: a model.feature_provenance entry with available_at "after_prediction" makes check(spec) emit a DSX-ML-034 finding at severity CRITICAL.
    - unknown-no-waiver: an entry with available_at "unknown" and no waiver field makes check(spec) emit DSX-ML-034 at severity HIGH.
    - unknown-with-waiver: an entry with available_at "unknown" AND a truthy waiver emits no DSX-ML-034 finding.
    - legitimate: entries with available_at "before_prediction" or "at_prediction" emit no DSX-ML-034 finding.
    - silent-when-absent: a spec with a model block but no feature_provenance key emits no DSX-ML-034 finding (empty/absent boundary).
    - normalization: available_at "After_Prediction" (mixed case / surrounding whitespace) is treated identically to "after_prediction".
  </behavior>
  <action>
    Author tests/test_ml_feature_provenance.py as a stdlib unittest.TestCase that imports
    `check` from dsx.checks.ml and builds minimal in-memory spec dicts (a `model` block with
    `task`, `target`, `split` plus a `feature_provenance` list) to drive each behavior bullet
    above. Assert on the presence/absence and severity of the DSX-ML-034 code in the returned
    Report's findings (mirror how existing ml tests read report findings by code). Include one
    test per behavior bullet. Add the line `# D-05: DSX-ML-034` as a standalone comment in this
    file, and beside it note the citation is "secondary-corroborated, primary PDF paywalled"
    (per §S3-1-CLOSE — do NOT claim a first-hand ACM PDF read). The tests reference
    `_check_feature_provenance`/DSX-ML-034 behavior that does not exist yet, so the module fails
    to satisfy its assertions — this is the intended RED state before Task 2. Do NOT stub or
    weaken any assertion to make it pass early.
  </action>
  <acceptance_criteria>
    - `tests/test_ml_feature_provenance.py` exists and contains the standalone comment line `# D-05: DSX-ML-034`.
    - The file contains the exact phrase `secondary-corroborated, primary PDF paywalled`.
    - Running `python312 -m unittest tests.test_ml_feature_provenance` exits NON-ZERO (RED) because DSX-ML-034 is not yet emitted.
    - The file contains at least six test methods, one per behavior bullet (after_prediction, unknown-no-waiver, unknown-with-waiver, before/at_prediction legitimate, silent-when-absent, normalization).
  </acceptance_criteria>
  <verify>
    <automated>python312 -m unittest tests.test_ml_feature_provenance 2>&1 | grep -Eq "FAILED|Error|Traceback" && echo RED-CONFIRMED</automated>
  </verify>
  <done>The unit test exists, carries the D-05 marker with the honesty phrase, and fails (RED) because the check is not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): implement _check_feature_provenance + Kaufman D-05 docstring</name>
  <files>dsx/checks/ml.py</files>
  <read_first>
    - dsx/checks/ml.py (`check()` :122-156 — add the dispatch call; `_check_features` :473-527 — mirror the read/emit idiom and `report.add(... where=...)` shape; `_check_cleaning` :303-346 — mirror the Kaufman `Citation:` + `Structural criterion:` docstring shape for DSX-ML-023)
    - .planning/phases/27-evidence-case-feature-origin-only-leak/27-CONTEXT.md §S3-1-CLOSE (severity map; binding honesty instruction)
    - .planning/phases/27-evidence-case-feature-origin-only-leak/27-RESEARCH.md §D (placement, emit idiom, D-05 docstring gate requirements)
  </read_first>
  <behavior>
    - Implements every behavior bullet from Task 1; running Task 1's unit test now passes (GREEN).
    - Existing specs (no feature_provenance block) remain unaffected — the check returns early.
  </behavior>
  <action>
    Add `_check_feature_provenance(model, report)` to dsx/checks/ml.py and call it from `check()`
    in the model-present block, alongside `_check_features` at line 148. The function reads
    `model.get("feature_provenance")`; when that is absent or empty it returns immediately (per
    27-RESEARCH.md §D — additive/optional, so every existing spec still validates). For each
    entry, `normalize()` the `available_at` string field and branch: `after_prediction` emits
    `report.add("DSX-ML-034", "CRITICAL", ...)` with `where="spec.model.feature_provenance"`;
    `unknown` with no truthy `waiver` emits `report.add("DSX-ML-034", "HIGH", ...)`;
    `before_prediction` and `at_prediction` emit no finding (optionally `report.ok(...)`). Both
    `report.add` calls MUST pass the severity as a string literal (the catalogue extractor skips
    any non-literal severity — gen-finding-catalogue.py:237-241). Name the offending feature in
    each finding's detail so the attribution is legible. Write the function docstring to satisfy
    the D-05 gate (per §D): a `Citation:` line naming Kaufman, Rosset, Perlich & Stitelman (2012),
    "Leakage in Data Mining: Formulation, Detection, and Avoidance", ACM Transactions on Knowledge
    Discovery from Data 6(4), Article 15, DOI 10.1145/2382577.2382579 — stating the legitimacy
    condition (a feature value is legitimate only if observable strictly before the target
    instance) and explicitly describing the source as secondary-corroborated with the primary ACM
    PDF paywalled (do NOT claim a first-hand read; do NOT invent a page locator); and a
    `Structural criterion:` line describing the closed-vocabulary membership test and the
    attribution-not-detection limit. Do NOT touch the existing DSX-ML-023 Kaufman citation
    (out of scope). Do NOT modify dsx/checks/dq.py.
  </action>
  <acceptance_criteria>
    - `python312 -m unittest tests.test_ml_feature_provenance` exits 0 (GREEN — every behavior bullet passes).
    - `check()` in dsx/checks/ml.py calls `_check_feature_provenance` within the model-present block.
    - `_check_feature_provenance` returns early when `model.get("feature_provenance")` is falsy (grep confirms an early `return` guarded on that key).
    - The function docstring contains a line beginning `Citation:` and a line beginning `Structural criterion:`, and contains the phrase `secondary-corroborated` and `paywalled`.
    - Both DSX-ML-034 `report.add` sites pass a string-literal severity (`"CRITICAL"` and `"HIGH"`).
    - `git diff --stat -- dsx/checks/dq.py` prints nothing (byte-frozen).
    - Full suite green: `python312 -m unittest discover -s tests -q` reports OK (the check is silent for all existing fixtures).
  </acceptance_criteria>
  <verify>
    <automated>python312 -m unittest tests.test_ml_feature_provenance -q && python312 -m unittest discover -s tests -q</automated>
  </verify>
  <done>DSX-ML-034 fires CRITICAL/HIGH per the frozen severity map, is silent when the block is absent, carries an honest Kaufman D-05 docstring, and the full suite is green; dq.py untouched.</done>
</task>

<task type="auto">
  <name>Task 3: allowlist DSX-ML-034, regenerate the catalogue deterministically, move the two catalogue count pins</name>
  <files>scripts/gen-finding-catalogue.py, references/finding-codes.md, tests/test_finding_catalogue_invariant.py, tests/test_phase20_zero_mint_close.py</files>
  <read_first>
    - scripts/gen-finding-catalogue.py (`_D05_ALLOWLIST_CODES` :183-203; `collect()` dedupe by code, last-seen-wins :275-292; `check_d05` :385-415; `extract()` literal-severity requirement :226-242)
    - references/finding-codes.md (Total line :16; ML 03x family rows :150-166 — 030-033 then 040, 034 slot free)
    - tests/test_finding_catalogue_invariant.py (`_EXPECTED_TOTAL = 276` :39; method `test_finding_catalogue_stays_at_276_codes` :65)
    - tests/test_phase20_zero_mint_close.py (`_declared_total(_CATALOGUE), 276` :99; docstring/message 276 literals :94-101)
    - .planning/phases/27-evidence-case-feature-origin-only-leak/27-RESEARCH.md §C5/§C6/§C6b/§E and open RISK 3 (one-code-two-severity dedupe)
  </read_first>
  <action>
    Add the exact code string "DSX-ML-034" to `_D05_ALLOWLIST_CODES` in
    scripts/gen-finding-catalogue.py (exact-code entry, NOT a "DSX-ML-" prefix — the legacy
    DSX-ML family carries no citation and a prefix add would fail the build red; mirror the
    DSX-VIZ-071 exact-code precedent comment at :194-201). Regenerate the catalogue by running
    the generator's `--write` mode so `references/finding-codes.md` gains the DSX-ML-034 row and
    the Total line moves to 277 — never hand-edit the catalogue. Resolve RISK 3 (D-06 additive,
    one code emitted at two severities): the catalogue generator dedupes DSX-ML-034 to a single
    row (last-seen wins after a stable sort by code), so arrange the source order of the two
    `report.add("DSX-ML-034", ...)` sites in dsx/checks/ml.py so the committed catalogue row shows
    the CRITICAL (after_prediction) severity — the headline disposition. Confirm the choice by
    regenerating and grepping the rendered row; if HIGH survives, swap the two sites' order and
    regenerate until CRITICAL is the committed severity. Then move the two catalogue count pins in
    lockstep with the regenerated Total: set `_EXPECTED_TOTAL` to 277
    (tests/test_finding_catalogue_invariant.py:39) and its cosmetic method-name literal 276→277
    for honesty; set the phase-20 assert `_declared_total(_CATALOGUE)` to 277
    (tests/test_phase20_zero_mint_close.py:99) and update the cosmetic 276 literals in that
    test's docstring and assertion message for honesty. Do NOT add DSX-ML-034 to any backlog set;
    do NOT alter the Phase-12 256 snapshot pin or the DSX-STA reserve-band pin (both unrelated —
    DSX-ML-034 is in the DSX-ML family).
  </action>
  <acceptance_criteria>
    - `python312 scripts/gen-finding-catalogue.py --check` exits 0 (catalogue is current and the D-05 gate passes: Citation + Structural criterion + `# D-05: DSX-ML-034` marker all resolve).
    - `references/finding-codes.md` contains exactly one line matching `| \`DSX-ML-034\` |` and its severity column reads CRITICAL; the DSX-ML-034 row sits between the DSX-ML-033 and DSX-ML-040 rows.
    - `references/finding-codes.md` Total line reads `**Total: 277 codes.**`.
    - Regenerating the catalogue a second time produces a byte-identical file (deterministic — `git diff --stat -- references/finding-codes.md` is empty after the second `--write`).
    - `python312 -m unittest tests.test_finding_catalogue_invariant tests.test_phase20_zero_mint_close -q` reports OK with both total-assert pins reading 277.
  </acceptance_criteria>
  <verify>
    <automated>python312 scripts/gen-finding-catalogue.py --check && python312 -m unittest tests.test_finding_catalogue_invariant tests.test_phase20_zero_mint_close -q</automated>
  </verify>
  <done>DSX-ML-034 is D-05-allowlisted, the catalogue is deterministically regenerated at 277 with a single CRITICAL row, and both catalogue count pins are at 277.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| minted code → finding catalogue | A new `report.add` call-site becomes a catalogue row via AST extraction; a non-deterministic or over-claiming emission corrupts the committed catalogue. |
| docstring/test marker → D-05 honesty record | The Citation text is the durable provenance claim; over-claiming a first-hand read launders a paywalled source into an asserted read. |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-03 | Repudiation | DSX-ML-034 docstring + `# D-05:` marker | high | mitigate | Both MUST state "secondary-corroborated, primary PDF paywalled" and MUST NOT claim a first-hand ACM PDF read (§S3-1-CLOSE). Verified by the presence-grep in Task 1/Task 2 acceptance. |
| T-27-04 | Tampering | catalogue regeneration (one code, two severities) | high | mitigate | Arrange source order so the committed DSX-ML-034 row is deterministically CRITICAL; verify exactly one row + byte-identical re-regeneration (Task 3). |
| T-27-02a | Tampering | catalogue count pins | high | mitigate | Move references/finding-codes.md Total, `_EXPECTED_TOTAL`, and the phase-20 pin together to 277; the invariant + phase-20 tests fail on drift (Task 3). |
| T-27-08 | Tampering | `_D05_ALLOWLIST_CODES` entry | medium | mitigate | Add DSX-ML-034 as an exact code, never a `DSX-ML-` prefix (a prefix add fails the build red on the legacy family). |
| T-27-09 | Tampering | dsx/checks/dq.py byte-freeze | medium | mitigate | Only dsx/checks/ml.py is edited; `git diff --stat -- dsx/checks/dq.py` must be empty (Task 2 acceptance). |
| T-27-SC | Supply chain | package installs | low | accept | This plan installs no npm/pip/cargo packages. The fixture entrypoint (plan 27-02) is read as text, never executed; no Package Legitimacy Gate applies. |
</threat_model>

<verification>
- `python312 -m unittest tests.test_ml_feature_provenance -q` — GREEN after Task 2.
- `python312 scripts/gen-finding-catalogue.py --check` exit 0 — catalogue current + D-05 gate green.
- `python312 -m unittest discover -s tests -q` — full suite OK (check silent for all existing fixtures; pins at 277).
- `git diff --stat -- dsx/checks/dq.py` empty — byte-freeze held.
</verification>

<success_criteria>
- DSX-ML-034 fires CRITICAL on `after_prediction`, HIGH on `unknown`-no-waiver, silent when the block is absent (REQ-P27-02).
- The catalogue declares exactly one DSX-ML-034 CRITICAL row and totals 277, regenerated deterministically; both catalogue count pins read 277.
- The D-05 docstring and test marker cite Kaufman honestly (secondary-corroborated, primary PDF paywalled) and pass the generator's D-05 gate.
- dsx/checks/dq.py unchanged.
</success_criteria>

<output>
Create `.planning/phases/27-evidence-case-feature-origin-only-leak/27-01-SUMMARY.md` when done.
</output>
