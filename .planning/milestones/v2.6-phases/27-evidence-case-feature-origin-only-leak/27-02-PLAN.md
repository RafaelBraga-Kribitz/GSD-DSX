---
phase: 27-evidence-case-feature-origin-only-leak
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - examples/known-bad/feature-origin-only-leak-ANALYSIS-SPEC.yaml
  - examples/known-bad/feature-origin-only-leak-entrypoint.py
  - examples/known-bad/feature-origin-only-leak-POSTMORTEM.md
  - examples/known-bad/feature-origin-only-leak-ATTRIBUTION.yaml
  - tests/test_known_bad_corpus.py
  - tests/test_frame_val.py
  - tests/test_causal_verb_golden.py
  - tests/test_dsx.py
  - brief.md
autonomous: true
requirements: [REQ-P27-01, REQ-P27-03]
must_haves:
  truths:
    - "examples/known-bad/feature-origin-only-leak-* is discovered by every corpus glob and validates (`dsx validate` exit 0), with a POSTMORTEM naming at least one DSX-<LETTERS>-<digits> code and recording the four-point measured table as the entry-condition test (REQ-P27-01)."
    - "The promotion is a single-fixture add whose _EXPECTED_CAUGHT_DEFECTS entry is the empty frozenset() and whose _EXPECTED_VAL_CODES entry is the empty set() — a MISS catches nothing (empty/single-element result)."
    - "Running the live gate on the promoted fixture reproduces the ship set {DSX-CLM-031, DSX-COH-031, DSX-MET-040, DSX-NAR-001} and DSX-ML-034 appears at no gate point (adjacency: the shipped absent_code stays silent on the fixture; the golden set is an order-independent frozenset, so ordering of equal elements is immaterial)."
    - "The spec count moves 42 → 43 exactly when the fixture lands — one added spec, no drift (boundary/precision)."
    - "The ATTRIBUTION sidecar names absent_code: DSX-ML-034 (a shipped catalogue code after wave 1) and promotes_backlog_item: 6.5-item-7-feature-provenance (verbatim member of _SECTION_65_ITEM_IDS), and the falsifiability test confirms DSX-ML-034 fires nowhere CRITICAL on the fixture (encoding/equality: the item id must match the frozen id string exactly)."
    - "brief.md §6.5 item 7 (the Feature-provenance per-feature list row) states the measured evidence: a LIVE MISS with DSX-ML-034 minted (REQ-P27-03)."
    - "`node install.mjs` then `node install.mjs --check` pass at phase end — the dsx/, references/, and examples/ overlays are re-synced (boundary: the installed copy equals the source tree)."
  artifacts:
    - examples/known-bad/feature-origin-only-leak-ANALYSIS-SPEC.yaml
    - examples/known-bad/feature-origin-only-leak-entrypoint.py
    - examples/known-bad/feature-origin-only-leak-POSTMORTEM.md
    - examples/known-bad/feature-origin-only-leak-ATTRIBUTION.yaml
  key_links:
    - "The three glob-discovered harness maps (_EXPECTED_CAUGHT_DEFECTS, _EXPECTED_VAL_CODES, _GOLDEN_SHIP_FINDINGS) each gain one keyed entry under the corpus basename feature-origin-only-leak; key-parity tests fail loudly if any is missing."
    - "The sidecar's absent_code DSX-ML-034 resolves against the shipped catalogue produced in wave 1 (plan 27-01)."
  prohibitions:
    - "The promoted ANALYSIS-SPEC must not declare a model.feature_provenance block — that would convert the miss into a catch and break kind: miss (the falsifiability and golden-set tests would flip)."
    - "DSX-ML-034 must not be added to _SECTION_65_BACKLOG_CODES — it ships to the catalogue; the disjointness assertion forbids a shipped code there."
    - "No entry may be added to _TARGET_DEFECT_CODES and no edit made to _INCIDENTAL_GAP_CODES (a MISS catches nothing; the four ship residuals are already documented)."
    - "dsx/checks/dq.py must not be modified."
---

<objective>
Promote the measured spike into a committed known-bad corpus fixture and complete every harness
entry, then rewrite the brief's §6.5 item 7 row with the measured evidence. This is the REQ-P27-01
and REQ-P27-03 deliverable. The spike (spike/feature-origin-leak-*) was measured a LIVE MISS at all
four gate points (27-MEASUREMENT.md; D-13 satisfied); promote it under the corpus basename
`feature-origin-only-leak` with a new POSTMORTEM (recording the four-point table as the
entry-condition test) and an ATTRIBUTION sidecar naming `absent_code: DSX-ML-034` (the code minted
in plan 27-01) and `promotes_backlog_item: 6.5-item-7-feature-provenance` (the harness-frozen id,
per §S3-2-RESEARCH-CORRECTIONS — NOT the `-feature-origin-only-leak` string). Register the fixture
as a MISS in the four harness maps, move the spec count 42→43, and keep DSX-ML-034 OUT of the
backlog disjoint set. The fixture must stay a MISS: it declares NO feature_provenance block, so the
wave-1 check stays silent on it (attribution, not detection).

Purpose: turn the measured miss into a permanent, self-testing corpus case with a falsifiable
attribution sidecar.
Output: four `examples/known-bad/feature-origin-only-leak-*` files; four harness map/count edits;
the rewritten brief row; a re-synced installer overlay.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Frozen contract, measured verdict, wiring research (do NOT re-open D-27-01/02; guardrail 1)
@.planning/phases/27-evidence-case-feature-origin-only-leak/27-CONTEXT.md
@.planning/phases/27-evidence-case-feature-origin-only-leak/27-MEASUREMENT.md
@.planning/phases/27-evidence-case-feature-origin-only-leak/27-RESEARCH.md

# Spike sources to promote + corpus siblings to mirror
@.planning/phases/27-evidence-case-feature-origin-only-leak/spike/feature-origin-leak-ANALYSIS-SPEC.yaml
@.planning/phases/27-evidence-case-feature-origin-only-leak/spike/feature-origin-leak-entrypoint.py
@examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml
@examples/known-bad/full-frame-cleaning-POSTMORTEM.md
@examples/known-bad/retracted-fabricated-field-experiment-ATTRIBUTION.yaml
</context>

<artifacts>
## Artifacts this phase produces (this plan's share)

- `examples/known-bad/feature-origin-only-leak-ANALYSIS-SPEC.yaml` — promoted from the spike spec;
  `spec_id` and `reproducibility.entrypoint` renamed to the corpus basename; spike header stripped,
  corpus header added; NO `model.feature_provenance` block (keeps it a MISS).
- `examples/known-bad/feature-origin-only-leak-entrypoint.py` — promoted from the spike entrypoint
  (basename in the header only; body is static text, never executed).
- `examples/known-bad/feature-origin-only-leak-POSTMORTEM.md` — authored new; records the
  four-point measured table (27-MEASUREMENT.md) and names DSX-ML-034 as the absent code.
- `examples/known-bad/feature-origin-only-leak-ATTRIBUTION.yaml` — authored new; `absent_code:
  DSX-ML-034`, `promotes_backlog_item: 6.5-item-7-feature-provenance`, `kind: miss`,
  `protocol_adherence: skipped`, rationale.

(Plan 27-01 produces `_check_feature_provenance` / DSX-ML-034 / the catalogue row.)
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: promote the spike spec + entrypoint into the known-bad corpus</name>
  <files>examples/known-bad/feature-origin-only-leak-ANALYSIS-SPEC.yaml, examples/known-bad/feature-origin-only-leak-entrypoint.py</files>
  <read_first>
    - .planning/phases/27-evidence-case-feature-origin-only-leak/spike/feature-origin-leak-ANALYSIS-SPEC.yaml (source; spike header :1-21, `spec_id` :25, `reproducibility.entrypoint` :124)
    - .planning/phases/27-evidence-case-feature-origin-only-leak/spike/feature-origin-leak-entrypoint.py (source; header :1-14)
    - examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml (corpus header precedent :1-38; `spec_id` == basename at :38)
    - .planning/phases/27-evidence-case-feature-origin-only-leak/27-RESEARCH.md §A (exact file list, edits, "do NOT add a feature_provenance block")
  </read_first>
  <action>
    Copy spike/feature-origin-leak-ANALYSIS-SPEC.yaml to
    examples/known-bad/feature-origin-only-leak-ANALYSIS-SPEC.yaml and
    spike/feature-origin-leak-entrypoint.py to
    examples/known-bad/feature-origin-only-leak-entrypoint.py. In the promoted spec: change
    `spec_id` from "feature-origin-leak" to "feature-origin-only-leak" (basename convention,
    full-frame precedent), and change `reproducibility.entrypoint` to
    "feature-origin-only-leak-entrypoint.py". Strip the spike "MEASUREMENT-SPIKE / NOT a committed
    corpus fixture" header and replace it with a corpus header modelled on
    full-frame-cleaning-ANALYSIS-SPEC.yaml:1-38 — naming REQ-P27-01/02/03, stating the FROZEN
    D-27-01 defect (an innocuously-named `account_health_index` recomputed nightly from activity
    that includes the outcome window, matching none of the ten LEAKAGE_PATTERNS), and that the
    case is a measured LIVE MISS (27-MEASUREMENT.md) whose only attribution is DSX-ML-034. Leave
    every non-model and model block byte-equal to the spike otherwise. Do NOT add a
    `model.feature_provenance` block and do NOT add an explanatory comment that names that block
    token — its absence is what keeps the fixture a MISS (the wave-1 check stays silent). In the
    promoted entrypoint, update only the basename reference in its head comment; the body is static
    text read by the code scan and is never executed, and the declared
    `account_churn_features.csv` is narrative-only (the reproducibility check tests only that the
    declared path string exists, never that it runs — so no CSV is needed).
  </action>
  <acceptance_criteria>
    - Both target files exist under examples/known-bad/ with the `feature-origin-only-leak-` basename.
    - `python312 -c "import dsx.cli"` style load is not needed; instead `dsx validate --spec examples/known-bad/feature-origin-only-leak-ANALYSIS-SPEC.yaml` exits 0.
    - The promoted spec's `spec_id` is `feature-origin-only-leak` and `reproducibility.entrypoint` is `feature-origin-only-leak-entrypoint.py`.
    - The promoted spec contains no line whose YAML key is a feature-provenance block (the fixture stays a MISS) — asserted behaviourally by Task 3's golden-set and falsifiability tests, not by editing the block in.
    - The spike header text ("MEASUREMENT-SPIKE", "NOT a committed corpus fixture") is absent from the promoted spec; a corpus header naming REQ-P27-01/02/03 is present.
  </acceptance_criteria>
  <verify>
    <automated>python312 -m dsx.cli validate --spec examples/known-bad/feature-origin-only-leak-ANALYSIS-SPEC.yaml</automated>
  </verify>
  <done>The spike spec and entrypoint are promoted under the corpus basename, validate exit 0, and the fixture declares no feature_provenance block.</done>
</task>

<task type="auto">
  <name>Task 2: author the POSTMORTEM and the falsifiable ATTRIBUTION sidecar</name>
  <files>examples/known-bad/feature-origin-only-leak-POSTMORTEM.md, examples/known-bad/feature-origin-only-leak-ATTRIBUTION.yaml</files>
  <read_first>
    - examples/known-bad/full-frame-cleaning-POSTMORTEM.md (postmortem structure; four-point gate table format :101-107)
    - examples/known-bad/retracted-fabricated-field-experiment-ATTRIBUTION.yaml (sidecar schema; already uses promotes_backlog_item: "6.5-item-7-feature-provenance")
    - .planning/phases/27-evidence-case-feature-origin-only-leak/27-MEASUREMENT.md (the verbatim four-point table :46-52; swap-invariance :73-85)
    - .planning/phases/27-evidence-case-feature-origin-only-leak/27-RESEARCH.md §B (sidecar field table; required POSTMORTEM code + pairing)
  </read_first>
  <action>
    Author examples/known-bad/feature-origin-only-leak-POSTMORTEM.md modelled on
    full-frame-cleaning-POSTMORTEM.md: name the paired spec and entrypoint; describe the encoded
    defect (the feature-origin leak in `account_health_index`, attributable only through feature
    origin); reproduce the four-point measured gate table verbatim from 27-MEASUREMENT.md:46-52
    (validate/plan/execute/verify/ship, exit codes and CRITICAL/HIGH codes) and state it is the
    D-27-02 entry-condition test; record the swap-still-fires counterfactual (the four residuals
    DSX-CLM-031, DSX-COH-031, DSX-MET-040, DSX-NAR-001 are swap-invariant incidental corpus-gap
    codes, none about feature origin); and name DSX-ML-034 as the code that now attributes the
    miss. The POSTMORTEM MUST contain at least one finding code matching the DSX-<LETTERS>-<digits>
    shape (the corpus pairing test requires it). Author
    examples/known-bad/feature-origin-only-leak-ATTRIBUTION.yaml with exactly: `absent_code:
    DSX-ML-034`; `promotes_backlog_item: "6.5-item-7-feature-provenance"` (the harness-frozen id
    at tests/test_known_bad_corpus.py:839, per §S3-2-RESEARCH-CORRECTIONS — NOT
    `-feature-origin-only-leak`); `kind: miss`; `protocol_adherence: skipped`; and a `rationale`
    explaining the leak is attributable only through feature origin, that DSX-ML-034 reads a
    declared model.feature_provenance block and this honest spec declares none, so the code
    correctly stays silent — attribution, not detection.
  </action>
  <acceptance_criteria>
    - Both files exist under examples/known-bad/ with the `feature-origin-only-leak-` basename.
    - The POSTMORTEM contains at least one token matching the pattern `DSX-[A-Z]+-[0-9]+` and contains the string `DSX-ML-034`.
    - The POSTMORTEM contains a gate table with rows for validate, plan, execute, verify, ship matching 27-MEASUREMENT.md (verify/ship exit code 1; ship residuals include DSX-NAR-001).
    - The ATTRIBUTION file's `absent_code` is `DSX-ML-034`, `promotes_backlog_item` is `6.5-item-7-feature-provenance`, `kind` is `miss`, and `protocol_adherence` is `skipped`.
    - `python312 -m unittest tests.test_known_bad_corpus -q` reports OK (pairing, code-presence, and — after Task 3's map entries — falsifiability all pass).
  </acceptance_criteria>
  <verify>
    <automated>python312 -m unittest tests.test_known_bad_corpus -q</automated>
  </verify>
  <done>A POSTMORTEM recording the measured table and an ATTRIBUTION sidecar naming DSX-ML-034 against the harness-valid backlog id exist and satisfy the corpus tests.</done>
</task>

<task type="auto">
  <name>Task 3: complete the four harness maps + spec count, rewrite brief §6.5 item 7, re-sync installer</name>
  <files>tests/test_known_bad_corpus.py, tests/test_frame_val.py, tests/test_causal_verb_golden.py, tests/test_dsx.py, brief.md</files>
  <read_first>
    - tests/test_known_bad_corpus.py (`_EXPECTED_CAUGHT_DEFECTS` map; `_TARGET_DEFECT_CODES` :185; `_INCIDENTAL_GAP_CODES` :65; `_SECTION_65_BACKLOG_CODES` :819 + disjointness assertion :1603-1610; `_SECTION_65_ITEM_IDS` :832-842; falsifiability test :1712-1760)
    - tests/test_frame_val.py (`_EXPECTED_VAL_CODES` :1363; full-frame entry `set()` :1398; key-parity test :1482-1500)
    - tests/test_causal_verb_golden.py (`_GOLDEN_SHIP_FINDINGS` :82; full-frame golden entry :161; per-fixture re-measure :372-394; key-parity :357-370)
    - tests/test_dsx.py (spec count `len(paths) == 42` :579)
    - brief.md (§6.5 item 7 row — "Feature-provenance per-feature list (origin, method, fitted-on, motivating result)" ~:376)
    - .planning/phases/27-evidence-case-feature-origin-only-leak/27-RESEARCH.md §C (every map, symbol, and the re-measure instruction)
  </read_first>
  <action>
    Add exactly these keyed entries, all under the corpus basename `feature-origin-only-leak`:
    in tests/test_known_bad_corpus.py add `"feature-origin-only-leak": frozenset()` to
    `_EXPECTED_CAUGHT_DEFECTS` (a MISS catches nothing); in tests/test_frame_val.py add
    `"feature-origin-only-leak-ANALYSIS-SPEC.yaml": set()` to `_EXPECTED_VAL_CODES`; in
    tests/test_causal_verb_golden.py add
    `"examples/known-bad/feature-origin-only-leak-ANALYSIS-SPEC.yaml":
    frozenset({"DSX-CLM-031", "DSX-COH-031", "DSX-MET-040", "DSX-NAR-001"})` to
    `_GOLDEN_SHIP_FINDINGS`. These map values are MEASURED, not assumed — the golden and val tests
    re-run the live gate on the promoted fixture; run them and, if the measured sets differ from
    the values above, record the discrepancy and stop (do NOT silently overwrite — a differing
    golden set means DSX-ML-034 or another code fired, which would contradict the frozen MISS).
    Do NOT add an entry to `_TARGET_DEFECT_CODES`, do NOT edit `_INCIDENTAL_GAP_CODES` (the four
    residuals are already present), and do NOT add DSX-ML-034 to `_SECTION_65_BACKLOG_CODES` (it
    ships to the catalogue; the disjointness assertion at :1603-1610 forbids it). Move the spec
    count in tests/test_dsx.py:579 from 42 to 43 and update the running-tally comment above it.
    Rewrite the brief.md §6.5 item 7 row (the Feature-provenance per-feature list row) so its
    entry-condition column states the measured evidence: the M5 corpus now contains the
    feature-origin-only-leak case, measured a LIVE MISS at all four gate points (D-13), and
    DSX-ML-034 was minted under D-05 to attribute it — attribution, not a catch. Finally re-sync
    the installer overlay: run `node install.mjs` (syncs dsx/, references/, examples/) then `node
    install.mjs --check`.
  </action>
  <acceptance_criteria>
    - `python312 -m unittest tests.test_known_bad_corpus tests.test_frame_val tests.test_causal_verb_golden -q` reports OK (all three key-parity + per-fixture-equality tests pass with the new entries).
    - The golden entry for the fixture is exactly `{DSX-CLM-031, DSX-COH-031, DSX-MET-040, DSX-NAR-001}` and DSX-ML-034 is NOT among the fixture's measured ship findings (falsifiability test green).
    - `_SECTION_65_BACKLOG_CODES` does not contain DSX-ML-034 (the disjointness assertion in tests/test_known_bad_corpus.py passes); `_TARGET_DEFECT_CODES` has no feature-origin-only-leak key.
    - `python312 -m unittest tests.test_dsx -q` reports OK with the spec count at 43.
    - brief.md's §6.5 item 7 row contains the strings `LIVE MISS` (or "live miss") and `DSX-ML-034`.
    - Full suite green: `python312 -m unittest discover -s tests -q` reports OK; `python312 scripts/gen-finding-catalogue.py --check` exits 0.
    - `node install.mjs` completes and `node install.mjs --check` exits 0 (overlays re-synced).
  </acceptance_criteria>
  <verify>
    <automated>python312 -m unittest discover -s tests -q && python312 scripts/gen-finding-catalogue.py --check && node install.mjs --check</automated>
  </verify>
  <done>All four harness maps and the spec count are consistent, the golden ship set is re-measured with DSX-ML-034 absent, the brief row states the measured evidence, and the installer overlay is re-synced and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| promoted fixture → corpus harness | A fixture that silently gains a feature_provenance block converts the MISS into a CATCH and defeats the requirement's whole point (kind: miss). |
| ATTRIBUTION sidecar → §6.5 backlog id set | An id that does not match the frozen `_SECTION_65_ITEM_IDS` string fails the sidecar gate; a shipped absent_code placed in the backlog set fails the disjointness gate. |
| source tree → installed overlay | An un-synced overlay means the shipped package disagrees with the repo; `install --check` is the boundary check. |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering | promoted ANALYSIS-SPEC | high | mitigate | The fixture MUST declare no model.feature_provenance block; the falsifiability test (:1712-1760) runs the live gate and requires DSX-ML-034 silent CRITICAL, and the golden-set test re-measures the ship set — either flips if a block is added (Task 1/Task 3). |
| T-27-07 | Spoofing | ATTRIBUTION promotes_backlog_item | high | mitigate | Use the harness-frozen id `6.5-item-7-feature-provenance` (§S3-2-RESEARCH-CORRECTIONS); the sidecar membership gate (:1646-1651) fails on any other string. |
| T-27-06 | Tampering | _SECTION_65_BACKLOG_CODES | high | mitigate | Keep DSX-ML-034 out of the backlog set; the disjointness assertion (:1603-1610) fails if a shipped code is added there (Task 3). |
| T-27-02b | Tampering | spec-count pin | high | mitigate | Move tests/test_dsx.py:579 42→43 exactly when the fixture lands; the count assert fails on drift (Task 3). |
| T-27-10 | Tampering | golden ship set (vacuous pin) | high | mitigate | Re-measure via the live per-fixture golden test rather than pinning a guessed set; a wrong set fails the equality re-measure (Task 3). |
| T-27-11 | Information disclosure | installed overlay drift | medium | mitigate | Re-run `node install.mjs` then `node install.mjs --check` after all synced-dir edits (Task 3). |
| T-27-SC | Supply chain | package installs | low | accept | No npm/pip/cargo installs. The promoted entrypoint imports pandas/lightgbm/sklearn but is read as text by the code scan and never executed; no Package Legitimacy Gate applies. |
</threat_model>

<verification>
- `python312 -m unittest tests.test_known_bad_corpus tests.test_frame_val tests.test_causal_verb_golden tests.test_dsx -q` — OK with all new entries.
- `python312 -m unittest discover -s tests -q` — full suite OK (spec count 43; catalogue 277 from wave 1).
- `python312 scripts/gen-finding-catalogue.py --check` — exit 0.
- `node install.mjs --check` — exit 0 (overlays re-synced).
- `git diff --stat -- dsx/checks/dq.py` empty — byte-freeze held.
</verification>

<success_criteria>
- The four `examples/known-bad/feature-origin-only-leak-*` files exist, validate, and are a MISS (no feature_provenance block; DSX-ML-034 silent) (REQ-P27-01).
- The POSTMORTEM records the four-point measured table and names DSX-ML-034; the ATTRIBUTION sidecar is falsifiable against the live gate with the harness-valid backlog id (REQ-P27-01/03).
- All four harness maps + the spec count (43) are consistent and green; DSX-ML-034 stays out of the backlog set (REQ-P27-03).
- brief.md §6.5 item 7 states the measured evidence (LIVE MISS; DSX-ML-034 minted) (REQ-P27-03).
- The installer overlay is re-synced and `node install.mjs --check` passes.
</success_criteria>

<output>
Create `.planning/phases/27-evidence-case-feature-origin-only-leak/27-02-SUMMARY.md` when done.
</output>
