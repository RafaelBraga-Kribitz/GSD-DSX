---
phase: 29-evidence-case-subgroup-harm-prescriptive
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - dsx/checks/coherence.py
  - tests/test_subgroup_harm_disposition.py
  - templates/ANALYSIS-SPEC.yaml
  - scripts/gen-finding-catalogue.py
  - references/finding-codes.md
  - tests/test_finding_catalogue_invariant.py
  - tests/test_phase20_zero_mint_close.py
  - tests/test_p19_categorical_rows.py
autonomous: true
requirements: [REQ-P29-01, REQ-P29-03]
must_haves:
  truths:
    - "Task 1 (the D-13 act) runs the four/five-point gate protocol from a FRESH tempfile.TemporaryDirectory() mirroring tests/test_known_bad_corpus.py::_gate_findings and writes 29-MEASUREMENT.md whose FIRST content line is an explicit `VERDICT: LIVE MISS` or `VERDICT: CAUGHT`, produced BEFORE any mint code is authored (measure-first, D-13)."
    - "The measurement confirms MET-030/031 are SILENT at 1-of-4 opposing: metrics.py:316 needs all segments opposing (`len(opposing) == len(effects)`; 1==4 is False → DSX-MET-030 silent) and metrics.py:334 needs at least half (`len(opposing) >= len(effects)/2`; 1>=2.0 is False → DSX-MET-031 silent) — the structural miss seam, recorded verbatim before interpreting (the miss)."
    - "The swap-still-fires counterfactual is applied by literal code (flip segment D's effect from opposing to positive) to prove which residual codes are incidental (still fire under the swap) versus a real catch (stop firing under the swap); a code that stops firing under the swap makes the verdict CAUGHT (precision/adjacency)."
    - "On the LIVE MISS branch, DSX-COH-041 fires CRITICAL when a `results.segments[]` entry opposes `results.overall_effect` by sign AND its declared `n >= decision.subgroup_harm_floor` and no matching `decision.subgroup_harm[]` row is declared (segment name match) — asserted at the trigger boundary by RED unit tests (boundary)."
    - "On the LIVE MISS branch, the sign+floor trigger boundary is exact: an opposing segment with `n` just BELOW the declared floor is silent, and one with `n` AT or ABOVE the floor fires — the just-touching floor case (`n == floor` fires) asserted by paired unit tests (adjacency)."
    - "On the LIVE MISS branch, the opposition predicate reuses the `_sign` convention from metrics.py (`_sign(v) = (v>0)-(v<0)`, metrics.py:352-353) with `harmed(S) := _sign(S.effect) == -_sign(overall)` mirroring metrics.py:314 — never a recomputed or divergent sign convention (encoding/equality)."
    - "On the LIVE MISS branch, the two severities of the ONE code DSX-COH-041 are exercised: a matching `accept` row with blank/missing rationale fires HIGH; a matching row with a non-blank rationale is silent; a missing row for an opposing-above-floor segment fires CRITICAL — the CRITICAL/HIGH ladder is per-segment mutually exclusive (severity ladder)."
    - "On the LIVE MISS branch, the check returns early and emits nothing when results are empty or underpopulated — it guards on `overall_effect is None` and on fewer than the required segments, mirroring _check_simpsons_paradox's `len(segments) < 2: return` (metrics.py:300) so real-workflow plan runs on empty results stay silent (empty)."
    - "On the LIVE MISS branch, the DSX-COH-041 docstring cites Gail & Simon (1985) as the MOTIVATING DEFINITION of qualitative/crossover interaction ONLY (opposite-sign-across-subsets = when a segment counts as harmed), carries a `Citation:` line, a `Structural criterion:` line, states the bounded-catch honesty (attribution over honestly-declared segments, NOT detection of hidden/mis-signed/omitted harm), and is linked to a `# D-05: DSX-COH-041` test marker; DSX-COH-041 is named by EXACT CODE in _D05_ALLOWLIST_CODES so the generator's D-05 gate passes (D-05 honesty)."
    - "On the LIVE MISS branch, the catalogue moves 278 → 279 in lockstep across references/finding-codes.md Total AND all three test pins (_EXPECTED_TOTAL in tests/test_finding_catalogue_invariant.py:42, _declared_total(_CATALOGUE) in tests/test_phase20_zero_mint_close.py:100, _EXPECTED_TOTAL in tests/test_p19_categorical_rows.py:39) with exactly one DSX-COH-041 row, regenerated deterministically (byte-identical on a second --write) — the four count pins move together with no off-by-one (precision)."
    - { statement: "On the LIVE MISS branch, the CI OR-arm of the trigger (`S has declared ci AND ci excludes zero`) is written in to honour the scope's OR but is LATENT — today's `results.segments[]` schema (templates/ANALYSIS-SPEC.yaml:260 `[{ name, effect, n }]`) has no `ci` field, so only the `n >= floor` arm bites; no gate test can exercise the CI arm until the schema gains `ci`", verification: backstop }
    - "The new check is dispatched from coherence.check() and, because the `coherence` family is already registered at plan/verify/ship in GATE_PROFILES (cli.py:116-130) and absent from execute (cli.py:120), it inherits exactly the plan/verify/ship registration the CONTEXT names — with NO dsx/cli.py edit (key link)."
  artifacts:
    - .planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-MEASUREMENT.md
    - .planning/phases/29-evidence-case-subgroup-harm-prescriptive/spike/subgroup-harm-without-disposition-ANALYSIS-SPEC.yaml
    - .planning/phases/29-evidence-case-subgroup-harm-prescriptive/spike/subgroup-harm-without-disposition-entrypoint.py
    - dsx/checks/coherence.py (new _check_subgroup_harm_disposition function; DSX-COH-041)
    - tests/test_subgroup_harm_disposition.py
    - references/finding-codes.md (DSX-COH-041 row; Total 279)
  key_links:
    - "The new sub-check is dispatched from coherence.check() (coherence.py:48-55) alongside the five existing sub-checks; registration at plan/verify/ship is inherited from the `coherence` family entry in GATE_PROFILES (cli.py:116-130), NOT from a cli.py edit."
    - "DSX-COH-041 is named by EXACT CODE in _D05_ALLOWLIST_CODES (scripts/gen-finding-catalogue.py:183, following the DSX-ML-034 exact-code precedent at :209 and DSX-CLM-034 at :218) so the D-05 docstring gate enforces its Citation + Structural criterion lines and the linked `# D-05: DSX-COH-041` test marker."
  prohibitions:
    - "Nothing is minted before Task 1 records `VERDICT: LIVE MISS` — no subgroup_harm/subgroup_harm_floor vocabulary shipped, no DSX-COH-041 authored, no catalogue move — until the four/five-point measurement confirms a live miss (guardrail 2)."
    - "The frozen D-29-02 case is not widened, weakened, or re-shaped to manufacture a miss (do NOT shave segment D toward n=2 or -0.1pp); a `VERDICT: CAUGHT` is a valid no-mint terminal that satisfies the requirement and routes to Branch A — the phase closes in 29-01 and Plan 29-02 does NOT run (D-13; guardrail 1)."
    - "DSX-COH-041 is a BRAND-NEW function _check_subgroup_harm_disposition, NOT folded into _check_simpsons_paradox (metrics.py) or any existing coherence sub-check, so the D-05 citation obligation does not retrofit an uncited legacy docstring."
    - "DSX-COH-041 is added to _D05_ALLOWLIST_CODES as an EXACT CODE, never as a `DSX-COH-` prefix (a prefix add retroactively obligates the uncited legacy COH codes 001/010/020/030/031/040 and fails the build red)."
    - "The DSX-COH-041 docstring and `# D-05:` marker do NOT claim Gail & Simon (1985) authorise or supply the enforcement MECHANIC (their paper is a likelihood-ratio TEST; this check computes nothing on the gate path, D-02) and do NOT claim the check detects hidden harm — it catches only the honestly-declared, undispositioned segment (bounded-catch honesty, D-29-05)."
    - "The four/five-point measurement runs only from a fresh tempfile.TemporaryDirectory(), never against the repo root or the spike directory (no stray DECISIONS.jsonl at the repo root)."
    - "dsx/checks/dq.py is not modified (byte-frozen this milestone); the only dsx/ edit is dsx/checks/coherence.py. REQUIREMENTS.md, STATE.md, and ROADMAP.md are single-writer (orchestrator only) and are not touched."
---

<objective>
Measure the §6.5 item 9 case live (D-13), then — and only then — mint DSX-COH-041 if the measurement
is a live miss. This is the REQ-P29-01 measurement plus the conditional REQ-P29-03 mint. Task 1 IS the
D-13 act: it builds the FROZEN D-29-02 fixture (a genuinely prescriptive recommendation, positive
overall effect ≈ +3.2pp, four declared segments — three positive plus one minority segment D whose
effect opposes at n well above the declared floor, and NO `decision.subgroup_harm[]` row), runs the
four/five-point gate protocol from a fresh temp directory, applies the swap-still-fires counterfactual,
and writes 29-MEASUREMENT.md with an explicit VERDICT line first. Every task below Task 1 is
CONDITIONAL on that recorded verdict.

REQ-P29-02's documented public case is ALREADY FOUND (Obermeyer et al. 2019, confirmed at its locator —
29-RESEARCH.md), so the case-source condition of the mint is satisfied; the ONLY open condition is the
live miss.

Branch discipline (the single highest authoring risk for this phase; the mirrored 28-01 shipped a
Task 1 `<done>` misroute — do NOT reproduce it: every CAUGHT pointer routes to Branch A, every
LIVE-MISS pointer to Branch B):
- `VERDICT: CAUGHT` (an existing code fires on the merits of the target defect): STOP after the closure
  task's Branch A. Mint nothing. Record which code caught it, confirmed against the swap counterfactual.
  REQ-P29-01/02/03 are satisfied by this branch and Plan 29-02 is NOT executed.
- `VERDICT: LIVE MISS` (all four/five points pass on the merits after subtracting documented incidental
  codes): proceed through the RED/GREEN mint tasks, then hand off to Plan 29-02 for fixture promotion
  (Branch B).

Purpose: satisfy the measure-first rule and, on a live miss, close the miss with a cited,
declaration-only disposition check so an undispositioned minority harm under a prescriptive
recommendation is at least forced to disclosure.
Output: 29-MEASUREMENT.md with a VERDICT; on a live miss, _check_subgroup_harm_disposition + DSX-COH-041
in coherence.py, the two additive decision-schema keys, a new unit test carrying the
`# D-05: DSX-COH-041` marker, the regenerated catalogue at 279, and the four count pins moved.
</objective>

<recorded_authoring_choices>
Two S5-2 finalisations the frozen CONTEXT explicitly delegated (D-29-01, D-29-03/declaration
vocabulary) — recorded here, not silently resolved:
- Field name finalised as `decision.subgroup_harm_floor` (not `subgroup_harm_floor_n`), matching the
  REQ-P29-03 verbatim intent and the CONTEXT's primary usage; the frozen semantics (int absolute n,
  default 0) are unchanged.
- New check function name finalised as `_check_subgroup_harm_disposition` in dsx/checks/coherence.py.

One authoring clarification the plan-checker should note (mechanism, not a design change): the CONTEXT
names registration as "cli.py:117/123/128". Live verification shows the `coherence` family is ALREADY
in the plan/verify/ship GATE_PROFILES (cli.py:116-130) and absent from execute (cli.py:120), so the
new sub-check inside coherence.check() inherits exactly plan/verify/ship registration with NO cli.py
edit — which is why dsx/cli.py is correctly absent from files_modified.
</recorded_authoring_choices>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Frozen design + plan-input research (do NOT re-open D-29-00..05; guardrail 1)
@.planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-CONTEXT.md
@.planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-RESEARCH.md

# The measurement protocol + the file being modified + the mould/citation idioms to mirror
@tests/test_known_bad_corpus.py
@dsx/checks/coherence.py
@dsx/checks/metrics.py
@scripts/gen-finding-catalogue.py
@references/finding-codes.md
</context>

<artifacts>
## Artifacts this phase produces (this plan's share)

- `.planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-MEASUREMENT.md` — the four/five-point
  measured gate table + swap-still-fires counterfactual + an explicit `VERDICT:` line. Produced ALWAYS
  (both branches).
- `.planning/phases/29-evidence-case-subgroup-harm-prescriptive/spike/subgroup-harm-without-disposition-*`
  — the throwaway measurement spike (spec + static entrypoint), NOT under examples/known-bad/ until
  Plan 29-02 promotes it. Produced ALWAYS.
- `_check_subgroup_harm_disposition` — new function in `dsx/checks/coherence.py`, dispatched from
  coherence.check(). LIVE MISS branch only.
- `DSX-COH-041` — the minted finding code (a declaration-only, sign+floor-triggered subgroup-harm
  disposition check; one code, two severities: missing-row CRITICAL / accept-without-rationale HIGH).
  LIVE MISS branch only.
- `decision.subgroup_harm[]` (list of `{segment, effect, ci, n, disposition, rationale}`) +
  `decision.subgroup_harm_floor` (int absolute n, default 0) — two additive commented-optional keys in
  `templates/ANALYSIS-SPEC.yaml`, consistent with the existing optional decision keys. LIVE MISS branch
  only.
- `tests/test_subgroup_harm_disposition.py` — new unit test carrying `# D-05: DSX-COH-041`. LIVE MISS
  branch only.
- The `DSX-COH-041` row in `references/finding-codes.md` (catalogue total 278 → 279). LIVE MISS branch
  only.

(Plan 29-02 promotes the spike into examples/known-bad/ as a TARGET — D-29-00 — wires the corpus
harness maps with DSX-COH-041 PRESENT, moves the spec count 44→45, and rewrites brief §6.5 item 9 — all
on the LIVE MISS branch only.)
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1 (MEASURE — the D-13 act): build the frozen D-29-02 spike, run the four/five-point protocol, write 29-MEASUREMENT.md with a VERDICT</name>
  <files>.planning/phases/29-evidence-case-subgroup-harm-prescriptive/spike/subgroup-harm-without-disposition-ANALYSIS-SPEC.yaml, .planning/phases/29-evidence-case-subgroup-harm-prescriptive/spike/subgroup-harm-without-disposition-entrypoint.py, .planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-CONTEXT.md (D-29-02 exact fixture shape — the four-segment table, +3.2pp n-weighted overall, floor 500; the clear-table listing every existing check the fixture must clear HONESTLY and its locator)
    - tests/test_known_bad_corpus.py (`_gate_findings` — fresh-tempdir discipline; the per-gate-point seeding of the entrypoint and the plan header)
    - dsx/cli.py (GATE_PROFILES :115-131 — `coherence` registers at plan :116-119, verify :121-125, ship :126-130, and is ABSENT from execute :120; GATE_THRESHOLDS :135-140 — plan/execute block at CRITICAL, verify/ship at HIGH)
    - dsx/checks/metrics.py (_check_simpsons_paradox :296-349 — guard :300; sign reuse :311-314; _sign :352-353; DSX-MET-030 all-oppose :316; DSX-MET-031 ≥half :334)
    - .planning/phases/28-evidence-case-magnitude-no-test-computed/28-01-PLAN.md (the precedent Task 1 measurement shape, the spike/ convention, the swap-invariance record, and the Branch A/Branch B routing to mirror)
    - .planning/phases/28-evidence-case-magnitude-no-test-computed/28-MEASUREMENT.md (the precedent four-point measured-table + swap-diff format, if present on disk)
  </read_first>
  <action>
    Author the FROZEN D-29-02 fixture under the phase spike/ path (NOT under examples/known-bad/ — it
    stays a throwaway until the verdict is known, mirroring Phase 27/28's spike/ convention). Construct
    it as a minimal-perturbation clone of a good-corpus fixture with design.kind experiment and
    question_type causal that already clears the full frame (SRM/observed_n, a fully-reported primary
    test with effect + two-bound CI + p + effect_size_kind + standardized_effect + interpretation, a
    discriminating time-anchored decision.revisit_when, at least one checked assumption, decision MPE +
    action_if_null, and a structured decision.replay that greenlights the rollout on the positive
    aggregate). Then apply the D-29-02 recipe exactly: (i) set question_type causal to prescriptive;
    (ii) retype the claim to prescriptive (recommend the rollout to all at-risk accounts); (iii) inject
    results.segments as the frozen four-segment table — A n=4000 +5.0pp, B n=3000 +4.0pp, C n=2000
    +3.0pp, D (minority) n=1000 -6.0pp — with results.overall_effect declared as the positive n-weighted
    aggregate +3.2pp; (iv) declare decision.subgroup_harm_floor as 500; (v) OMIT decision.subgroup_harm[]
    entirely — that omission, given segment D opposing at n=1000 well above the floor, is the sole defect.
    Do NOT shave D toward a trivial n or a near-zero effect (that manufactures a trivial miss — D-13).
    Author the minimal static entrypoint referenced by reproducibility.entrypoint — read as text, never
    executed. Then run the gate protocol at every point the harness exercises (plan, execute, verify,
    ship — plus validate) from a FRESH tempfile.TemporaryDirectory() mirroring
    tests/test_known_bad_corpus.py::_gate_findings, never against the repo root or the spike directory.
    Record every measured code verbatim at each point BEFORE interpreting. Confirm and record that
    DSX-MET-030 and DSX-MET-031 are SILENT at 1-of-4 opposing (metrics.py:316 all-oppose is False since
    1!=4; metrics.py:334 ≥half is False since 1 < 2.0). Apply the swap-still-fires counterfactual by
    literal code: re-run a variant in which segment D's effect is flipped to positive (so nothing
    opposes) and diff the CRITICAL/HIGH residual sets; a code that stops firing under the swap IS a
    catch, a code that still fires is incidental. Write 29-MEASUREMENT.md with the verbatim per-point
    table, the swap diff, and — as its FIRST content line — either `VERDICT: LIVE MISS` or
    `VERDICT: CAUGHT`, naming the catching code(s) if CAUGHT. Use the real interpreter at
    C:/Users/Benutzer1/AppData/Local/Programs/Python/Python312/python.exe (invoked as `python312`);
    never the python3 3.14 stub. This task mints nothing.
  </action>
  <acceptance_criteria>
    - The spike spec and entrypoint exist under .planning/phases/29-evidence-case-subgroup-harm-prescriptive/spike/ and are NOT under examples/known-bad/.
    - The spike spec declares question_type prescriptive, a prescriptive claim, results.overall_effect positive, and exactly four results.segments entries (three positive, one minority opposing at n above the declared floor); it declares decision.subgroup_harm_floor and declares NO decision.subgroup_harm[] row.
    - 29-MEASUREMENT.md exists and its FIRST content line matches `VERDICT: LIVE MISS` or `VERDICT: CAUGHT`.
    - 29-MEASUREMENT.md records a per-point gate table (validate/plan/execute/verify/ship) with exit codes and the verbatim CRITICAL/HIGH codes at each point, records DSX-MET-030/031 SILENT at 1-of-4, and records the swap-still-fires diff.
    - The measurement was run from a fresh temp directory (the record states this and the repo root has no new DECISIONS.jsonl: `git status --porcelain DECISIONS.jsonl` prints nothing).
    - If VERDICT is CAUGHT: the catching code is named and shown to stop firing under the swap (proving it is a catch, not incidental).
  </acceptance_criteria>
  <verify>
    <automated>grep -Eq "VERDICT: *(LIVE MISS|CAUGHT)" .planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-MEASUREMENT.md && echo VERDICT-RECORDED</automated>
  </verify>
  <done>29-MEASUREMENT.md records the four/five-point measured verdict from a fresh tempdir with an explicit VERDICT line first, and the swap counterfactual is applied by literal code. If VERDICT is CAUGHT, go to Task 4 Branch A only — the no-mint terminal (do NOT run Task 2 or Task 3, do NOT run Plan 29-02). If VERDICT is LIVE MISS, proceed to Task 2 (Branch B).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (RED — CONDITIONAL, LIVE MISS only): failing unit tests for the disposition check + D-05 marker</name>
  <files>tests/test_subgroup_harm_disposition.py</files>
  <read_first>
    - .planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-MEASUREMENT.md (this task runs ONLY if its first line is `VERDICT: LIVE MISS`)
    - dsx/checks/coherence.py (check() dispatch :48-55; _check_revisit_completeness :149-194 for the Citation:/Structural criterion: docstring idiom to mirror; how existing sub-checks read section(spec, "decision") and items(spec, "results"))
    - dsx/checks/metrics.py (_check_simpsons_paradox :296-349 — the `_sign` convention :352-353, the opposition predicate :314, and the `overall_effect is None` / `len(segments) < 2` early-return :300 to mirror)
    - .planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-CONTEXT.md (the Trigger predicate block; D-29-03 severity ladder; D-29-05 Gail & Simon motivating-definition-only + bounded-catch honesty)
    - .planning/phases/28-evidence-case-magnitude-no-test-computed/28-01-PLAN.md (Task 2 RED shape — behaviour bullets, marker placement, the honesty phrase)
  </read_first>
  <behavior>
    - missing-row-critical: a prescriptive spec with an opposing segment (opposite sign to overall) whose declared n is >= decision.subgroup_harm_floor and NO matching decision.subgroup_harm[] row makes check(spec) emit DSX-COH-041 at CRITICAL.
    - accept-blank-rationale-high: a matching decision.subgroup_harm[] row for that segment with disposition `accept` and a blank/missing rationale makes check(spec) emit DSX-COH-041 at HIGH.
    - matching-row-with-rationale-silent: a matching row with a non-blank rationale (any valid disposition) emits no DSX-COH-041 finding.
    - floor-boundary: an opposing segment with n just BELOW the declared floor emits nothing; the same segment with n AT the floor (`n == floor`) emits DSX-COH-041 — the sign+floor trigger boundary and the just-touching case.
    - sign-gate: a segment whose effect has the SAME sign as overall never triggers, regardless of n (only opposite-sign segments are candidates).
    - default-floor-zero: when decision.subgroup_harm_floor is absent, the floor defaults to 0 (every declared opposing segment demands a disposition).
    - empty-early-return: a spec with no results.segments, or with overall_effect absent, or question_type not prescriptive, emits no DSX-COH-041 finding (declaration-guarded silence).
  </behavior>
  <action>
    Author tests/test_subgroup_harm_disposition.py as a stdlib unittest.TestCase importing `check` from
    dsx.checks.coherence and building minimal in-memory spec dicts (a decision block, a results block
    with overall_effect and a segments list, and question_type prescriptive) to drive each behaviour
    bullet. Assert on the presence, absence, and SEVERITY of DSX-COH-041 in the returned Report's
    findings, mirroring how existing coherence tests read findings by code. Include one test method per
    behaviour bullet (at least seven). Add the standalone comment line `# D-05: DSX-COH-041`, and beside
    it a comment stating the citation is Gail & Simon (1985) as the MOTIVATING DEFINITION of a
    qualitative/crossover interaction (opposite-sign-across-subsets), and that the check enforces a
    declaration-level disposition obligation, NOT the likelihood-ratio test Gail & Simon describe (per
    D-29-05 — do NOT phrase it as Gail & Simon mandating the mechanic, and do NOT imply the check detects
    hidden harm). The tests reference DSX-COH-041 behaviour that does not exist yet, so the module fails
    its assertions — the intended RED state before Task 3. Do NOT stub or weaken any assertion to make it
    pass early. Use python312.
  </action>
  <acceptance_criteria>
    - tests/test_subgroup_harm_disposition.py exists and contains the standalone comment line `# D-05: DSX-COH-041`.
    - The file names `Gail` and the phrase `motivating definition` (or `definition that motivates`) in a comment, and does not assert Gail & Simon mandate the enforcement mechanic.
    - Running `python312 -m unittest tests.test_subgroup_harm_disposition` exits NON-ZERO (RED) because DSX-COH-041 is not yet emitted.
    - The file contains at least seven test methods, one per behaviour bullet (missing-row-critical, accept-blank-rationale-high, matching-row-with-rationale-silent, floor-boundary, sign-gate, default-floor-zero, empty-early-return).
  </acceptance_criteria>
  <verify>
    <automated>python312 -m unittest tests.test_subgroup_harm_disposition 2>&1 | grep -Eq "FAILED|Error|Traceback" && echo RED-CONFIRMED</automated>
  </verify>
  <done>The unit tests exist, carry the D-05 marker with the Gail-&-Simon-as-motivating-definition honesty phrase, and fail (RED) because the check is not yet implemented. Skip this task entirely if Task 1 recorded VERDICT: CAUGHT (Branch A).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN — CONDITIONAL, LIVE MISS only): implement the disposition check, add the schema keys + D-05 docstring, allowlist and regenerate the catalogue, move the four count pins</name>
  <files>dsx/checks/coherence.py, templates/ANALYSIS-SPEC.yaml, scripts/gen-finding-catalogue.py, references/finding-codes.md, tests/test_finding_catalogue_invariant.py, tests/test_phase20_zero_mint_close.py, tests/test_p19_categorical_rows.py</files>
  <read_first>
    - dsx/checks/coherence.py (check() dispatch :48-55 — add the new sub-check call; _check_revisit_completeness :149-194 — mirror its Citation:/Structural criterion: docstring shape and its section(spec, "decision") reads; DSX-COH-040 is the 04x tier sibling)
    - dsx/checks/metrics.py (_check_simpsons_paradox :296-349 — reuse the identical `_sign` convention (metrics.py:352-353) and the opposition predicate :314, and mirror the `overall_effect is None` / `len(segments) < 2: return` guard :300; do NOT fold the new emit into this function)
    - scripts/gen-finding-catalogue.py (_D05_ALLOWLIST_CODES :183, DSX-ML-034 exact-code precedent :209, DSX-CLM-034 :218; _CITATION_RE :222; _REFVALUE_RE :223-226; _TEST_MARKER_RE :226; _resolve_docstrings :345; check_d05 :402-428; the literal-severity requirement of the report.add extractor)
    - references/finding-codes.md (Total line :16 `**Total: 278 codes.**`; the DSX-COH family rows :351-356 with the 041 slot free after DSX-COH-040 :356)
    - tests/test_finding_catalogue_invariant.py (_EXPECTED_TOTAL = 278 :42; method test_finding_catalogue_stays_at_278_codes :70; _SNAPSHOT_TOTAL = 256 :48 stays frozen — do NOT touch)
    - tests/test_phase20_zero_mint_close.py (_declared_total(_CATALOGUE), 278 :100 and the cosmetic 278 literals; the frozen 256 snapshot test :107-111 — do NOT touch)
    - tests/test_p19_categorical_rows.py (_EXPECTED_TOTAL = 278 :39; method test_catalogue_declares_exactly_the_expected_total :80; the cosmetic 278 literals :34,:86,:90 — the frozen 275/256 historical baselines stay)
    - templates/ANALYSIS-SPEC.yaml (the decision block :34-66 with the commented replay sub-block :48-56 and the deadline line :66 as the additive-key precedent; results.segments :260 `[{ name, effect, n }]` — no ci)
    - .planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-CONTEXT.md (D-29-05 exact docstring obligations; Trigger predicate; D-29-04 tier-placement rationale)
  </read_first>
  <behavior>
    - Implements every behaviour bullet from Task 2; running Task 2's unit tests now passes (GREEN).
    - Existing specs (non-prescriptive, or with no opposing segment, or with empty results) remain unaffected — the check returns early.
  </behavior>
  <action>
    Add a brand-new function `_check_subgroup_harm_disposition(spec, qtype, report)` (or similar) to
    dsx/checks/coherence.py and dispatch it from coherence.check() alongside the five existing sub-checks.
    The function returns immediately unless question_type is prescriptive; it reads
    section(spec, "results") for overall_effect and segments and returns early when overall_effect is
    None or there are no segments, mirroring _check_simpsons_paradox's guard (metrics.py:300). It reads
    decision.subgroup_harm_floor (default 0) from section(spec, "decision"), and the optional
    decision.subgroup_harm[] list. For each segment with a numeric effect it computes harmed(S) using the
    IDENTICAL `_sign` convention as metrics.py (`harmed := _sign(S.effect) == -_sign(overall)`, reusing
    metrics.py:352-353 — import it or replicate the one-line helper, never a divergent convention) AND
    `S.n >= floor`. For each harmed segment: if there is no decision.subgroup_harm[] row whose `segment`
    matches the segment name, emit `report.add("DSX-COH-041", "CRITICAL", ...)`; else if the matching row
    has disposition `accept` and a blank/missing rationale, emit `report.add("DSX-COH-041", "HIGH", ...)`.
    Both severities are STRING LITERALS (the catalogue extractor skips non-literal severities). The CI
    OR-arm of the trigger is written in structurally but is latent — `results.segments[]` has no `ci`
    field today (templates:260), so only the `n >= floor` arm bites; do not make a CI-required trigger
    (it could never fire on the target miss). Do NOT place this report.add inside _check_simpsons_paradox
    or any existing coherence sub-check. Write the function docstring to satisfy the D-05 gate: a
    `Citation:` line naming Gail, M. & Simon, R. (1985), Biometrics 41(2):361-372, PMID 4027319, stated
    as the MOTIVATING DEFINITION of a qualitative/crossover interaction (opposite-sign treatment effects
    across subsets = when a segment counts as harmed); a `Structural criterion:` line describing the
    declaration-level obligation (an opposite-sign-above-floor declared segment must carry a disposition
    row — a pure structural read of declared fields, no statistic computed on the gate path, D-02); and a
    plain statement of the bounded-catch honesty (attribution over honestly-declared segments, NOT
    detection of hidden, mis-signed, or omitted harm). Explicitly do NOT claim Gail & Simon supply or
    authorise the enforcement mechanic (their paper is a likelihood-ratio test; this check runs no such
    test). Add decision.subgroup_harm[] (a list of `{segment, effect, ci, n, disposition, rationale}`)
    and decision.subgroup_harm_floor (int absolute n, default 0) as two commented-optional keys in the
    decision block of templates/ANALYSIS-SPEC.yaml, consistent with the existing optional decision keys
    (the replay sub-block :48-56 precedent); no dsx/spec.py change. Add the EXACT CODE string DSX-COH-041
    to _D05_ALLOWLIST_CODES in scripts/gen-finding-catalogue.py (exact code, following the DSX-ML-034 and
    DSX-CLM-034 precedents — never a `DSX-COH-` prefix). Regenerate the catalogue via the generator's
    --write mode so references/finding-codes.md gains exactly one DSX-COH-041 row after DSX-COH-040 and
    the Total line moves to 279 — never hand-edit the catalogue. Then move the FOUR count pins in
    lockstep to 279: references/finding-codes.md Total (via regen); _EXPECTED_TOTAL in
    tests/test_finding_catalogue_invariant.py:42 (with its cosmetic method-name test_..._278_codes and
    message literals updated for honesty); _declared_total(_CATALOGUE) in
    tests/test_phase20_zero_mint_close.py:100 (with its cosmetic 278 literals updated); and _EXPECTED_TOTAL
    in tests/test_p19_categorical_rows.py:39 (with its cosmetic 278 literals updated). Leave the frozen
    256 Phase-12 snapshot pins and the 275 historical baselines untouched. Do NOT add DSX-COH-041 to any
    backlog set. Do NOT modify dsx/checks/dq.py.
  </action>
  <acceptance_criteria>
    - `python312 -m unittest tests.test_subgroup_harm_disposition` exits 0 (GREEN — every behaviour bullet passes).
    - coherence.check() dispatches the new function, and the new function returns early when question_type is not prescriptive or when overall_effect is None / segments empty (grep confirms an early-return guard inside a function distinct from _check_simpsons_paradox).
    - The DSX-COH-041 report.add is in a brand-new function with its own docstring, NOT inside any existing coherence sub-check.
    - The new function's docstring contains a `Citation:` line naming `Gail`, a `Structural criterion:` line, and states the check catches only honestly-declared segments (attribution, not detection of hidden harm).
    - `python312 scripts/gen-finding-catalogue.py --check` exits 0 (catalogue current; the D-05 gate resolves Citation + Structural criterion + the `# D-05: DSX-COH-041` marker).
    - references/finding-codes.md contains exactly one row whose code cell is DSX-COH-041 and its Total line reads `**Total: 279 codes.**`; regenerating a second time leaves `git diff --stat -- references/finding-codes.md` empty (deterministic).
    - `python312 -m unittest tests.test_finding_catalogue_invariant tests.test_phase20_zero_mint_close tests.test_p19_categorical_rows -q` reports OK with all three pins at 279.
    - `git diff --stat -- dsx/checks/dq.py` prints nothing (byte-frozen).
    - Full suite green: `python312 -m unittest discover -s tests -q` reports OK (the check is silent for all existing specs).
  </acceptance_criteria>
  <verify>
    <automated>python312 -m unittest tests.test_subgroup_harm_disposition -q && python312 scripts/gen-finding-catalogue.py --check && python312 -m unittest tests.test_finding_catalogue_invariant tests.test_phase20_zero_mint_close tests.test_p19_categorical_rows -q && python312 -m unittest discover -s tests -q</automated>
  </verify>
  <done>DSX-COH-041 fires CRITICAL on a missing disposition row and HIGH on an accept-without-rationale row, is silent when results are empty or the question is not prescriptive, carries an honest Gail & Simon D-05 docstring, the catalogue is deterministically at 279 with all four count pins moved, and the full suite is green; dq.py untouched. Skip this task entirely if Task 1 recorded VERDICT: CAUGHT (Branch A).</done>
</task>

<task type="auto">
  <name>Task 4 (CLOSURE — branches on Task 1's verdict)</name>
  <files>.planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-MEASUREMENT.md (the recorded VERDICT decides which branch runs — CAUGHT → Branch A, LIVE MISS → Branch B)
    - .planning/phases/28-evidence-case-magnitude-no-test-computed/28-01-PLAN.md (Task 4 Branch A / Branch B shape to mirror)
  </read_first>
  <action>
    Read the VERDICT line from 29-MEASUREMENT.md and branch (CAUGHT routes to Branch A, LIVE MISS routes
    to Branch B — do not cross the wires):

    Branch A (VERDICT: CAUGHT): the case is caught by an existing code on the merits. Append the no-mint
    record into 29-MEASUREMENT.md (which code caught it, confirmed against the swap counterfactual, and a
    line stating that per D-13 a caught case is a valid no-mint terminal that satisfies REQ-P29-01/02/03).
    Mint nothing; Tasks 2 and 3 were correctly skipped; Plan 29-02 does NOT run. Do NOT rewrite brief.md
    §6.5 item 9 here — the brief is edited only on the LIVE MISS path by Plan 29-02, and item 9 stays on
    the backlog with the caught evidence recorded in 29-MEASUREMENT.md. Do NOT edit REQUIREMENTS.md,
    STATE.md, or ROADMAP.md (single-writer — the orchestrator owns them).

    Branch B (VERDICT: LIVE MISS): this is a verification-only step. Confirm Tasks 2 and 3 landed cleanly:
    the full suite is green, the catalogue is deterministically at 279 with all four count pins moved, and
    dq.py is byte-frozen. Record in 29-MEASUREMENT.md that the phase proceeds to Plan 29-02 for fixture
    promotion as a TARGET (D-29-00). Do NOT rewrite the brief here — that belongs to Plan 29-02. Leave
    brief.md unchanged in this branch. Do NOT edit REQUIREMENTS.md, STATE.md, or ROADMAP.md.
  </action>
  <acceptance_criteria>
    - Branch A only: 29-MEASUREMENT.md carries the no-mint record naming the catching code; brief.md is unchanged by this plan; Plan 29-02 does not run.
    - Branch B only: `python312 -m unittest discover -s tests -q` reports OK and `python312 scripts/gen-finding-catalogue.py --check` exits 0; 29-MEASUREMENT.md states the phase proceeds to Plan 29-02.
    - Neither branch edits REQUIREMENTS.md, STATE.md, ROADMAP.md, or dq.py (`git status --porcelain .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md dsx/checks/dq.py` prints nothing).
  </acceptance_criteria>
  <verify>
    <automated>python312 -m unittest discover -s tests -q && python312 scripts/gen-finding-catalogue.py --check</automated>
  </verify>
  <done>The correct branch ran off Task 1's recorded verdict: on CAUGHT, 29-MEASUREMENT.md records the catch and the phase closes with no mint (Plan 29-02 does not run); on LIVE MISS, the suite and catalogue are green at 279 and the phase hands off to Plan 29-02. No single-writer tracking file or dq.py was edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured verdict → mint decision | Task 1's recorded VERDICT is the sole gate on whether any code is minted; an ambiguous conditional read as "mint by default" would mint on a CAUGHT verdict and violate D-13. |
| minted code → finding catalogue | A new report.add call-site becomes a catalogue row via AST extraction; a non-deterministic or over-claiming emission corrupts the committed catalogue and the four count pins. |
| docstring / test marker → D-05 honesty record | The Citation text is the durable provenance claim; over-claiming that Gail & Simon supply the enforcement mechanic (a likelihood-ratio test) launders a motivating definition into an asserted mechanism (D-29-05). |
| untrusted spec content → static check | decision.subgroup_harm_floor / decision.subgroup_harm[] / results.segments are read off a parsed YAML dict; the entrypoint is read as text and never executed (V5 input validation, standing gate-path rule). |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Tampering (of design intent) / Repudiation (of D-13) | plan conditional executed as "mint by default" on a CAUGHT verdict | high | mitigate | Task 1's `<done>` routes CAUGHT to Branch A and LIVE MISS to Branch B explicitly; every mint task (2/3) is skip-on-CAUGHT and Task 4 branches on the recorded VERDICT. The measurement is the literal first act. |
| T-29-02 | Repudiation | DSX-COH-041 docstring + `# D-05:` marker | high | mitigate | Both cite Gail & Simon (1985) as the motivating DEFINITION only, state the declaration-only structural criterion as the mechanism, and record the bounded-catch honesty; presence-grepped in Task 2/Task 3 acceptance (D-29-05). |
| T-29-03 | Tampering | catalogue count pins (278→279) | high | mitigate | Move references/finding-codes.md Total plus all three test pins (invariant, phase20, p19) together; the three invariant tests fail on drift (Task 3). |
| T-29-04 | Tampering | D-05 docstring placement | high | mitigate | Write DSX-COH-041 as a brand-new function so its Citation obligation does not retrofit an uncited legacy coherence docstring (Task 3 acceptance greps for the separate function). |
| T-29-05 | Tampering | _D05_ALLOWLIST_CODES entry | high | mitigate | Add DSX-COH-041 as an EXACT CODE, never a `DSX-COH-` prefix (a prefix add obligates the uncited legacy COH family and fails the build red). |
| T-29-06 | Tampering | frozen D-29-02 case widened to manufacture a miss | high | mitigate | The four-segment shape and numbers are frozen; segment D is not shaved to a trivial n/effect; a CAUGHT verdict is accepted as a valid no-mint terminal, not re-shaped (guardrail 1). |
| T-29-07 | Tampering | dsx/checks/dq.py byte-freeze | medium | mitigate | Only dsx/checks/coherence.py is edited; `git diff --stat -- dsx/checks/dq.py` must be empty (Task 3 acceptance). |
| T-29-SC | Supply chain | package installs | low | accept | This plan installs no npm/pip/cargo packages. The spike entrypoint is read as text by the code scan and never executed; no Package Legitimacy Gate applies. |
</threat_model>

<verification>
- `grep -Eq "VERDICT: (LIVE MISS|CAUGHT)"` — 29-MEASUREMENT.md carries an explicit VERDICT line (Task 1).
- LIVE MISS branch: `python312 -m unittest tests.test_subgroup_harm_disposition -q` GREEN after Task 3.
- LIVE MISS branch: `python312 scripts/gen-finding-catalogue.py --check` exit 0 — catalogue current + D-05 gate green at 279.
- LIVE MISS branch: `python312 -m unittest tests.test_finding_catalogue_invariant tests.test_phase20_zero_mint_close tests.test_p19_categorical_rows -q` — all three pins at 279.
- Both branches: `python312 -m unittest discover -s tests -q` — full suite OK.
- Both branches: `git diff --stat -- dsx/checks/dq.py` empty — byte-freeze held.
</verification>

<success_criteria>
- The case is measured live FIRST, from a fresh tempdir, with an explicit VERDICT recorded before any mint code (REQ-P29-01; D-13), and MET-030/031 confirmed silent at 1-of-4.
- On CAUGHT: nothing is minted, the catching code is recorded, and Plan 29-02 does not run — a valid no-mint close (REQ-P29-01/02/03).
- On LIVE MISS: DSX-COH-041 fires CRITICAL on a missing disposition row and HIGH on an accept-without-rationale row, is silent on empty/non-prescriptive specs; the catalogue is deterministically at 279 with all four count pins moved; the D-05 docstring cites Gail & Simon honestly (motivating definition, not mechanism; bounded catch) and passes the generator's D-05 gate (REQ-P29-03).
- dsx/checks/dq.py unchanged; REQUIREMENTS.md / STATE.md / ROADMAP.md untouched.
</success_criteria>

<output>
Create `.planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-01-SUMMARY.md` when done.
</output>
