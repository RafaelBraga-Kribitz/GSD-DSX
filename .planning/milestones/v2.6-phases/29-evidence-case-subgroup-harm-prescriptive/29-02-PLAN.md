---
phase: 29-evidence-case-subgroup-harm-prescriptive
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - examples/known-bad/subgroup-harm-without-disposition-ANALYSIS-SPEC.yaml
  - examples/known-bad/subgroup-harm-without-disposition-entrypoint.py
  - examples/known-bad/subgroup-harm-without-disposition-POSTMORTEM.md
  - examples/known-bad/subgroup-harm-without-disposition-ATTRIBUTION.yaml
  - tests/test_known_bad_corpus.py
  - tests/test_frame_val.py
  - tests/test_causal_verb_golden.py
  - tests/test_dsx.py
  - brief.md
autonomous: true
requirements: [REQ-P29-01, REQ-P29-02, REQ-P29-03]
must_haves:
  truths:
    - "This plan executes ONLY on the LIVE MISS branch: Plan 29-01's Task 1 must have recorded `VERDICT: LIVE MISS` as the first content line of 29-MEASUREMENT.md. If it recorded `VERDICT: CAUGHT`, the phase already closed in 29-01 (Branch A, no-mint terminal) and this plan does NOT run (D-13; guardrail 1)."
    - "The promoted ANALYSIS-SPEC declares the FROZEN D-29-02 shape verbatim from the 29-01 spike — question_type prescriptive, a prescriptive claim, positive results.overall_effect ≈ +3.2pp, exactly four results.segments (A n=4000 +5.0pp, B n=3000 +4.0pp, C n=2000 +3.0pp, D minority n=1000 −6.0pp), decision.subgroup_harm_floor declared 500, and NO decision.subgroup_harm[] disposition row. Given segment D opposes at n=1000 well above the floor, that omission makes DSX-COH-041 fire CRITICAL at plan/verify/ship — this is the TARGET being caught (D-29-00), the INVERSE of the Phase-27/28 permanent-miss polarity."
    - "DSX-COH-041's catch is wired in `_TARGET_DEFECT_CODES[slug]` at plan/verify/ship, and `_EXPECTED_CAUGHT_DEFECTS[slug]` is `frozenset()` (empty). This mirrors the prescriptive-churn-recommendation precedent EXACTLY: `_CRITICAL_THRESHOLD_POINTS` is `(\"plan\", \"execute\")`, the `coherence` family is registered at plan/verify/ship and ABSENT from execute, so DSX-COH-041 does NOT fire at execute — a `frozenset({DSX-COH-041})` in `_EXPECTED_CAUGHT_DEFECTS` (a both-points map) would wrongly demand it at execute and fail test_every_spec_blocks_only_on_its_target_defect_at_critical_threshold_points. The empty entry is required by the key-parity test test_expected_caught_defects_keys_match_the_corpus_on_disk (key link)."
    - "`_EXPECTED_VAL_CODES[\"subgroup-harm-without-disposition-ANALYSIS-SPEC.yaml\"]` is `set()` (measured via val.check on the loaded fixture — the frame is a clean causal→prescriptive clone and DSX-COH-041 is a DSX-COH-* coherence code, not DSX-VAL-*, so no DSX-VAL-* fires); `_GOLDEN_SHIP_FINDINGS[\"examples/known-bad/subgroup-harm-without-disposition-ANALYSIS-SPEC.yaml\"]` is the MEASURED CRITICAL/HIGH ship residual with DSX-COH-041 PRESENT (it fires CRITICAL at ship — the INVERSE of the magnitude miss, where the attributing code was ABSENT from the golden set). Both maps' key-parity tests fail loudly if either key is missing; the golden set is re-measured live at execute, never guessed."
    - "The ATTRIBUTION sidecar is `kind: target` — a NEW value taught to the harness this plan: the closed-vocabulary assertion in test_attribution_sidecars_reference_valid_codes_and_items is extended from `(\"miss\", \"caught\")` to `(\"miss\", \"caught\", \"target\")`. Its `absent_code` is `DSX-COH-041` (the field name is schema legacy; for a caught/target kind the falsifiability test's non-miss branch asserts this code MUST fire CRITICAL live — assertIn against the plan/execute/verify/ship union); its `promotes_backlog_item` is the frozen `6.5-item-9-subgroup-harm-declaration` (a verbatim `_SECTION_65_ITEM_IDS` member at tests/test_known_bad_corpus.py:951). DSX-COH-041 stays OUT of `_SECTION_65_BACKLOG_CODES` — it ships to the catalogue in wave 1, and the disjointness assertion (:1723-1728) forbids a shipped code there (key link)."
    - "The spec count moves 44 → 45 in tests/test_dsx.py:588 exactly when the fixture lands (one added ANALYSIS-SPEC, no drift). `_ABSENT_PARTITION_FLOOR` STAYS 3 (tests/test_known_bad_corpus.py:993) — a `kind: target` sidecar is excluded from the ABSENT/miss partition by the `!= \"miss\"` guard at :1958, so it subtracts nothing from the existing miss set; the plan VERIFIES the floor via the standing assertions (:2071/:2129/:2174 and the stratified `>= floor` check), it does NOT edit it (D-29-00 watch item)."
    - "brief.md §6.5 item 9 (the subgroup-harm-declaration / prescriptive subgroup-harm row) is rewritten with the measured evidence: a LIVE MISS at the gate points pre-mint (MET-030/031 structurally silent at 1-of-4 opposing) → DSX-COH-041 minted under D-05/D-06 to CATCH it — a real closed catch (PRESENT/DETECTED, D-29-00), NOT an attribution-only miss — plus the Obermeyer et al. 2019 real-world case recorded with the honest 'abstract + authoritative metadata confirmed' caveat (never implying the body was read)."
    - "D-05 honesty (D-29-05) holds in every POSTMORTEM and brief mention: Gail & Simon (1985) is framed as the MOTIVATING DEFINITION only (opposite-sign-across-subsets = when a segment counts as harmed), never as the enforcement mechanic — their paper is a likelihood-ratio test and this check computes nothing on the gate path (D-02); Obermeyer et al. 2019 is the real-world motivation (abstract+metadata grade), not an enforcement authority; the bounded-catch limit is stated (DSX-COH-041 buys attribution over honestly-declared segments, not detection of hidden/mis-signed/omitted harm)."
    - "`node install.mjs` then `node install.mjs --check` both pass at phase end — the examples/ overlay changed, so the installed copy is re-synced to the source tree."
    - "dsx/checks/dq.py is byte-frozen (`git diff --stat -- dsx/checks/dq.py` empty); no single-writer tracking file (REQUIREMENTS.md, STATE.md, ROADMAP.md) and none of wave-1's owned catalogue files (references/finding-codes.md, scripts/gen-finding-catalogue.py, tests/test_finding_catalogue_invariant.py, tests/test_phase20_zero_mint_close.py, tests/test_p19_categorical_rows.py) is touched."
  artifacts:
    - examples/known-bad/subgroup-harm-without-disposition-ANALYSIS-SPEC.yaml
    - examples/known-bad/subgroup-harm-without-disposition-entrypoint.py
    - examples/known-bad/subgroup-harm-without-disposition-POSTMORTEM.md
    - examples/known-bad/subgroup-harm-without-disposition-ATTRIBUTION.yaml
  key_links:
    - "DSX-COH-041 is this fixture's own-target code via `_TARGET_DEFECT_CODES[slug]` at plan/verify/ship (single code per point, mirroring chart-truncated-axis-bar's bare-string shape); `_own_target_codes(slug)` therefore returns `{DSX-COH-041}` and the ship-completeness test (test_ship_gate_findings_are_all_documented_incidental_corpus_gaps :1392) admits it. `_EXPECTED_CAUGHT_DEFECTS[slug]` is `frozenset()` (empty), required only for the key-parity test."
    - "The sidecar's `absent_code: DSX-COH-041` resolves against the catalogue shipped in wave 1 (plan 29-01, Total 279) via the validated-union check (:1757-1762); its `promotes_backlog_item` resolves against the frozen `_SECTION_65_ITEM_IDS` member at tests/test_known_bad_corpus.py:951; `kind: target` resolves against the closed-vocabulary tuple this plan extends (:1771)."
    - "The `kind: target` polarity is ENFORCED by the falsifiability test's non-miss branch (:1878-1885): `assertIn(absent_code, all_critical)` over the plan/execute/verify/ship CRITICAL union — DSX-COH-041 must fire CRITICAL live somewhere. This is the exact assertion that makes the sidecar unfalsifiable-by-lying; a spec that adds a decision.subgroup_harm[] row (silencing DSX-COH-041) flips this assertion red."
  prohibitions:
    - "This plan executes ONLY if Plan 29-01 recorded `VERDICT: LIVE MISS`; if 29-MEASUREMENT.md records `VERDICT: CAUGHT`, this plan does not run (the phase closed in 29-01's Branch A). Never route a CAUGHT verdict into execution; every CAUGHT pointer terminates in 29-01, every LIVE-MISS pointer proceeds here."
    - "The promoted ANALYSIS-SPEC must NOT gain a decision.subgroup_harm[] disposition row — that would silence DSX-COH-041 and convert the catch into a non-catch (the falsifiability non-miss branch would then fail `assertIn`, and the golden ship set would lose DSX-COH-041). The frozen D-29-02 four-segment shape and its numbers are not widened, weakened, or shaved (segment D is not moved toward a trivial n or a near-zero effect — that manufactures a trivial case, D-13)."
    - "DSX-COH-041 is NOT placed in `_EXPECTED_CAUGHT_DEFECTS[slug]` — that both-points map would demand it at execute, where `coherence` does not run, failing the critical-threshold test. The catch lives in `_TARGET_DEFECT_CODES` (point-scoped, plan/verify/ship); `_EXPECTED_CAUGHT_DEFECTS[slug]` stays `frozenset()`."
    - "DSX-COH-041 is NOT added to `_SECTION_65_BACKLOG_CODES` — it ships to the catalogue in wave 1; the disjointness assertion (:1723-1728) forbids a shipped code there."
    - "The golden ship set is re-measured live on the promoted fixture from 29-MEASUREMENT.md, never guessed; if the measured CRITICAL/HIGH ship set does NOT contain DSX-COH-041, or contains an undocumented incidental, STOP and record the discrepancy — a golden set without DSX-COH-041 would mean the fixture stopped being a catch, contradicting the frozen D-29-00 design."
    - "`_ABSENT_PARTITION_FLOOR` (:993) is NOT edited (stays 3, verified via the standing assertions). No entry is added to `_PER_FIXTURE_INCIDENTAL_CODES` unless 29-MEASUREMENT.md's swap-still-fires record documents an actual swap-invariant incidental firing on this fixture — the frozen design deliberately clears every other check on the merits, so the expectation (verified, not assumed) is that DSX-COH-041 is the only firing code."
    - "dsx/checks/dq.py is not modified (byte-frozen). references/finding-codes.md, scripts/gen-finding-catalogue.py, tests/test_finding_catalogue_invariant.py, tests/test_phase20_zero_mint_close.py and tests/test_p19_categorical_rows.py are wave-1 (29-01) territory and are NOT touched here. REQUIREMENTS.md, STATE.md and ROADMAP.md are single-writer (orchestrator only)."
---

<recorded_authoring_choices>
Two harness-mechanism resolutions the frozen CONTEXT (D-29-00) delegated to plan authoring — grounded by reading tests/test_known_bad_corpus.py this firing, recorded here for the plan-checker (not silent design changes; the frozen D-29-00 intent — "DSX-COH-041 is PRESENT/caught, not an absent miss" — is fully honored):

1. `_EXPECTED_CAUGHT_DEFECTS` vs `_TARGET_DEFECT_CODES` wiring. The CONTEXT's "Harness wiring (D-29-00)" line reads `_EXPECTED_CAUGHT_DEFECTS[slug] = frozenset({DSX-COH-041})` AND `_TARGET_DEFECT_CODES[slug] = DSX-COH-041 at plan/verify/ship`. Live code shows these cannot both hold: `_CRITICAL_THRESHOLD_POINTS = ("plan", "execute")` (tests/test_known_bad_corpus.py:57), `_EXPECTED_CAUGHT_DEFECTS` applies its frozenset at BOTH those points (`_effective_target_map` / test_every_spec_blocks_only_on_its_target_defect_at_critical_threshold_points), and the `coherence` family is registered at plan/verify/ship and ABSENT from execute (dsx/cli.py GATE_PROFILES), so DSX-COH-041 does not fire at execute. Putting it in `_EXPECTED_CAUGHT_DEFECTS` would demand it at execute and fail the critical-threshold test. RESOLUTION (mirrors the prescriptive-churn-recommendation precedent exactly, :294-298 / :519): the catch lives point-scoped in `_TARGET_DEFECT_CODES[slug] = {"plan": "DSX-COH-041", "verify": "DSX-COH-041", "ship": "DSX-COH-041"}`; `_EXPECTED_CAUGHT_DEFECTS[slug] = frozenset()` (empty, key-parity only). The design decision "DSX-COH-041 PRESENT/caught, not an absent miss" is honored — the code is this fixture's declared own-target, PRESENT everywhere it fires. This is a mechanism clarification, exactly analogous to 29-01's cli.py-registration note.

2. `kind: target` sidecar schema — a harness parser change IS needed. The falsifiability parser recognizes `kind == "miss"` explicitly and routes every other value (its `else` branch, documented `caught`) to `assertIn(absent_code, all_critical)` — "the named code MUST fire CRITICAL somewhere" (:1878-1885). BUT test_attribution_sidecars_reference_valid_codes_and_items enforces a CLOSED vocabulary `kind in ("miss", "caught")` (:1769-1774), so a raw `kind: target` sidecar fails schema. RESOLUTION: teach the parser `target` minimally — (a) extend the closed-vocabulary tuple to `("miss", "caught", "target")` with an updated message documenting `target` as a caught §6.5-promotion (code PRESENT, promotes a backlog item); (b) make the falsifiability `else` branch's intent explicit for `caught`/`target` (the `assertIn` enforcement is unchanged — it already routes any non-miss kind to "must fire CRITICAL"; DSX-COH-041 fires, so it passes). The stratified ABSENT-partition guard already excludes any non-miss kind via `!= "miss"` (:1958), so a `target` sidecar subtracts nothing from `_ABSENT_PARTITION_FLOOR = 3` — no change there (verified). The `absent_code` field name is schema legacy shared by both branches (`data["absent_code"]`, :1856); for a `target` kind it names the code that FIRES, asserted PRESENT by the non-miss branch. A distinct `kind: target` (rather than reusing `caught`) is chosen deliberately: the five existing caught fixtures carry NO sidecar (pure catches), and this fixture is a hybrid — caught AND a §6.5-backlog promotion needing a sidecar to carry `promotes_backlog_item` — so a first-class `target` value makes that dual role legible and machine-checkable, matching the CONTEXT's D-29-00 language ("wires as a TARGET").

One open item the plan-checker/orchestrator should verify at execute (cannot be closed from static reads): the exact measured CRITICAL/HIGH ship residual set for `_GOLDEN_SHIP_FINDINGS` — the frozen design predicts `{DSX-COH-041}` only (every other check clears on the merits), but the value is re-measured live from 29-MEASUREMENT.md and the fresh-tempdir gate at execute time, never pinned from this plan. If any swap-invariant incidental is recorded, it is encoded per `_PER_FIXTURE_INCIDENTAL_CODES` (with the D-28-06 guard set) rather than credited as this fixture's catch.
</recorded_authoring_choices>

<objective>
Promote the measured spike into a committed known-bad corpus fixture and complete every harness
entry, then rewrite the brief's §6.5 item 9 row with the measured evidence. This is the REQ-P29-01
(committed corpus case), REQ-P29-02 (the documented public case landing in the brief/POSTMORTEM) and
REQ-P29-03 (harness entries complete + brief rewrite) deliverable, and it executes ONLY on the LIVE
MISS branch — Plan 29-01's Task 1 must have recorded `VERDICT: LIVE MISS`. (If 29-01 recorded CAUGHT,
the phase already closed in 29-01's Branch A and this plan does not run.)

The spike (spike/subgroup-harm-without-disposition-*) was built to the FROZEN D-29-02 shape and, on
the LIVE MISS branch, measured a pre-mint LIVE MISS before DSX-COH-041 was authored (29-MEASUREMENT.md;
D-13). Wave 1 (29-01) then minted DSX-COH-041, so on this fixture the honestly-declared, undispositioned
opposing segment D is now CAUGHT — DSX-COH-041 fires CRITICAL at plan/verify/ship. This is the
load-bearing INVERSE of Phases 27/28 (D-29-00): the fixture wires as a TARGET (the minted code is
PRESENT everywhere it fires), NOT as a `kind: miss` sidecar with an ABSENT code. Promote it under the
corpus basename `subgroup-harm-without-disposition` with a new POSTMORTEM (reproducing the four/five-point
gate table verbatim) and an ATTRIBUTION sidecar with `kind: target`, `absent_code: DSX-COH-041` (the
code that now fires) and `promotes_backlog_item: 6.5-item-9-subgroup-harm-declaration` (the frozen id at
tests/test_known_bad_corpus.py:951). Register the fixture as a TARGET across the harness maps, move the
spec count 44→45, verify `_ABSENT_PARTITION_FLOOR` still holds at 3, keep DSX-COH-041 OUT of the backlog
disjoint set, and re-sync the installer overlay.

Purpose: turn the measured, minted catch into a permanent, self-testing corpus case whose ATTRIBUTION
sidecar is falsifiable against the live gate — the first `kind: target` sidecar in the corpus.
Output: four `examples/known-bad/subgroup-harm-without-disposition-*` files; the target/expected/val/
golden harness map entries; the spec-count move; the rewritten brief §6.5 item 9 row; a re-synced
installer overlay.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Frozen design, plan-input research, wave-1 dependency (do NOT re-open D-29-00..05; guardrail 1)
@.planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-CONTEXT.md
@.planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-RESEARCH.md
@.planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-01-PLAN.md

# The structural template to mirror (invert its miss→target wiring)
@.planning/phases/28-evidence-case-magnitude-no-test-computed/28-02-PLAN.md

# Corpus siblings to mirror for file format + harness precedent
@examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml
@examples/known-bad/full-frame-cleaning-POSTMORTEM.md
@examples/known-bad/magnitude-without-computed-effect-ATTRIBUTION.yaml
</context>

<artifacts>
## Artifacts this phase produces (this plan's share)

- `examples/known-bad/subgroup-harm-without-disposition-ANALYSIS-SPEC.yaml` — promoted from the 29-01
  spike spec; `spec_id` and `reproducibility.entrypoint` renamed to the corpus basename; spike header
  stripped, corpus header added; the FROZEN D-29-02 shape (four segments incl. minority D opposing above
  the declared floor, NO decision.subgroup_harm[] row) is preserved byte-equal to the spike.
- `examples/known-bad/subgroup-harm-without-disposition-entrypoint.py` — promoted from the spike
  entrypoint (basename in the head comment only; body is static text read by the code scan, never
  executed).
- `examples/known-bad/subgroup-harm-without-disposition-POSTMORTEM.md` — authored new; reproduces the
  four/five-point measured gate table (29-MEASUREMENT.md), records MET-030/031 silent at 1-of-4 and the
  swap-still-fires counterfactual, and names DSX-COH-041 as the code that now CATCHES the declared harm
  (PRESENT/DETECTED — the D-29-00 target polarity).
- `examples/known-bad/subgroup-harm-without-disposition-ATTRIBUTION.yaml` — authored new; the first
  `kind: target` sidecar; `absent_code: DSX-COH-041`,
  `promotes_backlog_item: "6.5-item-9-subgroup-harm-declaration"`, `protocol_adherence: skipped`,
  rationale.

(Plan 29-01 produces _check_subgroup_harm_disposition / DSX-COH-041 / the schema keys / the catalogue
row at 279 and the four count pins — all on the LIVE MISS branch.)
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: promote the spike spec + entrypoint into the known-bad corpus</name>
  <files>examples/known-bad/subgroup-harm-without-disposition-ANALYSIS-SPEC.yaml, examples/known-bad/subgroup-harm-without-disposition-entrypoint.py</files>
  <read_first>
    - .planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-MEASUREMENT.md (confirm the recorded first-line VERDICT is LIVE MISS before doing anything; this plan does not run on CAUGHT)
    - .planning/phases/29-evidence-case-subgroup-harm-prescriptive/spike/subgroup-harm-without-disposition-ANALYSIS-SPEC.yaml (source; spike header, spec_id, reproducibility.entrypoint, the frozen four-segment shape)
    - .planning/phases/29-evidence-case-subgroup-harm-prescriptive/spike/subgroup-harm-without-disposition-entrypoint.py (source head comment)
    - examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml (corpus header precedent :1-38; spec_id == basename convention)
    - .planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-CONTEXT.md (D-29-02 exact fixture shape; D-29-00 target-not-miss; the "omit decision.subgroup_harm[] is the sole defect" invariant)
  </read_first>
  <action>
    First confirm 29-MEASUREMENT.md's first content line records VERDICT: LIVE MISS; if it records
    CAUGHT, stop — this plan must not run. Copy the 29-01 spike spec
    spike/subgroup-harm-without-disposition-ANALYSIS-SPEC.yaml to
    examples/known-bad/subgroup-harm-without-disposition-ANALYSIS-SPEC.yaml and the spike entrypoint
    spike/subgroup-harm-without-disposition-entrypoint.py to
    examples/known-bad/subgroup-harm-without-disposition-entrypoint.py. In the promoted spec: set spec_id
    to "subgroup-harm-without-disposition" (basename convention, full-frame precedent) and set
    reproducibility.entrypoint to "subgroup-harm-without-disposition-entrypoint.py". Strip any spike
    header (the throwaway "MEASUREMENT-SPIKE / NOT a committed corpus fixture" banner) and replace it with
    a corpus header modelled on full-frame-cleaning-ANALYSIS-SPEC.yaml:1-38 — naming REQ-P29-01/02/03,
    stating the FROZEN D-29-02 defect (a genuinely prescriptive recommendation with positive overall
    effect ≈ +3.2pp and four declared segments, of which minority segment D opposes at n=1000 well above
    the declared floor of 500, with decision.subgroup_harm_floor declared and NO decision.subgroup_harm[]
    disposition row — the sole defect), and that on the post-mint gate the case is CAUGHT by DSX-COH-041
    CRITICAL at plan/verify/ship (a measured LIVE MISS pre-mint per 29-MEASUREMENT.md, D-13). In the
    header, cite Gail & Simon (1985) as the MOTIVATING DEFINITION of a qualitative/crossover interaction
    only (opposite-sign-across-subsets) and Obermeyer et al. 2019 as the real-world motivation confirmed
    at abstract + authoritative metadata grade — never implying the paper body was read, never implying
    either source supplies the enforcement mechanic (D-29-05). Leave every other block byte-equal to the
    spike — in particular do NOT add a decision.subgroup_harm[] row (that silences DSX-COH-041 and
    un-catches the defect) and do NOT alter segment D. In the promoted entrypoint, update only the
    basename reference in its head comment; the body is static text read by the code scan and is never
    executed. Use python312 for any validate run.
  </action>
  <acceptance_criteria>
    - Both target files exist under examples/known-bad/ with the `subgroup-harm-without-disposition-` basename.
    - `python312 -m dsx.cli validate --spec examples/known-bad/subgroup-harm-without-disposition-ANALYSIS-SPEC.yaml` exits 0.
    - The promoted spec's spec_id is `subgroup-harm-without-disposition` and reproducibility.entrypoint is `subgroup-harm-without-disposition-entrypoint.py`.
    - The promoted spec declares question_type prescriptive, positive results.overall_effect, exactly four results.segments (three positive, minority D opposing at n above the declared floor), decision.subgroup_harm_floor, and NO decision.subgroup_harm[] row.
    - Any spike-banner header text is absent from the promoted spec; a corpus header naming REQ-P29-01/02/03 is present.
  </acceptance_criteria>
  <verify>
    <automated>python312 -m dsx.cli validate --spec examples/known-bad/subgroup-harm-without-disposition-ANALYSIS-SPEC.yaml</automated>
  </verify>
  <done>The spike spec and entrypoint are promoted under the corpus basename, validate exit 0, and the fixture preserves the frozen D-29-02 shape with no decision.subgroup_harm[] row. If 29-MEASUREMENT.md recorded VERDICT: CAUGHT, this task and the rest of this plan do NOT run.</done>
</task>

<task type="auto">
  <name>Task 2: author the POSTMORTEM and the falsifiable kind: target ATTRIBUTION sidecar</name>
  <files>examples/known-bad/subgroup-harm-without-disposition-POSTMORTEM.md, examples/known-bad/subgroup-harm-without-disposition-ATTRIBUTION.yaml</files>
  <read_first>
    - examples/known-bad/full-frame-cleaning-POSTMORTEM.md (postmortem structure; four-point gate table format)
    - examples/known-bad/magnitude-without-computed-effect-ATTRIBUTION.yaml (sidecar schema fields — absent_code, promotes_backlog_item, kind, protocol_adherence, rationale; NOTE it is kind: miss — this new sidecar inverts kind to target)
    - .planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-MEASUREMENT.md (the verbatim four/five-point table, the MET-030/031-silent record, and the swap-still-fires diff)
    - tests/test_known_bad_corpus.py (`_SECTION_65_ITEM_IDS` member :951 — the exact promotes_backlog_item string; the corpus pairing test's required DSX-<LETTERS>-<digits> POSTMORTEM code :1235-1244; the closed-vocabulary `kind` assertion :1769-1774; the falsifiability non-miss branch :1878-1885; the target-defect naming test :2446-2482 requiring DSX-COH-041 to appear in this slug's POSTMORTEM or ATTRIBUTION)
    - .planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-CONTEXT.md (D-29-05 Gail & Simon motivating-definition-only + bounded-catch honesty; D-29-00 target polarity)
    - .planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-RESEARCH.md (the Obermeyer et al. 2019 case + the binding "abstract + authoritative metadata confirmed" honesty flag)
  </read_first>
  <action>
    Author examples/known-bad/subgroup-harm-without-disposition-POSTMORTEM.md modelled on
    full-frame-cleaning-POSTMORTEM.md: name the paired spec and entrypoint; describe the encoded defect
    (a genuinely prescriptive rollout recommendation on a positive ≈+3.2pp aggregate that declares a
    material opposing minority segment D — n=1000, −6.0pp, well above the declared floor of 500 — with no
    decision.subgroup_harm[] disposition row); reproduce the four/five-point measured gate table verbatim
    from 29-MEASUREMENT.md (validate/plan/execute/verify/ship, exit codes and CRITICAL/HIGH codes) and
    state it is the D-29-02 / D-13 entry-condition test; record that DSX-MET-030/031 are structurally
    SILENT at 1-of-4 opposing (1==4 False, 1>=2 False); record the swap-still-fires counterfactual
    (flip D to positive) and state which residual codes, if any, are swap-invariant incidentals versus
    the real catch that stops firing under the swap; and name DSX-COH-041 as the minted code that CATCHES
    the declared harm at plan/verify/ship (PRESENT/DETECTED — the D-29-00 target polarity, the INVERSE of
    the Phase-27/28 attribution-only misses). Frame Gail & Simon (1985) as the motivating definition only
    (opposite-sign-across-subsets = when a segment counts as harmed), NOT the enforcement mechanic (their
    paper is a likelihood-ratio test; this check computes nothing on the gate path, D-02); frame Obermeyer
    et al. 2019 as the real-world motivation confirmed at abstract + authoritative metadata grade (never
    implying the body was read); and state the bounded-catch limit (attribution over honestly-declared
    segments, not detection of hidden, mis-signed, or omitted harm). The POSTMORTEM MUST contain at least
    one token matching the DSX-<LETTERS>-<digits> shape (the corpus pairing test requires it) and MUST
    name DSX-COH-041 (the target-defect naming test requires this slug's own-target code to appear in its
    POSTMORTEM or ATTRIBUTION).
    Author examples/known-bad/subgroup-harm-without-disposition-ATTRIBUTION.yaml with exactly:
    `kind: target` (the first target sidecar in the corpus — the closed-vocabulary assertion is extended
    to admit it in Task 3); `absent_code: DSX-COH-041` (the field name is schema legacy shared by both
    falsifiability branches; for kind target it names the code that FIRES, and the non-miss branch asserts
    it fires CRITICAL live); `promotes_backlog_item: "6.5-item-9-subgroup-harm-declaration"` (the frozen id
    at tests/test_known_bad_corpus.py:951 — NOT the phase slug); `protocol_adherence: skipped` (the spike
    entrypoint is static text, never executed — reproduce was not run, mirroring the promoted-spike
    siblings); and a rationale explaining that DSX-COH-041 fires CRITICAL because the spec declares an
    opposing segment above the floor with no matching disposition row — a real detected catch (D-29-00),
    with Gail & Simon as the motivating definition and Obermeyer as the real-world motivation, both stated
    at their honest confirmation grade. Use python312 for any check run.
  </action>
  <acceptance_criteria>
    - Both files exist under examples/known-bad/ with the `subgroup-harm-without-disposition-` basename.
    - The POSTMORTEM contains at least one token matching the pattern `DSX-[A-Z]+-[0-9]+` and contains the string `DSX-COH-041`.
    - The POSTMORTEM contains a gate table with rows for validate, plan, execute, verify, ship matching 29-MEASUREMENT.md, records MET-030/031 silent at 1-of-4, and records the swap-still-fires diff.
    - The ATTRIBUTION file's `kind` is `target`, `absent_code` is `DSX-COH-041`, `promotes_backlog_item` is `6.5-item-9-subgroup-harm-declaration`, and `protocol_adherence` is `skipped`.
    - The POSTMORTEM and ATTRIBUTION frame Gail & Simon (1985) as the motivating definition only (not the mechanic) and Obermeyer et al. 2019 at abstract + authoritative metadata grade (not implying a full-body read).
    - `python312 -m unittest tests.test_known_bad_corpus -q` reports OK (pairing, code-presence, sidecar-schema-with-target, and — after Task 3's map entries and vocabulary edit — falsifiability all pass).
  </acceptance_criteria>
  <verify>
    <automated>python312 -m unittest tests.test_known_bad_corpus -q</automated>
  </verify>
  <done>A POSTMORTEM reproducing the measured table and naming DSX-COH-041 as the catch, plus the corpus's first kind: target ATTRIBUTION sidecar naming DSX-COH-041 against the frozen backlog id, exist and satisfy the corpus tests.</done>
</task>

<task type="auto">
  <name>Task 3: complete the harness maps + teach kind: target + spec count 44→45, rewrite brief §6.5 item 9, re-sync installer</name>
  <files>tests/test_known_bad_corpus.py, tests/test_frame_val.py, tests/test_causal_verb_golden.py, tests/test_dsx.py, brief.md</files>
  <read_first>
    - tests/test_known_bad_corpus.py (`_TARGET_DEFECT_CODES` :223-312 and the prescriptive-churn-recommendation precedent :294-298; `_EXPECTED_CAUGHT_DEFECTS` :487-624 and its prescriptive-churn-recommendation empty entry :519; `_CRITICAL_THRESHOLD_POINTS` :57; the closed-vocabulary `kind` assertion :1769-1774; the falsifiability docstring + non-miss branch :1829-1885; `_SECTION_65_BACKLOG_CODES` :929-934 + disjointness assertion :1723-1728; the ABSENT-partition guard :1958 and floor assertions :1980-1989/:2071/:2129/:2174; the ship-completeness test :1392-1437; the target-defect naming test :2446-2482)
    - tests/test_frame_val.py (`_EXPECTED_VAL_CODES` :1363-1483 — the magnitude/feature-origin `set()` precedents :1473/:1482; the key-parity guard test_discovered_fixture_set_equals_the_expected_dictionarys_key_set :1499-1517; val.check is the measurement)
    - tests/test_causal_verb_golden.py (`_GOLDEN_SHIP_FINDINGS` :82-326 — the full-relpath key convention and the magnitude entry :323-325 where the incidental is PRESENT but the attributing code is ABSENT; the key-parity test :381-394; the per-fixture re-measure equality :396-418; NOTE the subgroup entry INVERTS this — DSX-COH-041 is PRESENT because it fires CRITICAL at ship)
    - tests/test_dsx.py (spec count `self.assertEqual(len(paths), 44, ...)` :588)
    - brief.md (§6.5 item 9 row — the "subgroup-harm-declaration" / "prescriptive subgroup harm" row named by the comment at tests/test_known_bad_corpus.py:951; locate by section text, tolerating CRLF)
    - .planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-MEASUREMENT.md (the measured ship residual set to pin in the golden map + the swap record)
  </read_first>
  <action>
    Add exactly these keyed entries, all under the corpus basename `subgroup-harm-without-disposition`:
    in tests/test_known_bad_corpus.py add `_TARGET_DEFECT_CODES["subgroup-harm-without-disposition"] =
    {"plan": "DSX-COH-041", "verify": "DSX-COH-041", "ship": "DSX-COH-041"}` (single code per point, the
    bare-string shape chart-truncated-axis-bar uses; coherence is registered at plan/verify/ship and
    absent from execute, so this is where DSX-COH-041 fires) with an inline reason block matching house
    style and citing the prescriptive-churn-recommendation precedent; and add
    `_EXPECTED_CAUGHT_DEFECTS["subgroup-harm-without-disposition"] = frozenset()` (empty by design, not
    omission — this map's frozenset applies at both `_CRITICAL_THRESHOLD_POINTS` plan AND execute, but
    DSX-COH-041 does not fire at execute, so the catch lives in `_TARGET_DEFECT_CODES`; the key is
    required only for test_expected_caught_defects_keys_match_the_corpus_on_disk), with an inline reason
    block mirroring prescriptive-churn-recommendation's :519 entry. In tests/test_frame_val.py add
    `_EXPECTED_VAL_CODES["subgroup-harm-without-disposition-ANALYSIS-SPEC.yaml"] = set()` (MEASURED via
    val.check on the loaded fixture — the frame is a clean causal→prescriptive clone and DSX-COH-041 is a
    DSX-COH-* code, not DSX-VAL-*, so no DSX-VAL-* fires) with a measured comment mirroring the
    magnitude/feature-origin entries. In tests/test_causal_verb_golden.py add
    `_GOLDEN_SHIP_FINDINGS["examples/known-bad/subgroup-harm-without-disposition-ANALYSIS-SPEC.yaml"]` =
    the MEASURED CRITICAL/HIGH ship residual frozenset (re-measured live per 29-MEASUREMENT.md — DSX-COH-041
    is PRESENT because it fires CRITICAL at ship; this INVERTS the magnitude entry where the attributing
    code was ABSENT) with a measured comment. These three map values are MEASURED, not assumed — the val
    and golden tests re-run the live gate; run them and, if the measured golden set does NOT contain
    DSX-COH-041 or contains an undocumented incidental, STOP and record the discrepancy (do NOT silently
    overwrite — a golden set without DSX-COH-041 would mean the fixture stopped being a catch, contradicting
    the frozen D-29-00 design). Do NOT add DSX-COH-041 to `_SECTION_65_BACKLOG_CODES` (it ships to the
    catalogue in wave 1; the disjointness assertion at :1723-1728 forbids a shipped code there). Add a
    `_PER_FIXTURE_INCIDENTAL_CODES` entry ONLY if 29-MEASUREMENT.md's swap record documents an actual
    swap-invariant incidental firing on this fixture (with the D-28-06 guard set); the frozen design
    expects none — do not invent one.
    ── Teach the harness `kind: target` (the minimal parser change; see recorded_authoring_choices). In
    tests/test_known_bad_corpus.py extend the closed-vocabulary assertion in
    test_attribution_sidecars_reference_valid_codes_and_items (:1769-1774) from `("miss", "caught")` to
    `("miss", "caught", "target")` and update its message to document `target` as a caught §6.5-promotion
    (the code is PRESENT/fires and the sidecar records the backlog item it promotes). Update the
    falsifiability test's docstring (:1838-1849) and its non-miss branch comment (:1878) to name `target`
    alongside `caught`; the assertion itself — `assertIn(absent_code, all_critical)` at :1879-1885 — is
    UNCHANGED and is the exact enforcement that requires DSX-COH-041 to fire CRITICAL live for this
    target sidecar. Do NOT change the ABSENT-partition guard `!= "miss"` (:1958) — it already excludes a
    target sidecar from the miss partition, which is why `_ABSENT_PARTITION_FLOOR` stays 3. Do NOT edit
    `_ABSENT_PARTITION_FLOOR` (:993); the standing assertions (:1980-1989/:2071/:2129/:2174) verify it
    holds.
    Move the spec count in tests/test_dsx.py:588 from `len(paths), 44` to `len(paths), 45` (one added
    ANALYSIS-SPEC). Rewrite the brief.md §6.5 item 9 row (the subgroup-harm-declaration row) so its
    entry-condition column states the measured evidence: the corpus now contains the
    subgroup-harm-without-disposition case, measured a LIVE MISS at the gate points pre-mint (MET-030/031
    structurally silent at 1-of-4), and DSX-COH-041 was minted under D-05/D-06 to CATCH it — a real closed
    catch (PRESENT/DETECTED), NOT an attribution-only miss — with the Obermeyer et al. 2019 real-world case
    recorded at abstract + authoritative metadata grade and Gail & Simon (1985) as the motivating
    definition only (never the enforcement mechanic). Finally re-sync the installer overlay: run
    `node install.mjs` (syncs dsx/, references/, templates/, examples/) then `node install.mjs --check`.
    Do NOT edit dsx/checks/dq.py, references/finding-codes.md, scripts/gen-finding-catalogue.py, the three
    catalogue count-pin test files (wave-1 owns them), REQUIREMENTS.md, STATE.md, or ROADMAP.md. Use
    python312 for every test run.
  </action>
  <acceptance_criteria>
    - `python312 -m unittest tests.test_known_bad_corpus tests.test_frame_val tests.test_causal_verb_golden -q` reports OK (all three key-parity + per-fixture-equality tests pass with the new entries, and the sidecar-schema test admits `kind: target`).
    - The golden entry for the fixture equals the measured ship residual set from 29-MEASUREMENT.md and DSX-COH-041 IS among the fixture's measured ship findings (the falsifiability non-miss branch is green: DSX-COH-041 fires CRITICAL live).
    - `_TARGET_DEFECT_CODES["subgroup-harm-without-disposition"]` names DSX-COH-041 at plan/verify/ship; `_EXPECTED_CAUGHT_DEFECTS["subgroup-harm-without-disposition"]` is `frozenset()`; `_SECTION_65_BACKLOG_CODES` does not contain DSX-COH-041 (the disjointness assertion passes).
    - The closed-vocabulary assertion admits `("miss", "caught", "target")`; the ABSENT-partition guard `!= "miss"` and `_ABSENT_PARTITION_FLOOR = 3` are unchanged, and the floor assertions (:2071/:2129/:2174) pass (the target sidecar subtracts nothing from the miss partition).
    - `_EXPECTED_VAL_CODES["subgroup-harm-without-disposition-ANALYSIS-SPEC.yaml"]` is `set()` and its per-fixture equality test passes.
    - `python312 -m unittest tests.test_dsx -q` reports OK with the spec count at 45.
    - brief.md's §6.5 item 9 row contains the strings `LIVE MISS` (or "live miss") and `DSX-COH-041`.
    - Full suite green: `python312 -m unittest discover -s tests -q` reports OK; `python312 scripts/gen-finding-catalogue.py --check` exits 0 (catalogue at 279 from wave 1, untouched here).
    - `node install.mjs` completes and `node install.mjs --check` exits 0 (overlays re-synced).
    - `git diff --stat -- dsx/checks/dq.py` prints nothing; `git status --porcelain .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md references/finding-codes.md scripts/gen-finding-catalogue.py tests/test_finding_catalogue_invariant.py tests/test_phase20_zero_mint_close.py tests/test_p19_categorical_rows.py` prints nothing (wave-1 and single-writer files untouched).
  </acceptance_criteria>
  <verify>
    <automated>python312 -m unittest discover -s tests -q && python312 scripts/gen-finding-catalogue.py --check && node install.mjs --check</automated>
  </verify>
  <done>The target/expected/val/golden harness maps and the spec count are consistent, `kind: target` is taught and the sidecar passes schema + falsifiability, the golden ship set is re-measured with DSX-COH-041 PRESENT, `_ABSENT_PARTITION_FLOOR` holds at 3, the brief row states the measured evidence, and the installer overlay is re-synced and verified; dq.py, the wave-1 catalogue files, and the single-writer tracking files are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| promoted fixture → corpus harness | A fixture that silently gains a decision.subgroup_harm[] disposition row silences DSX-COH-041 and converts the CATCH into a non-catch, defeating the requirement's whole point (the falsifiability non-miss branch would fail, and the golden set would lose DSX-COH-041). |
| ATTRIBUTION sidecar → kind vocabulary + §6.5 backlog id set | `kind: target` must be taught to the closed-vocabulary assertion or the sidecar fails schema; an id that does not match the frozen `_SECTION_65_ITEM_IDS` string fails the sidecar gate; a shipped absent_code placed in the backlog set fails the disjointness gate. |
| DSX-COH-041 map placement → critical-threshold test | Placing DSX-COH-041 in `_EXPECTED_CAUGHT_DEFECTS` (a plan+execute map) instead of `_TARGET_DEFECT_CODES` (point-scoped) demands it fire at execute, where coherence does not run, failing the corpus's positive gate guarantee. |
| source tree → installed overlay | An un-synced overlay means the shipped package disagrees with the repo; `install --check` is the boundary check. |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-08 | Tampering | promoted ANALYSIS-SPEC | high | mitigate | The fixture MUST NOT gain a decision.subgroup_harm[] row; the falsifiability non-miss branch (:1878-1885) runs the live gate and requires DSX-COH-041 fire CRITICAL, and the golden-set test re-measures the ship set — either flips red if a disposition row is added (Task 1/Task 3). |
| T-29-09 | Spoofing | ATTRIBUTION promotes_backlog_item | high | mitigate | Use the harness-frozen id `6.5-item-9-subgroup-harm-declaration` (:951); the sidecar membership gate fails on any other string (Task 2). |
| T-29-10 | Tampering | kind vocabulary + placement | high | mitigate | Extend the closed vocabulary to `("miss", "caught", "target")` (:1771) and wire DSX-COH-041 in `_TARGET_DEFECT_CODES` with `_EXPECTED_CAUGHT_DEFECTS[slug]=frozenset()`, mirroring prescriptive-churn-recommendation; the schema and critical-threshold tests fail on a wrong kind or a mis-placed code (Task 2/Task 3). |
| T-29-11 | Tampering | spec-count pin | high | mitigate | Move tests/test_dsx.py:588 44→45 exactly when the fixture lands; the count assert fails on drift (Task 3). |
| T-29-12 | Tampering | golden ship set (mis-pinned) | high | mitigate | Re-measure via the live per-fixture golden test rather than pinning a guessed set; a set missing DSX-COH-041 (fixture stopped being a catch) or carrying an undocumented incidental fails the equality re-measure and halts the task (Task 3). |
| T-29-14 | Tampering | ABSENT-partition floor | high | mitigate | Do not edit `_ABSENT_PARTITION_FLOOR`; the `kind: target` sidecar is excluded from the miss partition by `!= "miss"` (:1958), so the floor stays 3 and the standing assertions (:2071/:2129/:2174) verify it (Task 3). |
| T-29-13 | Information disclosure | installed overlay drift | medium | mitigate | Re-run `node install.mjs` then `node install.mjs --check` after all synced-dir edits (Task 3). |
| T-29-06 | Tampering | dsx/checks/dq.py + wave-1 catalogue files byte-freeze | medium | mitigate | This plan edits no dsx/ source and none of wave-1's catalogue files; `git diff --stat -- dsx/checks/dq.py` and `git status --porcelain` over the wave-1 files must be empty (Task 3). |
| T-29-SC | Supply chain | package installs | low | accept | No npm/pip/cargo installs. The promoted entrypoint is read as text by the code scan and never executed; no Package Legitimacy Gate applies. |
</threat_model>

<verification>
- `python312 -m unittest tests.test_known_bad_corpus tests.test_frame_val tests.test_causal_verb_golden tests.test_dsx -q` — OK with all new entries and the kind: target vocabulary.
- `python312 -m unittest discover -s tests -q` — full suite OK (spec count 45; catalogue 279 from wave 1).
- `python312 scripts/gen-finding-catalogue.py --check` — exit 0.
- `node install.mjs --check` — exit 0 (overlays re-synced).
- `git diff --stat -- dsx/checks/dq.py` empty — byte-freeze held; wave-1 catalogue files and single-writer tracking files untouched.
</verification>

<success_criteria>
- The four `examples/known-bad/subgroup-harm-without-disposition-*` files exist, validate, and are a TARGET (DSX-COH-041 fires CRITICAL at plan/verify/ship; no disposition row) (REQ-P29-01).
- The POSTMORTEM reproduces the four/five-point measured table and names DSX-COH-041; the ATTRIBUTION sidecar is the corpus's first `kind: target`, falsifiable against the live gate with the frozen backlog id, and records the Obermeyer + Gail & Simon sources at their honest grade (REQ-P29-01/02).
- The target/expected/val/golden harness maps + the spec count (45) are consistent and green; DSX-COH-041 stays out of the backlog set; `_ABSENT_PARTITION_FLOOR` holds at 3 (REQ-P29-03).
- brief.md §6.5 item 9 states the measured evidence (LIVE MISS pre-mint; DSX-COH-041 minted to catch; Obermeyer real-world case) (REQ-P29-02/03).
- The installer overlay is re-synced and `node install.mjs --check` passes.
</success_criteria>

<output>
Create `.planning/phases/29-evidence-case-subgroup-harm-prescriptive/29-02-SUMMARY.md` when done.
</output>
