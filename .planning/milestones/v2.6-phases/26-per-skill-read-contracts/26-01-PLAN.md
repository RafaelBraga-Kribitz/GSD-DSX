---
phase: 26-per-skill-read-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/dsx-scope-analysis/SKILL.md
  - skills/dsx-define-metrics/SKILL.md
  - skills/dsx-design-experiment/SKILL.md
autonomous: true
requirements:
  - REQ-P26-01
tags:
  - skill-contracts
  - repo-integrity
  - eda
must_haves:
  truths:
    - "dsx-scope-analysis, dsx-define-metrics and dsx-design-experiment each open (immediately after </objective>, before the next existing block) with exactly one <inputs> block naming its EDA front-matter keys and DATA-PROFILE keys, per D-26-01/D-26-02."
    - "Each block names one backtick-quoted key per bullet line (D-26-01); when a skill names both a parent and a child key (scope-analysis and design-experiment both name grain.implied_dependence.structure and grain.implied_dependence.cluster_var) each is on its own bullet, never merged onto one line."
    - "Each of the three blocks' key regions contains at least one bullet key; a key region with zero bullet keys is invalid and would fail the Wave-2 guard."
    - "Each block's When absent line names the eda_artifact: none tier first, then the DATA-PROFILE fallback, then the computed_by declaration (D-26-03, mirroring capabilities/dsx/fragments/executor.md)."
    - "define-metrics carries an Also consult line for section 1 Joins containing zero backticks; scope-analysis and design-experiment omit the Also consult line (D-26-02 lists their prose refs as none)."
    - "Every backtick-quoted key written in these three blocks resolves verbatim against templates/EDA.md front-matter or templates/DATA-PROFILE.yaml (including uncommented example keys); zero new keys are invented."
  artifacts:
    - skills/dsx-scope-analysis/SKILL.md
    - skills/dsx-define-metrics/SKILL.md
    - skills/dsx-design-experiment/SKILL.md
  key_links:
    - "Each backtick-quoted key string must match templates/EDA.md front-matter or templates/DATA-PROFILE.yaml (uncommented) exactly; the Wave-2 guard tests/test_skill_read_contracts.py (26-03) is the enforcing link that fails on any drift."
    - "The When absent wording is the link to capabilities/dsx/fragments/executor.md's three-tier honesty ladder; it must not read a missing EDA as a stop_triggered clear."
---

<objective>
Add the D-26-01 `<inputs>` read-step block to the heads of three of the five downstream
skills — `dsx-scope-analysis`, `dsx-define-metrics`, `dsx-design-experiment` — each
naming the exact EDA front-matter and DATA-PROFILE keys it consumes (D-26-02), what each
key changes in the skill's output, and the `eda_artifact: none` absent-artifact fallback
(D-26-03). This delivers REQ-P26-01 for three of the five skills; plan 26-02 delivers the
other two.

Purpose: These skills currently open `<objective>` and jump straight into their process
block with zero read step (S0-2 premise 4, re-verified live in 26-RESEARCH.md) — so they
plan from assumption, not from measured evidence. The read step makes each skill start
from the profile numbers Phase 25 now computes.

Output: One `<inputs>` block inserted at each skill's head, byte-scoped to `skills/` only.

**Skill-only invariant (REQ-P26-03).** This plan edits only the three named `SKILL.md`
files. It MUST NOT touch anything under `dsx/`, MUST NOT edit `templates/EDA.md` or
`templates/DATA-PROFILE.yaml`, and MUST NOT mint any finding code. If a genuinely needed
fact has no existing template key, that is a scope escalation (a template would grow) —
stop and surface it, do not invent a key.

**Single-writer prohibition.** The executor MUST NOT edit `.planning/REQUIREMENTS.md`,
`.planning/STATE.md`, or `.planning/ROADMAP.md`. Those are orchestrator-only tracking
files; report status back instead.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-per-skill-read-contracts/26-CONTEXT.md
@.planning/phases/26-per-skill-read-contracts/26-RESEARCH.md
@capabilities/dsx/fragments/executor.md
@templates/EDA.md
@templates/DATA-PROFILE.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the &lt;inputs&gt; read step to dsx-scope-analysis</name>
  <files>skills/dsx-scope-analysis/SKILL.md</files>
  <read_first>
    - skills/dsx-scope-analysis/SKILL.md (insert point is between `</objective>` on line 19 and `<process>` on line 21)
    - .planning/phases/26-per-skill-read-contracts/26-CONTEXT.md (D-26-01 literal skeleton; D-26-02 mapping row for scope-analysis; D-26-03 exact scope-analysis fallback sentence, quoted verbatim there)
    - .planning/phases/26-per-skill-read-contracts/26-RESEARCH.md (Pattern 1 verified insertion point; Pattern 2 table confirming every scope-analysis key resolves live)
    - capabilities/dsx/fragments/executor.md (the three-tier honesty ladder the When absent line mirrors)
    - templates/EDA.md and templates/DATA-PROFILE.yaml (source of the verbatim key strings)
  </read_first>
  <action>
    Insert exactly one `<inputs>...</inputs>` block between `</objective>` and `<process>`,
    following the D-26-01 skeleton one-backtick-key-per-bullet shape. Open with the bold
    "Read `EDA.md` front-matter first when it exists" clause and a one-clause reason this
    skill scopes from measured evidence. Under "EDA front-matter keys read:" list one bullet
    each for `grain.verdict`, `grain.implied_dependence.structure`,
    `grain.implied_dependence.cluster_var`, `missingness[]`, `base_rate.verdict`,
    `contradictions`, `stop_triggered` (D-26-02). Under "DATA-PROFILE keys read (the fallback
    source when EDA is absent):" list one bullet each for `primary_key_unique`,
    `duplicate_rate`, `unit.rows_per_unit`, `unit.largest_unit_share`, `columns[].null_rate`,
    `target.verdict`. Write a "These set:" prose line covering validity-frame units,
    dependence and missingness, and the refusal to scope past `stop_triggered: true` until
    `contradictions` are resolved. OMIT the "Also consult" line (D-26-02 prose refs = none
    for this skill). Write the "When absent:" line verbatim from D-26-03's scope-analysis
    exemplar (records `eda_artifact: none`, sources facts from the DATA-PROFILE keys above,
    else declares each with `computed_by` honesty, and never reads a missing EDA as a
    `stop_triggered` clear). One backtick token per bullet line, nothing else on a key bullet.
    Do NOT reformat or renumber the existing `<process>` block.
  </action>
  <verify>
    <automated>python -c "import io,re; t=io.open('skills/dsx-scope-analysis/SKILL.md',encoding='utf-8').read(); m=re.search('<inputs>(.*?)</inputs>',t,re.S); assert m, 'no inputs block'; s=m.group(1); assert all(k in s for k in ['EDA front-matter keys read:','DATA-PROFILE keys read','eda_artifact: none','grain.verdict','stop_triggered','columns[].null_rate','target.verdict'])"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one `<inputs>...</inputs>` block exists, located between `</objective>` and `<process>`.
    - All seven EDA keys and all six DATA-PROFILE keys above appear, one backtick-quoted key per bullet line.
    - The `When absent:` line contains the literal `eda_artifact: none` and names the DATA-PROFILE fallback and `computed_by` tiers in that order.
    - No "Also consult" line is present.
    - `git diff --stat -- dsx/ templates/` shows no change (only `skills/dsx-scope-analysis/SKILL.md` changed).
  </acceptance_criteria>
  <done>dsx-scope-analysis opens with a grounded read step naming only live template keys and the eda_artifact: none fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Add the &lt;inputs&gt; read step to dsx-define-metrics</name>
  <files>skills/dsx-define-metrics/SKILL.md</files>
  <read_first>
    - skills/dsx-define-metrics/SKILL.md (insert point is between `</objective>` on line 18 and `<definition_contract>` on line 20)
    - .planning/phases/26-per-skill-read-contracts/26-CONTEXT.md (D-26-01 skeleton; D-26-02 mapping row for define-metrics, including the section 1 Joins Also-consult ref; D-26-03 three-tier fallback pattern)
    - .planning/phases/26-per-skill-read-contracts/26-RESEARCH.md (Pattern 2 table confirming grain.* leaves and columns[].n_unique/columns[].dtype resolve; the joins-is-prose-only confirmation at EDA.md lines 102-105)
    - capabilities/dsx/fragments/executor.md (three-tier ladder)
    - templates/EDA.md and templates/DATA-PROFILE.yaml
  </read_first>
  <action>
    Insert exactly one `<inputs>...</inputs>` block between `</objective>` and
    `<definition_contract>`, per the D-26-01 shape. Under "EDA front-matter keys read:" list
    one bullet each for `grain.declared`, `grain.observed`, `grain.verdict`,
    `grain.duplicate_rate` (D-26-02). Under "DATA-PROFILE keys read (the fallback source when
    EDA is absent):" list one bullet each for `primary_key`, `primary_key_unique`,
    `duplicate_rate`, `columns[].n_unique`, `columns[].dtype`. Write a "These set:" prose line
    covering the metric `grain`, the `denominator` (fan-out / double-count risk), and
    `computed_by`. INCLUDE an "Also consult (EDA prose, not front-matter):" line pointing to
    section 1 Joins — the join fan-out matrix informing the denominator — written as plain
    text with ZERO backticks (the guard asserts no backticks on this line; this is the
    mechanism that keeps the prose-only joins reference honest-but-unparsed, D-26-02). Author
    the "When absent:" line following D-26-03's three-tier ladder scoped to define-metrics'
    own facts: record `eda_artifact: none`, source grain / duplicate-rate / uniqueness facts
    from the DATA-PROFILE keys above, and where the profile is also absent declare each with
    `computed_by` honesty rather than asserting it. One backtick token per key bullet.
  </action>
  <verify>
    <automated>python -c "import io,re; t=io.open('skills/dsx-define-metrics/SKILL.md',encoding='utf-8').read(); m=re.search('<inputs>(.*?)</inputs>',t,re.S); assert m, 'no inputs block'; s=m.group(1); assert all(k in s for k in ['EDA front-matter keys read:','DATA-PROFILE keys read','eda_artifact: none','grain.declared','columns[].n_unique','Also consult']); ac=[l for l in s.splitlines() if l.strip().startswith('Also consult')][0]; assert '\`' not in ac, 'Also consult line must have zero backticks'"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one `<inputs>...</inputs>` block exists, located between `</objective>` and `<definition_contract>`.
    - The four `grain.*` EDA keys and the five DATA-PROFILE keys above appear, one backtick key per bullet.
    - An "Also consult" line names section 1 Joins with zero backtick characters on that line.
    - The `When absent:` line contains `eda_artifact: none` and names the DATA-PROFILE and `computed_by` tiers in order.
    - `git diff --stat -- dsx/ templates/` shows no change.
  </acceptance_criteria>
  <done>dsx-define-metrics opens with a grounded read step; the joins reference is prose-only with no backticks, so the guard never demands it as a key.</done>
</task>

<task type="auto">
  <name>Task 3: Add the &lt;inputs&gt; read step to dsx-design-experiment</name>
  <files>skills/dsx-design-experiment/SKILL.md</files>
  <read_first>
    - skills/dsx-design-experiment/SKILL.md (insert point is between `</objective>` on line 19 and `<design_mode>` on line 21)
    - .planning/phases/26-per-skill-read-contracts/26-CONTEXT.md (D-26-01 skeleton; D-26-02 mapping row for design-experiment; D-26-03 pattern)
    - .planning/phases/26-per-skill-read-contracts/26-RESEARCH.md (Pattern 2 table confirming dependence.* and target.*/unit.* keys resolve; target.*/unit.* are commented example keys, valid after uncomment)
    - capabilities/dsx/fragments/executor.md (three-tier ladder)
    - templates/EDA.md and templates/DATA-PROFILE.yaml
  </read_first>
  <action>
    Insert exactly one `<inputs>...</inputs>` block between `</objective>` and `<design_mode>`,
    per the D-26-01 shape. Under "EDA front-matter keys read:" list one bullet each for
    `dependence.icc`, `dependence.outcome_sd`, `dependence.weekly_cycle_amplitude`,
    `base_rate.overall`, `grain.implied_dependence.structure`,
    `grain.implied_dependence.cluster_var` (D-26-02). Under "DATA-PROFILE keys read (the
    fallback source when EDA is absent):" list one bullet each for `target.overall`,
    `target.weekly_range`, `unit.rows_per_unit`, `unit.largest_unit_share`. Write a "These
    set:" prose line covering `dsx power` inputs, `design.baseline_rate`, the
    `variance_adjustment` clustered from `cluster_var`, and whole-week run duration. OMIT the
    "Also consult" line (D-26-02 prose refs = none). Author the "When absent:" line per
    D-26-03's three-tier ladder scoped to design-experiment: record `eda_artifact: none`,
    source dependence / base-rate / target facts from the DATA-PROFILE keys above, and where
    the profile is also absent declare each with `computed_by` honesty rather than guessing a
    power input. One backtick token per key bullet.
  </action>
  <verify>
    <automated>python -c "import io,re; t=io.open('skills/dsx-design-experiment/SKILL.md',encoding='utf-8').read(); m=re.search('<inputs>(.*?)</inputs>',t,re.S); assert m, 'no inputs block'; s=m.group(1); assert all(k in s for k in ['EDA front-matter keys read:','DATA-PROFILE keys read','eda_artifact: none','dependence.icc','dependence.weekly_cycle_amplitude','target.overall','unit.rows_per_unit'])"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one `<inputs>...</inputs>` block exists, located between `</objective>` and `<design_mode>`.
    - The six EDA keys and four DATA-PROFILE keys above appear, one backtick key per bullet.
    - `dependence.weekly_cycle_amplitude` is written with its full dotted path (not bare `weekly_cycle_amplitude`).
    - The `When absent:` line contains `eda_artifact: none` and names the DATA-PROFILE and `computed_by` tiers in order.
    - No "Also consult" line is present. `git diff --stat -- dsx/ templates/` shows no change.
  </acceptance_criteria>
  <done>dsx-design-experiment opens with a grounded read step wiring the profile's dependence and target numbers into the power arithmetic.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates NO new files and NO new symbols — it adds one `<inputs>` documentation
block to the head of each of three existing skill files:

- `skills/dsx-scope-analysis/SKILL.md` — new `<inputs>` block (7 EDA keys, 6 DATA-PROFILE keys, `eda_artifact: none` fallback, no Also-consult line)
- `skills/dsx-define-metrics/SKILL.md` — new `<inputs>` block (4 EDA keys, 5 DATA-PROFILE keys, `eda_artifact: none` fallback, one Also-consult line for section 1 Joins with zero backticks)
- `skills/dsx-design-experiment/SKILL.md` — new `<inputs>` block (6 EDA keys, 4 DATA-PROFILE keys, `eda_artifact: none` fallback, no Also-consult line)

No `dsx/` symbols, no template keys, no finding codes are created. These `<inputs>` blocks
are the newly-created symbols the Wave-2 source-grounding drift pass should exclude.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template key set → skill read step | A skill names a key; if the key is not a real template key the read step silently orphans. This boundary is enforced by the Wave-2 guard, not at edit time. |
| skill prose → prose-only reference | The joins matrix (define-metrics) is EDA prose, not a front-matter key; naming it as a backtick key would create a false read contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-02 | Tampering | `<inputs>` blocks in the three SKILL.md files | high | mitigate | Every backtick key is copied verbatim from templates/EDA.md or templates/DATA-PROFILE.yaml (RESEARCH Pattern 2 table); the Wave-2 guard (26-03) fails loudly on any orphaned key naming skill+region+key. |
| T-26-06 | Tampering | define-metrics Also-consult line | medium | mitigate | The joins reference is written as zero-backtick prose; the guard asserts zero backticks on the Also-consult line, so it can never be mistaken for a machine-parsed key. |
| T-26-04 | Tampering | `dsx/`, `templates/`, finding catalogue | high | mitigate | This plan edits only three `skills/*/SKILL.md` files; each task's acceptance criteria assert `git diff --stat -- dsx/ templates/` is empty; zero codes minted. |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs in this plan (RESEARCH Package Legitimacy Audit = N/A); no `[ASSUMED]`/`[SUS]` packages exist to gate. |
</threat_model>

<verification>
- Per task: the python one-liner in each `<verify>` confirms the block structure, the
  representative keys, and (define-metrics) the zero-backtick Also-consult line.
- After this plan lands, the authoritative cross-template membership check is the Wave-2
  guard `python -m unittest tests.test_skill_read_contracts -v` (created in 26-03).
- Skill-only invariant: `git diff --stat -- dsx/ templates/` empty across all three edits.
</verification>

<success_criteria>
- All three skills carry exactly one `<inputs>` block at the correct insertion point.
- Every backtick key resolves against the live templates (proven authoritatively by 26-03).
- Each `When absent:` line names the `eda_artifact: none` → DATA-PROFILE → `computed_by` ladder.
- `dsx/` and `templates/` byte-unchanged by this plan.
</success_criteria>

<output>
Create `.planning/phases/26-per-skill-read-contracts/26-01-SUMMARY.md` when done.
</output>
