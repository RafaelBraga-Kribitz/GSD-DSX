---
phase: 26-per-skill-read-contracts
plan: 03
type: tdd
wave: 2
depends_on:
  - 26-01
  - 26-02
files_modified:
  - tests/test_skill_read_contracts.py
autonomous: true
requirements:
  - REQ-P26-02
tags:
  - repo-integrity
  - tdd
  - guard
must_haves:
  truths:
    - "tests/test_skill_read_contracts.py exists, imports nothing from dsx/, and is discovered by unittest discover -s tests."
    - "A fabricated key (grain.__orphan__ for EDA, columns[].__orphan__ for DATA-PROFILE) fed through the same matcher is rejected, and the parser did not invent it — so a renamed template key fails the suite instead of silently orphaning a read step (D-26-04 negative control, the load-bearing anti-silent-orphan proof for REQ-P26-02)."
    - "The guard records every intermediate dotted path (not only leaves), so columns[].categorical resolves as an intermediate node and full dotted-path matching keeps leaf collisions (overall under both base_rate and target; five distinct verdict leaves) from matching the wrong parent."
    - "The guard is anti-vacuous: len(parse_eda_keys) >= 30, grain.verdict is present, columns[].null_rate is present only after the uncomment step, and the DATA-PROFILE side carries an equivalent non-vacuity assertion."
    - "Line splitting uses \\r?\\n, so templates/EDA.md (CRLF) and templates/DATA-PROFILE.yaml (bare-LF) both parse to nonzero key counts; key comparison is exact-string dotted-path equality with no Unicode normalization and no case folding, so a re-cased or renamed key does not match."
    - "Set-membership is order-independent and deterministic: reordering the bullets within a region, or running the suite twice over unchanged inputs, yields the same verdict."
    - "Each of the five skills is asserted to carry exactly one <inputs> block with at least one key region, and checked == 5; the Also consult line, where present, is asserted to contain zero backticks."
  artifacts:
    - tests/test_skill_read_contracts.py
  key_links:
    - "The uncomment pre-processor (replace the first # with a single space, length-preserving, then strip trailing inline # comments) is the link that lets per-column / flag-gated example keys (unit.*, target.*, columns[].*) count as existing — without it, 8 of the ~30 named keys false-fail."
    - "The column_name -> [] normalization is the link between the profile template's single per-element placeholder token and the columns[].* notation the skills write."
---

<objective>
Create the off-gate-path repo-integrity guard `tests/test_skill_read_contracts.py`
(REQ-P26-02, D-26-04) that asserts every EDA front-matter key and every DATA-PROFILE key
named in any of the five skills' `<inputs>` blocks exists in the live templates — so a
renamed or fabricated key FAILS the suite instead of silently orphaning a read step. Built
test-first: write the failing test with its negative control, then implement the stdlib
key-path parser that makes it pass.

Purpose: Without this guard, a template rename would leave the five read steps pointing at
keys that no longer exist, and nothing would notice. The negative control is the
load-bearing part — it proves the assertion actually discriminates rather than passing
vacuously.

Output: One new test module under `tests/`, importing nothing from `dsx/` (off the gate
path by construction — `tests/` is never in `test_gate_path_hermetic`'s import closure).

**Skill-only invariant (REQ-P26-03).** This plan adds only `tests/test_skill_read_contracts.py`.
It MUST NOT touch anything under `dsx/`, MUST NOT edit the templates or the skills, and MUST
NOT mint any finding code. Do NOT `import yaml` — PyYAML is an optional dependency in this
repo's own pattern (`dsx/loader.py`), and comments are invisible to every YAML parser
anyway, so the stdlib line-oriented parser is required regardless (26-RESEARCH.md "Correction
to CONTEXT's premise").

**Single-writer prohibition.** The executor MUST NOT edit `.planning/REQUIREMENTS.md`,
`.planning/STATE.md`, or `.planning/ROADMAP.md`. Report status back to the orchestrator instead.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-per-skill-read-contracts/26-CONTEXT.md
@.planning/phases/26-per-skill-read-contracts/26-RESEARCH.md
@templates/EDA.md
@templates/DATA-PROFILE.yaml
@tests/test_phase14_onboarding.py
@tests/test_gate_path_hermetic.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write the failing guard test with negative control and anti-vacuity anchors</name>
  <files>tests/test_skill_read_contracts.py</files>
  <behavior>
    - Negative control: a fabricated key `grain.__orphan__` is NOT a member of the EDA key set, and `columns[].__orphan__` is NOT a member of the DATA-PROFILE key set — asserted through the SAME matcher the real checks use (proves the guard discriminates).
    - Anti-vacuity: `len(parse_eda_keys(...)) >= 30`; `grain.verdict` is present; `columns[].null_rate` is present only after the uncomment step; the DATA-PROFILE key set is also asserted non-empty (equivalent non-vacuity on both templates).
    - Per-skill presence: each of the five skills carries exactly one `<inputs>` block with at least one key region; `checked == 5`.
    - Per-skill / per-region membership: every backtick key each skill names under "EDA front-matter keys read:" is a member of `parse_eda_keys(templates/EDA.md)`, and every key under "DATA-PROFILE keys read" is a member of `parse_profile_keys(templates/DATA-PROFILE.yaml)`; a miss fails with a message naming skill + region + orphaned key ("was the template key renamed or removed?").
    - Fallback presence: each skill's `When absent:` line exists and contains the literal `eda_artifact: none`.
    - Also-consult honesty: where an `Also consult:` line is present (define-metrics, build-model), it contains zero backtick characters.
    - Determinism: two calls to `parse_eda_keys` over the same text return equal sets; membership does not depend on bullet order.
  </behavior>
  <read_first>
    - .planning/phases/26-per-skill-read-contracts/26-CONTEXT.md (D-26-04 full guard contract; D-26-01 parse contract for `<inputs>` regions and the one-key-per-bullet regex `^\s*-\s+`+backtick token; the per-skill key matrix in D-26-02)
    - .planning/phases/26-per-skill-read-contracts/26-RESEARCH.md (Code Examples: the guard skeleton and corrected module-docstring rationale; Pitfalls 1-3; the uncomment indentation math; the leaf-collision inventory)
    - tests/test_phase14_onboarding.py (style precedent: ROOT resolution via Path(__file__).resolve().parents[1], per-requirement test method naming, unittest structure)
    - tests/test_gate_path_hermetic.py (confirms tests/ is outside the gate import closure — why this guard is off the gate path)
    - templates/EDA.md and templates/DATA-PROFILE.yaml (the live key sets the guard parses)
  </read_first>
  <action>
    Create `tests/test_skill_read_contracts.py` as a stdlib-only `unittest` module (imports:
    `re`, `unittest`, `pathlib`; NO `import yaml`, NO import from `dsx/`). Write a module
    docstring stating the CORRECTED rationale from 26-RESEARCH.md (comments are invisible to
    every YAML parser; PyYAML is optional in this repo — NOT "won't load"), the CRLF discipline
    (EDA.md CRLF, DATA-PROFILE.yaml bare-LF, verified live; every split uses `\r?\n`), the
    `column_name` single per-element placeholder token note (residual #5), and the off-gate-path
    note. Define constants ROOT (`Path(__file__).resolve().parents[1]`), SKILLS, EDA_TEMPLATE,
    PROFILE_TEMPLATE, the FIVE_SKILLS tuple, and the D-26-02 per-skill expected-key mapping as a
    data structure the tests iterate. Declare the parser helpers `parse_eda_keys`,
    `parse_profile_keys`, `extract_inputs_block`, `extract_key_region`, and `_lines` as STUBS
    that raise NotImplementedError (this is the RED lever — the tests error until Task 2
    implements them). Write the `TestSkillReadContracts` test methods realizing every bullet in
    the behavior block above: `test_negative_control_orphan_key_is_rejected`,
    `test_anchor_non_vacuity`, `test_every_skill_carries_at_least_one_key_region`,
    `test_when_absent_names_eda_artifact_none`, `test_also_consult_line_has_zero_backticks`, a
    per-skill/per-region membership test (data-driven over the D-26-02 mapping), and
    `test_parse_is_deterministic_and_order_independent`. Do NOT implement the parser bodies yet.
    Run the suite and confirm it FAILS (RED). Commit: `test(26-03): failing skill read-contract
    guard with negative control (RED)`.
  </action>
  <verify>
    <automated>python -m unittest tests.test_skill_read_contracts -v; test $? -ne 0 && echo "RED confirmed (suite fails as expected)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_skill_read_contracts.py` exists; `grep -c "import yaml" tests/test_skill_read_contracts.py` returns 0 and no `from dsx` / `import dsx` line is present.
    - The module docstring states the corrected rationale (comments invisible to any parser; PyYAML optional) and the `\r?\n` CRLF discipline, and names the `column_name` placeholder token.
    - All the behavior-block test methods are defined, including `test_negative_control_orphan_key_is_rejected` and `test_anchor_non_vacuity`.
    - Running `python -m unittest tests.test_skill_read_contracts -v` exits NON-ZERO (RED) because the parser helpers are unimplemented.
    - `git diff --stat -- dsx/ templates/ skills/` shows no change.
  </acceptance_criteria>
  <done>The guard's behavior is fully specified as failing tests, with the negative control and anti-vacuity anchors present, before any parser is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement the stdlib key-path parser so the guard passes</name>
  <files>tests/test_skill_read_contracts.py</files>
  <behavior>
    - `parse_eda_keys(text)` returns the set of every dotted path (including intermediates, `[]` for list-of-map elements) in the EDA front-matter block: the lines strictly between the first two `---` fence lines, skipping `#` comment lines and stripping any inline `# ...`, splitting on `\r?\n`.
    - `parse_profile_keys(text)` first UNCOMMENTS every `#` example line (replace the FIRST `#` with a single space, length-preserving, so indentation is retained), THEN strips any remaining trailing inline `# ...`, THEN builds dotted paths with the same indent-stack logic, normalizing the placeholder `columns.column_name.*` to `columns[].*`.
    - `extract_inputs_block(skill_text)` returns the single `<inputs>...</inputs>` body (DOTALL, `\r?\n` tolerant); `extract_key_region(inputs_text, header)` returns the contiguous run of one-key-per-bullet lines following a region header, each matching `^\s*-\s+` + one backtick token + end.
    - After implementation, the full behavior of Task 1 holds: negative control rejects the orphans, anti-vacuity passes (>= 30 EDA keys, columns[].null_rate present after uncomment), every D-26-02 key resolves, checked == 5.
  </behavior>
  <read_first>
    - tests/test_skill_read_contracts.py (the failing test written in Task 1 — the contract to satisfy)
    - .planning/phases/26-per-skill-read-contracts/26-RESEARCH.md (the traced uncomment indentation math: columns(0) > column_name(4) > null_rate(6); Pitfall 1 trailing-inline-comment handling; Pitfall 2 mixed line endings; the leaf-collision inventory proving full dotted-path matching is necessary)
    - .planning/phases/26-per-skill-read-contracts/26-CONTEXT.md (D-26-04 parser spec: indent-stack + inline-flow awareness, every intermediate path, leaf-only matching REJECTED)
    - templates/EDA.md and templates/DATA-PROFILE.yaml (validate the parser against the real bytes)
  </read_first>
  <action>
    Implement the five parser helpers stubbed in Task 1, following D-26-04 and the RESEARCH
    indentation math. Use an indent-stack that records EVERY intermediate dotted path (not just
    leaves) so `columns[].categorical` (an intermediate map header) and prefix keys like
    `grain.implied_dependence` resolve as distinct members; reject leaf-only matching. For
    DATA-PROFILE, apply the uncomment step (`raw.replace("#", " ", 1)`) BEFORE the
    trailing-inline-comment strip and BEFORE building paths, then normalize `column_name` to
    `[]`. Split every line-oriented operation on `\r?\n` (or `str.splitlines()`), never a bare
    `\n` or a `\r\n`-only pattern, so both templates parse. Key comparison is exact-string
    dotted-path equality — no normalization, no case folding. Keep the failure message naming
    skill + region + orphaned key. Run the suite and confirm it PASSES (GREEN). If a REFACTOR
    pass is warranted (extract a shared bullet-parser, tighten names), do it and re-run to
    stay green. Commit GREEN: `feat(26-03): implement stdlib key-path parser for skill
    read-contract guard (GREEN)`; commit any refactor separately as `refactor(26-03): ...`.
  </action>
  <verify>
    <automated>python -m unittest tests.test_skill_read_contracts -v</automated>
  </verify>
  <acceptance_criteria>
    - `python -m unittest tests.test_skill_read_contracts -v` exits 0 (all tests pass, GREEN).
    - `test_negative_control_orphan_key_is_rejected` passes against a genuinely populated key set (the orphans are absent from a >= 30-key EDA set and a nonempty DATA-PROFILE set).
    - `test_anchor_non_vacuity` passes: `columns[].null_rate` is a member only because of the uncomment step; `grain.verdict` present; `len(eda) >= 30`.
    - Every key in the D-26-02 mapping (all five skills, both regions) is a member of the matching live key set.
    - `grep -c "import yaml" tests/test_skill_read_contracts.py` returns 0; no `dsx` import present.
    - `git diff --stat -- dsx/ templates/ skills/` shows no change.
  </acceptance_criteria>
  <done>The guard is green: every named key resolves against the live templates, a fabricated/renamed key is proven to fail, and the parser recovers the commented example keys via the uncomment step.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates one new file:

- `tests/test_skill_read_contracts.py` — new off-gate-path `unittest` module. New symbols:
  - Class `TestSkillReadContracts`
  - Test methods: `test_negative_control_orphan_key_is_rejected`, `test_anchor_non_vacuity`, `test_every_skill_carries_at_least_one_key_region`, `test_when_absent_names_eda_artifact_none`, `test_also_consult_line_has_zero_backticks`, the per-skill/per-region membership test (data-driven over the D-26-02 mapping), `test_parse_is_deterministic_and_order_independent`
  - Parser helpers: `parse_eda_keys`, `parse_profile_keys`, `extract_inputs_block`, `extract_key_region`, `_lines`, `_read`
  - Module constants: `ROOT`, `SKILLS`, `EDA_TEMPLATE`, `PROFILE_TEMPLATE`, `FIVE_SKILLS`, the D-26-02 expected-key mapping structure

No new fixtures are needed (the test reads the templates and skills directly). No `dsx/`
symbols and no finding codes are created — these are the newly-created symbols the
source-grounding drift pass should exclude.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-committed files → static test | The guard's only input is the repo's own committed templates and skills, resolved via `Path(__file__).resolve().parents[1]` — no user-controlled path, no network, no untrusted data (same trust model as every existing `tests/` repo-integrity test). |
| line-ending convention → line split | EDA.md is CRLF, DATA-PROFILE.yaml is bare-LF; a wrong split silently yields an empty key set (vacuous pass). |
| commented example key → live key set | Per-column / flag-gated keys live behind `#`; without the uncomment step they are invisible and false-fail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Tampering | `parse_eda_keys` / `parse_profile_keys` line splitting | high | mitigate | Split on `\r?\n` so both CRLF and bare-LF templates parse; the anti-vacuity anchors (`len(eda) >= 30`, `columns[].null_rate` present after uncomment, DATA-PROFILE non-empty) fail immediately on a mis-split empty parse — a vacuous pass cannot slip through. |
| T-26-02 | Tampering | key-set membership matcher | high | mitigate | Full dotted-path set-membership fails loudly (naming skill + region + key) when a named key is absent; the negative control proves the matcher discriminates. |
| T-26-05 | Tampering | leaf-only collision (`overall` under base_rate and target; five `verdict` leaves) | medium | mitigate | Leaf-only matching is rejected; the parser records every intermediate dotted path and matches full paths, so a parent rename cannot be masked by a same-named leaf elsewhere. |
| T-26-04 | Tampering | `dsx/`, templates, finding catalogue | high | mitigate | This plan adds only `tests/test_skill_read_contracts.py`; acceptance criteria assert `git diff --stat -- dsx/ templates/ skills/` empty; zero codes minted; no `dsx` import (off gate path). |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs; no `import yaml` — no new third-party test dependency introduced (RESEARCH Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- RED gate (Task 1): `python -m unittest tests.test_skill_read_contracts -v` exits non-zero.
- GREEN gate (Task 2): the same command exits 0.
- No `import yaml`, no `dsx` import (grep proofs in acceptance criteria).
- Off gate path confirmed: `tests/` is never in `test_gate_path_hermetic`'s import closure.
- Skill-only invariant: `git diff --stat -- dsx/ templates/ skills/` empty.
</verification>

<success_criteria>
- The guard fails when a fabricated/renamed key is fed through the matcher (negative control).
- The guard is anti-vacuous on both templates (CRLF and bare-LF), with the uncomment step
  proven necessary by `columns[].null_rate` membership.
- Every D-26-02 key resolves; `checked == 5`; the Also-consult lines carry zero backticks.
- `dsx/` byte-unchanged; nothing imported from `dsx/`.
</success_criteria>

<output>
Create `.planning/phases/26-per-skill-read-contracts/26-03-SUMMARY.md` when done.
</output>
