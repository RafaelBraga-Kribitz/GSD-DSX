---
phase: 17-foundation-repairs-and-spec-vocabulary
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_time_to_event_fallthrough.py
autonomous: true
requirements: [REQ-P17-03, REQ-P17-04]
tags: [statistics, routing-table, regression-pin, disposition-table, code-ranges]

must_haves:
  truths:
    - "recommend_test('time_to_event', ...) always returns log_rank via the unconditional final fallthrough, for every n_groups and paired combination"
    - "no equality guard on the time_to_event outcome literal exists in recommend_test's source (reaching log_rank is a fallthrough, not a match)"
    - "the D-12a disposition table (all nine Phase 18/19 gates) and the D-06 DSX-STA range pre-allocation are recorded in committed 17-CONTEXT.md"
  artifacts:
    - tests/test_time_to_event_fallthrough.py
  key_links:
    - "test_time_to_event_fallthrough.py structural scan (inspect.getsource) <-> dsx/checks/stats.py recommend_test source shape — a future contributor adding an outcome-type branch that silently reroutes turns it red"
---

<objective>
Pin the two REQ-P17-04 behaviours that are pure guards (no production-code change) and confirm the two recorded-decision requirements (REQ-P17-03, REQ-P17-04's range half) are already discharged in committed 17-CONTEXT.md.

Purpose: recommend_test reaches log_rank for a time_to_event outcome only by unconditional fallthrough after all four explicit outcome branches — there is no time_to_event guard. A future author adding a new outcome-type branch could silently change that routing. A regression test pins it now, before Phase 18/19 add outcome-type rows. The D-12a disposition table and the D-06 range pre-allocation are documentation deliverables already committed (17-CONTEXT.md D-02 and D-03, commit 2a6b7c8); this plan verifies them by review, mints nothing, and edits no production code.

Output: One new regression test file. No edit to dsx/checks/stats.py, dsx/spec.py, or any reference/fixture — so this plan is a clean Wave-1 parallel to 17-01 (Boschloo) with zero shared writable file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-foundation-repairs-and-spec-vocabulary/17-CONTEXT.md
@.planning/phases/17-foundation-repairs-and-spec-vocabulary/17-RESEARCH.md
@.planning/phases/17-foundation-repairs-and-spec-vocabulary/17-VALIDATION.md
@dsx/checks/stats.py
@tests/test_no_shapiro_autoswitch.py
</context>

<artifacts_produced>
Symbols this plan creates (zero finding codes, zero production-code edits):

- `tests/test_time_to_event_fallthrough.py` — NEW test module (class `TimeToEventFallthroughTest`) with a behavioural leg (recommend_test routes every time_to_event call to log_rank) and a structural leg (inspect.getsource of recommend_test contains no equality guard comparing the outcome variable to the time_to_event literal). Modelled on the dual behavioural + source-scan idiom in tests/test_no_shapiro_autoswitch.py.

No code, no vocabulary, no fixture, no doc is created or widened. REQ-P17-03 and the range half of REQ-P17-04 are recorded decisions verified in place (17-CONTEXT.md), not new artifacts.
</artifacts_produced>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED then GREEN): Pin the time_to_event unconditional fallthrough</name>
  <read_first>
    - dsx/checks/stats.py lines 32-70 (recommend_test signature and the proportion branch) and lines 120-129 (the four explicit outcome branches ending in the unconditional final return _rec("log_rank", ...) at line 128-129 — confirm there is no if-guard on the time_to_event literal)
    - dsx/checks/stats.py line 15 (OUTCOME_TYPES — time_to_event is a member) and lines 47-50 (the ValueError raised for an outcome not in OUTCOME_TYPES)
    - tests/test_no_shapiro_autoswitch.py (the house dual-pattern template: a behavioural test plus a source-scan test using inspect.getsource, CRLF-safe, scoped and non-line-anchored)
    - 17-RESEARCH.md § "The exact time_to_event fallthrough target" and § Code Examples (the recommended test shape); 17-VALIDATION.md Per-Task Verification Map row REQ-P17-04 (fallthrough)
  </read_first>
  <files>tests/test_time_to_event_fallthrough.py</files>
  <behavior>
    - Behavioural leg: for n_groups in {1, 2, 3} and paired in {True, False}, recommend_test("time_to_event", n_groups, paired=paired) returns a recommendation whose test is log_rank.
    - Structural leg: inspect.getsource(recommend_test) contains NO equality comparison between the normalized outcome variable and the time_to_event string literal — i.e. log_rank is reached by unconditional fallthrough, not by an explicit branch. Use a non-line-anchored regex tolerant of single or double quotes and surrounding whitespace (CRLF-safe per repo rule). A future contributor who adds such a guard turns this leg red, forcing a deliberate update of the routing contract.
  </behavior>
  <action>Create tests/test_time_to_event_fallthrough.py as a stdlib unittest.TestCase module named TimeToEventFallthroughTest, following the dual behavioural-plus-source-scan idiom of tests/test_no_shapiro_autoswitch.py. Import stats from dsx.checks and use inspect.getsource(stats.recommend_test) for the structural leg. The behavioural leg iterates the n_groups and paired combinations named in the behavior block and asserts the returned test equals log_rank. The structural leg negative-matches the source against a whitespace-tolerant, quote-agnostic pattern that would match an equality guard comparing the outcome variable to the time-to-event outcome literal, asserting no such guard is present; include a failure message citing REQ-P17-04. Because the source already satisfies both legs (the guard genuinely does not exist and the fallthrough already returns log_rank), this test is GREEN on first run — it is a pin, not a red-first feature; that is the correct TDD shape for a regression guard over already-correct behaviour. Do NOT edit dsx/checks/stats.py.</action>
  <verify>
    <automated>python3 -m unittest tests.test_time_to_event_fallthrough -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_time_to_event_fallthrough.py exists, is discovered by unittest, and both legs are GREEN.
    - Structural-leg self-check: temporarily reasoning through it, inserting an explicit time_to_event equality guard into recommend_test would turn the structural leg red (state this in the SUMMARY as the pin's proof-of-teeth; do not actually leave such a guard in the tree).
    - dsx/checks/stats.py is byte-unchanged by this plan (git diff shows no production edit).
    - Module uses stdlib unittest only; no pytest constructs.
  </acceptance_criteria>
  <done>test_time_to_event_fallthrough.py exists with both legs green; recommend_test routes every time_to_event input to log_rank and no time_to_event guard exists in source; stats.py is unedited.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the recorded-decision requirements are discharged in committed 17-CONTEXT.md (REQ-P17-03, REQ-P17-04 range half)</name>
  <read_first>
    - .planning/phases/17-foundation-repairs-and-spec-vocabulary/17-CONTEXT.md D-02 (the D-12a paradigm-disposition table — must list all nine Phase 18/19 gate checks with a disposition each, and must explicitly disposition the observed-power ban's Bayesian sibling as D-13-deferred)
    - .planning/phases/17-foundation-repairs-and-spec-vocabulary/17-CONTEXT.md D-03 (the D-06 DSX-STA range pre-allocation table: decades 050-129 by theme, 130-139 reserve)
    - .planning/REQUIREMENTS.md rows REQ-P17-03 and REQ-P17-04; 17-VALIDATION.md Manual-Only Verifications table
  </read_first>
  <files>.planning/phases/17-foundation-repairs-and-spec-vocabulary/17-02-SUMMARY.md</files>
  <action>This is a documentation-review verification with a deterministic grep oracle, not a code change. Confirm from committed 17-CONTEXT.md that (a) D-02 records a disposition for every one of the nine Phase 18/19 gates (REQ-P18-03, REQ-P18-04, REQ-P19-01, REQ-P19-02, REQ-P19-04, REQ-P19-05, REQ-P19-06a, REQ-P19-06b, REQ-P19-07) and names the observed-power Bayesian sibling as D-13-deferred, satisfying REQ-P17-03; and (b) D-03 records the DSX-STA range pre-allocation decades 050-129 plus the 130-139 reserve, satisfying the range half of REQ-P17-04. Do NOT edit 17-CONTEXT.md, REQUIREMENTS.md, STATE.md, or ROADMAP.md (they are single-writer / orchestrator-only). Record the verification outcome (which gate rows and which range rows were found) in this plan's SUMMARY. This plan mints no code and touches no production file.</action>
  <verify>
    <automated>python3 -c "import pathlib,re; t=pathlib.Path('.planning/phases/17-foundation-repairs-and-spec-vocabulary/17-CONTEXT.md').read_text(encoding='utf-8'); c=' '.join(t.split()); gates=['REQ-P18-03','REQ-P18-04','REQ-P19-01','REQ-P19-02','REQ-P19-04','REQ-P19-05','REQ-P19-06a','REQ-P19-06b','REQ-P19-07']; missing=[g for g in gates if g not in c]; assert not missing, ('missing gate dispositions: '+str(missing)); assert 'D-13' in c and '050' in c and '130' in c, 'range/sibling note incomplete'; print('D-02 dispositions + D-03 ranges present')"</automated>
  </verify>
  <acceptance_criteria>
    - The grep oracle prints "D-02 dispositions + D-03 ranges present" and exits 0 (all nine gate IDs found in 17-CONTEXT.md; D-13 sibling marker and the 050 / 130 range anchors found).
    - SUMMARY records REQ-P17-03 and the REQ-P17-04 range half as verified-discharged against committed 17-CONTEXT.md (commit 2a6b7c8), not newly written.
    - No tracking file (REQUIREMENTS.md, STATE.md, ROADMAP.md) or 17-CONTEXT.md is edited by this plan.
  </acceptance_criteria>
  <done>The grep oracle exits 0, confirming all nine D-12a gate dispositions and the D-06 range anchors are present in committed 17-CONTEXT.md; SUMMARY records REQ-P17-03 and REQ-P17-04's range half as verified-discharged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyst-authored ANALYSIS-SPEC.yaml -> dsx loader/parser | Unchanged by this plan. This plan adds a test and reads planning docs; no new input, network, auth, or file-write surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-02-01 | Tampering | recommend_test routing — a future outcome-type branch silently rerouting time_to_event | low | mitigate | The structural leg of test_time_to_event_fallthrough.py negative-scans the source for an equality guard on the time_to_event literal; any future guard forces a deliberate, reviewed contract change (REQ-P17-04, feeding REQ-P20-03). |
| T-17-02-02 | Repudiation | a recorded decision (D-12a dispositions / D-06 ranges) silently absent, letting a later phase mint outside the reserved space | low | mitigate | Task 2's deterministic grep oracle asserts all nine gate dispositions and the range anchors are present in committed 17-CONTEXT.md before this phase closes. |
| T-17-02-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed (stdlib unittest + stdlib inspect/re only). No package legitimacy audit owed. |
</threat_model>

<verification>
- After Task 1 commit: `python3 -m unittest tests.test_time_to_event_fallthrough -v` (both legs green).
- After Task 2 commit: the inline grep oracle exits 0.
- Wave-1 merge gate (orchestrator): `python3 -m unittest discover -s tests -q` fully green from a clean tree.
- This plan shares NO writable file with 17-01 (17-01 writes dsx/checks/stats.py + its own test; this plan writes only tests/test_time_to_event_fallthrough.py), so the two run concurrently in Wave 1.
</verification>

<success_criteria>
- recommend_test routes every time_to_event input to log_rank, pinned behaviourally and structurally.
- No time_to_event equality guard exists in recommend_test source.
- REQ-P17-03 (D-12a disposition table) and REQ-P17-04's range half are verified discharged in committed 17-CONTEXT.md.
- Zero production-code edits; zero new finding codes.
</success_criteria>

<output>
Create `.planning/phases/17-foundation-repairs-and-spec-vocabulary/17-02-SUMMARY.md` when done.
</output>
