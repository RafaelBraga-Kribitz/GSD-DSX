---
phase: 07-validity-frame-checks-dsx-val
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - dsx/spec.py
  - dsx/mathx.py
  - tests/test_dsx.py
autonomous: true
requirements: [REQ-P7-01, REQ-P7-02, REQ-P7-04]

must_haves:
  truths:
    - "A dependence structure declared as clustered, repeated_measures, temporal, spatial or hierarchical has a named admissible method set, and every member of every set is a member of VARIANCE_ADJUSTMENTS (REQ-P7-04, D-04, M-09)."
    - "A dependence structure of none has no entry in the map at all, so a check reading the map cannot accidentally demand a method family for independent observations (REQ-P7-04)."
    - "A falsifier that is blank, an angle-bracket placeholder, or a refusal word is classified as non-discriminating (REQ-P7-01, D-05)."
    - "A falsifier carrying prose but neither a discriminating predicate nor a numeric token is classified as non-discriminating (REQ-P7-01, D-05)."
    - "The good fixture's falsifier at examples/good-ANALYSIS-SPEC.yaml:302 is classified as discriminating, and the string 'none identified' — the good fixture's selection_risk value — is NOT classified as a refusal (REQ-P7-01, D-08)."
    - "design_effect(29.8, 0.02) equals 1.576 to three decimal places, the Cochrane Handbook's own published worked example (REQ-P7-02, D-10)."
    - "is_blank() is unchanged, so placeholder text still counts as present for the sampling-frame and measurement checks (D-06)."
    - statement: "The temporal, spatial and hierarchical structure-to-method pairings are defensible against the primary literature, not only internally consistent."
      verification: backstop
    - statement: "The Kish section locator and the Gelman/Simpson/Betancourt typeset-version caveat are labelled unverified rather than invented."
      verification: backstop
  artifacts:
    - dsx/spec.py (DEPENDENCE_ADMISSIBLE_METHODS, FALSIFIER_DISCRIMINATORS, is_placeholder_or_refusal, falsifier_is_discriminating)
    - dsx/mathx.py (design_effect)
    - tests/test_dsx.py (new test methods for all of the above)
  key_links:
    - "DEPENDENCE_ADMISSIBLE_METHODS keys must stay in step with DEPENDENCE_STRUCTURES (dsx/spec.py:186-193) — a future structure added without a map entry must fail a test, not pass silently."
    - "Every DEPENDENCE_ADMISSIBLE_METHODS value member must stay inside VARIANCE_ADJUSTMENTS (dsx/spec.py:96) — M-09 forbids a parallel vocabulary."
    - "Neither new constant may enter _VOCABULARIES (dsx/spec.py:272) — they are lookup tables, not dsx vocab dumpable enumerations."
    - "design_effect() is imported by dsx/frame/val.py in plan 07-04; dsx/mathx.py is a permitted peer module under the D-03a boundary (tests/test_frame_boundary.py:35 denies only dsx.checks)."
  prohibitions:
    - statement: "No docstring or code comment written in this plan presents a project-defined convention as a published result."
      status: active
      verification: human
    - statement: "No citation locator is written that was not actually verified; an unconfirmed locator is labelled unverified rather than invented."
      status: active
      verification: human
    - statement: "No check trigger is narrowed and no severity lowered in order to make a fixture, the template or an existing test pass."
      status: active
      verification: human
    - statement: "The unpublished design-effect illustration that D-10 retires is not asserted, printed or reintroduced anywhere in this plan's output."
      status: active
      verification: human
---

<objective>
Land the three pieces of shared infrastructure the nine validity-frame checks read, before any
check exists to read them: the dependence structure to method-family map (D-04), the falsifier
word-list plus its placeholder and refusal detector (D-05), and the design-effect helper with
its published reference value (D-10, D-11).

Purpose: `dsx/frame/val.py` cannot import what does not exist. Every one of these three lands in
a module that `dsx/frame/` is allowed to import — `dsx/spec.py` and `dsx/mathx.py` — and none of
them emits a `DSX-VAL-*` finding code, so this plan is free of the build-plumbing atomicity
constraint that governs plan 07-03.

Output: two new public module constants and two new public helpers in `dsx/spec.py`, one new
pure function in `dsx/mathx.py`, and unit tests for all five.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-validity-frame-checks-dsx-val/07-CONTEXT.md
@.planning/phases/07-validity-frame-checks-dsx-val/07-RESEARCH.md
@.planning/phases/07-validity-frame-checks-dsx-val/07-PATTERNS.md
@.claude/CLAUDE.md
@.claude/skills/plain-language/SKILL.md
@.claude/skills/decision-format/SKILL.md
</context>

<shell_note>
This machine runs Windows with PowerShell as the default shell. Do not write bash-only syntax
into commands. The Python interpreter answers to `python3` here (verified: Python 3.14.6); if a
future shell does not have `python3` on PATH, use `python`. The `dsx` console script is NOT on
PATH — invoke the command-line interface as `python3 -m dsx.cli ...`. The repository checks out
CRLF, so any regular expression that matches a line start or line end must use `\r?\n`, never a
bare `\n`.
</shell_note>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the dependence structure to method-family map to dsx/spec.py</name>

  <files>dsx/spec.py, tests/test_dsx.py</files>

  <read_first>
    - `dsx/spec.py` lines 40-60 (`CAUSAL_VERBS` — the bare-module-constant-with-comment shape to copy)
    - `dsx/spec.py` line 96 (`VARIANCE_ADJUSTMENTS` — the four admissible method names, verbatim)
    - `dsx/spec.py` lines 186-193 (`DEPENDENCE_STRUCTURES` — the six structure names)
    - `dsx/spec.py` lines 265-290 (`_VOCABULARIES` and its deliberate-exclusion comment)
    - `dsx/spec.py` lines 714-728 (`_VALIDITY_FRAME_MEMBERSHIP` — confirms `dependence.method_family_required` already reuses `VARIANCE_ADJUSTMENTS`, M-09)
    - `.planning/phases/07-validity-frame-checks-dsx-val/07-PATTERNS.md` section "dsx/spec.py module constants"
    - `.planning/phases/07-validity-frame-checks-dsx-val/07-RESEARCH.md` section 8a (the structure-to-method recommendations and their citation confidence tiers)
  </read_first>

  <behavior>
    - Test 1: the map's key set equals `set(DEPENDENCE_STRUCTURES) - {"none"}` exactly — five keys.
    - Test 2: every method named in every value is a member of `VARIANCE_ADJUSTMENTS`.
    - Test 3: `"none"` is not a key, so a caller must handle the independent case before indexing.
    - Test 4: `cluster_robust` is admissible for `clustered`; `delta_method` is admissible for no structure at all.
    - Test 5: the map does not appear in `describe_vocabulary()` output — it is a lookup table, not a vocabulary.
  </behavior>

  <action>
    Add a public module constant named `DEPENDENCE_ADMISSIBLE_METHODS` to `dsx/spec.py`,
    positioned immediately after the `DEPENDENCE_STRUCTURES` dictionary so the map sits beside
    the vocabulary it keys on. Type it as a dictionary from structure name to a frozenset of
    method names.

    The five entries, exactly as follows, with every method name drawn verbatim from
    `VARIANCE_ADJUSTMENTS` (M-09 forbids inventing a parallel vocabulary): `clustered` admits
    `cluster_robust`, `bootstrap_cluster` and `mixed_effects`; `repeated_measures` admits
    `mixed_effects` and `cluster_robust`; `temporal` admits `cluster_robust`,
    `bootstrap_cluster` and `mixed_effects`; `spatial` admits `cluster_robust`,
    `bootstrap_cluster` and `mixed_effects`; `hierarchical` admits `mixed_effects` and
    `cluster_robust`. The structure `none` gets no entry — a declared independence is the
    nothing-to-validate case and the consuming check in plan 07-05 skips it before it indexes
    the map.

    `delta_method` appears in no entry. It addresses transformed-parameter variance, not
    correlated observations, so admitting it anywhere here would let a spec satisfy a
    dependence declaration with a method that does not address dependence.

    Write the comment block above the constant in the shape `CAUSAL_VERBS` uses at
    `dsx/spec.py:40-52` — a plain-prose explanation, then the citations. Carry these citations
    and these honesty labels:

    For `clustered` and `repeated_measures`, cite Cameron, A.C. and Miller, D.L. (2015), "A
    Practitioner's Guide to Cluster-Robust Inference", Journal of Human Resources 50(2):317-372.
    For `temporal` and `spatial`, cite the same Cameron and Miller (2015) paper, and state on
    its own line that the exact section locator inside that paper is UNVERIFIED — author, year,
    title, journal, volume, issue and page range were confirmed; the section number was not.
    For `hierarchical`, cite Gelman, A. and Hill, J. (2007), Data Analysis Using Regression and
    Multilevel/Hierarchical Models, Cambridge University Press, and state on its own line that
    the exact chapter locator is UNVERIFIED. Follow the precedent at `dsx/frame/paradigm.py:66-72`
    for how an unverified locator is written — flagged, never invented.

    Add one sentence to the same comment recording that Conley, T.G. (1999) was considered as a
    second source for the `spatial` pairing and deliberately NOT cited, because only
    training-knowledge attribution was available for it and this project does not ship a citation
    it has not confirmed.

    Extend the existing deliberate-exclusion comment above `_VOCABULARIES` (currently naming
    `SPEC_VERSION`, `CAUSAL_VERBS`, `REQUIRED_TOP_LEVEL` and `IMBALANCE_UNSAFE_METRICS`) so it
    also names `DEPENDENCE_ADMISSIBLE_METHODS`. Do NOT add the constant to `_VOCABULARIES` and do
    NOT add it to `_VALIDITY_FRAME_MEMBERSHIP` — it is a lookup table, and an operator reading a
    `dsx vocab` dump under time pressure should not have to skip past it.

    Add the five behaviour tests to `tests/test_dsx.py`. Put them in a new test class named
    `TestDependenceAdmissibleMethods`, placed next to the other `dsx.spec` vocabulary tests.
    Name at least one test method so that it contains the word `dependence`, because the phase's
    standing verification command filters on that word.
  </action>

  <verify>
    <automated>python3 -m unittest tests.test_dsx -v -k dependence</automated>
  </verify>

  <acceptance_criteria>
    - `python3 -c "from dsx.spec import DEPENDENCE_ADMISSIBLE_METHODS as m, DEPENDENCE_STRUCTURES as s; assert set(m) == set(s) - {'none'}, sorted(m)"` exits 0
    - `python3 -c "from dsx.spec import DEPENDENCE_ADMISSIBLE_METHODS as m, VARIANCE_ADJUSTMENTS as v; assert all(set(x) <= set(v) for x in m.values())"` exits 0
    - `python3 -c "from dsx.spec import DEPENDENCE_ADMISSIBLE_METHODS as m; assert all('delta_method' not in x for x in m.values())"` exits 0
    - `python3 -c "import dsx.spec as s; d=s.describe_vocabulary(); assert 'dependence_admissible_methods' not in d"` exits 0
    - `python3 -m unittest tests.test_dsx -v -k dependence` exits 0
    - `python3 -m unittest discover -s tests` exits 0 with the same test-count baseline plus the new tests (baseline measured 2026-08-12: 306 tests, OK, 2 skipped)
  </acceptance_criteria>

  <done>
    `DEPENDENCE_ADMISSIBLE_METHODS` exists in `dsx/spec.py` with exactly five keys, every value a
    subset of `VARIANCE_ADJUSTMENTS`, a citation comment carrying two explicit UNVERIFIED locator
    labels and the recorded non-citation of Conley (1999), and it is excluded from
    `_VOCABULARIES` both in code and in the exclusion comment.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the falsifier word list and its placeholder and refusal detector to dsx/spec.py</name>

  <files>dsx/spec.py, tests/test_dsx.py</files>

  <read_first>
    - `dsx/spec.py` lines 40-60 (`CAUSAL_VERBS` — the tuple-of-lower-case-substrings shape to copy)
    - `dsx/spec.py` lines 300-360 (`get`, `section`, `items`, `is_blank`, `normalize` — the helper block the two new helpers join)
    - `dsx/checks/claims.py` lines 340-375 (the numeric-token regular expression idiom to re-home; this file must NOT be imported, D-03a — copy the pattern, not the import)
    - `dsx/checks/narrative.py` lines 17-21 (proof that word-list adjudication is an established idiom here, not an invention; also not importable)
    - `.planning/phases/07-validity-frame-checks-dsx-val/07-CONTEXT.md` decision D-05 and the note beneath it recording which corpus values must pass and which must fail
    - `.planning/phases/07-validity-frame-checks-dsx-val/07-RESEARCH.md` section 4, the paragraph headed "is_blank() vs. placeholder detection" — the load-bearing asymmetry
    - `examples/good-ANALYSIS-SPEC.yaml` lines 296-345 (the falsifier that must pass and the `selection_risk` value that must not be read as a refusal)
    - `templates/ANALYSIS-SPEC.yaml` lines 283-340 (the placeholder falsifier that must fail)
  </read_first>

  <behavior>
    - Test 1: the good fixture's falsifier string, quoted verbatim from `examples/good-ANALYSIS-SPEC.yaml:302`, is discriminating.
    - Test 2: an empty string is not discriminating.
    - Test 3: the template's angle-bracket falsifier string, quoted verbatim from `templates/ANALYSIS-SPEC.yaml:288`, is not discriminating.
    - Test 4: each of `n/a`, `N/A`, `tbd`, `TBD`, `none`, `unknown` is not discriminating.
    - Test 5: a prose string carrying neither a discriminating predicate nor a number is not discriminating.
    - Test 6: a string whose only qualifying token is numeric — for example a percentage-point threshold — is discriminating.
    - Test 7: the string `none identified` is NOT classified as a refusal, because it is the good fixture's `selection_risk` value and a different check reads that field.
    - Test 8: a twenty-thousand-character input is classified in under one second, proving the matching has no catastrophic backtracking (threat T-7-03).
  </behavior>

  <action>
    Add a public module constant named `FALSIFIER_DISCRIMINATORS` to `dsx/spec.py`, positioned
    immediately after `CAUSAL_VERBS`, as a tuple of lower-case substrings. Membership is exactly:
    `includes zero`, `crosses`, `below`, `above`, `exceeds`, `does not exceed`, `falls below`,
    `fails to`, `greater than`, `less than`, and the two bare comparison symbols for
    less-than and greater-than. The first eight of those are fixed by D-05; the remainder are
    the planner's discretion under D-05's explicit grant, chosen to widen the accepted set
    rather than narrow it, because the accepted risk in D-05 runs toward false positives at the
    earliest and highest-friction gate.

    Add a private frozenset named `_FALSIFIER_REFUSALS` with exactly these members, all
    lower-case: `n/a`, `na`, `tbd`, `tba`, `none`, `unknown`, `not assessed`,
    `to be determined`. Matching against this set is whole-value equality after normalisation,
    never substring containment — that is what keeps `none identified` out of it.

    Add a private compiled regular expression named `_PLACEHOLDER_RE` matching a whole value
    that opens with a less-than symbol and closes with a greater-than symbol with no intervening
    greater-than symbol. Strip the value before matching. Do not use multiline mode and do not
    anchor on a line end, so the CRLF checkout cannot change the result.

    Add a private compiled regular expression named `_FALSIFIER_NUMBER_RE`, re-homed from the
    idiom at `dsx/checks/claims.py:340-375`. It matches a run of digits with an optional single
    decimal part and an optional trailing percent sign or the two-letter percentage-point
    abbreviation. Use bounded, non-nested quantifiers only — a nested quantifier here would
    expose the gate to a denial-of-service through catastrophic backtracking on adversarial free
    text (threat T-7-03).

    Add a public helper `is_placeholder_or_refusal(value)` returning a boolean. It returns True
    when `is_blank(value)` is True, or when the stripped value matches `_PLACEHOLDER_RE`, or when
    `normalize(value)` is a member of `_FALSIFIER_REFUSALS`. It returns False otherwise.

    Add a public helper `falsifier_is_discriminating(value)` returning a boolean. It returns
    False when `is_placeholder_or_refusal(value)` is True. Otherwise it returns True when the
    lower-cased value contains any member of `FALSIFIER_DISCRIMINATORS`, or when
    `_FALSIFIER_NUMBER_RE` finds a match in the value; and False otherwise.

    Do NOT change `is_blank()`. Its current behaviour — any non-empty string counts as present,
    including angle-bracket placeholder text — is load-bearing for the sampling-frame and
    measurement checks in plan 07-06, which must treat placeholder text as present so the
    template does not trip them. The placeholder detector is a second, separate helper layered
    beside `is_blank()`, never a replacement for it.

    Extend the deliberate-exclusion comment above `_VOCABULARIES` so it also names
    `FALSIFIER_DISCRIMINATORS`. Do not add it to `_VOCABULARIES`.

    Add the eight behaviour tests to `tests/test_dsx.py` in a new class named
    `TestFalsifierLexicon`. Name at least one test method so it contains the word `estimand`,
    because the phase's standing verification command for this requirement filters on that word.
    Quote the two fixture strings verbatim from the files rather than paraphrasing them — a
    paraphrase that passes while the real value fails is exactly the failure this test exists to
    prevent.
  </action>

  <verify>
    <automated>python3 -m unittest tests.test_dsx -v -k estimand</automated>
  </verify>

  <acceptance_criteria>
    - `python3 -c "from dsx.spec import falsifier_is_discriminating as f; assert f('95% CI on the activation uplift includes zero, or its lower bound sits below +1.0pp')"` exits 0
    - `python3 -c "from dsx.spec import falsifier_is_discriminating as f; assert not f('')  and not f('n/a') and not f('TBD') and not f('unknown')"` exits 0
    - `python3 -c "from dsx.spec import is_placeholder_or_refusal as p; assert p('<the observation that would prove this wrong>') and not p('none identified')"` exits 0
    - `python3 -c "from dsx.spec import falsifier_is_discriminating as f; assert not f('the result will look different than we expect')"` exits 0
    - `python3 -c "from dsx.spec import is_blank; assert not is_blank('<a placeholder>')"` exits 0 — proves `is_blank` was not changed
    - `python3 -m unittest tests.test_dsx -v -k estimand` exits 0
    - `python3 -m unittest discover -s tests` exits 0
  </acceptance_criteria>

  <done>
    `FALSIFIER_DISCRIMINATORS`, `is_placeholder_or_refusal` and `falsifier_is_discriminating`
    exist in `dsx/spec.py`; every one of the eight behaviours above is asserted by a test;
    `is_blank()` is byte-identical to its pre-task state; and the long-input test completes in
    under one second.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add design_effect() to dsx/mathx.py with the Cochrane published reference value</name>

  <files>dsx/mathx.py, tests/test_dsx.py</files>

  <read_first>
    - `dsx/mathx.py` lines 400-440 (`inflation_from_peeking()` — the pure-function placement and shape; note its docstring does NOT satisfy the D-05 regular expressions and is explicitly a Phase 9 open item, so copy its structure but not its docstring)
    - `dsx/frame/paradigm.py` lines 61-77 (the docstring that DOES satisfy the D-05 regular expressions — this is the shape to copy)
    - `dsx/frame/paradigm.py` lines 66-72 (the unverified-locator precedent)
    - `tests/test_dsx.py` lines 30-70 (`TestMath` — the reference-value assertion idiom)
    - `scripts/gen-finding-catalogue.py` lines 68-80 (the three exact regular expressions the docstring lines must match)
    - `.planning/phases/07-validity-frame-checks-dsx-val/07-CONTEXT.md` decisions D-10 and D-11, and the citation ledger row for code `020`
  </read_first>

  <behavior>
    - Test 1: `design_effect(29.8, 0.02)` equals 1.576 to three decimal places — the Cochrane Handbook's own worked example.
    - Test 2: `design_effect(1, 0.5)` equals 1.0 — a cluster of one inflates nothing.
    - Test 3: `design_effect(50, 0.0)` equals 1.0 — zero intraclass correlation inflates nothing.
    - Test 4: an average cluster size below one raises `ValueError`.
    - Test 5: an intraclass correlation outside the closed interval from zero to one raises `ValueError`.
  </behavior>

  <action>
    Add a public pure function `design_effect(m, icc)` to `dsx/mathx.py`, positioned next to
    `inflation_from_peeking()`. It takes the average cluster size and the intraclass correlation
    as floats and returns one plus the product of the average cluster size minus one and the
    intraclass correlation. It raises `ValueError` when the average cluster size is below one,
    and `ValueError` when the intraclass correlation lies outside the closed interval from zero
    to one.

    Give it a docstring whose first paragraph states in plain sentences what the design effect
    is: the factor by which the variance of an estimate is inflated when observations inside a
    cluster are correlated and the analysis is run at a level finer than the true dependence
    unit.

    The docstring must then carry two labelled lines, each matching the build script's regular
    expressions at `scripts/gen-finding-catalogue.py:68-80` exactly — a line beginning
    `Citation:` and a line beginning `Reference value:`, each with non-whitespace content after
    the colon.

    The `Citation:` line names Kish, L. (1965), Survey Sampling, page 258 for the design-effect
    definition and pages 161 to 162 for the intraclass correlation, and Higgins, Eldridge and Li
    (2024), Cochrane Handbook for Systematic Reviews of Interventions version 6.5, sections
    23.1.4 and 23.1.4.1. Add a following line stating plainly that a section number inside Kish
    for the formula itself is UNVERIFIED — the page numbers were confirmed, the section number
    was not. Do not invent one.

    The `Reference value:` line states the intraclass correlation of 0.02 and the average cluster
    size of 29.8 yielding 1.576, and names it as the Cochrane Handbook's own worked example.

    Do NOT write, assert or mention the older illustrative value that D-10 retires. It has no
    published source, its arithmetic being correct is not the same thing as it being published,
    and asserting it in a test is precisely the laundering the citation discipline exists to
    prevent. Plan 07-02 corrects the research file that still carries it.

    Add the five behaviour tests as new methods on the existing `TestMath` class in
    `tests/test_dsx.py`. Place a standalone comment line reading `# D-05: DSX-VAL-020`
    immediately above the reference-value test method, matching the marker convention at
    `tests/test_dsx.py:391`. That marker is what the build check counts when the `DSX-VAL-020`
    finding code ships in plan 07-04; it is harmless before then. Name at least one test method
    so it contains the phrase `design_effect`.

    Note for the record, in the summary rather than in the code: `design_effect()` never calls
    `report.add(...)`, so the build check does not mechanically police its docstring. The
    docstring is written to the same bar anyway, because the function is the discoverable home
    of the citation, and the check function that does get policed is written in plan 07-04.
  </action>

  <verify>
    <automated>python3 -m unittest tests.test_dsx -v -k design_effect</automated>
  </verify>

  <acceptance_criteria>
    - `python3 -c "from dsx.mathx import design_effect as d; assert abs(d(29.8, 0.02) - 1.576) < 0.0005"` exits 0
    - `python3 -c "from dsx.mathx import design_effect as d; assert d(1, 0.5) == 1.0 and d(50, 0.0) == 1.0"` exits 0
    - `python3 -c "from dsx.mathx import design_effect as d; import sys;\nfor a in ((0.5,0.1),(30,-0.1),(30,1.1)):\n try:\n  d(*a); sys.exit(1)\n except ValueError: pass"` exits 0
    - `python3 -c "import dsx.mathx as m, re; s=m.design_effect.__doc__; assert re.search(r'^\s*Citation:\s*\S', s, re.M) and re.search(r'^\s*Reference value:\s*\S', s, re.M)"` exits 0
    - `python3 -c "import dsx.mathx as m; assert 'UNVERIFIED' in m.design_effect.__doc__"` exits 0
    - `python3 -m unittest tests.test_dsx -v -k design_effect` exits 0
    - `python3 -m unittest discover -s tests` exits 0
    - `python3 scripts/gen-finding-catalogue.py --check` exits 0 and prints that the finding catalogue is current
  </acceptance_criteria>

  <done>
    `dsx.mathx.design_effect` exists, returns 1.576 for the Cochrane inputs, raises `ValueError`
    on both out-of-range arguments, carries a docstring with both labelled lines matching the
    build script's regular expressions plus an explicit UNVERIFIED label on the Kish section
    locator, and the `# D-05: DSX-VAL-020` marker is present in `tests/`.
  </done>
</task>

</tasks>

<threat_model>
Application Security Verification Standard level 1. Blocking severity: high. Built from the
traced threat surface in `07-RESEARCH.md` section "Security Domain", not from a generic template.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analysis-spec file to gate process | Free text a spec author controls reaches `falsifier_is_discriminating()` and the two regular expressions. This is the only untrusted input this plan adds a code path for. |
| planner and researcher prose to shipped docstring | A citation locator crosses from a planning document into a shipped source file where a reader will treat it as verified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7-01 | Denial of Service | `_FALSIFIER_NUMBER_RE` and `_PLACEHOLDER_RE` in `dsx/spec.py` | medium | mitigate | Bounded, non-nested quantifiers only; Task 2 Test 8 classifies a twenty-thousand-character input in under one second |
| T-7-02 | Denial of Service | `falsifier_is_discriminating()` and `is_placeholder_or_refusal()` given a non-string value | low | mitigate | Both helpers route through `is_blank()` and `normalize()`, which already accept any type; a non-string never reaches a string method unguarded |
| T-7-07 | Spoofing | `design_effect()` docstring and the `DEPENDENCE_ADMISSIBLE_METHODS` comment | medium | mitigate | Two explicit UNVERIFIED labels (Kish section, Cameron and Miller section, Gelman and Hill chapter) and a recorded non-citation of Conley (1999); acceptance criterion greps the docstring for the label |
| T-7-SC | Tampering | package-manager installs | low | accept | This phase adds zero external dependencies — `07-RESEARCH.md` section "Package Legitimacy Audit" states it and this plan installs nothing. No legitimacy checkpoint applies. |
</threat_model>

## Flagged assumptions (spec-less edge probe)

This phase has no SPEC file, so the deterministic edge probe ran as a fallback. It returned nine
applicable rows and classified every one of them `unclassified`, leaving all nine unresolved. An
`unclassified` row is never auto-resolved and never dropped, so each is surfaced explicitly.

Read the probe result honestly: classifying nine rows out of nine as unclassified is a
classification miss, not evidence that this phase has no edge cases. The probe's categories are
shaped for feature work — user-facing flows, data shapes, permission states — and this phase
writes gate checks that adjudicate other people's declarations. The real edge cases are concrete
and have been authored into `must_haves.truths` above from the requirement text directly.

Rows this plan owns:

| Probe row | Status | How it is handled here |
|---|---|---|
| REQ-P7-01 | unresolved, flagged | Falsifier classification edges — blank, angle-bracket placeholder, refusal token, prose-without-predicate, numeric-token-only — authored into `must_haves.truths` and covered by Task 2 tests 1 through 7 |
| REQ-P7-02 | unresolved, flagged | Design-effect boundary inputs — cluster size of one, zero intraclass correlation, out-of-range arguments — authored into Task 3 behaviour tests 2 through 5 |
| REQ-P7-04 | unresolved, flagged | A dependence structure with no admissible method-family member, and the `none` structure having no map entry at all, authored into `must_haves.truths` and Task 1 tests 1 through 3 |

No-silent-drop accounting for the phase: nine probe rows equals nine rows surfaced as flagged
assumptions across plans 07-01 through 07-06, with their concrete edge predicates authored into
`must_haves.truths`. This plan surfaces three of the nine; plans 07-03 through 07-06 surface the
remaining six.

## Artifacts this phase produces

Phase-wide list. This plan creates the rows marked 07-01.

| Artifact | Kind | Created by |
|---|---|---|
| `dsx/spec.py::DEPENDENCE_ADMISSIBLE_METHODS` | module constant | 07-01 |
| `dsx/spec.py::FALSIFIER_DISCRIMINATORS` | module constant | 07-01 |
| `dsx/spec.py::is_placeholder_or_refusal` | function | 07-01 |
| `dsx/spec.py::falsifier_is_discriminating` | function | 07-01 |
| `dsx/mathx.py::design_effect` | function | 07-01 |
| `tests/test_dsx.py::TestDependenceAdmissibleMethods` | test class | 07-01 |
| `tests/test_dsx.py::TestFalsifierLexicon` | test class | 07-01 |
| `dsx/frame/val.py` | module | 07-03 |
| `dsx/frame/val.py::check` | function | 07-03 |
| `dsx/frame/val.py::_check_estimand_completeness` | function | 07-03 |
| `dsx/frame/val.py::_check_estimand_falsifiability` | function | 07-03 |
| `dsx/frame/val.py::_check_unit_triad` | function | 07-04 |
| `dsx/frame/val.py::_check_unit_drift` | function | 07-04 |
| `dsx/frame/val.py::_check_dependence` | function | 07-05 |
| `dsx/frame/val.py::_check_identification` | function | 07-05 |
| `dsx/frame/val.py::_PARAMETER_SCALE_CONSTRAINT_SOURCES` | module constant | 07-05 |
| `dsx/frame/val.py::_check_sampling_frame` | function | 07-06 |
| `dsx/frame/val.py::_check_missingness` | function | 07-06 |
| `dsx/frame/val.py::_MISSINGNESS_METHOD_VALIDITY` | module constant | 07-06 |
| `dsx/frame/val.py::_check_measurement` | function | 07-06 |
| Finding codes `DSX-VAL-010` and `DSX-VAL-011` | finding codes | 07-03 |
| Finding codes `DSX-VAL-020` and `DSX-VAL-021` | finding codes | 07-04 |
| Finding codes `DSX-VAL-030`, `DSX-VAL-040`, `DSX-VAL-041` | finding codes | 07-05 |
| Finding codes `DSX-VAL-050`, `DSX-VAL-060`, `DSX-VAL-070` | finding codes | 07-06 |
| `dsx/cli.py` `CHECKS["val"]` entry and three gate-profile entries | registration | 07-03 |
| `tests/test_frame_val.py` | test module | 07-03 |
| `tests/test_frame_val.py::TestValEstimand` | test class | 07-03 |
| `tests/test_frame_val.py::TestValUnits` | test class | 07-04 |
| `tests/test_frame_val.py::TestValDependenceIdentification` | test class | 07-05 |
| `tests/test_frame_val.py::TestValSamplingMissingnessMeasurement` | test class | 07-06 |
| `tests/test_frame_val.py::TestValGateIntegration` | test class | 07-07 |
| `tests/test_frame_boundary.py::TestFrameParadigmReadBoundary` | test class | 07-03 |
| `examples/known-bad/weak-identification-mmm-ANALYSIS-SPEC.yaml` | fixture | 07-07 |
| `examples/known-bad/weak-identification-mmm-POSTMORTEM.md` | fixture post-mortem | 07-07 |
| `tests/test_known_bad_corpus.py::_EXPECTED_PLAN_BLOCKERS` | test constant | 07-07 |

Ten finding codes ship for nine requirements. The planning brief for this phase says "the nine
finding codes"; decision D-02's own table lists ten rows. Ten is the number to build. This is
stated rather than silently corrected.

<verification>
- `python3 -m unittest discover -s tests` exits 0 with no test weakened, skipped or deleted
- `python3 scripts/gen-finding-catalogue.py --check` exits 0
- No file under `dsx/frame/` is touched by this plan
- No file under `dsx/checks/` is touched by this plan
- `git diff --stat` names exactly `dsx/spec.py`, `dsx/mathx.py` and `tests/test_dsx.py`
</verification>

<success_criteria>
Five new public names exist and are importable: `DEPENDENCE_ADMISSIBLE_METHODS`,
`FALSIFIER_DISCRIMINATORS`, `is_placeholder_or_refusal`, `falsifier_is_discriminating` and
`design_effect`. Every behaviour listed in the three `<behavior>` blocks is asserted by a test.
The full suite is green and the finding catalogue is current.
</success_criteria>

<output>
Create `.planning/phases/07-validity-frame-checks-dsx-val/07-01-SUMMARY.md` when done.
</output>
