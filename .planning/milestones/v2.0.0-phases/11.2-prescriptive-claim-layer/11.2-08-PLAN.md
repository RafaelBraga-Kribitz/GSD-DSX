---
phase: 11.2-prescriptive-claim-layer
plan: 08
type: execute
wave: 4
depends_on: ["11.2-05", "11.2-06", "11.2-07"]
files_modified:
  - examples/known-bad/prescriptive-churn-recommendation-ANALYSIS-SPEC.yaml
  - examples/known-bad/prescriptive-churn-recommendation-POSTMORTEM.md
  - tests/test_known_bad_corpus.py
  - tests/test_causal_verb_golden.py
  - README.md
autonomous: true
requirements: [REQ-P11.2-03]
tags: [corpus-green, flagship-fixture, golden-file, negative-fixture, known-limits, closing-gate]

must_haves:
  truths:
    - "The flagship 'offer bundled incentives to reduce churn' fixture is committed to examples/known-bad/, typed with a prescriptive claim on none identification under a descriptive question, and blocks with its target defects at the correct gate points (ROADMAP Success Criterion 1, D-06)."
    - "A golden-file finding-set-equality test over every examples/** fixture asserts the causal-verb widening changed no existing finding set — additions are only those the plan explicitly expects (D-06)."
    - "A negative case ('sales increase in Q4', a noun) produces zero causal-verb findings (D-06)."
    - "The full known-bad corpus re-runs green; scripts/gen-finding-catalogue.py --check passes; the whole suite passes; scripts/check.sh reports all checks passed (D-06)."
    - "The README records the named limits: the verbless-recommendation hole (D-15) and the amendment-counter residual limits (D-12), extending the standing 'a frame that lies passes' limit."
  artifacts:
    - "examples/known-bad/prescriptive-churn-recommendation-ANALYSIS-SPEC.yaml + -POSTMORTEM.md with catch-attribution"
    - "tests/test_known_bad_corpus.py: the flagship's entry in the point-scoped target map with catch-attribution tags"
    - "tests/test_causal_verb_golden.py: per-fixture finding-set-equality plus the noun negative case"
    - "README.md: a Known limits entry for the verbless-recommendation hole and the amendment-counter residuals"
  key_links:
    - "dsx validate runs only the `spec` check (cli.py:245-247), so a full-shape clone with a prescriptive-claim defect passes validate while blocking at plan (DSX-COH-001/010) and verify/ship (DSX-CLM-011/020)"
    - "coherence is in the plan profile but NOT the execute profile (cli.py:114-118), so the flagship's coherence codes are point-scoped to plan; the fixture must exit 0 at execute — use _TARGET_DEFECT_CODES point-scoped, not _EXPECTED_CAUGHT_DEFECTS which contributes its whole set at both plan and execute"
    - "measure per gate point in a fresh tempfile.TemporaryDirectory (RESEARCH landmine f); never assert against the shared examples/known-bad/DECISIONS.jsonl"
  prohibitions:
    - statement: "No corpus verdict change is absorbed by editing _INCIDENTAL_GAP_CODES, _TARGET_DEFECT_CODES or _EXPECTED_CAUGHT_DEFECTS to hide an unexpected finding — every entry is measured and attributed; an unexpected incidental code is investigated, not allowlisted (D-06)."
      status: resolved
      verification: "the flagship's target entry names exactly the codes measured; _INCIDENTAL_GAP_CODES is unchanged except where a genuinely-incidental new code is documented with its cause"
    - statement: "The two live noun-collision sites (weak-identification-mmm; the good fixture's results.tests[].interpretation) stay exempt; the golden-file test proves their finding sets are unchanged (D-06)."
      status: resolved
      verification: "golden-file equality green for both fixtures"
    - statement: "No change to dsx/findings.py (Option C not implemented)."
      status: resolved
      verification: "dsx/findings.py absent from files_modified"
  flagged_assumptions:
    - statement: "The D-16 citations for every new/extended check across this phase require a human D-05 source read (HQ-3) before ship — the loop assembles the evidence pack, the human signs. This closing plan pins the code; it does not substitute for that read."
      probe: "REQ-P11.2-03"
      status: unresolved
    - statement: "The D-15 residual (a verbless recommendation typed descriptive, 'offer bundled incentives', caught by neither the identification check nor the verb widening) is a NAMED, DEFERRED limit — the README states it as a limit; closing it (an imperative lexicon) is a future phase."
      probe: "REQ-P11.2-03"
      status: unresolved
---

<objective>
Prove the phase's union of effects is exactly the expected union: commit the flagship "offer bundled
incentives to reduce churn" fixture, pin every existing fixture's finding set against the verb
widening, prove the noun negative case, re-run the whole corpus green, and record the named limits.

Purpose: this is the D-06 corpus-green gate — the last gating task, run after every lexicon/vocabulary/
check change, because its whole job is asserting the union of their effects is the expected union, not
a superset (a regression) or a subset (a landmine left unclosed). It also records what the phase does
NOT catch (the verbless-recommendation hole, D-15) and the amendment counter's residual limits (D-12).

Output: the flagship fixture + post-mortem + catch-attribution, a golden-file equality test, a noun
negative case, README known-limits, and a fully green corpus and suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11.2-prescriptive-claim-layer/11.2-CONTEXT.md
@.planning/phases/11.2-prescriptive-claim-layer/11.2-RESEARCH.md
@.planning/phases/11.2-prescriptive-claim-layer/11.2-PATTERNS.md
@.planning/phases/11.1-generated-pipeline-reality/11.1-08-SUMMARY.md
</context>

## Artifacts this phase produces

This plan produces: the flagship known-bad fixture and its post-mortem, its catch-attribution entry in
the corpus target map, `tests/test_causal_verb_golden.py` (per-fixture finding-set equality plus the
noun negative case), and the README Known-limits entries for the verbless-recommendation hole (D-15)
and the amendment-counter residuals (D-12). It mints no finding code.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the flagship fixture, its post-mortem, and its catch-attribution</name>
  <files>examples/known-bad/prescriptive-churn-recommendation-ANALYSIS-SPEC.yaml, examples/known-bad/prescriptive-churn-recommendation-POSTMORTEM.md, tests/test_known_bad_corpus.py</files>
  <read_first>
    - examples/known-bad/weak-identification-mmm-ANALYSIS-SPEC.yaml:1-30 (the header-comment convention: cite the requirement, name the encoded defect, name the expected gate point and exit code)
    - tests/test_known_bad_corpus.py:200-360 (_run_gate helper, _CRITICAL_THRESHOLD_POINTS, _TARGET_DEFECT_CODES point-scoped shape, _EXPECTED_CAUGHT_DEFECTS whole-set shape, _INCIDENTAL_GAP_CODES), :555-700 (the per-fixture and post-hoc-procedure-switch tests to mirror)
    - dsx/cli.py:113-128 (GATE_PROFILES — coherence at plan+verify+ship, NOT execute; claims at verify+ship), :245-247 (validate runs only spec)
    - 11.2-CONTEXT.md "Specific Ideas" (the flagship), D-06; 11.2-PATTERNS.md section 9
  </read_first>
  <behavior>
    - dsx validate on the flagship exits 0 (structurally valid — a full-shape clone with the defect).
    - dsx gate plan exits 1 with DSX-COH-001 (a prescriptive claim exceeds a descriptive question) and DSX-COH-010 (a causal verb in the decision rule under a descriptive question) among CRITICAL findings.
    - dsx gate execute exits 0 (coherence and claims are not in the execute profile; the clone is clean for ml/code/dq).
    - dsx gate verify and ship exit 1 with DSX-CLM-011 (prescriptive claim uses the causal verb 'reduce') and DSX-CLM-020 (prescriptive claim, no identification) among findings.
    - The post-mortem names each catch-attribution finding code.
  </behavior>
  <action>
    Create the flagship spec as a full-shape clone of the good fixture's structure (so it passes dsx
    validate), then encode the defect: question_type descriptive; one claim typed prescriptive with
    text "Offer bundled incentives to reduce churn" and NO identification (blank/none); a decision_rule
    containing "to reduce churn". Do NOT set design.kind experiment and do NOT set question_type
    prescriptive — that keeps DSX-COH-040 and DSX-PRE-040 out of scope so the fixture isolates the
    REQ-01/02/03 catches. Write the header-comment block per the convention. MEASURE the findings at
    each gate point in a fresh tempfile.TemporaryDirectory (never the shared DECISIONS.jsonl).
    Populate the POINT-SCOPED _TARGET_DEFECT_CODES for this slug: the plan key gets
    {DSX-COH-001, DSX-COH-010}; the verify and ship keys get the claims codes measured
    ({DSX-CLM-011, DSX-CLM-020}); NO execute entry (the fixture exits 0 there). Do NOT add these to
    _EXPECTED_CAUGHT_DEFECTS (that map contributes its whole set at plan AND execute, which coherence
    does not satisfy). Add catch-attribution tags per the Phase 11.1/12 convention. If any unexpected
    incidental CRITICAL/HIGH appears, fix the fixture shape — do not allowlist it. Write the paired
    -POSTMORTEM.md naming each catch-attribution code and the requirement it demonstrates.
  </action>
  <verify>
    <automated>python -c "import subprocess,sys; s='examples/known-bad/prescriptive-churn-recommendation-ANALYSIS-SPEC.yaml'; r=subprocess.run([sys.executable,'-m','dsx.cli','validate','--spec',s],capture_output=True); assert r.returncode==0,(r.returncode,(r.stderr or r.stdout).decode()[:2000]); print('flagship passes validate')"</automated>
    <automated>python -c "import subprocess,sys,tempfile; s='examples/known-bad/prescriptive-churn-recommendation-ANALYSIS-SPEC.yaml'; d=tempfile.mkdtemp(); r=subprocess.run([sys.executable,'-m','dsx.cli','gate','plan','--spec',s,'--phase-dir',d],capture_output=True); out=(r.stderr or r.stdout).decode(); assert r.returncode==1,(r.returncode,out[:2000]); assert 'DSX-COH-001' in out and 'DSX-COH-010' in out, out[:2000]; print('flagship blocks plan on COH-001+COH-010')"</automated>
    <automated>python -c "import subprocess,sys,tempfile; s='examples/known-bad/prescriptive-churn-recommendation-ANALYSIS-SPEC.yaml'; d=tempfile.mkdtemp(); r=subprocess.run([sys.executable,'-m','dsx.cli','gate','execute','--spec',s,'--phase-dir',d],capture_output=True); assert r.returncode==0,(r.returncode,(r.stderr or r.stdout).decode()[:2000]); print('flagship clears execute')"</automated>
    <automated>python -c "import subprocess,sys,tempfile; s='examples/known-bad/prescriptive-churn-recommendation-ANALYSIS-SPEC.yaml'; d=tempfile.mkdtemp(); r=subprocess.run([sys.executable,'-m','dsx.cli','gate','verify','--spec',s,'--phase-dir',d],capture_output=True); out=(r.stderr or r.stdout).decode(); assert r.returncode==1,(r.returncode,out[:2000]); assert 'DSX-CLM-020' in out and 'DSX-CLM-011' in out, out[:2000]; print('flagship blocks verify on CLM-011+CLM-020')"</automated>
    <automated>python -m unittest tests.test_known_bad_corpus -v</automated>
  </verify>
  <acceptance_criteria>
    The flagship passes validate, blocks plan on {DSX-COH-001, DSX-COH-010}, clears execute, blocks
    verify on {DSX-CLM-011, DSX-CLM-020}; the corpus suite is green with the flagship's point-scoped
    attribution; the post-mortem names each code.
  </acceptance_criteria>
  <done>The flagship is committed with a post-mortem and a point-scoped, measured catch-attribution entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: golden-file finding-set equality over examples/**, plus the noun negative case</name>
  <files>tests/test_causal_verb_golden.py</files>
  <read_first>
    - tests/test_known_bad_corpus.py:480-555 (the _run_gate / json-capture helper and the isolated-tempdir pattern to reuse)
    - dsx/checks/claims.py:104-141, dsx/checks/coherence.py:83-108 (the two causal-verb consumers)
    - 11.2-CONTEXT.md D-06 (golden-file equality on every examples/**; negative fixture; corpus green)
  </read_first>
  <behavior>
    - For every fixture under examples/** (good, bad, and every known-bad including the new flagship), the CRITICAL/HIGH finding set at ship is exactly a committed golden set — no fixture gained or lost a finding from the causal-verb widening except the flagship's expected additions.
    - The two live noun-collision sites are unchanged: weak-identification-mmm still fires exactly its prior set; the good fixture's 'activation increase' in results.tests[].interpretation produces no causal-verb finding.
    - Negative noun: a synthetic association/descriptive claim "sales increase in Q4" run through claims.check + coherence.check yields zero causal-verb findings (DSX-CLM-010/011, DSX-COH-010 all absent).
  </behavior>
  <action>
    Create tests/test_causal_verb_golden.py (stdlib unittest). Enumerate every examples/** fixture,
    run `dsx gate ship --json` for each in a fresh tempfile.TemporaryDirectory, collect the sorted
    CRITICAL/HIGH code set, and assert it equals a committed per-fixture golden mapping defined in the
    module (record the measured sets as the golden baseline — this pins the post-widening reality so
    any future lexicon change is caught). Add explicit assertions that weak-identification-mmm's and
    the good fixture's causal-verb finding sets are unchanged. Add the noun negative case as an inline
    synthetic spec (not a committed known-bad fixture — a known-bad fixture must block, and this one
    must not): assert claims.check and coherence.check emit no DSX-CLM-010/011 or DSX-COH-010 on
    "sales increase in Q4". Tolerate CRLF where any text is matched (`\r?\n`).
  </action>
  <verify>
    <automated>python -m unittest tests.test_causal_verb_golden -v</automated>
    <automated>python -m unittest discover -s tests</automated>
  </verify>
  <acceptance_criteria>
    Every examples/** fixture's finding set equals its golden baseline; the two noun-collision sites are
    unchanged; the noun negative case yields zero causal-verb findings; the full suite is green.
  </acceptance_criteria>
  <done>The golden-file equality and the noun negative case are pinned; the widening is provably verdict-neutral on every existing fixture.</done>
</task>

<task type="auto">
  <name>Task 3: README known limits, and the closing corpus/catalogue/suite gate</name>
  <files>README.md</files>
  <read_first>
    - README.md (the `## Known limits` section — the standing "a frame that lies passes" line is the honest register to match)
    - 11.2-CONTEXT.md D-12 (the four amendment-counter residuals), D-15 (the verbless-recommendation named limit)
    - .claude/CLAUDE.md (Windows/PowerShell shell; scripts/check.sh runs under Git Bash)
  </read_first>
  <action>
    Under README's `## Known limits`, add two entries in the honest register of the existing "a frame
    that lies passes" line: (1) the verbless-recommendation hole (D-15) — a recommendation typed
    descriptive with no causal verb ("offer bundled incentives", "the optimal action is X",
    "prioritise segment Y") is caught by neither the identification check nor the causal-verb widening;
    it is a named, deferred limit, and a future imperative lexicon would close it. (2) the
    amendment-counter residual limits (D-12) — DECISIONS.jsonl is a plain, unsigned, tolerant-read
    local file, so the counter is a committed-trail honesty signal, not a tamper-proof control; it is
    blind to everything outside {validity_frame, inference}; the identity-free floor re-opens the
    cross-spec false positive on a shared root; and the amendment reason is checkable for form, not
    truth. State each as a limit (what is NOT checked), never as something the gate catches. Do not
    imply enforcement the code does not provide.
  </action>
  <verify>
    <automated>python -c "import io; t=io.open('README.md',encoding='utf-8').read(); assert 'offer bundled incentives' in t.lower() or 'verbless' in t.lower(); assert 'amendment' in t.lower(); print('README records the D-15 and D-12 named limits')"</automated>
    <automated>python -m unittest tests.test_known_bad_corpus -v</automated>
    <automated>python scripts/gen-finding-catalogue.py --check</automated>
    <automated>python -m unittest discover -s tests</automated>
    <automated>bash scripts/check.sh</automated>
  </verify>
  <acceptance_criteria>
    README's Known limits names the verbless-recommendation hole and the amendment-counter residuals in
    the honest register; the known-bad corpus, the catalogue check, the full suite, and scripts/check.sh
    all report success ("all checks passed").
  </acceptance_criteria>
  <done>The named limits are recorded and the whole phase is green end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus expectation maps → measured reality | The catch-attribution and incidental maps encode what the corpus is expected to do; a maintainer could silently absorb a verdict change by editing them instead of the code, hiding a regression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.2-13 | Tampering | corpus expectation maps | medium | mitigate | A verdict change silently absorbed by editing _EXPECTED_CAUGHT_DEFECTS / _INCIDENTAL_GAP_CODES / _TARGET_DEFECT_CODES. Mitigation: the golden-file finding-set-equality test plus measured, attributed entries; an unexpected incidental code is investigated and its cause documented, never allowlisted to make the suite pass. |
| T-11.2-SC | Tampering | package installs | low | accept | No package-manager installs (brief D-01, stdlib-only); scripts/check.sh runs the committed, pinned suite. |
</threat_model>

<verification>
- Flagship: passes validate; blocks plan on {DSX-COH-001, DSX-COH-010}; clears execute; blocks verify on {DSX-CLM-011, DSX-CLM-020}.
- `python -m unittest tests.test_causal_verb_golden -v` green (per-fixture equality + noun negative case).
- `python -m unittest tests.test_known_bad_corpus -v`, `python scripts/gen-finding-catalogue.py --check`, `python -m unittest discover -s tests`, `bash scripts/check.sh` all green.
- README records the D-15 and D-12 named limits.
</verification>

<success_criteria>
The flagship blocks at the correct gate points; the widening is verdict-neutral on every existing
fixture; the noun negative case fires nothing; the named limits are recorded; the corpus, catalogue,
suite and check.sh are all green.
</success_criteria>

<output>
Create `.planning/phases/11.2-prescriptive-claim-layer/11.2-08-SUMMARY.md` when done.
</output>
