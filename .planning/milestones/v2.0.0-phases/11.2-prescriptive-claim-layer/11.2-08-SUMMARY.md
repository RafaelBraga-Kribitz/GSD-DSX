---
phase: 11.2-prescriptive-claim-layer
plan: 08
subsystem: testing
tags: [corpus-green, flagship-fixture, golden-file, negative-fixture, known-limits, closing-gate, prescriptive, coherence, claims]

# Dependency graph
requires:
  - phase: 11.2-05
    provides: prescriptive claim-layer checks (DSX-CLM-020/021 superset gate)
  - phase: 11.2-06
    provides: DSX-COH-040 revisit_when prescriptive claim-layer
  - phase: 11.2-07
    provides: dsx explain self-reported trust-boundary section
provides:
  - "Flagship known-bad fixture (prescriptive-churn-recommendation) + post-mortem with point-scoped catch-attribution"
  - "Golden-file finding-set-equality test over every examples/** fixture proving the causal-verb widening is verdict-neutral"
  - "Inline noun-homograph negative case proving 'sales increase in Q4' fires no causal-verb finding"
  - "README Known-limits entries for the verbless-recommendation hole (D-15) and the amendment-counter residuals (D-12)"
  - "Corpus, catalogue, full suite and scripts/check.sh all green — the D-06 corpus-green closing gate"
affects: [phase 12 corpus growth, future imperative-lexicon phase]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Golden-file (characterization) finding-set-equality pinning over the whole fixture corpus at dsx gate ship"
    - "Point-scoped catch-attribution for a fixture whose catches split across gate points by check-family registration"

key-files:
  created:
    - examples/known-bad/prescriptive-churn-recommendation-ANALYSIS-SPEC.yaml
    - examples/known-bad/prescriptive-churn-recommendation-POSTMORTEM.md
    - tests/test_causal_verb_golden.py
  modified:
    - tests/test_known_bad_corpus.py
    - README.md
    - tests/test_dsx.py
    - tests/test_frame_interference.py
    - tests/test_frame_val.py

key-decisions:
  - "The flagship stays question_type=descriptive + design.kind=observational so its four catches isolate REQ-01/02/03 and keep DSX-COH-040/DSX-PRE-040 out of scope."
  - "The flagship carries a grandfather DSX-PRE-020 suppression so its verify/ship gates run to completion against a fresh phase directory with no seeded plan header (prereg's content-lock would otherwise abort at exit 2)."
  - "The flagship's catch-attribution is point-scoped in _TARGET_DEFECT_CODES (plan -> COH-001/COH-010; verify/ship -> CLM-011/CLM-020) and given an empty _EXPECTED_CAUGHT_DEFECTS entry, because coherence codes fire at plan but not execute."
  - "The golden baseline is measured reality, recorded verbatim; no verdict change was absorbed into any allowlist (T-11.2-13)."

patterns-established:
  - "Golden-file corpus pinning: measure every fixture's ship CRITICAL/HIGH set and assert equality, so any future lexicon change is caught."
  - "A descriptive/observational known-bad fixture requires an entry in every cross-cutting corpus-expectation map, not only the corpus target maps."

requirements-completed: [REQ-P11.2-03]

coverage:
  - id: D1
    description: "Flagship prescriptive-churn fixture blocks at the correct gate points (validate 0; plan 1 on COH-001+COH-010; execute 0; verify/ship 1 on CLM-011+CLM-020)"
    requirement: "REQ-P11.2-03"
    verification:
      - kind: unit
        ref: "tests/test_known_bad_corpus.py::test_every_spec_blocks_only_on_its_target_defect_at_critical_threshold_points"
        status: pass
      - kind: automated
        ref: "python -m dsx.cli validate/gate {plan,execute,verify} --spec examples/known-bad/prescriptive-churn-recommendation-ANALYSIS-SPEC.yaml (fresh tempdir)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Causal-verb widening is verdict-neutral on every existing fixture; noun negative case fires nothing"
    requirement: "REQ-P11.2-03"
    verification:
      - kind: unit
        ref: "tests/test_causal_verb_golden.py (6 tests)"
        status: pass
    human_judgment: false
  - id: D3
    description: "README records the D-15 verbless-recommendation hole and the D-12 amendment-counter residuals as named limits"
    requirement: "REQ-P11.2-03"
    verification:
      - kind: manual_procedural
        ref: "README.md ## Known limits — two new subsections"
        status: pass
    human_judgment: true
    rationale: "Whether the limits are stated honestly (as what is NOT checked, never as enforcement) is an editorial judgment automation cannot fully verify."
  - id: D4
    description: "Corpus, catalogue, full suite and scripts/check.sh all green"
    requirement: "REQ-P11.2-03"
    verification:
      - kind: integration
        ref: "bash scripts/check.sh -> 'all checks passed'"
        status: pass
    human_judgment: false

# Metrics
duration: 55min
completed: 2026-08-26
status: complete
---

# Phase 11.2 Plan 08: D-06 Corpus-Green Closing Gate Summary

**Committed the flagship "offer bundled incentives to reduce churn" fixture that blocks a prescriptive claim smuggled under a descriptive question, pinned every fixture's finding set against the causal-verb widening with a golden-file equality test, proved the noun negative case, recorded the verbless-recommendation and amendment-counter limits, and closed the phase with a fully green corpus, catalogue, suite and check.sh.**

## Performance

- **Duration:** ~55 min
- **Completed:** 2026-08-26
- **Tasks:** 3 (2 TDD)
- **Files modified:** 8 (3 created, 5 modified)

## Accomplishments

- **Flagship fixture + post-mortem.** `prescriptive-churn-recommendation-ANALYSIS-SPEC.yaml` is a full-shape clone of the good fixture converted to `question_type: descriptive` / `design.kind: observational`, carrying one `prescriptive`-typed claim ("Offer bundled incentives to reduce churn") with no identification and a decision rule using the purpose-gated causal verb `reduce`. Measured behaviour: `dsx validate` exits 0; `dsx gate plan` exits 1 on **DSX-COH-001** and **DSX-COH-010**; `dsx gate execute` exits 0; `dsx gate verify`/`ship` exit 1 on **DSX-CLM-011** and **DSX-CLM-020**. The paired post-mortem names each catch-attribution code and the requirement it demonstrates.
- **Point-scoped catch-attribution.** Added `prescriptive-churn-recommendation` to `_TARGET_DEFECT_CODES` (plan → `{DSX-COH-001, DSX-COH-010}`, verify/ship → `{DSX-CLM-011, DSX-CLM-020}`, no execute entry) and to `_EXPECTED_CAUGHT_DEFECTS` as an empty `frozenset()` (key-parity only). No verdict change was absorbed into `_INCIDENTAL_GAP_CODES` — every ship-time incidental (`DSX-CLM-031`, `DSX-MET-040`, `DSX-NAR-001`, `DSX-REP-030`) was already documented.
- **Golden-file equality + noun negative case.** `tests/test_causal_verb_golden.py` measures every `examples/**` fixture's CRITICAL/HIGH set at `dsx gate ship` and asserts equality against a committed baseline, with explicit sub-assertions that the two live noun-collision sites (`weak-identification-mmm`; the good fixture's "activation increase") fire no causal-verb finding, plus an inline synthetic spec proving `claims.check` + `coherence.check` emit no `DSX-CLM-010/011` or `DSX-COH-010` on "sales increase in Q4".
- **README known limits.** Two new subsections under `## Known limits`: the verbless-recommendation hole (D-15, a named deferred limit an imperative lexicon would close) and the four amendment-counter residuals (D-12), each stated as what is *not* checked.
- **Closing gate green.** Corpus (30), golden (6), catalogue `--check` (exit 0), full suite (1139 OK), and `scripts/check.sh` ("all checks passed").

## Task Commits

| Task | Gate | Commit | Subject |
|------|------|--------|---------|
| 1 | RED   | 28472fd | test(11.2-08): add flagship prescriptive-churn fixture + postmortem |
| 1 | GREEN | c8d41bb | feat(11.2-08): map flagship catch-attribution in corpus target maps |
| 2 | fix   | 97d68a9 | fix(11.2-08): update three corpus-expectation maps for the descriptive flagship |
| 2 | test  | cdbcdfd | test(11.2-08): add causal-verb golden-file equality + noun negative case |
| 3 | docs  | af4e1e7 | docs(11.2-08): record the verbless-recommendation hole (D-15) and amendment-counter residuals (D-12) |

## Golden Baseline (measured 2026-08-26, dsx gate ship CRITICAL/HIGH)

| Fixture | Ship CRITICAL/HIGH set |
|---------|------------------------|
| bad-ANALYSIS-SPEC.yaml | 68 codes (unchanged; full defective spec) |
| good-ANALYSIS-SPEC.yaml | CLM-031, DQ-001, FIG-001, NAR-010 |
| bayesian-continuous-monitoring | CLM-031, COH-031, MET-040, NAR-001, PAR-011, REP-001, REP-030, STA-041, VAL-041 |
| frequentist-uncontrolled-continuous | CLM-031, COH-031, EXP-007, MET-040, NAR-001, PAR-010, REP-030 |
| full-frame-cleaning | CLM-031, CODE-020, CODE-021, CODE-030, COH-031, MET-040, ML-090, NAR-001 |
| interference-shared-budget | CLM-031, COH-031, INT-010, MET-040, NAR-001, REP-030 |
| post-hoc-procedure-switch | CLM-031, COH-031, MET-040, NAR-001, PRE-030, REP-030, STA-041 |
| **prescriptive-churn-recommendation** | **CLM-011, CLM-020, CLM-031, COH-001, COH-010, MET-040, NAR-001, REP-030** |
| triggering-dilution | CLM-031, COH-031, INT-030, MET-040, NAR-001, REP-030 |
| weak-identification-mmm | CLM-031, COH-031, INT-030, MET-040, NAR-001, REP-030, VAL-040 |

(`DSX-` prefix elided for brevity; recorded verbatim in `tests/test_causal_verb_golden.py::_GOLDEN_SHIP_FINDINGS`.)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Extended three cross-cutting corpus-expectation maps beyond `files_modified`.**
- **Found during:** Task 2 (`python -m unittest discover -s tests`).
- **Issue:** The plan's `files_modified` lists only the corpus target maps in `tests/test_known_bad_corpus.py`, but the flagship is the first *descriptive/observational* known-bad fixture, and three other cross-cutting corpus-completeness assertions hardcode "every fixture is causal": `tests/test_dsx.py::test_every_committed_spec_declares_a_valid_estimand_type` (fixture count 10), `tests/test_frame_interference.py::test_needs_causal_block_true_for_known_bad_and_canonical_fixtures`, and `tests/test_frame_val.py::TestValFixtureMatrix` (`_EXPECTED_VAL_CODES` key-parity). All three failed on the new fixture, which would leave the suite red and violate the plan's own non-negotiable closing gate.
- **Fix:** Minimal, measured edits — bumped the committed-spec count 10→11 (fixture declares a valid estimand.type); excluded the flagship from the `needs_causal_block`-True sweep by slug and asserted `False` for it instead; added the flagship to `_EXPECTED_VAL_CODES` with the *measured* empty set. No finding was allowlisted to hide a verdict (T-11.2-13); every entry was measured against the loaded fixture.
- **Files modified:** tests/test_dsx.py, tests/test_frame_interference.py, tests/test_frame_val.py.
- **Commit:** 97d68a9.

**2. [Rule 3 - Blocking] Flagship carries a grandfather DSX-PRE-020 suppression.**
- **Found during:** Task 1 (measuring the plan's verify `python -c` command).
- **Issue:** The plan's verify/ship measurement runs against a fresh `tempfile.mkdtemp()` with no seeded plan header. From Phase 10, `prereg`'s content-lock (`_check_content_lock`) raises `CheckError` (exit 2) when no plan-time header is recorded and no grandfather suppression exists — so the raw verify command would exit 2, not 1, breaking the plan's own assertion. Confirmed by measuring existing fixtures (both weak-identification-mmm and good exit 2 under a fresh tempdir).
- **Fix:** Added a single well-formed `suppressions[]` entry (`code: DSX-PRE-020`, non-blank reason + authority) to the flagship. It grandfathers only the prereg content-lock; it suppresses none of the four target findings, and with a seeded header (the corpus test) reconciliation runs normally and passes.
- **Files modified:** examples/known-bad/prescriptive-churn-recommendation-ANALYSIS-SPEC.yaml.
- **Commit:** 28472fd.

### TDD note

Task 2's golden-file test is a **characterization (pinning) test** over already-shipped behaviour — this plan mints no finding code and changes no `dsx/` source, so a classic RED-against-new-production-code phase does not apply. It was committed as a single `test(...)` commit (preceded by the `fix(...)` commit that made the pre-existing suite green). Task 1 followed a genuine RED (fixture on disk, corpus red on missing map entries) → GREEN (map entries added) cycle.

## Self-Check: PASSED

- examples/known-bad/prescriptive-churn-recommendation-ANALYSIS-SPEC.yaml — FOUND
- examples/known-bad/prescriptive-churn-recommendation-POSTMORTEM.md — FOUND
- tests/test_causal_verb_golden.py — FOUND
- Commits 28472fd, c8d41bb, 97d68a9, cdbcdfd, af4e1e7 — all present in git log
- No `dsx/` source file and no tracking file (STATE/ROADMAP/REQUIREMENTS/LOOP-LEDGER/HUMAN-QUEUE) touched — confirmed via `git diff --name-only`
