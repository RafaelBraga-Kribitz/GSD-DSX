---
phase: 11.2-prescriptive-claim-layer
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - dsx/spec.py
  - dsx/checks/coherence.py
  - tests/test_prescriptive_ladder.py
autonomous: true
requirements: [REQ-P11.2-01]
tags: [coherence, vocabulary, claim-type, ladder, precondition]

must_haves:
  truths:
    - "A claim typed `prescriptive` is recognised at rank 4 of the coherence ladder; under any weaker `question_type` it fires DSX-COH-001 CRITICAL (ROADMAP Success Criterion 1, D-01)."
    - "A claim typed `prescriptive` no longer trips DSX-SPEC-062 'unrecognised type', because `prescriptive` is a member of CLAIM_TYPES (D-02)."
    - "Both stale remedy strings at spec.py:840 (DSX-SPEC-061) and spec.py:849 (DSX-SPEC-062) name five claim types, so no message quotes a stale count once the member is added (D-02)."
    - "No existing fixture verdict changes: no committed fixture declares a `type: prescriptive` claim, so adding the member is verdict-neutral (D-02, RESEARCH landmine a)."
  artifacts:
    - "dsx/spec.py CLAIM_TYPES contains 'prescriptive'"
    - "dsx/checks/coherence.py CLAIM_STRENGTH contains 'prescriptive': 4"
    - "tests/test_prescriptive_ladder.py exists and asserts DSX-COH-001 fires and DSX-SPEC-062 does not misfire on a type: prescriptive claim"
  key_links:
    - "coherence.py:61-62 `if ctype not in CLAIM_STRENGTH: continue` is the silent-skip line; the fix is the dict row, not a logic change"
    - "coherence.py:23-29 QUESTION_STRENGTH already carries `prescriptive: 4` since Phase 1 — CLAIM_STRENGTH is the only side missing the row"
    - "spec.py:839/848 detail line `', '.join(sorted(CLAIM_TYPES))` self-updates; only the two hardcoded remedy strings are stale"
  prohibitions:
    - statement: "No new finding code is minted for REQ-01 — DSX-COH-001 and DSX-SPEC-061/062 are reused (D-01, one-fact-one-code)."
      status: resolved
      verification: "grep the diff for report.add(\"DSX- shows only DSX-COH-001/DSX-SPEC-061/DSX-SPEC-062 touched, no new code string"
    - statement: "No change to dsx/findings.py (Option C is not implemented this phase)."
      status: resolved
      verification: "dsx/findings.py absent from files_modified and from the diff"

  flagged_assumptions:
    - statement: "Verdict-neutrality rests on the current corpus containing no `type: prescriptive` claim. RESEARCH confirmed this on 2026-08-24; if a fixture is added before this plan executes, re-confirm."
      probe: "REQ-P11.2-01"
      status: unresolved
---

<assumption_delta_decision>
add-alongside: `prescriptive` is added as the fifth CLAIM_TYPES member and the fourth-rank
CLAIM_STRENGTH row. This is an ADDITIVE ladder extension, not a singular→plural change of a
primary identity key — the existing four members and every ranking below rank 4 are untouched,
and QUESTION_STRENGTH already carries the mirror row. No migration or key-shape change is implied.
</assumption_delta_decision>

<objective>
Add `prescriptive` to the claim-type vocabulary and the coherence ladder so a prescriptive claim is
held to the strength ceiling instead of being silently skipped — and correct the two remedy strings
that would otherwise quote a stale "four claim types" count. This is the D-02 ordering precondition:
nothing that reads a `type: prescriptive` claim may land before it.

Purpose: today a `type: prescriptive` claim hits `elif ctype not in CLAIM_TYPES` in
`_validate_claims_shape` (spec.py:843) and fires DSX-SPEC-062 HIGH "unrecognised type", and is then
silently skipped by `_check_claim_ceiling` at coherence.py:61-62 — a HIGH typo-looking warning with
ZERO CRITICAL ceiling finding. An operator who dismisses the HIGH walks away with an unbounded
prescriptive claim. This is the silent pass the gate exists to prevent (RESEARCH landmine a).

Output: `prescriptive` in CLAIM_TYPES and in CLAIM_STRENGTH at rank 4; both remedy strings updated to
five types; a test proving DSX-COH-001 fires and DSX-SPEC-062 no longer misfires.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.2-prescriptive-claim-layer/11.2-CONTEXT.md
@.planning/phases/11.2-prescriptive-claim-layer/11.2-RESEARCH.md
@.planning/phases/11.2-prescriptive-claim-layer/11.2-PATTERNS.md
</context>

## Artifacts this phase produces

This plan produces: the `prescriptive` member of `CLAIM_TYPES` (spec.py), the
`CLAIM_STRENGTH["prescriptive"] = 4` row (coherence.py), the two corrected remedy strings
(DSX-SPEC-061/062), and `tests/test_prescriptive_ladder.py`. It mints no finding code.

(Phase-wide new symbols, produced across sibling plans: widened `CAUSAL_VERBS` tiers; the
`causal_verb_matches` helper; `_check_revisit_completeness`; `revisit_when_is_discriminating`;
`DSX-COH-040`; `DSX-PRE-040`; `DSX-PRE-041`; the top-level `spec_id` field; the `AmendmentRecord`
dataclass; the `dsx explain` self-reported section; the flagship known-bad fixture; new test modules.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pin the prescriptive ladder and the five-type remedy</name>
  <files>tests/test_prescriptive_ladder.py</files>
  <read_first>
    - dsx/checks/coherence.py:20-80 (QUESTION_STRENGTH:23-29 already has prescriptive:4; CLAIM_STRENGTH:31-36 missing it; _check_claim_ceiling:55-80, skip line :61-62)
    - dsx/spec.py:824-853 (_validate_claims_shape; remedy strings :840 and :849; detail line uses sorted(CLAIM_TYPES))
    - 11.2-PATTERNS.md section 1 (the analog and exact current wording to mirror)
    - An existing coherence unit test in tests/test_dsx.py (search for "DSX-COH-001") for the spec-dict construction idiom to mirror
  </read_first>
  <behavior>
    - Test 1: a spec with question_type "descriptive" (strength 0) and one claim typed "prescriptive" (strength 4), run through dsx.checks.coherence.check, emits exactly one DSX-COH-001 at CRITICAL. RED today because CLAIM_STRENGTH lacks the row (claim is skipped, no finding).
    - Test 2: a spec with a claim typed "prescriptive", run through dsx.spec structural validation (the _validate_claims_shape path), emits NO DSX-SPEC-062. RED today because prescriptive is not in CLAIM_TYPES.
    - Test 3 (edge): question_type "prescriptive" (strength 4) with a claim typed "prescriptive" — DSX-COH-001 does NOT fire (claim strength equals question strength, not exceeds).
    - Test 4 (remedy text): the DSX-SPEC-061 remedy string and the DSX-SPEC-062 remedy string each name five claim types and neither contains the substring "four claim types" nor ends "predictive or causal".
  </behavior>
  <action>
    Create tests/test_prescriptive_ladder.py as a stdlib unittest.TestCase module (mirror the
    construction idiom of the existing DSX-COH-001 test in tests/test_dsx.py — build a plain spec
    dict, call the check function, inspect report codes/severities). Assert the four behaviours above.
    Do NOT weaken any assertion to make it pass yet — this task is RED. For Test 4, read the two
    remedy strings by importing the message the check emits (construct a spec that trips DSX-SPEC-061
    via a typeless claim and DSX-SPEC-062 via an unknown type, and assert on the emitted remedy),
    not by grepping source, so the test survives a refactor.
  </action>
  <verify>
    <automated>python -m unittest tests.test_prescriptive_ladder -v</automated>
  </verify>
  <acceptance_criteria>
    The module imports and runs; tests 1, 2 and 4 FAIL (RED) against the unmodified source and test 3
    passes. The run output names DSX-COH-001 absent (test 1) and DSX-SPEC-062 present (test 2) as the
    failure reasons.
  </acceptance_criteria>
  <done>tests/test_prescriptive_ladder.py exists with four tests; three fail RED for the stated reasons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add the member, the ladder row, and the five-type remedy</name>
  <files>dsx/spec.py, dsx/checks/coherence.py</files>
  <read_first>
    - dsx/spec.py:50 (CLAIM_TYPES set), :839-841 (DSX-SPEC-061 remedy), :848-850 (DSX-SPEC-062 remedy)
    - dsx/checks/coherence.py:31-36 (CLAIM_STRENGTH dict)
    - 11.2-CONTEXT.md D-01, D-02; RESEARCH landmine (a) "Guard for the planner"
  </read_first>
  <action>
    In dsx/spec.py: add "prescriptive" to the CLAIM_TYPES set literal at line 50. Change the
    DSX-SPEC-061 remedy (line 840) from the four-type wording ("Label every claim descriptive,
    association, predictive or causal.") to include prescriptive as the fifth type. Change the
    DSX-SPEC-062 remedy (line 849) from "Use one of the four claim types." to name five claim types.
    Leave the two `detail=` lines untouched — they already render `', '.join(sorted(CLAIM_TYPES))`
    and self-update. In dsx/checks/coherence.py: add the row `"prescriptive": 4` to the CLAIM_STRENGTH
    dict (31-36), mirroring the QUESTION_STRENGTH row at :28. Make NO logic change to
    _check_claim_ceiling — the generic `if ctype not in CLAIM_STRENGTH: continue` now admits
    prescriptive and the existing `c_strength > q_strength` branch fires DSX-COH-001. Mint no code.
  </action>
  <verify>
    <automated>python -m unittest tests.test_prescriptive_ladder -v</automated>
    <automated>python -c "from dsx.spec import CLAIM_TYPES; from dsx.checks.coherence import CLAIM_STRENGTH, QUESTION_STRENGTH; assert 'prescriptive' in CLAIM_TYPES; assert CLAIM_STRENGTH['prescriptive']==4==QUESTION_STRENGTH['prescriptive']; print('vocabulary aligned at rank 4')"</automated>
    <automated>python -m unittest discover -s tests</automated>
  </verify>
  <acceptance_criteria>
    tests/test_prescriptive_ladder.py is fully GREEN. The full suite (`python -m unittest discover -s
    tests`) passes with zero regressions — confirming verdict-neutrality on every existing fixture.
    The alignment one-liner prints the confirmation.
  </acceptance_criteria>
  <done>prescriptive is in CLAIM_TYPES and CLAIM_STRENGTH at rank 4; both remedy strings name five types; the whole suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-authored ANALYSIS-SPEC → gate | An operator declares a claim type; the gate must recognise `prescriptive` and hold it to the ceiling rather than treating it as a typo and skipping it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.2-01 | Tampering | `_check_claim_ceiling` / `_validate_claims_shape` | high | mitigate | The D-02 silent pass: a `type: prescriptive` claim trips DSX-SPEC-062 then is skipped at coherence.py:61-62. Mitigation is this plan's own deliverable — add the CLAIM_TYPES member AND the CLAIM_STRENGTH row AND both remedy strings in one slice so the ceiling fires DSX-COH-001; Task 1 tests 1 and 2 prove firing and non-misfire. |
| T-11.2-SC | Tampering | package installs | low | accept | No package-manager installs in this phase (brief D-01, stdlib-only). No supply-chain surface. |
</threat_model>

<verification>
- `python -m unittest tests.test_prescriptive_ladder -v` green.
- `python -m unittest discover -s tests` green (verdict-neutrality on the full corpus).
- `prescriptive` present in both CLAIM_TYPES and CLAIM_STRENGTH at rank 4, aligned with QUESTION_STRENGTH.
</verification>

<success_criteria>
A claim typed `prescriptive` under a weaker `question_type` fires DSX-COH-001 CRITICAL and no longer
fires DSX-SPEC-062; both remedy strings name five types; the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/11.2-prescriptive-claim-layer/11.2-01-SUMMARY.md` when done.
</output>
