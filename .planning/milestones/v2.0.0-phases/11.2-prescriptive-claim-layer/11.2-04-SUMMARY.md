---
phase: 11.2-prescriptive-claim-layer
plan: 04
subsystem: testing
tags: [template, storyteller, narrate, scaffold, quarantine, documentation-hygiene, ast-invariant]

# Dependency graph
requires:
  - phase: 11.2-03
    provides: DSX-PRE-040 (top-level spec_id gate-read for prescriptive/experiment specs)
  - phase: 11.2-06
    provides: DSX-COH-040 (decision.revisit_when gate-read re-open trigger)
provides:
  - "templates/ANALYSIS-SPEC.yaml: a 'Documentation-only — NOT gate-read' block quarantining decision.reversible + decision.deadline (kept, not deleted)"
  - "templates/ANALYSIS-SPEC.yaml: gate-read decision.revisit_when example with a deadline-distinction comment; top-level spec_id example; five-type claim comment"
  - "agents/dsx-data-storyteller.md + skills/dsx-narrate/SKILL.md repointed at the gate-read revisit_when field"
  - "tests/test_scaffold_quarantine.py: an AST+text invariant that no dsx/ check reads the quarantined fields and the docs name neither"
affects: [template maintenance, prescriptive claim layer, narrative agents]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Two-detectors-layered invariant scanner (blunt text scan + precise AST scan), mirroring tests/test_frame_boundary.py, with self-firing subtests proving RED capability"
    - "Dotted-path text detection keyed on the qualified form (decision.deadline) to avoid false positives on unrelated prose (date/deadline, irreversible)"

key-files:
  created:
    - tests/test_scaffold_quarantine.py
  modified:
    - templates/ANALYSIS-SPEC.yaml
    - agents/dsx-data-storyteller.md
    - skills/dsx-narrate/SKILL.md

key-decisions:
  - "Audit set of reader-less decision.* fields is exactly {reversible, deadline} — grep across dsx/ surfaced no other reader-less decision field, and the only reversible/deadline hits were unrelated comments (date/deadline in spec.py, irreversible in admissibility.py)."
  - "Used real (non-placeholder) values for revisit_when and spec_id example fields; the template is descriptive/observational so DSX-COH-040/DSX-PRE-040 never fire on it, but real values keep the examples exemplary and gate-safe under any future test that runs the template through a prescriptive gate."
  - "Removed a contrasting 'deadline' mention from the two doc files so they name NEITHER quarantined field, satisfying the Task 3 behavior (docs contain no reference to reversible/deadline) as well as the Task 2 verify (no reversible)."

patterns-established:
  - "Invariant-guard test for a documentation label: when the honest 'not gate-read' label could silently become a lie, a scanner pins it and its own self-firing subtests supply the RED that production cannot."

requirements-completed: [REQ-P11.2-07]

coverage:
  - id: D1
    description: "decision.reversible and decision.deadline moved into a 'Documentation-only — NOT gate-read' block in the template (kept, not deleted); gate-read revisit_when + top-level spec_id examples added; claim-type comment names five types"
    requirement: "REQ-P11.2-07"
    verification:
      - kind: unit
        ref: "python -c template-invariant check (Task 1 <verify>): reversible/deadline present, revisit_when+spec_id present, prescriptive present, documentation-only label present"
        status: pass
      - kind: integration
        ref: "tests/test_dsx.py::test_template_draws_no_finding_and_exits_zero_at_gate_plan (template still green through gate plan)"
        status: pass
    human_judgment: false
  - id: D2
    description: "storyteller prompt and narrate skill reference only gate-read fields; add a note that a prescriptive narrative should state its revisit_when trigger; associational-verb discipline preserved"
    requirement: "REQ-P11.2-07"
    verification:
      - kind: unit
        ref: "python -c storyteller/narrate check (Task 2 <verify>): 'reversible' absent from both files"
        status: pass
      - kind: unit
        ref: "tests/test_scaffold_quarantine.py::TestNarrativeDocsNameNoQuarantinedField.test_doc_files_name_neither_quarantined_field"
        status: pass
    human_judgment: false
  - id: D3
    description: "invariant test pins that no dsx/ check reads decision.reversible/decision.deadline and the docs name neither"
    requirement: "REQ-P11.2-07"
    verification:
      - kind: unit
        ref: "tests/test_scaffold_quarantine.py (9 tests, incl. self-firing detector subtests)"
        status: pass
      - kind: unit
        ref: "python -m unittest discover -s tests (1133 OK)"
        status: pass
    human_judgment: false

# Metrics
duration: 14min
completed: 2026-08-26
status: complete
---

# Phase 11.2 Plan 04: Scaffold Quarantine + Storyteller Repoint Summary

**Quarantined the reader-less decision.reversible/decision.deadline template fields behind an explicit "NOT gate-read" label, added gate-read revisit_when + spec_id examples with a deadline-distinction comment, repointed the storyteller/narrate docs at enforced fields, and pinned it all with an AST+text invariant test.**

## Performance

- **Duration:** ~14 min
- **Started:** 2026-08-26
- **Completed:** 2026-08-26
- **Tasks:** 3
- **Files modified:** 4 (3 modified, 1 created)

## Accomplishments
- Template no longer implies enforcement of fields no gate reads: `decision.reversible` and `decision.deadline` sit under a "Documentation-only — NOT gate-read" block, kept (not deleted) for their human-documentation value.
- Template distinguishes the newly gate-read `decision.revisit_when` (a re-open trigger, DSX-COH-040) from the scaffold `decision.deadline` (a decide-by date) via an inline comment, so operators do not conflate the two temporal fields.
- Added a top-level `spec_id` example (DSX-PRE-040) and updated the claim-type comment to name five types including `prescriptive`.
- Repointed the storyteller prompt and narrate skill at the gate-read `revisit_when` field; associational-verb discipline preserved unchanged.
- New `tests/test_scaffold_quarantine.py` (9 tests) proves no dsx/ code path reads the quarantined fields and the two docs name neither.

## Task Commits

Each task was committed atomically:

1. **Task 1: quarantine scaffold fields; add revisit_when + spec_id; five-type comment** - `c7a8559` (docs)
2. **Task 2: repoint the storyteller and the narrate skill at gate-read fields** - `12145eb` (docs)
3. **Task 3: pin the no-check-reads-scaffold invariant** - `03c3ac1` (test)

_TDD note: Task 3 was marked tdd="true" but no genuine RED state existed — dsx/ never read these fields and the docs never named them, so the invariant already held after Tasks 1-2. It is committed as a single regression-guard test; its RED capability is proven by the scanner's self-firing subtests rather than by a failing production state._

## Files Created/Modified
- `templates/ANALYSIS-SPEC.yaml` - Quarantine block for reversible/deadline; gate-read revisit_when + top-level spec_id examples; five-type claim comment
- `agents/dsx-data-storyteller.md` - Note pointing prescriptive narratives at the gate-read revisit_when trigger
- `skills/dsx-narrate/SKILL.md` - Same revisit_when note; names no reader-less scaffold field
- `tests/test_scaffold_quarantine.py` - AST+text invariant that no check reads the quarantined fields and the docs name neither

## Decisions Made
- Confirmed the reader-less audit set is exactly `{reversible, deadline}` by grep across `dsx/checks`, `dsx/frame`, `dsx/spec.py` and the wider `dsx/` tree; the only hits for these tokens were unrelated comments ("date/deadline", "irreversible"), not reads.
- Used real discriminating values (`revisit_when: "activation_rate below +1.0pp at the 2026-Q4 review"`, a concrete `spec_id`) rather than placeholder tokens. Verified via `test_template_draws_no_finding_and_exits_zero_at_gate_plan` that the template runs through `gate plan` clean; because the template is `descriptive`/`observational`, DSX-COH-040 and DSX-PRE-040 (prescriptive/experiment-only) do not fire on it regardless.
- The text detector keys on the qualified dotted path (`decision.deadline`), not the bare word, precisely so the pre-existing "date/deadline" comment in `dsx/spec.py` and "irreversible" comment in `dsx/frame/admissibility.py` are not false positives — covered by an explicit permitting test.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking self-consistency] Removed a `deadline` mention from the doc edits**
- **Found during:** Task 2 (storyteller/narrate repoint)
- **Issue:** My first Task 2 edit named `deadline` to contrast it with `revisit_when`. Task 3's stated behavior requires the two docs to contain no reference to `reversible`/`deadline`; that mention would have made a faithful Task 3 test fail on the docs.
- **Fix:** Rewrote both notes to point at `revisit_when` as "a field the gate actually reads" / "not an unenforced scaffold note" without naming either quarantined field.
- **Files modified:** agents/dsx-data-storyteller.md, skills/dsx-narrate/SKILL.md
- **Verification:** `python -c` check asserting neither `reversible` nor `deadline` appears in either file (both lower-cased); Task 3 doc-scan test passes.
- **Committed in:** `12145eb` (Task 2 commit — the correction was made before the commit)

---

**Total deviations:** 1 auto-fixed (1 blocking self-consistency)
**Impact on plan:** Kept Task 2 and Task 3 mutually consistent; no scope change. All plan artifacts delivered as written.

## Issues Encountered
- None beyond the deviation above. The pre-existing "declared twice with different text" registry warnings are unrelated to this plan and appear on the baseline suite run.

## TDD Gate Compliance
Task 3 carried `tdd="true"`, but Tasks 1-2 make the invariant hold before the test exists (dsx/ never read the quarantined fields; the docs never named them). No production RED was achievable without introducing a real enforcement regression, so the test ships as a regression guard whose firing capability is proven by its own self-firing detector subtests (`test_ast_detector_fires_*`, `test_text_detector_fires_*`). Documented here per the plan-level TDD gate note.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- The template honestly labels enforcement, distinguishes the two temporal fields, and is pinned by an invariant test. Ready for downstream prescriptive-claim-layer work.
- No blockers.

## Self-Check: PASSED
All four files_modified plus the SUMMARY exist on disk; all three task commits (c7a8559, 12145eb, 03c3ac1) are present in git history.

---
*Phase: 11.2-prescriptive-claim-layer*
*Completed: 2026-08-26*
