---
phase: 11-frequentist-admissibility-adjudicator-dsx-adm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - references/test-selection.md
  - brief.md
autonomous: true
requirements: [REQ-P11-01, REQ-P11-06]

must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md` REQ-P11-01 no longer says 25–35; it states 14 families and records why the number changed (D-02)."
    - "`.planning/ROADMAP.md` Phase 11 Success Criterion 1 no longer says 25–35; it states 14 and carries the same reason (D-02)."
    - "`references/test-selection.md` no longer prescribes Fisher's exact as the small-expected-cell fallback, and the replacement row carries its own citation (D-27)."
    - "`brief.md` section 7 carries the Kohavi, Tang & Xu Chapter 22 locator and the Cameron & Miller Section VI locator, each with its verification caveat stated (D-29)."
    - "Two `references/test-selection.md` rows that name the identical procedure remain two distinct rows — the D-27 correction edits the small-expected-cell fallback wording only and merges no rows."
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - references/test-selection.md
    - brief.md
  key_links:
    - "REQ-P11-01's amended count and `references/families.yaml`'s delivered entry count must be the same number — plan 11-04 asserts the file has exactly that many entries."
    - "`references/test-selection.md`'s corrected fallback and `references/families.yaml`'s Boschloo-over-Fisher ordering must state the same preference, or the reference doc and the ontology contradict each other."
  prohibitions:
    - statement: "A citation added to `references/test-selection.md` or `brief.md` in this plan must not carry a page, chapter or section locator that nobody has opened. An unconfirmed locator is stated as unverified in the surrounding prose, never guessed to look complete."
      flagged: true
---

<objective>
Amend this phase's own requirement and roadmap success criterion to the number the phase
actually delivers, and fix the two citation defects the ontology would otherwise inherit.

Purpose: D-02 makes the family-count change a recorded amendment rather than a silent
under-delivery. D-27 names a live D-05 defect in `references/test-selection.md` that the alias
layer would copy. D-29 closes two locators the project currently carries as unverified.

Output: amended `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`, a corrected
`references/test-selection.md` row, and two resolved locators in `brief.md` section 7.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-frequentist-admissibility-adjudicator-dsx-adm/11-CONTEXT.md
</context>

## Artifacts this phase produces

Every symbol below is created by Phase 11 and does not exist in the codebase before it.

- Data file: `references/families.yaml`
- Module: `dsx/frame/admissibility.py`
- Functions: `admissible_families(spec)`, `check(spec, *, applies_to_frame)` (both in
  `dsx/frame/admissibility.py`); `applies_to_frequentist_admissibility(spec)` (in
  `dsx/frame/paradigm.py`); `check_families_citations(families_path)` (in
  `scripts/gen-finding-catalogue.py`)
- Vocabulary constants: `ESTIMAND_TYPES` in `dsx/spec.py`; the `("estimand_types", ESTIMAND_TYPES)`
  entry in `_VOCABULARIES`; the `("estimand", "type", ESTIMAND_TYPES)` row in
  `_VALIDITY_FRAME_MEMBERSHIP`
- ANALYSIS-SPEC field: `validity_frame.estimand.type` (new, optional, closed vocabulary)
- Finding codes: `DSX-ADM-010` (HIGH), `DSX-ADM-020` (CRITICAL)
- Registry entries: `CHECKS["admissibility"]`; `"admissibility"` in `GATE_PROFILES["plan"]`,
  `["verify"]` and `["ship"]`; `"DSX-ADM-"` in `_D05_ALLOWLIST_PREFIXES`; the
  `("DSX-ADM", "Frequentist admissibility", …)` row in `PREFIX_GROUPS`
- CLI surface: `--spec` and `--phase-dir` flags on `dsx recommend-test`; the additive
  `"admissibility"` key in that command's JSON output
- Test modules: `tests/test_families_yaml.py`, `tests/test_frame_admissibility.py`,
  `tests/test_frame_paradigm.py`; the reverse-direction scanner added to
  `tests/test_frame_boundary.py`

<tasks>

<task type="auto">
  <name>Task 1: Amend REQ-P11-01 and ROADMAP Success Criterion 1 to the delivered family count</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — read line 134 (REQ-P11-01's current text) and the
      surrounding REQ-P11-* block, plus the traceability table rows at lines 221-226 so the
      edit does not disturb them.
    - `.planning/ROADMAP.md` — read the `### Phase 11:` section, specifically Success
      Criterion 1.
    - `.planning/phases/11-frequentist-admissibility-adjudicator-dsx-adm/11-CONTEXT.md` — D-01
      and D-02, which are the authority for this edit and supply the reason text.
  </read_first>
  <action>
    Replace the number range "25–35" with the literal count 14 in exactly two places, and record
    the reason at each.

    In `.planning/REQUIREMENTS.md`, REQ-P11-01 currently reads "`references/families.yaml` holds
    25–35 estimator families as data, keyed on estimand × family × inference method × dependence
    handling, parsed by the existing loader". Rewrite it to state 14 estimator families with the
    same four axes and the same loader clause, and append an amendment note directly beneath the
    requirement line. The note must state: the count was amended from 25–35 to 14 during Phase 11
    planning per `11-CONTEXT.md` D-01/D-02; the reason is that ROADMAP Success Criterion 5 requires
    every family to trace to a case committed in the repository at the time Phase 11 lands, the
    committed corpus supplies six distinct procedure labels, and Phase 12 is the phase that grows
    the corpus; sizing to the evidence keeps "a family is added when a real case needs it" literally
    true of the file that ships. Do not delete the original wording — state it as the amended-from
    value inside the note so the change is auditable.

    In `.planning/ROADMAP.md`, Phase 11 Success Criterion 1 currently reads "`references/families.yaml`
    holds 25–35 estimator families as data keyed on estimand × family × inference method ×
    dependence handling, parsed by the existing `dsx.loader.load()` with no new parser, and named
    tests resolve as aliases into families rather than being enumerated as a test catalogue".
    Change "25–35" to 14 and add a one-line parenthetical naming the amendment: amended from 25–35,
    Phase 11 planning, `11-CONTEXT.md` D-02.

    Use scoped `Edit` calls on both files. Never rewrite either file whole — both are single-writer
    shared tracking files under the project's CLAUDE.md, and a whole-file write destroys entries
    outside the diff window. Leave the REQ-P11-01 checkbox unticked and the traceability table row
    for REQ-P11-01 at "Pending"; this plan amends the requirement text, it does not complete it.

    Applies D-02.
  </action>
  <verify>
    <automated>python -c "import io,re; t=io.open('.planning/REQUIREMENTS.md',encoding='utf-8').read(); r=io.open('.planning/ROADMAP.md',encoding='utf-8').read(); line=[l for l in re.split(r'\r?\n',t) if 'REQ-P11-01' in l and 'families.yaml' in l][0]; assert '25' not in line and '14' in line, line; sc=[l for l in re.split(r'\r?\n',r) if 'estimator families as data' in l][0]; assert '25' not in sc and '14' in sc, sc; assert 'D-02' in t and 'D-02' in r; print('amendment recorded')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md`'s REQ-P11-01 line contains the literal `14` and does not contain
      the literal `25`.
    - `.planning/REQUIREMENTS.md` contains an amendment note within five lines of REQ-P11-01 that
      names `D-02`, names the amended-from value, and gives the traceability reason.
    - `.planning/ROADMAP.md` Phase 11 Success Criterion 1 contains `14` and does not contain `25`,
      and names `D-02`.
    - `.planning/REQUIREMENTS.md`'s traceability row `| REQ-P11-01 | Phase 11 | Pending |` is
      unchanged.
    - `git diff --stat` for this task lists exactly two files.
  </acceptance_criteria>
  <done>Both the requirement and the roadmap success criterion state 14 families and carry the recorded reason for the change.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the Fisher's exact fallback in references/test-selection.md (D-27)</name>
  <files>references/test-selection.md</files>
  <read_first>
    - `references/test-selection.md` — read the whole file. The defect is on line 10: the
      `proportion | 2 | no` row prescribes "two-proportion z (Fisher exact if any expected cell < 5)".
      Read the surrounding table so the replacement matches the file's column shape and prose register.
    - `.planning/phases/11-frequentist-admissibility-adjudicator-dsx-adm/11-CONTEXT.md` — D-27 (the
      defect and its authority) and D-13 (the Lydersen ordering, which is the replacement's source).
    - `.claude/skills/plain-language/SKILL.md` — this file is read by practitioners; the replacement
      wording must expand any abbreviation on first use and read as a plain sentence.
  </read_first>
  <action>
    Replace the parenthetical "Fisher exact if any expected cell < 5" in the `proportion | 2 | no`
    row so the file stops prescribing Fisher's exact test as the small-expected-cell fallback.

    The replacement names Boschloo's unconditional exact test as the small-expected-cell choice, on
    the authority of Lydersen, Fagerland and Laake (2009), *Statistics in Medicine* 28(7):1159-1175,
    section 9, which states that the traditional Fisher's exact test should practically never be
    used and establishes the only uniform power domination found anywhere in this phase's research.

    Because the table cell is narrow, put the citation in a footnote or a short prose note directly
    beneath the table rather than inside the cell, and reference it from the cell. The file today is
    uncited; this is the first citation it carries, so add whatever minimal heading or footnote
    convention the file needs and keep it consistent.

    Constraints:
    - Do not touch any other row. Do not change any row's `test` value for any other
      outcome-type/group/paired combination.
    - Do not change any check's firing set. `dsx/checks/stats.py` and `dsx/frame/prereg.py` are not
      edited by this plan or by this phase (D-03). `references/test-selection.md` is documentation;
      no code reads it.
    - Cite by author, year, journal, volume(issue):pages and section number. The section-9 locator
      is verified; state it plainly. Do not add a page number for the specific sentence unless it has
      been confirmed.

    Applies D-27, supported by D-13.
  </action>
  <verify>
    <automated>python -c "import io,re; t=io.open('references/test-selection.md',encoding='utf-8').read(); rows=[l for l in re.split(r'\r?\n',t) if l.startswith('| proportion') and '| 2 |' in l]; assert rows, 'proportion row not found'; assert not any('Fisher exact if' in l for l in rows), rows; assert 'Boschloo' in t and 'Lydersen' in t and '2009' in t; print('D-27 corrected')"</automated>
    <human-check>Read the amended row and the new citation note. Confirm the replacement no longer prescribes Fisher's exact as the small-cell fallback, that the sentence reads correctly to a practitioner, and that the Lydersen section-9 locator is stated at the strength the source supports.</human-check>
  </verify>
  <acceptance_criteria>
    - `references/test-selection.md` contains no line matching `Fisher exact if`.
    - `references/test-selection.md` contains the literals `Boschloo`, `Lydersen`, `2009` and
      `28(7)`.
    - The `proportion | 2 | no` row still exists and still names two-proportion z as the primary
      choice.
    - `git diff references/test-selection.md` touches only the `proportion | 2 | no` row and the
      added citation note — no other table row changes.
    - `python -m unittest discover -s tests` still reports OK (no code reads this file; a change in
      test count is a signal something else moved).
  </acceptance_criteria>
  <done>The file no longer carries the D-27 defect and the replacement recommendation is cited to its primary source.</done>
</task>

<task type="auto">
  <name>Task 3: Fold the two resolved locators into brief.md section 7 (D-29)</name>
  <files>brief.md</files>
  <read_first>
    - `brief.md` — read section 7 in full (the citations section), plus section 4's D-05 and D-14
      entries so the edit is recognisably an addition rather than a reversal.
    - `.planning/phases/11-frequentist-admissibility-adjudicator-dsx-adm/11-CONTEXT.md` — D-29,
      which supplies both locators verbatim including the caveat on the Cameron & Miller numbering.
    - `.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-VERIFICATION.md`
      — the open unverified-locator flag this edit closes for Kohavi, Tang & Xu.
    - `dsx/frame/paradigm.py` lines 60-80 — the `locator_status` honesty convention this edit
      follows.
  </read_first>
  <action>
    Add two locators to `brief.md` section 7, each stated at exactly the strength its evidence
    supports.

    First: Kohavi, Tang and Xu, Chapter 22, *Leakage and Interference between Variants*, pages
    226-234. This closes the unverified-locator flag recorded in `06-08-SUMMARY.md` and
    `06-VERIFICATION.md` for the shared-budget interference citation. State it as verified.

    Second: Cameron and Miller (2015), Section VI *Few Clusters*, with Section II for the estimator
    and Section IV for the clustering dimension. This partially closes the flag recorded in
    `07-01-SUMMARY.md`. State the caveat in the same sentence: the accepted manuscript jumps from
    Section VIII to Section XI, so the typeset journal numbering may differ, and the manuscript
    numbering is the verified object.

    Follow the file's existing citation format. Where a prior entry carried an explicit
    unverified-locator flag for either work, update that flag rather than leaving two contradictory
    statements in the same file. Do not add any other citation in this task — the wider citation
    spine that `references/families.yaml` needs lives inside that data file, not in `brief.md`.

    This is a citation addition and a flag resolution, not a decision reversal, so no D-14 reversal
    record is created and `REVERSALS.md` is not touched.

    Applies D-29.
  </action>
  <verify>
    <automated>python -c "import io; t=io.open('brief.md',encoding='utf-8').read(); assert 'Chapter 22' in t and '226' in t, 'Kohavi locator missing'; assert 'Section VI' in t and 'Few Clusters' in t, 'Cameron and Miller locator missing'; assert 'Section VIII' in t and 'Section XI' in t, 'numbering caveat missing'; print('D-29 locators folded in')"</automated>
    <human-check>Read both added entries against the sources named in D-29 and confirm each locator is stated at the strength the evidence supports — the Kohavi chapter as verified, the Cameron and Miller section numbering as manuscript-verified with the typeset-numbering caveat intact.</human-check>
  </verify>
  <acceptance_criteria>
    - `brief.md` contains the literals `Chapter 22`, `Leakage and Interference between Variants` and
      `226`.
    - `brief.md` contains the literals `Section VI`, `Few Clusters`, `Section VIII` and `Section XI`.
    - No file under `.planning/` named `REVERSALS.md` gains a new entry from this task.
    - `git diff --stat` for this task lists exactly one file, `brief.md`.
  </acceptance_criteria>
  <done>Both previously-unverified locators are recorded in brief.md section 7 with their verification status stated honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → repository documentation | A human or agent writes citation text that later phases and the ontology treat as established fact. No parser validates the claim. |
| documentation → ontology data | `references/test-selection.md`'s recommendation is copied by hand into `references/families.yaml`'s ranking notes in plan 11-04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Repudiation | `.planning/REQUIREMENTS.md` amendment | medium | mitigate | The amendment note states the amended-from value and the reason, so the change cannot later be read as the original requirement. Acceptance criterion asserts `D-02` is named. |
| T-11-02 | Tampering | `brief.md` / `references/test-selection.md` citations | high | mitigate | Every added locator is either confirmed or explicitly caveated; the human-check verify step reads each added citation against its source before the phase closes. No locator is added without a stated verification status. |
| T-11-03 | Information disclosure | none | low | accept | These files carry no credential, personal data or endpoint. Nothing to disclose. |
| T-11-04 | Denial of service | none | low | accept | Documentation edits are not on any execution path. |
| T-11-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package. `references/families.yaml`'s parser is the already-shipped `dsx.loader`; PyYAML is a pre-existing optional dependency, not newly added. No package-legitimacy checkpoint applies. |
</threat_model>

<verification>
- `python -m unittest discover -s tests` reports OK with 640 tests (the measured 2026-08-20
  baseline). This plan changes no code, so the count must not move.
- `python scripts/gen-finding-catalogue.py --check` exits 0 and prints "finding catalogue is
  current". Four pre-existing "declared twice with different text" warnings (DSX-SPEC-070 twice,
  DSX-VAL-021, DSX-VAL-060) are expected and unrelated to this phase — their presence is not a
  failure, and their absence would be the surprise.
- `git status` shows exactly four modified files and no new files.
</verification>

<success_criteria>
- REQ-P11-01 and ROADMAP Phase 11 Success Criterion 1 both state 14 families with the reason
  recorded and `D-02` named.
- `references/test-selection.md` no longer prescribes Fisher's exact as the small-expected-cell
  fallback and carries the Lydersen 2009 section 9 citation.
- `brief.md` section 7 carries both D-29 locators with their verification status stated.
- Test count and catalogue check are unchanged from the baseline.
</success_criteria>

<output>
Create `.planning/phases/11-frequentist-admissibility-adjudicator-dsx-adm/11-01-SUMMARY.md` when done.
</output>
