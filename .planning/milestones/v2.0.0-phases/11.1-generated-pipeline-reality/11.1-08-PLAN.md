---
phase: 11.1-generated-pipeline-reality
plan: 08
type: execute
wave: 7
depends_on: ["11.1-07", "11.1-02"]
files_modified:
  - tests/test_known_bad_corpus.py
  - examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml
  - examples/known-bad/full-frame-cleaning-POSTMORTEM.md
  - examples/known-bad/full-frame-cleaning-entrypoint.py
  - references/finding-codes.md
autonomous: true
requirements: [REQ-P11.1-07, REQ-P11.1-08]

must_haves:
  truths:
    - "`examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml` exists with a sibling post-mortem and a sibling entrypoint, and the post-mortem names at least one finding code in this repository's code format."
    - "The fixture reproduces the 2026-08-20 case exactly: full-frame mean imputation and a spread-based row filter above a legitimate split, a statistical test against the declared target above that split, a scaler fitted on the full frame after the split, an algorithm declared with no selection ledger, and a whole-pipeline preprocessing boundary of training rows only declared honestly."
    - "Measured against the pre-phase gate, the same fixture produces zero findings at every severity from the entrypoint check; measured after this phase it blocks `dsx gate execute` with exit 1."
    - "`tests/test_known_bad_corpus.py` records the fixture's target codes at the execute gate point, and every one of them appears among the CRITICAL findings of a real gate run."
    - "The fixture clears `dsx gate plan` with exit 0, because the entrypoint check and the machine-learning check are not registered at that gate point."
    - "The fixture passes `dsx validate` with exit 0, like every other fixture in the corpus."
    - "Every CRITICAL and HIGH finding the fixture produces at `dsx gate ship` is either one of its own recorded target codes or a member of `_INCIDENTAL_GAP_CODES` carrying an inline comment naming the corpus-completeness gap that causes it."
    - "The corpus harness seeds the fixture's declared entrypoint into the temporary phase directory before running the gate, so the entrypoint check can resolve it; a fixture that declares no entrypoint is unaffected and every pre-existing fixture's exit code is unchanged."
    - "`_own_target_codes` returns the individual code strings for a fixture whose target-map value holds several codes, not the container holding them, so the ship-completeness test recognises every one of them."
    - "`python scripts/gen-finding-catalogue.py --check` exits 0 and `./scripts/check.sh` exits 0 at the end of this plan."
  artifacts:
    - examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml
    - examples/known-bad/full-frame-cleaning-POSTMORTEM.md
    - examples/known-bad/full-frame-cleaning-entrypoint.py
    - tests/test_known_bad_corpus.py
    - references/finding-codes.md
  key_links:
    - "The temporary phase directory the corpus harness passes as the resolve root is why the entrypoint check currently cannot see any fixture's entrypoint. The seeding helper and the fixture's declared entrypoint path are one link: change either and the fixture stops blocking for a reason that has nothing to do with the defect it encodes."
    - "`_TARGET_DEFECT_CODES` and `_EXPECTED_CAUGHT_DEFECTS` are combined by one function into the map the classifier reads. A fixture whose family is absent from the plan gate profile belongs in the first map, keyed by the point it does fire at, never in the second."
    - "The post-mortem's named finding code and the code recorded in the target map must be the same code. A post-mortem naming a code the map does not expect is documentation that describes a different gate than the one that runs."
  prohibitions:
    - statement: "MUST NOT weaken, delete or rewrite an existing corpus expectation to make this fixture fit. `_INCIDENTAL_GAP_CODES` records corpus-completeness gaps, never a fixture's own encoded defect, and a pre-existing fixture whose exit code moves is a regression to investigate, not an entry to edit."
      flagged: true
    - statement: "MUST NOT make this fixture block by adding a second, unrelated defect to it. It must block on the reproduction's own defects, because the whole claim of the requirement is that this exact shape passed and now does not."
      flagged: true
    - statement: "MUST NOT let a misspelled or out-of-vocabulary value in the fixture clear a gate that a blank value would have blocked. If the fixture clears a gate it should block, read why before changing anything."
      flagged: true
    - statement: "MUST NOT ship a post-mortem claim about what the gate does that was not measured by running the gate. Every exit code and every finding code in the post-mortem is copied from a real run, not predicted."
      flagged: true
---

<objective>
Commit the reproduction as a known-bad corpus case, with the catch attribution recorded beside it,
so the failure mode this phase exists to close is measured rather than asserted.

Purpose: the roadmap's first success criterion for this phase is a claim about a specific artefact —
that full-frame cleaning above an honest split, with the preprocessing boundary honestly declared,
is blocked at execute where today it passes with zero findings at every severity. That claim is only
true if the artefact exists in the corpus and the corpus harness actually runs the entrypoint check
against it, which today it cannot.

Output: a fixture triple, a corpus-harness extension that lets any fixture's entrypoint be resolved,
the catch-attribution entries, and a final currency pass over the catalogue.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-generated-pipeline-reality/11.1-RESEARCH.md
@.planning/phases/11.1-generated-pipeline-reality/11.1-PATTERNS.md
@.planning/phases/11.1-generated-pipeline-reality/11.1-01-SUMMARY.md
@.planning/phases/11.1-generated-pipeline-reality/11.1-03-SUMMARY.md
@.planning/phases/11.1-generated-pipeline-reality/11.1-07-SUMMARY.md
</context>

## Artifacts this phase produces

Every symbol below is created by Phase 11.1 and does not exist in the codebase before it. The plan
that creates each one is named in brackets.

- Finding codes: `DSX-CODE-020` CRITICAL [01], `DSX-CODE-021` CRITICAL [01], `DSX-CODE-030`
  CRITICAL [03], `DSX-CODE-031` HIGH [03], `DSX-ML-043` HIGH [04], `DSX-ML-023` CRITICAL [05],
  `DSX-ML-024` HIGH [05], `DSX-ML-052` HIGH [06], `DSX-ML-053` MEDIUM [06], `DSX-ML-090` HIGH [07],
  `DSX-ML-091` CRITICAL [07], `DSX-ML-092` HIGH [07]
- Constants in `dsx/checks/code.py`: `TRAINING_FRAME_NAMES`, `FULL_FRAME_IMPUTE_RE`,
  `FULL_FRAME_SPREAD_FILTER_RE`, `FIT_CALL_RE` [01]; `STAT_TEST_CALL_RE` [03]
- Functions in `dsx/checks/code.py`: `_first_full_frame_cleaning_line`, `_fit_call_arguments`,
  `_is_training_frame` [01]; `_stat_test_lines_referencing` [03]
- Constants in `dsx/checks/ml.py`: `TRAIN_ONLY_FIT_VALUES` [05], `SCORE_SOURCES` [06],
  `SELECTION_BASES` [07]
- Functions in `dsx/checks/ml.py`: `_check_prediction_time_definition` [04], `_check_cleaning` [05],
  `_check_selection_ledger` [07]
- ANALYSIS-SPEC fields, all new and all optional: `data[].cleaning` as a list of
  `{column, method, fit_on}` [05]; `results.model_score_source` and `results.fold_scores` [06];
  `model.selection_ledger` as `{candidates_evaluated, configurations_tried, selected_on}` [07]
- Fixture files: `examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml`,
  `examples/known-bad/full-frame-cleaning-POSTMORTEM.md`,
  `examples/known-bad/full-frame-cleaning-entrypoint.py` [08]
- Test classes: `tests/test_dsx.py::TestPhase11_1Code` [01], `tests/test_dsx.py::TestPhase11_1ML` [04]
- Harness surface in `tests/test_known_bad_corpus.py`: the `_seed_entrypoint` helper, the widened
  `_TARGET_DEFECT_CODES` value type, and frozenset flattening in `_own_target_codes` [08]
- Registry entries: twelve new codes in `scripts/gen-finding-catalogue.py::_D05_ALLOWLIST_CODES`;
  a `full-frame-cleaning` entry in `_TARGET_DEFECT_CODES` and in `_EXPECTED_CAUGHT_DEFECTS` [08]
- Documentation: the type-3 ensemble sub-case and the discretisation note in
  `references/leakage-taxonomy.md`, and two new signals in `agents/dsx-ml-integrity-auditor.md` [02]

## Flagged assumptions

- The edge probe classified REQ-P11.1-07 and REQ-P11.1-08 as unclassified, so no edge predicates
  were derived for them mechanically. The assumptions this plan proceeds under are authored into
  `must_haves.truths` above: the fixture must clear plan and block execute, its declared entrypoint
  must be resolvable from the harness's temporary root, and every blocking finding it produces at
  ship must be attributed. These are assumptions, not probe results.
- The corpus harness change is the one piece of this phase that was not derivable from the research
  file. It follows from a fact measured at plan time: the harness passes a fresh temporary directory
  as both the phase directory and the resolve root, so a relative entrypoint declared in a fixture
  cannot be found, and no fixture in the corpus declares one today. Verify this by running the gate
  before writing the helper, rather than trusting this paragraph.

<tasks>

<task type="auto">
  <name>Task 1: Let the corpus harness resolve a fixture's declared entrypoint, and flatten multi-code target entries</name>
  <files>tests/test_known_bad_corpus.py</files>
  <read_first>
    - `tests/test_known_bad_corpus.py` lines 55-150 — `_CRITICAL_THRESHOLD_POINTS`,
      `_INCIDENTAL_GAP_CODES` with its per-entry comments, and `_TARGET_DEFECT_CODES` with its long
      rationale comment. Read the rationale before changing the type it describes.
    - `tests/test_known_bad_corpus.py` lines 152-200 — `_classify_target_defect`. Its docstring
      already states that a map value may be a bare string or a container of strings, and that both
      normalise to one set. That is the half of the contract this task completes.
    - `tests/test_known_bad_corpus.py` lines 289-340 — `_EXPECTED_CAUGHT_DEFECTS`,
      `_effective_target_map` and `_own_target_codes`. `_own_target_codes` currently collects the
      values of the point map directly, which puts a container into a set of code strings rather
      than the codes it holds.
    - `tests/test_known_bad_corpus.py` lines 342-400 — `_gate_findings`, including the comment
      explaining why the temporary directory exists and the plan-header seeding it already performs
      for the verify and ship points.
    - `tests/_trail_seed.py` — `seed_plan_header`, the existing seeding helper whose call shape and
      placement the new one copies.
    - `dsx/cli.py` around lines 147-190 — `run_checks`, specifically that the entrypoint check
      receives the resolve root and the reproducibility check receives the phase directory. Both are
      the temporary directory under the harness.
  </read_first>
  <behavior>
    - A fixture declaring a reproducibility entrypoint that exists beside its specification has that
      file copied into the temporary phase directory before the gate runs, at the same relative path
      the specification declares.
    - A fixture declaring no entrypoint, or declaring one that does not exist beside its
      specification, is unaffected and the gate runs exactly as before.
    - Every pre-existing fixture's exit code at every gate point is unchanged.
    - `_own_target_codes` returns individual code strings for a fixture whose target-map value holds
      several codes.
    - `_own_target_codes` still returns the single code for a fixture whose value is one string.
    - `_classify_target_defect` accepts a multi-code value at one gate point and requires every one
      of those codes among the CRITICAL findings.
  </behavior>
  <action>
    Before writing anything, measure the premise. Run a real gate at the execute point against one
    existing fixture with a temporary phase directory and confirm first-hand that a relative
    entrypoint would not resolve. Record what you ran and what came back in the summary. If the
    premise turns out to be wrong, stop and say so rather than adding a helper nothing needs.

    Add a module-level helper `_seed_entrypoint(tmp, spec_path)` to `tests/test_known_bad_corpus.py`.
    It loads the specification, reads the reproducibility entrypoint, and when that value is
    non-blank and a file of that name exists beside the specification, copies it into the temporary
    directory at the same relative path, creating parent directories as needed. It returns quietly in
    every other case. Give it a docstring stating why it exists in the same voice the module's other
    comments use: the harness deliberately points the resolve root at a fresh temporary directory so
    the decision trail is never written under the examples tree, and that same choice makes a
    fixture's own entrypoint unreachable to the entrypoint check.

    Call it from `_gate_findings` for every gate point, immediately before the plan-header seeding
    that already runs for verify and ship. Unlike that one, it is unconditional, because the
    entrypoint check is registered at the execute point as well.

    Widen `_TARGET_DEFECT_CODES`'s type annotation so a point's value may be either one code string
    or a container of code strings, matching what `_classify_target_defect` already documents and
    accepts. Extend the constant's comment to record that this phase is the first to need the
    multi-code shape at a point-scoped entry, and why: the entrypoint check family and the
    machine-learning check family are both registered at the execute point but not at the plan point,
    and this phase's fixture demonstrates several codes from both.

    Fix `_own_target_codes` to flatten a container value into its individual codes rather than
    collecting the container itself. Both maps feed it, and the ship-completeness test consults it,
    so a container left unflattened would report every one of a fixture's own codes as undocumented.

    Prove the flattening with a test that uses a synthetic map rather than the module constants,
    following the discipline `_classify_target_defect`'s docstring already sets out for exactly this
    reason. Add a second test proving that seeding is a no-op for a specification declaring no
    entrypoint.

    Do not touch `_INCIDENTAL_GAP_CODES`, `_EXPECTED_CAUGHT_DEFECTS` or any existing entry in
    `_TARGET_DEFECT_CODES` in this task. If a pre-existing fixture's exit code moves after the
    seeding helper lands, that is a regression to investigate, not a map to edit.
  </action>
  <verify>
    <automated>python -m unittest tests.test_known_bad_corpus -v</automated>
    <automated>python -c "import tests.test_known_bad_corpus as c; assert callable(c._seed_entrypoint); assert callable(c._own_target_codes); vals=[v for pts in c._TARGET_DEFECT_CODES.values() for v in pts.values()]; assert all(isinstance(v,(str,frozenset)) for v in vals), vals; assert all(isinstance(x,str) for slug in c._TARGET_DEFECT_CODES for x in c._own_target_codes(slug)); print('helper present and every target code is a string')"</automated>
    <automated>python -m unittest discover -s tests</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import tests.test_known_bad_corpus as c; assert callable(c._seed_entrypoint); print('helper present')"` exits 0.
    - `python -c "import inspect, tests.test_known_bad_corpus as c; src=inspect.getsource(c.TestKnownBadCorpus._gate_findings); assert '_seed_entrypoint' in src; print('seeding wired into the gate helper')"` exits 0.
    - A test method with a synthetic map asserts that a multi-code value flattens to its individual codes and a single-string value still returns that one code.
    - A test method asserts that seeding a specification with no declared entrypoint leaves the temporary directory empty.
    - `python -m unittest tests.test_known_bad_corpus` exits 0 with every pre-existing fixture's expectations unchanged.
    - `git diff tests/test_known_bad_corpus.py` shows no change to `_INCIDENTAL_GAP_CODES`, to `_EXPECTED_CAUGHT_DEFECTS`, or to any existing entry in `_TARGET_DEFECT_CODES`.
  </acceptance_criteria>
  <done>A fixture's own entrypoint is reachable from the corpus harness, and a fixture that demonstrates several codes at one gate point can say so.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the full-frame-cleaning fixture triple</name>
  <files>examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml, examples/known-bad/full-frame-cleaning-entrypoint.py, examples/known-bad/full-frame-cleaning-POSTMORTEM.md</files>
  <read_first>
    - `examples/known-bad/post-hoc-procedure-switch-ANALYSIS-SPEC.yaml` in full, 206 lines. Its
      header comment block states what blocks, at which gate point, why, and what the precondition
      for reaching that point is. Copy that structure, substituting this fixture's own narrative.
    - `examples/known-bad/post-hoc-procedure-switch-POSTMORTEM.md` in full — the post-mortem
      structure and the catch-attribution cross-reference into the corpus test module.
    - `examples/bad-ANALYSIS-SPEC.yaml` lines 85-150 — the only committed fixture with a model block
      and a reproducibility block. Its field roster is the basis for this fixture's model block, and
      its reproducibility block shows what an entrypoint declaration looks like here.
    - `examples/analysis/leaky_model.py`, 9 lines — the terse, directly-readable shape an
      intentionally-leaky entrypoint fixture takes in this repository.
    - `references/The AI Data Scientist.md`, sections 2.1 through 2.5 and the code samples in
      Tables 1 and 2. These are the idioms to transcribe. Read them rather than the research file's
      paraphrase.
    - Any one committed known-bad specification that currently exits 0 at both `dsx gate plan` and
      `dsx gate execute` — read its validity-frame, inference, design, metrics and data blocks in
      full. Those blocks are cloned wholesale so the new fixture trips only its own defect and not
      unrelated shape findings. Phase 11 added at least one field to every committed specification,
      so clone from the current file on disk, never from an example in a planning document.
  </read_first>
  <behavior>
    - `dsx validate --spec examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml` exits 0.
    - `dsx gate plan` against the fixture exits 0.
    - `dsx gate execute` against the fixture exits 1.
    - The entrypoint file contains, in this order: a full-frame imputation using a mean, a
      spread-based row filter, a cross-tabulation and statistical test referencing the declared
      target, a legitimate split marker, and a scaler fitted on the full frame after that split.
    - The specification declares a model algorithm, no selection ledger, and a whole-pipeline
      preprocessing boundary of training rows only.
    - The specification declares no cleaning block, so the per-step cleaning codes stay silent and
      the entrypoint scan is what catches the defect.
    - The post-mortem names at least one finding code in this repository's code format.
  </behavior>
  <action>
    Write the entrypoint file first, at `examples/known-bad/full-frame-cleaning-entrypoint.py`. Keep
    it terse and directly readable in the style of the existing leaky entrypoint. Transcribe the
    source paper's own stages in the paper's own order, with a comment naming the stage and the
    paper section above each: the cleaning stage imputes a numeric column with its own full-frame
    mean and filters rows by a spread statistic; the hypothesis stage cross-tabulates a candidate
    feature against the outcome column and runs a chi-square test on the table; then a legitimate
    split; then the preprocessing stage fits a scaler on the full frame rather than the training
    frame. Put a header comment naming the paper, its identifier and the tables the code came from.

    The split marker must sit below the cleaning and the hypothesis stages and above the scaler
    call. That ordering is the whole point of the fixture: this is the shape that passed.

    Write the specification at `examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml`. Clone
    the validity-frame, inference, design, metrics and data blocks from a committed fixture that
    currently exits 0 at plan and execute, following the full-shape-clone practice this corpus
    already uses so that only the target defect fires. Adapt the narrative fields to a customer
    churn prediction so the file reads as one analysis rather than a graft.

    Its model block declares the task, the outcome column the entrypoint references, an algorithm, a
    split strategy consistent with the entrypoint, a features list, a prediction-time definition, a
    positive rate, a whole-pipeline preprocessing boundary of training rows only, a primary metric
    that is not in the imbalance-unsafe set, a baseline, and a threshold basis that is not the test
    set. Every one of those is chosen so that only the intended codes fire: the honest boundary is
    what makes the fixture's point, and the metric and threshold choices are what keep unrelated
    codes out of it.

    It declares no selection ledger, so the ledger code fires. It declares no cleaning block, so the
    per-step cleaning codes stay silent — the requirement's own claim is that the entrypoint scan
    catches this without any new declaration. It reports no model score and no baseline score, so
    the score-provenance codes stay silent.

    Its reproducibility block declares the entrypoint by its bare filename, so the harness's seeding
    helper places it at the same relative path in the temporary directory. Declare a random seed.
    Declare nothing else whose absence or presence you have not measured.

    Give the file the same header comment shape the post-hoc fixture uses: what it clears, what it
    blocks, at which gate points, the encoded defect in one plain paragraph, and the two commands a
    reader can run to see it for themselves.

    Now measure. Run the validate command and the four gate points against the fixture and record
    every exit code and every CRITICAL and HIGH finding code in the summary. Do not predict them.
    If plan does not exit 0, the cloned blocks are incomplete — fix the fixture, never the map or
    the check.

    Write the post-mortem last, at `examples/known-bad/full-frame-cleaning-POSTMORTEM.md`, from the
    measurements. It states what the pipeline did, why every honest declaration in the specification
    was true, what the gate saw before this phase and what it sees now, and which code caught it at
    which gate point. Name the finding codes explicitly — the corpus test asserts a post-mortem names
    at least one. Cross-reference the catch-attribution entry in the corpus test module by name, the
    way the existing post-mortems do.

    Every exit code and every finding code in the post-mortem is copied from a real run. A predicted
    number in a document that exists to record what happened is the drift class this project's
    honesty controls exist to catch.
  </action>
  <verify>
    <automated>python -c "import subprocess,sys; s='examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml'; r=subprocess.run([sys.executable,'-m','dsx.cli','validate','--spec',s],capture_output=True); assert r.returncode==0, r.stderr.decode()[:2000]; print('fixture passes validate')"</automated>
    <automated>python -c "import subprocess,sys,json,tempfile,os,shutil; s='examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml'; d=tempfile.mkdtemp(); shutil.copy('examples/known-bad/full-frame-cleaning-entrypoint.py', os.path.join(d,'full-frame-cleaning-entrypoint.py')); r=subprocess.run([sys.executable,'-m','dsx.cli','gate','execute','--spec',s,'--phase-dir',d,'--json'],capture_output=True); out=(r.stderr or r.stdout).decode(); rep=json.loads(out); crit={f['code'] for f in rep['findings'] if f['severity']=='CRITICAL'}; assert r.returncode==1, (r.returncode, sorted(crit)); assert 'DSX-CODE-020' in crit, sorted(crit); print('blocks execute on', sorted(crit))"</automated>
    <automated>python -c "import subprocess,sys,tempfile; s='examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml'; d=tempfile.mkdtemp(); r=subprocess.run([sys.executable,'-m','dsx.cli','gate','plan','--spec',s,'--phase-dir',d],capture_output=True); assert r.returncode==0, (r.returncode, (r.stderr or r.stdout).decode()[:3000]); print('clears plan')"</automated>
    <automated>python -c "import io,re; t=io.open('examples/known-bad/full-frame-cleaning-POSTMORTEM.md',encoding='utf-8').read(); assert re.search(r'DSX-[A-Z]+-\d{3}', t); print('post-mortem names a finding code')"</automated>
  </verify>
  <acceptance_criteria>
    - The four automated commands above all exit 0.
    - `python -c "import io; t=io.open('examples/known-bad/full-frame-cleaning-entrypoint.py',encoding='utf-8').read(); lines=t.splitlines(); imp=next(i for i,l in enumerate(lines) if 'fillna' in l); spl=next(i for i,l in enumerate(lines) if 'train_test_split' in l); fit=next(i for i,l in enumerate(lines) if 'fit_transform' in l); assert imp < spl < fit, (imp,spl,fit); print('cleaning above the split, scaler below it')"` exits 0.
    - `python -c "from dsx.loader import load; s=load('examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml'); m=s['model']; assert m.get('preprocessing_fit_on')=='train_only'; assert m.get('algorithm'); assert 'selection_ledger' not in m; assert all('cleaning' not in d for d in (s.get('data') or [])); print('honest boundary, declared algorithm, no ledger, no cleaning block')"` exits 0.
    - The summary records the measured exit code and the measured CRITICAL and HIGH finding codes for validate and for all four gate points.
    - `python -m unittest discover -s tests` exits 0 or fails only on the corpus map entries that task 3 adds; say which in the summary.
  </acceptance_criteria>
  <done>The reproduction is a committed fixture that clears plan, blocks execute, and carries a post-mortem written from measured output.</done>
</task>

<task type="auto">
  <name>Task 3: Record the catch attribution, account for every blocking finding, and close the phase's catalogue currency</name>
  <files>tests/test_known_bad_corpus.py, references/finding-codes.md</files>
  <read_first>
    - `tests/test_known_bad_corpus.py` as it stands after task 1 — `_INCIDENTAL_GAP_CODES` with its
      per-entry comment style, `_TARGET_DEFECT_CODES`, `_EXPECTED_CAUGHT_DEFECTS`, and the two tests
      that consult them: the one asserting a fixture blocks only on its target defect at the
      critical-threshold points, and the one asserting every blocking finding at ship is documented.
    - `tests/test_known_bad_corpus.py` — the test asserting the expected-caught map's keys match the
      corpus on disk. A new fixture with no key there fails it, which is why an empty entry is still
      an entry.
    - The measurements recorded in task 2's summary — the CRITICAL and HIGH finding codes the fixture
      produces at each gate point. Every entry written here comes from those numbers.
    - `dsx/cli.py` lines 100-113 — the gate profiles, to confirm from the source which check families
      run at which point and therefore why this fixture's entry is scoped to the execute point.
  </read_first>
  <behavior>
    - `_TARGET_DEFECT_CODES` carries a `full-frame-cleaning` entry at the execute gate point holding
      every CRITICAL code the fixture exists to demonstrate.
    - `_EXPECTED_CAUGHT_DEFECTS` carries a `full-frame-cleaning` key with an empty container, with a
      comment saying why: this map applies at both critical-threshold points and the fixture's check
      families are absent from the plan profile.
    - Every CRITICAL and HIGH finding the fixture produces at ship is either one of its own target
      codes or a member of `_INCIDENTAL_GAP_CODES`.
    - Any code newly added to `_INCIDENTAL_GAP_CODES` carries an inline comment naming the
      corpus-completeness gap that causes it, in the existing per-entry style, and is not one of this
      fixture's own defect codes.
    - `python -m unittest tests.test_known_bad_corpus` exits 0.
    - `python scripts/gen-finding-catalogue.py --check` exits 0 and `./scripts/check.sh` exits 0.
  </behavior>
  <action>
    Add the fixture's entry to `_TARGET_DEFECT_CODES` at the execute gate point, using the
    multi-code container shape task 1 enabled, holding every CRITICAL code the measurement in task 2
    actually produced. Add a comment above or beside it naming which plan shipped each of those
    codes and stating why the entry is scoped to execute: the entrypoint check family and the
    machine-learning check family are both absent from the plan gate profile, so there is nothing to
    catch at plan.

    Add the fixture's key to `_EXPECTED_CAUGHT_DEFECTS` with an empty container and a comment saying
    it is empty by design rather than by omission, in the same voice the post-hoc fixture's entry
    already uses for the same reason.

    Run the corpus test module and read every failure rather than guessing. For each CRITICAL or HIGH
    code the ship-completeness test reports as undocumented, decide honestly which of two things it
    is. If it is one of the fixture's own defects, it belongs in the target map. If it is a
    corpus-completeness gap — an artefact the corpus does not commit, a field this corpus never fills
    — it belongs in `_INCIDENTAL_GAP_CODES` with an inline comment naming that gap specifically, not
    the semantic defect. Putting a fixture's own defect in the incidental list is the misuse a
    dedicated test in that module already forbids.

    If a pre-existing fixture's exit code moved, stop and investigate. That is a regression from one
    of this phase's checks or from the seeding helper, and the fix is in the check or the helper,
    never in the map.

    Add a dedicated test for this fixture in the positive direction, in the shape the two existing
    dedicated fixture tests use. It runs the gate at the execute point, asserts exit code 1, asserts
    every recorded target code is among the CRITICAL findings, and asserts that the entrypoint check
    produces no finding at the plan point because it is not registered there. The generic test does
    not supply this proof for a point-scoped entry, which is the gap a previous phase's review named
    for the other point-scoped fixture in this corpus.

    Finally, close the phase's currency obligations. Run the catalogue script's check flag; if it
    reports the catalogue is stale, regenerate it with the write flag and commit the result. Run the
    full check script and confirm every step is green. The requirement this task closes alongside the
    fixture asks that fixtures, tests and catalogue all be current at the end of the phase, and this
    is where that is measured.
  </action>
  <verify>
    <automated>python -m unittest tests.test_known_bad_corpus -v</automated>
    <automated>python -c "import tests.test_known_bad_corpus as c; assert 'full-frame-cleaning' in c._TARGET_DEFECT_CODES; assert 'execute' in c._TARGET_DEFECT_CODES['full-frame-cleaning']; assert 'full-frame-cleaning' in c._EXPECTED_CAUGHT_DEFECTS; own=c._own_target_codes('full-frame-cleaning'); assert own and all(isinstance(x,str) for x in own), own; assert not (own & set(c._INCIDENTAL_GAP_CODES)), own & set(c._INCIDENTAL_GAP_CODES); print('attribution recorded:', sorted(own))"</automated>
    <automated>python scripts/gen-finding-catalogue.py --check</automated>
    <automated>python -m unittest discover -s tests</automated>
  </verify>
  <acceptance_criteria>
    - The four automated commands above all exit 0.
    - `./scripts/check.sh` exits 0, and the summary pastes its output.
    - A dedicated test method for this fixture asserts exit code 1 at the execute point with every recorded target code among the CRITICAL findings, and asserts no entrypoint-family finding at the plan point.
    - Every code added to `_INCIDENTAL_GAP_CODES` by this task, if any, carries an inline comment naming the corpus-completeness gap that causes it, and none of them is one of this fixture's own target codes.
    - `git diff tests/test_known_bad_corpus.py` shows no existing entry in any of the three maps deleted or changed in meaning.
    - The summary states, from a measured run, the fixture's exit code at all four gate points and the full set of CRITICAL and HIGH codes at ship, with each one attributed to either the target map or the incidental list.
  </acceptance_criteria>
  <done>The corpus records what catches this fixture and where, every blocking finding is attributed, and the catalogue, the fixtures and the tests are all current.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture file to gate | The new fixture is a committed input the whole corpus guarantee is measured against. A fixture that blocks for the wrong reason proves nothing. |
| test harness to corpus guarantee | The harness decides what the gate can see. Extending it changes what every fixture's exit code means, not only the new one's. |
| post-mortem prose to public claim | The post-mortem states what the gate did. A predicted number there becomes a claim this project makes in public. |
| entrypoint fixture to test execution | A Python file is committed under the examples tree. Nothing must execute it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.1-38 | Tampering | `_INCIDENTAL_GAP_CODES` absorbing the fixture's own defect | high | mitigate | Task 3's action states the two-way decision explicitly, its automated check asserts the fixture's own codes are disjoint from the incidental list, and a dedicated test already in that module forbids the misuse independently. |
| T-11.1-39 | Tampering | a pre-existing fixture's exit code moving unnoticed | high | mitigate | Task 1 forbids editing any existing map entry and asserts that by diff; task 3 instructs stopping and investigating rather than editing. The corpus test module is the detection mechanism and it runs in all three tasks' verification. |
| T-11.1-40 | Spoofing | a post-mortem stating a predicted rather than measured result | high | mitigate | Task 2's action requires the measurements before the post-mortem is written, and the acceptance criteria require the measured exit codes and finding codes in the summary. The prohibition against unmeasured claims is recorded in this plan's `must_haves.prohibitions`. |
| T-11.1-41 | Elevation of privilege | the committed entrypoint fixture being executed | high | mitigate | Nothing in the gate path executes an entrypoint; the entrypoint check reads it as text and the reproducibility check tests only for existence. The file imports libraries that need not be installed, which is safe precisely because it is never run. Confirm by reading the entrypoint check's source rather than assuming. |
| T-11.1-42 | Denial of service | the seeding helper copying an unexpected path | medium | mitigate | The helper copies only when a file of the declared name exists beside the specification, and the fixture declares a bare filename with no directory component. Task 1's behaviour list names the no-entrypoint and missing-file cases as no-ops. |
| T-11.1-43 | Repudiation | catch attribution not recorded with the fixture | high | mitigate | The whole of task 3 exists to record it, and the dedicated positive-direction test proves the attribution against a real gate run rather than against the map alone. |
| T-11.1-SC | Tampering | package-manager installs | high | accept | This plan installs no package in any ecosystem. The committed entrypoint fixture names libraries in import statements but is never executed and no dependency is added to any lockfile. The package-legitimacy gate is skipped by its own trigger condition and no checkpoint applies. |
</threat_model>

<verification>
- `python -m unittest discover -s tests` exits 0.
- `python -m unittest tests.test_known_bad_corpus -v` exits 0.
- `python scripts/gen-finding-catalogue.py --check` exits 0.
- `./scripts/check.sh` exits 0, with its output pasted into the summary.
- The fixture's exit codes at all four gate points and its full CRITICAL and HIGH finding sets at
  ship are recorded in the summary from measured runs.
- No pre-existing fixture's exit code has moved.
</verification>

<success_criteria>
- The full-frame-cleaning fixture exists as a specification, a post-mortem and an entrypoint, clears
  plan, and blocks execute.
- The corpus harness can resolve any fixture's declared entrypoint, and no pre-existing fixture is
  affected.
- The catch attribution is recorded at the execute gate point and proved by a dedicated
  positive-direction test.
- Every CRITICAL and HIGH finding the fixture produces at ship is attributed, and nothing of the
  fixture's own is laundered into the incidental list.
- The finding catalogue, the fixtures and the tests are all current, and the full check script is
  green.
</success_criteria>

<output>
Create `.planning/phases/11.1-generated-pipeline-reality/11.1-08-SUMMARY.md` when done.
</output>
