---
phase: 11.1-generated-pipeline-reality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - references/leakage-taxonomy.md
  - agents/dsx-ml-integrity-auditor.md
autonomous: true
requirements: [REQ-P11.1-08]

must_haves:
  truths:
    - "`references/leakage-taxonomy.md` type 3 carries an ensemble sub-case that names type 7 explicitly, stating that a large candidate roster searched by grid or random search and then combined into a stacking or voting ensemble is simultaneously a preprocessing decision surface and an iterative selection informed by held-out data."
    - "`references/leakage-taxonomy.md` carries a discretisation note stating that binning a continuous column is information loss, and that binning chosen because a full-frame hypothesis test found a relationship compounds a target-adjacent feature with a preprocessing leak in one step."
    - "`agents/dsx-ml-integrity-auditor.md` carries a motivating-statistic question in its leakage-heuristics block: whether a reported statistic or p-value came from a test run against the target on the full, unsplit frame."
    - "`agents/dsx-ml-integrity-auditor.md` carries a large-roster heuristic in the same block: a wide candidate roster searched by grid or random search and then ensembled, with no declared selection basis and no nested protocol."
    - "Both new agent-prompt entries are numbered, one-line, plain-declarative statements in the existing style, and the closing disclaimer sentence that none of the signals prove leakage is preserved."
    - "No file under `dsx/`, `tests/`, `examples/`, `scripts/` or `templates/` is edited by this plan, so every gate exit code and the full test suite are unchanged."
    - "Neither addition names a finding code that does not yet exist — the prose describes the phenomenon, and the codes that catch it are wired by later plans."
  artifacts:
    - references/leakage-taxonomy.md
    - agents/dsx-ml-integrity-auditor.md
  key_links:
    - "The type-3 ensemble sub-case and the selection-ledger checks in plan 11.1-07 describe the same failure. The prose is what the auditor agent reads; the ledger is what the gate reads. They must name the same thing — a selection made on data that also produced the reported score."
    - "The motivating-statistic question and DSX-CODE-030 in plan 11.1-03 are the judgment-layer and deterministic-layer halves of one check. The agent asks where a statistic came from; the scan reads whether the call touched the declared target."
  prohibitions:
    - statement: "MUST NOT turn the discretisation note into a new numbered leakage type. The requirement asks for a note; a new type renumbers a reference document that other prose and docstrings cite by number."
      flagged: true
    - statement: "MUST NOT ship a docstring or prose citation whose page, section or theorem locator was not read from the paper itself. An unconfirmed locator carries an explicit unverified marker on the same line and is never presented as confirmed."
      flagged: true
    - statement: "MUST NOT weaken, delete or rewrite an existing corpus expectation to absorb a change caused by this phase. This plan edits no test and no fixture, and that is the point."
      flagged: true
    - statement: "MUST NOT let a misspelled or out-of-vocabulary value clear a gate that a blank value would have blocked."
      flagged: true
---

<objective>
Write down the two leakage phenomena this phase's checks exist to catch, in the two documents a
human or an agent reads before the gate ever runs: the reference taxonomy and the machine-learning
integrity auditor's prompt.

Purpose: the gate catches what it can compute. The ensemble-plus-large-roster case and the
discretisation case are judgment calls, not computations, and the milestone's own architectural
boundary keeps judgment out of the deterministic check modules. Writing them down is how they get
caught at all.

Output: one sub-case and one note added to `references/leakage-taxonomy.md`, and two numbered
signals added to `agents/dsx-ml-integrity-auditor.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-generated-pipeline-reality/11.1-RESEARCH.md
@.planning/phases/11.1-generated-pipeline-reality/11.1-PATTERNS.md
</context>

## Artifacts this phase produces

Every symbol below is created by Phase 11.1 and does not exist in the codebase before it. The plan
that creates each one is named in brackets.

- Finding codes: `DSX-CODE-020` CRITICAL [01], `DSX-CODE-021` CRITICAL [01], `DSX-CODE-030`
  CRITICAL [03], `DSX-CODE-031` HIGH [03], `DSX-ML-043` HIGH [04], `DSX-ML-023` CRITICAL [05],
  `DSX-ML-024` HIGH [05], `DSX-ML-052` HIGH [06], `DSX-ML-053` MEDIUM [06], `DSX-ML-090` HIGH [07],
  `DSX-ML-091` CRITICAL [07], `DSX-ML-092` HIGH [07]
- Constants in `dsx/checks/code.py`: `TRAINING_FRAME_NAMES`, `FULL_FRAME_IMPUTE_RE`,
  `FULL_FRAME_SPREAD_FILTER_RE`, `FIT_CALL_RE` [01]; `STAT_TEST_CALL_RE` [03]
- Functions in `dsx/checks/code.py`: `_first_full_frame_cleaning_line`, `_fit_call_arguments`,
  `_is_training_frame` [01]; `_stat_test_lines_referencing` [03]
- Constants in `dsx/checks/ml.py`: `TRAIN_ONLY_FIT_VALUES` [05], `SCORE_SOURCES` [06],
  `SELECTION_BASES` [07]
- Functions in `dsx/checks/ml.py`: `_check_prediction_time_definition` [04], `_check_cleaning` [05],
  `_check_selection_ledger` [07]
- ANALYSIS-SPEC fields, all new and all optional: `data[].cleaning` as a list of
  `{column, method, fit_on}` [05]; `results.model_score_source` and `results.fold_scores` [06];
  `model.selection_ledger` as `{candidates_evaluated, configurations_tried, selected_on}` [07]
- Fixture files: `examples/known-bad/full-frame-cleaning-ANALYSIS-SPEC.yaml`,
  `examples/known-bad/full-frame-cleaning-POSTMORTEM.md`,
  `examples/known-bad/full-frame-cleaning-entrypoint.py` [08]
- Test classes: `tests/test_dsx.py::TestPhase11_1Code` [01], `tests/test_dsx.py::TestPhase11_1ML` [04]
- Harness surface in `tests/test_known_bad_corpus.py`: the `_seed_entrypoint` helper, the widened
  `_TARGET_DEFECT_CODES` value type, and frozenset flattening in `_own_target_codes` [08]
- Registry entries: twelve new codes in `scripts/gen-finding-catalogue.py::_D05_ALLOWLIST_CODES`;
  a `full-frame-cleaning` entry in `_TARGET_DEFECT_CODES` and in `_EXPECTED_CAUGHT_DEFECTS` [08]
- Documentation: the type-3 ensemble sub-case and the discretisation note in
  `references/leakage-taxonomy.md`, and two new signals in `agents/dsx-ml-integrity-auditor.md` [02]

## Flagged assumptions

- The edge probe classified REQ-P11.1-08 as unclassified, so no edge predicate was derived for it
  mechanically. The assumption this plan proceeds under: a prose requirement has no data-shape
  boundary to probe, and its acceptance criteria are content assertions over two files. That is an
  assumption, not a probe result.
- `11.1-RESEARCH.md` open question 3 asks whether the discretisation note is documentation only or
  also a finding-text addition. This plan takes it as documentation only, on the requirement's own
  wording, which groups it with the ensemble sub-case rather than with the requirement's
  code-level clauses. If a later plan finds a concrete gate-checkable trigger, that is a new
  decision, not a correction of this one.

<tasks>

<task type="auto">
  <name>Task 1: Add the ensemble sub-case and the discretisation note to the leakage taxonomy</name>
  <files>references/leakage-taxonomy.md</files>
  <read_first>
    - `references/leakage-taxonomy.md` in full. Specifically the numbered type entries at lines
      18-45 — read type 3 and type 7 verbatim, because the sub-case cross-references type 7 by
      number and must not contradict what that entry already says — and the detection-signals
      section at lines 47-57.
    - `.planning/phases/11.1-generated-pipeline-reality/11.1-RESEARCH.md`, the section headed
      "Corpus Case Design", items 4 and 5. Item 4 is the eighteen-candidate roster and the
      stacking or voting ensemble. Item 5 is the paper's own statement that a continuous column was
      kept continuous because a full-frame hypothesis test found a relationship, and that binning
      would have been acceptable had it not.
    - `references/The AI Data Scientist.md`, sections 2.3 and 2.5. Read the two passages the
      research names rather than trusting the research's paraphrase of them.
  </read_first>
  <action>
    Append an ensemble sub-case to the type 3 entry in `references/leakage-taxonomy.md`, in the
    same terse one-paragraph-per-item style the file uses throughout. It must state three things:
    that a wide candidate roster searched by grid or random search and then combined into a
    stacking or voting ensemble is a preprocessing decision surface, that the same act is an
    iterative selection informed by whatever data produced the comparison scores, and that this is
    the reason it is filed under type 3 with an explicit cross-reference to type 7 rather than
    given a type of its own. Name type 7 by its number and by its own title as that entry states
    it, so the cross-reference survives a reader who only reads one entry.

    Append a discretisation note in the same file. Placement is the executor's judgment between the
    tail of the type 3 entry and the detection-signals section; choose one and say which in the
    summary. Content: binning a continuous column discards information the model could have used,
    and binning chosen because a hypothesis test computed over the full unsplit frame found a
    relationship compounds a target-adjacent feature choice with a preprocessing leak in a single
    step. Cite the source concretely — the paper describes keeping a continuous age column
    continuous for exactly this reason while its own worked example elsewhere shows the binning
    idiom in active use.

    Do not add a new numbered leakage type. Other prose and docstrings cite these types by number,
    and renumbering is the failure mode `references/finding-codes.md` already documents for finding
    codes.

    Do not name any finding code. The codes that catch these phenomena do not exist yet when this
    plan runs, and a document naming a code that does not exist is the drift class this project's
    honesty controls exist to catch. Describe the phenomenon; later plans wire the codes.
  </action>
  <verify>
    <automated>python -c "import re,io; t=io.open('references/leakage-taxonomy.md',encoding='utf-8').read(); body='\n'.join(l for l in t.splitlines() if not l.lstrip().startswith('>')); low=body.lower(); assert 'ensemble' in low, 'no ensemble sub-case'; assert ('stacking' in low or 'voting' in low), 'ensemble sub-case names no ensemble kind'; assert 'type 7' in low, 'no explicit type 7 cross-reference'; assert ('binning' in low or 'discretis' in low or 'discretiz' in low), 'no discretisation note'; print('taxonomy additions present')"</automated>
    <automated>python -c "import re,io; t=io.open('references/leakage-taxonomy.md',encoding='utf-8').read(); found=sorted(set(re.findall(r'DSX-[A-Z]+-\d{3}',t))); assert not found, found; print('names no finding code')"</automated>
    <automated>python -m unittest discover -s tests</automated>
  </verify>
  <acceptance_criteria>
    - The type 3 entry in `references/leakage-taxonomy.md` contains a sentence naming a stacking or voting ensemble and a sentence cross-referencing type 7 by number.
    - The file contains a paragraph stating that binning a continuous column is information loss and that binning chosen on a full-frame test result compounds two leakage types.
    - The file names no finding code of the form used elsewhere in this repository, asserted by the second automated command.
    - The number of numbered leakage types in the file is unchanged from its pre-plan value.
    - `git status --porcelain` lists `references/leakage-taxonomy.md` as the only file changed by this task.
  </acceptance_criteria>
  <done>The taxonomy states the ensemble sub-case and the discretisation phenomenon, with no new numbered type and no forward reference to a code that does not exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add the motivating-statistic question and the large-roster heuristic to the auditor prompt</name>
  <files>agents/dsx-ml-integrity-auditor.md</files>
  <read_first>
    - `agents/dsx-ml-integrity-auditor.md` in full, and specifically the leakage-heuristics block at
      lines 61-73. Read the six existing numbered signals and the closing disclaimer sentence
      verbatim; the two additions copy their one-line plain-declarative form and the disclaimer
      must survive unchanged in meaning.
    - `references/leakage-taxonomy.md` as it stands after task 1, so the prompt and the taxonomy
      describe the same two phenomena in compatible words.
    - `.planning/phases/11.1-generated-pipeline-reality/11.1-RESEARCH.md`, the "Architectural
      Responsibility Map" row for the agent prompt. It records that this tier is advisory and
      deliberately separate from the deterministic check modules; the additions must not read as
      gate rules.
  </read_first>
  <action>
    Add two numbered signals to the leakage-heuristics block in `agents/dsx-ml-integrity-auditor.md`,
    continuing the existing numbering as signals seven and eight, in the same single-line
    plain-declarative style as the six already there.

    The first is the motivating-statistic question: a reported statistic or p-value that came from a
    test run against the outcome column on the full, unsplit frame, whose result then informed which
    features were kept. Phrase it as a signal to trace, not as a rule that fires.

    The second is the large-roster heuristic: a wide candidate roster searched by grid or random
    search and then combined into an ensemble, with no stated basis for the selection and no nested
    protocol separating the tuning data from the data the reported score came from.

    Keep the closing disclaimer sentence that none of these prove leakage and that all of them mean
    stop and trace. Extend it only if the two new signals need a different tracing instruction than
    the existing six; if they do not, leave it byte-identical.

    Do not name any finding code, and do not phrase either signal as a gate condition. This file is
    the judgment layer; the deterministic layer is `dsx/checks/`, and the milestone's architectural
    boundary keeps them apart on purpose.
  </action>
  <verify>
    <automated>python -c "import io,re; t=io.open('agents/dsx-ml-integrity-auditor.md',encoding='utf-8').read(); m=re.search(r'<leakage_heuristics>(.*?)</leakage_heuristics>', t, re.S); assert m, 'leakage_heuristics block not found'; block=m.group(1); nums=re.findall(r'^\s*(\d+)\.\s', block, re.M); assert len(nums)>=8, nums; low=block.lower(); assert ('p-value' in low or 'statistic' in low), 'no motivating-statistic signal'; assert ('roster' in low or 'candidate' in low), 'no large-roster signal'; assert 'ensemble' in low, 'large-roster signal names no ensemble'; print('signals present:', len(nums))"</automated>
    <automated>python -c "import io,re; t=io.open('agents/dsx-ml-integrity-auditor.md',encoding='utf-8').read(); found=sorted(set(re.findall(r'DSX-[A-Z]+-\d{3}',t))); assert not found, found; print('names no finding code')"</automated>
    <automated>python -m unittest discover -s tests</automated>
  </verify>
  <acceptance_criteria>
    - The leakage-heuristics block contains at least eight numbered signals, asserted by the first automated command.
    - Signal seven names a statistic or p-value sourced from a test against the outcome on the full unsplit frame.
    - Signal eight names a candidate roster, a grid or random search, and an ensemble.
    - The closing disclaimer sentence stating that none of these prove leakage is still present in the block.
    - `git status --porcelain` lists `agents/dsx-ml-integrity-auditor.md` as the only file changed by this task.
    - `python -m unittest discover -s tests` exits 0 and its test count is unchanged from before this plan.
  </acceptance_criteria>
  <done>The auditor prompt asks where a motivating statistic came from and flags an unledgered large-roster search, in the file's own advisory voice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference prose to agent judgment | `references/leakage-taxonomy.md` and `agents/dsx-ml-integrity-auditor.md` are read by a language model that then makes calls about someone's analysis. Wrong or overstated prose becomes a wrong verdict with no compiler to catch it. |
| documentation to shipped claim | Both files are committed and public. A citation or a claim here is an assertion this project makes about the literature. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.1-07 | Spoofing | prose attribution to the source paper | medium | mitigate | Both additions are written after reading sections 2.3 and 2.5 of `references/The AI Data Scientist.md` directly, named in task 1's read-first list, rather than from the research file's paraphrase. Any locator that cannot be confirmed carries an explicit unverified marker. |
| T-11.1-08 | Tampering | forward reference to a finding code that does not exist yet | medium | mitigate | Both tasks assert, by executable check, that neither file names a finding code. A document naming a code that has not shipped is the drift class this project's honesty controls exist to catch. |
| T-11.1-09 | Elevation of privilege | advisory prose read as a gate rule | low | accept | The additions are phrased as signals to trace, and the file's own closing disclaimer that none of them prove leakage is preserved. The architectural boundary between the judgment tier and the deterministic tier is unchanged by this plan. |
| T-11.1-10 | Denial of service | none | low | accept | This plan edits two Markdown files and adds no executable path. |
| T-11.1-SC | Tampering | package-manager installs | high | accept | This plan installs no package in any ecosystem. The package-legitimacy gate is skipped by its own trigger condition and no checkpoint applies. |
</threat_model>

<verification>
- `python -m unittest discover -s tests` exits 0 with an unchanged test count — this plan touches no
  test, no fixture and no module.
- `./scripts/check.sh` exits 0.
- `git diff --stat` lists exactly two files: `references/leakage-taxonomy.md` and
  `agents/dsx-ml-integrity-auditor.md`.
- Neither file names a finding code, asserted by the executable check in both tasks.
</verification>

<success_criteria>
- The type 3 entry in the leakage taxonomy carries an ensemble sub-case that cross-references type 7
  by number.
- The leakage taxonomy carries a discretisation-is-information-loss note and no new numbered type.
- The auditor prompt carries a motivating-statistic question and a large-roster heuristic as
  numbered signals seven and eight, with the closing disclaimer intact.
- No code, test, fixture, template or script is edited, and the full suite and gate contract are
  unchanged.
</success_criteria>

<output>
Create `.planning/phases/11.1-generated-pipeline-reality/11.1-02-SUMMARY.md` when done.
</output>
