---
status: complete
phase: 11.1-generated-pipeline-reality
source: [11.1-VERIFICATION.md]
started: 2026-08-20T22:30:00Z
updated: 2026-08-21T00:41:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Resolve plan 11.1-07's backstop truth on emission-order stability for the selection-ledger codes
expected: A human decision: accept as vacuously true and retire/reword the must_have, or add a regression test that pins the mutual-exclusivity invariant explicitly. (Why human: `verification: backstop` — non-inferable per the honest-verifier contract; the verifier must abstain rather than silently pass.)
result: pass
decision: "Add the regression test; leave the executed plan's wording untouched."
resolution: |
  The backstop truth is vacuously satisfied. `_check_selection_ledger` (dsx/checks/ml.py:941-993)
  resolves DSX-ML-090/091/092 through a single if/elif/elif/else chain, so at most one of the
  three can fire per report and there is no emission order to stabilise. Verified by reading the
  source directly, not inferred from the plan text.

  Rather than accept that as an undocumented property of the current branch shape, two tests were
  added to tests/test_dsx.py::TestPhase11_1ML pinning the invariant:
    - test_at_most_one_selection_ledger_code_fires_across_the_branch_space — exhaustive sweep of
      180 combinations (algorithm declared/blank × candidates × configurations × all five
      SELECTION_BASES members plus blank, whitespace, mixed case, and an out-of-vocabulary
      misspelling), asserting at most one ledger code is ever emitted.
    - test_incomplete_ledger_selected_on_test_emits_only_090 — the exact "more than one condition
      holds at once" case the backstop truth names (incomplete ledger AND selected_on: test),
      asserting the chain resolves it to DSX-ML-090 alone.

  Mutation-tested to confirm the tests have teeth: temporarily changing `elif basis == "test"` to
  a plain `if` (the realistic future regression) made the focused test report
  ['DSX-ML-090', 'DSX-ML-091'] against an expected ['DSX-ML-090'] and failed 22 sweep subtests.
  ml.py was restored byte-identical (git status clean) and the suite re-ran green.

  Suite after the change: 806 tests OK (804 baseline + 2); ./scripts/check.sh exits 0 with all
  four gate-contract and determinism checks passing.

  The executed plan 11.1-07 was deliberately NOT reworded — an executed plan is the audit record
  of what was committed to before execution, and editing it after the fact weakens that trail.

### 2. Review 11.1-REVIEW.md's four CRITICAL findings (CR-01…CR-04) and decide remediation priority
expected: A human decision on remediation priority — immediate follow-up plan before Phase 11.2/11.3, or accepted tracked debt. All four were independently reproduced by the verifier: FULL_FRAME_SPREAD_FILTER_RE is cubic and FULL_FRAME_IMPUTE_RE quadratic in line length (real DoS risk on long entrypoint lines); `model.fit (df)` with an extra space bypasses pre-existing DSX-CODE-001; `model.fit(X=data, y=target)` keyword-argument calls are invisible to the new DSX-CODE-021 — directly relevant to this phase's own threat model (LLM-generated pipelines idiomatically use keyword arguments). None falsify a must_have as literally worded; the corpus fixture is unaffected.
result: pass
decision: "Option A — raise one gap-closure plan before Phase 11.2 covering CR-01, CR-02, CR-03, CR-04 and WR-01; WR-02 and IN-01 recorded as tracked debt."
resolution: |
  All seven review findings were independently reproduced before this decision was taken
  (12-agent verification pass; per-finding evidence in the phase transcript). None were refuted.

  Measured directly in this session, not carried over from the review:
    - FULL_FRAME_SPREAD_FILTER_RE fitted growth exponent 2.99 (cubic): 100 chars 0.0028s,
      200 chars 0.0214s, 400 chars 0.1690s, 800 chars 1.4324s.
    - FULL_FRAME_IMPUTE_RE fitted growth exponent 2.00 (quadratic): 40,000 chars 1.6233s.
    - The `code` check runs inside the blocking `execute` gate profile (dsx/cli.py:102), plus
      `verify` and `ship` — so the stall is reachable through a gate that blocks work.
    - ROADMAP confirms Phases 11.2 and 11.3 depend on Phases 4/6/7/10 and do not touch
      dsx/checks/code.py, so deferring would not compound inside those phases. Phase 12
      ("Calibration — measured catch rate and FPR") does compute over this code.

  Three corrections to 11.1-REVIEW.md's framing, carried forward into the fix plan:
    - CR-01 penalises CLEAN files, not leaky ones: the scan stops at the first match, so a
      leaky file is diagnosed immediately and a correct file pays the full cubic cost.
    - CR-03 is not "a single space" — any whitespace defeats it (space, double space, tab,
      backslash line continuation).
    - CR-04 is wider than keyword arguments: 9 of 12 genuinely leaky variants were missed
      end-to-end, including auto-formatted multi-line calls and chained method calls. Its
      stated mechanism in the review is also wrong (the pattern does tolerate whitespace after
      the parenthesis; the match dies at the `=` character).

  Decision: Option A. Fix CR-01+CR-02 (patterns and the false "linear" comment at
  code.py:53-60, which a newer pattern at code.py:99 already cites as the house template),
  CR-03 (delete the redundant literal re-check at code.py:505), and CR-04+WR-01 (shared
  first-argument extraction logic) in one plan before Phase 11.2 begins. Roughly 15-25 lines
  in one file plus four tests, including a timing test with a threshold that fails today's code.

  Deferred as tracked debt in deferred-items.md: WR-02 (decision-trail line endings) and
  IN-01 (dead split markers, three entries not one, with a documented fix trap).

  Not a blocker for this phase: no must_have truth, roadmap success criterion, or requirement
  ID is falsified by any of these. Phase 11.1 closes on its declared criteria.

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — both items resolved by human decision; no code defect reported]
