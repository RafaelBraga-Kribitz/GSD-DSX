---
status: complete
phase: 11.1.1-detection-code-hardening-inserted
source: [11.1.1-01-SUMMARY.md, 11.1.1-02-SUMMARY.md, 11.1.1-03-SUMMARY.md, 11.1.1-04-SUMMARY.md, 11.1.1-05-SUMMARY.md, 11.1.1-VERIFICATION.md]
started: 2026-08-22T04:50:00Z
updated: 2026-08-22T05:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. WR-02 -- ship or close the declared-but-unscannable entrypoint's missing decision record
expected: |
  A human decision on whether a declared-but-unscannable entrypoint producing zero
  DecisionRecord entries (so `dsx explain` can replay nothing for that judgment) and a pass
  line invisible without `--verbose` is an acceptable known gap to ship, or requires a new
  closure plan implementing the `scan_path:not-scanned:<reason>` decision input that
  11.1.1-AST-DESIGN.md's blocker table specified.
why_human: |
  Self-flagged by plan 03's own test (test_unscannable_entrypoint_also_produces_no_decision_record)
  as a possible design/ship gap; explicitly a product decision, not a code defect, per the
  phase's own review. Carried forward unresolved by plans 04, 05, 06 and 07 by design.
evidence_reproduced_live: |
  Run during this UAT session against HEAD, not restated from the summaries:
    - `grep -c "scan_path:not-scanned" dsx/checks/code.py` -> 0 (also 0 excluding comment lines).
    - Unscannable entrypoint (spec entrypoint = entry.txt, unsupported suffix):
        `dsx check code`            -> "code: PASS (blocking at HIGH) -- CRITICAL=0 HIGH=0 MEDIUM=0 LOW=0 INFO=0", exit 0
        `dsx check code --verbose`  -> "passed: entrypoint NOT scanned -- could not be read ... (no leak scan ran)"
        `dsx gate execute`          -> DECISIONS.jsonl has 4 records; NEITHER leak-scan record is present
        `dsx explain`               -> renders nothing about DSX-CODE-020/021/030/031
    - Scannable entrypoint (entry.py, same spec otherwise):
        `dsx check code --verbose`  -> "passed: entrypoint parsed with ast (call-level scan), entrypoint split marker at line 2"
        `dsx gate execute`          -> DECISIONS.jsonl has 6 records, including the DSX-CODE-020/021
                                       and DSX-CODE-030/031 rule records
        `dsx explain`               -> renders both leak-scan rules with citation and counterfactual
covers_coverage_ids: [11.1.1-05/D7 (partial -- WR-02 half)]
result: pass
decision: "Option B -- close WR-02 in code: implement the missing decision record only. Do NOT change default --verbose visibility (that was Option C, not chosen)."
resolution: |
  Decided, not yet implemented. `check()`'s early return on an unscannable-but-declared
  entrypoint must call `record_decision` with a `scan_path: not-scanned:<reason>` input
  before returning, exactly as 11.1.1-AST-DESIGN.md's blocker table (B4/B6) specified and as
  plans 03 through 07 each carried forward without implementing. Scope, stated explicitly so
  a new plan cannot silently widen it:
    - IN scope: the decision record only, so `DECISIONS.jsonl` gets an entry and `dsx explain`
      can replay this judgment. Regression tests following the pattern of
      test_unscannable_entrypoint_also_produces_no_decision_record (tests/test_dsx.py:7125),
      updated to assert a decision record now exists instead of asserting `decisions` is None.
    - OUT of scope: Report.render()'s --verbose gate. The bare `code: PASS ... CRITICAL=0` line
      stays the default output; that was Option C, not selected.
  This needs a new plan. It cannot be folded into 11.1.1-06-PLAN.md or 11.1.1-07-PLAN.md --
  both are already written, gate-passed, and each carries an explicit prohibition against
  touching the scan_path:not-scanned decision input (06-PLAN.md line 67, 07-PLAN.md line 60).
  It touches the same `_read_source` / `check()` region those two plans touch, so it must be
  sequenced to run after them, not concurrently, to avoid two plans editing the same lines.
  Raised as the next action once this UAT session completes -- see Gaps section below.

### 2. D7/D8 -- README honesty-tone judgment on the parser section
expected: |
  A human reader agrees README.md's heading "### The entrypoint leak scan now parses your
  code" plus the prose under it does not read, in isolation, as an affirmative soundness
  claim for DSX-CODE-021 or for the AST (Abstract Syntax Tree, Python's parsed-code
  representation) mechanism generally -- or requests a change.
why_human: |
  Named non-automatable by 11.1.1-VALIDATION.md's own Manual-Only Verifications table
  ("Requires reading prose for an overclaim"). Carried forward unresolved through plans 04
  and 05 by design.
evidence_reproduced_live: |
  Checked during this UAT session against HEAD:
    - The section runs README.md lines 154-284 (next heading "## What the gates actually
      catch" is line 285).
    - CORRECTION to 11.1.1-05-SUMMARY.md and 11.1.1-VERIFICATION.md: both describe the
      sentence "None of this makes `DSX-CODE-001`, `DSX-CODE-021`, or any other `DSX-CODE-*`
      check sound, complete or exhaustive" as "the section's closing sentence". It is not in
      this section. It is at README.md line 699, under "## Known limits" (line 438) ->
      "### What the entrypoint scan does not catch" (line 550) -- 415 lines after the section
      being judged ends. `sed -n '154,284p' README.md | grep -i "sound|exhaustive|not a
      verdict|judgement"` returns no match. The section's actual closing sentence is the
      remedy sentence at lines 279-281.
    - The disclaimer and the heading were both introduced in commit 82814d4 (plan 03). Plan
      05's README commit 1d5d612 did not touch the disclaimer -- that part of the summaries'
      claim is accurate.
covers_coverage_ids: [11.1.1-01/D9, 11.1.1-02/D7, 11.1.1-03/D8, 11.1.1-05/D7 (tone half)]
result: pass
decision: "Section holds up as written. No overclaim found; no change requested."

### 3. [11.1.1-01 D1] DSX-CODE-001 fires at CRITICAL on space/double-space/tab/backslash-continuation/multi-line-open-paren forms of a fit call before a split, and on the zero-whitespace form (preserved)
expected: DSX-CODE-001 fires at CRITICAL on space/double-space/tab/backslash-continuation/multi-line-open-paren forms of a fit call before a split, and on the zero-whitespace form (preserved)
result: pass
source: automated
coverage_id: D1
requirement: SC1
verification_refs: 6 passing

### 4. [11.1.1-01 D2] The two measured false positives (a module docstring mentioning a fit call, a notebook markdown cell describing leakage) no longer draw DSX-CODE-001
expected: The two measured false positives (a module docstring mentioning a fit call, a notebook markdown cell describing leakage) no longer draw DSX-CODE-001
result: pass
source: automated
coverage_id: D2
requirement: SC1
verification_refs: 2 passing

### 5. [11.1.1-01 D3] One line axis: _source_lines replaces str.splitlines() so AST-derived and text-derived line indices never desynchronise (form feed, U+2028 reproductions)
expected: One line axis: _source_lines replaces str.splitlines() so AST-derived and text-derived line indices never desynchronise (form feed, U+2028 reproductions)
result: pass
source: automated
coverage_id: D3
requirement: -
verification_refs: 2 passing

### 6. [11.1.1-01 D4] A skipped or degraded scan is never mistaken for a clean/parsed one: four channels (pass line, per-finding scan data, detail sentence, decision-record input), plus RecursionError/BOM/undecodable/unscannable handling
expected: A skipped or degraded scan is never mistaken for a clean/parsed one: four channels (pass line, per-finding scan data, detail sentence, decision-record input), plus RecursionError/BOM/undecodable/unscannable handling
result: pass
source: automated
coverage_id: D4
requirement: SC4
verification_refs: 6 passing

### 7. [11.1.1-01 D5] first_split is the union of the AST split-call index and the masked text-marker scan (an aliased split still counts; a docstring-only split marker no longer does)
expected: first_split is the union of the AST split-call index and the masked text-marker scan (an aliased split still counts; a docstring-only split marker no longer does)
result: pass
source: automated
coverage_id: D5
requirement: -
verification_refs: 2 passing

### 8. [11.1.1-01 D6] The new mechanism has its own measured, pinned performance bound; the DSX-CODE-002 quadratic (already over budget on the primary path) is fixed with a proof; PIPELINE_FIT_TRAIN_RE is made genuinely linear on its adversarial shape
expected: The new mechanism has its own measured, pinned performance bound; the DSX-CODE-002 quadratic (already over budget on the primary path) is fixed with a proof; PIPELINE_FIT_TRAIN_RE is made genuinely linear on its adversarial shape
result: pass
source: automated
coverage_id: D6
requirement: SC5
verification_refs: 5 passing

### 9. [11.1.1-01 D7] Full suite, known-bad corpus, catalogue check, capability validation and ./scripts/check.sh all green; no corpus fixture exit code moved; no file under examples/ or the corpus expectation maps touched
expected: Full suite, known-bad corpus, catalogue check, capability validation and ./scripts/check.sh all green; no corpus fixture exit code moved; no file under examples/ or the corpus expectation maps touched
result: pass
source: automated
coverage_id: D7
requirement: SC4
verification_refs: 5 passing

### 10. [11.1.1-01 D8] What remains uncaught after this plan is enumerated and pinned as executable scope boundaries carrying the promotion protocol, including the exec/eval REGRESSION named explicitly in the looser direction
expected: What remains uncaught after this plan is enumerated and pinned as executable scope boundaries carrying the promotion protocol, including the exec/eval REGRESSION named explicitly in the looser direction
result: pass
source: automated
coverage_id: D8
requirement: -
verification_refs: 3 passing

### 11. [11.1.1-02 D1] DSX-CODE-021 fires on every SC2 shape: keyword argument (model.fit(X=data, y=target) -> 'data'), fit_transform keyword (-> 'data[[\'Age\']]' exactly), partial_fit, chained-call argument (model.fit(loader.get_full_frame())), and a multi-line call reported at the line the call opens on
expected: DSX-CODE-021 fires on every SC2 shape: keyword argument (model.fit(X=data, y=target) -> 'data'), fit_transform keyword (-> 'data[[\'Age\']]' exactly), partial_fit, chained-call argument (model.fit(loader.get_full_frame())), and a multi-line call reported at the line the call opens on
result: pass
source: automated
coverage_id: D1
requirement: SC2
verification_refs: 5 passing

### 12. [11.1.1-02 D2] DSX-CODE-021 stays silent on a keyword training frame, an out-of-allowlist keyword (on BOTH the parsed and forced-fallback paths), and a constant first argument (42, 'x' on both paths; None on the AST path — see key-decisions for the fallback scoping)
expected: DSX-CODE-021 stays silent on a keyword training frame, an out-of-allowlist keyword (on BOTH the parsed and forced-fallback paths), and a constant first argument (42, 'x' on both paths; None on the AST path — see key-decisions for the fallback scoping)
result: pass
source: automated
coverage_id: D2
requirement: SC2
verification_refs: 4 passing

### 13. [11.1.1-02 D3] SC3: a semicolon-joined line's leaky second call fires (the safe first call no longer masks it); fit calls on one line are extracted in left-to-right source order, stably across two runs; a nested collision (m.fit(a).fit(b)) reports the textually-first token
expected: SC3: a semicolon-joined line's leaky second call fires (the safe first call no longer masks it); fit calls on one line are extracted in left-to-right source order, stably across two runs; a nested collision (m.fit(a).fit(b)) reports the textually-first token
result: pass
source: automated
coverage_id: D3
requirement: SC3
verification_refs: 3 passing

### 14. [11.1.1-02 D4] Both timing bounds are this mechanism's own measurements (not inherited): AST parse+walk+sort+unparse of every fit argument, and the widened fallback FIT_CALL_RE on its own adversarial shapes
expected: Both timing bounds are this mechanism's own measurements (not inherited): AST parse+walk+sort+unparse of every fit argument, and the widened fallback FIT_CALL_RE on its own adversarial shapes
result: pass
source: automated
coverage_id: D4
requirement: SC4
verification_refs: 2 passing

### 15. [11.1.1-02 D5] A notebook whose markdown cell describes leakage draws no finding, and notebook line numbers are unchanged in both trailing-newline shapes against the pre-change baseline (plan 01) — plus this plan's own additions: magics are repaired and take the AST path, a non-Python cell-magic body stays on the fallback (uncaught-by-design pin), degenerate notebook shapes (no cells, empty source, raw cell, malformed JSON) produce no finding and no exception, and a BOM'd .ipynb now takes the AST path instead of silently reading as NOT scanned
expected: A notebook whose markdown cell describes leakage draws no finding, and notebook line numbers are unchanged in both trailing-newline shapes against the pre-change baseline (plan 01) — plus this plan's own additions: magics are repaired and take the AST path, a non-Python cell-magic body stays on the fallback (uncaught-by-design pin), degenerate notebook shapes (no cells, empty source, raw cell, malformed JSON) produce no finding and no exception, and a BOM'd .ipynb now takes the AST path instead of silently reading as NOT scanned
result: pass
source: automated
coverage_id: D5
requirement: SC4
verification_refs: 6 passing

### 16. [11.1.1-02 D6] Exactly one extraction path exists per mechanism (FIT_CALL_RE.search count 0, .finditer count 1); all eight report.add calls with a DSX-CODE- literal resolve mechanically to check()'s FunctionDef; the six full-frame-cleaning symbols stay byte-identical; no corpus fixture exit code moved; full suite, corpus, catalogue check, capability validation and ./scripts/check.sh all green on both python 3.12.10 and python3 3.14.6
expected: Exactly one extraction path exists per mechanism (FIT_CALL_RE.search count 0, .finditer count 1); all eight report.add calls with a DSX-CODE- literal resolve mechanically to check()'s FunctionDef; the six full-frame-cleaning symbols stay byte-identical; no corpus fixture exit code moved; full suite, corpus, catalogue check, capability validation and ./scripts/check.sh all green on both python 3.12.10 and python3 3.14.6
result: pass
source: automated
coverage_id: D6
requirement: SC4
verification_refs: 5 passing

### 17. [11.1.1-03 D1] The re-derived uncaught-by-design set (10 new tests + 5 pre-existing from plans 01/02, all carrying the promotion protocol) replaces the regex-era three, which are promoted to firing tests (already done by plan 02)
expected: The re-derived uncaught-by-design set (10 new tests + 5 pre-existing from plans 01/02, all carrying the promotion protocol) replaces the regex-era three, which are promoted to firing tests (already done by plan 02)
result: pass
source: automated
coverage_id: D1
requirement: SC2
verification_refs: 1 passing

### 18. [11.1.1-03 D2] Non-regression pins for line-index stability, the line axis, notebook geometry, determinism/ordering, degraded-path signalling (including two measured gaps relative to AST-DESIGN's promise) and prose-mask boundaries
expected: Non-regression pins for line-index stability, the line axis, notebook geometry, determinism/ordering, degraded-path signalling (including two measured gaps relative to AST-DESIGN's promise) and prose-mask boundaries
result: pass
source: automated
coverage_id: D2
requirement: -
verification_refs: 1 passing

### 19. [11.1.1-03 D3] Every retained regex pattern carries a timing pin on its own worst-case shape, on both python 3.12.10 and python3 3.14.6; a mechanical D-05 structural guard replaces the reviewed-only invariant
expected: Every retained regex pattern carries a timing pin on its own worst-case shape, on both python 3.12.10 and python3 3.14.6; a mechanical D-05 structural guard replaces the reviewed-only invariant
result: pass
source: automated
coverage_id: D3
requirement: SC6
verification_refs: 1 passing

### 20. [11.1.1-03 D4] README.md announces the behaviour change in both directions and states what the scan does not catch, re-derived from AST-DESIGN section 8/9; the source comment above FIT_METHOD_NAMES names the same forms (key_link, verified by grep)
expected: README.md announces the behaviour change in both directions and states what the scan does not catch, re-derived from AST-DESIGN section 8/9; the source comment above FIT_METHOD_NAMES names the same forms (key_link, verified by grep)
result: pass
source: automated
coverage_id: D4
requirement: SC2
verification_refs: 1 passing

### 21. [11.1.1-03 D5] The phase's headline number is a committed, executable end-to-end variant table (18 rows) driven through check(), distinct from and never printed alongside the function-level thirteen-variant table
expected: The phase's headline number is a committed, executable end-to-end variant table (18 rows) driven through check(), distinct from and never printed alongside the function-level thirteen-variant table
result: pass
source: automated
coverage_id: D5
requirement: SC6
verification_refs: 1 passing

### 22. [11.1.1-03 D6] No known-bad corpus fixture's plan/execute exit code moved anywhere in the phase, proven by measurement on both sides (phase base commit e0fe56a vs HEAD) rather than by inheriting the superseded plans' argument
expected: No known-bad corpus fixture's plan/execute exit code moved anywhere in the phase, proven by measurement on both sides (phase base commit e0fe56a vs HEAD) rather than by inheriting the superseded plans' argument
result: pass
source: automated
coverage_id: D6
requirement: SC6
verification_refs: 1 passing

### 23. [11.1.1-03 D7] Full suite, corpus, catalogue check, capability validation and ./scripts/check.sh all green on both interpreters; the six full-frame-cleaning symbols are byte-identical to commit 06ff2d7
expected: Full suite, corpus, catalogue check, capability validation and ./scripts/check.sh all green on both interpreters; the six full-frame-cleaning symbols are byte-identical to commit 06ff2d7
result: pass
source: automated
coverage_id: D7
requirement: SC6
verification_refs: 2 passing

### 24. [11.1.1-04 D1] Fallback FIT_CALL_RE resolves an allowlisted fit keyword at any position in
expected: Fallback FIT_CALL_RE resolves an allowlisted fit keyword at any position in
result: pass
source: automated
coverage_id: D1
requirement: SC2
verification_refs: 3 passing

### 25. [11.1.1-04 D2] Prose mask threaded into the full-frame-cleaning and statistical-test
expected: Prose mask threaded into the full-frame-cleaning and statistical-test
result: pass
source: automated
coverage_id: D2
requirement: SC4
verification_refs: 4 passing

### 26. [11.1.1-04 D3] A structurally malformed .ipynb (non-dict document, non-dict cell) is named
expected: A structurally malformed .ipynb (non-dict document, non-dict cell) is named
result: pass
source: automated
coverage_id: D3
requirement: SC5
verification_refs: 2 passing

### 27. [11.1.1-04 D4] SC1/SC3/SC6 do not regress: full suite, known-bad corpus, catalogue check
expected: SC1/SC3/SC6 do not regress: full suite, known-bad corpus, catalogue check
result: pass
source: automated
coverage_id: D4
requirement: -
verification_refs: 4 passing

### 28. [11.1.1-05 D1] README.md announces the looser direction (DSX-CODE-020/030/031
expected: README.md announces the looser direction (DSX-CODE-020/030/031
result: pass
source: automated
coverage_id: D1
requirement: SC4
verification_refs: 2 passing

### 29. [11.1.1-05 D2] Everything plan 04 leaves genuinely uncaught is disclosed:
expected: Everything plan 04 leaves genuinely uncaught is disclosed:
result: pass
source: automated
coverage_id: D2
requirement: SC4
verification_refs: 2 passing

### 30. [11.1.1-05 D3] The DSX-CODE-002 / DSX-CODE-003 comment-mention limit is
expected: The DSX-CODE-002 / DSX-CODE-003 comment-mention limit is
result: pass
source: automated
coverage_id: D3
requirement: SC4
verification_refs: 1 passing

### 31. [11.1.1-05 D4] A malformed-but-valid-JSON notebook's changed outcome
expected: A malformed-but-valid-JSON notebook's changed outcome
result: pass
source: automated
coverage_id: D4
requirement: SC5
verification_refs: 1 passing

### 32. [11.1.1-05 D5] _END_TO_END_VARIANT_TABLE extended with three rows for the
expected: _END_TO_END_VARIANT_TABLE extended with three rows for the
result: pass
source: automated
coverage_id: D5
requirement: SC6
verification_refs: 2 passing

### 33. [11.1.1-05 D6] SC6 re-proof: full suite, known-bad corpus, catalogue
expected: SC6 re-proof: full suite, known-bad corpus, catalogue
result: pass
source: automated
coverage_id: D6
requirement: SC6
verification_refs: 3 passing

## Summary

total: 33
passed: 33
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none from testing -- decisions requiring follow-up work are tracked below, not as UAT gaps]

## Decisions Requiring Follow-Up Work

- **WR-02 (test 1): close in code.** User chose Option B. Needs a new plan implementing the
  `scan_path:not-scanned:<reason>` decision record in `check()`'s early-return path
  (`dsx/checks/code.py`), plus regression tests. Must be sequenced after plans 06 and 07
  (both prohibit touching this input; both are gate-passed but not yet executed). Not folded
  into 06/07. Raised once this UAT session completes.

## Open Phase Gaps (not UAT tests)

Tracked by 11.1.1-VERIFICATION.md (status: gaps_found, score 5/6), not by this UAT session:

- SC5 remains FAILED. Three malformed-.ipynb shapes still raise uncaught out of `check()`:
  non-UTF-8 bytes (UnicodeDecodeError), `{"cells": 5}` (TypeError), and a `source` list
  holding a non-string element (TypeError). Gap-closure plans 11.1.1-06-PLAN.md and
  11.1.1-07-PLAN.md are written and gate-passed but NOT executed. Neither plan may settle
  test 1 or test 2 above -- both plans carry an explicit prohibition against doing so.
