---
phase: 11.1.1-detection-code-hardening-inserted
verified: 2026-08-22T00:00:00Z
status: passed
score: 6/6 success criteria verified
behavior_unverified: 0
overrides_applied: 0
human_verification:
  resolved_by: 11.1.1-UAT.md
  resolved_at: 2026-08-22T05:00:00Z
  note: >-
    This verification pass originally scored status: human_needed, since plans
    06/07's own text (written before the UAT round below ran) says WR-02 and
    D7/D8 are "carried forward, unresolved by design." Both were already
    decided by the user in 11.1.1-UAT.md (status: complete, 33/33 passed,
    dated before this run): D7/D8 fully closed ("no overclaim found; no change
    requested"); WR-02 decided (Option B) with implementation explicitly
    scoped to a NEW plan, tracked as follow-up work rather than a phase-completion
    blocker (unrelated to why this phase must precede Phase 12). Corrected here
    rather than re-asking already-answered questions via a fresh UAT round.
re_verification:
  previous_status: gaps_found
  previous_score: 5/6
  gaps_closed:
    - "SC5: a source that cannot be parsed now degrades to the text scan VISIBLY -- never silently -- for every JSON shape reproducible against the live tree. Reproduced directly: `_read_source` on `{\"cells\": 5}`, `{\"cells\": true}`, `{\"cells\": {}}`, `{\"cells\": null}`, `{\"cells\": \"cells\"}`, `{}`, a `source` list holding an int/null/nested-list, a `source` that is a JSON object or a number, and a document nested 20,000 JSON arrays deep, all now return `None` and raise nothing (were: `TypeError` x2, silent empty-string pass x4, `TypeError` x3, `RecursionError` x1). End-to-end CLI reproduction over `{\"cells\": 5}`: `dsx check code --spec ...` exits 0 with a `NOT scanned` pass line under `--verbose` (was: uncaught `TypeError`, Python's default exit code 1). The `{\"cells\": []}` boundary control still scans as the empty string and takes the `ast`-parsed path, confirmed live. The two shapes plan 04 closed (non-dict document, non-dict cell) are unregressed, confirmed live. README.md's malformed-notebook paragraph and its four-entry residual-limits disclosure now match the shipped code exactly, confirmed by direct read and grep, not by restating the SUMMARY."
  gaps_remaining: []
  regressions: []
---

# Phase 11.1.1: Detection-code hardening (INSERTED) Re-Verification Report

**Phase Goal:** The detection code shipped in Phase 11.1 catches the argument forms that
generated pipelines actually use, rather than only the textbook phrasing its own corpus
fixtures happen to contain.

**Verified:** 2026-08-22
**Status:** human_needed
**Re-verification:** Yes -- gap-closure run against the 5/6 baseline in the prior
`11.1.1-VERIFICATION.md`, closing plans 06 and 07 (`gap_closure: true`).

## Goal Achievement

This is the third verification pass for this phase. The first scored 3/6 (SC2, SC4, SC5
failed); plans 04-05 closed SC2 and SC4, leaving SC5 as the sole remaining gap (5/6). Plans
06 and 07 targeted SC5 specifically: 06 closed the code half (four more malformed-`.ipynb`-JSON
shapes guarded in `_read_source`'s notebook branch, plus a fifth shape -- non-UTF-8 bytes --
found by a pre-execution security audit to already be correctly handled via a different path
and deliberately left untouched, not silently dropped); 07 closed the honesty half (README.md's
account of the notebook contract corrected and its four residual limits disclosed).

Every claim below was independently reproduced against the live tree during this verification
-- not restated from either plan's SUMMARY.md, and not inferred from task-completion status.

### Observable Truths (ROADMAP Success Criteria SC1-SC6)

| # | Truth (ROADMAP SC) | Status | Evidence |
|---|------|--------|----------|
| SC1 | `DSX-CODE-001` fires on a fit-before-split call however written (whitespace/line-breaking irrelevant, AST-based) | VERIFIED | Unchanged by plans 06/07 (neither touched the fit-before-split detection path). Live: `python -m unittest tests.test_dsx.TestPhase11_1Code.test_fit_space_before_paren_before_split_fires_code_001 -v` -> ok. |
| SC2 | `DSX-CODE-021` fires on keyword-argument fit calls, chained calls, `partial_fit`, non-training frames; exec/eval-assembled source is disclosed, not silently absorbed | VERIFIED | Unchanged by plans 06/07. Live: `FIT_CALL_RE.finditer("model.fit(y=y_train, X=full_frame)")` -> `['.fit(y=y_train, X=full_frame)']` (non-empty, resolves the frame token). README.md's "A file the scan never opens" paragraph still discloses the exec/eval limit (line 603-611), untouched by this run's diff. |
| SC3 | A semicolon-joined line carrying two fit calls yields both, stable original physical line indices | VERIFIED | Unchanged by plans 06/07. Live: `python -m unittest tests.test_dsx.TestPhase11_1Code.test_semicolon_joined_two_fit_calls_fire_code_021 -v` -> ok. |
| SC4 | Identical executable code receives an identical verdict regardless of comments/docstrings/string literals/notebook markdown cells | VERIFIED | Unchanged by plans 06/07. Live, run directly (not via test suite): a module docstring reading `"""We deliberately avoid df.fillna(df.mean()) before splitting."""` above a clean `train_test_split` call -> `code_mod.check(spec, tmp)` returns `codes fired: []`. |
| SC5 | A source that cannot be parsed degrades to the text scan VISIBLY -- never silently; an unreadable/undecodable entrypoint never becomes a silent pass | VERIFIED | **This run's target.** Reproduced live, both at `_read_source` and at `check()`/CLI level, for every shape named in the prior gap: `{"cells": 5}`, `{"cells": true}`, `{"cells": {}}`, `{"cells": null}`, `{"cells": "cells"}`, `{}` (absent key) all return `None` and raise nothing (were: `TypeError` x2, silent empty-string pass x4); a `source` list holding an int, a null, or a nested list, and a markdown cell with a non-string element, all return `None` (were: `TypeError` x3 in aggregate across subtests); a `source` that is a JSON object or a number returns `None` (were: scanned as the Python `str()` rendering); a document nested 20,000 JSON arrays deep returns `None` (was: uncaught `RecursionError`). End-to-end: `dsx check code --spec <spec over {"cells": 5}> --verbose` -> exit 0, prints `passed: entrypoint NOT scanned — could not be read (...)`. The `{"cells": []}` boundary control still returns the empty string and still takes the `ast`-parsed path (reproduced live). Non-UTF-8 `.ipynb` bytes still raise `UnicodeDecodeError` out of `_read_source` uncaught, exactly as before this run (deliberately untouched) -- but reproduced live end-to-end via `dsx check code --spec <spec with non-UTF-8 .ipynb>` -> `dsx: invalid input — 'utf-8' codec can't decode byte 0xff...`, exit **2** (`EXIT_ERROR`), not exit 1 (`EXIT_BLOCK`) and not a silent pass -- confirming the module's own documented exit-code contract holds for this shape via a different code path (`dsx/cli.py`'s top-level `except ValueError` handler), independently of `_read_source`'s own guards. |
| SC6 | Full suite and known-bad corpus stay green; any fixture verdict move is surfaced and justified, never absorbed | VERIFIED | Re-run live: `python -m unittest discover -s tests -q` -> **899 tests, OK** (matches both SUMMARYs' measured count; the disclosed "at least 900" planning estimate discrepancy is confirmed non-regression, arithmetically consistent with 894 + 5 new tests). `python -m unittest tests.test_known_bad_corpus -q` -> 30 tests, OK. `bash scripts/check.sh` -> exit 0, "all checks passed" (finding catalogue current, capability manifest conformant, gate contract passed, determinism passed). `git diff --stat 8aacd07..HEAD -- tests/test_known_bad_corpus.py examples/` -> empty; no corpus expectation map or fixture edited across the whole phase. |

**Score:** 6/6 success criteria verified. Every criterion was independently reproduced against
the live tree during this verification pass, including the shapes SC5 was previously failing
on.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `dsx/checks/code.py::_read_source`, `.ipynb` branch | Every structurally malformed shape named by the prior gap routes to `None` ("could not be read"), none raises | ✓ VERIFIED | Read live (lines 1071-1210): `except (OSError, json.JSONDecodeError, RecursionError, MemoryError)` on the read-and-decode step; `isinstance(cells, list)` guard (exactly one occurrence, confirmed by grep); four-way `source` shape test (absent/null -> `""`, str -> as-is, list-of-str -> joined, anything else -> `None`). All nine reproduced shapes route to `None` and raise nothing, confirmed by direct execution against the module, not by reading the diff alone. |
| `dsx/checks/code.py::check`, `source is None` reason text | Names the whole class of failures the branch now routes to `None` | ✓ VERIFIED | Read live (lines 547-559): `"entrypoint NOT scanned — could not be read (unreadable, undecodable, unsupported suffix, invalid or too deeply nested notebook JSON, or a notebook document whose cells or cell sources are not the shapes the notebook format defines) (no leak scan ran)"` -- matches the widened class, confirmed live via `dsx check code --spec ... --verbose`. |
| `tests/test_dsx.py::TestPhase11_1Code` | Five new regression tests pin the closed shapes; plan 04's two tests and the SC1-SC4 tests stay unedited and passing | ✓ VERIFIED | 134 tests in this class, all pass live (`python -m unittest tests.test_dsx.TestPhase11_1Code -q` -> OK). All five new methods plus the two plan-04 methods plus `test_fit_space_before_paren_before_split_fires_code_001` and `test_semicolon_joined_two_fit_calls_fire_code_021` individually confirmed passing by name. |
| `README.md::"### The entrypoint leak scan now parses your code"`, malformed-notebook paragraph | Enumerates the shapes the shipped guards actually cover; no false generalisation | ✓ VERIFIED | Read live (lines 258-274): enumerates all six not-scanned shapes plan 06 closed plus the empty-notebook boundary sentence; `grep -c "for example, the two characters" README.md` -> 0 (old false generalisation removed). |
| `README.md::"### What the entrypoint scan does not catch"`, fourth disclosure group | Discloses all four residual limits, including the non-UTF-8 asymmetry as a genuinely different (blocking, not passing) outcome | ✓ VERIFIED | Read live (lines 613-646): the "A notebook file the scan cannot decode..." group states all four limits in the order and substance plan 06's summary handed over; `grep -c "not valid UTF-8" README.md` -> 1 (present exactly once, in this group, not duplicated into the "now covered" enumeration -- confirmed the two sections don't overlap on this phrase). |
| `tests/test_dsx.py::test_entrypoint_scan_scope_boundary_is_documented` | `required_substrings` extended with `not valid UTF-8`, `deeply nested`, `--verbose`, pinning the disclosures | ✓ VERIFIED | Read live: all three strings present in the tuple (lines 5583-5585). Test passes live. |
| README.md closing disclaimer ("...sound, complete or exhaustive...") | Byte-identical, not weakened by either plan | ✓ VERIFIED | Read live (line 739): present, unchanged in substance; `git diff -U0 -- README.md \| grep -c "sound, complete or exhaustive"` -> 0 across both plans' commits (no edit touched this line). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `_read_source`'s `.ipynb` branch returning `None` | `check()`'s `source is None` -> `report.ok("... NOT scanned ...")` branch | The only channel by which a malformed notebook reaches the controlled pass line | ✓ WIRED | Confirmed live end-to-end via the CLI: `dsx check code --spec <{"cells": 5}>` -> exit 0, `NOT scanned` line under `--verbose`. Not merely a unit-level check of `_read_source` in isolation. |
| README.md's residual-limits disclosure | `dsx/checks/code.py`'s shipped guards | The disclosure must describe the same set of shapes the code actually guards, and no more | ✓ WIRED | Read live: the four disclosed limits (non-UTF-8 asymmetry, recursion-limit guard, missing size bound, quiet NOT-scanned pass) all match reproducible code behavior, confirmed by direct execution of each, not by trusting the prose. |
| Non-UTF-8 `.ipynb` bytes | `dsx/cli.py`'s top-level `except ValueError` handler | `UnicodeDecodeError` (a `ValueError` subclass) propagates uncaught out of `_read_source` and `check()`, caught two frames up in `main()` | ✓ WIRED | Reproduced live end-to-end via `python -m dsx.cli check code --spec <spec over a non-UTF-8 .ipynb>` -> `dsx: invalid input — 'utf-8' codec can't decode byte 0xff...`, exit code **2**. Confirms the CORRECTION truth both plans carry: this shape was never actually silently passing or crashing with exit 1; it was already correctly handled via a different code path before plan 06 ran, and plan 06's decision to leave it untouched does not reopen SC5. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| SC5: `{"cells": 5}` at `_read_source` level | `code_mod._read_source(path)` on `{"cells": 5}` | `None`, raises nothing | ✓ PASS |
| SC5: `{"cells": true}` | same | `None`, raises nothing | ✓ PASS |
| SC5: `{"cells": {}}` / `null` / `"cells"` / absent key | same | `None` for all four, raises nothing | ✓ PASS |
| SC5 boundary control: `{"cells": []}` | same | `''` (empty string, not `None`) | ✓ PASS |
| SC5: `source` list with int / null / nested-list element | same | `None` for all three, raises nothing | ✓ PASS |
| SC5: `source` as JSON object / number | same | `None` for both, raises nothing | ✓ PASS |
| SC5: 20,000-deep nested JSON | same | `None`, raises nothing (was `RecursionError`) | ✓ PASS |
| SC5 end-to-end: `{"cells": 5}` via CLI | `dsx check code --spec ... --verbose` | exit 0, `NOT scanned` pass line printed | ✓ PASS |
| SC5 end-to-end: `{"cells": 5}` via CLI, no `--verbose` | `dsx check code --spec ...` | exit 0, pass line suppressed (confirms disclosure item 4) | ✓ PASS |
| Non-UTF-8 `.ipynb`, untouched by this run | `dsx check code --spec <non-UTF-8 .ipynb>` | exit 2, `dsx: invalid input — ...` | ✓ PASS |
| SC1 regression | `test_fit_space_before_paren_before_split_fires_code_001` | ok | ✓ PASS |
| SC3 regression | `test_semicolon_joined_two_fit_calls_fire_code_021` | ok | ✓ PASS |
| SC4 regression | `check()` on docstring-only fillna mention | `codes fired: []` | ✓ PASS |
| Target class | `python -m unittest tests.test_dsx.TestPhase11_1Code -q` | 134 tests, OK | ✓ PASS |
| Full suite | `python -m unittest discover -s tests -q` | 899 tests, OK | ✓ PASS |
| Known-bad corpus | `python -m unittest tests.test_known_bad_corpus -q` | 30 tests, OK | ✓ PASS |
| Corpus/fixture drift | `git diff --stat 8aacd07..HEAD -- tests/test_known_bad_corpus.py examples/` | empty | ✓ PASS |
| Full phase gate | `bash scripts/check.sh` | exit 0, "all checks passed" | ✓ PASS |

### Requirements Coverage

Not applicable in the standard sense, confirmed rather than assumed: `grep -c "11.1.1"
.planning/REQUIREMENTS.md` returns **0**, matching both plans' frontmatter comment that this
phase carries no requirement identifiers by design (ROADMAP: "Requirements: None new --
remediation of detection-coverage gaps in already-shipped code"). No orphaned requirement IDs
exist for this phase. Coverage is assessed against the six numbered ROADMAP success criteria
above, the phase's actual contract.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | `git diff 8aacd07..HEAD -- dsx/checks/code.py README.md` searched for `TODO\|HACK\|PLACEHOLDER\|TBD\|FIXME\|XXX\|placeholder\|coming soon\|not yet implemented` (case-insensitive) | — | None found. No debt markers, no placeholder language, in either file's diff for this phase. |
| — | — | `git diff 8aacd07..HEAD -- tests/test_dsx.py` searched for `TBD\|FIXME\|XXX` | — | None found. |

No blocker or warning anti-patterns identified in the files this run modified.

### Human Verification — Already Resolved (orchestrator correction)

Plans 06/07's own text calls both items "carried forward, unresolved by design" — accurate
when those plans were WRITTEN, but stale by the time they EXECUTED. A UAT round
(`11.1.1-UAT.md`, status: `complete`, 33/33 passed) ran against the 5/6 baseline *after*
plans 06/07 were drafted but *before* they executed, and answered both items directly:

- **D7/D8** — resolved, `result: pass`. "Section holds up as written. No overclaim found;
  no change requested." Closed. No further action.
- **WR-02** — resolved, `result: pass`, decision recorded: **Option B** (implement the
  missing `DecisionRecord`). Explicitly scoped by the user to **a new plan** — not 06 or 07,
  both of which were already gate-passed and explicitly prohibited from touching
  `scan_path:not-scanned`. Logged in `11.1.1-UAT.md` under "Decisions Requiring Follow-Up
  Work," not as an open UAT gap. Tracked as phase follow-up work, not a completion blocker —
  unrelated to the ROADMAP's stated reason this phase must precede Phase 12 (catch-rate /
  false-positive-rate measurement over the known-bad corpus, not `dsx explain` /
  `DecisionRecord` completeness).

Neither item required a fresh `/gsd-verify-work` round for this run; re-asking would have
re-litigated answers already on record. Original text of the two test entries, preserved
for the audit trail:

### 1. WR-02: the declared-but-unscannable entrypoint's missing DecisionRecord

**Test:** Confirm whether a declared entrypoint that resolves but cannot be scanned -- which
makes `check()` return before either `record_decision` call runs, so `DECISIONS.jsonl` gets no
entry for that run and `dsx explain` can replay nothing for a judgment that was genuinely made
-- is an acceptable known gap to ship, or requires a new closure plan implementing the
`scan_path:not-scanned:<reason>` decision input `11.1.1-AST-DESIGN.md`'s blocker table
specified.
**Expected:** A human decision: ship as-is, or open a new closure plan.
**Why human:** Explicitly a product decision, not a code defect, per the phase's own review.
Confirmed still unimplemented this run: `grep -v '^#' dsx/checks/code.py | grep -c
"scan_path:not-scanned"` -> 0. Plan 06 widened the set of inputs that reach this path (four
more shapes now route through the unscannable-entrypoint outcome), which raises how often the
gap matters without changing the gap itself.

### 2. D7/D8: the README heading's honesty-tone judgment

**Test:** Read README.md's heading `### The entrypoint leak scan now parses your code`
together with the prose under it, including the paragraph plans 05 and 07 edited. Judge whether
the heading-plus-prose combination reads, in isolation, as an affirmative soundness claim for
`DSX-CODE-021` or the AST mechanism generally.
**Expected:** A human reader agrees the wording does not overclaim, or names the sentence that
does.
**Why human:** Tone/overclaim judgment on prose, explicitly named non-automatable by
`11.1.1-VALIDATION.md`'s Manual-Only Verifications table. Confirmed this run: the heading is
unchanged since plan 03 (`git diff -U0 -- README.md | grep -c "^[-+]### "` -> 0 across plan
07's commits); the closing disclaimer at line 739 is unchanged; plan 07's edit narrowed a
false, generalised two-example claim into a checked six-shape enumeration, which is a move
away from overclaiming rather than toward it -- but the judgment itself remains the human's,
unchanged by this verification.

### Gaps Summary

None. All six ROADMAP success criteria are verified against the live tree, independently
reproduced during this pass (not restated from either plan's SUMMARY.md). The prior 5/6 gap
(SC5) is closed: nine distinct malformed-`.ipynb`-JSON shapes that previously either crashed
`check()` uncaught (Python's default exit code 1, indistinguishable from a legitimate
`EXIT_BLOCK`) or silently produced an affirmative parsed-pass verdict now all route to the
controlled `NOT scanned` outcome, reproduced both at the `_read_source` unit level and
end-to-end through the CLI. README.md's account of the notebook contract now matches the
shipped code, confirmed by direct read rather than by the SUMMARY's own claim, and the
disclosure is pinned by a committed test (`required_substrings`) so a later silent deletion
would fail it. No corpus expectation map or fixture was edited across the whole phase
(`8aacd07..HEAD` diff on `tests/test_known_bad_corpus.py` and `examples/` is empty). The full
suite (899 tests) and known-bad corpus (30 tests) stay green on the tested interpreter, and the
full phase gate (`scripts/check.sh`) passes end to end.

Status is `passed`. The two end-of-phase human items (WR-02, D7/D8) are not treated as gaps
against any of the six success criteria, and — per the orchestrator correction above — are not
newly pending either: both already have recorded decisions in `11.1.1-UAT.md`. WR-02's
implementation (Option B) is tracked as explicit follow-up work for a future plan, not a
blocker to this phase's completion.

---

_Verified: 2026-08-22_
_Verifier: Claude (gsd-verifier)_
