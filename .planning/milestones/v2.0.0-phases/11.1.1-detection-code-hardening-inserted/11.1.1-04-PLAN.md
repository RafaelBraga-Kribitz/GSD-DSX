---
phase: 11.1.1-detection-code-hardening-inserted
plan: 04
type: tdd
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - dsx/checks/code.py
  - tests/test_dsx.py
autonomous: true
# This phase carries no REQ identifiers by design (ROADMAP: "Requirements: None
# new -- remediation of detection-coverage gaps in already-shipped code", and
# .planning/REQUIREMENTS.md returns zero hits for 11.1.1, re-confirmed by grep
# while planning this run). Coverage is keyed to the ROADMAP's six numbered
# success criteria instead, labelled SC1-SC6, exactly as plans 01-03 did.
# This plan closes the three code gaps that made SC2, SC4 and SC5 fail
# verification. Plan 05 announces the resulting behaviour change and re-proves
# SC6.
requirements: [SC2, SC4, SC5, SC6]

must_haves:
  truths:
    - "SC2 (literal): DSX-CODE-021 fires on keyword-argument fit calls whatever the call's shape -- on the FALLBACK path as well as the parsed path. `model.fit(y=y_train, X=full_frame)` placed after a split marker, in a file forced onto the fallback by an unrelated syntax error elsewhere, draws DSX-CODE-021 CRITICAL with the token `full_frame`, matching the verdict the parsed path already returns for the identical call. Reproduced against the shipped code while planning: the parsed path fires and the fallback returns zero findings."
    - "SC2 (literal): nothing records DSX-CODE-021 -- or any DSX-CODE-* -- as sound. The source comment above FIT_CALL_RE and the `_fit_call_arguments` docstring currently claim the two mechanisms agree about which keyword names a training frame. That claim is false for any call whose recognised keyword is not the first one. It is REMOVED, not narrowed into a smaller claim of the same kind."
    - "SC2 (false-positive guard, unchanged direction): `model.fit(y=y_test)` and `model.fit(y=y_test, z=1)` stay silent on BOTH paths. The widening recognises an allowlisted keyword at any position; it does NOT widen the allowlist itself, and it never reports a keyword NAME as the frame that was fitted (threat T-11.1.1-07)."
    - "SC4 (literal): identical executable code receives an identical verdict regardless of comments, docstrings, string literals or notebook markdown cells -- for DSX-CODE-020, DSX-CODE-030 and DSX-CODE-031, not only for DSX-CODE-001 and DSX-CODE-021. A module docstring reading `We deliberately avoid df.fillna(df.mean()) before splitting.` draws no DSX-CODE-020 on the parsed path (today: CRITICAL at Line 2, reproduced). A docstring mentioning a statistical-test call on the declared target draws no DSX-CODE-030 before the split and no DSX-CODE-031 after it (today: CRITICAL at Line 2 and HIGH at Line 4, both reproduced)."
    - "SC4 (control direction, must not move): real executable code still fires. `df['Age'] = df['Age'].fillna(df['Age'].mean())` before the split still draws DSX-CODE-020 CRITICAL at Line 2, and a real `chi2_contingency(table)` call whose target reference sits on a real code line still draws DSX-CODE-030 CRITICAL at Line 3. Masking prose must not delete a true positive."
    - "SC5 (literal): a source that cannot be parsed degrades to the text scan visibly, never silently, and an unreadable, undecodable or structurally malformed entrypoint never becomes a silent pass or an uncaught crash -- it emits `report.ok(...)` before returning. A `.ipynb` whose content is `[]`, or `null`, or `{\"cells\": [\"not-a-dict-cell\"]}`, or whose `cells` value is not a list of objects, returns None from `_read_source` and routes into the existing 'could not be read' `report.ok(...)` branch. Today all four raise AttributeError out of `check()` uncaught (two reproduced directly), producing a raw traceback and Python's default exit code 1 -- numerically indistinguishable from EXIT_BLOCK, and a violation of the documented contract that exit 2 means the check could not run."
    - "SC1 and SC3 do not regress: the whitespace, tab, backslash-continuation, multi-line and semicolon-joined firing tests keep passing unchanged, and the reported line index stays the physical line the call expression begins on."
    - "SC6 does not regress: the full suite and the known-bad corpus stay green, and no corpus expectation map is edited. Measured while planning, against a clean `git archive HEAD` copy with all three fixes applied: 878 tests OK, 30 corpus tests OK, `gen-finding-catalogue.py --check` exit 0, `validate-capability.py` exit 0. That measurement is evidence the fixes are landable, not the proof; the proof is plan 05's run against the real tree."
    - statement: "No new shared mutable state and no new module-level cache is introduced. `_read_source`, `_first_full_frame_cleaning_line` and `_stat_test_lines_referencing` remain pure functions of their arguments, and the prose mask is the single one already computed once per `check()` call -- passed to two more consumers, never recomputed and never re-walked."
      verification: backstop
  artifacts:
    - path: dsx/checks/code.py
      provides: "FIT_CALL_RE recognising an allowlisted frame keyword at any position in the argument list up to a stated bound; `masked` parameters on `_first_full_frame_cleaning_line` and `_stat_test_lines_referencing` wired to the existing `prose_mask`; structural guards on the `.ipynb` branch of `_read_source`"
      contains: "masked"
    - path: dsx/checks/code.py
      provides: "the corrected comments -- the removed parity overclaim above FIT_CALL_RE and inside `_fit_call_arguments`, the narrowed PIPELINE_FIT_TRAIN_RE trade-off sentence (WR-01), and the corrected consumer-count sentence in `_prose_line_indices`"
      contains: "isinstance(nb, dict)"
    - path: tests/test_dsx.py
      provides: "regression pins for all three gaps in TestPhase11_1Code: the reordered-keyword fallback leak, the docstring-only DSX-CODE-020/030/031 false positives with their true-positive controls, and the two malformed-notebook shapes"
  key_links:
    - from: "dsx/checks/code.py::check"
      to: "dsx/checks/code.py::_first_full_frame_cleaning_line"
      via: "the SAME `prose_mask` frozenset already computed once per check() call and already passed to `_first_line_matching`, passed through as `masked=`. Recomputing it or re-walking the tree would reintroduce the per-code walk plan 01 forbade for both correctness and performance reasons."
      pattern: "masked=prose_mask"
    - from: "dsx/checks/code.py::check"
      to: "dsx/checks/code.py::_stat_test_lines_referencing"
      via: "the same mask, applied in BOTH places the function reads `lines` -- as a candidate filter for the statistical-test call line, and as a filter on the _TARGET_REFERENCE_LOOKBACK window, so a prose line cannot supply the target reference that decides a real call's verdict."
      pattern: "masked=prose_mask"
    - from: "dsx/checks/code.py::_read_source"
      to: "dsx/checks/code.py::check"
      via: "returning None on a structurally malformed notebook routes into the existing `report.ok(\"entrypoint NOT scanned -- could not be read ...\")` branch at the `source is None` guard. Returning an empty string instead (by skipping bad cells with `continue`) would produce an affirmative `entrypoint parsed with ast` pass line over a document nothing was scanned in -- a silent pass, which SC5 forbids -- and would also renumber every line below a dropped cell, breaking the byte-identical notebook line geometry plans 01 and 02 established."
      pattern: "isinstance"
    - from: "dsx/checks/code.py::FIT_CALL_RE"
      to: "dsx/checks/code.py::FIT_FIRST_PARAM_NAMES"
      via: "the widened pattern skips NON-allowlisted keyword arguments and still resolves the frame only through the same six-name allowlist `_first_argument` uses on the parsed path. Widening position must not widen membership, or the fallback starts reporting an unrelated keyword's value as the frame that was fitted."
      pattern: "FIT_FIRST_PARAM_NAMES"
  prohibitions:
    - statement: "MUST NOT record DSX-CODE-021 -- or any DSX-CODE-* -- as sound, complete, reliable or exhaustive, in a source comment, a docstring, the finding catalogue, README.md, this plan or a commit message. Making the fallback agree with the parsed path for one more call shape does not make either path sound. The comment correction this plan requires must REMOVE an overclaim, not replace it with a narrower one of the same kind. Judge the sentences; do not just count keywords."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT weaken, delete or rewrite an existing corpus expectation -- `_TARGET_DEFECT_CODES`, `_EXPECTED_CAUGHT_DEFECTS` or `_INCIDENTAL_GAP_CODES` in `tests/test_known_bad_corpus.py` -- to absorb a verdict change caused by this plan. If a fixture's gate exit code moves, surface and justify it in BOTH directions. The map is not the place to make that disappear."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT ship either direction of user-visible behaviour change unannounced. Files that pass today and start failing (the reordered-keyword true positives recovered here) AND files that fail today and start passing (the docstring false positives removed here) both belong in README.md. The looser direction is the more dangerous one to leave unstated, because nobody files a bug about a gate that stopped complaining. Plan 05 owns the announcement; this plan must not close without it being planned."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT leave anything that remains genuinely uncaught after this work undisclosed in README.md's 'What the entrypoint scan does not catch' section. That includes the fallback path's retained docstring false positive for DSX-CODE-020/030/031 (the prose mask is empty when there is no tree), the bounded keyword-skip miss the widened fallback pattern accepts, and the prose mask's opening-line and closing-line boundary, which becomes user-visible for DSX-CODE-020 for the first time in this plan."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT add any parsing dependency other than `ast` on the gate path. D-01 forbids third-party imports there. `ast`, `json` and `re` are standard library and already imported by this module; nothing new is installed, so the Package Legitimacy Gate stays untriggered."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT add a `\\r?\\n` alternative to any pattern in `dsx/checks/code.py`. Plan 01 recorded this prohibition and it still holds: line terminators are removed upstream by `_source_lines`, which normalises CRLF and lone CR before splitting, so a carriage-return branch inside a per-line pattern would be dead code. The repo-wide CRLF rule is satisfied at that one normalisation point, and `[^(),\\n]` is the module's established idiom for 'not a delimiter'."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT delete a `stays_uncaught_by_design` test that begins failing because a finding APPEARED, and MUST NOT delete a `measured_not_assumed` pin that begins failing because a false positive DISAPPEARED. Convert it, keeping its original name in the replacement's docstring so the record that scope changed survives. Measured while planning against a patched clean copy: all 878 committed tests still pass with all three fixes applied, so no conversion is expected -- if one is needed anyway, that is a finding to surface, not to absorb."
      status: unverified
      verification: flagged-unverified
---

<objective>
Close the three reproduced code defects that falsified ROADMAP success criteria SC2, SC4 and
SC5 at verification: the fallback text scanner silently missing a real post-split leak when
the recognised frame keyword is not the first keyword in the call; the prose mask never being
threaded into the two scanners that decide DSX-CODE-020, DSX-CODE-030 and DSX-CODE-031, so a
docstring alone can decide a CRITICAL verdict on the PRIMARY parsed path; and the notebook
reader's unguarded structural assumptions, which turn a syntactically-valid but structurally
malformed `.ipynb` into an uncaught AttributeError and an exit code indistinguishable from a
legitimate gate block.

Purpose: all three are blocking-gate defects, and two of them are the exact numbers Phase 12
exists to publish. The ROADMAP makes Phase 12's catch rate depend on SC2 and its
false-positive rate depend on SC4, and states this phase must precede Phase 12 for that
reason. Documenting rather than fixing would corrupt the measurements Phase 12 is chartered to
produce. SC5's defect is worse than a silent pass: the gate exits 1 with a raw traceback,
which a caller reading only the exit code cannot distinguish from "the gate legitimately
blocked".

Output: `dsx/checks/code.py` with a fallback keyword resolution that scans the whole argument
list, a prose mask reaching all five text-decided codes rather than one, and a notebook reader
that names a malformed document as NOT scanned instead of crashing; plus the regression pins in
`tests/test_dsx.py::TestPhase11_1Code` that hold each of the three closed, including the
controls proving no true positive was deleted along the way.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-VERIFICATION.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-REVIEW.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-AST-DESIGN.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-VALIDATION.md
@dsx/checks/code.py
@tests/test_dsx.py
</context>

<gap_closure_record>
This is a gap-closure plan. Plans `11.1.1-01`, `11.1.1-02` and `11.1.1-03` have all executed
and each has a `-SUMMARY.md`. None of them is modified, renumbered or superseded by this plan.

Source of truth for what must close:
`.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-VERIFICATION.md`
(status `gaps_found`, score 3/6). Every `missing:` item from every gap in that report maps to a
task below.

| Gap | Failed criterion | `missing:` item | Covered by |
|---|---|---|---|
| GAP-1 (CR-01) | SC2 | Make the fallback keyword resolution scan every keyword for an allowlisted name (rather than only the first token), and correct the overclaiming source comment | Task 1 |
| GAP-1 (CR-01) | SC2 | Regression test: `model.fit(y=y_train, X=full_frame)` after a split fires DSX-CODE-021 on the FALLBACK path, forced by an unrelated syntax error in the same file, matching the parsed path's verdict | Task 1 |
| GAP-2 (CR-02) | SC4 | Thread `prose_mask` into `_first_full_frame_cleaning_line` and `_stat_test_lines_referencing` the same way it is threaded into `_first_line_matching` | Task 2 |
| GAP-3 (CR-03) | SC5 | `isinstance(nb, dict)` guard before `nb.get("cells")` and `isinstance(cell, dict)` guard inside the loop, returning None so it routes into the existing "could not be read" `report.ok(...)` branch | Task 3 |
| GAP-3 (CR-03) | SC5 | Regression test alongside `test_notebook_degenerate_shapes_produce_no_finding_no_exception` covering a non-dict top-level document and a non-dict cell entry | Task 3 |

Two `missing:` items offered "or announce the limitation in README.md" as an alternative to
fixing. The orchestrator put that choice to the developer and is proceeding on the stated
default: **fix all three in code.** README disclosure is still required for what genuinely
remains uncaught after the fix, and plan 05 owns it -- it is not a substitute for the fix.
</gap_closure_record>

<visible_skips_and_deferrals>
Recorded here rather than left unexplained. None of these is a silent omission.

1. **Spec-less probe fallback: SKIPPED, visibly.** There is no `-SPEC.md` for this phase, so
   its `## Edge Coverage` and `## Prohibitions` sections do not exist. The probe derives
   predicates from requirement text, and this phase has zero requirement identifiers, so there
   is nothing for it to derive from. **No probe-derived predicates were generated this run.**
   `must_haves` are derived from the VERIFICATION.md gaps and the literal ROADMAP success
   criteria instead.

2. **No `ANALYSIS-SPEC.yaml`, and none is required.** This phase produces no number, model or
   chart -- it hardens a static leakage checker. The `dsx` capability's own plan:pre step says
   to skip the analytical-spec step for such phases, and the analytical task-shape rules
   (decision -> metric -> design -> data -> analysis -> communication) do not apply. This is
   planned as a code-correctness change with regression tests.

3. **Application-programming-interface coverage checkpoint: SKIPPED.** Ran the deterministic
   detector rather than pattern-matching prose: `node
   ~/.claude/gsd-core/bin/lib/api-coverage.cjs --json` returned `{"detected": false,
   "signals": []}`. This phase integrates no external interface, kit or service. No
   `COVERAGE.md` was created.

4. **Object-relational-mapper schema push: SKIPPED.** Scanned the phase scope for Payload,
   Prisma, Drizzle, Supabase and TypeORM schema file patterns and found none. This is a Python
   static-analysis codebase with no database. No blocking push task was injected.

5. **`.planning/intel/API-SURFACE.md` deliberately not supplied.** It was regenerated and
   returned `symbolCount: 0` -- the extractor found no symbols in this Python codebase -- so it
   carries no signal. Read `dsx/checks/code.py` directly instead; every task's `<read_first>`
   names the exact regions.

6. **WR-01, redundancy half: DEFERRED, with a measured reason.** The independent review's
   WR-01 has two halves. The documentation-accuracy half (its comment overstates the miss the
   `{0,200}` bound trades away) is CLOSED by task 1. The redundancy half -- delete
   `PIPELINE_FIT_TRAIN_RE` because `FIT_LEAK_MARKERS` already covers it -- is deferred, and not
   for difficulty. Measured while planning, against the shipped code: on the line
   `Pipeline(steps) . fit (X_train)`, `PIPELINE_FIT_TRAIN_RE.search` returns a match and NONE
   of the three `FIT_LEAK_MARKERS` patterns matches, because `\.fit\s*\(` requires `.fit` to be
   adjacent and this shape has whitespace between the dot and the method name. Deleting the
   pattern would therefore remove the only fallback detector for that shape and would delete a
   committed timing pin that plan 03 deliberately tightened from a 1.0-second to a
   0.05-second budget. The pattern is kept and its comment is corrected.

7. **The DSX-CODE-002 / DSX-CODE-003 comment-mention limit: DEFERRED, in scope terms.** Those
   two codes' text loops still have no leading-comment guard, so `# never use SMOTE before the
   split` still draws DSX-CODE-003 HIGH. This is NOT one of the verification report's gaps, and
   the orchestrator scoped SC4's closure explicitly to DSX-CODE-020, DSX-CODE-030 and
   DSX-CODE-031. It is already disclosed in README.md's limits section (unlike the
   DSX-CODE-020/030/031 docstring case, which is disclosed nowhere) and is pinned by two
   committed `measured_not_assumed` tests. Plan 05 restates it as still open rather than
   letting the reader infer from the new text that it closed.

8. **WR-02 and the honesty-tone judgment: CARRIED FORWARD, not settled here.** Both are human
   decisions. `workflow.human_verify_mode` is `end-of-phase`, so they are recorded as
   `<human-check>` items on plan 05's tasks and answered at `/gsd-verify-work`, not as blocking
   mid-execution checkpoints. No task in this plan may quietly decide either.
</visible_skips_and_deferrals>

<assumption_delta_decision>
Ran the deterministic query rather than reading prose for it:
`gsd-tools query assumption-delta scan "11.1.1" --json` returned `detected: true`, on two
pluralization signals -- the noun `fallback` (from the ROADMAP re-scope paragraph) and the word
`multiple` (from plan 02's roadmap line).

- **Noun that is now primary:** the parsed path. The `ast.parse` walk is the primary mechanism
  and the text scan is the exception, reached only when a source cannot be parsed.
- **Decision:** `no-change`.
- **Rationale:** this plan makes the FALLBACK agree with the primary path for one more call
  shape; agreement does not promote the fallback to primary, and the two roadmap sentences the
  detector matched already describe the fallback as the exception rather than the norm.
</assumption_delta_decision>

## Artifacts this phase produces

Every symbol this plan creates, renames or adds to an existing signature. The
source-grounding pass reads this section to exclude newly-created symbols from drift
verification.

**New parameters on existing functions** (both keyword, both defaulted, so every existing
caller is unaffected):

- `dsx/checks/code.py::_first_full_frame_cleaning_line` gains `masked: "frozenset[int]" = frozenset()`
- `dsx/checks/code.py::_stat_test_lines_referencing` gains `masked: "frozenset[int]" = frozenset()`

**No new module-level functions, classes or constants.** `FIT_CALL_RE`,
`PIPELINE_FIT_TRAIN_RE`, `FIT_FIRST_PARAM_NAMES`, `_prose_line_indices`, `_read_source`,
`_fit_call_arguments` and `check` all keep their current names, and `FIT_CALL_RE` keeps its
capture-group count and group semantics.

**New test method names in `tests/test_dsx.py::TestPhase11_1Code`:**

- `test_reordered_keyword_leak_fires_code_021_on_the_fallback_path`
- `test_reordered_keyword_leak_verdicts_agree_across_both_paths`
- `test_several_unrecognised_keywords_before_an_allowlisted_one_resolve_the_allowlisted_value`
- `test_equality_comparison_first_argument_is_not_read_as_a_keyword_on_either_path`
- `test_fit_call_re_timing_no_catastrophic_backtracking_with_reordered_keyword_form`
- `test_keyword_beyond_the_skip_bound_stays_uncaught_by_design_on_the_fallback`
- `test_docstring_describing_a_cleaning_idiom_draws_no_code_020`
- `test_docstring_mentioning_a_stat_test_draws_no_code_030`
- `test_docstring_after_the_split_mentioning_a_stat_test_draws_no_code_031`
- `test_prose_only_target_reference_draws_no_code_030_for_a_real_stat_test_call`
- `test_real_cleaning_idiom_still_fires_code_020_with_the_mask_wired`
- `test_real_target_reference_still_fires_code_030_with_the_mask_wired`
- `test_multiline_string_opening_line_still_fires_code_020_stays_uncaught_by_design`
- `test_docstring_still_fires_code_020_on_the_fallback_stays_uncaught_by_design`
- `test_non_dict_notebook_document_is_named_not_scanned_without_raising`
- `test_non_dict_notebook_cell_is_named_not_scanned_without_raising`

**No README headings are created by this plan.** Plan 05 owns every documentation change.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The fallback stops missing a real leak whose recognised keyword is not first (GAP-1, SC2), and three overclaiming comments are corrected</name>

  <files>dsx/checks/code.py, tests/test_dsx.py</files>

  <read_first>
    - `dsx/checks/code.py` lines 157-188 -- the comment block above `FIT_CALL_RE` and the
      pattern itself. The parity claim this task must remove lives in the plan-02 paragraph
      of that block.
    - `dsx/checks/code.py` lines 1167-1199 -- `_fit_call_arguments`, its docstring (which
      carries the second copy of the same parity claim) and its `finditer` loop.
    - `dsx/checks/code.py` lines 241-254 -- `FIT_METHOD_NAMES` and `FIT_FIRST_PARAM_NAMES`,
      the allowlist this widening must NOT widen.
    - `dsx/checks/code.py` lines 385-417 -- `_first_argument`, the parsed path's keyword
      resolution. This is the behaviour the fallback is being brought into agreement with:
      it scans ALL keywords in source order and takes the first whose `arg` is allowlisted.
    - `dsx/checks/code.py` lines 622-643 -- `check()`'s DSX-CODE-021 branch, where the parsed
      and fallback resolutions diverge.
    - `dsx/checks/code.py` lines 77-103 -- `PIPELINE_FIT_TRAIN_RE` and its comment (WR-01's
      documentation half), and lines 1121-1147 -- `_first_fit_leak_line`, its only consumer.
    - `tests/test_dsx.py` lines 4548-4565 -- `TestPhase11_1Code`'s `_entrypoint` and `_check`
      helpers. `_entrypoint` defaults to `name="entry.py"`.
    - `tests/test_dsx.py` lines 4883-4913 -- `test_out_of_allowlist_keyword_draws_nothing_on_both_paths`.
      This is the closest existing test and the model for the both-paths shape, including how
      it forces the fallback and how it asserts the fallback was actually reached.
    - `tests/test_dsx.py` lines 5908-5926 and 5963-5986 -- the two committed `FIT_CALL_RE`
      white-box tests. Both must stay green with NO edit.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-AST-DESIGN.md`
      sections 2.2 and 3.4 -- the fallback contract and the allowlist decision.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-VERIFICATION.md`
      frontmatter, the GAP-1 entry.
  </read_first>

  <behavior>
    RED first. Every expectation below was reproduced against the shipped code while planning,
    so each assertion's current (failing) value is known, not guessed.

    - `model.fit(y=y_train, X=full_frame)` placed after a `train_test_split(df)` marker, in a
      file made unparseable by an unrelated trailing `unclosed(` line, draws DSX-CODE-021 at
      CRITICAL with the reported token `full_frame`. Today: zero findings on that file, while
      the identical file without the `unclosed(` line draws DSX-CODE-021 correctly.
    - The same source, with and without the forcing syntax error, yields the same DSX-CODE-021
      verdict and the same token. Assert path parity directly, and assert the fallback was
      actually reached (`f.data.get("scan") == "text-fallback"`, or a `fallback scan` line in
      `report.passed_checks`, the way the existing both-paths test does it).
    - `model.fit(sample_weight=w, groups=g, data=full_frame)` after the split resolves to
      `full_frame` on both paths. Today: parsed path fires, fallback returns nothing.
    - `model.fit(y=y_test)` and `model.fit(y=y_test, z=1)` stay silent on both paths. Widening
      POSITION must not widen MEMBERSHIP.
    - `model.fit(a==b, y)` stays silent on both paths -- an equality comparison in the first
      positional slot is not a keyword argument, and must not be consumed as one.
    - Unchanged white-box behaviour, asserted through the committed tests without editing them:
      `FIT_CALL_RE.search("min_max_scaler.fit_transform(data[['Balance']])").group(1)` starts
      with `data`; `imputer.fit(df` (no closing parenthesis) yields no match;
      `model.fit(42)` yields no match; `imputer.fit(X_train); scaler.fit_transform(data)`
      yields `X_train` then `data` in that left-to-right order.
    - Timing: the widened pattern is measured on its own worst-case shapes, not inherited from
      the pre-widening figure (11.1.1-RESEARCH.md Pitfall 6).
    - The shape that remains uncaught after the widening -- an allowlisted keyword arriving
      after more skipped keyword arguments than the chosen bound -- is pinned as a
      `stays_uncaught_by_design` test carrying the promotion protocol sentence verbatim on ONE
      physical line, so the existing `grep -c` count over that sentence stays consistent.
  </behavior>

  <action>
    Two commits: `test(11.1.1-04): pin the reordered-keyword fallback leak as failing` (RED,
    the new tests only, the suite showing exactly the new failures), then
    `fix(11.1.1-04): resolve an allowlisted fit keyword at any argument position on the fallback`
    (GREEN, the pattern and comment changes).

    **The pattern change, stated as the exact edit.** `FIT_CALL_RE` currently reads, in three
    concatenated raw-string fragments: a method alternation followed by `\s*\(\s*`; an OPTIONAL
    allowlist keyword prefix, an alternation of the six `FIT_FIRST_PARAM_NAMES` members
    followed by `\s*=\s*`; and the capture group with its trailing `\s*[),]`. Insert ONE new
    fragment BETWEEN the first and second, and change nothing else. The new fragment is a
    bounded repetition that skips leading `name = value ,` pairs whose name is not allowlisted.
    Its body, in order: an identifier (`[A-Za-z_]\w*`), optional whitespace, a single `=`
    immediately followed by a negative lookahead for a second `=` (so `a==b` is not read as a
    keyword argument), optional whitespace, a bounded run of characters that EXCLUDES `(`, `)`,
    `,` and the newline, then a comma and optional whitespace. Bound the inner character run
    with an explicit `{0,80}` and the whole repetition with an explicit `{0,8}` -- both counts
    are yours to set, but both MUST be explicit numbers, and whatever you set must be named in
    the source comment and pinned by the out-of-bound test below. There must be no unbounded
    quantifier anywhere in the addition (threat T-11.1-01), and the inner character class must
    not overlap the delimiter that terminates it, so each repetition step is unambiguous.

    **What must NOT change in the pattern:** the capture group `([A-Za-z_][\w.\[\]'\"]*)` and
    the trailing `\s*[),]`. The capture must stay the argument VALUE, never a keyword name
    (threat T-11.1.1-07), and the trailing requirement must keep a call with no closing
    parenthesis from matching at all rather than capturing a truncated token (the T-11.1-02
    degrade contract). The number of capture groups must stay one, because
    `_fit_call_arguments` reads `match.group(1)` positionally. Do NOT add a `\r?\n`
    alternative anywhere: `_source_lines` has already normalised CRLF and lone CR before any of
    these patterns sees a line, so a carriage-return branch here would be dead code, and plan
    01 recorded that prohibition explicitly.

    **Reference measurement, taken while planning.** A prototype of exactly this shape, applied
    to a clean `git archive HEAD` copy, produced: `model.fit(y=y_train, X=full_frame)` ->
    `full_frame`; `model.fit(sample_weight=w, groups=g, data=full_frame)` -> `full_frame`;
    `model.fit(y=y_test)`, `model.fit(y=y_test, z=1)`, `model.fit(a==b, y)`, `model.fit(42)`,
    `model.fit(*args)` and `imputer.fit(df` -> no match; and every one of the 878 committed
    tests still passed. Treat that as evidence the shape is landable, not as permission to skip
    the RED step.

    **Comment correction 1 -- the parity overclaim, removed not narrowed.** The
    `_fit_call_arguments` docstring's plan-02 paragraph ends on a claim that the keyword
    allowlisting makes the two mechanisms agree about which keyword names a training frame.
    Verification reproduced that claim false. Delete the clause. Replace it with what is
    actually true after this task -- that the fallback now resolves an allowlisted frame
    keyword at any argument position up to the stated skip bound -- immediately followed by
    what is still NOT true: beyond that bound, and for any argument shape the pattern cannot
    express, the fallback finds nothing where the parsed path still does. The replacement must
    not assert parity, agreement, soundness or completeness in any form. Apply the same
    correction to the second copy of the claim, in the comment block above `FIT_CALL_RE`.

    **Comment correction 2 -- WR-01's documentation half.** The `PIPELINE_FIT_TRAIN_RE` comment
    block claims the `{0,200}` bound trades a single real miss for its linearity guarantee, and
    names a nested-parenthesis `Pipeline(...)` argument list as that miss. That framing
    overstates what is traded: `_first_fit_leak_line` returns the SAME index from either
    branch, and any line the nested-parenthesis case would have matched still contains `.fit(`,
    which `FIT_LEAK_MARKERS`' first pattern matches, so DSX-CODE-001's verdict on the fallback
    is unchanged for that shape. Narrow the sentence to the case the pattern genuinely still
    distinguishes -- a whitespace-separated dot, `Pipeline(steps) . fit (X_train)`, which
    `\.fit\s*\(` cannot match because it requires the dot and the method name to be adjacent.
    Record that this was measured, not reasoned: on that line `PIPELINE_FIT_TRAIN_RE.search`
    returns a match and no `FIT_LEAK_MARKERS` pattern does. Do NOT delete
    `PIPELINE_FIT_TRAIN_RE` -- deleting it would remove the only fallback detector for that
    shape and would delete the timing pin plan 03 deliberately tightened to a 0.05-second
    budget.

    **New tests**, all in `TestPhase11_1Code`, using its `_entrypoint`/`_check` helpers:
    `test_reordered_keyword_leak_fires_code_021_on_the_fallback_path`,
    `test_reordered_keyword_leak_verdicts_agree_across_both_paths`,
    `test_several_unrecognised_keywords_before_an_allowlisted_one_resolve_the_allowlisted_value`,
    `test_equality_comparison_first_argument_is_not_read_as_a_keyword_on_either_path`,
    `test_fit_call_re_timing_no_catastrophic_backtracking_with_reordered_keyword_form`, and
    `test_keyword_beyond_the_skip_bound_stays_uncaught_by_design_on_the_fallback`.

    The timing test follows the house shape already used by
    `test_fit_call_re_timing_no_catastrophic_backtracking_with_keyword_form`: local imports,
    inline input, one `perf_counter` bracket per input, a hard `assertLess`, no `subTest`. Give
    it at least two adversarial non-matching inputs built from repeated `name=value,` pairs --
    one with short values and one with values at the inner bound's length -- each under a
    1.0-second budget. Reference figures from the planning prototype, on inputs of 800,005 and
    1,620,005 characters: 0.0002 s and 0.0004 s. Record your own measured figures in the
    SUMMARY; do not copy these.

    The out-of-bound test must state in its docstring that its input is a REAL leak and not a
    true negative, and must carry the promotion protocol sentence verbatim on one physical line
    -- if it ever fails because a finding appeared, that is good news and it is promoted to a
    firing test rather than deleted. Check the exact wording already used by the file's other
    `stays_uncaught_by_design` tests and reuse it character for character, because plan 03
    left a `grep -c` consistency check over that sentence.
  </action>

  <verify>
    <automated>python -m unittest tests.test_dsx.TestPhase11_1Code -v</automated>
  </verify>

  <acceptance_criteria>
    - Behavior: an entrypoint containing `from sklearn.model_selection import train_test_split`,
      `train_test_split(df)`, `model.fit(y=y_train, X=full_frame)` and a trailing `unclosed(`
      produces exactly one DSX-CODE-021 finding at CRITICAL whose detail contains
      `'full_frame'`. Before this task the same file produces zero findings.
    - Behavior: the same entrypoint WITHOUT the trailing `unclosed(` line produces the same
      DSX-CODE-021 code and the same quoted token, and its report carries the
      `entrypoint parsed with ast (call-level scan)` pass line while the first carries a
      fallback pass line.
    - Behavior: an entrypoint whose post-split call is `model.fit(sample_weight=w, groups=g,
      data=full_frame)` produces DSX-CODE-021 with the token `full_frame` on both paths.
    - Behavior: entrypoints whose post-split calls are `model.fit(y=y_test)`,
      `model.fit(y=y_test, z=1)` and `model.fit(a==b, y)` each produce NO DSX-CODE-021 finding
      on either path.
    - Source: `python -c "import sys;sys.path.insert(0,'.');from dsx.checks import code as c;print([m.group(1) for m in c.FIT_CALL_RE.finditer('model.fit(y=y_train, X=full_frame)')])"`
      prints `['full_frame']`. Before this task it prints `[]` -- measured in both PowerShell and
      bash while revising this plan, exit 0 in both. The Python string literal is delimited with
      SINGLE quotes inside a double-quoted `-c` argument deliberately: backslash-escaped double
      quotes are a bash construct that PowerShell parses as an error, and the project's
      `CLAUDE.md` forbids emitting bash-only syntax. Keep this quoting shape for every variant
      below.
    - Source: the same one-liner, with only the input string swapped, prints `[]` over
      `imputer.fit(df`, `[]` over `model.fit(42)`, and `['X_train', 'data']` in that order over
      `imputer.fit(X_train); scaler.fit_transform(data)`. All three inputs are quote-free, so
      the single-quoted form carries them unchanged. Measured at HEAD in both shells while
      revising this plan: `[]`, `[]`, `['X_train', 'data']`.
    - Source: the `min_max_scaler.fit_transform(data[['Balance']])` case is discharged by the
      committed white-box test `test_fit_call_re_extracts_full_frame_token`
      (`tests/test_dsx.py` line 5908), which asserts the match is not None and that
      `m.group(1)` starts with `data`. Assert it there rather than as a one-liner: that input
      contains single quotes, so no single command line expresses it cleanly in both PowerShell
      and bash, and the committed test already pins it. The test must stay green with NO edit.
    - Source: `c.FIT_CALL_RE.groups` equals 1.
    - Source: `grep -c "instead of disagreeing on it" dsx/checks/code.py` reports 0. This grep
      is deliberately unfiltered, because the text it must not find IS comment text.
      <!-- planner-discipline-allow: instead of disagreeing on it -->
    - Source: `grep -c "trades one real miss for that guarantee" dsx/checks/code.py` reports 0,
      and the replacement sentence names the whitespace-separated-dot case. Also deliberately
      unfiltered, for the same reason.
      <!-- planner-discipline-allow: trades one real miss for that guarantee -->
    - Source: `grep -c "PIPELINE_FIT_TRAIN_RE" dsx/checks/code.py` is at least 2 -- the pattern
      is still defined and still consumed by `_first_fit_leak_line`; it was not deleted.
    - Source: `grep -c 'r"\\\\r?\\\\n"' dsx/checks/code.py` reports 0 and the widened
      `FIT_CALL_RE` contains no `\r`; `python -c "import sys;sys.path.insert(0,'.');from dsx.checks import code as c;print('\\r' in c.FIT_CALL_RE.pattern)"`
      prints `False`.
    - Source: the widened pattern contains no unbounded quantifier in the added fragment --
      every repetition operator introduced by this task is an explicit `{0,N}`.
    - Command: `python -m unittest tests.test_dsx.TestPhase11_1Code -v` exits 0 with the six
      new methods reported ok and no committed method edited.
    - Command: `python -m unittest tests.test_dsx -q` exits 0.
    - Source: `git diff --stat -- tests/test_known_bad_corpus.py examples/` prints nothing.
  </acceptance_criteria>

  <done>
    A real post-split leak whose recognised frame keyword arrives after another keyword is
    caught on the fallback path with the same verdict and the same token the parsed path
    already returns, without widening the keyword allowlist and without the pattern gaining an
    unbounded quantifier. The false parity claim is gone from both places it was written, the
    `PIPELINE_FIT_TRAIN_RE` trade-off sentence describes the case it actually trades, and the
    shape that stays uncaught after the widening is pinned by a named test rather than left
    implicit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A docstring stops deciding a CRITICAL verdict for DSX-CODE-020, DSX-CODE-030 and DSX-CODE-031 (GAP-2, SC4)</name>

  <files>dsx/checks/code.py, tests/test_dsx.py</files>

  <read_first>
    - `dsx/checks/code.py` lines 336-382 -- `_prose_line_indices`, its Rule A and Rule B, and
      the docstring sentence stating how many consumers the mask has, which this task makes
      false.
    - `dsx/checks/code.py` lines 1097-1118 -- `_first_line_matching`. Its `masked` parameter is
      the exact signature, default and skip placement the two new parameters must copy: keyword
      name `masked`, type `"frozenset[int]"`, default `frozenset()`, and the `if index in
      masked: continue` placed BEFORE the existing comment and import guards.
    - `dsx/checks/code.py` lines 1150-1164 -- `_first_full_frame_cleaning_line`, the first
      function to be threaded.
    - `dsx/checks/code.py` lines 1210-1257 -- `_stat_test_lines_referencing`, the second. Note
      that it reads `lines` TWICE: once as the candidate loop, once as the
      `_TARGET_REFERENCE_LOOKBACK` window slice.
    - `dsx/checks/code.py` lines 534-556 -- where `prose_mask` is computed once per `check()`
      call and where it is already passed to `_first_line_matching`.
    - `dsx/checks/code.py` lines 592-610 -- the DSX-CODE-020 call site, and lines 710-764 -- the
      DSX-CODE-030 and DSX-CODE-031 call sites.
    - `tests/test_dsx.py` lines 4548-4565 -- the `_entrypoint`/`_check` helpers, and line 6091
      -- `_check_with_target`, the helper that supplies `model.target`, which the DSX-CODE-030
      and DSX-CODE-031 tests need.
    - `tests/test_dsx.py` lines 5254-5289 --
      `test_multiline_string_interior_line_stays_uncaught_by_design`. Read the DOCSTRING, not
      only the assertions. It records two measurements this task falsifies, and it quotes the
      exact `_prose_line_indices` sentence this task rewrites. Its assertions are unaffected,
      so it stays green and nothing will surface the drift for you.
    - `tests/test_dsx.py` lines 6819-6848 --
      `test_split_marker_on_the_opening_or_closing_line_of_a_multiline_string_still_counts`.
      Same situation: its docstring calls the split-marker scan "the one place the prose mask
      is actually wired in the shipped code", which this task makes three places, while its
      assertions stay green.
    - Confirm the scope of the stale claim before editing: repo-wide, the phrase
      `exactly one consumer` appears in shipped code in exactly two files -- `dsx/checks/code.py`
      line 357 and `tests/test_dsx.py` line 5261. This task rewrites the first; it must not
      leave the second quoting it.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-AST-DESIGN.md`
      section 3.7 -- the mask's rules, and specifically why the opening line and the closing
      line of a multi-line string are deliberately spared.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-VERIFICATION.md`
      frontmatter, the GAP-2 entry.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-03-SUMMARY.md`,
      the "Prose Mask Coverage" section -- plan 03 measured this gap and declared it outside
      its own scope; that measurement is what this task closes.
  </read_first>

  <behavior>
    RED first. Every current value below was reproduced against the shipped code while
    planning.

    False positives that must close, all on the PRIMARY parsed path:
    - A module docstring whose text reads `We deliberately avoid df.fillna(df.mean()) before
      splitting.`, above a clean import and `train_test_split(df)`, draws no DSX-CODE-020.
      Today: DSX-CODE-020 CRITICAL, detail `Line 2: We deliberately avoid ...`.
    - The single-line form of the same docstring behaves identically -- Rule A masks a bare
      string statement whole, opening and closing lines included.
    - A module docstring mentioning a statistical-test call on the declared target, above a
      clean import and split, draws no DSX-CODE-030 for target `Exited`. Today: DSX-CODE-030
      CRITICAL at Line 2.
    - A FUNCTION docstring placed after the split, mentioning the same call, draws no
      DSX-CODE-031. Today: DSX-CODE-031 HIGH at Line 4.
    - A real `chi2_contingency(table)` call whose ONLY target reference sits on a prose line
      inside the three-line lookback window draws no DSX-CODE-030. Today: DSX-CODE-030
      CRITICAL at Line 3. This is the second place the function reads `lines`, and SC4's
      literal wording covers it: two files whose executable code is identical must not receive
      opposite verdicts because one of them documents itself.

    Controls that must NOT move -- masking prose must not delete a true positive:
    - `df['Age'] = df['Age'].fillna(df['Age'].mean())` before the split still draws
      DSX-CODE-020 CRITICAL at Line 2.
    - `table = pd.crosstab(df['Exited'], df['Geography'])` followed by
      `chi2_contingency(table)` before the split still draws DSX-CODE-030 CRITICAL at Line 3.
    - The line `df['Age'] = df['Age'].fillna(df['Age'].mean())` sits on three single-line
      string constants and must not be masked by Rule B. If it is, DSX-CODE-020 disappears
      outright, including on the corpus fixture.

    Limits that stay open and must be pinned rather than summarised away:
    - A cleaning idiom sharing the OPENING line of a multi-line string still draws
      DSX-CODE-020. The mask spares that line deliberately, because it can carry real code.
      Reproduced: `sql = """x"""; df = df.fillna(df.mean())` still fires.
    - On the FALLBACK path there is no tree, so `prose_mask` is the empty frozenset and the
      docstring false positive persists there for all three codes.
  </behavior>

  <action>
    Two commits: `test(11.1.1-04): pin the docstring-decides-a-verdict false positive as failing`
    (RED), then
    `fix(11.1.1-04): thread the prose mask into the cleaning and stat-test scanners` (GREEN).

    Add a keyword parameter `masked` to `_first_full_frame_cleaning_line`, typed
    `"frozenset[int]"` and defaulted to `frozenset()` -- identical in name, annotation and
    default to `_first_line_matching`'s, so the module has one convention rather than two. In
    its loop, skip an index that is in `masked` BEFORE the existing leading-comment and
    import guards, matching `_first_line_matching`'s ordering exactly.

    Add the same parameter to `_stat_test_lines_referencing`, and apply it in BOTH places that
    function reads `lines`. First: skip a masked index as a candidate for the statistical-test
    call itself. Second: exclude masked indices from the `_TARGET_REFERENCE_LOOKBACK` window
    before the reference search runs, so a prose line cannot supply the target reference that
    decides a real call's verdict. Build the filtered window by pairing each candidate line
    with its absolute index (the window starts at `max(0, index - _TARGET_REFERENCE_LOOKBACK)`,
    so the absolute index is that start plus the offset within the slice) and dropping the
    masked ones. Do not change `_TARGET_REFERENCE_LOOKBACK`, do not change how `reference_re`
    is built, and do not change the `re.escape` treatment of the target text -- that is threat
    T-11.1-11's mitigation and it stays exactly as it is.

    At both call sites inside `check()` -- the `cleaning_index` assignment and the
    `stat_test_lines` assignment -- pass `masked=prose_mask`, using the SAME `prose_mask`
    frozenset already computed once per `check()` call and already passed to
    `_first_line_matching`. Do not recompute the mask, do not call `_prose_line_indices` a
    second time, and do not re-walk the tree: plan 01 made one walk per `check()` call a
    correctness requirement, not only a performance one.

    Update `_prose_line_indices`'s docstring where it states how many consumers the mask has --
    that sentence becomes false. Replace it with the accurate list: the split-marker text scan,
    the full-frame-cleaning scan, and the statistical-test scan including its lookback window.
    In the same docstring, state plainly that the mask is EMPTY on the fallback path, because
    there is no tree to derive it from, so the docstring false positive persists there for
    every code it protects on the parsed path. That sentence is the source-side half of the
    README disclosure plan 05 owns; the two must name the same set of codes.

    **Follow that same sentence into the two committed test docstrings that quote it.** Both
    tests assert unrelated codes, so both stay GREEN after this task and nothing will flag the
    stale claims for you. Leaving them is not an option: this phase's subject is not leaving
    false claims in the source, and a false docstring under a passing test is the hardest kind
    to find later.

    1. `test_multiline_string_interior_line_stays_uncaught_by_design`
       (`tests/test_dsx.py` line 5254). **Keep the test. Do not promote, retitle or re-pin it.**
       It pins a split marker on a strictly interior line, and this task changes nothing about
       the split-marker scan, so the case it names stays genuinely uncaught and its three
       assertions stay correct. Two docstring sentences become false and must be corrected:
       - The sentence claiming `_prose_line_indices` "currently has exactly ONE consumer in the
         shipped code", which then quotes the source sentence `In this wave the mask has exactly
         one consumer`. After this task the mask has THREE consumers on the parsed path, and the
         sentence it quotes no longer exists in `dsx/checks/code.py` -- you rewrote it earlier
         in this same task. Corrected claim: the mask has three consumers on the parsed path --
         the split-marker text scan via `_first_line_matching`, the full-frame-cleaning scan via
         `_first_full_frame_cleaning_line`, and the statistical-test scan via
         `_stat_test_lines_referencing` including its lookback window -- and this pin exercises
         the first of the three. Point at the rewritten source sentence, not the deleted one.
       - The sentence claiming that DSX-CODE-020's own text loop "applies NO masking at all" and
         that a cleaning idiom inside a multi-line string's interior line "still fires
         DSX-CODE-020 unconditionally". This task is what stops it firing. Do NOT delete the
         sentence -- rewrite it as history, because deleting it erases the record that the gap
         was measured before it was closed. Corrected claim: plan 03 measured that gap and
         declared it outside its own scope; plan 04 task 2 closed it by threading the same mask
         into that scan, so a cleaning idiom on a strictly interior line no longer fires
         DSX-CODE-020. Keep the existing pointer to plan 03's SUMMARY section that recorded the
         measurement, and name plan 03 explicitly rather than saying "this plan", which now
         reads as plan 04.
    2. `test_split_marker_on_the_opening_or_closing_line_of_a_multiline_string_still_counts`
       (`tests/test_dsx.py` line 6819). **Keep the test unchanged in behaviour** -- Rule B's
       opening-line and closing-line sparing is untouched by this task and both assertions stay
       correct. One docstring clause becomes false: it calls the split-marker text scan "the one
       place the prose mask is actually wired in the shipped code". Corrected claim: the
       split-marker text scan is one of the three places the mask is wired after this task, and
       the boundary this test proves -- opening and closing lines spared, strictly interior
       lines masked -- is the SAME boundary for all three consumers, which is why it becomes
       user-visible for DSX-CODE-020 for the first time here. That last clause is the test-side
       half of the third README disclosure plan 05 owns.

    Neither correction may add a soundness claim, and neither may state that the fallback
    gained the mask -- it did not.

    Add the tests named in "Artifacts this phase produces" above. The DSX-CODE-030 and
    DSX-CODE-031 tests need a declared `model.target`, so use the `_check_with_target` helper
    rather than `_check`. The two limit tests --
    `test_multiline_string_opening_line_still_fires_code_020_stays_uncaught_by_design` and
    `test_docstring_still_fires_code_020_on_the_fallback_stays_uncaught_by_design` -- must each
    state in their docstring that the input is a REAL false positive that this task did not
    close, and must carry the promotion protocol sentence verbatim on ONE physical line, in the
    exact wording the file's other `stays_uncaught_by_design` tests use.

    Do not touch `_is_full_frame_impute`, `_is_full_frame_spread_filter`, `_IMPUTE_FILLNA_RE`,
    `_IMPUTE_STAT_RE`, `_SPREAD_SUBSCRIPT_RE`, `_SPREAD_STAT_RE` or `STAT_TEST_CALL_RE`. Threat
    T-11.1.1-04 requires those six full-frame symbols to stay byte-identical to commit
    `06ff2d7`, and plan 05 asserts that by diff.

    Reference measurement, taken while planning against a clean `git archive HEAD` copy with
    exactly this threading applied: all four false-positive shapes above returned no findings,
    both true-positive controls still fired with their original line numbers, and all 878
    committed tests still passed. Evidence, not a substitute for the RED step.
  </action>

  <verify>
    <automated>python -m unittest tests.test_dsx.TestPhase11_1Code -v</automated>
  </verify>

  <acceptance_criteria>
    - Behavior: an entrypoint whose first three lines are a triple-quoted module docstring
      containing `We deliberately avoid df.fillna(df.mean()) before splitting.`, followed by
      `from sklearn.model_selection import train_test_split` and `train_test_split(df)`,
      produces NO DSX-CODE-020 finding. Before this task it produces one at CRITICAL.
    - Behavior: the single-line docstring form of the same file also produces no DSX-CODE-020.
    - Behavior: with `model.target` set to `Exited`, a module docstring mentioning a
      `ttest_ind(df['Exited'], x)` call above a clean import and split produces NO DSX-CODE-030.
      Before this task it produces one at CRITICAL.
    - Behavior: with the same target, a function docstring placed AFTER the split mentioning
      the same call produces NO DSX-CODE-031. Before this task it produces one at HIGH.
    - Behavior: with the same target, a file whose only target reference is a bare string
      statement one line above a real `chi2_contingency(table)` call produces NO DSX-CODE-030.
      Before this task it produces one at CRITICAL.
    - Behavior (control): an entrypoint whose line 2 is
      `df['Age'] = df['Age'].fillna(df['Age'].mean())` and whose line 3 is `train_test_split(df)`
      still produces exactly one DSX-CODE-020 at CRITICAL whose detail begins `Line 2:`.
    - Behavior (control): with target `Exited`, an entrypoint whose line 2 builds a crosstab on
      `df['Exited']` and whose line 3 calls `chi2_contingency(table)` before the split still
      produces exactly one DSX-CODE-030 at CRITICAL whose detail begins `Line 3:`.
    - Source: `python -c "import sys,inspect;sys.path.insert(0,'.');from dsx.checks import code as c;print('masked' in inspect.signature(c._first_full_frame_cleaning_line).parameters, 'masked' in inspect.signature(c._stat_test_lines_referencing).parameters)"`
      prints `True True`.
    - Source: `python -c "import sys,inspect;sys.path.insert(0,'.');from dsx.checks import code as c;print(inspect.signature(c._first_full_frame_cleaning_line).parameters['masked'].default == frozenset())"`
      prints `True` -- every pre-existing caller is unaffected by the new parameter.
    - Source: `grep -v '^#' dsx/checks/code.py | grep -c "masked=prose_mask"` reports 3 -- the
      pre-existing `_first_line_matching` call site plus the two added here.
    - Source: `grep -c "_prose_line_indices(" dsx/checks/code.py` reports 2 -- the definition
      and the single call site in `check()`. The mask is computed once, not per consumer.
    - Source: `grep -c "exactly one consumer" dsx/checks/code.py` reports 0 AND
      `grep -c "exactly one consumer" tests/test_dsx.py` reports 0. Before this task each
      reports 1 -- `dsx/checks/code.py` line 357 and `tests/test_dsx.py` line 5261. Correcting
      only the source copy and leaving the test quoting it is the exact failure this criterion
      exists to catch. Both greps are deliberately unfiltered, because the text they must not
      find IS comment and docstring text.
      <!-- planner-discipline-allow: exactly one consumer -->
    - Source: `grep -c "prose mask is actually wired in the shipped code" tests/test_dsx.py`
      reports 0. Before this task it reports 1, inside
      `test_split_marker_on_the_opening_or_closing_line_of_a_multiline_string_still_counts`.
      <!-- planner-discipline-allow: prose mask is actually wired in the shipped code -->
    - Source: `grep -c "still fires DSX-CODE-020 unconditionally" tests/test_dsx.py` reports 0.
      Before this task it reports 1, and this task is what makes it false.
      <!-- planner-discipline-allow: still fires DSX-CODE-020 unconditionally -->
    - Source: `grep -c "def test_multiline_string_interior_line_stays_uncaught_by_design" tests/test_dsx.py`
      reports 1 and
      `grep -c "def test_split_marker_on_the_opening_or_closing_line_of_a_multiline_string_still_counts" tests/test_dsx.py`
      reports 1 -- both committed pins survive. Correcting a docstring must not become deleting
      or renaming the test that carried it.
    - Behavior: both of those tests still pass, and `git diff -- tests/test_dsx.py` shows no
      change to either one's `assert` lines or fixture source -- only to its docstring.
    - Source: `git diff -- dsx/checks/code.py` shows no change to `_is_full_frame_impute`,
      `_is_full_frame_spread_filter`, `_IMPUTE_FILLNA_RE`, `_IMPUTE_STAT_RE`,
      `_SPREAD_SUBSCRIPT_RE`, `_SPREAD_STAT_RE` or `STAT_TEST_CALL_RE`.
    - Command: `python -m unittest tests.test_dsx.TestPhase11_1Code -v` exits 0.
    - Command: `python -m unittest tests.test_known_bad_corpus -q` exits 0 and reports 30 tests.
    - Source: `git diff --stat -- tests/test_known_bad_corpus.py examples/` prints nothing.
  </acceptance_criteria>

  <done>
    Two files whose executable code is byte-identical and which differ only in a sentence of
    documentation receive the same verdict for DSX-CODE-020, DSX-CODE-030 and DSX-CODE-031 on
    the parsed path, exactly as they already do for DSX-CODE-001 and DSX-CODE-021. Real
    executable code still fires at the same line numbers. The mask is still computed once per
    run from one walk. The two limits that stay open -- the multi-line string's opening line,
    and the whole of the fallback path -- are pinned by named tests and stated in the source,
    not left for a reader to discover.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: A structurally malformed notebook is named NOT scanned instead of crashing the gate (GAP-3, SC5)</name>

  <files>dsx/checks/code.py, tests/test_dsx.py</files>

  <read_first>
    - `dsx/checks/code.py` lines 1023-1094 -- `_read_source` in full. The `.ipynb` branch's
      `try`/`except (OSError, json.JSONDecodeError)` guards only `json.loads`; everything after
      it assumes the document is an object and every cell is an object.
    - `dsx/checks/code.py` lines 501-518 -- `check()`'s early returns, including the
      `source is None` branch whose `report.ok(...)` text this fix routes into, and the
      unguarded `_read_source(path)` call the exception currently escapes through.
    - `dsx/findings.py` lines 1-15 -- the exit-code contract. Exit 2 means the check could not
      run; exit 1 is `EXIT_BLOCK`. An uncaught AttributeError produces 1, which is why this is
      a contract violation rather than a cosmetic bug.
    - `dsx/cli.py` around lines 791-804 -- the top-level handler, which catches `CheckError`,
      `SpecParseError`, `ValueError` and `KeyboardInterrupt`, and therefore does not catch this.
    - `tests/test_dsx.py` line 5610 --
      `test_notebook_degenerate_shapes_produce_no_finding_no_exception`, the test the new ones
      sit alongside and whose shape they should follow. Note that
      `_entrypoint` hardcodes `name="entry.py"`, so a notebook test must pass
      `name="entry.ipynb"` explicitly.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-AST-DESIGN.md`
      section 1.1(c) and the amendment banner's "Undecodable entrypoint" row -- the rule that a
      skip stays visible on every channel.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-VERIFICATION.md`
      frontmatter, the GAP-3 entry.
  </read_first>

  <behavior>
    RED first. Two of these four were reproduced directly by verification; all four were
    reproduced against the shipped code while planning.

    - `_read_source` on a `.ipynb` whose entire content is the two characters `[]` returns
      `None` and raises nothing. Today: `AttributeError: 'list' object has no attribute 'get'`.
    - `_read_source` on a `.ipynb` containing `{"cells": ["not-a-dict-cell"]}` returns `None`
      and raises nothing. Today: `AttributeError: 'str' object has no attribute 'get'`.
    - `_read_source` on a `.ipynb` containing `null` returns `None` and raises nothing.
    - `_read_source` on a `.ipynb` whose `cells` value is an object rather than a list returns
      `None` and raises nothing.
    - `check()` over each of those four, with the notebook declared as the entrypoint, returns
      a report with zero findings and a `passed_checks` line containing `NOT scanned`, and
      raises nothing. Today the first two propagate an AttributeError straight out of
      `check()`.
    - A well-formed notebook is unaffected: a document with one code cell whose source is
      `model.fit(df)\n` still reads back as that source, and the committed notebook
      line-geometry tests still pass unchanged.
  </behavior>

  <action>
    Two commits: `test(11.1.1-04): pin the malformed-notebook crash as failing` (RED), then
    `fix(11.1.1-04): guard the notebook document's structural assumptions` (GREEN).

    In `_read_source`'s `.ipynb` branch, guard the two structural assumptions the same way the
    parse itself is already guarded. First, immediately after the `try`/`except` around
    `json.loads` and BEFORE `nb.get("cells")`, return `None` unless `nb` is a `dict`. Second,
    as the first statement inside the cell loop and BEFORE the first `cell.get(...)`, return
    `None` unless the cell is a `dict`.

    **Return `None` for a non-object cell -- do not `continue` past it.** The independent
    review suggested `continue`; take `return None` instead, which is what the verification
    report's own `missing:` item specifies, and there are two reasons that are not taste.
    Measured while planning: with `continue`, `{"cells": ["x"]}` reads back as the empty string,
    so `check()` scans nothing and prints the affirmative `entrypoint parsed with ast
    (call-level scan)` pass line with zero findings -- a silent pass over a document nothing was
    read from, which is exactly what SC5 forbids. And silently dropping a cell renumbers every
    line below it, which breaks the byte-identical notebook line geometry plan 01's Decision 2
    and plan 02's character-wise markdown blanking were built to preserve. With `return None`,
    the same input routes into `check()`'s existing `source is None` branch and produces the
    `entrypoint NOT scanned -- could not be read ...` pass line and no findings; measured, that
    is exactly what happens.

    Widen that branch's parenthetical reason text so it names the structural class as well as
    the ones it already lists. It currently names an unreadable file, an unsupported suffix and
    invalid notebook JSON; a document that IS valid JSON but is not a cell-bearing object is a
    fourth class and the reason should say so, because a reader who sees "invalid notebook
    JSON" over a file that parses as JSON has been told something slightly false. The committed
    tests assert only the substring `NOT scanned`, not the parenthetical, so this is safe --
    confirm that by grep before editing.

    Do not add a `try`/`except` around the `_read_source` call in `check()`. The fix belongs at
    the point where the assumption is made, not in a catch-all at the call site: a broad guard
    there would also swallow the next unguarded assumption instead of surfacing it.

    Add `test_non_dict_notebook_document_is_named_not_scanned_without_raising` and
    `test_non_dict_notebook_cell_is_named_not_scanned_without_raising` alongside
    `test_notebook_degenerate_shapes_produce_no_finding_no_exception`, following its shape.
    Each must assert BOTH levels: that `_read_source` returns `None` and raises nothing, and
    that `check()` over the same file returns a report with zero findings and a `NOT scanned`
    pass line. Asserting only the helper would leave the contract the gap is actually about --
    that the gate reports a decidable state rather than a traceback -- untested. Cover all four
    malformed shapes across the two tests: the non-object document, the object whose `cells`
    holds a non-object, the JSON `null` document, and the object whose `cells` is not a list.
  </action>

  <verify>
    <automated>python -m unittest tests.test_dsx.TestPhase11_1Code -v</automated>
  </verify>

  <acceptance_criteria>
    - Behavior: `_read_source(Path(tmp, "entry.ipynb"))` on content `[]` returns `None` and
      raises nothing. Before this task it raises
      `AttributeError: 'list' object has no attribute 'get'`.
    - Behavior: the same call on `{"cells": ["not-a-dict-cell"]}` returns `None` and raises
      nothing. Before this task it raises
      `AttributeError: 'str' object has no attribute 'get'`.
    - Behavior: the same call on `null` and on `{"cells": {"a": 1}}` each returns `None` and
      raises nothing.
    - Behavior: `check()` with each of those four declared as the entrypoint returns a report
      whose `findings` is empty and whose `passed_checks` contains a line containing
      `NOT scanned`, and raises nothing.
    - Behavior (control): `_read_source` on a well-formed notebook whose single code cell has
      source `model.fit(df)\n` still returns `model.fit(df)\n`.
    - Source: `grep -v '^#' dsx/checks/code.py | grep -c "isinstance(nb, dict)"` reports 1 and
      `grep -v '^#' dsx/checks/code.py | grep -c "isinstance(cell, dict)"` reports 1.
    - Source: the `isinstance(nb, dict)` guard appears before the `nb.get("cells")` call and
      the `isinstance(cell, dict)` guard appears before the first `cell.get(` call, verified by
      comparing their line numbers with `grep -n`.
    - Source: `grep -c "_read_source(path)" dsx/checks/code.py` reports 1 and no `try` statement
      was added around it in `check()`.
    - Command: `python -m unittest tests.test_dsx.TestPhase11_1Code -v` exits 0, with the two
      new methods and the existing `test_notebook_degenerate_shapes_produce_no_finding_no_exception`
      all reported ok.
    - Command: `python -m unittest discover -s tests -q` exits 0.
  </acceptance_criteria>

  <done>
    Every structurally malformed notebook shape the gate can be handed -- a document that is not
    an object, a `cells` value that is not a list, and a cell that is not an object -- produces
    the same visible outcome as any other unscannable entrypoint: zero findings, one
    `report.ok(...)` line naming the file as NOT scanned, and a gate that returns a decidable
    exit code instead of a raw traceback and an exit code that means something else.
  </done>
</task>

</tasks>

<threat_model>
Application Security Verification Standard enforcement level 1; blocking severity threshold
`high`. Relevant here rather than boilerplate: `_read_source` parses input supplied by whoever
runs the gate, and GAP-3 is exactly an unhandled-input crash; and the widened `FIT_CALL_RE` is a
regular expression run over untrusted source text, in a module that already fixed a cubic and a
quadratic pattern under threat `T-11.1-01` during Phase 11.1's security gate (commit `06ff2d7`,
`11.1-SECURITY.md`).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyst-supplied entrypoint file -> `_read_source` | untrusted bytes and untrusted JSON STRUCTURE cross here; valid JSON guarantees nothing about shape |
| analyst-supplied entrypoint text -> every pattern on the fallback path | untrusted input drives regular-expression engine work with no size cap, by deliberate design (a cap would silently stop scanning large files, a worse failure for a blocking gate than a slow scan) |
| spec `model.target` string -> `_stat_test_lines_referencing`'s `reference_re` | untrusted text is compiled into a pattern; already mitigated by `re.escape` under T-11.1-11 and unchanged by this plan |
| `_read_source` return value -> `check()`'s `source is None` branch | the only channel that distinguishes "scanned clean" from "not scanned at all" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.1.1-11 | Denial of Service | `FIT_CALL_RE`, widened in task 1 | high | mitigate | Both added repetitions carry explicit `{0,N}` bounds; the inner character class excludes the `,` that terminates it, so no repetition step is ambiguous; a `(?!=)` guard stops `==` being consumed as a keyword separator. Task 1 adds `test_fit_call_re_timing_no_catastrophic_backtracking_with_reordered_keyword_form` with at least two adversarial non-matching inputs under a 1.0-second budget. Re-measured for this pattern, never inherited from a sibling's linearity proof (11.1.1-RESEARCH.md Pitfall 6). Planning prototype: 0.0002 s and 0.0004 s on inputs of 800,005 and 1,620,005 characters. |
| T-11.1.1-12 | Denial of Service | `_read_source` `.ipynb` cell loop, task 3 | low | accept | The loop bound is the cell count already materialised by `json.loads`; the added guard only shortens the loop, never lengthens it, and introduces no new allocation. Accepted with the note that no size cap is introduced anywhere on this path, consistent with the module's existing decision. |
| T-11.1.1-13 | Repudiation | `_read_source` structural assumptions reaching `check()` uncaught | high | mitigate | Task 3 returns `None` on every malformed shape so the existing `report.ok(...)` "NOT scanned" branch runs. Without it the gate exits 1 with a raw traceback, numerically indistinguishable from `EXIT_BLOCK`, which defeats the documented contract that a caller can decide the outcome from the exit code alone. |
| T-11.1.1-14 | Repudiation | the prose-mask widening in task 2 | medium | mitigate | The change removes findings, so an operator could read a newly-clean file as new evidence. Mitigated by pinning the retained limits as named tests (the multi-line string's opening line, and the whole fallback path), by stating the fallback's empty mask in `_prose_line_indices`' own docstring, and by plan 05's mandatory two-direction README announcement. Prohibition 3 of this plan makes shipping the looser direction unannounced a violation. |
| T-11.1.1-15 | Tampering | `tests/test_known_bad_corpus.py` expectation maps | high | mitigate | Prohibition 2, plus an acceptance criterion on every task: `git diff --stat -- tests/test_known_bad_corpus.py examples/` prints nothing. Editing a map to absorb a moved exit code destroys the only evidence of whether the change or the fixture is wrong. |
| T-11.1.1-07 | Spoofing | the widened fallback capture group | medium | mitigate | Unchanged and re-asserted: the capture stays the argument VALUE, never a keyword name, and the six-name allowlist is not widened. Pinned by the `model.fit(y=y_test)` and `model.fit(y=y_test, z=1)` silence criteria on both paths. A false-positive flood is as damaging to a blocking gate as a blind spot, and faster to erode trust. |
| T-11.1.1-04 | Tampering | the six full-frame-cleaning symbols | high | mitigate | Task 2 must not touch `_is_full_frame_impute`, `_is_full_frame_spread_filter`, `_IMPUTE_FILLNA_RE`, `_IMPUTE_STAT_RE`, `_SPREAD_SUBSCRIPT_RE` or `_SPREAD_STAT_RE`; they stay byte-identical to commit `06ff2d7`, asserted by diff in plan 05. |
| T-11.1.1-SC | Tampering | package installs | low | accept | No `npm`, `pip` or `cargo` step exists on this path. `ast`, `json` and `re` are standard library and already imported by this module; nothing is installed, so the Package Legitimacy Gate stays untriggered and no legitimacy checkpoint is required. |
</threat_model>

<verification>
1. `python -m unittest tests.test_dsx.TestPhase11_1Code tests.test_dsx.TestPhase3Code -v` exits
   0 after every task commit (the phase's quick command, ~5 seconds, per
   `11.1.1-VALIDATION.md`).
2. `python -m unittest discover -s tests -q` exits 0 at the end of the plan, and the reported
   test count equals 878 plus the number of methods added by this plan. Record both numbers as
   literals in the SUMMARY.
3. `python -m unittest tests.test_known_bad_corpus -v` exits 0 and reports 30 tests.
4. `python scripts/gen-finding-catalogue.py --check` exits 0 -- no `report.add(...)` call site
   moved out of `check()`'s body, so the D-05 citation resolution is undisturbed.
5. `python scripts/validate-capability.py` exits 0.
6. `git diff --stat -- tests/test_known_bad_corpus.py examples/` prints nothing across the whole
   plan.
7. `grep -c "instead of disagreeing on it" dsx/checks/code.py` and
   `grep -c "trades one real miss for that guarantee" dsx/checks/code.py` both report 0.
   <!-- planner-discipline-allow: instead of disagreeing on it -->
   <!-- planner-discipline-allow: trades one real miss for that guarantee -->
8. The three new `stays_uncaught_by_design` pins each add one method AND one copy of the
   promotion protocol sentence, so both counts move by exactly +3 against baselines measured on
   a clean `tests/test_dsx.py` at commit `8aacd07`:
   `grep -c "promoted to a firing test rather than deleted" tests/test_dsx.py` goes from 14 to
   17, and `grep -c "def test_.*stays_uncaught_by_design" tests/test_dsx.py` goes from 11 to 14.
   Check the DELTA on each count separately. Do NOT check the two counts against each other:
   they differ by 3 today, that offset is legitimate and structural, and this plan preserves it.
   The sentence appears three times outside any `stays_uncaught_by_design` method -- the Part B
   section banner at `tests/test_dsx.py:5064`, and the docstrings of
   `test_comment_mentioning_smote_still_fires_code_003_measured_not_assumed` (line 6794) and
   `test_comment_mentioning_scaler_fit_transform_still_fires_code_002_measured_not_assumed`
   (line 6810). Those three are the DSX-CODE-002 / DSX-CODE-003 honesty record plan 03
   installed. Editing the banner or either docstring to make two numbers agree would damage
   exactly the record this phase exists to protect, and is forbidden. Both greps are
   deliberately unfiltered, because the text they must find IS comment and docstring text. Run
   them in bash -- see item 10.
9. `python -W error::SyntaxWarning -c "import ast;ast.parse(open('tests/test_dsx.py',encoding='utf-8').read())"`
   raises nothing. Plan 03 hit an unescaped-backslash `SyntaxWarning` in a docstring; this plan
   adds docstrings containing regular-expression and backslash text, so run it before the final
   commit.
10. Shell, stated per command rather than certified for all of them at once. Every `python` and
    every `git` command in this plan runs unchanged in Windows PowerShell. Every criterion that
    invokes `grep` is a BASH criterion and must be run through the executor's Bash tool or Git
    Bash -- run it in PowerShell and it fails. Measured on this machine while revising this
    plan: `grep` resolves to `C:\Program Files\Git\usr\bin\grep.exe`, and that directory is on
    neither the persisted machine PATH nor the persisted user PATH, so a native PowerShell
    session raises `CommandNotFoundException` for it. That is a loud failure rather than a
    silent pass, which is why this is a labelling correction and not a rewrite of the criteria.
    No heredoc and no `printf` appears anywhere in this plan, and no criterion uses bash-only
    quoting: the `FIT_CALL_RE` one-liners in task 1 delimit their Python string literals with
    single quotes inside a double-quoted `-c` argument, which parses identically in both shells
    (verified in both while revising this plan). Commit messages go to a file and are applied
    with `git commit -F <file>`, which the project's `CLAUDE.md` requires and which works in
    both shells.
</verification>

<success_criteria>
- A real post-split leak whose recognised frame keyword is not the first keyword is caught on
  the fallback path with the same verdict and token the parsed path returns, and the source no
  longer claims the two mechanisms agree.
- Two files whose executable code is identical and which differ only in documentation receive
  the same verdict for DSX-CODE-020, DSX-CODE-030 and DSX-CODE-031, and both true-positive
  controls still fire at their original line numbers.
- Every structurally malformed notebook shape produces a visible NOT-scanned pass line and a
  decidable exit code instead of a traceback.
- SC1, SC3 and SC6 do not regress: the full suite, the known-bad corpus, the catalogue check and
  the capability validation are all green, and no corpus expectation map is edited.
- Nothing added by this plan records any DSX-CODE as sound, and every limit that stays open is
  pinned by a named test whose docstring says the input is a real defect rather than a true
  negative.
</success_criteria>

<output>
Create `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-04-SUMMARY.md` when
done. Record in it, as literal values rather than as a claim that they passed: the five gate
commands' exit codes; the full-suite test count before and after; the corpus test count; the two
repetition bounds chosen for the widened pattern and the measured timing figures for each
adversarial input, on both `python` and `python3`; the exact set of committed tests that
changed status, if any, and how each was disposed of; and one line per limit that stays open
after this plan, naming the test that pins it.
</output>
