---
phase: 11.1.1-detection-code-hardening-inserted
plan: 03
type: execute
wave: 3
depends_on: [11.1.1-02]
files_modified:
  - tests/test_dsx.py
  - README.md
autonomous: true
# No REQ identifiers by design (ROADMAP: "Requirements: None new"). Coverage is keyed to
# the ROADMAP's four numbered success criteria, labelled SC1-SC4. This plan carries the
# honesty half of SC2 and the whole of SC4.
requirements: [SC2, SC4]

must_haves:
  truths:
    - "SC2 (honesty): three leaky forms that this phase knowingly does not catch are pinned by tests that say so in their own docstrings — `partial_fit` in bare and keyword form, a fit call whose argument is itself a call, and a fit call whose argument sits on a different physical line from its opening parenthesis. Each test asserts DSX-CODE-021 is absent AND records in prose that the input is a real leak, not a true negative."
    - "SC2 (honesty): no shipped artefact records DSX-CODE-021 as sound or complete. `README.md` names the three uncaught forms in plain language, and the comment above `FIT_CALL_RE` in `dsx/checks/code.py` names them too."
    - "SC2 (honesty): the variant provenance cited anywhere in this phase is the 13-variant table independently reproduced against the live checker on 2026-08-21 — 6 caught, 7 missed — and not the older uncommitted figure from the Phase 11.1 verification session, for which no enumeration is committed anywhere in the repository."
    - "Announcement: `README.md` states that files which pass the entrypoint gate today may start failing it after this change, that those are true positives, and which four newly-caught shapes cause it. A user upgrading and seeing a new CRITICAL finding can find out why without reading the planning directory."
    - "SC4: `python -m unittest discover -s tests -q`, `python -m unittest tests.test_known_bad_corpus -v`, `python scripts/gen-finding-catalogue.py --check` and `./scripts/check.sh` all exit 0."
    - "SC4 (edge: adjacency): the only two fit calls anywhere under `examples/` are zero-whitespace and positional — `StandardScaler().fit_transform(X)` and `scaler.fit_transform(data[['Age']])`. Neither is a form this phase newly catches, so neither fixture's gate exit code moves. Verified during planning by grepping every Python file under `examples/`."
    - "SC4 (edge: empty): `examples/analysis/activation_readout.py` contains no fit call at all and still draws neither DSX-CODE-001 nor DSX-CODE-021, and `examples/good-ANALYSIS-SPEC.yaml` still exits 0 at all four gate points."
    - "SC4 (edge: ordering): the corpus harness compares expected codes as sets and frozensets, so the order findings arrive in cannot change a fixture's verdict. Finding order itself is already pinned by the existing `test_run_twice_over_same_entrypoint_is_deterministic`."
    - "Threat T-11.1-01 is still closed: `test_full_frame_cleaning_predicates_timing_no_catastrophic_backtracking` passes and the six full-frame cleaning symbols are byte-identical to their state at commit `06ff2d7`."
  artifacts:
    - path: README.md
      provides: "The user-facing announcement of the behaviour change, and the plain-language statement of what the scan still cannot see"
      contains: "What the fit-before-split scan does not catch"
    - path: tests/test_dsx.py
      provides: "Deliberate-scope-boundary tests pinning the three knowingly-uncaught forms"
  key_links:
    - from: "README.md"
      to: "dsx/checks/code.py"
      via: "the README's list of uncaught forms and the comment above FIT_CALL_RE must name the same three forms. If they drift, one of them is lying to a reader who has no way to tell which."
      pattern: "partial_fit"
    - from: "tests/test_dsx.py deliberate-scope-boundary tests"
      to: "README.md `### What the fit-before-split scan does not catch`"
      via: "each test pins one form the README claims is uncaught, so the README's claim is executable rather than merely written down"
      pattern: "stays_uncaught_by_design"
  prohibitions:
    - statement: "MUST NOT record DSX-CODE-021 as sound, complete, reliable, or as catching every leaky fit call — in the README, in a source comment, in a docstring, in the finding catalogue, or in this phase's SUMMARY. A clean entrypoint scan is not evidence that no leak exists, and any sentence a reader could take that way is the failure this criterion exists to prevent."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT ship this behaviour change unannounced. Fixing CR-03 and CR-04 makes files that pass the gate today start failing it. Those are true positives, but a blocking gate that silently gets stricter costs its users trust they do not get back. The announcement is a shipped artefact of this phase, not a nice-to-have."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT cite the older uncommitted variant-count figure from the Phase 11.1 verification session as though a committed enumeration backs it. Three files were checked during research and none enumerates those variants. The committed provenance is the 13-variant table in `11.1.1-RESEARCH.md` Pitfall 3, reproduced against the live checker on 2026-08-21."
      status: unverified
      verification: flagged-unverified
    - statement: "MUST NOT weaken, delete or rewrite an existing corpus expectation — `_TARGET_DEFECT_CODES`, `_EXPECTED_CAUGHT_DEFECTS` or `_INCIDENTAL_GAP_CODES` in `tests/test_known_bad_corpus.py` — to absorb a gate-result change caused by this phase. A moved fixture exit code means either the change or the fixture is wrong; editing the map destroys the only evidence of which."
      status: unverified
      verification: flagged-unverified
---

<objective>
Close the honesty half of ROADMAP success criterion 2 and all of success criterion 4: pin
the leaky forms this phase knowingly does not catch, announce the behaviour change to
users in a named shipped file, and prove the whole suite and the known-bad corpus stayed
green.

Purpose: plans 01 and 02 make two blocking checks fire more often. That is the right
outcome and it is a user-visible change — files that pass today will start failing. The
ROADMAP is explicit that the change "must be announced rather than slipped in", and
equally explicit that "nothing may record `DSX-CODE-021` as sound". Both obligations are
prose obligations, which is exactly the kind that quietly does not get met. This plan
turns both into shipped artefacts: tests that fail if a knowingly-uncaught form silently
starts being caught or a caught form silently stops, and a README section a user actually
reads.

Output: three deliberate-scope-boundary tests in `tests/test_dsx.py::TestPhase11_1Code`;
two new sections in `README.md`, one announcing the behaviour change and one stating in
plain language what the scan still cannot see; and a clean run of the full suite, the
known-bad corpus, the catalogue check and the phase gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-RESEARCH.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-PATTERNS.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-VALIDATION.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-01-PLAN.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-01-SUMMARY.md
@.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-02-SUMMARY.md
@.claude/skills/plain-language/SKILL.md
@dsx/checks/code.py
@tests/test_dsx.py
@README.md
</context>

<announcement_venue_decision>
The ROADMAP asks for "a changelog entry, release note, or equivalent — pick one, name the
file." The named file is **`README.md`**.

Why, rather than a new `CHANGELOG.md`: this repository has no changelog and no release
notes. Checked during planning — no `CHANGELOG.md` at the root, no release-note file
anywhere outside `.planning/`, and no reference to either in `scripts/check.sh` or any
other script. Creating one now would put this phase's announcement in a file nobody reads
and nothing maintains, which is a worse outcome than not announcing at all because it
looks like an announcement.

`README.md` is where this project already announces behaviour changes to users. It carries
`### Migrating a pre-v2.0.0 spec`, which exists for exactly this shape of message: "a
thing that passed before now blocks, here is why and here is what to do." It also carries
`## Known limits`, which is where this project already writes down what its gates cannot
see. Both new sections go where their neighbours already are.
</announcement_venue_decision>

<planning_verification>
Every claim below was executed during planning, not inferred.

| Claim | How it was checked | Result |
|---|---|---|
| No changelog convention exists | `ls CHANGELOG.md docs/`, plus a repository-wide case-insensitive grep for changelog and release-note references outside `.planning/` | no `CHANGELOG.md`; `docs/` contains one unrelated file; no references found |
| README already announces behaviour changes | `grep -n "^#\{1,3\} " README.md` | `### Migrating a pre-v2.0.0 spec` at line 135, `## Known limits` at line 309 with three existing `###` subsections |
| Every fit call under `examples/` | `grep -rn "\.fit\|partial_fit" examples/ --include=*.py` | exactly two: `StandardScaler().fit_transform(X)` in `examples/analysis/leaky_model.py`, `scaler.fit_transform(data[['Age']])` in `examples/known-bad/full-frame-cleaning-entrypoint.py`. Both zero-whitespace, both positional, neither newly caught. |
| `activation_readout.py` has no fit call | same grep, zero hits in that file | confirmed — this is the SC4 empty-input edge |
| The corpus harness already asserts exit codes per fixture | read `tests/test_known_bad_corpus.py` lines 605-660 | `test_every_spec_blocks_only_on_its_target_defect_at_critical_threshold_points` asserts `plan` and `execute` exit codes against `_TARGET_DEFECT_CODES` on every run — no new harness code needed for SC4 |
| No existing test pins a form as deliberately uncaught | pattern mapper grepped `tests/test_dsx.py` for `uncaught`, `stays uncaught`, `remains uncaught`, `not_caught` | zero hits. **This idiom does not exist in the codebase yet and is being introduced here.** The nearest structural scaffold is `test_malformed_fit_calls_no_finding_no_exception` at line 4690. |
| The 13-variant provenance | `11.1.1-RESEARCH.md` Pitfall 3, reproduced against the live module on 2026-08-21 | 13 variants run, 6 caught, 7 missed. This is the committed provenance for this phase. |
</planning_verification>

<edge_coverage>
Every SC4 row from the deterministic edge probe is resolved here. Nothing was dropped.

| Probe row | Resolution | Where |
|---|---|---|
| SC4 / adjacency | covered | must_haves truth naming both corpus fit calls as zero-whitespace and positional; enforced by the corpus run in task 3 exiting 0 with no fixture edit |
| SC4 / empty | covered | must_haves truth naming `activation_readout.py` (no fit call at all) and the good fixture clearing all four gate points; enforced by `./scripts/check.sh`'s four-point gate contract |
| SC4 / ordering | covered | must_haves truth: the corpus harness compares code sets and frozensets, so finding order cannot change a verdict; finding order itself is already pinned by `test_run_twice_over_same_entrypoint_is_deterministic` |
</edge_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the three knowingly-uncaught forms as deliberate scope boundaries</name>

  <files>tests/test_dsx.py</files>

  <read_first>
    - `tests/test_dsx.py` lines 4469-4501 — `TestPhase11_1Code`'s class header and its
      `_entrypoint` / `_check` helpers. Reuse them.
    - `tests/test_dsx.py` lines 4690-4700 — `test_malformed_fit_calls_no_finding_no_exception`.
      This is the closest structural scaffold in the codebase. Copy its shape, not its
      meaning: it pins a TRUE negative (a malformed call is not a leak), whereas these three
      tests pin a REAL leak that this module knowingly cannot see. The difference must be
      visible in the test, or a later reader will treat these as ordinary negatives and
      "fix" one by deleting it.
    - `tests/test_dsx.py` lines 4644-4670 —
      `test_fit_call_x_train_after_split_no_finding` and
      `test_lexicon_prefix_variants_after_split_no_finding`, two ordinary true-negative tests,
      so you can see exactly what these new tests must NOT be mistaken for.
    - `dsx/checks/code.py` — the comment block above `FIT_CALL_RE` as plan 02 left it,
      which already names the same three forms. The tests and that comment must agree.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-RESEARCH.md` Pitfall 3
      — the 13-variant table, which is the committed provenance for which forms are caught
      and which are not.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-PATTERNS.md`, the
      "No Analog Found" section — this idiom does not exist in the codebase and is being
      introduced here.
  </read_first>

  <behavior>
    Three new test methods in `tests/test_dsx.py::TestPhase11_1Code`, each asserting that
    DSX-CODE-021 is ABSENT for an entrypoint that genuinely leaks, and each carrying a
    docstring saying so:

    - `test_partial_fit_after_split_stays_uncaught_by_design` — `model.partial_fit(data)`
      and `model.partial_fit(X=data)`, both after a split marker, on a full frame. Real
      leaks. No finding today, no finding after this phase: the incremental-fit method name
      was never in the pattern's method alternation at all.
    - `test_chained_call_argument_after_split_stays_uncaught_by_design` —
      `model.fit(loader.get_full_frame())` after a split. A real leak. The captured token's
      character class excludes the opening parenthesis, so a call used as an argument does
      not match.
    - `test_multi_line_fit_call_after_split_stays_uncaught_by_design` — a fit call whose
      opening parenthesis ends one physical line and whose frame argument sits on the next,
      after a split. A real leak. The scan is per physical line, and plan 01's
      backslash-continuation join is deliberately scoped to the before-split check only, so
      this stays invisible to DSX-CODE-021.

    Each docstring must state three things: that the input is a real leak and not a true
    negative; that it is knowingly out of this module's detection scope, citing the ROADMAP
    Phase 11.1.1 success criterion 2 allowance by name; and that if this test ever fails
    because a finding appeared, that is good news and the test should be promoted to a
    firing test rather than deleted.

    Add a fourth method, `test_dsx_code_021_scope_boundary_is_documented`, asserting that
    `README.md` contains a section heading naming what the fit-before-split scan does not
    catch, and that the section body mentions the incremental-fit method name. This makes
    the documentation obligation executable rather than a promise. It will FAIL until task 2
    lands, which is intended — this task and task 2 are RED and GREEN of the same
    obligation.
  </behavior>

  <action>
    Add the four methods to `TestPhase11_1Code` in `tests/test_dsx.py`, in their own block at
    the end of the DSX-CODE-021 group, under a section separator comment in the style the
    class already uses for its code-family separators. The separator comment should say in
    one line that the block below records deliberate scope boundaries, not true negatives.

    Reuse `self._entrypoint` and `self._check`. Every fixture places its `train_test_split(`
    line BEFORE the fit line, so each one exercises the at-or-after-split path that
    DSX-CODE-021 owns.

    For `test_dsx_code_021_scope_boundary_is_documented`, resolve the README path from
    `self.ROOT` — the class already defines `ROOT` as the repository root — read it with an
    explicit utf-8 encoding, and assert on substrings. Do not use a regex that anchors to a
    line start or line end: this repository checks out with carriage-return line endings on
    Windows, and an anchored pattern would pass on one platform and fail on the other.
    Substring containment sidesteps the whole question.

    Do not modify any existing test. Do not touch `dsx/checks/code.py`. Do not touch
    `README.md` in this task — that is task 2.

    Commit with a `test(11.1.1-03):` message stating that these tests pin knowingly-uncaught
    forms rather than true negatives.
  </action>

  <acceptance_criteria>
    - Test command: `python -m unittest tests.test_dsx.TestPhase11_1Code -v` exits non-zero,
      with `test_dsx_code_021_scope_boundary_is_documented` as the ONLY failure.
    - Behavior: the three `stays_uncaught_by_design` tests PASS — they describe current
      behaviour that this phase deliberately does not change.
    - Source: `grep -c "stays_uncaught_by_design" tests/test_dsx.py` reports 3.
    - Source: `grep -c "def test_malformed_fit_calls_no_finding_no_exception" tests/test_dsx.py`
      reports 1, and `git diff HEAD~1 -- tests/test_dsx.py` shows no deletion inside any
      pre-existing test method.
    - Source: each of the three scope-boundary tests has a docstring —
      `git show HEAD -- tests/test_dsx.py` shows a triple-quoted docstring immediately under
      each of the three new `def test_..._stays_uncaught_by_design` lines.
    - Source: no anchored line-ending regex was used —
      `grep -c 'r"^' tests/test_dsx.py` reports the same count as before this task.
  </acceptance_criteria>

  <verify>
    <automated>python -m unittest tests.test_dsx.TestPhase11_1Code -v 2>&amp;1 | tail -30</automated>
  </verify>

  <done>
    Three tests pin `partial_fit`, chained-call arguments and multi-line calls as deliberate
    scope boundaries, each with a docstring that distinguishes them from a true negative and
    tells a future reader what to do if one starts failing. A fourth test makes the README
    obligation executable and currently fails, which task 2 closes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Announce the behaviour change and state what the scan cannot see, in README.md</name>

  <files>README.md</files>

  <read_first>
    - `README.md` lines 135-158 — `### Migrating a pre-v2.0.0 spec`. This is the existing
      house shape for "a thing that passed before now blocks": it states the change, states
      the supported path forward, and states plainly what the interim path is and is not.
      The announcement section goes immediately after it and matches its tone and register.
    - `README.md` lines 309-340 — `## Known limits` and its first subsection,
      `### Concurrent \`dsx gate\` invocations are not supported`. This is the house shape for
      an honest limitation: what the tool cannot do, why, what that means for the operator,
      and no hedging. The uncaught-forms section goes at the end of this group and matches it.
    - `README.md` lines 178-204 — `### The check families`, so the new text uses the same
      names for the same things as the table a reader will have already seen.
    - `.claude/skills/plain-language/SKILL.md` — governs this prose. Plain sentences, not
      telegraphese; every abbreviation expanded on first use; report outcomes as they are;
      no caveat attached to a claim of doneness.
    - `dsx/checks/code.py` — the comment above `FIT_CALL_RE` as plan 02 left it, so the
      README's list of uncaught forms names exactly the same three.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-RESEARCH.md` Pitfall 3
      — the 13-variant table, the committed provenance for the counts this section cites.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-01-PLAN.md`, the
      `<decisions_settled_here>` block — the reported-line-number consequence and the
      before-split-only scoping that the announcement must state.
  </read_first>

  <action>
    Two new sections in `README.md`. Both are prose a user reads, so both follow
    `.claude/skills/plain-language/SKILL.md`: plain sentences, no invented abbreviations,
    every acronym expanded on first use, and no claim of completeness anywhere.

    **Section A — the announcement.** Add a new `###` subsection immediately after
    `### Migrating a pre-v2.0.0 spec`, headed `### Fit-before-split detection got stricter`.
    It must state, in this order:

    1. That an entrypoint file which passes the gate today may start failing it after this
       change, and that a new finding is not a false alarm — the leak was always in the file,
       the scan could not see it.
    2. The four shapes now caught that were not caught before, each named in words a reader
       can match against their own code: whitespace between the fit method name and its
       opening parenthesis; a fit call split across two lines with a trailing backslash; a
       fit call written with named arguments rather than positional ones; and a second fit
       call written on the same line as a first, where the first one was safe.
    3. The one reported-line-number change: for a statement continued with a trailing
       backslash, the finding now reports the first line of the statement rather than the
       line carrying the fit call. State that the reported line only ever moves earlier,
       never later, so no previously-reported finding disappears.
    4. That understanding the trailing-backslash form is scoped to the before-split check
       only. The same construction written after the split is still not seen by the
       after-split check. Say this as a stated boundary, not as an accident.
    5. What to do about a new finding, pointing at the same remedy the finding itself prints:
       split first, then fit on the training fold only.

    **Section B — what the scan cannot see.** Add a new `###` subsection at the end of the
    `## Known limits` group, headed
    `### What the fit-before-split scan does not catch`. It must state:

    1. That the check reads the entrypoint as text, one line at a time, and is not a Python
       parser. That single fact explains every limitation below it.
    2. The three real leaks it knowingly does not catch, each named plainly: any use of the
       incremental fit method (`partial_fit`), in positional or named-argument form; a fit
       call whose argument is itself a call, such as fitting directly on the return value of
       a data-loading function; and a fit call whose argument sits on a different physical
       line from the opening parenthesis.
    3. That `DSX-CODE-021` is therefore not sound. A clean entrypoint scan is evidence that
       these particular shapes were not found. It is not evidence that the file does not
       leak. Say that in a sentence a reader cannot misread as reassurance.
    4. The provenance, with its date: thirteen concrete fit-call variants were run against
       the checker on 2026-08-21; six were caught and seven were missed, and this change
       closes the named-argument ones. Cite
       `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-RESEARCH.md` as the
       source. Do NOT cite the older variant-count figure that circulated during the Phase
       11.1 verification session — it has no committed enumeration anywhere in this
       repository, and repeating an unsourced count in a shipped file is how a wrong number
       becomes a fact.
    5. That closing all three needs a parser-based scan rather than more text patterns, and
       that this change deliberately did not attempt one. Do not promise a future phase that
       is not on the roadmap.

    Do not renumber, reword or reorder any existing README section. Do not touch
    `tests/test_dsx.py` or `dsx/checks/code.py` in this task.

    Commit with a `docs(11.1.1-03):` message naming the behaviour change as the reason.
  </action>

  <acceptance_criteria>
    - Test command: `python -m unittest tests.test_dsx.TestPhase11_1Code -v` exits 0 — the
      `test_dsx_code_021_scope_boundary_is_documented` test from task 1 now passes, which
      means the README section exists and names the incremental-fit method.
    - Source: `grep -c "### Fit-before-split detection got stricter" README.md` reports 1.
    - Source: `grep -c "### What the fit-before-split scan does not catch" README.md` reports 1.
    - Source: `grep -c "partial_fit" README.md` reports at least 1.
    - Source: `grep -c "DSX-CODE-021" README.md` reports at least 1.
    - Source: `grep -c "11.1.1-RESEARCH.md" README.md` reports 1 — the provenance is cited by
      file path, not asserted.
    - Source: the uncommitted figure is not repeated —
      `grep -c "9 of 12" README.md dsx/checks/code.py` reports 0 for both files.
    - Source: no existing section was disturbed —
      `git diff HEAD~1 -- README.md | grep -c "^-"` reports 0 deletions other than the
      unchanged-context marker lines git emits for the diff header.
    - Behavior: `python -c "print(open('README.md',encoding='utf-8').read().count('## Known limits'))"`
      prints 1 — the new subsection was added inside the existing group, not as a second group.
  </acceptance_criteria>

  <verify>
    <automated>python -m unittest tests.test_dsx.TestPhase11_1Code -v 2>&amp;1 | tail -10</automated>
    <human-check>
      Read the two new `README.md` sections end to end.
      1. Does `### Fit-before-split detection got stricter` make it clear, to someone who has
         not read the planning directory, why their previously-passing file now fails and
         that the new finding is real? If a reader could come away thinking the gate got
         noisier rather than more accurate, it is not adequate.
      2. Does `### What the fit-before-split scan does not catch` leave a reader in any doubt
         that a clean scan is not proof of no leak? If any sentence could be quoted as
         reassurance, rewrite it.
      3. Does either section contain a number without a source, or an abbreviation expanded
         nowhere?
    </human-check>
  </verify>

  <done>
    Two README sections exist. One tells a user why a file that passed yesterday fails today
    and what to do about it. The other tells them, without hedging, what the check still
    cannot see and that a clean run is not proof. Both cite their sources; neither repeats an
    unsourced count.
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suite, known-bad corpus, catalogue and honesty audit</name>

  <files>tests/test_dsx.py</files>

  <read_first>
    - `scripts/check.sh` — the phase gate, in full. It runs the unit tests, the finding
      catalogue currency check, capability manifest validation, the four-point good/bad
      fixture gate contract, the missing-spec exit-code-2 check, and a determinism check.
      Note it invokes `python3`, not `python`.
    - `tests/test_known_bad_corpus.py` lines 164-197 and 605-660 — `_TARGET_DEFECT_CODES` and
      the per-fixture exit-code assertion that makes a new harness unnecessary.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-01-SUMMARY.md` and
      `11.1.1-02-SUMMARY.md` — the recorded exit codes and test counts from the two earlier
      plans, which this run is compared against.
    - `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-VALIDATION.md`, the
      "Manual-Only Verifications" table — the two judgement calls this phase owes a human.
  </read_first>

  <action>
    Run the phase gate and the honesty audit, and record what came back. This task produces
    evidence, not code; the only file it may modify is `tests/test_dsx.py`, and only if the
    audit finds a genuine gap.

    Run, capturing exit code and output tail for each:
    1. `python -m unittest discover -s tests -v`
    2. `python -m unittest tests.test_known_bad_corpus -v`
    3. `python scripts/gen-finding-catalogue.py --check`
    4. `python scripts/validate-capability.py`
    5. `./scripts/check.sh`

    Then run the honesty audit. Search `README.md`, `dsx/checks/code.py` and this phase's
    three PLAN files for any sentence that presents `DSX-CODE-001` or `DSX-CODE-021` as
    sound, complete, reliable or exhaustive. Judge the sentences, do not just count matches —
    the failure this looks for is a reassuring phrasing, not a specific word. Every place
    either code is described must either name the still-open forms or point at the README
    section that does.

    Confirm the phase's two out-of-scope guarantees explicitly rather than assuming them:
    that the six full-frame cleaning symbols are unchanged since commit `06ff2d7`, and that
    `test_full_frame_cleaning_predicates_timing_no_catastrophic_backtracking` passes. Threat
    `T-11.1-01` was closed in that commit and this phase must not have reopened it.

    Record in the plan SUMMARY: the five exit codes as literal values; the final full-suite
    test count against the baseline of 44 for the two targeted classes and the recorded
    full-suite counts from plans 01 and 02; one line per known-bad corpus fixture confirming
    its `plan` and `execute` exit codes are unchanged across the whole phase; and the result
    of the honesty audit as a verdict with the sentences examined, not as a claim that it
    passed. Those numbers land in
    `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-03-SUMMARY.md` and
    nowhere else.
  </action>

  <acceptance_criteria>
    - CLI: `python -m unittest discover -s tests -q` exits 0.
    - CLI: `python -m unittest tests.test_known_bad_corpus -v` exits 0.
    - CLI: `python scripts/gen-finding-catalogue.py --check` exits 0 with no regeneration.
    - CLI: `python scripts/validate-capability.py` exits 0.
    - CLI: `./scripts/check.sh` exits 0 and prints its final `all checks passed` line.
    - Behavior: `python -m unittest tests.test_dsx.TestPhase11_1Code.test_full_frame_cleaning_predicates_timing_no_catastrophic_backtracking -v` exits 0.
    - Source: `git diff 06ff2d7..HEAD -- dsx/checks/code.py | grep -c "_is_full_frame_impute\|_is_full_frame_spread_filter\|_IMPUTE_FILLNA_RE\|_IMPUTE_STAT_RE\|_SPREAD_SUBSCRIPT_RE\|_SPREAD_STAT_RE"`
      reports 0 — threat `T-11.1-01`'s fix is untouched by this phase.
    - Source: `git diff 06ff2d7..HEAD --stat -- tests/test_known_bad_corpus.py examples/`
      reports no changes.
    - Source: the phase's total footprint is two source files plus documentation —
      `git diff 06ff2d7..HEAD --stat` lists `dsx/checks/code.py`, `tests/test_dsx.py`,
      `README.md` and files under `.planning/` only.
    - The SUMMARY records five literal exit codes, the corpus fixture lines, and the honesty
      audit verdict with the examined sentences quoted.
  </acceptance_criteria>

  <verify>
    <automated>python -m unittest discover -s tests -q 2>&amp;1 | tail -5 &amp;&amp; python -m unittest tests.test_known_bad_corpus -q 2>&amp;1 | tail -5 &amp;&amp; python scripts/gen-finding-catalogue.py --check &amp;&amp; python scripts/validate-capability.py &amp;&amp; ./scripts/check.sh 2>&amp;1 | tail -5</automated>
    <human-check>
      The two judgement calls this phase owes a human, from `11.1.1-VALIDATION.md`:
      1. Read the `README.md` announcement. Does it adequately announce the behaviour change
         to someone upgrading, or does it read as a footnote?
      2. Read every sentence in `README.md`, `dsx/checks/code.py` and this phase's three PLAN
         files that mentions `DSX-CODE-021`. Does any of them present the check as sound?
      Approve, or name the sentences to rewrite.
    </human-check>
  </verify>

  <done>
    All five commands exit 0. No corpus fixture's exit code moved anywhere in the phase. The
    six full-frame cleaning symbols are unchanged since commit `06ff2d7` and their timing test
    passes. The honesty audit is recorded as a verdict with the examined sentences, not as an
    assertion that it passed.
  </done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and files created, renamed or newly referenced by this plan. Drift verification
should treat these as new, not as expected-to-already-exist.

**New test methods in `tests/test_dsx.py::TestPhase11_1Code`:**
- `test_partial_fit_after_split_stays_uncaught_by_design`
- `test_chained_call_argument_after_split_stays_uncaught_by_design`
- `test_multi_line_fit_call_after_split_stays_uncaught_by_design`
- `test_dsx_code_021_scope_boundary_is_documented`

**New sections in `README.md`:**
- `### Fit-before-split detection got stricter` — placed immediately after the existing
  `### Migrating a pre-v2.0.0 spec`
- `### What the fit-before-split scan does not catch` — placed at the end of the existing
  `## Known limits` group

**New test idiom introduced by this plan.** No test in this repository currently pins a
form as deliberately, permanently uncaught — verified by grep across `tests/test_dsx.py`
for `uncaught`, `stays uncaught`, `remains uncaught` and `not_caught`, all zero hits. The
three `stays_uncaught_by_design` methods introduce that idiom. Their nearest structural
scaffold is `test_malformed_fit_calls_no_finding_no_exception`, which pins a true negative
rather than a scope boundary, and is left untouched.

**No new finding code, no new citation, no new dependency, no new source file.**

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyst-authored entrypoint file → line scanner | Unchanged by this plan. This plan adds no detection logic; it adds tests, documentation and a verification run. No new boundary is crossed. |
| shipped documentation → operator's belief about coverage | A README that overstates what the gate catches is a security-relevant defect in a tool whose entire value is telling a user when their pipeline leaks. This is the boundary this plan exists to hold. |

## STRIDE Threat Register

ASVS level 1. Blocking threshold: high.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.1.1-08 | Repudiation | shipped documentation of `DSX-CODE-021`'s coverage | high | mitigate | An operator who reads a clean entrypoint scan as proof that no leak exists will ship a leaking model believing this tool cleared it. That is the whole failure mode the ROADMAP's "nothing may record `DSX-CODE-021` as sound" clause exists to prevent. Mitigation: `README.md` states the three uncaught forms and states explicitly that a clean scan is not proof; the same three forms are named in the comment above `FIT_CALL_RE`; three tests pin them so the claim is executable; and task 3 runs an honesty audit over every sentence mentioning either code. |
| T-11.1.1-09 | Repudiation | an unsourced variant count repeated in a shipped file | medium | mitigate | The older variant-count figure from the Phase 11.1 verification session has no committed enumeration anywhere in this repository — three files were checked during research and none of them lists the variants. Repeating it in the README would launder an unverifiable number into a shipped fact. Mitigation: the README cites the 13-variant table reproduced against the live checker on 2026-08-21, by file path, and a source assertion in task 2 checks the older figure is absent from both `README.md` and `dsx/checks/code.py`. |
| T-11.1.1-04 | Tampering | the already-fixed full-frame cleaning predicates | high | mitigate | Threat `T-11.1-01` (regex catastrophic backtracking, high, denial of service) was closed in commit `06ff2d7`. Task 3 asserts by diff against that exact commit that the six symbols are untouched across the whole phase, and runs their timing test directly. This is a phase-level check, not a per-plan one — a regression could have entered in any of the three plans. |
| T-11.1.1-10 | Tampering | corpus expectation maps | high | mitigate | The easiest way to make a moved fixture exit code disappear is to edit the map that defines what "moved" means. Task 3 asserts by diff against commit `06ff2d7` that `tests/test_known_bad_corpus.py` and everything under `examples/` are unchanged across the whole phase, so the corpus verdict is evidence rather than a restatement of the change. |
| T-11.1.1-SC | Tampering | package-manager installs | low | accept | This plan installs no package in any ecosystem — no `pip`, `npm` or `cargo` step exists in any task. The Package Legitimacy Gate's trigger condition is not met, mirroring `11.1-SECURITY.md`'s disposition of the same non-trigger (`R-02`, `T-11.1-SC`). Verified: `11.1.1-RESEARCH.md` § Package Legitimacy Audit records "no packages evaluated". |

No threat in this register is `high` and undispositioned. All three `high` entries are
`mitigate`, each with a named assertion or a named test pinning the mitigation.
</threat_model>

<verification>
1. `python -m unittest discover -s tests -q` exits 0.
2. `python -m unittest tests.test_known_bad_corpus -v` exits 0 with no fixture exit-code move.
3. `python scripts/gen-finding-catalogue.py --check` exits 0 without regeneration.
4. `python scripts/validate-capability.py` exits 0.
5. `./scripts/check.sh` exits 0 and prints `all checks passed`.
6. `grep -c "stays_uncaught_by_design" tests/test_dsx.py` reports 3.
7. `grep -c "### What the fit-before-split scan does not catch" README.md` reports 1 and
   `grep -c "### Fit-before-split detection got stricter" README.md` reports 1.
8. `grep -c "9 of 12" README.md dsx/checks/code.py` reports 0 for both files.
9. `git diff 06ff2d7..HEAD --stat` lists only `dsx/checks/code.py`, `tests/test_dsx.py`,
   `README.md` and files under `.planning/`.
10. The two `<human-check>` items in task 2 and task 3 are answered by a human before the
    phase closes. `workflow.human_verify_mode` is `end-of-phase`, so these are recorded at
    `/gsd-verify-work` rather than as blocking checkpoints mid-execution.
</verification>

<success_criteria>
- Three tests pin `partial_fit`, chained-call arguments and multi-line calls as knowingly
  uncaught, each distinguishable from a true negative by its own docstring.
- A fourth test makes the README documentation obligation executable.
- `README.md` announces the behaviour change in the same place and register this project
  already uses for behaviour changes, and states in plain language what the scan cannot see
  and that a clean run is not proof.
- The provenance cited is the dated 13-variant reproduction, not an unsourced count.
- Full suite, known-bad corpus, catalogue check, capability validation and
  `./scripts/check.sh` all green, with no fixture exit code moved anywhere in the phase.
- The six full-frame cleaning symbols are byte-identical to commit `06ff2d7`, so threat
  `T-11.1-01` stays closed.
</success_criteria>

<output>
Create `.planning/phases/11.1.1-detection-code-hardening-inserted/11.1.1-03-SUMMARY.md` when done.
Record in it: the five gate exit codes as literal values; the final full-suite test count
against the counts recorded in the plan 01 and plan 02 SUMMARYs; one line per known-bad
corpus fixture confirming its `plan` and `execute` exit codes are unchanged across the whole
phase; and the honesty audit as a verdict with the examined sentences quoted, not as a claim
that it passed.
</output>
