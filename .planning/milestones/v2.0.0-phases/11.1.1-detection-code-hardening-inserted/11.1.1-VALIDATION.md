---
phase: 11.1.1
slug: detection-code-hardening-inserted
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-21
validated: 2026-08-24
---

# Phase 11.1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> **Reconciled retroactively 2026-08-24** (loop S0-8 / audit `not_validated_phases`): shipped as
> the unfilled plan-phase scaffold. The phase executed and passed (11.1.1-VERIFICATION.md `passed`
> 6/6 across success criteria SC1–SC6). This phase's "requirements" are its six success criteria
> (its plans declare `[SC1..SC6]`; the three gap-closure plans 06/07/08 declare `[]`). This audit
> maps each SC to its committed automated tests and finds **zero coverage gaps** — no
> `gsd-nyquist-auditor` run and no generated tests. (The open items in HQ-2 are *security*
> sign-offs recorded in `11.1.1-SECURITY.md` — a separate gate; they do not bear on Nyquist test
> coverage, which is complete.)

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Python stdlib `unittest` |
| **Config file** | none — discovery-based |
| **Quick run command** | `python3 -m unittest tests.test_dsx.TestPhase11_1Code` |
| **Full suite command** | `python3 -m unittest discover -s tests -q` (or `sh scripts/check.sh`) |
| **Estimated runtime** | `TestPhase11_1Code` ~4s (138 tests); full suite ~47s (1038 tests) — measured 2026-08-24 |

---

## Sampling Rate

- **After every task commit:** `python3 -m unittest tests.test_dsx.TestPhase11_1Code`
- **After every plan wave:** `python3 -m unittest discover -s tests -q`
- **Before `/gsd-verify-work`:** Full suite green (`sh scripts/check.sh`)
- **Max feedback latency:** ~47s (full suite)

---

## Per-Success-Criterion Verification Map

Granularity per success criterion (this phase's unit of requirement); each maps to committed
regression tests in `tests/test_dsx.py::TestPhase11_1Code` and the known-bad corpus.

| SC | Plans | Wave(s) | Covering tests | Automated Command | Status |
|----|-------|---------|----------------|-------------------|--------|
| SC1 | 01, 04, 05 | 1, 2 | `test_fit_space_before_paren_before_split_fires_code_001` (AST-based fit-before-split, whitespace-irrelevant) | `python3 -m unittest tests.test_dsx.TestPhase11_1Code` | ✅ green |
| SC2 | 02, 03, 04, 05 | 1, 2, 3 | `TestPhase11_1Code` `FIT_CALL_RE` cases (kwarg/chained/`partial_fit`/non-training frames; exec/eval disclosed) | `python3 -m unittest tests.test_dsx.TestPhase11_1Code` | ✅ green |
| SC3 | 02, 04, 05 | 1, 2 | `test_semicolon_joined_two_fit_calls_fire_code_021` (both fits, stable physical line indices) | `python3 -m unittest tests.test_dsx.TestPhase11_1Code` | ✅ green |
| SC4 | 01, 02, 04, 05 | 1, 2 | `TestPhase11_1Code` comment/docstring/string-literal/markdown-cell invariance cases | `python3 -m unittest tests.test_dsx.TestPhase11_1Code` | ✅ green |
| SC5 | 03, 06, 07 | 1, 2, 3 | Five `_read_source` regression tests pinning malformed-`.ipynb` shapes → `None`/no-raise; `test_entrypoint_scan_scope_boundary_is_documented` pins the residual-limit disclosures | `python3 -m unittest tests.test_dsx.TestPhase11_1Code` | ✅ green |
| SC6 | all | — | Full suite + `tests.test_known_bad_corpus` (30) stay green; no fixture verdict moved (`git diff` corpus empty) | `sh scripts/check.sh` | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

Live confirmation 2026-08-24: `python3 -m unittest tests.test_dsx.TestPhase11_1Code` → **138 tests, OK**;
`tests.test_known_bad_corpus` → 30 OK; full suite → 1038 OK.

---

## Wave 0 Requirements

Existing infrastructure covers all phase behaviors. Every plan delivered committed regression tests
(the five SC5 shape-guards were added by plan 06, pinned by 08); no scaffolding or framework install
was needed.

---

## Manual-Only Verifications

All phase behaviors have automated verification. The non-UTF-8 `.ipynb` shape (SC5's one
deliberately-untouched case) is covered end-to-end via the CLI's top-level `except ValueError`
handler (exit 2, `EXIT_ERROR`), reproduced live in 11.1.1-VERIFICATION.md — not a manual-only gap.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references — none MISSING (0 gaps)
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-08-24 (retroactive audit, loop S0-8)

---

## Validation Audit 2026-08-24

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

All six success criteria classified COVERED. No `gsd-nyquist-auditor` run required (0 gaps).
Evidence: `TestPhase11_1Code` 138 OK; known-bad corpus 30 OK; full suite 1038 OK (2026-08-24).
Nyquist coverage is independent of the phase's open HQ-2 security sign-offs (`11.1.1-SECURITY.md`).
