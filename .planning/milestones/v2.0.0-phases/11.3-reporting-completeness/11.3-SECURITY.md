---
phase: 11.3
slug: reporting-completeness
status: verified
# threats_open = count of OPEN threats at or above workflow.security_block_on severity (the blocking gate)
threats_open: 0
asvs_level: 1
created: 2026-08-27
---

# Phase 11.3 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.
> State B run (no prior SECURITY.md): the 16-threat register was consolidated from the
> `<threat_model>` blocks of all six 11.3 plans (`register_authored_at_plan_time: true`);
> no SUMMARY carried a `## Threat Flags` section (grep-confirmed), so nothing was added
> outside the plan-time register. Verified by `gsd-security-auditor` (opus) — verdict
> **SECURED**, 16/16 closed — then independently re-gated by the orchestrator (never
> trusted the subagent, brief §5).

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| operator ANALYSIS-SPEC `multiplicity`/`results` → `_check_multiplicity`/`_check_exploratory_looks` | Untrusted declared family/tests content crosses into the length/naming compare; must degrade, not raise, and never buy a softer verdict via a malformed shape. | untrusted operator spec (YAML) |
| operator `results.tests[].effect_size_kind` → magnitude dispatch (`stats.py`→`mathx.interpret_effect`) | An unrecognised/crafted kind string crosses into a dispatch whose `interpret_effect` raises `ValueError` on unknown kinds; the gate must guard membership before the call. | untrusted declared kind string |
| operator `validity_frame.missingness` → `spec.py` membership + `frame/val.py` check | Untrusted declared mechanism/method crosses into spec-only frame checks that must normalise case/whitespace and never read the paradigm/profile to soften the block. | untrusted operator spec (YAML) |
| operator `validity_frame.exclusions` → `frame/val.py` + `spec.py` closed-key guard + content lock | Untrusted exclusion rules cross into a spec-only frame check and the plan-time content lock; a data-dependent count must not be registrable in the frame. | untrusted operator spec (YAML) |
| agent-authored `CHART-REVIEW.md` → `chart_review.check` | An adversarial or malformed report artifact crosses into the gate reader; only structural properties may be read (never the stochastic agent verdict). | untrusted report artifact (Markdown) |
| the committed tree (catalogue + corpus) → the release corpus gate | The full known-bad corpus and rendered catalogue are the artifacts the release is judged against; a code counted-but-unrendered, or uncited-but-passing, is a repudiation/tampering surface. | release artifacts (internal) |

---

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-11.3-01 | Denial of Service | `_check_multiplicity`/`_check_exploratory_looks` on malformed `multiplicity`/`results` (`dsx/checks/design.py`) | low | mitigate | Type-guarded arithmetic: `max(len(family) if isinstance(family, list) else 0, len(tests))` (`design.py:384`), naming set-diff guarded by `isinstance` (`:394-402`), same guard in `_check_exploratory_looks` (`:462`); checks degrade, never raise (T-7-01 precedent). | closed |
| T-11.3-02 | Tampering | family declared as a set-like string to dodge the length compare (`design.py`) | low | accept | Membership/length reads the declared list only; a non-list family is treated as empty (base 0), which is strictly **more** likely to fire, not less — the dodge cannot buy a softer verdict. Documented residual (plan 11.3-01). | closed |
| T-11.3-03 | Tampering | crafted `effect_size_kind` (mixed-case/whitespace) reaching `interpret_effect` unguarded (`dsx/checks/stats.py`) | medium | mitigate | Membership routed through `normalize()` first: `kind = normalize(test.get("effect_size_kind","d"))` then `if kind in EFFECT_SIZE_KINDS` (`stats.py:286-287`), so casing/whitespace cannot slip past the guard. | closed |
| T-11.3-04 | Denial of Service | `interpret_effect` `ValueError` (`dsx/mathx.py`) reaching the gate path | high | mitigate | The membership guard decides **before** the call — an unrecognised kind fires DSX-STA-012 (MEDIUM) on the else branch and never calls `interpret_effect` (`stats.py:287-288,300`); NO `try/except` substitute (D-11); `EFFECT_SIZE_KINDS` single-sourced import (`stats.py:12`) so the guard set cannot drift from `mathx`'s band table. | closed |
| T-11.3-05 | Denial of Service | `_check_missingness`/membership loop on a malformed missingness block (`dsx/frame/val.py`, `dsx/spec.py`) | low | mitigate | `if not isinstance(missingness, dict): return` (`val.py:1133`); the membership loop guards `isinstance(block, dict)` (`spec.py:1270`) and `is_blank(value)` (`:1273`); degrades to an empty report on a non-dict — no traceback. | closed |
| T-11.3-06 | Tampering | a method value crafted (case/whitespace) to dodge the closed-vocab membership (`dsx/spec.py`) | low | mitigate | Membership compares `normalize(value)` against `{normalize(k) for k in vocab}` (`spec.py:1279`; `val.py:1140,1150`), so casing/whitespace resolve consistently. | closed |
| T-11.3-07 | Spoofing | a frame check reading `inference.paradigm` or the profile to soften the block (`dsx/frame/val.py`) | medium | mitigate | The split reads only `spec.validity_frame.missingness` (`val.py:1132`); `tests/test_frame_boundary.py` (`TestFrameParadigmReadBoundary`, text+AST scan over all frame modules) forbids `dsx.frame` importing `dsx.checks` and reading `inference.paradigm` (D-11) — the boundary test stays green. | closed |
| T-11.3-08 | Tampering | a data-dependent row count (`n_excluded`) smuggled into the frame to slip past the digest discipline (`dsx/spec.py`) | high | mitigate | DSX-SPEC-083 closed-key guard: `_EXCLUSIONS_ALLOWED_KEYS = {rule, action, applied_before_split, justification}` (`spec.py:1184`); `set(entry.keys()) - _EXCLUSIONS_ALLOWED_KEYS` fires HIGH for any key outside the set (`:1303-1312`), scoped to `frame.get("exclusions")` only — a count cannot be registered in the frame; a test pins the rejection. | closed |
| T-11.3-09 | Repudiation | a silently moved exclusion cutoff after results exist (`dsx/frame/val.py`, `dsx/frame/prereg.py`) | high | mitigate | `exclusions` sits under `validity_frame`; `frame_digest()` sha256s the `{validity_frame, inference}` subtree with `sort_keys=True` (`decisions.py:241-250`), so DSX-PRE-020 trips at verify — proven by the `TestExclusionsContentLock` regression (`test_frame_prereg.py`), no new machinery. | closed |
| T-11.3-10 | Denial of Service | a malformed `exclusions` block (non-dict/list) crashing the frame check (`dsx/frame/val.py`) | low | mitigate | Presence guard `if not isinstance(exclusions, (dict, list)): return` (`val.py:1334`) returns early; a test asserts no traceback. | closed |
| T-11.3-11 | Denial of Service | a malformed/truncated/undecodable `CHART-REVIEW.md` causing a gate-path traceback (`dsx/checks/chart_review.py`) | high | mitigate | `check()` catches decode/read errors → `report.ok` (`chart_review.py:52,57-58`); `_split_frontmatter` degrades to `({}, text)` on `SpecParseError` (`:108`) without re-raising; tests feed truncated + undecodable + unparseable content and assert no raise (`test_chart_review.py`). Mirrors `figures.check`/`val.check` (T-7-01). | closed |
| T-11.3-12 | Spoofing | a stochastic agent verdict (`final_assessment`/`scores.*`/transcribed `gates.A-D`/verdict prose) smuggled into a DSX-CRV fire (`dsx/checks/chart_review.py`) | high | mitigate | Enforced by construction — the four checks read ONLY structural properties: frontmatter schema tag (`:117-135`), literal `X/10` substring (`:149-167`), terminal sentinel (`:181-198`), per-finding `DSX-`/`UNMAPPED` token (`:211-232`); forbidden inputs appear only in the D-13 docstring (`:7-8`); a boundary test flips a forbidden-input field and asserts no verdict change (`test_chart_review.py`). | closed |
| T-11.3-13 | Elevation of Privilege | a malformed report minting a `DSX-FIG-*` code swept into Gate D (circularity) (`references/chart-review-schema.md`, `dsx/cli.py`) | medium | mitigate | The family carries the `DSX-CRV` prefix; the D-14 schema edit routes it as a **report-acceptance precondition**, explicitly NOT into Gate D's `DSX-FIG-*` wildcard (`chart-review-schema.md:53,57-70`); `chart_review` is a separate check wired verify+ship only (`cli.py:124,129,200-201`). | closed |
| T-11.3-14 | Repudiation | a new code silently dropped from the rendered catalogue while still counted in the total (`scripts/gen-finding-catalogue.py`) | medium | mitigate | `--check` drift compares the rendered **body** text (`gen-finding-catalogue.py:437-438`), and the DSX-CRV section is registered in `PREFIX_GROUPS` (`:58`) so all 8 net-new codes render under a section, not just in the total (Task-1 audit confirmed). | closed |
| T-11.3-15 | Tampering | an uncited new code passing `--check` because its prefix is absent from the D-05 allowlist (`scripts/gen-finding-catalogue.py`) | medium | mitigate | `DSX-CRV-` added to `_D05_ALLOWLIST_PREFIXES` (`:87-88`); `check_d05` enforces citation lines for the allowlisted new prefixes (DSX-VAL-080, DSX-CRV-*). DSX-EXP-053/STA-012/SPEC-083 are legacy off-allowlist families — deliberately NOT `--check`-enforced (D-05 read deferred to UAT per CONTEXT `<deferred>`); each code's citation line is pinned by its own unit test instead. | closed |
| T-11.3-SC | Tampering | package installs | low | accept | No packages installed this phase (brief D-01, stdlib-only); no dependency manifest changed in the phase diff; new imports are stdlib/internal only. No supply-chain surface. | closed |

*Status: open · closed · open — below high threshold (non-blocking)*
*Severity: critical > high > medium > low — only open threats at or above workflow.security_block_on (`high`) count toward threats_open*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-11.3-02 | T-11.3-02 | A set-like-string family is read as an empty list (base 0), which fires the multiplicity/undercount codes **more** readily, never less — the dodge cannot buy a softer verdict. Design-time disposition (plan 11.3-01 threat_model), plan-checker-approved; not a fresh mitigate→accept. | S2-1/S2-2 plan-time; human veto window HQ-5 | 2026-08-27 |
| AR-11.3-SC | T-11.3-SC | Stdlib-only phase (brief D-01); no dependency manifest; no supply-chain surface. | brief D-01 (standing) | 2026-08-27 |

*Accepted risks do not resurface in future audit runs. Both are design-time dispositions recorded at plan time in the plan `<threat_model>` blocks; neither is a fresh mitigate→accept redisposition.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-08-27 | 16 | 16 | 0 | gsd-security-auditor (opus) + orchestrator independent re-gate |

**Independent re-gate evidence (orchestrator, brief §5 — did not trust the subagent):**
- 5 HIGH blockers structurally re-verified by grep at the merged tree: T-11.3-04 (membership guard precedes `interpret_effect`, no `try/except`; `stats.py:286-288,300`, single-source import `:12`); T-11.3-08 (`_EXCLUSIONS_ALLOWED_KEYS` `spec.py:1184`, closed-key set-diff `:1303-1312`); T-11.3-09 (`frame_digest` sha256 over `{validity_frame, inference}` sort_keys, `decisions.py:241-250` → DSX-PRE-020); T-11.3-11 (degrade-to-`report.ok` on OSError/UnicodeDecodeError/SpecParseError, `chart_review.py:52,57-58,108`); T-11.3-12 (four checks read only structural props; D-13 docstring `:7-8`; forbidden fields never read).
- Targeted failure/regression-path suites: `python -m unittest tests.test_frame_boundary tests.test_chart_review tests.test_frame_val tests.test_frame_prereg tests.test_effect_size_kind tests.test_multiplicity_family -q` → **Ran 267 tests … OK**.
- Full corpus gate `bash scripts/check.sh` → **all checks passed** — unit suite **Ran 1199 tests … OK**, catalogue current (256 codes), capability manifest conformant (2 steps/5 contributions/5 gates/9 skills/6 agents/13 config keys), gate contract good passes / bad blocks / missing errors, determinism identical. (The `DSX-* declared twice with different text` warnings are the pinned S0-6c divergent-declaration set — non-blocking; both gates exit 0.)
- `dsx/findings.py` untouched by the phase (committed + working tree).

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified (technical) 2026-08-27 — gate SECURED, threats_open: 0. **Human sign-off granted 2026-08-27 (operator verdict recorded in HUMAN-QUEUE.md, item HQ-5):** the phase sign-off line is approved as written, and the two ACCEPT dispositions above (AR-11.3-02, AR-11.3-SC) are confirmed. Phase 11.3 is now both technically verified and human-approved.
