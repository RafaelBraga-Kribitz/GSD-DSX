---
phase: 11.3-reporting-completeness
plan: 03
subsystem: validity-frame
tags: [missingness, DSX-VAL-060, DSX-SPEC-082, closed-vocabulary, rubin-1987, tdd]

# Dependency graph
requires:
  - phase: 07-validity-frame-checks-dsx-val
    provides: _MISSINGNESS_METHOD_VALIDITY table + DSX-VAL-060 HIGH/CRITICAL emit; _VALIDITY_FRAME_MEMBERSHIP loop reusing DSX-SPEC-082
  - phase: 11.3-01
    provides: finding-catalogue + _CANONICAL_DECLARATIONS pin invariants this plan must not drift
provides:
  - MISSINGNESS_METHODS closed vocabulary (7 locked members) in dsx/spec.py
  - ("missingness","method_implied",MISSINGNESS_METHODS) membership row reusing the DSX-SPEC-082 loop (no new report.add site)
  - Per-method severity split of the MAR deny set (complete/available_case HIGH; single_imputation CRITICAL)
  - Third DSX-VAL-060 emit branch firing CRITICAL for single imputation under MAR, carrying its own Rubin (1987) §3.1 citation
affects: [11.3-04, 11.3-05, 11.3-06, verify-phase, reporting-completeness]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-method severity within a mechanism: a deny mode maps {method: severity} rather than one severity per mechanism, keyed so a reversion to one-severity-per-mechanism goes red"
    - "Code reuse without minting: a new severity branch reuses the verbatim finding title + severity literal so the catalogue row and _CANONICAL_DECLARATIONS (severity,title) set do not drift — only remedy/detail differ"

key-files:
  created: []
  modified:
    - dsx/spec.py
    - dsx/frame/val.py
    - tests/test_frame_val.py

key-decisions:
  - "D-04: method_implied is a closed 7-member vocabulary; an unknown method is DSX-SPEC-082 HIGH via the existing membership loop, minting no code"
  - "D-05: single imputation treated as if observed under MAR fires DSX-VAL-060 CRITICAL (blocks at plan), while complete/available-case under MAR stay HIGH"
  - "D-06: the CRITICAL branch carries its own Rubin (1987) §3.1 citation (T = W̄ + (1+1/m)·B), NOT the White & Carlin (2010) complete-case remedy"

patterns-established:
  - "Severity keyed at method granularity inside a mechanism's deny set"
  - "Reuse an existing finding code's title+severity literal verbatim to avoid catalogue/pin drift"

requirements-completed: [REQ-P11.3-03]

coverage:
  - id: D1
    description: "missingness.method_implied is a closed 7-member vocabulary; an unrecognised method fires DSX-SPEC-082 HIGH via the reused membership loop"
    requirement: "REQ-P11.3-03"
    verification:
      - kind: unit
        ref: "tests/test_frame_val.py#test_missingness_unknown_method_implied_fires_spec_082_high"
        status: pass
      - kind: unit
        ref: "tests/test_frame_val.py#test_missingness_methods_has_exactly_the_seven_locked_members"
        status: pass
    human_judgment: false
  - id: D2
    description: "single_imputation under a MAR mechanism fires DSX-VAL-060 CRITICAL (not HIGH); complete/available_case under MAR stay HIGH"
    requirement: "REQ-P11.3-03"
    verification:
      - kind: unit
        ref: "tests/test_frame_val.py#test_missingness_mar_with_single_imputation_fires_critical_val_060"
        status: pass
      - kind: unit
        ref: "tests/test_frame_val.py#test_missingness_mar_complete_and_available_case_stay_high_val_060"
        status: pass
      - kind: unit
        ref: "tests/test_frame_val.py#test_mar_deny_set_resolves_each_method_to_exactly_one_severity"
        status: pass
    human_judgment: false
  - id: D3
    description: "the good fixture (MAR + multiple_imputation) stays green — the vocab neither over- nor under-fires; no catalogue drift"
    requirement: "REQ-P11.3-03"
    verification:
      - kind: unit
        ref: "tests/test_frame_val.py#test_missingness_mar_with_multiple_imputation_fires_neither_val_060_nor_spec_082"
        status: pass
      - kind: integration
        ref: "./bin/dsx gate plan --spec examples/good-ANALYSIS-SPEC.yaml (exit=0)"
        status: pass
      - kind: integration
        ref: "python scripts/gen-finding-catalogue.py --check (exit 0, finding-codes.md unmodified)"
        status: pass
    human_judgment: false

# Metrics
duration: ~11min
completed: 2026-08-27
status: complete
---

# Phase 11.3 Plan 03: Missingness method_implied closed vocab + single-imputation CRITICAL Summary

**missingness.method_implied is now a closed 7-member vocabulary (unknown → DSX-SPEC-082 HIGH via the reused membership loop), and single imputation treated as if observed under MAR fires DSX-VAL-060 CRITICAL with its own Rubin (1987) §3.1 citation — minting no code and leaving the catalogue untouched.**

## Performance

- **Duration:** ~11 min
- **Started:** 2026-08-27T00:33Z (from clean HEAD after plan 02)
- **Completed:** 2026-08-27T00:44Z
- **Tasks:** 2 (RED, GREEN)
- **Files modified:** 3

## Accomplishments
- Added `MISSINGNESS_METHODS` (7 locked members) + the `("missingness","method_implied",MISSINGNESS_METHODS)` membership row, reusing the existing DSX-SPEC-082 loop with NO new `report.add` site.
- Split `_MISSINGNESS_METHOD_VALIDITY`'s MAR deny set from one-severity-per-mechanism into a per-method `{method: severity}` map: complete/available_case HIGH (unchanged), single_imputation CRITICAL.
- Added a third DSX-VAL-060 emit branch firing `CRITICAL` for single imputation under MAR, reusing the verbatim title and `CRITICAL` literal (zero catalogue / `_CANONICAL_DECLARATIONS` drift) with a distinct Rubin (1987) §3.1 remedy/docstring.
- Full suite 1156 → 1162 tests, all green; catalogue check exits 0 without regenerating `references/finding-codes.md`; good fixture stays exit 0; D-11 boundary intact.

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — pin the closed vocab + single-imputation CRITICAL + good-fixture regression** - `f2dc5da` (test)
2. **Task 2: GREEN — MISSINGNESS_METHODS + membership row + per-method severity split** - `1b73073` (feat)

**Plan metadata:** this commit (docs: complete plan)

## Files Created/Modified
- `dsx/spec.py` - `MISSINGNESS_METHODS` constant (7 members, name→description dict) near `MISSINGNESS_MECHANISMS`; membership row appended to `_VALIDITY_FRAME_MEMBERSHIP`.
- `dsx/frame/val.py` - `_MISSINGNESS_METHOD_VALIDITY` MAR entry re-keyed to a per-method severity map; `_check_missingness` emit reworked into three branches (HIGH deny / CRITICAL deny-single-imputation / CRITICAL allow-mnar); Rubin (1987) §3.1 added to the docstring and the new branch's remedy.
- `tests/test_frame_val.py` - six REQ-P11.3-03 cases (single_imputation CRITICAL, complete/available_case HIGH, multiple_imputation fires neither code, unknown method → DSX-SPEC-082 HIGH, 7-member vocab lock, per-method-severity invariant) + `validate_structure` import.

## Decisions Made
- Represented `MISSINGNESS_METHODS` as a name→description dict (consistent with the sibling membership vocabularies); did NOT register it in `_VOCABULARIES` (out of scope — no test requires it and the plan did not request it).
- Ordered the three `report.add` sites HIGH-first so the two CRITICAL declarations stay contiguous; the surviving deduped catalogue row remains `CRITICAL`, matching `references/finding-codes.md` exactly (no drift).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- The prompt described the pre-existing "declared twice" warning set as exactly `{DSX-VAL-021, DSX-VAL-060}`. The generator actually warns on the full pinned-divergent set (`DSX-SPEC-070`, `DSX-VAL-021`, `DSX-VAL-060`, `DSX-COH-030`, `DSX-PAR-002`, `DSX-CLM-020`, `DSX-CLM-021`). This is a pre-existing baseline, not caused by this plan. Verified via `tests/test_gen_finding_catalogue.py::TestCanonicalDeclarations` (42 tests pass), which asserts the divergent-code set is exactly the pin — so this change added no new divergent code and did not grow the warning set. The new CRITICAL branch's `(CRITICAL, title)` declaration is byte-identical to the existing CRITICAL declaration, so it collapses into the same pinned set element.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- REQ-P11.3-03 complete. method_implied is typed and single-imputation-under-MAR blocks at plan.
- No blockers. Tracking files (STATE/ROADMAP/REQUIREMENTS/LOOP-LEDGER) intentionally untouched — the orchestrator writes them after the wave merges.

---
*Phase: 11.3-reporting-completeness*
*Completed: 2026-08-27*
