---
phase: 11.3
slug: reporting-completeness
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-27
validated: 2026-08-27
---

# Phase 11.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from 11.3-RESEARCH.md `## Validation Architecture`; finalized by /gsd-validate-phase (S2-5).
> State A audit against the merged tree at HEAD `0ffbddf`: all 7 requirements COVERED, 0 gaps.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Python stdlib `unittest` (no pytest / no third-party test framework in the repo) |
| **Config file** | none — `python -m unittest discover -s tests -q` (see `scripts/check.sh`) |
| **Quick run command** | `python -m unittest tests.<module> -v` (the specific new/edited module) |
| **Full suite command** | `python -m unittest discover -s tests -q` (1199 tests as of 2026-08-27) |
| **Phase gate** | `bash scripts/check.sh` (full suite + `gen-finding-catalogue.py --check` + capability manifest + gate-contract good/bad/missing exit codes + determinism) |
| **Estimated runtime** | ~45 seconds (full suite: `Ran 1199 tests in ~44s`) |

---

## Sampling Rate

- **After every task commit:** Run `python -m unittest tests.<edited module> -v`, plus `python scripts/gen-finding-catalogue.py --check` whenever a `report.add(...)` call site changes.
- **After every plan wave:** Run `python -m unittest discover -s tests -q` (full suite).
- **Before `/gsd-verify-work`:** `bash scripts/check.sh` must be green.
- **Max feedback latency:** ~45 seconds.

---

## Per-Task Verification Map

*State A audit: every requirement maps to a named test that runs green (fire-on-bad AND silence-on-good).*

| Requirement | Observable behaviour | Test Type | Named passing test(s) | Status |
|-------------|----------------------|-----------|-----------------------|--------|
| REQ-P11.3-01 | `DSX-EXP-053` HIGH names absent metrics when the declared family is non-empty but smaller than `results.tests` (`max()` undercount); `PREREG_FACTS` comment references reading-function names, not line numbers (D-03) | unit | `tests.test_multiplicity_family.TestMultiplicityFamilyUnderDeclared.test_exp_053_fires_and_names_absent_metric`, `…test_exp_053_no_traceback_on_nonscalar_family` (WR-03) | ✅ green |
| REQ-P11.3-02 | `DSX-EXP-051` fires independent of family presence; verdict-neutral on good fixture (max(3,3)=3), still fires on bad | unit + gate | `tests.test_multiplicity_family.TestExploratoryLooksFamilyIndependent.test_exp_051_fires_with_no_family_declared`, `…test_exp_051_silent_on_good_arithmetic` | ✅ green |
| REQ-P11.3-03 | `DSX-SPEC-082` HIGH on unrecognised `method_implied`; good (`multiple_imputation`) stays green; `DSX-VAL-060` CRITICAL for `single_imputation`-under-MAR blocks at plan | unit | `tests.test_frame_val.TestValSamplingMissingnessMeasurement.test_missingness_mar_with_single_imputation_fires_critical_val_060`, `…test_missingness_unknown_method_implied_fires_spec_082_high`, `…test_missingness_mar_with_multiple_imputation_fires_neither_val_060_nor_spec_082` | ✅ green |
| REQ-P11.3-04 | `DSX-VAL-080` HIGH on exclusion rule w/o justification; `DSX-SPEC-083` HIGH on unexpected key in `exclusions`; changed cutoff/`applied_before_split` trips `DSX-PRE-020` via existing `frame_digest` lock | unit + regression | `tests.test_frame_val.TestValExclusions.test_rule_without_justification_fires_high_val_080_single_dict`, `…test_unexpected_key_in_exclusions_fires_high_spec_083_single_dict`; `tests.test_frame_prereg.TestExclusionsContentLock` | ✅ green |
| REQ-P11.3-05 | `DSX-STA-012` MEDIUM on unrecognised `effect_size_kind`; `interpret_effect` never called on that branch (no traceback); recognised set single-sourced/tested-equal to `mathx` table | unit | `tests.test_effect_size_kind.TestEffectSizeKindGuard.test_unrecognised_kind_fires_medium`, `…test_recognised_kind_stays_silent`, `…test_unrecognised_kind_does_not_raise` | ✅ green |
| REQ-P11.3-06 | Four `DSX-CRV-*` codes fire/clear on conforming vs malformed `CHART-REVIEW.md`; none of D-13's forbidden inputs (scores/gates/verdict prose) read | unit (new module) + fixture | `tests.test_chart_review.TestChartReview.test_good_fixture_fires_none_of_the_four_crv_codes`, `tests.test_chart_review.TestChartReviewD13Boundary.test_forbidden_input_flip_matches_good_fixture_verdict_exactly` (+ 16-test module) | ✅ green |
| REQ-P11.3-07 | Catalogue regenerates 248→256; full corpus green; all D-15 fixtures present | build gate + full suite | `python scripts/gen-finding-catalogue.py --check` (exit 0, "current", Total: 256); `bash scripts/check.sh` (Ran 1199 tests OK, all checks passed) | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

All Wave-0 dependencies (new test module + fixtures) were created during execution (waves 4–5) and are present in the merged tree; `wave_0_complete: true`.

- [x] `tests/test_chart_review.py` — new module for REQ-06 (16-test module; structural-only; D-13 boundary test present)
- [x] `examples/good-CHART-REVIEW.md` — conforming fixture (fires none of the four CRV codes)
- [x] Malformed `CHART-REVIEW.md` variants exercising each of `DSX-CRV-010..013` (D-15a) — under `tests/fixtures/chart-review/`
- [x] Fixture declaring an unrecognised `missingness.method_implied` (D-15b)
- [x] Fixture: no declared family but reported tests + `comparisons_looked_at` > reported (D-15c)
- [x] Fixture(s): `validity_frame.exclusions` with/without justification + one with an unexpected key (D-15d)
- [x] Fixture: unrecognised `effect_size_kind` (D-15e)
- [x] Fixture: `single_imputation` under MAR — proves `DSX-VAL-060` CRITICAL (D-15f)
- [x] `tests/test_frame_val.py::_DESIGN_PY_SHA256` update — done same-commit as the `design.py` change (Pitfall 1), and relocked again in the WR-03 fix commit `0ffbddf` per its deliberate-edit protocol

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| D-05 citation authenticity: Rubin (1987) *Multiple Imputation for Nonresponse in Surveys* §3.1 (`T = W̄ + (1+1/m)·B`) anchors the DSX-VAL-060 single-imputation-under-MAR **CRITICAL** branch | REQ-P11.3-03 | Project rule D-05 requires a **human** primary-source read; no test can verify the source says what the docstring claims (`dsx/frame/val.py:1102,1209`) | Queued as **HQ-5** cite 1 for the phase ship round; confirm the total-variance formula supports B→0 single imputation as the anti-conservative structural defect. Sign at phase verify/ship, not plan/execute. |
| D-05 citation authenticity: Simmons, Nelson & Simonsohn (2011) "False-Positive Psychology" anchors DSX-VAL-080 (data exclusion as a researcher degree of freedom) | REQ-P11.3-04 | Human source read; the exact requirement-number/table locator is honestly flagged **UNVERIFIED** in-code by design (D-05 discipline) | Queued as **HQ-5** cite 2; confirm or replace the locator, clear the unverified-locator flag. Sign at phase verify/ship. |

*These manual-only items do **not** reduce Nyquist compliance: the underlying gate
behaviours for REQ-P11.3-03/04 are covered by green automated tests above
(`test_missingness_mar_with_single_imputation_fires_critical_val_060`,
`test_rule_without_justification_fires_high_val_080_single_dict`). Only the citation
**authenticity** — a source read no test can perform — is manual, exactly as with
Phase 7's and Phase 11.2's D-05 reads. `nyquist_compliant: true` stands. The D-06
finding-code veto window (HQ-5) is an operator-governance record, not a coverage gap.*

---

## Validation Audit 2026-08-27

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

State A audit: all 7 requirements classified **COVERED** — each maps to a named test that
runs green on both the fire-on-bad and silence-on-good paths. **No `gsd-nyquist-auditor`
spawned and no tests generated (0 gaps).** Independent re-gate this firing (brief §5 — did
not trust the prior verification doc):

- 9 named requirement tests run verbosely → **Ran 9 tests … OK** (one/two per REQ, covering EXP-053/EXP-051/VAL-060-CRITICAL/SPEC-082/VAL-080/SPEC-083/STA-012/CRV-D13-boundary).
- Targeted module run `tests.test_frame_boundary tests.test_chart_review tests.test_frame_val tests.test_frame_prereg tests.test_effect_size_kind tests.test_multiplicity_family` → **Ran 267 tests … OK**.
- Full corpus gate `bash scripts/check.sh` → **all checks passed** (`Ran 1199 tests … OK`, catalogue current at 256, capability manifest conformant, gate contract good/bad/missing, determinism identical).

The two Manual-Only items (D-05 citation authenticity) are human source reads, not Nyquist
coverage gaps.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (none — 0 gaps)
- [x] No watch-mode flags
- [x] Feedback latency < 45s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** validated 2026-08-27 — `nyquist_compliant: true`, 0 gaps, 7/7 requirements COVERED by green automated tests; independent re-gate green (`Ran 1199 tests … OK`).
