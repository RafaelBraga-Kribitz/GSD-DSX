---
phase: 11.3-reporting-completeness
plan: 05
type: execute
wave: 4
depends_on: [11.3-04]
files_modified:
  - dsx/checks/chart_review.py
  - dsx/cli.py
  - scripts/gen-finding-catalogue.py
  - references/chart-review-schema.md
  - tests/test_chart_review.py
  - tests/test_gen_finding_catalogue.py
  - examples/good-CHART-REVIEW.md
  - tests/fixtures/chart-review/
  - references/finding-codes.md
autonomous: true
requirements: [REQ-P11.3-06]
must_haves:
  truths:
    - "Four structural codes validate CHART-REVIEW.md against references/chart-review-schema.md: DSX-CRV-010 HIGH (schema tag mismatch), DSX-CRV-011 MEDIUM (forbidden ten-point-scale substring present), DSX-CRV-012 HIGH (missing terminal sentinel), DSX-CRV-013 MEDIUM (finding line lacking a DSX-/UNMAPPED token) (D-12, ROADMAP SC5)."
    - "No stochastic agent verdict gates: the four codes never read final_assessment, scores.*, agent-transcribed gates.A-D, or verdict prose (D-13)."
    - "A missing or unparseable CHART-REVIEW.md degrades to an empty/ok report — never a gate-path traceback (D-01)."
    - "examples/good-ANALYSIS-SPEC.yaml still exits 0 and examples/bad-ANALYSIS-SPEC.yaml still exits 1 after chart_review joins the verify/ship profiles."
  artifacts:
    - "dsx/checks/chart_review.py (new module) emitting the four DSX-CRV-* codes from CHART-REVIEW.md text/frontmatter only"
    - "dsx/cli.py registration (import, CHECKS/run_checks branch, GATE_PROFILES verify + ship)"
    - "scripts/gen-finding-catalogue.py PREFIX_GROUPS + _D05_ALLOWLIST_PREFIXES entries for DSX-CRV"
    - "references/chart-review-schema.md Gate-proxy table routing DSX-CRV-* as a report-acceptance precondition (D-14)"
    - "examples/good-CHART-REVIEW.md + malformed variants under tests/fixtures/chart-review/; tests/test_chart_review.py; a DSX-CRV PREFIX_GROUPS assertion in tests/test_gen_finding_catalogue.py"
    - "references/finding-codes.md regenerated to include the four DSX-CRV-* codes"
  key_links:
    - "The DSX-CRV family needs BOTH gen-finding-catalogue registries (PREFIX_GROUPS so the rows render; _D05_ALLOWLIST_PREFIXES so citations are enforced) in the SAME commit as the module — both are silent-failure omissions."
    - "DSX-CRV-* must be a report-acceptance precondition, NOT folded into Gate D's DSX-FIG-* wildcard (circularity)."
  prohibitions:
    - statement: "The four DSX-CRV codes must NEVER read D-13's forbidden inputs: final_assessment, any scores.*, agent-transcribed gates.A/B/C/D values, or the verdict prose of the Executive Verdict / Final Assessment / Per-Exhibit Disposition sections."
      status: resolved
      verification: "A boundary test feeds a CHART-REVIEW.md with a deliberately wrong scores/gates block and asserts the four codes' fire/no-fire is unchanged (analogous to TestFrameParadigmReadBoundary)."
    - statement: "Reusing DSX-FIG-* for chart-review conformance is forbidden — the schema's own Gate-D proxy wildcards DSX-FIG-* at CRITICAL/HIGH, so a malformed report minting a DSX-FIG-* code would be swept into the gate the report is reporting on (D-12)."
      status: resolved
      verification: "The four codes carry the DSX-CRV prefix; the D-14 Gate-proxy edit routes them as a precondition, not into Gate D."
    - statement: "A malformed/truncated CHART-REVIEW.md must not raise on the gate path."
      status: resolved
      verification: "A test feeds truncated/undecodable content and asserts the check returns a report without raising."
---

<objective>
Deliver REQ-P11.3-06: a new checks-layer module dsx/checks/chart_review.py that validates
CHART-REVIEW.md for structural conformance against references/chart-review-schema.md with four
DSX-CRV-* codes, reading only structure (schema tag, the forbidden ten-point-scale substring, the
terminal sentinel, and the DSX-/UNMAPPED token on finding lines) and never any stochastic agent
verdict. Register it in the gate machinery and the catalogue, and route the family as a
report-acceptance precondition in the schema.

Purpose: the adversarial chart-audit report is itself an artifact whose completeness the gate must
be able to hold to account — deterministically, without letting a stochastic call gate the run.
Output: DSX-CRV-010/011/012/013, the new module, the four-registry wiring, the D-14 schema edit,
the conforming and malformed fixtures, the boundary test, and a regenerated catalogue.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.3-reporting-completeness/11.3-CONTEXT.md
@.planning/phases/11.3-reporting-completeness/11.3-RESEARCH.md
@.planning/phases/11.3-reporting-completeness/11.3-PATTERNS.md
@references/chart-review-schema.md
</context>

## Artifacts this phase produces (whole phase, for orientation)

- Net-new finding codes (8): `DSX-EXP-053`, `DSX-STA-012`, `DSX-VAL-080`, `DSX-SPEC-083`,
  `DSX-CRV-010/011/012/013` (THIS plan). Catalogue 248 → 256.
- Reused codes: `DSX-EXP-051`, `DSX-SPEC-082`, `DSX-VAL-060`.
- New module `dsx/checks/chart_review.py` (THIS plan); new functions `_check_exclusions`, the
  effect-size guard, the exclusions closed-key guard; new vocab `MISSINGNESS_METHODS`.
- New fixtures `examples/good-CHART-REVIEW.md` + malformed variants (THIS plan); new test module
  `tests/test_chart_review.py` (THIS plan).

**This plan produces:** `DSX-CRV-010/011/012/013`; the chart_review module + wiring; the D-14
schema Gate-proxy edit; the CHART-REVIEW fixtures; the boundary test; the catalogue rows for the
four codes.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — conforming + malformed CHART-REVIEW.md fixtures and the four DSX-CRV codes' fire/no-fire + D-13 boundary test</name>
  <files>examples/good-CHART-REVIEW.md, tests/fixtures/chart-review/, tests/test_chart_review.py</files>
  <read_first>
    - references/chart-review-schema.md:84-87 (the four Orchestrator-validation rules the four codes
      map to 1:1 and in order) and :69-80 (the fixed H2 body order + terminal sentinel), :46-53
      (the Gate-proxy table D-14 edits)
    - templates/CHART-REVIEW.md (existing skeleton — confirm whether it is schema-conformant enough
      to seed the conforming fixture, or whether a distinct example is needed)
    - dsx/checks/figures.py:35-80 (the check() signature, degrade-to-empty pattern, and report.add
      shape to mirror) and tests/test_frame_boundary.py:271-333 (TestFrameParadigmReadBoundary — the
      forbidden-input boundary-test shape to clone for D-13)
    - 11.3-CONTEXT.md D-12, D-13 and 11.3-PATTERNS.md chart_review + test_chart_review sections
  </read_first>
  <behavior>
    - DSX-CRV-010 HIGH fires when frontmatter schema is not dsx-chart-review-v1; silent when it is.
    - DSX-CRV-011 MEDIUM fires when the forbidden ten-point-scale substring (per schema rule 2)
      appears; silent when absent.
    - DSX-CRV-012 HIGH fires when the file does not end with the terminal `## CHART AUDIT COMPLETE`
      sentinel; silent when it does.
    - DSX-CRV-013 MEDIUM fires when a finding line carries neither a DSX- code nor UNMAPPED; silent
      when every finding line is tokenised.
    - The conforming examples/good-CHART-REVIEW.md fires none of the four.
    - D-13 boundary: a CHART-REVIEW.md that is structurally conforming but carries a deliberately
      wrong scores/gates/final_assessment block fires NONE of the four (the codes do not read
      judgement content); flipping only a forbidden-input field never changes any verdict.
    - A missing file and a truncated/undecodable file both return a report WITHOUT raising.
  </behavior>
  <action>
    Create a conforming examples/good-CHART-REVIEW.md (seed from templates/CHART-REVIEW.md; ensure
    the frontmatter schema tag, no ten-point-scale substring, the terminal sentinel, and tokenised
    finding lines). Create malformed variants under tests/fixtures/chart-review/ — one per code:
    a wrong-schema-tag file, a file containing the forbidden ten-point-scale substring, a file
    missing the terminal sentinel, and a file with an untokenised finding line. Add a
    forbidden-input fixture: structurally conforming but with a wrong scores/gates block. Create
    tests/test_chart_review.py (class per the TestValSamplingMissingnessMeasurement convention),
    one test per fire/no-fire clause above, plus the D-13 boundary test and the missing/undecodable
    no-raise tests; pass explicit fixture paths to the (not-yet-existing) chart_review.check. Mark
    with `# REQ-P11.3-06`. These MUST fail now: the module does not exist.
  </action>
  <acceptance_criteria>
    - `python -m unittest tests.test_chart_review -v` runs and FAILS (RED) — import/attribute error
      on the missing chart_review module or the missing check.
    - examples/good-CHART-REVIEW.md exists and is schema-conformant; four malformed variants plus a
      forbidden-input fixture exist under tests/fixtures/chart-review/.
    - The test module includes a D-13 boundary test and missing/undecodable no-raise tests.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_chart_review -v</automated>
  </verify>
  <done>The fixtures exist and the tests fail for the right reason: chart_review is unimplemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement chart_review.py (four codes, degrade-to-empty), register the catalogue dual-registry, regenerate</name>
  <files>dsx/checks/chart_review.py, scripts/gen-finding-catalogue.py, tests/test_gen_finding_catalogue.py, references/finding-codes.md</files>
  <read_first>
    - dsx/checks/figures.py (full — the artifact-reader module template) and dsx/loader.py (the
      shared bundled/PyYAML loader to parse the CHART-REVIEW.md frontmatter, per RESEARCH Don't Hand-Roll)
    - scripts/gen-finding-catalogue.py:25-58 (PREFIX_GROUPS) and :60-76 (_D05_ALLOWLIST_PREFIXES) —
      the DSX-ADM entry pair is the most recent brand-new-family precedent; :233-263 (render only
      emits sections for registered prefixes — Pitfall 2); :323-353 (check_d05 opt-in enforcement — Pitfall 3)
    - tests/test_gen_finding_catalogue.py:171-172 (the `"DSX-PAR"` PREFIX_GROUPS membership assertion to clone)
    - 11.3-RESEARCH.md Pitfalls 2 and 3 (both silent-failure omissions)
  </read_first>
  <behavior>
    - Task 1's tests turn GREEN; the four codes fire/stay-silent exactly as pinned.
    - `gen-finding-catalogue.py --check` becomes green only after finding-codes.md is regenerated
      WITH the DSX-CRV rows rendered under a DSX-CRV section.
  </behavior>
  <action>
    Implement dsx/checks/chart_review.py mirroring figures.py: `check(spec, phase_dir=None, *,
    strict=False) -> Report` named "chart_review"; resolve CHART-REVIEW.md under phase_dir by the
    schema's convention path; if absent or unparseable, `report.ok(...)` and return (never raise —
    catch decode/parse errors and degrade). Parse frontmatter via dsx/loader.py; read the body as
    text. Emit, using literal severities: DSX-CRV-010 HIGH when the frontmatter schema tag differs
    from dsx-chart-review-v1; DSX-CRV-011 MEDIUM when the forbidden ten-point-scale substring (schema
    rule 2) is present (plain `in` test); DSX-CRV-012 HIGH when `.rstrip().endswith("## CHART AUDIT
    COMPLETE")` is false; DSX-CRV-013 MEDIUM when a finding line (under the Issues headings) carries
    neither a DSX- token nor UNMAPPED. Read ONLY these structural properties — no scores.*, no
    agent-transcribed gates.A-D, no final_assessment / verdict prose (D-13). Give each emit site a
    D-05 `Structural criterion:` docstring line pointing at the schema rule it enforces (the schema
    file is its own structural criterion — no judgement-source citation needed). Add the DSX-CRV
    entry to PREFIX_GROUPS (heading "Chart review conformance", one-sentence blurb) AND add
    "DSX-CRV-" to _D05_ALLOWLIST_PREFIXES, both with a dated Phase-11.3/D-12 comment. Clone the
    DSX-PAR PREFIX_GROUPS membership assertion in tests/test_gen_finding_catalogue.py for DSX-CRV.
    Regenerate references/finding-codes.md via `python scripts/gen-finding-catalogue.py`.
  </action>
  <acceptance_criteria>
    - `python -m unittest tests.test_chart_review tests.test_gen_finding_catalogue -v` passes (GREEN).
    - `scripts/gen-finding-catalogue.py` PREFIX_GROUPS contains a DSX-CRV entry and
      _D05_ALLOWLIST_PREFIXES contains "DSX-CRV-".
    - `python scripts/gen-finding-catalogue.py --check` exits 0 and references/finding-codes.md
      renders all four DSX-CRV-* rows under a DSX-CRV section (not just in the total count).
    - dsx/checks/chart_review.py contains no reference to scores/gates/final_assessment fields.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_chart_review tests.test_gen_finding_catalogue -v && python scripts/gen-finding-catalogue.py --check</automated>
  </verify>
  <done>The four codes fire structurally, degrade safely, and render in the catalogue with citation enforcement on.</done>
</task>

<task type="execute">
  <name>Task 3: Register chart_review at verify/ship (cli.py) and route DSX-CRV-* as a report-acceptance precondition (D-14 schema)</name>
  <files>dsx/cli.py, references/chart-review-schema.md</files>
  <read_first>
    - dsx/cli.py:23-38 (import block), :63-83 (CHECKS dict), :119-128 (GATE_PROFILES), :187-234
      (run_checks — the `elif name == "figures":` branch at :196-197 to mirror for phase_dir)
    - references/chart-review-schema.md:46-53 (the Gate-proxy table; the Gate-D DSX-FIG-* wildcard
      at :53 is the circularity to avoid) and 11.3-CONTEXT.md D-14
    - 11.3-PATTERNS.md "dsx/cli.py" four-registry section
  </read_first>
  <action>
    Register chart_review in dsx/cli.py at all four sites mirroring figures: add the import
    (alphabetically before claims); add it to CHECKS and give it an explicit `elif name ==
    "chart_review":` branch in run_checks() that passes phase_dir/root like figures.check (NOT a
    bare CHECKS[name](spec) dispatch); add chart_review to GATE_PROFILES["verify"] and
    GATE_PROFILES["ship"] tuples ONLY (NOT plan/execute — it is a post-execution artifact, mirroring
    figures/narrative/claims/smells). In references/chart-review-schema.md update the Gate-proxy
    table (:46-53) so DSX-CRV-* gates the validity of the report AS AN ARTIFACT (a report-acceptance
    precondition row), explicitly NOT folded into Gate D's DSX-FIG-* wildcard. Confirm the gate
    contract is unaffected: examples/ has no convention-named CHART-REVIEW.md, so chart_review.check
    degrades to ok and the good spec still passes / the bad spec still blocks.
  </action>
  <acceptance_criteria>
    - dsx/cli.py imports chart_review and lists it in GATE_PROFILES verify and ship (not plan/execute),
      with an explicit run_checks branch passing phase_dir.
    - references/chart-review-schema.md Gate-proxy table routes DSX-CRV-* as a precondition, not into
      Gate D.
    - `./bin/dsx gate verify --spec examples/good-ANALYSIS-SPEC.yaml` exits 0 and
      `./bin/dsx gate ship --spec examples/good-ANALYSIS-SPEC.yaml` exits 0; the bad spec exits 1 at both.
    - `python -m unittest discover -s tests -q` green.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest discover -s tests -q</automated>
  </verify>
  <done>chart_review runs at verify/ship, the schema routes DSX-CRV-* as a precondition, and the canonical gate contract is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-authored CHART-REVIEW.md → chart_review.check | An adversarial or malformed report artifact crosses into the gate reader. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.3-11 | Denial of Service | Malformed/truncated/undecodable CHART-REVIEW.md causing a gate-path traceback | high | mitigate | check() catches decode/parse errors and degrades to an ok/empty report, mirroring figures.check/val.check (T-7-01); tests feed truncated + undecodable content and assert no raise. |
| T-11.3-12 | Spoofing | A stochastic agent verdict (final_assessment / scores.* / transcribed gates.A-D / verdict prose) smuggled into a DSX-CRV fire condition | high | mitigate | Enforced by construction — the four functions read only structural properties (D-13); a boundary test asserts flipping only a forbidden-input field changes no verdict. |
| T-11.3-13 | Elevation of Privilege | A malformed report minting a DSX-FIG-* code swept into Gate D (circularity) | medium | mitigate | The family carries the DSX-CRV prefix and the D-14 edit routes it as a report-acceptance precondition, never into Gate D's DSX-FIG-* wildcard. |
| T-11.3-SC | Tampering | package installs | low | accept | No packages installed (D-01 stdlib-only); parsing reuses the existing dsx/loader.py. |
</threat_model>

<verification>
- `python -m unittest tests.test_chart_review tests.test_gen_finding_catalogue -v` green.
- `python scripts/gen-finding-catalogue.py --check` exits 0 (four DSX-CRV rows rendered + enforced).
- `./bin/dsx gate verify --spec examples/good-ANALYSIS-SPEC.yaml` exits 0; the bad spec exits 1.
- `python -m unittest discover -s tests -q` green (wave-merge check).
</verification>

<success_criteria>
- REQ-P11.3-06: four structural DSX-CRV-* codes validate CHART-REVIEW.md against the schema with no
  stochastic agent verdict gates; the module degrades safely; the family is registered in both
  catalogue registries and routed as a report-acceptance precondition; the canonical gate contract
  is unchanged.
</success_criteria>

<output>
Create `.planning/phases/11.3-reporting-completeness/11.3-05-SUMMARY.md` when done.
</output>
