---
phase: 11.3-reporting-completeness
plan: 06
type: execute
wave: 5
depends_on: [11.3-01, 11.3-02, 11.3-03, 11.3-04, 11.3-05]
files_modified:
  - references/finding-codes.md
autonomous: true
requirements: [REQ-P11.3-07]
must_haves:
  truths:
    - "references/finding-codes.md is current and the total moves from 248 to 256 (8 net-new codes) — verified via gen-finding-catalogue.py --check, not asserted by hand (D-15)."
    - "The full corpus re-run is green: scripts/check.sh passes (unit suite + catalogue --check + capability manifest + gate contract good/bad/missing + determinism)."
    - "Every D-15 fixture obligation is present (conforming + malformed CHART-REVIEW.md, unknown missingness method, no-family multiplicity, exclusions with/without justification + unexpected key, unrecognised effect_size_kind, single_imputation under MAR)."
    - "All eight new codes are reachable from a gate profile. DSX-VAL-080 and DSX-CRV-010/011/012/013 carry a D-05 citation/structural-criterion line enforced by --check (their prefixes are on _D05_ALLOWLIST_PREFIXES). DSX-EXP-053, DSX-STA-012 and DSX-SPEC-083 live in legacy families NOT on the prefix allowlist, so --check does NOT enforce their citation lines; each still carries its Citation/Structural-criterion line, verified by that code's own unit test and the human D-05 read owed at the 11.3 UAT/ship round (CONTEXT <deferred>, non-blocking for plan/execute)."
  artifacts:
    - "references/finding-codes.md at 256 codes, all eight new codes rendered under their sections"
    - "A green scripts/check.sh run recorded in the SUMMARY"
  key_links:
    - "This plan gates on every prior code existing — it is the last wave. It regenerates/verifies the catalogue once against the fully-merged tree."
  prohibitions:
    - statement: "The corpus reconciliation must not be achieved by editing expected-defect maps to absorb a verdict change — a moved fixture verdict is surfaced and justified, never silently swallowed."
      status: resolved
      verification: "scripts/check.sh green with the corpus harness maps unedited except for deliberately added, documented fixtures."
---

<objective>
Deliver REQ-P11.3-07: prove the phase closed cleanly. Regenerate and verify the finding catalogue
at 256 codes, confirm every D-15 fixture obligation is present, and run the full corpus gate
(scripts/check.sh) green against the fully-merged tree.

Purpose: the catch rate Phase 12 measures must cover every check this phase adds, so the codes,
fixtures, tests and catalogue must all be current and the corpus must be green before the phase is
marked complete.
Output: a current 256-code catalogue, a green check.sh run, and a recorded fixture-presence audit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.3-reporting-completeness/11.3-CONTEXT.md
@.planning/phases/11.3-reporting-completeness/11.3-RESEARCH.md
@.planning/phases/11.3-reporting-completeness/11.3-VALIDATION.md
</context>

## Artifacts this phase produces (whole phase, for orientation)

- Net-new finding codes (8): `DSX-EXP-053`, `DSX-STA-012`, `DSX-VAL-080`, `DSX-SPEC-083`,
  `DSX-CRV-010/011/012/013`. Catalogue 248 → 256 (verified in THIS plan).
- Reused codes: `DSX-EXP-051` (rebased), `DSX-SPEC-082` (membership row), `DSX-VAL-060` (CRITICAL branch).
- New module `dsx/checks/chart_review.py`; new functions `_check_exclusions`, the effect-size
  guard, the exclusions closed-key guard; new vocab `MISSINGNESS_METHODS`.
- New spec fields `validity_frame.exclusions.{rule, action, applied_before_split, justification}`,
  `missingness.method_implied`. New fixtures + test modules per D-15.

**This plan produces:** the final currency verification — a 256-code catalogue, the D-15
fixture-presence audit, and a green full-corpus run.

<tasks>

<task type="execute">
  <name>Task 1: Regenerate + verify the catalogue at 256, and audit D-15 fixture presence</name>
  <files>references/finding-codes.md</files>
  <read_first>
    - references/finding-codes.md:16 (the `**Total: N codes.**` header — currently 248) and the
      DSX-EXP / DSX-STA / DSX-VAL / DSX-SPEC / DSX-CRV sections
    - scripts/gen-finding-catalogue.py (`--check` drift + D-05 citation enforcement)
    - 11.3-CONTEXT.md D-15(a)-(f) (the enumerated fixture obligations) and 11.3-VALIDATION.md
      "Wave 0 Requirements" (the fixture checklist)
  </read_first>
  <action>
    Run `python scripts/gen-finding-catalogue.py` to regenerate references/finding-codes.md against
    the fully-merged tree, then `python scripts/gen-finding-catalogue.py --check` to confirm it is
    current. --check enforces the D-05 citation line only for allowlisted prefixes (DSX-VAL-080,
    DSX-CRV-*); DSX-EXP-053/DSX-STA-012/DSX-SPEC-083 are in legacy families off the prefix allowlist
    and are not --check-enforced (their citation lines are verified by their own unit tests + the
    human UAT read). Confirm the total is 256 and
    that all eight new codes (DSX-EXP-053, DSX-STA-012, DSX-VAL-080, DSX-SPEC-083, DSX-CRV-010/011/
    012/013) render under their sections. Audit the D-15 fixtures against the tree by glob (whole
    repo, not just root): the conforming examples/good-CHART-REVIEW.md and the malformed variants
    under tests/fixtures/chart-review/; and confirm the unknown-missingness-method, no-family
    multiplicity, exclusions with/without justification + unexpected key, unrecognised
    effect_size_kind, and single_imputation-under-MAR cases are exercised by their unit tests. If
    any obligation is genuinely missing, STOP and report it — do not paper over a gap.
  </action>
  <acceptance_criteria>
    - `python scripts/gen-finding-catalogue.py --check` exits 0.
    - references/finding-codes.md header reads Total: 256 codes and contains rows for all eight new codes.
    - Every D-15(a)-(f) obligation is present (fixture file on disk or exercised by a named unit test),
      recorded explicitly in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>python scripts/gen-finding-catalogue.py --check</automated>
  </verify>
  <done>The catalogue is current at 256 and every D-15 fixture obligation is accounted for.</done>
</task>

<task type="execute">
  <name>Task 2: Run the full corpus gate green</name>
  <files>references/finding-codes.md</files>
  <read_first>
    - scripts/check.sh (unit suite + catalogue --check + capability manifest + gate contract
      good/bad/missing + determinism) and how it isolates the good/bad gate runs into temp copies
    - tests/test_known_bad_corpus.py `_TARGET_DEFECT_CODES` / `_EXPECTED_CAUGHT_DEFECTS` /
      `_INCIDENTAL_GAP_CODES` (the maps that must NOT be edited to absorb a verdict change)
  </read_first>
  <action>
    Run `bash scripts/check.sh` and confirm every stage passes: the full unit suite, the catalogue
    --check, the capability manifest, the good/bad/missing gate contract, and the determinism check.
    If the corpus harness reports a moved verdict, surface and justify it (a deliberately added,
    documented fixture) rather than editing _TARGET_DEFECT_CODES / _EXPECTED_CAUGHT_DEFECTS /
    _INCIDENTAL_GAP_CODES to swallow it. Record the green run in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `bash scripts/check.sh` exits 0 with every stage reported green.
    - No corpus-harness expected-defect map was edited to absorb an unexplained verdict change.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check.sh</automated>
  </verify>
  <done>The full corpus re-run is green; the phase is ready for verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the committed tree → the corpus gate | The full corpus and catalogue are the artifacts the release is judged against. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.3-14 | Repudiation | A new code silently dropped from the rendered catalogue while still counted in the total (Pitfall 2) | medium | mitigate | --check drift compares the rendered body, and Task 1 confirms every new code renders under a section, not just in the total. |
| T-11.3-15 | Tampering | An uncited new code passing --check because its prefix is absent from the D-05 allowlist (Pitfall 3) | medium | mitigate | Plan 11.3-05 added DSX-CRV- to _D05_ALLOWLIST_PREFIXES; --check here confirms enforcement for the allowlisted new prefixes (DSX-VAL-080, DSX-CRV-*). DSX-EXP-053/DSX-STA-012/DSX-SPEC-083 are in legacy off-allowlist families — deliberately NOT --check-enforced (D-05 read deferred to UAT per CONTEXT <deferred>); their citation lines are pinned by each code's own unit test instead. |
| T-11.3-SC | Tampering | package installs | low | accept | No packages installed (D-01 stdlib-only). |
</threat_model>

<verification>
- `python scripts/gen-finding-catalogue.py --check` exits 0; total is 256.
- `bash scripts/check.sh` exits 0 (all stages green).
- All D-15 fixture obligations accounted for and recorded.
</verification>

<success_criteria>
- REQ-P11.3-07: catalogue current at 256, full corpus re-run green, every D-15 fixture obligation present.
</success_criteria>

## Manual-only verification (non-blocking, end-of-phase UAT/ship)

- D-05 primary-source read: Rubin (1987) §3.1 (`T = W̄ + (1+1/m)·B`) anchors the
  single-imputation-CRITICAL branch (a NEW citation). The human confirms the source supports
  standard-error fabrication → CRITICAL. Owed at the 11.3 UAT/ship round; the citation text/anchor
  already ships in the code (Plan 11.3-03). This is a `<human-check>`, not a blocking checkpoint
  (human_verify_mode: end-of-phase).

<output>
Create `.planning/phases/11.3-reporting-completeness/11.3-06-SUMMARY.md` when done.
</output>
