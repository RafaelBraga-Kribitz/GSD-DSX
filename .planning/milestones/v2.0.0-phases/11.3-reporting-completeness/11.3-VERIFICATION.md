---
phase: 11.3-reporting-completeness
verified: 2026-08-27T12:00:00Z
status: passed
score: 7/7 requirements satisfied
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: none
  note: "Initial verification. Runs against merged tree at HEAD 0ffbddf (S2-4 WR-03 fix)."
gaps: []
deferred:
  - truth: "Missingness-rate reconciliation (declared missingness.rate vs profile null rates)"
    addressed_in: "Gated backlog (D-07 entry condition); Phase 12 measurement"
    evidence: "11.3-CONTEXT.md D-07 — OPEN ITEM, not a numbered requirement; deferred by the Statistician-led round. The concrete defect stays covered by DSX-DQ-030 + DSX-VAL-060."
  - truth: "Post-split-exclusion firing check (applied_before_split: false without a post-treatment-safety justification)"
    addressed_in: "Deferred backlog (D-10)"
    evidence: "11.3-CONTEXT.md D-10 — field carried RECORD-ONLY in 11.3; a MEDIUM-firing check is a distinct concept/code, deferred."
  - truth: "DSX-CRV-011 numeric N/10 catch-rate hole; chart-review corpus discoverability (IN-01)"
    addressed_in: "Phase 12"
    evidence: "11.3-REVIEW.md — WR-01 + IN-01 deferred to Phase-12 backlog with stated entry conditions; CRV-011 is MEDIUM (non-blocking), zero gate verdicts change today."
human_verification:
  - test: "Read Rubin (1987) Multiple Imputation for Nonresponse in Surveys §3.1 and confirm the total-variance formula T = W̄ + (1+1/m)·B anchors the DSX-VAL-060 single-imputation-under-MAR CRITICAL branch."
    expected: "The cited variance decomposition supports treating single imputation (B forced to 0) as the anti-conservative, strictly-worse structural defect that CRITICAL/blocks-at-plan encodes."
    why_human: "Citation-authenticity read against a primary source; no test can verify the source says what the docstring claims (dsx/frame/val.py:1102,1209)."
  - test: "Read Simmons, Nelson & Simonsohn (2011) 'False-Positive Psychology' and confirm it anchors DSX-VAL-080 (data exclusion as a researcher degree of freedom)."
    expected: "The paper supports requiring a justification on a declared row-exclusion rule; the exact requirement-number/table locator is honestly flagged UNVERIFIED in-code."
    why_human: "Citation-authenticity read; the in-code locator is disclosed UNVERIFIED per D-05 (dsx/frame/val.py _check_exclusions docstring)."
  - test: "Confirm the D-06 finding-code veto window: the 8 net-new codes (DSX-EXP-053 HIGH, DSX-STA-012 MEDIUM, DSX-VAL-080 HIGH, DSX-SPEC-083 HIGH, DSX-CRV-010 HIGH / -011 MEDIUM / -012 HIGH / -013 MEDIUM) carried an operator-veto window via the daily summary."
    expected: "No operator veto raised against the count or the severities during the window."
    why_human: "Operator-veto governance record; not a codebase artifact a test can assert."
---

# Phase 11.3: Reporting completeness and missing-data discipline — Verification Report

**Phase Goal:** The examined-but-unreported comparisons and the imputation method become first-class declarations the gate holds to account — an under-declared multiplicity family no longer buys silence; single imputation treated as observed under MAR blocks before the data is touched; row-exclusion rules must carry a justification; an unrecognised effect-size kind is a visible MEDIUM; and CHART-REVIEW.md is validated for structural conformance by four deterministic codes with no stochastic verdict allowed to gate.
**Verified:** 2026-08-27T12:00:00Z
**Status:** passed (technically PASSED — all 7 requirements VERIFIED with named passing tests; `human_needed` solely for the three D-05/D-06 primary-source / veto-window reads that no test can perform, mirroring Phase 11.2's pattern)
**Re-verification:** No — initial verification against the merged tree at HEAD `0ffbddf`.

## Goal Achievement

### Observable Truths (the 7 requirements ARE the truths)

| # | Truth (requirement) | Status | Evidence (`file:line` + named passing test) |
|---|---|---|---|
| 1 | REQ-P11.3-01 — Multiplicity family counts `max(declared family, reported tests)`; a non-empty family smaller than the reported set fires DSX-EXP-053 HIGH naming the absent metrics; the stale cross-reference comment is corrected. | ✓ VERIFIED | `dsx/checks/design.py:384` (`n_tests = max(len(family)…, len(tests))`), emit `DSX-EXP-053` at `:404`, absent-metric naming `family_metrics = {m for m in family if isinstance(m, str)}` / `absent = sorted(reported - family_metrics)` at `:401-402` (WR-03 hardening). D-03 stale-comment fix: `dsx/spec.py:506-507` now names reading functions `_check_exploratory_looks`/`_check_peeking` instead of drifting `design.py:452/:446/:407` line refs. Named passing tests: `tests.test_multiplicity_family.TestMultiplicityFamilyUnderDeclared.test_exp_053_fires_and_names_absent_metric`, `…test_exp_053_no_traceback_on_nonscalar_family` (WR-03 regression). |
| 2 | REQ-P11.3-02 — `comparisons_looked_at` exceeding reported results fires DSX-EXP-051 HIGH with the ratio quoted, independent of family presence (rebase, no new code). | ✓ VERIFIED | `dsx/checks/design.py:462` (`base = max(len(family)…, len(tests))`), emit `DSX-EXP-051` at `:466`; the `multiplicity.family` early-return dropped. Named passing tests: `tests.test_multiplicity_family.TestExploratoryLooksFamilyIndependent.test_exp_051_fires_with_no_family_declared` (fire), `…test_exp_051_silent_on_good_arithmetic` (verdict-neutral on good). |
| 3 | REQ-P11.3-03 — `missingness.method_implied` closed vocabulary registered in frame membership (reuse DSX-SPEC-082); single-imputation-as-if-observed under MAR blocks at plan CRITICAL (reuse DSX-VAL-060). | ✓ VERIFIED | `dsx/spec.py:460` `MISSINGNESS_METHODS` (7 locked members incl. `multiple_imputation`+`single_imputation`), membership row `("missingness","method_implied",MISSINGNESS_METHODS)` at `:1164`; `dsx/frame/val.py:197-210` per-method severity split (`single_imputation`→CRITICAL), CRITICAL branch with own Rubin (1987) §3.1 citation at `:1198-1219`. Named passing tests: `tests.test_frame_val.TestValSamplingMissingnessMeasurement.test_missingness_mar_with_single_imputation_fires_critical_val_060`, `…test_missingness_unknown_method_implied_fires_spec_082_high`, `…test_missingness_mar_with_multiple_imputation_fires_neither_val_060_nor_spec_082` (good stays green). |
| 4 | REQ-P11.3-04 — `validity_frame.exclusions` sub-block (rule, action, applied_before_split, justification); a rule without justification fires DSX-VAL-080 HIGH; row counts never in the frame (DSX-SPEC-083 closed-key guard). | ✓ VERIFIED | `dsx/frame/val.py:1295` `_check_exclusions` (presence-guarded, DSX-VAL-080 HIGH, Simmons/Nelson/Simonsohn 2011 citation, `applied_before_split` record-only per D-10); `dsx/spec.py:1184` `_EXCLUSIONS_ALLOWED_KEYS` + closed-key guard emitting `DSX-SPEC-083` at `:1307`, scoped to `frame.get("exclusions")` only (legacy inference tolerance untouched). Content-lock reuse: `frame_digest`/DSX-PRE-020 covers the sub-block, no new machinery. Named passing tests: `tests.test_frame_val.TestValExclusions.test_rule_without_justification_fires_high_val_080_single_dict`, `…test_unexpected_key_in_exclusions_fires_high_spec_083_single_dict`; content lock `tests.test_frame_prereg.TestExclusionsContentLock` (in 247-test module run). |
| 5 | REQ-P11.3-05 — An unrecognised effect-size kind fires DSX-STA-012 MEDIUM instead of silently skipping. | ✓ VERIFIED | `dsx/checks/stats.py:287` membership guard PRECEDES `interpret_effect`, single-sourced `EFFECT_SIZE_KINDS` imported from `dsx/mathx.py` (`:12`), emit `DSX-STA-012` MEDIUM at `:300`; no try/except, so the `mathx.py` ValueError path never reaches the gate (D-01). Named passing tests: `tests.test_effect_size_kind.TestEffectSizeKindGuard.test_unrecognised_kind_fires_medium`, `…test_recognised_kind_stays_silent`, `…test_unrecognised_kind_does_not_raise`. |
| 6 | REQ-P11.3-06 — Four structural conformance codes validate CHART-REVIEW.md against the schema; no stochastic agent verdict gates. | ✓ VERIFIED | `dsx/checks/chart_review.py` — `DSX-CRV-010` HIGH `:135` (schema tag ≠ `dsx-chart-review-v1`), `DSX-CRV-011` MEDIUM `:167` (forbidden `X/10`), `DSX-CRV-012` HIGH `:198` (missing `## CHART AUDIT COMPLETE` sentinel), `DSX-CRV-013` MEDIUM `:232` (untokenised finding line); structure-only reads, forbidden-input names appear only in docstrings. Wired into `dsx/cli.py` GATE_PROFILES verify+ship; D-14 schema routes `DSX-CRV-*` as a report-acceptance precondition (`references/chart-review-schema.md:57-70`), NOT into Gate D's `DSX-FIG-*` wildcard. Named passing tests: `tests.test_chart_review.TestChartReview.test_good_fixture_fires_none_of_the_four_crv_codes`, `tests.test_chart_review.TestChartReviewD13Boundary.test_forbidden_input_flip_matches_good_fixture_verdict_exactly` (proves no verdict content is read), plus the 16-test module. |
| 7 | REQ-P11.3-07 — Fixtures/tests/catalogue current; full corpus re-run green. | ✓ VERIFIED | `references/finding-codes.md` header `Total: 256 codes`; all 8 net-new codes render (DSX-EXP-053, DSX-STA-012, DSX-VAL-080, DSX-SPEC-083, DSX-CRV-010/011/012/013 confirmed via grep). `python scripts/gen-finding-catalogue.py --check` → exit 0 "finding catalogue is current". `bash scripts/check.sh` → **all checks passed**: `Ran 1199 tests … OK`, catalogue current, capability manifest conformant, gate contract (good passes / bad blocks / missing errors), determinism identical. |

**Score:** 7/7 requirements verified (0 present-but-behavior-unverified). Every truth is backed by a named passing test that exercises fire-on-bad and silence-on-good; no truth rests on symbol presence alone.

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|---|---|---|
| 1 | Missingness-rate reconciliation | Gated backlog (D-07) / Phase 12 | D-07 unanimous defer; OPEN ITEM not a numbered requirement; defect covered by DSX-DQ-030 + DSX-VAL-060. |
| 2 | Post-split-exclusion firing check | Deferred backlog (D-10) | `applied_before_split` carried record-only; MEDIUM check is a distinct concept/code, deferred. |
| 3 | DSX-CRV-011 numeric N/10 hole; chart-review corpus discoverability | Phase 12 | 11.3-REVIEW.md WR-01 + IN-01 deferred with entry conditions; CRV-011 MEDIUM, zero gate verdicts change today. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `dsx/checks/stats.py` | `dsx/mathx.py` | `from ..mathx import EFFECT_SIZE_KINDS` (single-source guard/dispatch) | ✓ WIRED | `stats.py:12`, membership tested at `:287` before the raising callee. |
| `dsx/checks/chart_review.py` | `dsx/cli.py` | CHECKS registry + run_checks branch + GATE_PROFILES verify/ship | ✓ WIRED | REVIEW.md "Clean" confirms four-site registration; gate contract green in check.sh. |
| `dsx/spec.py MISSINGNESS_METHODS` | `dsx/spec.py _VALIDITY_FRAME_MEMBERSHIP` | membership row → DSX-SPEC-082 loop | ✓ WIRED | Row at `:1164`; unknown-method test fires SPEC-082 HIGH. |
| `validity_frame.exclusions` | `DSX-PRE-020` content lock | `frame_digest` hashes whole `validity_frame` subtree | ✓ WIRED | `TestExclusionsContentLock` passes; no new machinery. |
| `DSX-CRV-*` | `references/chart-review-schema.md` | report-acceptance precondition table (D-14) | ✓ WIRED | `:57-70`; explicitly NOT folded into Gate D's DSX-FIG-* wildcard (D-12 circularity avoided). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| 5 named 11.3 test modules | `python -m unittest tests.test_multiplicity_family tests.test_effect_size_kind tests.test_frame_val tests.test_chart_review tests.test_frame_prereg` | Ran 247 tests OK | ✓ PASS |
| Full corpus gate | `bash scripts/check.sh` | Ran 1199 tests OK; all checks passed; good/bad/missing contract holds; determinism identical | ✓ PASS |
| Catalogue currency | `python scripts/gen-finding-catalogue.py --check` | exit 0, "finding catalogue is current", Total: 256 codes | ✓ PASS |
| Per-requirement fire/silence | 16 named tests across REQ-01..06 (run verbosely) | all OK | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Status | Evidence |
|---|---|---|---|
| REQ-P11.3-01 | 11.3-01 | ✓ SATISFIED | DSX-EXP-053 + max() undercount + D-03 comment fix; named tests pass. |
| REQ-P11.3-02 | 11.3-01 | ✓ SATISFIED | DSX-EXP-051 family-independent rebase; named tests pass. |
| REQ-P11.3-03 | 11.3-03 | ✓ SATISFIED | 7-member vocab + SPEC-082 membership + VAL-060 CRITICAL; named tests pass. |
| REQ-P11.3-04 | 11.3-04 | ✓ SATISFIED | DSX-VAL-080 + DSX-SPEC-083 closed-key guard + content lock; named tests pass. |
| REQ-P11.3-05 | 11.3-02 | ✓ SATISFIED | DSX-STA-012 guard-before-dispatch; named tests pass. |
| REQ-P11.3-06 | 11.3-05 | ✓ SATISFIED | 4 DSX-CRV codes, structure-only, precondition routing; named tests pass. |
| REQ-P11.3-07 | 11.3-06 | ✓ SATISFIED | Catalogue 256, corpus 1199 tests OK, check.sh green. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| — | — | None blocking | ℹ️ Info | REVIEW.md (status `resolved`, 0 open) already reproduced and dispositioned all 6 findings: WR-03 fixed (`0ffbddf`), 3 by-design, 2 deferred to Phase-12 with stated entry conditions. No unreferenced debt marker (TODO/FIXME/XXX) introduced by the phase; no gate-path traceback (degrade-to-empty + guard-before-dispatch confirmed). |

### Human Verification Required

Three items — all citation-authenticity / governance reads no test can perform (non-blocking; phase is technically PASSED):

1. **Rubin (1987) §3.1 primary-source read** — confirm `T = W̄ + (1+1/m)·B` anchors the DSX-VAL-060 single-imputation-under-MAR CRITICAL branch (`dsx/frame/val.py:1102,1209`).
2. **Simmons, Nelson & Simonsohn (2011) read** — confirm it anchors DSX-VAL-080; the exact locator is flagged UNVERIFIED in-code by design.
3. **D-06 finding-code veto window** — confirm the 8 net-new codes carried an operator-veto window via the daily summary with no veto raised.

### Gaps Summary

No genuine gaps. All 7 requirements are implemented in the merged tree at HEAD `0ffbddf`, each backed by a named passing test exercising both the fire-on-bad and silence-on-good (verdict-neutral) paths, and the full corpus gate is green (`Ran 1199 tests … OK`, catalogue current at 256 codes, gate contract intact, determinism confirmed). The three deferred items are governed backlog entry-conditions (D-07/D-10) and Phase-12 catch-rate boundaries (REVIEW WR-01/IN-01), explicitly out of scope for 11.3. Status is `human_needed` only for the D-05 primary-source reads and the D-06 veto-window confirmation, mirroring Phase 11.2's UAT pattern.

---

_Verified: 2026-08-27T12:00:00Z_
_Verifier: Claude (gsd-verifier)_

---

**S4-4 RECONCILIATION (2026-08-28):** Status reconciled `human_needed` → `passed`. The technical score was already full (`behavior_unverified: 0`, no gaps); `human_needed` was owed solely to the human D-05/veto/sign-off items in the `human_verification` list above, all of which are now answered by the operator: HQ-5 (operator verdict recorded 2026-08-27: “approve all citations + accept codes/dispositions; security approved”). 2 D-05 reads + the 8-code D-06 veto window + security sign-off all granted. See `.planning/HUMAN-QUEUE.md` §“Answered” for the verbatim verdicts. This closes the phase's human bar; it does not re-open or weaken any automated gate.
