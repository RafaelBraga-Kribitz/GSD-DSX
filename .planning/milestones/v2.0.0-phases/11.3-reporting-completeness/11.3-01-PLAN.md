---
phase: 11.3-reporting-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dsx/checks/design.py
  - dsx/spec.py
  - tests/test_frame_val.py
  - tests/test_multiplicity_family.py
  - references/finding-codes.md
autonomous: true
requirements: [REQ-P11.3-01, REQ-P11.3-02]
must_haves:
  truths:
    - "A spec declaring a non-empty multiplicity family SMALLER than results.tests fires DSX-EXP-053 HIGH and names the reported metrics absent from the family (D-01, ROADMAP SC1)."
    - "comparisons_looked_at exceeding the reported test count fires DSX-EXP-051 HIGH with the ratio quoted, whether or not a family is declared (D-02, ROADMAP SC2)."
    - "examples/good-ANALYSIS-SPEC.yaml still exits 0 at dsx gate plan and examples/bad-ANALYSIS-SPEC.yaml still exits 1 (verdict-neutral rebase)."
    - "The full unittest suite is green after the edit, including tests/test_frame_val.py's design.py content lock."
  artifacts:
    - "dsx/checks/design.py with DSX-EXP-053 minted in _check_multiplicity and the DSX-EXP-051 family early-return removed in _check_exploratory_looks"
    - "tests/test_multiplicity_family.py (new) covering both fire paths and the family-absent path"
    - "tests/test_frame_val.py _DESIGN_PY_SHA256 updated in the SAME commit with a dated Phase-11.3 comment"
    - "references/finding-codes.md regenerated to include DSX-EXP-053"
  key_links:
    - "The _DESIGN_PY_SHA256 content lock (tests/test_frame_val.py) must be recomputed in the same commit as the dsx/checks/design.py edit or the whole suite goes red on a misleading Phase-7 message."
  prohibitions:
    - statement: "The family count must never silently undercount — n_tests uses max(len(family), len(results.tests)), so a family with >=1 member no longer suppresses the results-based count (design.py:362-370 bug)."
      status: resolved
      verification: "tests/test_multiplicity_family.py asserts DSX-EXP-053 fires when family is non-empty and smaller than results.tests."
---

<objective>
Deliver the two multiplicity-reporting checks (REQ-P11.3-01, REQ-P11.3-02) inside the Phase 4
machinery in dsx/checks/design.py: mint DSX-EXP-053 for an under-declared multiplicity family,
and rebase the existing DSX-EXP-051 comparison so it fires independent of whether a family is
declared. Correct the stale PREREG_FACTS cross-reference comment (D-03) so it cannot drift again,
and update the design.py content-lock hash in the same commit.

Purpose: An analyst who declares one hypothesis but reports five no longer buys silence, and a
count of examined comparisons that exceeds the reported set is surfaced with the ratio quoted.
Output: DSX-EXP-053 (HIGH), a rebased DSX-EXP-051, a corrected PREREG_FACTS comment, a new test
module, the updated content-lock hash, and a regenerated catalogue.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.3-reporting-completeness/11.3-CONTEXT.md
@.planning/phases/11.3-reporting-completeness/11.3-RESEARCH.md
@.planning/phases/11.3-reporting-completeness/11.3-PATTERNS.md
</context>

## Artifacts this phase produces (whole phase, for orientation)

- Net-new finding codes (8): `DSX-EXP-053` HIGH, `DSX-STA-012` MEDIUM, `DSX-VAL-080` HIGH,
  `DSX-SPEC-083` HIGH, `DSX-CRV-010` HIGH, `DSX-CRV-011` MEDIUM, `DSX-CRV-012` HIGH,
  `DSX-CRV-013` MEDIUM. Catalogue 248 → 256.
- Reused codes (mint nothing): `DSX-EXP-051` (rebased family-independent), `DSX-SPEC-082` (new
  membership row), `DSX-VAL-060` (new CRITICAL branch).
- New module `dsx/checks/chart_review.py`; new functions `_check_exclusions` (dsx/frame/val.py),
  the effect-size membership guard (dsx/checks/stats.py), the exclusions closed-key guard
  (dsx/spec.py); new vocab constant `MISSINGNESS_METHODS` (dsx/spec.py, 7 members).
- New spec fields `validity_frame.exclusions.{rule, action, applied_before_split, justification}`
  and `missingness.method_implied`.
- New fixtures: `examples/good-CHART-REVIEW.md` + malformed variants; the D-15 case fixtures.
- New test modules `tests/test_chart_review.py`, `tests/test_multiplicity_family.py`,
  `tests/test_effect_size_kind.py`.

**This plan produces:** `DSX-EXP-053`; the rebased `DSX-EXP-051`; the corrected PREREG_FACTS
comment; `tests/test_multiplicity_family.py`; the `_DESIGN_PY_SHA256` update; the catalogue row
for `DSX-EXP-053`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pin DSX-EXP-053 and the family-independent DSX-EXP-051 against fixtures</name>
  <files>tests/test_multiplicity_family.py</files>
  <read_first>
    - tests/test_dsx.py (existing home of DSX-EXP-051 tests — copy its import + assertion style; a
      dedicated new module keeps this plan file-disjoint from Wave-2 plans that touch test_dsx.py)
    - dsx/checks/design.py:362-370 (_check_multiplicity n_tests undercount) and :399-404
      (the _check_exploratory_looks family early-return) — the exact current behaviour to invert
    - 11.3-RESEARCH.md "Code Examples" (good fixture family=3/tests=3/looked=3 → no fire; bad
      fixture family=5/tests=3/looked=20 → fires) and 11.3-PATTERNS.md `report.add` shared shape
  </read_first>
  <behavior>
    - DSX-EXP-053 fires HIGH when multiplicity.family is a non-empty list SHORTER than results.tests,
      and the finding detail names the reported test metrics not present in the declared family
      (set(t["metric"] for t in tests) - set(family)); e.g. family=[a,b], tests metrics {a,b,c} →
      fires naming c.
    - DSX-EXP-053 does NOT fire when family length >= results.tests length (bad fixture: family=5,
      tests=3 → silent for EXP-053).
    - DSX-EXP-051 fires HIGH when comparisons_looked_at > max(len(family), len(results.tests)),
      INCLUDING the case where no family is declared (D-15c: family absent, tests len 3,
      comparisons_looked_at 5 → fires), with the ratio ("N examined, M reported") in the detail.
    - DSX-EXP-051 stays silent on the good-fixture arithmetic (max(3,3)=3, looked=3, 3 is not > 3).
  </behavior>
  <action>
    Create tests/test_multiplicity_family.py as a unittest.TestCase module built on inline spec
    dicts (mirror the existing test_dsx.py convention — do NOT add YAML files under
    examples/known-bad/, which would pull in the corpus harness). Add one test per behavior clause
    above. Import the design-check entry the same way test_dsx.py does. These tests MUST fail now
    (RED) because DSX-EXP-053 does not exist and DSX-EXP-051 still early-returns on an absent family.
    Add a `# REQ-P11.3-01` / `# REQ-P11.3-02` marker comment above the relevant test methods.
  </action>
  <acceptance_criteria>
    - `python -m unittest tests.test_multiplicity_family -v` runs and FAILS (RED) with assertions
      referencing DSX-EXP-053 and the family-absent DSX-EXP-051 path.
    - The module contains at least: one DSX-EXP-053 fire test naming an absent metric, one
      DSX-EXP-053 no-fire test (family >= tests), one DSX-EXP-051 family-absent fire test, one
      DSX-EXP-051 good-arithmetic no-fire test.
    - No fixture file is created under examples/known-bad/.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_multiplicity_family -v</automated>
  </verify>
  <done>The new test module fails for the right reason: the two new behaviours are unimplemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — mint DSX-EXP-053, rebase DSX-EXP-051, update the content-lock hash (one commit)</name>
  <files>dsx/checks/design.py, tests/test_frame_val.py</files>
  <read_first>
    - dsx/checks/design.py full `_check_multiplicity` and `_check_exploratory_looks` bodies
    - 11.3-PATTERNS.md "dsx/checks/design.py" section (the exact undercount + early-return excerpts)
    - tests/test_frame_val.py:613-627 (the `_DESIGN_PY_SHA256` constant + its 2026-08-24 dated
      comment precedent) and :1038-1044 (the content-lock test that hashes design.py)
  </read_first>
  <behavior>
    - After the edit, tests/test_multiplicity_family.py (Task 1) turns GREEN.
    - The design.py content-lock test in tests/test_frame_val.py stays GREEN because the hash is
      updated in the same commit.
  </behavior>
  <action>
    In `_check_multiplicity` (design.py:362-370) replace the undercount `n_tests = len(family) if
    isinstance(family, list) else 0; if not n_tests and tests: n_tests = len(tests)` with
    `n_tests = max(len(family) if isinstance(family, list) else 0, len(tests))`. When family is a
    non-empty list AND len(family) < len(tests), emit `report.add("DSX-EXP-053", "HIGH", <short
    title>, detail=<names the absent metrics: set of results.tests metrics minus set(family)>,
    remedy="expand the multiplicity family to cover every reported test",
    where="spec.design.multiplicity.family")`. Severity MUST be the string literal "HIGH" (AST
    catalogue-extractor constraint). Carry a D-05 `Citation:`/`Structural criterion:` line —
    DSX-EXP-053 reuses the multiplicity family's existing citation lineage; state the structural
    criterion (family must be a superset of the reported tests) in the docstring.
    In `_check_exploratory_looks` (design.py:399-404) drop the `if not isinstance(family, list) or
    not family: return` early-return so the check runs with no declared family; compute the
    comparison base as `max(len(family) if isinstance(family, list) else 0, len(tests))` and fire
    the existing DSX-EXP-051 when comparisons_looked_at exceeds that base, quoting the ratio in the
    detail. Then, in the SAME commit: recompute
    `hashlib.sha256(Path("dsx/checks/design.py").read_bytes().replace(b"\r\n", b"\n")).hexdigest()`,
    paste it into `_DESIGN_PY_SHA256` (tests/test_frame_val.py), and append a new dated paragraph in
    the same style as the 2026-08-24 entry naming Phase 11.3 / D-01 / D-02 as the sanctioned reason.
  </action>
  <acceptance_criteria>
    - `python -m unittest tests.test_multiplicity_family -v` passes (GREEN).
    - `python -m unittest tests.test_frame_val -v` passes — the `_DESIGN_PY_SHA256` content-lock
      test does not fail.
    - design.py contains the literal `report.add("DSX-EXP-053", "HIGH"` and no longer contains the
      family early-return in `_check_exploratory_looks`.
    - `./bin/dsx gate plan --spec examples/good-ANALYSIS-SPEC.yaml` exits 0; `./bin/dsx gate plan
      --spec examples/bad-ANALYSIS-SPEC.yaml` exits 1.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_multiplicity_family tests.test_frame_val -v</automated>
  </verify>
  <done>Both new behaviours pass, the content lock is green, and the canonical good/bad gate verdicts are unchanged.</done>
</task>

<task type="execute">
  <name>Task 3: D-03 — replace the stale PREREG_FACTS line references with reading-function names, regenerate catalogue</name>
  <files>dsx/spec.py, references/finding-codes.md</files>
  <read_first>
    - dsx/spec.py:465-480 (the PREREG_FACTS comment block; the three hard-coded design.py:NNN refs
      at ~:469-471 that D-01/D-02's edits have just shifted)
    - 11.3-CONTEXT.md D-03 (use STABLE function-name references, not new line numbers)
    - scripts/gen-finding-catalogue.py usage (`--check`) and scripts/check.sh
  </read_first>
  <action>
    In the PREREG_FACTS comment (dsx/spec.py:465-480) replace the hard-coded `design.py:452`
    (alpha), `:446` (interim_looks) and `:407` (comparisons_looked_at) line references with the
    reading FUNCTIONS: comparisons_looked_at is read by `_check_exploratory_looks`; interim_looks
    and alpha are read by `_check_peeking`. This is a comment-only edit — no behaviour change — and
    it eliminates the staleness class rather than resetting line numbers REQ-01/02 have just proven
    drift. Do NOT touch the unrelated stale falsifier comment at spec.py:143 (deferred). Then run
    `python scripts/gen-finding-catalogue.py` (no `--check`) to regenerate references/finding-codes.md
    so the DSX-EXP-053 row is added; confirm the total ticks up by exactly one from this plan's code.
  </action>
  <acceptance_criteria>
    - The PREREG_FACTS comment names `_check_exploratory_looks` and `_check_peeking` and contains no
      `design.py:` line-number reference.
    - `python scripts/gen-finding-catalogue.py --check` exits 0 (references/finding-codes.md is
      current and includes a DSX-EXP-053 row).
    - `references/finding-codes.md` contains a `DSX-EXP-053` row.
  </acceptance_criteria>
  <verify>
    <automated>python scripts/gen-finding-catalogue.py --check</automated>
  </verify>
  <done>The cross-reference comment cannot drift on the next design.py edit, and the catalogue is current for DSX-EXP-053.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied ANALYSIS-SPEC.yaml → design.py checks | Untrusted declared `multiplicity`/`results` content crosses into the gate reader. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.3-01 | Denial of Service | `_check_multiplicity` / `_check_exploratory_looks` on a malformed `multiplicity`/`results` block | low | mitigate | Guard on types before indexing (family may be non-list, tests may be absent); the checks already degrade rather than raise (T-7-01 precedent) — assert no traceback on a malformed block in the new tests. |
| T-11.3-02 | Tampering | A family declared as a set-like string to dodge the length compare | low | accept | Membership/length arithmetic reads the declared list only; a non-list family is treated as empty, which is strictly more likely to fire, not less. |
| T-11.3-SC | Tampering | package installs | low | accept | No packages installed this plan (D-01 stdlib-only); supply-chain surface is nil. |
</threat_model>

<verification>
- `python -m unittest tests.test_multiplicity_family tests.test_frame_val -v` green.
- `python scripts/gen-finding-catalogue.py --check` exits 0.
- `./bin/dsx gate plan --spec examples/good-ANALYSIS-SPEC.yaml` exits 0; the bad spec exits 1.
- `python -m unittest discover -s tests -q` green (wave-merge check).
</verification>

<success_criteria>
- REQ-P11.3-01: DSX-EXP-053 HIGH fires on an under-declared family naming the absent metrics;
  PREREG_FACTS comment references reading functions.
- REQ-P11.3-02: DSX-EXP-051 fires independent of family presence with the ratio quoted.
- The design.py content lock is updated in the same commit; the suite is green; the canonical
  good/bad verdicts are unchanged.
</success_criteria>

<output>
Create `.planning/phases/11.3-reporting-completeness/11.3-01-SUMMARY.md` when done.
</output>
