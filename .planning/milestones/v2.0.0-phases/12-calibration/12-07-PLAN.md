---
phase: 12-calibration
plan: 07
type: execute
wave: 5
depends_on: [12-02, 12-05, 12-06]
files_modified:
  - brief.md
  - .planning/REVERSALS.md
autonomous: true
requirements: [REQ-P12-05]
must_haves:
  truths:
    - "Each of the nine §6.5 gated-backlog items is re-evaluated against its stated entry condition using the measured corpus and the measured paradigm split: carry 8, remove 1 (D-15)."
    - "Item 6 (ratio-metric dilution) is REMOVED as structurally unevaluable — relocated (not deleted) into a 'Removed / permanently out of scope (D-14)' subsection of §6.5, preserving the substrings the pin test asserts (D-16)."
    - "Items 4 and 5 stay CARRIED as prerequisite-pending; item 4 is NOT auto-promoted, because the honest operator-scoped split (plan 12-02) is expected below 15% (D-15)."
    - "Items 1, 3, 7, 8, 9 are carried per D-15; measurement-decided items (7, 8, 9) promote only if the measured corpus + attribution tags actually yield the naming case — never manufactured (D-02/D-15)."
    - "REV-002 is filed and survives SELF-001: its New evidence field frames the removal as the systematic REQ-P12-05 re-evaluation surfacing structural unreachability, not the determinism doctrine restated as novelty (D-17)."
  artifacts:
    - "brief.md §6.5 — 8 rows carried, item-6 row relocated into a new 'Removed / permanently out of scope (D-14)' subsection with pinned substrings intact"
    - ".planning/REVERSALS.md — REV-002 record (next free id after REV-001)"
  key_links:
    - "The dispositions read the measured (miss-rate, FPR) and friction counts (plans 12-05/12-06) and the measured paradigm split (plan 12-02) — the numbers decide, not narrative judgement."
    - "The pin test test_brief_states_the_ratio_metric_dilution_entry_condition_as_a_falsifiable_blocker (tests/test_known_bad_corpus.py:1043-1069) must keep passing UNCHANGED after the relocation."
  prohibitions:
    - statement: "The item-6 row is relocated, not deleted; the three pinned substrings remain present verbatim anywhere in brief.md, so the pin test passes without modification (D-16)."
      status: must-hold
      verification: "python -m unittest tests.test_known_bad_corpus.TestKnownBadCorpus.test_brief_states_the_ratio_metric_dilution_entry_condition_as_a_falsifiable_blocker passes with the pin test file unchanged."
    - statement: "REV-002's New evidence field never restates the D-01/D-02 determinism doctrine as if newly discovered — the doctrine pre-dates the deferral (SELF-001, D-17)."
      status: must-hold
      verification: "Human review at ship: New evidence names the systematic REQ-P12-05 re-evaluation as what surfaced the structural unreachability; What did not change lists D-01/D-02 stand, DSX-INT-030 stays shipped, items 4 & 5 carried."
    - statement: "No item is promoted by manufacturing a case to hit a threshold; measurement-decided items (7, 8, 9) and count-gated items (1) promote only on the honestly-measured corpus (D-02/D-15)."
      status: must-hold
      verification: "Each disposition cites the measured count/rate/split it rests on; item 4 is not auto-promoted by the polluted floor (plan 12-02 excludes it)."
    - statement: "'unevaluable ⇒ remove' applies only to structural unreachability, never to a merely-unmet condition; each such removal carries a D-14 REV and retains the pinned corrected row (D-17)."
      status: must-hold
      verification: "Only item 6 is removed; items 4/5 (evaluable-and-unmet) stay carried; REV-002 exists and the pinned row is retained."
---

<objective>
Re-evaluate every §6.5 gated-backlog item against its stated entry condition using the numbers plans 12-02/12-05/12-06 measured, dispose of them (carry 8, remove 1), relocate the one structurally-unevaluable item with its pinned substrings intact, and file REV-002 as an honest reclassification that survives SELF-001. This is the readout: the backlog hygiene the whole phase's measurement exists to make honest, plus the phase's corpus-green closing gate.

Purpose: Deliver REQ-P12-05 — each backlog condition evaluated against measured evidence or removed, with a D-14 reversal where a removal reverses a prior decision.
Output: The updated §6.5 table, REV-002, and a green full-suite closing gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-calibration/12-CONTEXT.md
@.planning/phases/12-calibration/12-RESEARCH.md
@.planning/phases/12-calibration/12-PATTERNS.md
@.planning/phases/12-calibration/12-05-SUMMARY.md
@.planning/phases/12-calibration/12-06-SUMMARY.md
@.planning/phases/12-calibration/12-02-SUMMARY.md
@.planning/REVERSALS.md
</context>

<artifacts_this_phase_produces>
- `brief.md` §6.5 — eight carried rows plus a new "Removed / permanently out of scope (D-14)" subsection holding the relocated item-6 row (pinned substrings intact).
- `.planning/REVERSALS.md` — the REV-002 record.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-evaluate the nine §6.5 items and relocate item 6 with its pinned substrings intact</name>
  <files>brief.md</files>
  <read_first>
    - brief.md:364-397 — the nine-item §6.5 table, the D-12a note, and the REV-001 record.
    - .planning/phases/12-calibration/12-05-SUMMARY.md and 12-06-SUMMARY.md — the measured (miss-rate, FPR) and friction counts.
    - .planning/phases/12-calibration/12-02-SUMMARY.md — the measured operator paradigm split (for item 4's <15% check).
    - tests/test_known_bad_corpus.py:1043-1069 — the pin test asserting the three item-6 substrings ("Ratio-metric dilution for trigger analysis", "Formula (3)", "per-unit trigger and outcome data reaching the gate"); whitespace-collapse normalized, location-independent.
    - .planning/phases/12-calibration/12-CONTEXT.md — D-15/D-16 (dispositions; carry 8 remove 1; item 4 not auto-promoted).
  </read_first>
  <action>
    Re-evaluate each of the nine §6.5 rows against its entry condition using the measured evidence: carry items 1, 3 (frequentist mirrors unwritten — a corpus count cannot promote them); carry items 4, 5 as prerequisite-pending (item 4's condition needs M4 AND a >15% honest split — the plan-12-02 operator split is expected below 15%, so do NOT auto-promote; item 5 needs M5 ship); note item 2 already promoted (REV-001); carry items 7, 8, 9 measurement-decided (promote ONLY if the measured corpus + validated attribution tags actually yield the naming case — item 7 needs ≥1 miss attributable only via feature origin, item 8 a case passing all claims checks while asserting an uncomputed magnitude, item 9 an admissible D-05 source AND a corpus case; never manufacture, D-02). Each disposition must cite the measured count/rate/split it rests on.

    REMOVE item 6 (ratio-metric dilution) as structurally unevaluable: create a new "Removed / permanently out of scope (D-14)" subsection under §6.5 and RELOCATE the existing item-6 row into it verbatim — do NOT delete it and do NOT soften it back to the access premise (D-12 proved access was never the blocker). Preserve the three substrings the pin test asserts exactly: "Ratio-metric dilution for trigger analysis", "Formula (3)", and "per-unit trigger and outcome data reaching the gate". Do not modify the pin test — it is location-independent and must keep passing as-is. Keep all edits CRLF-safe.
  </action>
  <acceptance_criteria>
    - source: §6.5 now carries eight items and has a "Removed / permanently out of scope (D-14)" subsection containing the item-6 row.
    - source: the three pinned substrings are present verbatim in brief.md; the pin test file is unchanged.
    - behavior: `python -m unittest tests.test_known_bad_corpus.TestKnownBadCorpus.test_brief_states_the_ratio_metric_dilution_entry_condition_as_a_falsifiable_blocker -v` passes.
    - source: each carried item's disposition cites the measured count/rate/split; item 4 is explicitly NOT auto-promoted.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_known_bad_corpus.TestKnownBadCorpus.test_brief_states_the_ratio_metric_dilution_entry_condition_as_a_falsifiable_blocker -v</automated>
  </verify>
  <done>All nine items are disposed (carry 8, remove 1) against measured evidence, item 6 is relocated with pinned substrings intact, and the pin test passes unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: File REV-002 (surviving SELF-001) and run the corpus-green closing gate</name>
  <files>.planning/REVERSALS.md</files>
  <read_first>
    - .planning/REVERSALS.md:12-56 — the reversal template, the four required fields, and the SELF-001 convention (New evidence must not restate the original reasoning).
    - .planning/REVERSALS.md:60-81 — REV-001, the honest-reversal exemplar; confirm REV-002 is the next free id.
    - .planning/phases/12-calibration/12-PATTERNS.md — the REV-002 constraint block (how to frame New evidence and What did not change).
    - .planning/phases/12-calibration/12-CONTEXT.md — D-16/D-17.
  </read_first>
  <action>
    Append REV-002 to .planning/REVERSALS.md's reversal log using the template. Reversed: item 6's deferral (deferred → permanently out of scope). New evidence: frame it as Phase 12's systematic REQ-P12-05 re-evaluation of all nine §6.5 rows against measured entry conditions, which surfaces — for item 6 specifically — that its entry condition requires a per-unit computation the gate path structurally cannot host; do NOT present the D-01/D-02 determinism doctrine as newly discovered (it pre-dates the deferral and the row already records it). What would have made the original deferral correct: a declaration-evaluable scalar multiplier (Formula (3) has none). What did not change: D-01/D-02 stand, the additive DSX-INT-030 case stays shipped, items 4 and 5 stay carried. Keep the general rule explicit: "unevaluable ⇒ remove" applies only to structural unreachability, never a merely-unmet condition. The SELF-001 survivability is a human-review bar at ship (no mechanical gate); make the New evidence field self-evidently non-laundering.

    Then run the corpus-green closing gate: the full unittest suite and the catalogue check. The phase ships only when both are green.
  </action>
  <acceptance_criteria>
    - source: .planning/REVERSALS.md contains a REV-002 record with all four template fields filled; New evidence names the systematic re-evaluation, not the determinism doctrine as novelty; What did not change lists D-01/D-02, DSX-INT-030, items 4 & 5.
    - source: REV-002 is the next id after REV-001 (no reuse).
    - behavior: `python -m unittest discover -s tests -p "test_*.py"` passes (full suite green).
    - behavior: `python scripts/gen-finding-catalogue.py --check` passes.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest discover -s tests -p "test_*.py" && python scripts/gen-finding-catalogue.py --check</automated>
  </verify>
  <done>REV-002 is filed as an honest reclassification that survives SELF-001, and the full suite plus catalogue check are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured numbers → backlog disposition | A disposition not grounded in the measured number is narrative judgement, the thing §6.5 exists to replace |
| reversal record → project decision log | A laundered reversal corrupts the discipline that makes "here is what would change it" honest |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06 | Repudiation | REV-002 laundering the determinism doctrine as new evidence | high | mitigate | SELF-001 human-review bar; New evidence framed as the systematic REQ-P12-05 re-evaluation, not the doctrine restated (.planning/REVERSALS.md:40-56; D-17) |
| T-12-13 | Tampering | item-6 row softened/deleted rather than relocated | high | mitigate | Pin test test_brief_states_the_ratio_metric_dilution_entry_condition_as_a_falsifiable_blocker keeps the three substrings present (tests/test_known_bad_corpus.py:1043-1069) |
| T-12-02 | Tampering | manufacturing a case to promote an item | high | mitigate | Dispositions cite measured counts; item 4 not auto-promoted (plan 12-02 excludes the polluted floor); measurement-decided items 7/8/9 promote only on the validated corpus (D-02/D-15) |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages (D-01); no install surface |
</threat_model>

<verification>
- `python -m unittest tests.test_known_bad_corpus.TestKnownBadCorpus.test_brief_states_the_ratio_metric_dilution_entry_condition_as_a_falsifiable_blocker -v` green (pin test unmodified).
- `python -m unittest discover -s tests -p "test_*.py"` green (full suite closing gate).
- `python scripts/gen-finding-catalogue.py --check` green (catalogue current, 256, unminted).
- The canonical aggregate `bash scripts/check.sh` may be run via Git Bash as the equivalent closing gate.
</verification>

<success_criteria>
Nine §6.5 items disposed against measured evidence (carry 8, remove 1), item 6 relocated with pinned substrings intact, REV-002 filed and surviving SELF-001, and the full suite plus catalogue check green.
</success_criteria>

<output>
Create `.planning/phases/12-calibration/12-07-SUMMARY.md` when done.
</output>
