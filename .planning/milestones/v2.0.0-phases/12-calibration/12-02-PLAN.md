---
phase: 12-calibration
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - dsx/cli.py
  - tests/test_cli_stats.py
autonomous: true
requirements: [REQ-P12-04]
must_haves:
  truths:
    - "`dsx stats --paradigm` exits 0 by construction on every input, including an empty or unreadable source (D-12)."
    - "The command reports a three-way frequentist/bayesian/undeclared split over the operator's own decision trails, deduplicated to distinct frame_digests (D-14)."
    - "The denominator for the split is the count of distinct operator frame_digests; the raw invocation count is a secondary diagnostic only (D-14)."
    - "The command never sources examples/**/DECISIONS.jsonl or templates/DECISIONS.jsonl (D-13)."
    - "With no operator trails present (the repo's current state), the command reports an honest empty history and never divides by zero (RESEARCH Pitfall 3)."
  artifacts:
    - "dsx/cli.py::cmd_stats (new reader command, modelled on cmd_explain)"
    - "dsx/cli.py p_stats subparser (sub.add_parser('stats', ...), --paradigm and --json flags, no --block-on)"
    - "tests/test_cli_stats.py (new test module: always-exit-0, negative-source assertion, synthetic-trail dedup guard)"
  key_links:
    - "cmd_stats aggregates across many DECISIONS.jsonl via Path(root).rglob('DECISIONS.jsonl') feeding the existing dsx.decisions.read_all() per file (multi-file divergence from cmd_explain's single-root read)."
    - "Paradigm read per distinct frame from choice='paradigm=…' (dsx/frame/paradigm.py:616,626); dedup key = frame_digest (dsx/decisions.py:241-250)."
  prohibitions:
    - statement: "`dsx stats --paradigm` never sources the known-bad fixture floor (examples/known-bad/DECISIONS.jsonl), examples/DECISIONS.jsonl, or templates/DECISIONS.jsonl — sourcing it would inflate the Bayesian share past 15% and falsely auto-promote §6.5 item 4 (D-13)."
      status: must-hold
      verification: "tests/test_cli_stats.py::test_never_sources_the_known_bad_floor: point the command at a root seeded with a huge known-bad-shaped trail and assert that trail's frames are excluded from the reported split."
    - statement: "The split is never computed over raw invocation records; re-running the same spec must collapse to one frame and cannot move the share (D-14)."
      status: must-hold
      verification: "tests/test_cli_stats.py::test_dedup_is_by_distinct_frame_digest: N distinct frequentist frames each repeated many times + 1 distinct Bayesian frame ⇒ reported Bayesian share is 1/(N+1), not the raw-record proportion."
    - statement: "cmd_stats is never registered as a check and never added to GATE_PROFILES; it carries no --block-on and is not a gate (D-12/D-18)."
      status: must-hold
      verification: "git diff shows no change to CHECKS (dsx/cli.py:64-85) or GATE_PROFILES (dsx/cli.py:115-131); `dsx stats --paradigm --block-on ...` exits 2 (argparse rejects the unknown flag)."
---

<objective>
Add a `dsx stats` subcommand with a `--paradigm` flag that reports the operator's own frequentist/Bayesian/undeclared frame split, deduplicated by frame_digest, as a pure reader that always exits 0 and never touches the block-contract machinery. This is the readout that the §6.5 item-4 Bayesian-admissibility entry condition is stated against — its correctness turns entirely on excluding the polluted fixture floor and deduplicating by distinct frame.

Purpose: Deliver REQ-P12-04's measured split so plan 12-07 can re-evaluate §6.5 item 4 against an honest number.
Output: `cmd_stats`, the `p_stats` subparser, and a test module proving the always-exit-0 contract, the negative-source exclusion (D-13), and the distinct-frame dedup (D-14).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-calibration/12-CONTEXT.md
@.planning/phases/12-calibration/12-RESEARCH.md
@.planning/phases/12-calibration/12-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- `dsx/cli.py::cmd_stats` — new reader command; multi-file rglob aggregation + frame_digest dedup + three-way split; new logic with no direct precedent (RESEARCH landmine 3).
- `dsx/cli.py` `p_stats = sub.add_parser("stats", ...)` with `--paradigm`, `--json`, optional `--root`; no `--block-on`.
- `tests/test_cli_stats.py::TestCmdStats` — new module with `test_always_exits_zero`, `test_never_sources_the_known_bad_floor`, `test_dedup_is_by_distinct_frame_digest`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The reader contract — always-exit-0, empty-source honesty, negative-source exclusion (RED→GREEN)</name>
  <files>dsx/cli.py, tests/test_cli_stats.py</files>
  <read_first>
    - dsx/cli.py:563-637 — cmd_explain, the "always return 0 by construction" reader template (outer try/except CheckError root fallback; inner try/except Exception around read-through-print; never imports Severity/GATE_THRESHOLDS/Report).
    - dsx/cli.py:826-854 — subparser registration shape (p_explain, add_common with include_block_on=False, set_defaults(func=...)).
    - dsx/cli.py:64-85, 115-131 — CHECKS registry (stats is a check LABEL at :67) and GATE_PROFILES (must NOT be edited).
    - dsx/decisions.py:182-257 — read_all (tolerant: [] on missing/unreadable, skips unparseable lines), decisions_path.
    - .planning/phases/12-calibration/12-PATTERNS.md — the cmd_explain analog and its MANDATORY DIVERGENCE note.
  </read_first>
  <behavior>
    - Test test_always_exits_zero: `cli.main(["stats", "--paradigm"])` and `cli.main(["stats", "--paradigm", "--json"])` both return 0 when the search root has no DECISIONS.jsonl; output names an empty operator history (no traceback, no division by zero).
    - Test test_always_exits_zero (unreadable case): a root containing an undecodable/half-written DECISIONS.jsonl still returns 0.
    - Test test_never_sources_the_known_bad_floor: with a root seeded to contain a known-bad-shaped trail file at an examples/known-bad-style path plus a templates-style path, the command excludes those trails; their frames never enter the split.
    - Test: `cli.main(["stats", "--paradigm", "--block-on", "high"])` exits 2 (argparse rejects the unknown flag — no --block-on on a command that always passes).
  </behavior>
  <action>
    Register `p_stats = sub.add_parser("stats", help="report the operator's own paradigm split")` near p_explain (dsx/cli.py:848). Add `--paradigm` (store_true), `--json` (store_true), and an optional `--root` defaulting to `.planning/` (the operator trail location, D-13; keep it configurable per RESEARCH Assumption A2 while defaulting to .planning/). Do NOT call add_common with block_on, and do NOT add `--block-on`. `p_stats.set_defaults(func=cmd_stats)`. Do not touch CHECKS or GATE_PROFILES.

    Write `cmd_stats(args)` modelled on cmd_explain's structural safety: outer root resolution with a defensive fallback, inner try/except Exception wrapping read-through-print so KeyboardInterrupt/SystemExit propagate but everything else degrades to an informative empty result, and `return 0` at the end unconditionally. Build a hard-exclude predicate that drops any discovered trail whose path is under an examples/ tree or a templates/ tree (match by path components, not a single hardcoded string literal, so both examples/DECISIONS.jsonl and examples/known-bad/DECISIONS.jsonl and templates/DECISIONS.jsonl are excluded) — this is D-13's negative-source boundary and has no analog in cmd_explain. Emit an honest "no operator history yet" message and exit 0 when zero trails survive the exclusion. `--json` prints json.dumps(..., indent=2, sort_keys=True). Never import Severity, GATE_THRESHOLDS, or Report.
  </action>
  <acceptance_criteria>
    - source: `sub.add_parser("stats"` exists in dsx/cli.py; `p_stats` has `--paradigm` and `--json` and no `--block-on`; git diff shows CHECKS (:64-85) and GATE_PROFILES (:115-131) unchanged.
    - source: cmd_stats never imports Severity/GATE_THRESHOLDS/Report; its final statement returns 0.
    - behavior: `python -m unittest tests.test_cli_stats.TestCmdStats.test_always_exits_zero -v` passes.
    - behavior: `python -m unittest tests.test_cli_stats.TestCmdStats.test_never_sources_the_known_bad_floor -v` passes.
    - CLI: `dsx stats --paradigm --block-on high` (or `python -m dsx ... `) exits 2.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_cli_stats.TestCmdStats.test_always_exits_zero tests.test_cli_stats.TestCmdStats.test_never_sources_the_known_bad_floor -v</automated>
  </verify>
  <done>The subcommand exists as a pure reader, exits 0 on empty/unreadable/excluded input, and provably never sources the fixture floor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-file aggregation with distinct-frame dedup (RED→GREEN)</name>
  <files>dsx/cli.py, tests/test_cli_stats.py</files>
  <read_first>
    - dsx/decisions.py:241-250 — frame_digest (the dedup key: sha256 over {validity_frame, inference}, key-order-invariant).
    - dsx/frame/paradigm.py:604-626 — the choice="paradigm=frequentist|bayesian|undeclared" record shape (three-way vocabulary; an out-of-vocabulary value folds to undeclared).
    - dsx/decisions.py:110-121, 182-215 — InvocationHeader/frame_digest grouping and read_all's invocation/decision record split.
    - .planning/phases/12-calibration/12-RESEARCH.md — Pitfall 3 (no .planning/ trail exists today; the synthetic-trail test is the only exerciser of populated aggregation) and the "Reading every invocation header" code example.
  </read_first>
  <behavior>
    - Test test_dedup_is_by_distinct_frame_digest: build a synthetic trail directory (no real .planning/ dependency) with N distinct frequentist frames, each repeated many times as invocation records, plus 1 distinct Bayesian frame. `dsx stats --paradigm --root <synthetic> --json` reports the Bayesian share as 1/(N+1) over distinct frames, and reports the raw invocation count only as a secondary diagnostic.
    - Test: an undeclared/out-of-vocabulary paradigm frame is reported in the undeclared bucket, not forced into a binary split.
    - Test: multiple DECISIONS.jsonl files scattered under the root are all discovered and aggregated before dedup.
  </behavior>
  <action>
    In cmd_stats, aggregate across trails: `Path(root).rglob("DECISIONS.jsonl")`, apply the D-13 exclusion predicate, and feed each surviving file through the existing dsx.decisions.read_all(). Collect distinct frames by frame_digest (D-14) so repeated invocations of the same frame collapse to one; read each distinct frame's paradigm from its choice="paradigm=…" decision record into one of three buckets frequentist/bayesian/undeclared. Report the split with denominator = count of distinct frame_digests; include the raw invocation count as a labelled secondary diagnostic only. Handle the zero-distinct-frames case as the honest empty result from Task 1 (never divide by zero). This aggregation is new logic — the single-root cmd_explain read does not generalize (RESEARCH landmine 3); reuse read_all/frame_digest, do not reimplement trail parsing.
  </action>
  <acceptance_criteria>
    - source: cmd_stats uses `rglob("DECISIONS.jsonl")` + `read_all(...)` per file and deduplicates by frame_digest before computing the split.
    - behavior: `python -m unittest tests.test_cli_stats.TestCmdStats.test_dedup_is_by_distinct_frame_digest -v` passes and asserts the share is 1/(N+1) over distinct frames.
    - source: the reported split has three buckets (frequentist/bayesian/undeclared); denominator is distinct frame_digests; raw invocation count is labelled secondary.
    - behavior: full module `python -m unittest tests.test_cli_stats -v` green.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_cli_stats -v</automated>
  </verify>
  <done>The split is computed over distinct frame_digests from many discovered trails, repeated runs cannot move it, and the empty-history case is honest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-disk DECISIONS.jsonl trails → reported split | Any trail the command reads becomes measured operator history |
| CLI flag surface → gate semantics | A block flag on an always-pass command would be a lie in help text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03 | Tampering | cmd_stats source selection | high | mitigate | D-13 hard-exclude of examples/** and templates/** trails, proven by test_never_sources_the_known_bad_floor (tests/test_cli_stats.py); exclusion matches path components, not one hardcoded literal |
| T-12-03b | Tampering | raw-record inflation | high | mitigate | Dedup by frame_digest (dsx/decisions.py:241-250) so repeated runs cannot inflate the share; test_dedup_is_by_distinct_frame_digest asserts 1/(N+1) |
| T-12-08 | Elevation of Privilege | cmd_stats masquerading as a gate | medium | mitigate | No --block-on, not in GATE_PROFILES; argparse rejects --block-on (exit 2); returns 0 by construction (dsx/cli.py cmd_explain template) |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages (D-01 hermeticity); no install surface |
</threat_model>

<verification>
- `python -m unittest tests.test_cli_stats -v` green (all three guards).
- `dsx stats --paradigm` in this repo reports an honest empty operator history and exits 0.
- git diff confirms CHECKS and GATE_PROFILES untouched.
</verification>

<success_criteria>
`dsx stats --paradigm` exits 0, reports a three-way distinct-frame split, excludes the fixture floor, handles the empty repo honestly, and mints no code and no gate registration.
</success_criteria>

<output>
Create `.planning/phases/12-calibration/12-02-SUMMARY.md` when done.
</output>
