---
phase: 12-calibration
plan: 04
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - examples/good-corpus/
  - tests/test_causal_verb_golden.py
autonomous: true
requirements: [REQ-P12-03]
must_haves:
  truths:
    - "A multi-spec good-side control corpus of at least ten clean ANALYSIS-SPECs exists, spanning paradigms and outcome shapes, so the false-positive rate is a rate with resolution rather than 0/1 (D-04)."
    - "Each control spec is a genuinely clean analysis (it declares a valid frame and should not block on any statistical-validity concern)."
    - "Every control spec has a matching `_GOLDEN_SHIP_FINDINGS` entry so the whole-examples golden test stays green (RESEARCH Pitfall 2)."
    - "The control corpus is glob-discovered by the same suffix convention as every other spec — no hardcoded list."
  artifacts:
    - "examples/good-corpus/<slug>-ANALYSIS-SPEC.yaml (≥10 clean specs)"
    - "Sibling artifacts each clean spec references (DATA-PROFILE, figure manifest, narrative, evidence) OR a documented decision to exclude the resulting tempdir-noise codes (recorded for plan 12-05, RESEARCH Pitfall 1)"
    - "New _GOLDEN_SHIP_FINDINGS entries in tests/test_causal_verb_golden.py (one per control spec)"
  key_links:
    - "The FPR harness (plan 12-05) globs this directory; the golden entries here must be measured with the golden test's fresh-tempdir _ship_findings helper."
    - "Each spec models examples/good-ANALYSIS-SPEC.yaml (the current n=1 baseline) for clean-spec shape."
  prohibitions:
    - statement: "The control corpus never inherits fresh-tempdir artifact-stripping noise into the FPR headline as if it were a real false positive; the noise is either seeded away or documented as excluded, decided deliberately and recorded (RESEARCH Pitfall 1, D-04)."
      status: must-hold
      verification: "For each control spec, record whether it needs sibling-artifact seeding or noise-code exclusion; plan 12-05 consumes this record. A spec that blocks at ship with a finding whose `where` names a file path (evidence/figure/profile/narrative) is measurement noise, not a false positive."
    - statement: "No control spec is a defective spec dressed as clean; each must declare a valid frame and pass dsx validate on its own merits (D-04)."
      status: must-hold
      verification: "`dsx validate --spec examples/good-corpus/<slug>-ANALYSIS-SPEC.yaml` reports no CRITICAL for each control spec."
    - statement: "No DSX-* code is minted; this plan is data plus golden-file bookkeeping only (D-18)."
      status: must-hold
      verification: "git diff touches only examples/good-corpus/ and tests/test_causal_verb_golden.py; no change to dsx/."
---

<objective>
Add a genuine multi-spec good-side control corpus so the false-positive rate has a real denominator (D-04). Today REQ-P12-03's FPR is defined over a single clean spec (n=1, no resolution); this plan delivers at least ten clean specs spanning paradigms and outcome shapes, and resolves the fresh-tempdir artifact-stripping trap so "there is a number" means an honest number.

Purpose: Supply the denominator the stratified rate/FPR harness (plan 12-05) consumes; without it the FPR is 0/1 or k/1 with no resolution.
Output: The good-control corpus directory, each spec's sibling-artifact/noise decision recorded for plan 12-05, and lockstep golden-file entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-calibration/12-CONTEXT.md
@.planning/phases/12-calibration/12-RESEARCH.md
@.planning/phases/12-calibration/12-PATTERNS.md
@examples/good-ANALYSIS-SPEC.yaml
</context>

<artifacts_this_phase_produces>
- `examples/good-corpus/<slug>-ANALYSIS-SPEC.yaml` — at least ten new clean control specs spanning frequentist/Bayesian paradigms and multiple outcome shapes.
- Each spec's referenced sibling artifacts, OR a recorded per-spec decision to exclude specific tempdir-noise codes (handed to plan 12-05).
- New `_GOLDEN_SHIP_FINDINGS` entries in `tests/test_causal_verb_golden.py` — one per control spec.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the clean control specs and resolve the fresh-tempdir noise trap</name>
  <files>examples/good-corpus/</files>
  <read_first>
    - examples/good-ANALYSIS-SPEC.yaml — the current n=1 clean baseline, for clean-spec shape and its sibling files (DATA-PROFILE, figure manifest, narrative, evidence).
    - .planning/phases/12-calibration/12-RESEARCH.md — Pitfall 1 (fresh-tempdir isolation fires DSX-DQ-001 CRITICAL + DSX-CLM-031/DSX-FIG-001/DSX-NAR-010 HIGH on a clean spec purely because sibling artifacts are absent) and the two mitigation routes (seed siblings via the _seed_entrypoint pattern, or document the noise codes like _INCIDENTAL_GAP_CODES).
    - .planning/phases/12-calibration/12-CONTEXT.md — D-04 (≈≥10 clean specs, paradigms + outcome shapes; FPR must be a rate with resolution).
  </read_first>
  <action>
    Create examples/good-corpus/ and author at least ten clean ANALYSIS-SPECs, each glob-discoverable via the standard `-ANALYSIS-SPEC.yaml` suffix. Span both paradigms (frequentist and Bayesian) and multiple outcome shapes (binary, continuous, count/ratio) so the FPR denominator has resolution. Model each on examples/good-ANALYSIS-SPEC.yaml — a valid validity_frame/inference declaration that should not block on any statistical-validity concern.

    For each spec, decide and RECORD (in the plan-12-04 SUMMARY, for plan 12-05 to consume) how the fresh-tempdir noise is handled: EITHER commit the sibling artifacts the spec references (DATA-PROFILE, figure manifest, narrative, evidence) so plan 12-05 can seed them into the tempdir (extending the _seed_entrypoint pattern), OR keep the spec deliberately minimal so it references no sibling artifacts and therefore fires none of the four noise codes. Prefer the minimal-reference route where it keeps the spec a valid clean analysis, because it removes the noise at the source rather than allowlisting it. Do not silently let DSX-DQ-001/DSX-CLM-031/DSX-FIG-001/DSX-NAR-010 enter the FPR headline as if they were real false positives.
  </action>
  <acceptance_criteria>
    - source: at least ten examples/good-corpus/<slug>-ANALYSIS-SPEC.yaml files exist, spanning ≥2 paradigms and ≥2 outcome shapes.
    - behavior: `dsx validate --spec examples/good-corpus/<slug>-ANALYSIS-SPEC.yaml` (for each) reports no CRITICAL finding on the spec's own merits.
    - source: the plan SUMMARY records, per spec, the sibling-seed-vs-minimal-reference decision for plan 12-05.
    - source: no spec references a sibling artifact it does not commit (minimal route) OR commits every sibling artifact it references (seed route).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import glob,sys; n=len(glob.glob('examples/good-corpus/*-ANALYSIS-SPEC.yaml')); print(str(n)+' good-corpus specs discovered'); sys.exit(0 if n>=10 else 1)"</automated>
  </verify>
  <done>At least ten clean control specs exist spanning paradigms and outcome shapes, each validating clean, with the tempdir-noise handling recorded per spec.</done>
</task>

<task type="auto">
  <name>Task 2: Land the golden-file entries for every control spec in lockstep</name>
  <files>tests/test_causal_verb_golden.py</files>
  <read_first>
    - tests/test_causal_verb_golden.py:58-142 — SPEC_GLOB="**/*-ANALYSIS-SPEC.yaml" over the whole examples/ tree, `_GOLDEN_SHIP_FINDINGS`, the `_ship_findings` fresh-tempdir helper, and `test_golden_keys_match_the_examples_tree_on_disk`.
    - .planning/phases/12-calibration/12-RESEARCH.md — Pitfall 2 (the recursive glob hard-fails the moment any new spec under examples/ lacks a golden entry).
  </read_first>
  <action>
    For every new examples/good-corpus/ spec, measure its ship-time CRITICAL/HIGH finding set with the golden test's own fresh-tempdir `_ship_findings` helper (the same isolation the test uses), and add one `_GOLDEN_SHIP_FINDINGS` entry per spec. Measure the set — never guess it. Because these are clean specs, the measured set will be empty only if the noise trap is fully resolved by the minimal-reference route; if the seed route is chosen, the measured set records whatever the isolated run produces, and plan 12-05 seeds siblings so the FPR count is honest. Keep this in the SAME change as the specs so the whole-examples golden test never goes red.
  </action>
  <acceptance_criteria>
    - source: `_GOLDEN_SHIP_FINDINGS` has one entry per new examples/good-corpus/ spec, each measured via `_ship_findings`.
    - behavior: `python -m unittest tests.test_causal_verb_golden -v` passes (test_golden_keys_match_the_examples_tree_on_disk green).
    - source: no golden entry is hand-guessed (the SUMMARY records the measured set per spec).
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_causal_verb_golden -v</automated>
  </verify>
  <done>Every control spec has a measured golden entry and the whole-examples golden test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| control spec + tempdir isolation → FPR count | Isolation noise can masquerade as a false positive |
| new spec under examples/ → whole-tree golden test | A missing golden entry reddens the suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-09 | Tampering | FPR denominator honesty | high | mitigate | Per-spec sibling-seed-or-minimal-reference decision recorded; tempdir-noise codes (DSX-DQ-001/DSX-CLM-031/DSX-FIG-001/DSX-NAR-010) never enter the FPR headline as real (RESEARCH Pitfall 1; consumed by plan 12-05) |
| T-12-01 | Tampering | golden entries | medium | mitigate | Every entry measured via the golden test's fresh-tempdir `_ship_findings`, never guessed (tests/test_causal_verb_golden.py) |
| T-12-10 | Tampering | control spec authenticity | medium | mitigate | Each control spec passes `dsx validate` with no CRITICAL on its own merits — a defective spec cannot pose as a clean control |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages (D-01); no install surface |
</threat_model>

<verification>
- `dsx validate` clean (no CRITICAL) for each control spec.
- `python -m unittest tests.test_causal_verb_golden -v` green.
- SUMMARY records the per-spec noise-handling decision for plan 12-05.
</verification>

<success_criteria>
At least ten clean control specs spanning paradigms and outcome shapes exist, the FPR denominator has resolution, tempdir noise is handled deliberately, and the golden test is green.
</success_criteria>

<output>
Create `.planning/phases/12-calibration/12-04-SUMMARY.md` when done.
</output>
