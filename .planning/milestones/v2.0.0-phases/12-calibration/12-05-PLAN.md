---
phase: 12-calibration
plan: 05
type: tdd
wave: 3
depends_on: [12-03, 12-04]
files_modified:
  - tests/test_known_bad_corpus.py
autonomous: true
requirements: [REQ-P12-03]
must_haves:
  truths:
    - "The harness reports catch rate stratified by target-code PRESENT vs ABSENT, each partition with its own denominator, per-case pass/block, attributable to specific codes (D-10)."
    - "The headline is the pair (miss-rate, FPR), not catch-rate alone; the ABSENT/miss partition is floored so a 100%-present corpus cannot pass as a calibration (D-10)."
    - "Adding an already-caught (target-present) case is mathematically incapable of moving the headline (D-10)."
    - "The false-positive rate is computed over the good-control corpus (plan 12-04) as a rate with resolution, with tempdir-noise handled per the recorded decision so no phantom finding inflates it (D-04, RESEARCH Pitfall 1)."
    - "Every reported number is computed live via _gate_findings, never lifted from _INCIDENTAL_GAP_CODES or _GOLDEN_SHIP_FINDINGS (D-09)."
  artifacts:
    - "tests/test_known_bad_corpus.py::TestKnownBadCorpus.test_stratified_catch_rate_and_fpr_report (D-10 stratified rate + D-04 FPR)"
    - "Sibling-artifact seeding or documented noise-exclusion for the good-control FPR run (RESEARCH Pitfall 1), following the decision recorded in plan 12-04"
    - "A headline-invariance assertion proving target-present additions cannot move (miss-rate, FPR)"
  key_links:
    - "Catch rate and FPR both consume self._gate_findings (tests/test_known_bad_corpus.py:555-616) and _classify_target_defect (:251-306) — the same live source as the golden test."
    - "The ABSENT/miss partition is populated from the live-confirmed attribution tags validated in plan 12-03."
  prohibitions:
    - statement: "The headline is never a single catch-rate number; it is always (miss-rate, FPR) with the ABSENT partition floored, so adding easy target-present catches cannot drive it to ~100% for free (D-10)."
      status: must-hold
      verification: "test_stratified_catch_rate_and_fpr_report includes a synthetic invariance check: injecting an additional target-present case leaves (miss-rate, FPR) unchanged."
    - statement: "No reported number is lifted from a stale ledger; catch rate, FPR and per-case pass/block are all computed from a fresh live _gate_findings run in the same test (D-09)."
      status: must-hold
      verification: "grep the new test body — it calls self._gate_findings / _classify_target_defect and does not read _INCIDENTAL_GAP_CODES or _GOLDEN_SHIP_FINDINGS as a source of the reported number."
    - statement: "Fresh-tempdir artifact-stripping noise (DSX-DQ-001/DSX-CLM-031/DSX-FIG-001/DSX-NAR-010) never counts as a real false positive; it is seeded away or excluded per the plan-12-04 record (RESEARCH Pitfall 1)."
      status: must-hold
      verification: "The FPR run seeds sibling artifacts or applies the documented exclusion set; a control spec whose only block names a file-path `where` is not counted in FPR."
---

<objective>
Build the stratified catch-rate and false-positive-rate report over the corpus and the good-control set: catch rate split by target-code PRESENT vs ABSENT with independent denominators, a floored ABSENT/miss partition, and an FPR with resolution — every number computed live. This is the measurement step that turns "reduced risk" from a feeling into a number, and the stratification is what stops the number being a regression-pin dressed as detection.

Purpose: Deliver REQ-P12-03's reproducible (miss-rate, FPR) headline that plan 12-07 re-evaluates §6.5 against.
Output: The stratified rate/FPR test, the good-control FPR run with tempdir-noise resolved, and the headline-invariance proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-calibration/12-CONTEXT.md
@.planning/phases/12-calibration/12-RESEARCH.md
@.planning/phases/12-calibration/12-PATTERNS.md
@.planning/phases/12-calibration/12-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `tests/test_known_bad_corpus.py::TestKnownBadCorpus.test_stratified_catch_rate_and_fpr_report` — the stratified catch rate (PRESENT/ABSENT partitions), the (miss-rate, FPR) headline, the floored ABSENT partition, and the headline-invariance check.
- Good-control FPR seeding/exclusion wiring following the plan-12-04 record.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FPR over the good-control corpus with tempdir noise resolved (RED→GREEN)</name>
  <files>tests/test_known_bad_corpus.py</files>
  <read_first>
    - .planning/phases/12-calibration/12-04-SUMMARY.md — the per-spec sibling-seed-vs-minimal-reference decision recorded by plan 12-04.
    - tests/test_known_bad_corpus.py:500-541, 555-616 — `_seed_entrypoint` (the seeding pattern to extend) and `_gate_findings`; tests/test_known_bad_corpus.py:64-88 for the `_INCIDENTAL_GAP_CODES` exclusion-with-reason shape.
    - .planning/phases/12-calibration/12-RESEARCH.md — Pitfall 1 (which four codes are tempdir noise and why) and the FPR-denominator discussion.
  </read_first>
  <behavior>
    - The FPR is the fraction of good-control specs that block at ship on a genuine statistical-validity finding, over the count of control specs (a rate with resolution, ≥10 denominator).
    - A block whose `where` names a sibling file path (evidence/figure/profile/narrative) is tempdir noise, seeded away or excluded per the plan-12-04 record, and is NOT counted as a false positive.
    - RED before wiring: a naive FPR that counts the four noise codes reports a spuriously high rate; GREEN after seeding/exclusion: the FPR reflects real false positives only.
  </behavior>
  <action>
    Compute the FPR live over examples/good-corpus/ by calling self._gate_findings(spec, "ship") for each control spec inside the same fresh-tempdir discipline. Apply the plan-12-04 decision: for seed-route specs, extend `_seed_entrypoint`'s pattern to copy the spec's referenced sibling artifacts (data profile, figure manifest, narrative, evidence targets) into the tempdir before the gate run; for minimal-reference specs, no seeding is needed. Where a residual noise code remains, exclude it via a documented allowlist mirroring `_INCIDENTAL_GAP_CODES` (each excluded code carrying an inline reason). The FPR numerator counts only control specs that block on a genuine statistical-validity finding; the denominator is the count of control specs. Report the FPR as a fraction with its numerator and denominator, computed live in the test — never lifted.
  </action>
  <acceptance_criteria>
    - source: the FPR run calls self._gate_findings over examples/good-corpus/ specs; the denominator is ≥10.
    - source: tempdir-noise codes are seeded away or excluded with an inline-reason allowlist; no file-path-`where` finding counts toward FPR.
    - behavior: the FPR test computes a rate with resolution (denominator ≥10) and passes.
    - source: no FPR number is read from _INCIDENTAL_GAP_CODES or _GOLDEN_SHIP_FINDINGS as a source of truth.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_known_bad_corpus.TestKnownBadCorpus.test_stratified_catch_rate_and_fpr_report -v</automated>
  </verify>
  <done>FPR is a live-computed rate over ≥10 clean control specs, with tempdir noise resolved so no phantom finding inflates it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stratified catch rate with floored ABSENT partition and headline invariance (RED→GREEN)</name>
  <files>tests/test_known_bad_corpus.py</files>
  <read_first>
    - tests/test_known_bad_corpus.py:172-306 — `_TARGET_DEFECT_CODES` and `_classify_target_defect` (per-(slug,point) present-code classification).
    - .planning/phases/12-calibration/12-CONTEXT.md — D-10 (stratify PRESENT vs ABSENT, headline = (miss-rate, FPR), floor the ABSENT/miss partition, adding easy catches cannot move the headline).
    - The plan-12-03 falsifiability test (the ABSENT/miss partition is populated from live-confirmed attribution tags).
  </read_first>
  <behavior>
    - Catch rate is reported in two partitions: PRESENT (fixtures carrying a firing target code — measured via _classify_target_defect) and ABSENT (miss cases whose absent_code fires nowhere, from the live-confirmed attribution tags), each with its own denominator and per-case pass/block attributable to specific codes.
    - Headline = (miss-rate, FPR). miss-rate is over the floored ABSENT partition; a corpus with a 100%-present composition cannot report a passing calibration because the ABSENT partition is floored (a minimum-size / minimum-representation requirement).
    - Invariance: injecting a synthetic additional target-PRESENT case into the aggregation leaves (miss-rate, FPR) unchanged — proving easy catches cannot move the headline.
  </behavior>
  <action>
    Extend test_stratified_catch_rate_and_fpr_report to compute the PRESENT-partition catch rate from live self._gate_findings + _classify_target_defect (per (slug, point), attributable to the firing code), and the ABSENT-partition miss-rate from the live-confirmed miss tags (a miss whose named absent_code fires nowhere is an uncaught defect). Report each partition with its own denominator and per-case pass/block. Set the headline to the pair (miss-rate over the ABSENT partition, FPR from Task 1), and FLOOR the ABSENT partition: assert a minimum ABSENT/miss representation so a 100%-present corpus fails to report a passing calibration. Add a synthetic invariance check: construct the aggregation with one extra fabricated target-PRESENT case and assert the (miss-rate, FPR) headline is byte-identical to the headline without it. Everything computed live; do not lift.
  </action>
  <acceptance_criteria>
    - source: catch rate is reported in PRESENT and ABSENT partitions, each with an independent denominator, per-case pass/block attributable to codes.
    - source: the headline is the tuple (miss-rate, FPR); the ABSENT partition is floored (an explicit minimum-representation assertion).
    - behavior: the synthetic invariance check shows adding a target-PRESENT case leaves (miss-rate, FPR) unchanged.
    - behavior: `python -m unittest tests.test_known_bad_corpus.TestKnownBadCorpus.test_stratified_catch_rate_and_fpr_report -v` passes and full `python -m unittest tests.test_known_bad_corpus -v` is green.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_known_bad_corpus -v</automated>
  </verify>
  <done>Catch rate is stratified with independent denominators, the headline is (miss-rate, FPR) with a floored ABSENT partition, and target-present additions provably cannot move it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus composition → headline number | A present-heavy corpus could inflate a single catch-rate to ~100% for free |
| stale ledgers → reported number | Lifting a stamped-date snapshot rots the moment a new check ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-11 | Tampering | headline gaming via easy catches | high | mitigate | Headline = (miss-rate, FPR) with floored ABSENT partition + synthetic invariance check (test_stratified_catch_rate_and_fpr_report, tests/test_known_bad_corpus.py) |
| T-12-01 | Tampering | lifted/stale number | high | mitigate | All numbers computed live via _gate_findings (:555-616) + _classify_target_defect (:251-306); never from _INCIDENTAL_GAP_CODES (:64-88) / _GOLDEN_SHIP_FINDINGS |
| T-12-09 | Tampering | FPR noise inflation | high | mitigate | Tempdir-noise codes seeded away or excluded per plan-12-04 record; file-path-`where` findings excluded from FPR (RESEARCH Pitfall 1) |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages (D-01); no install surface |
</threat_model>

<verification>
- `python -m unittest tests.test_known_bad_corpus -v` green.
- The (miss-rate, FPR) headline is reproducible across runs (live, fresh-tempdir).
- The invariance check demonstrably fails if the ABSENT partition floor is removed (edit-and-revert).
</verification>

<success_criteria>
A stratified, live-computed (miss-rate, FPR) headline exists with independent denominators, a floored ABSENT partition, an FPR over ≥10 clean control specs, and provable immunity to easy-catch inflation.
</success_criteria>

<output>
Create `.planning/phases/12-calibration/12-05-SUMMARY.md` when done.
</output>
