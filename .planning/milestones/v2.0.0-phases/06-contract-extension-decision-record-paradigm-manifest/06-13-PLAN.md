---
phase: 06-contract-extension-decision-record-paradigm-manifest
plan: 13
type: execute
wave: 7
depends_on: ["06-10"]
files_modified:
  - dsx/spec.py
  - scripts/gen-finding-catalogue.py
  - tests/test_gen_finding_catalogue.py
  - tests/test_frame_boundary.py
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "The D-05 enforcement boundary is boundary-safe: every entry in the prefix tuple is hyphen-terminated so it can only match a whole code family, and the individual `dsx/spec.py` codes that are enforced are named exactly rather than matched by a bare numeric-string prefix that admits unenumerated neighbours"
    - "A hypothetical longer numeric neighbour of an enforced code — a code sharing the enforced code's digits and continuing with more — is not silently pulled into enforcement, proven by a synthetic row rather than by inspection"
    - "The set of codes `check_d05` enforces on the real tree is unchanged by this plan: the same five `DSX-SPEC-08x` codes and the whole `DSX-PAR-` family are covered before and after, so `--check` still exits 0 and no previously-exempt legacy code becomes newly enforced"
    - "No comment in `dsx/spec.py` claims a level of `inference:` validation the code does not perform — the six-name constant is documented as a requirement-traceability manifest, and the comment states plainly that only three fields are vocabulary-checked, that exactly one removed field is rejected, and that an unknown key under `inference:` is accepted today"
    - "`tests/test_frame_boundary.py::_package_for` computes its result once rather than through two identical branches, and still returns the containing package for both an `__init__.py` and a plain module, proven by assertion on both real `dsx/frame/` sources"
    - "`references/finding-codes.md` is byte-for-byte unchanged — this plan changes which codes the enforcement covers by construction, not the rendered catalogue"
  artifacts:
    - path: "scripts/gen-finding-catalogue.py"
      provides: "a hyphen-terminated family prefix tuple plus an explicit enforced-code set, and the widened membership test in check_d05"
      contains: "_D05_ALLOWLIST_CODES"
    - path: "tests/test_gen_finding_catalogue.py"
      provides: "the boundary-safety property test and the longer-numeric-neighbour test"
    - path: "dsx/spec.py"
      provides: "a corrected comment above the inference field-name manifest"
      contains: "_INFERENCE_FIELDS"
    - path: "tests/test_frame_boundary.py"
      provides: "a single-expression `_package_for` with the two-case reasoning kept as a comment"
  key_links:
    - from: "scripts/gen-finding-catalogue.py::check_d05"
      to: "_D05_ALLOWLIST_PREFIXES and _D05_ALLOWLIST_CODES"
      via: "family-prefix match or exact-code membership, replacing a single startswith over a mixed-shape tuple"
      pattern: "_D05_ALLOWLIST_CODES"
    - from: "tests/test_gen_finding_catalogue.py"
      to: "scripts/gen-finding-catalogue.py::check_d05"
      via: "synthetic rows proving the boundary holds in both directions"
      pattern: "check_d05"
  prohibitions:
    - statement: "No new finding code is minted — D-06 makes numbering irreversible, so the WR-01 remedy of adding an unknown-key check under `inference:` is explicitly not taken here"
      status: enforced
      verification: diff-assertion
    - statement: "`_INFERENCE_FIELDS` is not removed or reordered; `tests/test_dsx.py::test_inference_fields_constant_matches_req_p6_04` stays green and `tests/test_dsx.py` is not edited by this plan — that file is owned by plan 06-11 in this wave"
      status: enforced
      verification: diff-assertion
    - statement: "The D-05 exemption boundary stays finite and visible per D-20 — no pattern is introduced that admits codes nobody enumerated"
      status: enforced
      verification: test
    - statement: "No previously-exempt legacy code becomes newly enforced and no previously-enforced code becomes exempt"
      status: enforced
      verification: cli-assertion
    - statement: "`references/finding-codes.md` is not regenerated or edited"
      status: enforced
      verification: diff-assertion
    - statement: "No behaviour in `dsx/spec.py` changes — the WR-01 remedy is comment text only"
      status: enforced
      verification: diff-assertion
    - statement: "No third-party dependency is introduced; the script and tests stay stdlib only"
      status: enforced
      verification: source-assertion
    - statement: "No existing 06-01..06-10 plan, summary or committed artifact is renumbered, rewritten or deleted by this plan"
      status: enforced
      verification: diff-assertion
---

<objective>
Close the three carried-forward maintainability findings from `06-REVIEW.md` that concern the
mechanical-enforcement surface and the code that documents it: a D-05 allow-list entry that is not
boundary-safe, a comment that overstates what `inference:` validation does, and a dead branch in a
boundary-test helper (WR-01, WR-03, IN-01; D-20, D-06).

Purpose: D-20's whole justification for an allow-list is that the exemption boundary is "finite and
visible". A bare numeric-string prefix is neither — it silently admits every code sharing its
digits, and its shape differs from the hyphen-terminated entry beside it in the same tuple, which
is exactly the asymmetry a future contributor copies forward incorrectly. The `dsx/spec.py` comment
is the same species of defect as the known-bad corpus over-claim closed by plan 06-12: committed
text asserting a level of enforcement the code does not perform. The dead branch is cosmetic, but
it invites a future edit to "fix" one arm under the belief the two paths differ.

Output: a boundary-safe enforcement set with a property test, a comment that matches the code, and
a helper that computes its result once.
</objective>

<traceability>
`requirements:` is deliberately empty. This plan hardens an artifact of **REQ-P6-11**'s delivery —
the D-05 mechanical-enforcement surface in `scripts/gen-finding-catalogue.py` — without re-claiming
that delivery. REQ-P6-11 is owned by the sealed `06-03-PLAN.md`, which remains its single source
plan in the requirements-coverage table; declaring it here again would read as a second delivery
claim for one requirement. The two other findings closed here, WR-01 in `dsx/spec.py` and IN-01 in
`tests/test_frame_boundary.py`, are maintainability corrections that carry no requirement at all.
The relationship is recorded in these sentences rather than in the machine-checked field.
</traceability>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-CONTEXT.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-VERIFICATION.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-REVIEW.md
@.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Every symbol below is CREATED by this plan. Drift and source-grounding passes must treat these as
new, not as pre-existing references.

**New module-level constant in `scripts/gen-finding-catalogue.py`**
- `_D05_ALLOWLIST_CODES` — the exact set of individually-enforced finding codes

**New test methods in `tests/test_gen_finding_catalogue.py` (class `TestD05Core`)**
- `test_every_d05_allowlist_prefix_ends_in_a_hyphen`
- `test_check_d05_does_not_cover_a_longer_numeric_neighbour_of_an_enforced_code`
- `test_d05_covered_code_set_on_the_real_tree_is_exactly_the_documented_set`

**Modified existing symbols (NOT new — these already exist and are changed in place)**
- `scripts/gen-finding-catalogue.py::_D05_ALLOWLIST_PREFIXES` — narrowed to hyphen-terminated
  family prefixes only
- `scripts/gen-finding-catalogue.py::check_d05` — the coverage membership test
- `dsx/spec.py` — the comment block above `_INFERENCE_FIELDS`; the constant itself is unchanged
- `tests/test_frame_boundary.py::_package_for` — the dead branch collapsed

**No new finding codes, no new CLI surface, no new modules, no new dependencies.**
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the D-05 enforcement boundary boundary-safe (WR-03, D-20)</name>
  <files>scripts/gen-finding-catalogue.py, tests/test_gen_finding_catalogue.py</files>
  <read_first>
    - `scripts/gen-finding-catalogue.py` lines 40-60 — the prefix-group table above the constant,
      the D-20 comment at lines 47-50, and `_D05_ALLOWLIST_PREFIXES` at line 51
    - `scripts/gen-finding-catalogue.py::check_d05` (from line 233) — its docstring's statement that
      only matching codes are checked so the 206 pre-existing legacy codes produce zero new
      failures, and the `covered = [...]` comprehension at line 242 that is the sole consumer
    - `scripts/gen-finding-catalogue.py`'s `collect()` — the shape of the rows `check_d05` receives,
      so the synthetic rows the new tests build match it exactly
    - `tests/test_gen_finding_catalogue.py` in full — in particular `TestD05Core`'s
      `test_check_d05_ignores_uncovered_prefix` (line 44), which already establishes the
      synthetic-row idiom, and `TestD05RealTreeStandingGuarantee::test_real_tree_check_d05_is_empty`
      (line 260), which must stay green
    - `.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-REVIEW.md` WR-03
      — the over-match mechanism and both candidate remedies
    - `.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-CONTEXT.md`
      decision D-20 — "the exemption is finite, visible and shrinks as codes get cited", the
      property this task makes structurally true
  </read_first>
  <behavior>
    - every entry in the family-prefix tuple ends in a hyphen
    - a synthetic row whose code is an enforced code with additional digits appended is not covered
      by `check_d05`, and therefore produces no problem even when its docstring carries neither a
      citation nor a reference value
    - a synthetic row whose code is an enforced code exactly, with a non-compliant docstring, still
      produces a problem
    - the set of codes `check_d05` covers on the real tree is exactly the same set before and after
      this change
    - `--check` on the real tree still exits 0
  </behavior>
  <action>
    Measure first: enumerate the codes `collect()` returns on the real tree that the current
    `_D05_ALLOWLIST_PREFIXES` covers. That measured set is the authority for the constants below —
    the review names five `DSX-SPEC-08x` codes and the whole `DSX-PAR-` family, but the tree is the
    source of truth, not the review's prose.

    Narrow `_D05_ALLOWLIST_PREFIXES` to hold only hyphen-terminated family prefixes — the
    `DSX-PAR-` entry alone today — and extend the D-20 comment above it with an explicit rule: every
    entry in this tuple must end in a hyphen, so it can only ever match a whole family and never
    part of a numeric suffix; individual codes go in the exact-code set instead.

    Add `_D05_ALLOWLIST_CODES` immediately below it as a `frozenset` naming, exactly, each
    individually-enforced code measured above. Comment it as the individually-enumerated half of
    D-20's finite, visible boundary: these are the codes this milestone introduced inside a
    pre-existing family, so they cannot be covered by a family prefix without dragging that whole
    legacy family into enforcement.

    Change `check_d05`'s coverage comprehension to include a row when its code either starts with a
    member of the family-prefix tuple or is a member of the exact-code set. Update the function
    docstring's first line to describe both halves rather than only the prefix half.

    Add the three test methods named in this plan's artifacts section to `TestD05Core`.
    `test_every_d05_allowlist_prefix_ends_in_a_hyphen` asserts the property on every tuple member,
    so a future contributor adding a sub-family prefix fails immediately rather than silently
    over-matching. `test_check_d05_does_not_cover_a_longer_numeric_neighbour_of_an_enforced_code`
    builds two synthetic rows against a temporary code root: one whose code is an enforced code with
    an extra digit appended and whose docstring carries no citation and no reference value, asserted
    to produce no problem, and one whose code is that enforced code exactly with the same
    non-compliant docstring, asserted to produce at least one problem. Both assertions are needed —
    the first alone would also pass if coverage were accidentally empty.
    `test_d05_covered_code_set_on_the_real_tree_is_exactly_the_documented_set` collects the real
    tree and asserts the covered code set equals the union of the family-matched codes and the
    exact-code set, which is the regression guard against the boundary drifting in either direction.

    Follow the existing synthetic-row and temporary-directory idioms in `TestD05Core` rather than
    introducing a new fixture pattern; `tests/fixtures/d05/bad_check.py` stays untouched.

    Do not regenerate `references/finding-codes.md`. This change alters which codes are subject to
    D-05 enforcement by construction, not the rendered catalogue rows.

    Commit as `fix(06-13): make the D-05 enforcement boundary hyphen- and code-exact`.
  </action>
  <acceptance_criteria>
    - `python3 -c "import importlib.util,pathlib; s=importlib.util.spec_from_file_location('g', pathlib.Path('scripts/gen-finding-catalogue.py')); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert all(p.endswith('-') for p in m._D05_ALLOWLIST_PREFIXES), m._D05_ALLOWLIST_PREFIXES; assert isinstance(m._D05_ALLOWLIST_CODES, frozenset) and len(m._D05_ALLOWLIST_CODES)>=5, m._D05_ALLOWLIST_CODES"` exits 0
    - `python3 -c "import importlib.util,pathlib; s=importlib.util.spec_from_file_location('g', pathlib.Path('scripts/gen-finding-catalogue.py')); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert all(c.startswith('DSX-SPEC-08') for c in m._D05_ALLOWLIST_CODES), m._D05_ALLOWLIST_CODES"` exits 0
    - `python3 scripts/gen-finding-catalogue.py --check` exits 0
    - `python3 -m unittest tests.test_gen_finding_catalogue -v` exits 0 and reports at least 20 tests
    - `python3 -m unittest tests.test_gen_finding_catalogue.TestD05RealTreeStandingGuarantee` exits 0
    - `python3 -m unittest tests.test_gen_finding_catalogue.TestD05EnforcementFixture` exits 0
    - `git diff --stat references/finding-codes.md` reports no changes
    - `git diff --stat dsx/` reports no changes in this task's commit
    - `python3 -m unittest discover -s tests` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest tests.test_gen_finding_catalogue -v &amp;&amp; python3 scripts/gen-finding-catalogue.py --check &amp;&amp; git diff --quiet references/finding-codes.md</automated>
  </verify>
  <done>Family prefixes are hyphen-terminated by rule and by test, individual enforced codes are enumerated exactly, a longer numeric neighbour is proven not to be swept in, the real-tree covered set is unchanged, and the catalogue is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the inference-validation comment to match what the code does (WR-01)</name>
  <files>dsx/spec.py</files>
  <read_first>
    - `dsx/spec.py` lines 826-843 — the comment block above `_INFERENCE_FIELDS`, the constant
      itself, `_INFERENCE_MEMBERSHIP` and `_INFERENCE_REMOVED_FIELD`
    - `dsx/spec.py::_validate_inference_shape` (from line 846) — confirm for yourself which of the
      six field names it actually consults; the comment must describe this function, not the
      requirement
    - `tests/test_dsx.py::test_inference_fields_constant_matches_req_p6_04` — read it only; this
      plan does not edit `tests/test_dsx.py`, which plan 06-11 owns in this wave. The corrected
      comment must leave that test green and must describe the constant's real job, which is
      pinning REQ-P6-04's field list against drift
    - `.planning/REQUIREMENTS.md` REQ-P6-04 — the six field names the constant traces to
    - `.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-REVIEW.md` WR-01
      — the two remedies offered, one of which this task declines
  </read_first>
  <action>
    Replace the comment block above `_INFERENCE_FIELDS` with text that is true of the code as
    written. It must say four things. First, that the tuple is the machine-readable manifest of
    REQ-P6-04's six field names, whose one consumer is the drift-guard test in `tests/test_dsx.py`
    — not the catalogue, which does not read it. Second, that the tuple is not an enforced closed
    set: `_validate_inference_shape` vocabulary-checks only the three fields listed in
    `_INFERENCE_MEMBERSHIP` and rejects exactly one non-member, the field named by
    `_INFERENCE_REMOVED_FIELD`. Third, that an unrecognised or misspelled key under `inference:` is
    accepted silently today — there is no unknown-key check for this block. Fourth, that M-02
    removed the stopping-rule field and the concept it named lives in `design.peeking_policy`,
    which the existing comment already states correctly and which stays.

    Then correct the comment inside `_validate_inference_shape` that describes its own field-name
    checking, if that comment overstates the same thing, so the function and its constant tell one
    story.

    This task declines the review's alternative remedy of wiring the constant into a real
    unknown-key check. That would mint a new finding code, and D-06 makes finding-code numbering
    irreversible; a code minted inside a gap-closure plan, after this phase's catalogue was
    regenerated and its version bumped to 2.0.0, would be a contract addition smuggled in as a
    cleanup. It also declines removing the constant, because the drift guard it feeds is genuinely
    useful — it is the only mechanical link between REQ-P6-04's declared field list and the source,
    and it is what would catch a silent reintroduction of the field M-02 removed. The defect was
    always the comment, and the comment is what changes.

    Change no executable line. No behaviour, no signature, no constant value, no finding code.

    Commit as `docs(06-13): state what inference-block validation actually enforces`.
  </action>
  <acceptance_criteria>
    - `python3 -c "import dsx.spec as s; assert s._INFERENCE_FIELDS == ('paradigm','paradigm_justification','declared_at','primary_procedure','alpha_spending','fallback_rule'), s._INFERENCE_FIELDS"` exits 0
    - `python3 -c "import dsx.spec as s; assert [n for n,_ in s._INFERENCE_MEMBERSHIP]==['paradigm','paradigm_justification','declared_at']; assert s._INFERENCE_REMOVED_FIELD=='stopping_rule'"` exits 0
    - `python3 -c "import pathlib; t=pathlib.Path('dsx/spec.py').read_text(encoding='utf-8'); assert 'not an enforced closed set' in t, 'the corrected comment must say so in these words'"` exits 0
    - `python3 -c "import pathlib; t=pathlib.Path('dsx/spec.py').read_text(encoding='utf-8').lower(); assert 'design.peeking_policy' in t"` exits 0
    - `git diff -U0 dsx/spec.py` shows changed lines only inside comment lines — every added and removed line begins with optional whitespace followed by a hash character
    - `python3 -m unittest tests.test_dsx.TestSpecStructure -v` exits 0
    - `python3 -m unittest discover -s tests` exits 0
    - `python3 scripts/gen-finding-catalogue.py --check` exits 0
    - `git diff --stat tests/test_dsx.py references/finding-codes.md` reports no changes
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s tests &amp;&amp; python3 scripts/gen-finding-catalogue.py --check &amp;&amp; git diff --quiet tests/test_dsx.py references/finding-codes.md</automated>
  </verify>
  <done>The comment above the inference field-name manifest describes the three vocabulary-checked fields, the one rejected removed field and the silently-accepted unknown key; no executable line changed; no finding code was minted.</done>
</task>

<task type="auto">
  <name>Task 3: Collapse the dead branch in the frame-boundary test helper (IN-01)</name>
  <files>tests/test_frame_boundary.py</files>
  <read_first>
    - `tests/test_frame_boundary.py` lines 30-56 — `_package_for`, its docstring explaining the two
      cases, and the `if`/`else` at lines 47-52 whose arms are identical
    - `tests/test_frame_boundary.py::_scan_source_for_checks_imports` and
      `TestFrameImportBoundary` — the two callers, so the collapse is verified against actual usage
      rather than in isolation
    - `dsx/frame/__init__.py` and `dsx/frame/paradigm.py` — the two real inputs the helper resolves,
      one an `__init__.py` and one a plain module, which is the pair the acceptance criteria assert
      on
    - `.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-REVIEW.md` IN-01
  </read_first>
  <action>
    Replace the two-armed branch with the single assignment both arms already perform: drop the last
    path segment unconditionally. Keep the docstring exactly as written — it correctly narrates the
    two cases and remains accurate. Add one comment line at the assignment stating that dropping the
    last segment is correct for both inputs by construction: an `__init__.py`'s package is its own
    directory, which is the path minus the `__init__` segment, and a plain module's package is its
    containing directory, which is the path minus the module segment. Same operation, two
    derivations, one line.

    Verify against both real inputs rather than only re-running the suite: `dsx/frame/__init__.py`
    and `dsx/frame/paradigm.py` must both resolve to the same dotted package.

    Change nothing else in the file. The two `TestFrameImportBoundary` tests — the one proving the
    scanner fires on a deliberately violating source and the one confirming the real `dsx/frame/`
    modules import nothing from the checks package — are REQ-P6-10's mechanical D-03a proof and stay
    byte-for-byte unedited.

    Commit as `refactor(06-13): collapse the identical branches in _package_for`.
  </action>
  <acceptance_criteria>
    - `python3 -c "import sys; sys.path.insert(0,'.'); import tests.test_frame_boundary as t, dsx.frame; from pathlib import Path; d=Path(dsx.frame.__file__).resolve().parent; assert t._package_for(d/'__init__.py')=='dsx.frame'; assert t._package_for(d/'paradigm.py')=='dsx.frame'"` exits 0
    - `python3 -c "import sys; sys.path.insert(0,'.'); import inspect, tests.test_frame_boundary as t; s=inspect.getsource(t._package_for); assert s.count('parts[:-1]')==1, s"` exits 0
    - `python3 -m unittest tests.test_frame_boundary -v` exits 0 and reports 2 tests passing
    - `git diff -U0 tests/test_frame_boundary.py` shows changes only inside `_package_for` — no line inside `TestFrameImportBoundary` or `_scan_source_for_checks_imports` is added, removed or altered
    - `python3 -m unittest discover -s tests` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest tests.test_frame_boundary -v &amp;&amp; python3 -m unittest discover -s tests</automated>
  </verify>
  <done>`_package_for` computes its result in one expression, both real `dsx/frame/` inputs still resolve to the same package, and the two D-03a boundary tests are unedited and green.</done>
</task>

</tasks>

<deferred_with_rationale>
One of the four carried-forward `06-REVIEW.md` findings is **not** closed by this plan set, and is
deferred deliberately rather than dropped:

- **WR-02 — non-atomic `next_invocation_id()` + `append()`.** The review's first remedy (document
  the limitation) IS taken, in plan 06-11 Task 3, at the function, module and README level. Its
  second remedy (an OS-level advisory lock around the read-count-then-append sequence) is deferred.
  Reasoning: it requires a platform-guarded `fcntl`/`msvcrt` split, a lock-file lifecycle including
  stale-lock recovery, and a concurrency test that cannot be made deterministic inside a
  single-process `unittest` suite on Windows. That is new engineering against a mode this phase's
  suite does not exercise, and the verifier explicitly classified it as a documented limitation
  rather than a regression from a prior working state. 06-CONTEXT.md D-19 already recorded the same
  boundary from the writer's side. Deferred to whichever phase first needs concurrent gate
  invocations against one root; Phase 10 is the first plausible candidate, being the first to read
  recorded invocation history as an input.

The other three — WR-01, WR-03 and IN-01 — are all closed here. No carried-forward finding is
silently dropped.
</deferred_with_rationale>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finding-code strings -> `check_d05` coverage decision | The string match that decides whether a code is subject to D-05 citation and reference-value enforcement; an over-matching pattern silently changes that decision for codes nobody enumerated |
| committed comments in `dsx/spec.py` -> a future contributor | Text asserting a level of validation that the code does not perform |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6-29 | Tampering | `check_d05`'s coverage boundary | medium | mitigate | A bare numeric-string prefix matches any code sharing its digits, so the enforced set can change without an intentional edit, and the mixed-shape tuple invites a future contributor to add a sub-family prefix that over-matches. Mitigated by narrowing the tuple to hyphen-terminated family prefixes with that rule asserted as a property test, enumerating individual enforced codes exactly, and pinning the real-tree covered set with a regression test. D-20's "finite and visible" becomes structural rather than aspirational. |
| T-6-30 | Repudiation | committed comments in `dsx/spec.py` | low | mitigate | A comment claiming six field names form an enforced closed set, when three are vocabulary-checked and one non-member is rejected, misleads the next contributor about what a spec is protected against. Mitigated by rewriting the comment to state exactly what runs, including that an unknown key under `inference:` is accepted today. |
| T-6-31 | Tampering | minting a finding code inside a cleanup | medium | mitigate | The alternative WR-01 remedy would add an unknown-key check and therefore a new finding code; D-06 makes numbering irreversible and this phase's catalogue is already regenerated and version-bumped. Mitigated by declining that remedy explicitly and asserting no catalogue change. |
| T-6-32 | Denial of Service | the collapsed test helper | low | accept | A wrong collapse would break the D-03a boundary proof rather than degrade it. Accepted because the change is verified against both real `dsx/frame/` inputs and the two `TestFrameImportBoundary` tests stay unedited and must remain green. |
| T-6-SC | Tampering | npm/pip/cargo installs | low | accept | Zero third-party installs ship in this plan (D-01, stdlib only); `06-RESEARCH.md`'s Package Legitimacy Audit records the gate as not applicable for this phase, and no install task exists here. |
</threat_model>

<verification>
- `python3 -m unittest discover -s tests` exits 0
- `python3 -m unittest tests.test_gen_finding_catalogue -v` exits 0 and reports at least 20 tests
- `python3 -m unittest tests.test_frame_boundary -v` exits 0 and reports 2 tests
- `python3 scripts/gen-finding-catalogue.py --check` exits 0
- Every member of the D-05 family-prefix tuple ends in a hyphen
- The set of codes `check_d05` covers on the real tree is unchanged from before this plan
- `git diff --stat references/finding-codes.md tests/test_dsx.py` reports no changes
- `git diff -U0 dsx/spec.py` shows only comment lines changed
- `git diff --stat dsx/checks/ dsx/frame/ dsx/cli.py dsx/decisions.py` reports no changes
</verification>

<success_criteria>
- REQ-P6-11: D-05 remains mechanically enforced, and its exemption boundary is now finite and
  visible by construction rather than by a prefix that happens to match the right set today
- WR-03 closed: hyphen-termination is a tested property, individually-enforced codes are
  enumerated, and a longer numeric neighbour is proven not to be swept in
- WR-01 closed: no comment in `dsx/spec.py` overstates what `inference:` validation performs, and
  no finding code was minted to close it
- IN-01 closed: `_package_for` computes its result once, with the two-case reasoning kept as
  documentation rather than as a dead branch
- WR-02 explicitly deferred with recorded reasoning; its documentation half lands in plan 06-11
</success_criteria>

<output>
Create `.planning/phases/06-contract-extension-decision-record-paradigm-manifest/06-13-SUMMARY.md` when done.
Record the measured before-and-after D-05 covered code set from Task 1 in the SUMMARY — it is the evidence the boundary change was set-preserving.
</output>