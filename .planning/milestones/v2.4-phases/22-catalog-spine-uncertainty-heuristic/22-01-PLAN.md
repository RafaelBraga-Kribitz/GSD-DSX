---
phase: 22-catalog-spine-uncertainty-heuristic
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_viz_vocabulary_invariant.py
  - dsx/checks/viz.py
  - dsx/spec.py
  - dsx/checks/smells.py
  - dsx/data/input_types.json
autonomous: true
requirements:
  - REQ-P22-02
  - REQ-P22-03

must_haves:
  truths:
    - "RELATIONSHIP_CHARTS has an 11th key 'uncertainty' whose tuple is exactly the ten Wilke §5.6 marks (D-2), and _check_relationship_match admits/rejects those marks for a declared 'uncertainty' relationship with no change to its own body."
    - "Every one of the ten uncertainty marks has a capability home (CHART_CAPABILITIES['interval-range']), so tests/test_viz_vocabulary_invariant.py::test_every_mark_has_a_capability_home stays green (Pitfall 1)."
    - "The input-type-id gate path is refreshed: dsx/data/input_types.json is regenerated so the interval-range ids (IT014, IT036, IT040) admit the ten uncertainty marks."
    - "BANNED_TYPES has seven complete {reason, code=DSX-VIZ-001, citation} refusal records — the five inherited plus gauge and word_cloud — and radar's citation is Duan et al. 2023 (J Clin Epidemiol 156:85-94), not the PROVISIONAL placeholder (D-4 sibling, HQ-27 Tier-3)."
    - "facet_by is an orthogonal declaration, not a chart type: it appears in no RELATIONSHIP_CHARTS / CHART_CAPABILITIES / BANNED_TYPES value, and an existing smell remedy (DSX-SMELL-007) names facet_by as a route (REQ-P22-03, no new code)."
    - "The finding-code catalogue is unchanged at 275 codes: this plan edits vocabulary dicts and one remedy string only, minting zero codes."
  artifacts:
    - "dsx/checks/viz.py (RELATIONSHIP_CHARTS gains 'uncertainty'; BANNED_TYPES gains gauge + word_cloud and radar's citation is swapped)"
    - "dsx/spec.py (CHART_CAPABILITIES['interval-range'] gains the ten uncertainty marks)"
    - "dsx/checks/smells.py (DSX-SMELL-007 remedy string routes to facet_by; no new code)"
    - "dsx/data/input_types.json (regenerated: IT014/IT036/IT040 admit the ten marks)"
    - "tests/test_viz_vocabulary_invariant.py (extended: uncertainty-key + capability-home + refusal-completeness-of-7 + facet-orthogonality + smell-routing assertions)"
  key_links:
    - "CHART_CAPABILITIES['interval-range'] edit -> python scripts/gen-input-types.py -> dsx/data/input_types.json -> the input-type-id path of _check_input_type_matrix (Pitfall 1: editing the dict without regenerating leaves the id path stale)."
    - "RELATIONSHIP_CHARTS['uncertainty'] tuple is the single source of truth for the ten marks — Plan 22-02's DSX-VIZ-071 check reads set(RELATIONSHIP_CHARTS['uncertainty']); do NOT introduce a parallel UNCERTAINTY_MARKS dict (Research Pattern 1 / Anti-Pattern)."
  prohibitions:
    - "[unverified — never auto-dismiss] No gauge/word_cloud/radar citation may be drawn from outside the signed HQ-27 pack, and none from its 8 still-unverified items; the gauge reason must state Few's actual grounds (wasted space, missing context, unlabeled scale) and mark the arbitrary-maximum criticism as DSX's own reasoning, not Few's (HQ-27 T3-GAUGE)."
    - "[unverified — never auto-dismiss] No mark may be added to any vocabulary surface without a capability home (D-01 invariant); the ten uncertainty marks MUST land in an EXISTING family (interval-range), never a new input-type id (GA-2 rejects new ids)."
    - "[unverified — never auto-dismiss] Gate path stays stdlib-only: no pandas/scipy/numpy import enters dsx/checks/viz.py, dsx/spec.py, or dsx/checks/smells.py (D-01/D-02)."
    - "[unverified — never auto-dismiss] The two SCOPE-doc uncertainty names D-2 rejects (which do not exist in Wilke) must not appear as marks: only the frozen ten §5.6 ids ship."
---

<objective>
Add the uncertainty function family to the live chart vocabulary as an 11th
RELATIONSHIP_CHARTS key carrying Wilke's ten §5.6 marks (D-2), give those ten marks
a capability home so Phase 21's every-mark-has-a-home invariant stays green, complete
the refusal doctrine by adding gauge and word_cloud to BANNED_TYPES and swapping
radar's PROVISIONAL citation for the signed Duan et al. 2023 source, and land faceting
as an orthogonal facet_by declaration whose remedy routes to an existing smell — all
minting zero finding codes.

This is a code-and-test extension of in-repo Python vocabularies. It produces no
number, model, or chart and installs no packages. It implements REQ-P22-02 (uncertainty
family enters the vocabulary, GA-2's 11th-key shape, D-12a-clean by construction) and
REQ-P22-03 (facet_by orthogonal declaration, smells route to it). The gate code that
validates a declared uncertainty mark (DSX-VIZ-071) is minted separately in Plan 22-02,
which reads the tuple this plan establishes.

Purpose: this plan is the vocabulary spine the whole phase stands on. The catalog
(Plan 22-03) enumerates the ten new marks and the two new refusal rows; the gate mint
(Plan 22-02) validates against the tuple defined here. Homing the ten marks into an
existing family — not a new input-type id — is the one non-obvious correctness move
(Research Pitfall 1): omit it and the D-01 invariant goes red.

Output: an 11th relationship key, ten marks capability-homed, a regenerated input-type
artifact, two new complete refusal records plus one citation swap, a facet_by
declaration with smell-remedy routing, an extended invariant test, and a green
finding-code catalogue still at 275.
</objective>

<artifacts_this_phase_produces>
The complete set of symbols Phase 22 creates across all four plans (this section is the
canonical index; each plan re-lists its own share in must_haves.artifacts):

- `RELATIONSHIP_CHARTS["uncertainty"]` — 11th relationship key, ten Wilke §5.6 marks (this plan)
- The ten uncertainty mark ids (frozen here): `error_bars`, `graded_error_bars`,
  `error_bars_2d`, `confidence_strips`, `eye`, `half_eye`, `quantile_dot_plot`,
  `confidence_band`, `graded_confidence_band`, `fitted_draws` (this plan)
- `BANNED_TYPES["gauge"]`, `BANNED_TYPES["word_cloud"]` — new refusal records, code=DSX-VIZ-001 (this plan)
- `facet_by` — orthogonal declaration; DSX-SMELL-007 remedy routes to it (this plan)
- `DSX-VIZ-071` finding code + `_check_uncertainty_vocabulary` function (Plan 22-02)
- `DSX-VIZ-072` — DELIBERATELY NOT MINTED (Plan 22-02 records the decision loudly)
- `references/chart-catalog.md` — new merged catalog reference doc (Plan 22-03)
- `tests/test_uncertainty_vocabulary.py` — DSX-VIZ-071 gate test + `# D-05:` marker (Plan 22-02)
- `tests/test_chart_catalog_invariant.py` — catalog↔vocabulary conformance + perceptual tie-break (Plan 22-03)
- Extended `tests/test_viz_vocabulary_invariant.py` (this plan); bumped
  `tests/test_finding_catalogue_invariant.py` 275→276 (Plan 22-02)
</artifacts_this_phase_produces>

<assumption_delta_decision>
Adding the 11th RELATIONSHIP_CHARTS key "uncertainty" is a singular→plural transition on
the relationship-function taxonomy (the noun now primary = the relationship-function
taxonomy). Decision: **add-alongside** — Uncertainty is a new peer category; the existing
ten keys keep their identity unchanged. This is category addition, not a generalisation
that demotes an existing primary, so no promote step is needed. Rationale: Wilke treats
Uncertainty as a peer top-level function (ch.5 directory of visualizations lists it
alongside Amounts / Distributions / Proportions / x–y relationships / Geospatial), and
GA-2 decided this shape. Suggested (not required) invariant: the catalog↔vocabulary
conformance test (Plan 22-03) goes red if the "uncertainty" key is later dropped.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-catalog-spine-uncertainty-heuristic/22-CONTEXT.md
@.planning/phases/22-catalog-spine-uncertainty-heuristic/22-RESEARCH.md
@.planning/v2.4-D05-EVIDENCE-PACK.md

# Live structures this plan edits (read the exact line spans; do not re-derive):
@dsx/checks/viz.py
@dsx/spec.py
@dsx/checks/smells.py
@scripts/gen-input-types.py

# The invariant this plan extends and must keep green (the every-mark-has-a-home + refusal tests):
@tests/test_viz_vocabulary_invariant.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Extend the vocabulary invariant for the 11th key, the capability home, the seven refusal records, and facet routing</name>
  <files>tests/test_viz_vocabulary_invariant.py</files>
  <read_first>
    - tests/test_viz_vocabulary_invariant.py (full) — the existing _mark_universe / _capability_homed / _relationship_homed helpers and the TestEveryMarkHasAHome + TestRefusalEntryCompleteness classes are what this task extends, not replaces. These are live Python objects, so there is no CRLF or line-anchoring concern in THIS file.
    - dsx/checks/viz.py:17-29 (RELATIONSHIP_CHARTS) and :45-72 (BANNED_TYPES) — the current 10 keys and 5 refusal records this task asserts will grow to 11 and 7.
    - dsx/spec.py:296-329 (CHART_CAPABILITIES) — the interval-range family the ten marks join.
    - dsx/checks/smells.py:59-80 (_check_atoms_under_density → DSX-SMELL-007) — the remedy string that must name facet_by.
    - 22-RESEARCH.md Pitfall 1 (capability-home requirement) and Pattern 1 (no parallel UNCERTAINTY_MARKS dict).
    - 22-CONTEXT.md D-2 (the ten §5.6 marks and the eye≠half_eye distinction).
  </read_first>
  <behavior>
    - Uncertainty-key assertion: RELATIONSHIP_CHARTS contains a key "uncertainty" whose value, as a set, equals exactly the ten frozen ids {error_bars, graded_error_bars, error_bars_2d, confidence_strips, eye, half_eye, quantile_dot_plot, confidence_band, graded_confidence_band, fitted_draws}. At RED this fails (no such key yet).
    - Capability-home assertion: the existing test_every_mark_has_a_capability_home stays the mechanism; add a targeted assertion that each of the ten uncertainty marks is in _capability_homed(). At RED this fails (the ten marks have no capability home).
    - Relationship count: len(RELATIONSHIP_CHARTS) == 11. At RED this fails (currently 10).
    - Refusal completeness of seven: BANNED_TYPES has exactly the seven keys {3d_bar, 3d_pie, 3d_line, radar, dual_axis_line, gauge, word_cloud}, each a complete {reason, code, citation} record, every code == DSX-VIZ-001. At RED this fails (gauge/word_cloud absent).
    - Radar citation: BANNED_TYPES["radar"]["citation"] contains "Duan" (the signed replacement) and does NOT contain "PROVISIONAL". At RED this fails.
    - facet_by orthogonality: the string "facet_by" appears in no value of RELATIONSHIP_CHARTS, no value of CHART_CAPABILITIES, and no key of BANNED_TYPES (it is a declaration, not a chart type). At RED this passes vacuously today — keep it and let it stay green (it guards a future misuse).
    - Smell-routing: DSX-SMELL-007's remedy string (obtained by running smells.check on a spec whose visual triggers it — relationship "distribution", type "violin", atomicity true) contains "facet_by". At RED this fails (the remedy does not mention faceting yet).
  </behavior>
  <action>
    Extend tests/test_viz_vocabulary_invariant.py in place (do not rewrite the working file). Add a new TestCase class (e.g. TestUncertaintyFamilyAndFacet) importing the already-imported live objects RELATIONSHIP_CHARTS, BANNED_TYPES, CHART_CAPABILITIES plus the smells check. Freeze the ten uncertainty mark ids as a module-level frozenset with a comment stating it is the D-2 §5.6 vocabulary and that RELATIONSHIP_CHARTS["uncertainty"] is the source of truth this set cross-checks (not a second authority). Write the eight assertions in <behavior> as separate test methods, using self.subTest per uncertainty mark and per banned type. For the smell-routing assertion, construct a small spec dict with a visual declaring relationship "distribution", type "violin", atomicity True, call dsx.checks.smells.check, find the DSX-SMELL-007 finding, and assert "facet_by" is a substring of its remedy. For the seven-refusal assertion, assert set(BANNED_TYPES) equals the seven-key set exactly (not merely superset) so a stray eighth ban is caught. Do NOT soften any assertion to make RED pass — RED is the goal. Keep the "Run: python -m unittest tests.test_viz_vocabulary_invariant -v" footer convention.
  </action>
  <verify>
    <automated>python -m unittest tests.test_viz_vocabulary_invariant</automated>
    RED expected: exits non-zero. The uncertainty-key, relationship-count, capability-home, seven-refusal, radar-citation, and smell-routing methods fail for the documented reasons; the facet_by-orthogonality method passes (it is a standing guard). Confirm the failure messages match before proceeding.
  </verify>
  <acceptance_criteria>
    tests/test_viz_vocabulary_invariant.py contains a new class asserting: RELATIONSHIP_CHARTS["uncertainty"] set-equals the ten frozen ids; len(RELATIONSHIP_CHARTS)==11; the ten marks are capability-homed; BANNED_TYPES set-equals the seven keys with all records complete and code DSX-VIZ-001; radar's citation contains "Duan" and not "PROVISIONAL"; facet_by is in no chart-type value; DSX-SMELL-007's remedy contains "facet_by". The module is RED for exactly these reasons, no assertion softened.
  </acceptance_criteria>
  <done>The invariant is extended and RED for the documented reasons (no uncertainty key, ten marks unhomed, gauge/word_cloud absent, radar still PROVISIONAL, DSX-SMELL-007 remedy silent on faceting); no assertion softened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Add the uncertainty relationship key, home its ten marks, regenerate the input-type artifact</name>
  <files>dsx/checks/viz.py, dsx/spec.py, dsx/data/input_types.json</files>
  <read_first>
    - dsx/checks/viz.py:16-29 (RELATIONSHIP_CHARTS shape and the "first entry is the default" comment) and :122-160 (_check_relationship_match — mechanical over the dict, needs NO body change).
    - dsx/spec.py:296-329 (CHART_CAPABILITIES; the interval-range value at :310-313 already holds box, violin, histogram, density, ecdf, strip, kde — the shape family the ten marks join).
    - scripts/gen-input-types.py:31-86 (FAMILY maps IT014/IT036/IT040 → interval-range; EXTRA_MARKS is additive-only) and :112-130 (the regeneration entrypoint).
    - 22-RESEARCH.md Pitfall 1 (capability-home fix) and the Assumptions Log A1 (interval-range is the recommended home; any single existing home keeps the invariant green).
  </read_first>
  <behavior>
    - After the edit, RELATIONSHIP_CHARTS["uncertainty"] equals the ten frozen ids as a tuple (error_bars first as the default choice), and _check_relationship_match admits e.g. error_bars for a declared "uncertainty" relationship and refuses e.g. "bar" (DSX-VIZ-012) — with no change to the function body.
    - After the edit, _capability_homed() covers all ten marks, so test_every_mark_has_a_capability_home is green.
    - After regeneration, dsx/data/input_types.json's IT014, IT036, and IT040 records admit the ten uncertainty marks (proving the id path was refreshed, not just the live dict).
  </behavior>
  <action>
    In dsx/checks/viz.py, add an 11th entry to RELATIONSHIP_CHARTS with key "uncertainty" and a tuple of exactly the ten ids in this order: error_bars, graded_error_bars, error_bars_2d, confidence_strips, eye, half_eye, quantile_dot_plot, confidence_band, graded_confidence_band, fitted_draws (error_bars first = the default recommendation). Add a short comment recording D-2 as the source (Wilke §5.6, ten marks; eye = violin+error bar, half_eye = ridgeline+error bar — distinct per D-2) and that the family is D-12a-clean by construction (the set spans frequentist marks — error_bars, graded_error_bars, confidence_strips, confidence_band — and Bayesian marks — eye, half_eye, quantile_dot_plot, graded_confidence_band, fitted_draws — symmetrically, per Wilke §16.2). In dsx/spec.py, add the same ten mark ids to the CHART_CAPABILITIES["interval-range"] frozenset (Pitfall 1: an uncertainty band, an error bar, and a box plot are all interval-shaped renderings of a distribution or estimate, so interval-range is the honest data-signature home; do NOT create a new input-type id — GA-2 rejects that). Then run python scripts/gen-input-types.py and commit the regenerated dsx/data/input_types.json (Pitfall 1: the input-type-id path of _check_input_type_matrix reads the static generated JSON, not CHART_CAPABILITIES live, so without regeneration IT014/IT036/IT040 stay stale). Do NOT add an EXTRA_MARKS entry — the capability addition lands entirely in the existing interval-range family, so no new EXTRA_MARKS key is needed. Do NOT define a second dict enumerating the uncertainty marks (Research Pattern 1: the tuple is the source of truth).
  </action>
  <verify>
    <automated>python scripts/gen-input-types.py && python -m unittest tests.test_viz_vocabulary_invariant.TestEveryMarkHasAHome tests.test_input_types</automated>
    Expected: regeneration succeeds; test_every_mark_has_a_capability_home is green (the ten marks are homed); the existing input-type suite stays green. The seven-refusal and radar-citation and smell-routing assertions are still expected to fail here — that is Task 3.
  </verify>
  <acceptance_criteria>
    RELATIONSHIP_CHARTS has 11 keys; RELATIONSHIP_CHARTS["uncertainty"] is the ten-id tuple with error_bars first. CHART_CAPABILITIES["interval-range"] contains all ten marks. dsx/data/input_types.json is regenerated and IT014/IT036/IT040 admit the ten marks. TestEveryMarkHasAHome and tests.test_input_types are green. No new EXTRA_MARKS key, no new input-type id, no parallel uncertainty-mark dict.
  </acceptance_criteria>
  <done>The 11th "uncertainty" key holds the ten §5.6 marks, all ten are homed into interval-range, input_types.json is regenerated so the id path admits them, and the capability-home invariant plus the input-type suite are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN): Complete the refusal doctrine (gauge, word_cloud, radar citation) and route faceting to the smell remedy</name>
  <files>dsx/checks/viz.py, dsx/checks/smells.py</files>
  <read_first>
    - dsx/checks/viz.py:45-72 (BANNED_TYPES current five records) and :110-119 (_check_banned — reads ["reason"]; unaffected by adding keys).
    - dsx/checks/smells.py:59-80 (_check_atoms_under_density, DSX-SMELL-007, current remedy "Use an ECDF, stem, or strip plot instead.").
    - .planning/v2.4-D05-EVIDENCE-PACK.md Tier-3 rows: RADAR (Duan et al. 2023, J Clin Epidemiol 156:85-94), GAUGES (Few) — Few's actual grounds are wasted space / missing context / unlabeled scale; "arbitrary maximum" is DSX's own reasoning, NOT Few's — and WORD CLOUD (Jacob Harris, Nieman Journalism Lab 2011-10-13, editorial/practice rationale, not perceptual).
    - 22-CONTEXT.md GA-1 "Refusal rows and the drift guard" (gauge/word_cloud become BANNED_TYPES entries reusing DSX-VIZ-001 → zero new code) and REQ-P22-03 (facet_by orthogonal, remedies route to it, no new code).
  </read_first>
  <behavior>
    - BANNED_TYPES has seven complete records; gauge and word_cloud carry non-empty reason and a citation traceable to the signed HQ-27 pack; every code == DSX-VIZ-001; _check_banned still emits DSX-VIZ-001 with the reason as detail for gauge and word_cloud.
    - radar's citation now names Duan et al. 2023 and no longer says PROVISIONAL.
    - A visual whose type is "gauge" or "word_cloud" yields a DSX-VIZ-001 finding (the ban is enforced, so the catalog's refusal rows in Plan 22-03 are backed — the drift guard holds).
    - DSX-SMELL-007's remedy string names facet_by (small multiples) as a route; facet_by remains absent from every chart-type value.
    - The finding-code catalogue is still 275 (no code minted).
  </behavior>
  <action>
    In dsx/checks/viz.py BANNED_TYPES, add two records reusing the existing five's shape. gauge: reason states Few's actual grounds — a radial gauge wastes space through its circular shape, gives no context for the single number, and its scale is unlabeled — and explicitly attributes the arbitrary-maximum criticism to DSX's own reasoning rather than to Few (HQ-27 T3-GAUGE); code DSX-VIZ-001; citation "Few 2006 (Information Dashboard Design) §3.2 / §6.2.1.1 — HQ-27 T3-GAUGE; arbitrary-maximum criticism is DSX's own, not Few's". word_cloud: reason states the editorial grounds — a word cloud supports only the crudest textual analysis, strips context, and carries no narrative — framed as practice rationale, not a perceptual-encoding finding (HQ-27 word-cloud row); code DSX-VIZ-001; citation "Jacob Harris, 'Word clouds considered harmful', Nieman Journalism Lab 2011-10-13 — HQ-27; editorial rationale, not perceptual". Replace radar's citation string with the signed "Duan et al. 2023 (J Clin Epidemiol 156:85-94), Introduction — area-vs-axis-order and area-proportional-to-square-of-value criticisms; HQ-27 Tier-3" and remove the PROVISIONAL wording. Leave radar's reason string and _check_banned unchanged. In dsx/checks/smells.py, extend DSX-SMELL-007's remedy string so it also offers faceting: append that the reader may split the data into small multiples with a facet_by declaration (facet_by is orthogonal to the mark — a declaration, not a chart type — so it mints no code and adds no BANNED entry). Do not add facet_by to any RELATIONSHIP_CHARTS, CHART_CAPABILITIES, or BANNED_TYPES value. Confirm mechanically that no code is minted: scripts/gen-finding-catalogue.py extract() reads only the code/severity/title args of report.add, never a citation or remedy kwarg, so these edits cannot change the catalogue count.
  </action>
  <verify>
    <automated>python -m unittest tests.test_viz_vocabulary_invariant tests.test_finding_catalogue_invariant && python scripts/gen-finding-catalogue.py --check</automated>
    Expected: the full extended invariant module is green (uncertainty key, capability home, seven complete refusal records, radar citation, facet orthogonality, DSX-SMELL-007 routing); the catalogue invariant still reports 275; gen-finding-catalogue --check exits 0 (catalogue current, no new code, D-05 unchanged). Note the two pre-existing declared-twice VAL warnings are unrelated to this plan.
  </verify>
  <acceptance_criteria>
    BANNED_TYPES has seven complete records; gauge and word_cloud reasons state the HQ-27-signed grounds (gauge marks arbitrary-max as DSX's own; word_cloud framed as editorial), each code DSX-VIZ-001, each citation traceable to the signed pack. radar's citation names Duan et al. 2023 with no PROVISIONAL. A gauge/word_cloud visual fires DSX-VIZ-001. DSX-SMELL-007's remedy contains "facet_by" and facet_by is in no chart-type value. tests.test_viz_vocabulary_invariant and tests.test_finding_catalogue_invariant are green; gen-finding-catalogue --check exits 0 at 275 codes.
  </acceptance_criteria>
  <done>Seven complete refusal records ship (gauge/word_cloud added, radar citation swapped to Duan 2023), the DSX-SMELL-007 remedy routes to facet_by with facet_by kept orthogonal, the full invariant module is green, and the catalogue holds 275 codes (zero mint).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan edits static in-repo Python vocabularies (RELATIONSHIP_CHARTS, CHART_CAPABILITIES,
BANNED_TYPES), one smell remedy string, and one generated JSON artifact, and extends one
off-gate-path test. No network input, no untrusted input parsed, no auth/session, no new
file or process I/O. The realistic threats are integrity/drift threats on the vocabulary
and on citation provenance, mirroring Phase 21's T-21-01..03.

| Boundary | Description |
|----------|-------------|
| (none introduced) | The only "input" is the repo's own static vocabularies, edited under review. No data crosses a new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Tampering | A new mark (any of the ten uncertainty ids) added to RELATIONSHIP_CHARTS with no capability home | medium | mitigate | test_every_mark_has_a_capability_home turns red at commit on exactly this drift; Task 2 homes the ten into interval-range (Pitfall 1, D-01). |
| T-22-02 | Repudiation | gauge / word_cloud / radar refusal citation traceable to no signed source, or gauge attributing the arbitrary-maximum claim to Few | medium | mitigate | Task 1 asserts radar's citation names Duan and drops PROVISIONAL; Task 3 sources gauge/word_cloud from the signed HQ-27 rows and marks the arbitrary-max criticism as DSX's own (HQ-27 T3-GAUGE). A shipped citation not traceable to a signed HQ-27 row is treated as [ASSUMED]. |
| T-22-03 | Tampering | facet_by silently treated as a chart type (widening what the gate admits under cover of a "declaration") | low | mitigate | The facet_by-orthogonality assertion (Task 1) fails if facet_by ever appears in a RELATIONSHIP_CHARTS / CHART_CAPABILITIES / BANNED_TYPES value; REQ-P22-03 keeps it a declaration only. |

No threat rises to `high`, so nothing blocks under the ASVS level-1 block-on-high policy.

**Package legitimacy gate: N/A.** This plan installs no npm/pip/cargo packages — nothing to
audit against a registry, so no T-22-SC supply-chain threat and no legitimacy checkpoint apply.
</threat_model>

<verification>
Phase-level checks for this plan, in order:

1. Full new-and-existing invariant module green: `python -m unittest tests.test_viz_vocabulary_invariant -v` — uncertainty key + 11 relationships, ten marks capability-homed, seven complete refusal records with code DSX-VIZ-001, radar citation = Duan 2023, facet_by orthogonal, DSX-SMELL-007 remedy routes to facet_by.
2. Input-type id path refreshed: `python scripts/gen-input-types.py` regenerates with no error and dsx/data/input_types.json's IT014/IT036/IT040 admit the ten marks.
3. Zero mint held: `python -m unittest tests.test_finding_catalogue_invariant` reports 275 two ways, and `python scripts/gen-finding-catalogue.py --check` exits 0.
4. Full suite green (per-wave gate): `python -m unittest discover -s tests` — the ~1471-test baseline plus the extended invariant module, all passing.
</verification>

<success_criteria>
- The 11th "uncertainty" relationship key holds exactly Wilke's ten §5.6 marks (D-2), D-12a-clean by construction; _check_relationship_match admits/refuses them with no body change (REQ-P22-02).
- All ten marks are capability-homed into CHART_CAPABILITIES["interval-range"] and dsx/data/input_types.json is regenerated so the id path admits them (Pitfall 1; every-mark-has-a-home invariant green).
- BANNED_TYPES has seven complete {reason, code=DSX-VIZ-001, citation} records; gauge/word_cloud added with HQ-27-signed grounds; radar's PROVISIONAL citation replaced by Duan et al. 2023 (REQ-P22-03 drift guard prerequisite for the catalog's refusal rows).
- facet_by ships as an orthogonal declaration; DSX-SMELL-007's remedy routes to it; no new code, no new chart type (REQ-P22-03).
- Finding-code catalogue unchanged at 275; full tests/ suite green.
</success_criteria>

<output>
Create `.planning/phases/22-catalog-spine-uncertainty-heuristic/22-01-SUMMARY.md` when done.
</output>
