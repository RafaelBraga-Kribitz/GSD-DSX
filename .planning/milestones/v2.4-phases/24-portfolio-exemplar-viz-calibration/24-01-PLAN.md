---
phase: 24-portfolio-exemplar-viz-calibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/analysis/charts.py
  - examples/figures/activation_uplift.svg
  - examples/figures/daily_activation_trend.svg
  - examples/figures/activation_uplift_ci.svg
  - examples/good-ANALYSIS-SPEC.yaml
  - examples/good-FIGURE-MANIFEST.yaml
  - examples/good-NARRATIVE.md
  - examples/good-REPRO-REPORT.md
autonomous: true
requirements: [REQ-P24-01]

must_haves:
  truths:
    - "The good exemplar declares a THIRD visuals[] entry encoding the real 95% CI (1.0-3.8pp) via uncertainty_mark: error_bars, routed through DSX-VIZ-071 / RELATIONSHIP_CHARTS['uncertainty'] (GA-1)."
    - "All three figures are rendered through the v2.4 style layer: plt.style.use dsx-urban + dsx_plotstyle.finalise_figure (mandatory source kw) + save_deterministic (writes only, never hashes)."
    - "All three SVGs are freshly sealed via `dsx seal` into spec.visuals[N].svg_sha256 (the two pre-existing seals RE-sealed after re-render — Risk P2)."
    - "good-NARRATIVE.md is strict What / So What / Now What."
    - "good-REPRO-REPORT.md proves deterministic re-render (status: reproduced, activation_rate: 0.024)."
    - "`dsx gate ship --spec examples/good-ANALYSIS-SPEC.yaml` exits 0 (capstone acceptance test — passes every gate at every threshold)."
  artifacts:
    - path: "examples/analysis/charts.py"
      provides: "Renders all 3 figures through the style layer (only matplotlib importer; NOT a gate module)"
      contains: "finalise_figure"
    - path: "examples/figures/activation_uplift_ci.svg"
      provides: "Uncertainty figure — uplift with 95% CI error bars"
    - path: "examples/good-ANALYSIS-SPEC.yaml"
      provides: "3rd visuals[] uncertainty entry + 3 re-sealed svg_sha256 + reproducibility.reproduce_report"
      contains: "uncertainty_mark"
    - path: "examples/good-FIGURE-MANIFEST.yaml"
      provides: "Uncertainty manifest row + matplotlib_version"
      contains: "matplotlib_version"
    - path: "examples/good-NARRATIVE.md"
      provides: "Strict What / So What / Now What narrative"
    - path: "examples/good-REPRO-REPORT.md"
      provides: "Deterministic re-render proof (status: reproduced)"
      contains: "activation_rate"
  key_links:
    - from: "examples/analysis/charts.py"
      to: "styles/dsx-urban.mplstyle + templates/dsx_plotstyle.py"
      via: "plt.style.use('styles/dsx-urban.mplstyle') then finalise_figure(...) then save_deterministic(...)"
      pattern: "dsx-urban|finalise_figure|save_deterministic"
    - from: "examples/good-ANALYSIS-SPEC.yaml"
      to: "dsx seal (single hashing authority)"
      via: "spec.visuals[N].svg_sha256 pasted from `dsx seal <svg>` output"
      pattern: "svg_sha256"
    - from: "examples/good-ANALYSIS-SPEC.yaml"
      to: "examples/good-REPRO-REPORT.md"
      via: "reproducibility.reproduce_report -> DSX-REP-060/061"
      pattern: "reproduce_report"
---

<objective>
Upgrade the existing `examples/good-*` onboarding-activation exemplar IN PLACE into the
v2.4 capstone (GA-1 (a) — no net-new analytical question; reuse the proven-green
statistics). Deliver the v2.4 presentation delta only: figures re-rendered through the
`dsx-urban` house style + `dsx_plotstyle`, exactly ONE honest uncertainty figure showing
the real 95% CI (1.0-3.8pp), a freshly SEALED set of figure hashes, a strict
What/So What/Now What narrative, and a REPRO-REPORT proving deterministic re-render.

Purpose: This is the milestone's integration-verification capstone — the single artifact a
scrutinizing senior DS reads whole. It exercises the v2.4 style layer, the uncertainty
family (DSX-VIZ-071), the seal authority (`dsx seal`), and the repro surface end-to-end on
genuine, load-bearing uncertainty content.

Output: An `examples/good-*` set that passes `dsx gate ship` at every threshold, with all
three figures deterministically re-renderable and sealed. Mints ZERO finding codes (D-06).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-portfolio-exemplar-viz-calibration/24-CONTEXT.md
@.planning/phases/24-portfolio-exemplar-viz-calibration/24-RESEARCH.md
@examples/good-ANALYSIS-SPEC.yaml
@examples/good-FIGURE-MANIFEST.yaml
@examples/good-NARRATIVE.md
@examples/analysis/charts.py
@templates/dsx_plotstyle.py
@templates/REPRO-REPORT.md
@styles/dsx-urban.mplstyle
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author examples/analysis/charts.py and re-render all 3 figures through the style layer</name>
  <files>examples/analysis/charts.py, examples/figures/activation_uplift.svg, examples/figures/daily_activation_trend.svg, examples/figures/activation_uplift_ci.svg</files>
  <action>
    Author `examples/analysis/charts.py` (currently a 1-line empty stub per 24-RESEARCH Reuse anchors) to render all three exemplar figures via the v2.4 style layer. For every figure: call `plt.style.use("styles/dsx-urban.mplstyle")`, build the matplotlib figure, then call `templates/dsx_plotstyle.finalise_figure(fig, title=<the visual's takeaway>, source=<real provenance string>)` — `source` is a MANDATORY keyword (dsx_plotstyle.py:72-95) — optionally `direct_label(ax, ...)`, then `save_deterministic(fig, "examples/figures/<name>.svg")`. `save_deterministic` WRITES ONLY and never hashes (GA-2 Phase 23) — do NOT compute any hash here; sealing is Task 2's `dsx seal` step.

    Render (i) `activation_uplift.svg` — the point-estimate uplift bar (re-render of the existing hand-made SVG through the style layer); (ii) `daily_activation_trend.svg` — the trend line (re-render); (iii) `activation_uplift_ci.svg` — the NEW uncertainty figure: the activation uplift drawn with its real 95% CI (1.0-3.8pp) as error bars (uncertainty_mark: error_bars). Anchor the CI numbers to results.tests[0] in good-ANALYSIS-SPEC.yaml (ci: [0.0101, 0.0384], effect: 0.024 — 24-RESEARCH Ground-truth confirmations); do NOT invent numbers.

    charts.py is the ONLY matplotlib importer in the repo and is NOT a gate module — it must never be imported on the gate path (test_gate_path_hermetic.FORBIDDEN already includes matplotlib, per 24-RESEARCH §Standing / P7). matplotlib is a pre-existing dependency (introduced Phase 23 for the style layer) — this task installs NOTHING new.

    Re-rendering the two existing figures intentionally CHANGES their bytes — this is expected and is exactly why Task 2 must re-seal all three (Risk P2). Run the generator so all three SVGs land under examples/figures/.
  </action>
  <verify>
    <automated>python examples/analysis/charts.py && ls examples/figures/activation_uplift.svg examples/figures/daily_activation_trend.svg examples/figures/activation_uplift_ci.svg && python -m unittest tests.test_gate_path_hermetic -v</automated>
  </verify>
  <done>Generator runs clean; all three SVGs exist under examples/figures/; test_gate_path_hermetic stays green (matplotlib remains off the gate path — charts.py is not a gate module).</done>
</task>

<task type="auto">
  <name>Task 2: Add the 3rd uncertainty visuals[] entry, re-seal all 3 SVGs, and wire the manifest</name>
  <files>examples/good-ANALYSIS-SPEC.yaml, examples/good-FIGURE-MANIFEST.yaml</files>
  <action>
    Add a THIRD entry to the `visuals:` list in `examples/good-ANALYSIS-SPEC.yaml` (existing two entries at :172-214; this is an ADD, not an edit of the existing two — 24-RESEARCH Q1). The new uncertainty visual must be gate-clean at EVERY threshold, so set exactly these fields: `relationship: uncertainty`; `type: error_bars`; `uncertainty_mark: error_bars` (the DSX-VIZ-071 field, viz.py:494 — a valid member passes silently); `shows_estimates: true` + `shows_uncertainty: true` (else DSX-VIZ-070 HIGH); `data_input_type: interval-range` (RESOLVED at plan time — dsx/spec.py:310-321 CHART_CAPABILITIES["interval-range"] admits error_bars, so no DSX-VIZ-013 HIGH; A1/P3 closed); `units:` non-blank (else DSX-VIZ-061 HIGH); `takeaway:` containing a magnitude token / digit / `pp` (else DSX-VIZ-063 HIGH / DSX-VIZ-064 MEDIUM); `source:` (else DSX-VIZ-062 LOW); `artifact_path: figures/activation_uplift_ci.svg`; `chart_id:` + `generator: analysis/charts.py`.

    SEAL: run `dsx seal examples/figures/<name>.svg` for ALL THREE SVGs (the two re-rendered + the new one) and paste each `sha256:<hex>` into the matching `spec.visuals[N].svg_sha256`. The two pre-existing seals (:180, :203) hash the OLD hand-made bytes and are now STALE — re-seal them or DSX-FIG-010 fires CRITICAL at verify/ship (Risk P2). `dsx seal` is the SINGLE hashing authority; the seal lives in the SPEC visuals field, never in the manifest file (24-RESEARCH Summary / Q1).

    Add a matching row to `examples/good-FIGURE-MANIFEST.yaml` `figures:` for the uncertainty figure (chart_id / path / generator, mirroring the existing 2 rows at :3-9) so the coverage check stays green (else DSX-FIG-040 HIGH). Add the `matplotlib_version:` field (per templates/FIGURE-MANIFEST.yaml — Phase 23 field). The manifest carries NO svg_sha256.
  </action>
  <verify>
    <automated>dsx gate ship --spec examples/good-ANALYSIS-SPEC.yaml; echo "EXIT: $?"</automated>
  </verify>
  <done>`dsx gate ship --spec examples/good-ANALYSIS-SPEC.yaml` exits 0. Spec has exactly 3 visuals[]; the 3rd declares uncertainty_mark: error_bars + data_input_type: interval-range; all 3 svg_sha256 match the freshly-sealed SVG bytes; manifest has the uncertainty row + matplotlib_version.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite good-NARRATIVE.md (What/So What/Now What) and author good-REPRO-REPORT.md</name>
  <files>examples/good-NARRATIVE.md, examples/good-REPRO-REPORT.md, examples/good-ANALYSIS-SPEC.yaml</files>
  <action>
    Rewrite `examples/good-NARRATIVE.md` from its current Answer/Limits/Method shape (:5-21) into strict **What / So What / Now What** sections. The narrative must not exceed the verified numbers — the uplift is 2.4pp with 95% CI 1.0-3.8pp (results.tests[0]); the decision rule is the CI lower bound > +1.0pp (24-CONTEXT §standing). State the honest, load-bearing uncertainty; no decorative claims.

    Author `examples/good-REPRO-REPORT.md` from `templates/REPRO-REPORT.md`. Only the FIRST ```yaml block is parsed (repro.py:22-29, CRLF-safe parser at :340 uses `\r?\n`); use flat `key: value`. Set `status: reproduced` and `activation_rate: 0.024` (must equal results.tests[0].effect = 0.024 within rel_tol=1e-2, else DSX-REP-061 HIGH). The deterministic re-render proof is structural: re-running examples/analysis/charts.py reproduces the sealed bytes (save_deterministic + dsx-urban svg.hashsalt/fonttype recipe — GA-3).

    Add `reproducibility.reproduce_report: good-REPRO-REPORT.md` under the `reproducibility:` block in `examples/good-ANALYSIS-SPEC.yaml` (:216-228) — this opts the exemplar into the strict repro check (repro.py:285-388); a missing report then fires DSX-REP-060 HIGH, so the report must exist and its lead number must agree.
  </action>
  <verify>
    <automated>dsx gate ship --spec examples/good-ANALYSIS-SPEC.yaml; echo "EXIT: $?"</automated>
  </verify>
  <done>good-NARRATIVE.md uses What/So What/Now What headings; good-REPRO-REPORT.md exists with status: reproduced + activation_rate: 0.024; spec.reproducibility.reproduce_report points at it; `dsx gate ship --spec examples/good-ANALYSIS-SPEC.yaml` still exits 0 (DSX-REP-060/061 pass).</done>
</task>

</tasks>

<threat_model>
register_authored_at_plan_time: true
Block posture: this milestone blocks on HIGH only.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator -> gate | charts.py (matplotlib) writes SVG bytes; the gate reads only the SPEC visuals + sealed hashes. matplotlib must never cross onto the gate path. |
| author -> seal | Figure bytes are trusted only via `dsx seal`; a stale/hand-edited hash is an integrity break the gate must catch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01-01 | Tampering | spec.visuals[].svg_sha256 vs re-rendered SVG bytes | critical | mitigate | Re-seal ALL THREE SVGs via `dsx seal` after re-render (Task 2); acceptance test `dsx gate ship` exit 0 proves DSX-FIG-010 (CRITICAL) does not fire on a stale seal. |
| T-24-01-02 | Tampering | matplotlib on the gate path | high | mitigate | charts.py is the sole matplotlib importer and is not a gate module; Task 1 verify re-runs test_gate_path_hermetic (matplotlib in FORBIDDEN). |
| T-24-01-03 | Information disclosure | good-NARRATIVE.md / good-REPRO-REPORT.md overstating the verified effect | medium | mitigate | Narrative and repro lead number pinned to results.tests[0] (effect 0.024, CI 1.0-3.8pp); DSX-REP-061 HIGH gates lead-number disagreement (Task 3). |
| T-24-01-04 | Tampering | accidental finding-code mint via exemplar edits | high | mitigate | Exemplar routes only to EXISTING codes (DSX-VIZ-071/DSX-FIG-*/DSX-REP-*); zero-mint set-identity 276->276 is asserted in plan 24-03 Task 1 across the final tree. |
</threat_model>

<verification>
- `dsx gate ship --spec examples/good-ANALYSIS-SPEC.yaml` exits 0 (capstone acceptance — every gate at every threshold).
- All three figures re-render deterministically (re-running charts.py reproduces the sealed bytes).
- test_gate_path_hermetic green (matplotlib off the gate path).
- No new finding code introduced (verified holistically in 24-03).
</verification>

<success_criteria>
- Exemplar has exactly 3 visuals[]; the 3rd is a valid uncertainty figure (uncertainty_mark: error_bars, data_input_type: interval-range).
- All 3 SVGs freshly sealed into spec.visuals[N].svg_sha256; manifest has matching row + matplotlib_version.
- good-NARRATIVE.md is strict What/So What/Now What; good-REPRO-REPORT.md proves deterministic re-render (status: reproduced, activation_rate: 0.024).
- `dsx gate ship --spec examples/good-ANALYSIS-SPEC.yaml` exit 0.
</success_criteria>

<output>
Create `.planning/phases/24-portfolio-exemplar-viz-calibration/24-01-SUMMARY.md` when done.
</output>
