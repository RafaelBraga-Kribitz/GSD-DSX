---
phase: 24-portfolio-exemplar-viz-calibration
plan: 03
type: execute
wave: 2
depends_on: [24-01, 24-02]
files_modified:
  - tests/test_selection_heuristic_docs.py
autonomous: true
requirements: [REQ-P24-03]

must_haves:
  truths:
    - "Both doc/code-agreement tests are green on the FINAL tree: test_doc_code_agreement.py (its deliberate one-directionality UNCHANGED — documented-by-design) and test_selection_heuristic_docs.py (GA-3)."
    - "test_viz_vocabulary_invariant (len==11 / uncertainty set / BANNED_TYPES==7 pins) and test_chart_catalog_invariant (BANNED_TYPES equality) are UNMUTATED and green (snapshots unmutated — Risk P8)."
    - "`scripts/gen-finding-catalogue.py --check` exits 0 @276 — set-identity 276->276, added={} removed={} (D-06 zero-mint proved live on the final tree after 24-01 and 24-02 land)."
    - "RECOMMENDED (plan-checker's final call): the chart-selection live-dict binding gap is closed by ONE both-directions assertion importing RELATIONSHIP_CHARTS — or left as transitive coverage if the checker rules the gap not-real."
  artifacts:
    - path: "tests/test_selection_heuristic_docs.py"
      provides: "IF gap closed: +1 both-directions assertion binding the chart-selection relationship vocabulary to the live RELATIONSHIP_CHARTS (dsx.checks.viz)"
      contains: "RELATIONSHIP_CHARTS"
  key_links:
    - from: "tests/test_selection_heuristic_docs.py"
      to: "dsx.checks.viz.RELATIONSHIP_CHARTS"
      via: "IF gap closed: import + set-equality both directions vs the 11 relationship names in the chart-selection surface"
      pattern: "RELATIONSHIP_CHARTS"
---

<objective>
REQ-P24-03 is verify-not-build (GA-3). Confirm the milestone-audit prerequisites hold on the
FINAL tree (after 24-01 and 24-02 land): both selection-surface doc/code-agreement tests
green, the snapshot/count-pin invariants unmutated, and the finding catalogue current at 276
with set-identity 276->276 (D-06 zero-mint). Then — as a RECOMMENDED, plan-checker-flagged
step — close the one narrow, defensible gap the research identified in the chart-selection
surface's test (24-RESEARCH Q3), and nothing more.

Purpose: This is the milestone's integrity gate. It proves the calibration phase added the
exemplar and the fixtures WITHOUT perturbing the frozen vocabulary snapshots or minting any
code — the guarantee the milestone audit depends on.

Output: Verification that all audit-prerequisite tests are green + catalogue @276; plus,
if the plan-checker approves, a one-assertion tightening of test_selection_heuristic_docs.py.
Mints ZERO codes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-portfolio-exemplar-viz-calibration/24-CONTEXT.md
@.planning/phases/24-portfolio-exemplar-viz-calibration/24-RESEARCH.md
@tests/test_doc_code_agreement.py
@tests/test_selection_heuristic_docs.py
@tests/test_viz_vocabulary_invariant.py
@tests/test_chart_catalog_invariant.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify audit prerequisites green on the final tree + set-identity 276->276</name>
  <files>(verification only — mutates nothing)</files>
  <action>
    Run the milestone-audit prerequisite checks on the FINAL tree (this plan is Wave 2, so
    24-01's exemplar and 24-02's fixtures are already in place). Confirm ALL of the following
    are green WITHOUT mutating any of them (verify-not-build — GA-3):

    - `tests/test_doc_code_agreement.py` — the test-selection.md <-> dsx.checks.stats surface.
      Its primary-cell equality + otherwise doc-subset-of-code one-directionality is
      DELIBERATE and documented (module docstring). Do NOT change its directionality (24-CONTEXT / 24-RESEARCH Q3).
    - `tests/test_selection_heuristic_docs.py` — the chart-selection.md prose/route-and-cite surface.
    - `tests/test_viz_vocabulary_invariant.py` — the len==11 / uncertainty-set / BANNED_TYPES==7 pins (Risk P8).
    - `tests/test_chart_catalog_invariant.py` — BANNED_TYPES equality pin.

    Then prove D-06 zero-mint on the final tree: run `python scripts/gen-finding-catalogue.py --check`
    (must exit 0 — "finding catalogue is current"), and confirm the live count is still 276 via a
    CRLF-safe unique DSX-code count over references/finding-codes.md AND the "Total: 276 codes." line.
    Set-identity target: 276->276, added={} removed={}. (Re-measured at plan time = 276; adding the
    exemplar + 4 fixtures routes only to EXISTING codes, so the count must be unchanged.)

    If any of these is RED, do NOT patch the invariant to make it pass — a red pin means an
    accidental mint or snapshot mutation slipped in from 24-01/24-02; report it for gap closure.
  </action>
  <verify>
    <automated>python -m unittest tests.test_doc_code_agreement tests.test_selection_heuristic_docs tests.test_viz_vocabulary_invariant tests.test_chart_catalog_invariant -v && python scripts/gen-finding-catalogue.py --check; echo "CHECK EXIT: $?"</automated>
  </verify>
  <done>All four test modules green; `gen-finding-catalogue.py --check` exits 0 @276; live CRLF-safe unique count == 276 and the "Total: 276 codes." line agree (set-identity 276->276, added={} removed={}). No invariant/agreement test was mutated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [RECOMMENDED — plan-checker's call] Close the chart-selection live-dict binding gap</name>
  <files>tests/test_selection_heuristic_docs.py</files>
  <behavior>
    - After the change, `set(RELATIONSHIP_CHARTS)` (imported from dsx.checks.viz) equals the set of relationship names enumerated in the chart-selection surface, asserted in BOTH directions (no name in the live dict missing from the doc; no name in the doc absent from the live dict).
    - Adding an 11th->12th relationship key to the live dict, or renaming one, would now FAIL this test (drift caught directly, not only transitively via test_viz_vocabulary_invariant's len==11 pin).
    - The existing citation-presence / forbidden-token / no-parallel-file / catalog-pointer assertions still pass unchanged.
  </behavior>
  <action>
    FLAG FOR THE PLAN-CHECKER: this task is RECOMMENDED but its inclusion is the plan-checker's
    final call (24-RESEARCH Q3 frames it as a bounded plan-checker judgment; 24-brief says leave
    the final call flagged). Planner recommendation: CLOSE it. Rationale: the current
    `test_selection_heuristic_docs.py` binds the chart-selection relationship vocabulary to a
    HARDCODED `_RELATIONSHIPS` tuple (:32-44), one-directional (constant-subset-of-doc), and never
    imports the live dict; today's only guard that the doc matches the LIVE vocabulary is the
    TRANSITIVE len==11 pin in a different test (test_viz_vocabulary_invariant), which guards SET SIZE,
    not name agreement between the prose and the live dict. On the terminal viz-integrity phase, a
    direct both-directions binding is exactly the drift-guard that belongs here, and the close is
    non-speculative (one assertion, honours GA-3 "close exactly that gap and no more"). If the
    plan-checker rules the transitive coverage sufficient, DROP this task and REQ-P24-03 is pure
    verification (Task 1 alone).

    If closing: in `tests/test_selection_heuristic_docs.py`, import `RELATIONSHIP_CHARTS` from
    `dsx.checks.viz` and add ONE assertion that `set(RELATIONSHIP_CHARTS)` equals the set of the 11
    relationship names enumerated in the chart-selection surface (SKILL.md / references/chart-selection.md)
    BOTH directions. This is a one-assertion tightening of the existing file — NOT a new test surface,
    NOT a change to the hardcoded tuple's existing checks. Do NOT touch test_doc_code_agreement.py
    (its one-directionality is documented-by-design). Stay stdlib-only; use `\r?\n` for any
    line-anchored regex (repo is CRLF). RED before GREEN: the both-directions assertion should be
    written to fail if the doc and live dict ever diverge.
  </action>
  <verify>
    <automated>python -m unittest tests.test_selection_heuristic_docs -v</automated>
  </verify>
  <done>IF the plan-checker approves the close: test_selection_heuristic_docs.py imports RELATIONSHIP_CHARTS and asserts set-equality both directions against the doc's 11 relationship names; full module green; test_doc_code_agreement.py untouched. IF the plan-checker drops it: this task is removed and REQ-P24-03 is satisfied by Task 1 alone.</done>
</task>

</tasks>

<threat_model>
register_authored_at_plan_time: true
Block posture: this milestone blocks on HIGH only.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| final tree -> milestone audit | The audit trusts that the vocabulary snapshots and the catalogue count are unchanged; a silent mint or snapshot mutation from an earlier plan would corrupt the audit. |
| doc <-> live code | The chart-selection prose is trusted to match the live RELATIONSHIP_CHARTS; today that agreement is only transitively guarded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-03-01 | Tampering | accidental finding-code mint from 24-01/24-02 (D-06 violation) | high | mitigate | Task 1 runs `gen-finding-catalogue.py --check` (exit 0) + CRLF-safe unique count == 276 + Total-line agreement on the FINAL tree; set-identity 276->276 asserted. |
| T-24-03-02 | Tampering | snapshot/count pins silently mutated to force a pass | high | mitigate | Task 1 verifies test_viz_vocabulary_invariant + test_chart_catalog_invariant green WITHOUT mutation; a red pin is reported for gap closure, never patched. |
| T-24-03-03 | Tampering | test_doc_code_agreement.py's deliberate one-directionality "fixed" | medium | mitigate | Explicitly forbidden (Task 1 + Task 2); the one-directionality is documented-by-design (module docstring). |
| T-24-03-04 | Spoofing | chart-selection doc drifts from the live vocabulary undetected | medium | mitigate | RECOMMENDED close (Task 2, plan-checker's call): direct both-directions binding to RELATIONSHIP_CHARTS catches drift the transitive len pin misses. |
</threat_model>

<verification>
- All four audit-prerequisite modules green on the final tree.
- `gen-finding-catalogue.py --check` exit 0 @276; set-identity 276->276.
- No invariant/agreement test mutated (except the RECOMMENDED one-assertion close in Task 2, if approved).
</verification>

<success_criteria>
- test_doc_code_agreement.py, test_selection_heuristic_docs.py, test_viz_vocabulary_invariant.py, test_chart_catalog_invariant.py all green.
- Catalogue current at 276; zero codes minted across the whole phase (276->276).
- The narrow chart-selection gap is closed (recommended) or explicitly left per the plan-checker's ruling.
</success_criteria>

<output>
Create `.planning/phases/24-portfolio-exemplar-viz-calibration/24-03-SUMMARY.md` when done.
</output>
