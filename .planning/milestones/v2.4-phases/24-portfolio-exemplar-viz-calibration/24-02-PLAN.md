---
phase: 24-portfolio-exemplar-viz-calibration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/known-bad/chart-gauge-single-kpi-ANALYSIS-SPEC.yaml
  - examples/known-bad/chart-gauge-single-kpi-POSTMORTEM.md
  - examples/known-bad/chart-word-cloud-text-ANALYSIS-SPEC.yaml
  - examples/known-bad/chart-word-cloud-text-POSTMORTEM.md
  - examples/known-bad/chart-radar-multimetric-ANALYSIS-SPEC.yaml
  - examples/known-bad/chart-radar-multimetric-POSTMORTEM.md
  - examples/known-bad/chart-uncertainty-mark-misuse-ANALYSIS-SPEC.yaml
  - examples/known-bad/chart-uncertainty-mark-misuse-POSTMORTEM.md
  - tests/test_known_bad_corpus.py
autonomous: true
requirements: [REQ-P24-02]

must_haves:
  truths:
    - "Four bad-CHART-choice fixtures exist under examples/known-bad/ mirroring the corpus convention (<slug>-ANALYSIS-SPEC.yaml + <slug>-POSTMORTEM.md) — the FIRST chart-choice fixtures in the corpus (GA-2)."
    - "Each fixture is BASED ON a clean examples/good-corpus/*-ANALYSIS-SPEC.yaml with exactly ONE bad visuals[] entry -> exits 0 at plan+execute and passes `dsx validate` (Risks P4/P5)."
    - "The 3 HIGH fixtures (gauge, word_cloud, radar) fire DSX-VIZ-001 HIGH at verify+ship and are named in their POSTMORTEMs."
    - "The MEDIUM fixture (uncertainty-mark-misuse) fires DSX-VIZ-071 MEDIUM and is caught via a NEW MEDIUM stratum gated with --block-on MEDIUM, reported BESIDE the headline, never folded in (Risk P1, mirrors HIGH stratum's D-06 invariance)."
    - "Every fixture is registered in _EXPECTED_CAUGHT_DEFECTS (total-equality guard stays green)."
    - "The headline (miss-rate over ABSENT, FPR) is INVARIANT to the added caught fixtures."
    - "`python -m unittest tests.test_known_bad_corpus` is green; zero finding codes minted (D-06)."
  artifacts:
    - path: "examples/known-bad/chart-gauge-single-kpi-ANALYSIS-SPEC.yaml"
      provides: "gauge -> DSX-VIZ-001 HIGH fixture (one bad visual on a clean base)"
      contains: "gauge"
    - path: "examples/known-bad/chart-word-cloud-text-ANALYSIS-SPEC.yaml"
      provides: "word_cloud -> DSX-VIZ-001 HIGH fixture"
      contains: "word_cloud"
    - path: "examples/known-bad/chart-radar-multimetric-ANALYSIS-SPEC.yaml"
      provides: "radar -> DSX-VIZ-001 HIGH banned-type control fixture"
      contains: "radar"
    - path: "examples/known-bad/chart-uncertainty-mark-misuse-ANALYSIS-SPEC.yaml"
      provides: "uncertainty_mark not-a-member -> DSX-VIZ-071 MEDIUM fixture"
      contains: "uncertainty_mark"
    - path: "tests/test_known_bad_corpus.py"
      provides: "Extended: _EXPECTED_CAUGHT_DEFECTS +4, _HIGH_TARGET_DEFECT_CODES +3, _MEDIUM_TARGET_DEFECT_CODES (new), block_on threaded into _gate_findings, MEDIUM stratum readout beside headline"
      contains: "_MEDIUM_TARGET_DEFECT_CODES"
  key_links:
    - from: "examples/known-bad/*-ANALYSIS-SPEC.yaml"
      to: "tests/test_known_bad_corpus.py::_EXPECTED_CAUGHT_DEFECTS"
      via: "total-equality guard test_expected_caught_defects_keys_match_the_corpus_on_disk requires a key per spec on disk"
      pattern: "_EXPECTED_CAUGHT_DEFECTS"
    - from: "chart-uncertainty-mark-misuse fixture"
      to: "_MEDIUM_TARGET_DEFECT_CODES -> _gate_findings(block_on='MEDIUM') -> _classify_target_defect(severity='MEDIUM')"
      via: "--block-on MEDIUM makes the fired MEDIUM produce exit 1 so the catch classifies"
      pattern: "block_on|--block-on|severity=.MEDIUM"
---

<objective>
Author the FIRST bad-CHART-choice fixtures in the known-bad corpus (GA-2) and re-baseline
the stratified catch-rate / false-positive-rate across the now chart-inclusive corpus
(Phase-12/20 calibration discipline). Four fixtures: three banned-type refusals routed to
DSX-VIZ-001 HIGH (gauge, word_cloud, radar-control) and one uncertainty-mark misuse routed
to DSX-VIZ-071 MEDIUM — the phase's principal landmine, which needs a NEW MEDIUM stratum
because DSX-VIZ-071 fires MEDIUM and does not block at the default HIGH threshold (Risk P1).

Purpose: Prove the v2.4 viz gates catch bad chart choices, and measure the catch-rate
honestly (the MEDIUM catch-rate reported BESIDE the headline, never folded in) so the
corpus stays a truthful calibration instrument.

Output: 4 fixtures + an extended tests/test_known_bad_corpus.py. Mints ZERO codes: gauge and
word_cloud are pre-existing BANNED_TYPES keys mapping to the existing DSX-VIZ-001 (D-06).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-portfolio-exemplar-viz-calibration/24-CONTEXT.md
@.planning/phases/24-portfolio-exemplar-viz-calibration/24-RESEARCH.md
@tests/test_known_bad_corpus.py
@examples/good-corpus/freq-proportion-checkout-ANALYSIS-SPEC.yaml
@examples/known-bad
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 3 banned-type HIGH fixtures + POSTMORTEMs and register them</name>
  <files>examples/known-bad/chart-gauge-single-kpi-ANALYSIS-SPEC.yaml, examples/known-bad/chart-gauge-single-kpi-POSTMORTEM.md, examples/known-bad/chart-word-cloud-text-ANALYSIS-SPEC.yaml, examples/known-bad/chart-word-cloud-text-POSTMORTEM.md, examples/known-bad/chart-radar-multimetric-ANALYSIS-SPEC.yaml, examples/known-bad/chart-radar-multimetric-POSTMORTEM.md, tests/test_known_bad_corpus.py</files>
  <action>
    For each of the three banned-type fixtures: copy a clean `examples/good-corpus/*-ANALYSIS-SPEC.yaml` (guarantees exit 0 at plan/execute and `dsx validate` pass — Risks P4/P5), rename to `examples/known-bad/<slug>-ANALYSIS-SPEC.yaml`, and set exactly ONE visuals[] entry's `type` to the banned value. Suggested bases (Claude's discretion — any clean good-corpus spec works): chart-gauge-single-kpi on a proportion spec (e.g. freq-proportion-checkout), chart-word-cloud-text on a count spec (e.g. freq-count-installs), chart-radar-multimetric on a multi-metric spec (e.g. freq-continuous-aov).

    Set on the single bad visual: `type: gauge` / `type: word_cloud` / `type: radar` respectively. Keep the visual otherwise complete so DSX-VIZ-001 is the ONLY HIGH finding (Fixture recipe): declare `units:`, a magnitude `takeaway:`, `source:`; OMIT `artifact_path`/`svg_sha256` (no file in the fresh tempdir -> avoids DSX-FIG-001 HIGH; figures.py skips when artifact blank); OMIT `relationship:` on the banned-type visual (accept DSX-VIZ-010 MEDIUM rather than trip DSX-VIZ-012 HIGH from a type-not-admissible mismatch — Risk P5). gauge and word_cloud are live BANNED_TYPES keys (viz.py:93-108); radar is a pre-existing banned type (viz.py:60-92). All three map to the EXISTING DSX-VIZ-001 — no new code (D-06).

    Author `<slug>-POSTMORTEM.md` for each, naming the literal target code the fixture trips and explaining WHY the chart choice is substantively bad (test_every_postmortem_names_a_catch_attribution_finding_code:982; test_high_stratum_target_codes_fire_and_are_named requires the code named in the POSTMORTEM). Use `\r?\n`-safe prose (repo is CRLF).

    Register in tests/test_known_bad_corpus.py: add each slug -> frozenset() to `_EXPECTED_CAUGHT_DEFECTS` (:419) and add each to `_HIGH_TARGET_DEFECT_CODES` (:505) as {"verify":"DSX-VIZ-001","ship":"DSX-VIZ-001"}. Do NOT add any to _INCIDENTAL_GAP_CODES. Authoring the fixtures first makes the total-equality guard go RED; registering them makes it GREEN (RED->GREEN).
  </action>
  <verify>
    <automated>dsx validate examples/known-bad/chart-gauge-single-kpi-ANALYSIS-SPEC.yaml && python -m unittest tests.test_known_bad_corpus -v 2>&1 | tail -25</automated>
  </verify>
  <done>Three fixtures + POSTMORTEMs exist; each passes `dsx validate` and exits 0 at plan+execute; each fires DSX-VIZ-001 HIGH at verify+ship and is named in its POSTMORTEM; _EXPECTED_CAUGHT_DEFECTS and _HIGH_TARGET_DEFECT_CODES include all three; test_known_bad_corpus green (HIGH stratum).</done>
</task>

<task type="auto">
  <name>Task 2: Author the DSX-VIZ-071 MEDIUM uncertainty-mark-misuse fixture and register its key</name>
  <files>examples/known-bad/chart-uncertainty-mark-misuse-ANALYSIS-SPEC.yaml, examples/known-bad/chart-uncertainty-mark-misuse-POSTMORTEM.md, tests/test_known_bad_corpus.py</files>
  <action>
    Copy a clean `examples/good-corpus/*-ANALYSIS-SPEC.yaml` (e.g. a proportion spec) to `examples/known-bad/chart-uncertainty-mark-misuse-ANALYSIS-SPEC.yaml`. Set exactly ONE visuals[] entry to declare an uncertainty figure with a bad mark: `relationship: uncertainty`, `type: error_bars`, `uncertainty_mark: gradient_band` (NOT a member of RELATIONSHIP_CHARTS["uncertainty"] -> trips DSX-VIZ-071 MEDIUM at viz.py:498-510), `shows_estimates: true`, `shows_uncertainty: true`, `data_input_type: interval-range` (admits error_bars per dsx/spec.py:310-321 — so no DSX-VIZ-013 HIGH), plus `units:`, a magnitude `takeaway:`, `source:`. This keeps DSX-VIZ-071 MEDIUM the ONLY off-clean finding; it does NOT block at any default gate (MEDIUM < HIGH threshold) so the fixture still exits 0 at plan+execute and passes `dsx validate`.

    Author `chart-uncertainty-mark-misuse-POSTMORTEM.md` naming the literal DSX-VIZ-071 and explaining the misuse (declared an uncertainty relationship but chose a non-member mark). CRLF-safe (`\r?\n`).

    Register slug -> frozenset() in `_EXPECTED_CAUGHT_DEFECTS` (Risk P6 — the total-equality guard at :1199 requires a key for EVERY spec on disk, easy to forget for this one). Do NOT add it to `_HIGH_TARGET_DEFECT_CODES` (it does not fire HIGH — Risk P1). Do NOT register it as a `kind: miss` ABSENT case (it fires; the ABSENT partition checks CRITICAL-tier absence — mislabeling it a miss is the dishonesty the corpus forbids). Do NOT change DSX-VIZ-071 to HIGH.
  </action>
  <verify>
    <automated>dsx validate examples/known-bad/chart-uncertainty-mark-misuse-ANALYSIS-SPEC.yaml && python -m unittest tests.test_known_bad_corpus.KnownBadCorpusTests.test_expected_caught_defects_keys_match_the_corpus_on_disk -v</automated>
  </verify>
  <done>Fixture + POSTMORTEM exist; `dsx validate` passes; the fixture exits 0 at plan+execute (MEDIUM non-blocking); DSX-VIZ-071 MEDIUM fires at verify/ship; slug is keyed in _EXPECTED_CAUGHT_DEFECTS; total-equality guard green. (The catch is CLASSIFIED in Task 3's MEDIUM stratum.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the MEDIUM stratum (--block-on MEDIUM threaded) and re-baseline beside the headline</name>
  <files>tests/test_known_bad_corpus.py</files>
  <behavior>
    - With block_on='MEDIUM' threaded into _gate_findings, running `dsx gate <point> --spec chart-uncertainty-mark-misuse... --block-on MEDIUM` yields exit_code == 1 and DSX-VIZ-071 present among findings at the MEDIUM tier.
    - _classify_target_defect(slug, point, exit_code, findings, _MEDIUM_TARGET_DEFECT_CODES, severity="MEDIUM") returns no problems for the misuse fixture at verify+ship (a "catch").
    - test_stratified_catch_rate_and_fpr_report reports a `medium_catch_rate` in [0,1] BESIDE the headline; the headline (miss-rate over ABSENT, FPR) is UNCHANGED by adding the caught MEDIUM fixture (D-06-style invariance, mirroring the HIGH stratum at :1806-1816).
    - The default-severity strata (CRITICAL/HIGH) are untouched: existing block_on default preserves current behavior for every other fixture.
  </behavior>
  <action>
    Extend tests/test_known_bad_corpus.py with the MEDIUM stratum exactly as 24-RESEARCH Risk P1 prescribes (Phase-20-precedented, reuses existing machinery — mints nothing, changes NO gate code):

    1. Add `_MEDIUM_TARGET_DEFECT_CODES = {"chart-uncertainty-mark-misuse": {"verify":"DSX-VIZ-071","ship":"DSX-VIZ-071"}}`.
    2. Thread a `block_on` argument through `_gate_findings` (:850-911) so it appends `--block-on <level>` to the `cli.main([...])` argv when set (the CLI already accepts --block-on, cli.py:974, consumed :300). Preserve the current default (no --block-on) so CRITICAL/HIGH strata behavior is byte-for-byte unchanged.
    3. Add a MEDIUM readout in `test_stratified_catch_rate_and_fpr_report`: iterate `_MEDIUM_TARGET_DEFECT_CODES` over ("verify","ship"), call `_gate_findings(..., block_on="MEDIUM")`, classify LIVE via `_classify_target_defect(..., severity="MEDIUM")` (the severity= parameter already exists at :271, :285-290), and report `medium_catch_rate` BESIDE the headline — assert it is within [0,1] only (no pinned baseline; self-rebaselines like the HIGH stratum). NEVER fold the MEDIUM rate into the headline.
    4. Add an invariance assertion that the headline value is unchanged by the added fixtures (mirror :1746-1753 / :1806-1816).

    Do NOT change DSX-VIZ-071's severity; do NOT alter the ABSENT/miss partition; do NOT import matplotlib (stdlib-only off-gate-path discipline, Risk P7). Use `\r?\n` for any new line-anchored regex.
  </action>
  <verify>
    <automated>python -m unittest tests.test_known_bad_corpus -v 2>&1 | tail -30</automated>
  </verify>
  <done>_MEDIUM_TARGET_DEFECT_CODES exists; _gate_findings accepts block_on and appends --block-on when set; the MEDIUM fixture classifies as a catch under severity="MEDIUM"; medium_catch_rate is reported beside the headline within [0,1]; the headline is invariant to the added fixtures; full tests.test_known_bad_corpus green.</done>
</task>

</tasks>

<threat_model>
register_authored_at_plan_time: true
Block posture: this milestone blocks on HIGH only.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture spec -> gate | Each fixture is untrusted input to `dsx gate`; the corpus asserts the gate catches exactly the intended defect and nothing else. |
| catch-rate readout -> reviewer | The stratified report is the calibration instrument a reviewer trusts; folding easy catches into the headline would falsify it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-02-01 | Tampering | Fixture trips MORE than its target defect (e.g. DSX-VIZ-012/DSX-FIG-001) | high | mitigate | Base on a clean good-corpus spec, keep exactly one bad visual, omit artifact_path/relationship per recipe; test_ship_gate_findings_are_all_documented_incidental_corpus_gaps (:1136) gates any stray HIGH. |
| T-24-02-02 | Repudiation | MEDIUM catch-rate folded into the headline to inflate apparent coverage | high | mitigate | MEDIUM stratum reported BESIDE the headline; headline-invariance assertion (Task 3) proves the headline is unchanged by added caught fixtures. |
| T-24-02-03 | Spoofing | DSX-VIZ-071 mislabeled as a `kind: miss` ABSENT case to force a catch | medium | mitigate | Explicitly forbidden (Task 2); ABSENT partition checks CRITICAL-tier absence only — a firing MEDIUM is not a miss. |
| T-24-02-04 | Tampering | accidental finding-code mint / DSX-VIZ-071 severity change | high | mitigate | Fixtures route only to EXISTING DSX-VIZ-001 / DSX-VIZ-071; no gate-code edit; zero-mint 276->276 asserted in 24-03 Task 1. |
| T-24-02-05 | Tampering | matplotlib imported into an off-gate-path corpus test | high | mitigate | Corpus tests stay stdlib-only (Risk P7); test_gate_path_hermetic (verified in 24-01) keeps matplotlib in FORBIDDEN. |
</threat_model>

<verification>
- `python -m unittest tests.test_known_bad_corpus` green (all strata + total-equality + incidental-gap guards).
- Each fixture passes `dsx validate` and exits 0 at plan+execute.
- The 3 HIGH fixtures fire DSX-VIZ-001 HIGH at verify+ship; the MEDIUM fixture fires DSX-VIZ-071 MEDIUM and classifies as a catch under --block-on MEDIUM.
- Headline (miss-rate, FPR) invariant to the added fixtures; MEDIUM catch-rate reported beside it.
- No new finding code introduced (verified holistically in 24-03).
</verification>

<success_criteria>
- 4 bad-chart fixtures under examples/known-bad/, each with a sibling POSTMORTEM naming its target code.
- _EXPECTED_CAUGHT_DEFECTS has all 4 keys; _HIGH_TARGET_DEFECT_CODES has the 3 HIGH; _MEDIUM_TARGET_DEFECT_CODES has the 1 MEDIUM.
- _gate_findings threads block_on; MEDIUM catch-rate reported beside the headline; headline invariant.
- Full tests.test_known_bad_corpus green; zero codes minted.
</success_criteria>

<output>
Create `.planning/phases/24-portfolio-exemplar-viz-calibration/24-02-SUMMARY.md` when done.
</output>
