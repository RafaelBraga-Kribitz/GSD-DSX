---
phase: 23-style-snippet-layer
plan: 02
type: tdd
wave: 2
depends_on:
  - 23-01
files_modified:
  - templates/dsx_plotstyle.py
  - templates/FIGURE-MANIFEST.yaml
  - tests/test_dsx_plotstyle_api.py
  - tests/test_dsx_plotstyle_determinism.py
  - tests/test_gate_path_hermetic.py
autonomous: true
requirements:
  - REQ-P23-02
  - REQ-P23-03

must_haves:
  truths:
    - "templates/dsx_plotstyle.py exposes finalise_figure(fig, *, title, source, subtitle=None, note=None) -> Figure with source a required keyword with NO default — a call omitting source raises TypeError; direct_label(ax, *, labels=None, color_from_line=True, x_offset=6.0, fontsize=None) -> list[Text]; save_deterministic(fig, path, *, metadata=None, **savefig_kwargs) -> Path that writes an SVG and does NOT hash (GA-2, REQ-P23-02)."
    - "save_deterministic applies the GA-3 recipe: rcParams svg.fonttype='path' and svg.hashsalt='dsx', metadata merged as {'Date': None, **(metadata or {})}, format='svg'; Lato registered via font_manager.fontManager.addfont before the style resolves font.family; a double-render of one figure through save_deterministic yields byte-identical file_sha256 (REQ-P23-03)."
    - "The determinism proof lives in tests/test_dsx_plotstyle_determinism.py, @unittest.skipIf matplotlib absent, off every GATE_PROFILES path, reusing dsx.checks.figures.file_sha256 as the single hasher — save_deterministic itself never hashes (GA-2/GA-3, D-P23-04)."
    - "templates/FIGURE-MANIFEST.yaml records matplotlib_version: \"3.11.1\" at the top level alongside manifest_version (REQ-P23-03); dsx/checks/figures.py does not read the key, so it is additive and mints nothing."
    - "tests/test_gate_path_hermetic.py FORBIDDEN includes \"matplotlib\" and the test stays green — no gate module imports matplotlib today (D-P23-03, REQ-P23-03)."
  artifacts:
    - "templates/dsx_plotstyle.py (finalise_figure, direct_label, save_deterministic, register_fonts)"
    - "templates/FIGURE-MANIFEST.yaml (top-level matplotlib_version field, additive)"
    - "tests/test_dsx_plotstyle_api.py (new: signatures + mandatory-source TypeError, skipIf matplotlib absent)"
    - "tests/test_dsx_plotstyle_determinism.py (new: double-render svg_sha256 equality, skipIf matplotlib absent, off gate path)"
    - "tests/test_gate_path_hermetic.py (edit: FORBIDDEN += \"matplotlib\")"
  key_links:
    - "save_deterministic MUST merge metadata as {'Date': None, **(metadata or {})} — passing metadata=None or omitting the key triggers matplotlib's datetime.today() auto-stamp (verified 23-RESEARCH §4 _write_metadata), which re-stamps a fresh timestamp per render and breaks the hash. The helper owns this default, not the caller (Pitfall 2)."
    - "register_fonts()/addfont must run BEFORE plt.style.use resolves font.family to Lato, or matplotlib silently falls back to DejaVu Sans for the in-flight render (Pitfall 1: the findfont cache is cleared only forward). Ordering is font-register → style.use → draw → savefig."
    - "The determinism test needs styles/fonts/Lato-*.ttf and styles/dsx-urban.mplstyle on disk from Plan 01 — this is the font→helper→test build-order seam (depends_on 23-01)."
  prohibitions:
    - "[unverified — never auto-dismiss] save_deterministic must NOT compute or return a sha256 — dsx seal (stdlib hashlib) stays the single hashing authority (GA-2); a second hasher that could silently diverge from dsx seal is prohibited."
    - "[unverified — never auto-dismiss] Adding \"matplotlib\" to FORBIDDEN must not turn the hermeticity test red — verified no gate module imports matplotlib today; if it ever does, that is a real regression to fix, never a reason to revert the guard (D-P23-03)."
    - "[unverified — never auto-dismiss] Zero finding codes minted; the determinism/api tests carry no report.add and sit off GATE_PROFILES; dsx_plotstyle.py lives in templates/, outside the dsx/ gate closure (D-P23-04, REQ-P23-03)."
---

<objective>
Author `templates/dsx_plotstyle.py` — the analyst-side, matplotlib-only figure-finalisation
helper (off the gate path) — with the three GA-2 keyword-explicit functions, and wire in the
GA-3 SVG-determinism recipe so `FIGURE-MANIFEST.yaml` `svg_sha256` seals are reproducible
across re-render. Prove the recipe with an off-gate-path double-render hash-equality test,
record the pinned `matplotlib_version` in the manifest template, and harden the gate-path
hermeticity guard by adding `"matplotlib"` to its FORBIDDEN set.

This plan implements REQ-P23-02 (the `finalise_figure` / `direct_label` / `save_deterministic`
helper) and REQ-P23-03 (the proven determinism recipe + the double-render test kept off the
gate path + `test_gate_path_hermetic` stays true, hardened per D-P23-03). It is Wave 2: it
consumes Plan 01's vendored Lato font and dsx-urban style.

Purpose: `save_deterministic` is the single render-side production path that makes the
existing `dsx/checks/figures.py` seals (DSX-FIG-010/011/020, unchanged) reproducible. The
mandatory-`source` keyword makes the "every figure cites its source" doctrine a signature
property (omitting it is a TypeError, mirroring DSX-VIZ-062). The single-hasher rule
(save_deterministic writes, `dsx seal` hashes) keeps one source of truth for the hash.

Output: `templates/dsx_plotstyle.py`, a `matplotlib_version` field in
`templates/FIGURE-MANIFEST.yaml`, two new off-gate-path tests, and the hardened
hermeticity FORBIDDEN set. Zero finding codes minted.

## Analytical contract: Not Applicable
Non-analytical style/tooling phase — no metric/model/experiment/decision-rule/statistical
claim. No ANALYSIS-SPEC.yaml; `dsx gate plan --allow-missing` passes clean (dsx.require_spec
disabled). Do not scaffold an analysis.

## Artifacts this plan produces
- `templates/dsx_plotstyle.py` — `finalise_figure`, `direct_label`, `save_deterministic`,
  `register_fonts`
- `templates/FIGURE-MANIFEST.yaml` — top-level `matplotlib_version` field
- `tests/test_dsx_plotstyle_api.py`, `tests/test_dsx_plotstyle_determinism.py` (new)
- `tests/test_gate_path_hermetic.py` — FORBIDDEN += "matplotlib" (edit)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-style-snippet-layer/23-CONTEXT.md
@.planning/phases/23-style-snippet-layer/23-RESEARCH.md

# The single hasher this helper must reuse (never re-implement), and the hermeticity guard:
@dsx/checks/figures.py
@tests/test_gate_path_hermetic.py
# The manifest shape to extend:
@templates/FIGURE-MANIFEST.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write the API test (signatures + mandatory-source TypeError) and the double-render determinism test</name>
  <files>tests/test_dsx_plotstyle_api.py, tests/test_dsx_plotstyle_determinism.py</files>
  <read_first>
    - 23-RESEARCH.md §Code Examples (finalise_figure signature; save_deterministic body sketch) and §5 (font_manager.fontManager.addfont signature + register_fonts helper) and §Pattern 1 (the skipIf-matplotlib determinism test skeleton, which reuses dsx.checks.figures.file_sha256).
    - dsx/checks/figures.py:24-32 (file_sha256 — the single hasher the determinism test must import and reuse; returns "sha256:"+hexdigest).
    - 23-RESEARCH.md §3/§4 (why svg.hashsalt: dsx and metadata={'Date': None} make the bytes deterministic — the mechanism the test proves).
    - 23-CONTEXT.md GA-2 (the three exact signatures; save_deterministic writes but does NOT hash) and GA-3 (the recipe).
    - templates/ has NO __init__.py — the tests must load the module by file path via importlib.util.spec_from_file_location(ROOT/"templates"/"dsx_plotstyle.py"), not `from templates.dsx_plotstyle import ...`.
  </read_first>
  <behavior>
    - API test (RED, guarded @unittest.skipIf matplotlib absent because importing the helper imports matplotlib): loads dsx_plotstyle by file path; asserts inspect.signature(finalise_figure) has keyword-only params title and source with source having NO default (inspect.Parameter.empty), and subtitle/note defaulting to None; asserts calling finalise_figure with title= but no source= raises TypeError; asserts direct_label and save_deterministic exist with their GA-2 keyword-only signatures (save_deterministic has metadata=None and accepts **savefig_kwargs). At RED all fail because dsx_plotstyle.py does not exist.
    - Determinism test (RED, @unittest.skipIf matplotlib absent): registers Lato via the helper's register_fonts (or font_manager.addfont on styles/fonts/Lato-Regular.ttf), applies plt.style.use(ROOT/"styles"/"dsx-urban.mplstyle"), builds one representative figure (e.g. ax.plot([1,2,3],[1,4,9])), calls save_deterministic twice writing into a tempfile.TemporaryDirectory (NEVER into ./figures — that would trip DSX-FIG-040 manifest coverage), and asserts dsx.checks.figures.file_sha256(p1) == file_sha256(p2). At RED it fails because the helper does not exist.
  </behavior>
  <action>
    Create tests/test_dsx_plotstyle_api.py as a stdlib unittest module. Add the matplotlib-availability guard (try: import matplotlib; _MPL=True; except ImportError: _MPL=False) and @unittest.skipIf(not _MPL, "matplotlib not installed — analyst-side only") on the TestCase. Load the helper via importlib.util.spec_from_file_location from ROOT/"templates"/"dsx_plotstyle.py". Use inspect.signature to assert the keyword-only parameter kinds and the source-has-no-default property; assert TypeError on the missing-source call (construct a throwaway Figure via matplotlib.figure.Figure() or pass a bare object — the TypeError fires at call binding before the body runs). Assert the presence + kw-only shape of direct_label and save_deterministic. Add the "Run:" footer.

    Create tests/test_dsx_plotstyle_determinism.py modelled on 23-RESEARCH §Pattern 1: the same matplotlib guard + skipIf, file-path import of the helper, render helper writing into tempfile.TemporaryDirectory, and self.assertEqual(file_sha256(p1), file_sha256(p2)) importing file_sha256 from dsx.checks.figures (insert ROOT on sys.path first). Place a module docstring noting: off the gate path, no report.add, mints no code (D-P23-04). Add the "Run:" footer. Keep every regex/path CRLF-safe. Do NOT soften either assertion — RED is the goal.
  </action>
  <verify>
    <automated>python -m unittest tests.test_dsx_plotstyle_api tests.test_dsx_plotstyle_determinism</automated>
    RED expected: exits non-zero (import/attribute errors — templates/dsx_plotstyle.py does not exist yet). If matplotlib were absent both would SKIP cleanly; here matplotlib 3.11.1 is installed so they run and fail for the documented reason. Confirm before proceeding.
  </verify>
  <acceptance_criteria>
    tests/test_dsx_plotstyle_api.py asserts finalise_figure's keyword-only title/source with source having no default, the missing-source TypeError, and the direct_label/save_deterministic signatures; it loads the helper by file path and is skipIf-guarded. tests/test_dsx_plotstyle_determinism.py double-renders through save_deterministic into a temp dir and asserts file_sha256 equality reusing dsx.checks.figures.file_sha256, skipIf-guarded, off the gate path. Both are RED because the helper does not exist; no assertion softened.
  </acceptance_criteria>
  <done>The API test (signatures + mandatory-source TypeError) and the double-render determinism test exist, are skipIf-matplotlib-guarded and off the gate path, and are RED because templates/dsx_plotstyle.py is not written yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Author dsx_plotstyle.py with the GA-3 recipe, and record matplotlib_version in the manifest</name>
  <files>templates/dsx_plotstyle.py, templates/FIGURE-MANIFEST.yaml</files>
  <read_first>
    - 23-RESEARCH.md §Code Examples (finalise_figure signature; the save_deterministic body wiring svg.hashsalt/svg.fonttype/merged metadata; register_fonts globbing styles/fonts/Lato-*.ttf via _FONT_DIR = Path(__file__).resolve().parent.parent / "styles" / "fonts").
    - 23-RESEARCH.md §3 (svg.hashsalt: dsx makes _make_id deterministic), §4 (metadata={'Date': None} suppresses the timestamp; metadata=None does NOT), §5 (addfont ordering: register before style.use/draw).
    - 23-RESEARCH.md §6 (FIGURE-MANIFEST.yaml current shape; matplotlib_version recommended at the TOP LEVEL alongside manifest_version; figures.py does not read it → additive).
    - dsx/checks/figures.py (confirm the helper does NOT import or call anything here — dsx seal stays the single hasher; save_deterministic writes only).
    - tests/test_dsx_plotstyle_api.py + tests/test_dsx_plotstyle_determinism.py from Task 1 — the exact signatures and determinism property this file must satisfy.
  </read_first>
  <behavior>
    - finalise_figure(fig, *, title, source, subtitle=None, note=None) -> Figure: sets the takeaway title, optional subtitle, mandatory source line (rendered as a footnote/caption), optional note; returns the mutated fig. source is a required keyword with no default (omission is a TypeError at call binding).
    - direct_label(ax, *, labels=None, color_from_line=True, x_offset=6.0, fontsize=None) -> list[Text]: places a label at the terminal (rightmost) point of each Line2D in ax; labels maps a line's label to display text (default uses line.get_label()); colour inherited from the line when color_from_line; x_offset in points; returns the created Text artists.
    - save_deterministic(fig, path, *, metadata=None, **savefig_kwargs) -> Path: sets rcParams svg.fonttype='path' and svg.hashsalt='dsx'; merges metadata as {'Date': None, **(metadata or {})}; calls fig.savefig(path, format='svg', metadata=merged, **savefig_kwargs); returns Path(path). It does NOT hash and does NOT import hashlib.
    - register_fonts() -> None: font_manager.fontManager.addfont(str(ttf)) for each styles/fonts/Lato-*.ttf, called at import time so Lato resolves before any draw.
    - templates/FIGURE-MANIFEST.yaml gains a top-level matplotlib_version: "3.11.1" line alongside manifest_version; figures.py still parses it unchanged (it never reads the key).
  </behavior>
  <action>
    Write templates/dsx_plotstyle.py (matplotlib-only, analyst-side). Module top: import annotations, pathlib.Path, and matplotlib pieces (matplotlib as mpl, matplotlib.pyplot as plt, from matplotlib import font_manager, and typing imports for the return annotations). Define _HASHSALT = "dsx" and _FONT_DIR = Path(__file__).resolve().parent.parent / "styles" / "fonts". Define register_fonts() that addfont()s each sorted styles/fonts/Lato-*.ttf, and CALL it once at module import (so registration precedes any caller's plt.style.use — Pitfall 1). Implement the three GA-2 functions exactly per <behavior>. In save_deterministic, set mpl.rcParams["svg.fonttype"] = "path" and mpl.rcParams["svg.hashsalt"] = _HASHSALT, build merged_metadata = {"Date": None, **(metadata or {})}, then fig.savefig(out, format="svg", metadata=merged_metadata, **savefig_kwargs); return Path(path). Add a short module docstring stating: analyst-side, matplotlib-only, OFF the gate path (templates/, outside the dsx/ closure); save_deterministic writes only — dsx seal (stdlib hashlib) is the single hashing authority (GA-2). Do NOT import or call hashlib / dsx.checks.figures / dsx seal anywhere in this file. Do NOT place any fenced code — write real Python.

    Edit templates/FIGURE-MANIFEST.yaml: add a top-level `matplotlib_version: "3.11.1"` line directly under `manifest_version: 1` (keep the existing shape and comment header intact). This is the reproducibility contract field (REQ-P23-03); it is additive — dsx/checks/figures.py never reads it.
  </action>
  <verify>
    <automated>python -m unittest tests.test_dsx_plotstyle_api tests.test_dsx_plotstyle_determinism -v && python -c "import re,pathlib; t=pathlib.Path('templates/FIGURE-MANIFEST.yaml').read_text(encoding='utf-8'); assert re.search(r'(?m)^matplotlib_version:\s*[\"\x27]?3\.11\.1', t), 'matplotlib_version missing'; print('manifest_version + matplotlib_version present')"</automated>
    Expected: both test modules GREEN (signatures correct; mandatory-source TypeError fires; the double render produces byte-identical svg_sha256); the manifest carries a top-level matplotlib_version: "3.11.1".
  </verify>
  <acceptance_criteria>
    templates/dsx_plotstyle.py defines finalise_figure/direct_label/save_deterministic with the exact GA-2 keyword-only signatures and register_fonts called at import; save_deterministic sets svg.fonttype=path + svg.hashsalt=dsx, merges {'Date': None, ...}, writes SVG, returns Path, and never hashes. The double-render determinism test and the API test are GREEN. templates/FIGURE-MANIFEST.yaml carries top-level matplotlib_version: "3.11.1". No hashlib/dsx-seal call in the helper; no gate code minted.
  </acceptance_criteria>
  <done>The GA-2 helper exists and is green against both the signature/TypeError test and the double-render determinism test; the manifest records the pinned matplotlib version; save_deterministic writes but never hashes.</done>
</task>

<task type="auto">
  <name>Task 3: Harden the gate-path hermeticity guard — add "matplotlib" to FORBIDDEN (D-P23-03)</name>
  <files>tests/test_gate_path_hermetic.py</files>
  <read_first>
    - tests/test_gate_path_hermetic.py:32 (FORBIDDEN = {"pandas", "scipy", "numpy", "csv"}) and :86-142 (the AST closure walk over the dsx/ gate roots resolved from GATE_PROFILES, and the assertion that FORBIDDEN & top_level == set()).
    - 23-CONTEXT.md D-P23-03 (add "matplotlib" — a strictly-strengthening structural guard; safe today because no gate module imports matplotlib; figures.py uses hashlib only).
    - 23-RESEARCH.md §Security Domain (the Elevation-of-Privilege threat this guard closes: a future "render inline on the gate path" pulling matplotlib into the hermetic dsx/ closure).
  </read_first>
  <behavior>
    - FORBIDDEN becomes {"pandas", "scipy", "numpy", "csv", "matplotlib"}. The existing test_no_data_library_reaches_the_gate_path still passes because no dsx/ gate module imports matplotlib (verified: figures.py is hashlib-only; the helper is in templates/, outside the walked closure). The docstring/prose is updated to name matplotlib as a forbidden import and cite D-P23-03/REQ-P23-03.
  </behavior>
  <action>
    In tests/test_gate_path_hermetic.py, add "matplotlib" to the FORBIDDEN set literal. Update the module docstring and the FORBIDDEN comment to record that matplotlib is now forbidden on the gate path per D-P23-03 (REQ-P23-03) — a strictly-strengthening guard that turns a future "just render the chart inline on the gate path" simplification red, safe today because the gate path is stdlib-pure (figures.py hashes with hashlib only; dsx_plotstyle.py lives in templates/, outside the dsx/ AST closure the test walks). Change nothing else — the closure-walk logic and the GATE_PROFILES resolution are already correct and CRLF-tolerant (ast.parse).
  </action>
  <verify>
    <automated>python -m unittest tests.test_gate_path_hermetic -v</automated>
    Expected: GREEN. "matplotlib" is in FORBIDDEN and no gate module's import closure contains it, so test_no_data_library_reaches_the_gate_path passes with the strengthened set; test_profiler_absent_from_dq_closure is unaffected.
  </verify>
  <acceptance_criteria>
    tests/test_gate_path_hermetic.py FORBIDDEN == {"pandas", "scipy", "numpy", "csv", "matplotlib"}; the docstring/comment cites D-P23-03/REQ-P23-03; the full module is GREEN (no gate module imports matplotlib). No other logic changed.
  </acceptance_criteria>
  <done>"matplotlib" is in the hermeticity FORBIDDEN set and the guard stays green — a future inline-render regression on the gate path now turns red structurally.</done>
</task>

</tasks>

<threat_model>
register_authored_at_plan_time: true

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyst render path (templates/) ↔ hermetic gate path (dsx/) | The new helper imports matplotlib (large native surface: FreeType, HarfBuzz, Qhull). It must stay strictly off the gate path; this plan hardens the structural guard that keeps it there. No network, no untrusted input — the functions take in-process Figure/Axes objects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-02 | Elevation of Privilege | A future contributor "just renders inline on the gate path", silently pulling matplotlib (+ native code) into the hermetic dsx/ closure | medium | mitigate | D-P23-03: Task 3 adds "matplotlib" to tests/test_gate_path_hermetic.FORBIDDEN — a structural AST-closure guard that turns the regression red; verified safe today (no gate module imports matplotlib; figures.py is hashlib-only). |
| T-23-04 | Tampering | A second hasher: save_deterministic computing/returning its own sha256 that could silently diverge from `dsx seal` | medium | mitigate | GA-2: save_deterministic writes only and imports no hashlib; the determinism test reuses dsx.checks.figures.file_sha256 (the same stdlib hasher dsx seal uses); the API test asserts the write-only signature. |
| T-23-05 | Tampering | A leaked per-render timestamp (metadata=None mistaken for date-stripping) silently breaking seal reproducibility | low | mitigate | save_deterministic owns the merge {'Date': None, **(metadata or {})} so a caller cannot omit the key (Pitfall 2); the double-render equality test would fail at a different wall-clock second if the date leaked. |

No threat rises to `high`, so nothing blocks under the ASVS level-1 block-on-high policy
(no auth/session/network/untrusted-input surface; V5/V6 marginal per 23-RESEARCH §Security).

**Package legitimacy gate: N/A.** No npm/pip/cargo install — matplotlib is a pre-existing
analyst-side dependency. No T-23-SC threat; no legitimacy checkpoint.
</threat_model>

<verification>
Phase-relevant checks for this plan, in order:
1. Signatures + mandatory source: `python -m unittest tests.test_dsx_plotstyle_api -v` —
   keyword-only title/source, source has no default, missing-source raises TypeError.
2. Determinism proven off the gate path: `python -m unittest tests.test_dsx_plotstyle_determinism -v`
   — double render of one figure yields byte-identical svg_sha256; skipIf matplotlib absent.
3. Hermeticity hardened + green: `python -m unittest tests.test_gate_path_hermetic -v` —
   FORBIDDEN includes matplotlib, no gate module imports it.
4. Zero mint (no edit expected): `python scripts/gen-finding-catalogue.py --check` still exits
   0 at 276 — this plan adds no report.add.
</verification>

<success_criteria>
- templates/dsx_plotstyle.py exposes the three GA-2 keyword-explicit functions; source is a
  required keyword (omission is a TypeError); save_deterministic writes the deterministic SVG
  and never hashes (REQ-P23-02).
- The GA-3 recipe (svg.fonttype path + svg.hashsalt dsx + metadata Date:None + Lato via
  addfont-before-resolve + pinned matplotlib_version in the manifest) is proven by an
  off-gate-path double-render hash-equality test, skipIf matplotlib absent (REQ-P23-03).
- test_gate_path_hermetic FORBIDDEN includes "matplotlib" and stays green (D-P23-03).
- Zero finding codes minted; the new tests carry no report.add and sit off GATE_PROFILES.
</success_criteria>

<output>
Create `.planning/phases/23-style-snippet-layer/23-02-SUMMARY.md` when done.
</output>
