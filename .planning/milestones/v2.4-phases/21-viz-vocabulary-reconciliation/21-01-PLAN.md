---
phase: 21-viz-vocabulary-reconciliation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_viz_vocabulary_invariant.py
  - dsx/spec.py
  - dsx/checks/viz.py
  - dsx/data/input_types.json
  - .planning/HUMAN-QUEUE.md
autonomous: true
requirements:
  - REQ-P21-01
  - REQ-P21-02
  - REQ-P21-03

must_haves:
  truths:
    - "A visual declaring a homed mark on its matching shape (interval-range + histogram, both the coarse-family name and an interval-range input-type id) no longer draws a DSX-VIZ-013 false positive."
    - "The every-mark-has-a-home invariant test passes: no mark in the universe lacks a capability home; every mark has a relationship home or is on the frozen CAPABILITY_ONLY allowlist (D-01, REQ-P21-01)."
    - "Each of the five banned types (3d_bar, 3d_pie, 3d_line, radar, dual_axis_line) is a complete refusal record with non-empty reason, code, and citation (D-02, REQ-P21-02)."
    - "code equals DSX-VIZ-001 for every refusal record, and _check_banned still emits DSX-VIZ-001 with the human-readable reason as its detail (D-02)."
    - "The finding-code catalogue still contains exactly 275 codes; zero codes are minted or altered (REQ-P21-03)."
    - "HQ-27 Tier-3 is annotated with the per-banned-type to citation-source mapping, with the radar gap flagged for S5-2 operator correction (D-02 D-05 handling)."
  artifacts:
    - "tests/test_viz_vocabulary_invariant.py (new repo-integrity test, off the gate path)"
    - "dsx/spec.py (CHART_CAPABILITIES: 9 marks homed across interval-range, categorical-value, composition, categorical-multi, matrix)"
    - "dsx/checks/viz.py (RELATIONSHIP_CHARTS: kde/population_pyramid/butterfly homed; BANNED_TYPES promoted to {reason,code,citation}; _check_banned reads [\"reason\"])"
    - "dsx/data/input_types.json (regenerated so the input-type-id gate path sees the new admissible marks)"
    - ".planning/HUMAN-QUEUE.md (HQ-27 Tier-3 per-banned-type citation-source mapping, radar gap flagged)"
  key_links:
    - "CHART_CAPABILITIES / EXTRA_MARKS edit -> python scripts/gen-input-types.py -> dsx/data/input_types.json -> the input-type-id path of _check_input_type_matrix (Pitfall 1: editing the dict without regenerating leaves the id path stale)."
    - "BANNED_TYPES value-type promotion (str -> dict) -> the single _check_banned reader at dsx/checks/viz.py:86 (the one call site that breaks if the detail= expression is not updated to [\"reason\"])."
---

<objective>
Reconcile the existing chart-mark vocabulary so every non-banned mark is reachable
through the gate's admissibility surface (and, where sensible, the recommendation
surface), turn the five banned marks into first-class refusal entries instead of
silent absences, and prove the finding-code catalogue is unchanged at 275.

This is a code-and-test reconciliation of in-repo Python data structures. It produces
no number, model, or chart, and installs no packages. It implements user decisions
D-01 (every-mark-has-a-home invariant, two clauses + a frozen CAPABILITY_ONLY
allowlist) and D-02 (enrich BANNED_TYPES in place to {reason, code, citation}).

Purpose: Phase 21 hard-blocks Phase 22 — the catalog spine is built directly on this
reconciled vocabulary. Homing the 12 orphans now removes DSX-VIZ-013 false positives
the catalog would otherwise compound; the refusal enrichment makes banned marks
traceable rather than invisible.

Output: one new repo-integrity test, edits to two source vocabularies, a regenerated
generated artifact, a HUMAN-QUEUE annotation, and a green set-identity check at 275.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-viz-vocabulary-reconciliation/21-CONTEXT.md
@.planning/phases/21-viz-vocabulary-reconciliation/21-RESEARCH.md

# Live structures this plan edits (read the exact line spans, do not re-derive):
@dsx/spec.py
@dsx/checks/viz.py
@scripts/gen-input-types.py

# Test idioms to copy (import-live-objects, subTest, hyphenated-script import):
@tests/test_input_types.py
@tests/test_phase20_zero_mint_close.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write the every-mark-has-a-home + refusal-completeness invariant test</name>
  <files>tests/test_viz_vocabulary_invariant.py</files>
  <read_first>
    - 21-RESEARCH.md "Pattern to copy: test idiom" and "Pattern to copy: EXTRA_MARKS is only importable via importlib.util" — the skeleton there is the pattern to adapt, not a file to paste.
    - tests/test_input_types.py — the import-live-objects + self.subTest idiom to copy (structural, NOT the regex-over-Markdown idiom of test_finding_catalogue_invariant.py; these are live Python objects, so there is no CRLF or line-anchoring concern in THIS file).
    - tests/test_phase20_zero_mint_close.py:79-84 — the importlib.util.spec_from_file_location precedent for loading EXTRA_MARKS out of the hyphenated scripts/gen-input-types.py (import scripts.gen_input_types is not valid Python — the filename has hyphens and no __init__.py).
    - tests/test_input_types.py Gate-style spec construction (viz.check on a small spec dict) for the two gate smokes.
  </read_first>
  <behavior>
    - Mark universe = union of marks in RELATIONSHIP_CHARTS values, CHART_CAPABILITIES values, EXTRA_MARKS values, LENGTH_ENCODED, DENSITY_MARKS, STACKED_MARKS, minus the BANNED_TYPES keys.
    - Clause 1 (capability-completeness): every universe mark is in some CHART_CAPABILITIES value OR some EXTRA_MARKS value. At RED this fails, listing the 10 capability-orphans (histogram, density, ecdf, strip, diverging_bar, waterfall, dumbbell, bump, sankey, kde).
    - Clause 2 (relationship-completeness): every universe mark is in some RELATIONSHIP_CHARTS value OR on the frozen CAPABILITY_ONLY allowlist. At RED this fails, listing kde, population_pyramid, butterfly as unhomed-and-not-allowlisted.
    - Allowlist-staleness: no CAPABILITY_ONLY entry already has a relationship home (guards a stale/over-broad allowlist). The 14 allowlist marks are column, grouped_bar, multi_line, bubble, donut, sunburst, icicle, circle_pack, timeline, gantt, big_number, candlestick, ohlc_bar, column_range.
    - Refusal completeness: for every BANNED_TYPES value, it is a dict with non-empty reason, code, and citation. At RED this fails/errors because the values are still plain strings.
    - Refusal code identity: every refusal record's code equals DSX-VIZ-001. At RED this fails.
    - _check_banned detail contract: viz.check on a spec whose visual type is radar produces a DSX-VIZ-001 finding whose detail equals BANNED_TYPES["radar"]["reason"]. At RED this errors (string indexed by "reason").
    - Gate smoke, coarse-family path: viz.check on a visual with data_input_type "interval-range" and type "histogram" produces NO DSX-VIZ-013 finding. At RED this fails (it currently fires).
    - Gate smoke, input-type-id path: viz.check on a visual with data_input_type "IT040" (an interval-range-family id with no EXTRA_MARKS entry) and type "histogram" produces NO DSX-VIZ-013 finding. At RED this fails (IT040 admissible currently excludes histogram — verified against the committed input_types.json).
  </behavior>
  <action>
    Create tests/test_viz_vocabulary_invariant.py as a plain unittest.TestCase module (stdlib unittest is the repo standard — there is no pytest config anywhere in the repo). Import the live objects: RELATIONSHIP_CHARTS, LENGTH_ENCODED, BANNED_TYPES from dsx.checks.viz; CHART_CAPABILITIES from dsx.spec; DENSITY_MARKS, STACKED_MARKS from dsx.checks.smells; and load EXTRA_MARKS from scripts/gen-input-types.py via importlib.util.spec_from_file_location (never call its main(); its module-level code only defines dicts and a main() function, so exec_module is side-effect-free). Define the frozen CAPABILITY_ONLY allowlist (the 14 marks named in <behavior>) as a module-level frozenset with a comment stating it is frozen per D-01 clause 2 and that adding a mark here without justification fails review. Implement _mark_universe(), _capability_homed(), _relationship_homed() helpers exactly as the mark-universe algebra in <behavior> describes. Write the assertions in <behavior> as separate test methods across two classes (one for the homing clauses + allowlist staleness, one for refusal completeness + code identity), using self.subTest per banned mark. Add the two gate smokes and the _check_banned detail assertion by constructing small spec dicts and calling viz.check, modeled on the Gate pattern in tests/test_input_types.py; assert on finding codes present/absent. Do NOT weaken any assertion to make RED pass — RED is the goal for this task. Include a "Run: python -m unittest tests.test_viz_vocabulary_invariant -v" footer comment per the repo convention. This test is off the gate path by construction (tests/ is never in dsx.cli GATE_PROFILES' import closure).
  </action>
  <verify>
    <automated>python -m unittest tests.test_viz_vocabulary_invariant</automated>
    RED expected: the command exits non-zero. The capability-completeness failure names the 10 orphans; the relationship-completeness failure names kde/population_pyramid/butterfly; the refusal-completeness assertions fail or error against the still-string BANNED_TYPES values; both gate smokes fail. Confirm the failure messages match these expectations before moving on.
  </verify>
  <acceptance_criteria>
    tests/test_viz_vocabulary_invariant.py exists, imports the six live structures plus EXTRA_MARKS via importlib, defines the frozen 14-mark CAPABILITY_ONLY allowlist, and fails for the documented reasons (orphans present, refusal records not yet enriched, gate smokes still firing). No assertion has been softened to force a pass.
  </acceptance_criteria>
  <done>The new invariant test exists and is RED for the documented reasons (10 capability orphans, kde/population_pyramid/butterfly unhomed, string-valued refusal records, both gate smokes firing); no assertion softened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN, D-01): Home the 12 orphan marks and regenerate the input-type artifact</name>
  <files>dsx/spec.py, dsx/checks/viz.py, dsx/data/input_types.json</files>
  <read_first>
    - dsx/spec.py:296-325 (CHART_CAPABILITIES current membership).
    - dsx/checks/viz.py:17-28 (RELATIONSHIP_CHARTS current membership).
    - 21-RESEARCH.md "Homing guidance table, with exact target line spans" and Pitfalls 1, 2, and 4.
  </read_first>
  <behavior>
    - After the edits, _capability_homed() covers every universe mark (Clause 1 green) and _relationship_homed() plus CAPABILITY_ONLY covers every universe mark (Clause 2 green).
    - After regeneration, IT040's admissible set in dsx/data/input_types.json includes histogram (and the other interval-range additions), so the input-type-id gate smoke passes.
  </behavior>
  <action>
    Implement the homing per D-01, choosing the narrowest single family per mark (Pitfall 4: pick one family per mark, not both a primary and an alt). In dsx/spec.py CHART_CAPABILITIES: add histogram, density, ecdf, strip, kde to "interval-range"; add diverging_bar to "categorical-value"; add waterfall to "composition" (chosen over the categorical-value alternative — a waterfall is a cumulative bridge of contributions to a total, a composition signature); add dumbbell and bump to "categorical-multi" (bump chosen over the time-series alternative — a rank series across ordered categorical steps is a multi-category signature; dumbbell carries two values per category); add sankey to "matrix" (a weighted adjacency / edge list, the sibling of chord). In dsx/checks/viz.py RELATIONSHIP_CHARTS: add kde and population_pyramid to "distribution" (population_pyramid chosen over the comparison alternative — mirrored age-band distributions); add butterfly to "comparison". Do NOT add population_pyramid or butterfly to any CHART_CAPABILITIES family — they are already capability-homed via EXTRA_MARKS["IT011"], and widening a base family for them is the over-widening D-01 warns against (Pitfall 2). No new EXTRA_MARKS key is needed — every capability addition lands in an existing base family. Then run python scripts/gen-input-types.py and commit the regenerated dsx/data/input_types.json (Pitfall 1: the input-type-id path of _check_input_type_matrix reads the static generated JSON, not CHART_CAPABILITIES live, so without this step the id path stays stale even though the coarse-family path already works). These additions widen admissibility to fix existing false positives and introduce no false negative and no new code, per D-01.
  </action>
  <verify>
    <automated>python scripts/gen-input-types.py && python -m unittest tests.test_viz_vocabulary_invariant.TestEveryMarkHasAHome tests.test_input_types</automated>
    Expected: regeneration succeeds; the homing-clause + allowlist-staleness tests pass; both gate smokes (coarse-family and IT040 input-type-id) pass; the existing input-type suite stays green. The refusal-completeness tests are still expected to fail here (that is Task 3's work).
  </verify>
  <acceptance_criteria>
    All 12 orphans are homed exactly as specified (9 into CHART_CAPABILITIES, 3 relationship homes with kde spanning both). population_pyramid/butterfly are untouched in CHART_CAPABILITIES. dsx/data/input_types.json is regenerated and its IT040 record now admits histogram. TestEveryMarkHasAHome passes; tests.test_input_types passes.
  </acceptance_criteria>
  <done>The 12 orphans are homed per D-01 (primary family only), input_types.json is regenerated so IT040 admits histogram, and TestEveryMarkHasAHome plus tests.test_input_types are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN, D-02 + REQ-P21-03): Enrich the refusal entries, fix the one reader, prove zero new codes</name>
  <files>dsx/checks/viz.py, .planning/HUMAN-QUEUE.md</files>
  <read_first>
    - dsx/checks/viz.py:36-42 (BANNED_TYPES current shape) and 80-89 (_check_banned — the sole reader whose line 86 detail= expression must change).
    - 21-RESEARCH.md "D-02: BANNED_TYPES enrichment", "D-02: _check_banned one-line change", and Pitfall 5 (the radar citation gap).
    - .planning/HUMAN-QUEUE.md HQ-27 and .planning/v2.4-D05-EVIDENCE-PACK.md Tier-3 table — the citation sources to point at.
  </read_first>
  <behavior>
    - Every BANNED_TYPES value becomes a dict with non-empty reason, code, citation (refusal-completeness green); every code equals DSX-VIZ-001 (code-identity green).
    - viz.check on a radar visual still yields a DSX-VIZ-001 finding whose detail equals the reason string (the _check_banned detail contract green; the existing viz gate unbroken).
    - The finding-code catalogue still holds exactly 275 codes after the change (REQ-P21-03).
  </behavior>
  <action>
    Promote BANNED_TYPES in dsx/checks/viz.py from dict[str, str] to dict[str, dict[str, str]] in place (D-02). For each of the five keys, keep the existing distortion string as reason, set code to DSX-VIZ-001 (the code _check_banned already emits — this is a cross-reference to an existing code, not a new one), and set a non-empty citation drawn from the HQ-27 Tier-3 refusal-doctrine pack: dual_axis_line -> Muth 2018 (Datawrapper), noting see-also DSX-VIZ-030 for its separate dual-axis property check; 3d_bar / 3d_pie / 3d_line -> Tufte 1983 and Munzner 2014 ch.6 (no-unjustified-3D / chartjunk doctrine). For radar, populate a best-fit citation now (Tufte / Munzner proportional-encoding doctrine, matching radar's own "area scales with the square of the value" reason) but treat it as provisional — there is no exact Tier-3 source pre-mapped to radar in the evidence pack (Pitfall 5); do not silently reuse another mark's citation as if it were exact. Change exactly the detail= expression at dsx/checks/viz.py:86 to read BANNED_TYPES[chart_type]["reason"]; leave the membership check on line 81 unchanged (in tests dict keys, unaffected by the value-type promotion). Then annotate HQ-27 Tier-3 in .planning/HUMAN-QUEUE.md with the per-banned-type to citation-source mapping and explicitly flag the radar entry as the least-certain of the five for the operator to confirm or supply a better source at S5-2 (this is D-02's non-blocking D-05 handling — Phase 21 ships without the sign-off because DSX-VIZ-001 already fires and the reason strings shipped long ago). REQ-P21-03 holds mechanically: scripts/gen-finding-catalogue.py extract() never reads the detail= kwarg for code derivation, so this edit cannot mint or alter a code — verify that property by re-running the catalogue oracle below.
  </action>
  <verify>
    <automated>python -m unittest tests.test_viz_vocabulary_invariant tests.test_finding_catalogue_invariant tests.test_gen_finding_catalogue</automated>
    Expected: the full new invariant module is green (both homing clauses, allowlist staleness, refusal completeness, code identity, the _check_banned detail contract, and both gate smokes); the catalogue invariant still reports 275 codes; the generator still matches the committed catalogue (zero mint, REQ-P21-03).
  </verify>
  <acceptance_criteria>
    All five BANNED_TYPES entries are complete {reason, code, citation} records with code = DSX-VIZ-001. _check_banned reads ["reason"] and the existing viz gate still fires DSX-VIZ-001 with the reason as detail. tests.test_viz_vocabulary_invariant is fully green. tests.test_finding_catalogue_invariant and tests.test_gen_finding_catalogue pass at 275 codes. HUMAN-QUEUE.md HQ-27 carries the per-banned-type citation mapping with the radar gap flagged for S5-2.
  </acceptance_criteria>
  <done>All five refusal records are complete with code DSX-VIZ-001, _check_banned reads ["reason"] with the viz gate unbroken, the full invariant module is green, the catalogue holds 275 codes (zero mint), and HQ-27 is annotated with the radar gap flagged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase has essentially no runtime trust boundary. It edits static, in-repo Python
vocabulary data structures (`CHART_CAPABILITIES`, `RELATIONSHIP_CHARTS`, `BANNED_TYPES`),
regenerates one static generated artifact (`dsx/data/input_types.json`), and adds one
off-gate-path test. There is no network input, no untrusted input parsed, no
authentication, no session, and no file or process I/O introduced. The `dsx` checks
operate on an already-loaded declared spec; this phase does not touch loading or parsing.

| Boundary | Description |
|----------|-------------|
| (none introduced) | No new data crosses any trust boundary in this phase. The only "input" is the repo's own static vocabularies, edited by a developer under review. |

## STRIDE Threat Register

The honest threat surface is small. The two real integrity concerns are drift in the
vocabulary itself, and they are caught by the invariant test at commit time — not by any
runtime control. Stated plainly rather than inflated.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Tampering | `CHART_CAPABILITIES` / `RELATIONSHIP_CHARTS` (a future mark added with no capability home, and no relationship home and no CAPABILITY_ONLY allowlist entry) | medium | mitigate | The every-mark-has-a-home invariant test (Task 1) turns red at commit time on exactly this drift class — the mitigation is a test, not a runtime guard (D-01, REQ-P21-01). |
| T-21-02 | Repudiation | `BANNED_TYPES` refusal record shipping with an empty reason/code/citation (an unfounded ban with no traceable justification) | medium | mitigate | `TestRefusalEntryCompleteness` asserts all three fields non-empty for all five banned types and code = DSX-VIZ-001 (Task 1/Task 3, D-02, REQ-P21-02). |
| T-21-03 | Tampering | finding-code catalogue (a code silently minted or altered by the `BANNED_TYPES` value-type change) | low | mitigate | REQ-P21-03 set-identity: `gen-finding-catalogue.py::extract()` never reads `detail=`, so the edit cannot mint a code; `test_finding_catalogue_invariant` + `test_gen_finding_catalogue` confirm 275 -> 275 (Task 3). |

No threat rises to `high`, so nothing blocks under the ASVS level-1 block-on-high policy.

**Package legitimacy gate: N/A.** This phase installs no npm/pip/cargo packages — there is
nothing to audit against a registry, so no `T-21-SC` supply-chain threat and no legitimacy
checkpoint apply.
</threat_model>

<verification>
Phase-level checks, in order:

1. Full suite green (per-wave gate): `python -m unittest discover -s tests -v` — the
   existing suite (121 tests per README) plus the new invariant module, all passing.
2. REQ-P21-01 (homing): `python -m unittest tests.test_viz_vocabulary_invariant.TestEveryMarkHasAHome -v` — capability + relationship clauses and allowlist-staleness green; both gate smokes (coarse-family `interval-range`+`histogram` and input-type-id `IT040`+`histogram`) produce no DSX-VIZ-013.
3. REQ-P21-02 (refusal): `python -m unittest tests.test_viz_vocabulary_invariant.TestRefusalEntryCompleteness -v` — all five refusal records complete, code = DSX-VIZ-001, and the `_check_banned` detail contract holds.
4. REQ-P21-03 (zero new codes / set-identity): `python -m unittest tests.test_finding_catalogue_invariant tests.test_gen_finding_catalogue -v` — the catalogue oracle reports 275 codes two ways and the generator still matches the committed catalogue. Belt-and-suspenders CRLF-safe count (any shell): a Python read of `references/finding-codes.md` matching `(?:DSX-[A-Z]+-[0-9]+|SQL-[0-9]+)` into a set must yield exactly 275 (verified 275 at baseline this session).
5. HUMAN-QUEUE annotation present: HQ-27 Tier-3 carries the per-banned-type citation-source mapping with the radar gap flagged for S5-2.
</verification>

<success_criteria>
- The 12 orphans (histogram, density, ecdf, strip, diverging_bar, waterfall, dumbbell, bump, sankey, kde, population_pyramid, butterfly) are homed exactly per D-01; the invariant test proves no mark in the universe is unaccounted for.
- The five banned types are complete {reason, code, citation} refusal records; `_check_banned` still emits DSX-VIZ-001 with the reason as detail.
- `dsx/data/input_types.json` is regenerated so the input-type-id gate path admits the homed marks (no stale disagreement with the coarse-family path).
- Finding-code catalogue unchanged at 275 (zero new codes), proven by the existing oracle plus the mechanical extract() property.
- HQ-27 Tier-3 annotated with the radar gap flagged; no blocking checkpoint (D-02 non-blocking D-05 path).
- Full `tests/` suite green.
</success_criteria>

<output>
Create `.planning/phases/21-viz-vocabulary-reconciliation/21-01-SUMMARY.md` when done.
</output>
