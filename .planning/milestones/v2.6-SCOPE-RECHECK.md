# v2.6 Scope re-check (S0-2)

**Run:** 2026-09-06, headless ceremony firing, against live HEAD `1232859`
(`gsd/v2.6.0-exploration-depth`, one commit past the milestone-open state the scope
was written on). **Interpreter for the catalogue/spec measurements:**
`C:\Users\Benutzer1\AppData\Local\Programs\Python\Python312\python.exe`.

**Verdict in one line:** the scope in `.planning/research/V2.6-SCOPE.md` still holds
against the live tree — every material premise re-measured and confirmed, one minor
table imprecision noted (immaterial to any requirement), zero contradictions. **This
milestone needs execution, not a fresh scoping round.**

---

## 1. Premise re-verification (V2.6-SCOPE.md §2 live-tree facts)

| # | Premise (scope §2) | Where re-measured | Live value | Verdict |
|---|---|---|---|---|
| 1 | Profiler output keys are the shallow set only (`row_count`, per-col `null_rate`/`n_unique`/`dtype`, `primary_key`, `primary_key_unique`, `duplicate_rate`, `time{column,min,max,max_gap_days}`, `sentinels_found`, `source_hash`, `computed_by`, `profile_version`) | `dsx/profiler.py::profile_csv` return dict (L197–209) | Exactly that set (+`source_path`, which §2 omitted). **No** numeric five-number summary, categorical shares, `rows_per_day`, `--unit` or `--target` block exist today. | STILL-VALID |
| 2 | DQ assertion vocabulary + codes are frozen | `dsx/checks/dq.py` | Codes `DSX-DQ-001,010,020,030,040,050,060`; assertion keys read: `row_count`, `primary_key`, `max_null_rate`, `max_gap_days`, `banned_sentinels` | STILL-VALID (see Discrepancy A re `time_column`) |
| 3 | Explore-skill steps naming profiler-absent numbers exist (1a, 2, 3a, 4a, 4b, 4e, 4f) | `skills/dsx-explore-data/SKILL.md` | Headings present: 1a (L83), 1b (L115), 3a (L159), 4a (L185), 4b (L202), 4c (L227), 4d (L260), 4e (L296), 4f (L311); step 2 (null cross-tabs) referenced | STILL-VALID |
| 4 | Five skills have no named-input read step | `skills/{dsx-scope-analysis,dsx-define-metrics,dsx-design-experiment,dsx-build-model,dsx-narrate}/SKILL.md` | Read-cue hits = 0 for all five; `dsx-explore-data` = 3 (the only consumer today) | STILL-VALID |
| 5 | `LEAKAGE_PATTERNS` count = 10 | `dsx/checks/ml.py` (L28–…) | 10 `re.compile` entries | STILL-VALID |
| 6 | `DSX-MET-030`/`031` thresholds (all-oppose → 030 CRIT; ≥half → 031 HIGH; fewer → ok) | `dsx/checks/metrics.py::_check_simpsons_paradox` | L316 `len(opposing)==len(effects)`→030; L334 `len(opposing)>=len(effects)/2`→031. **1-of-4 opposing fires neither** → Phase 29 premise holds | STILL-VALID |
| 7 | `DSX-CLM-070` (claims), `DSX-STA-012` (stats), `DSX-REP-061` (repro) exist | resp. files | All three present | STILL-VALID |
| 8 | Corpus counts 39 known-bad / 15 good-control | `examples/known-bad/`, `examples/good-corpus/` | 39 `*-ANALYSIS-SPEC.yaml` known-bad; 15 good-corpus | STILL-VALID |
| 9 | `_SECTION_65_ITEM_IDS` holds items 7, 8, 9 (exactly nine members) | `tests/test_known_bad_corpus.py:832` | 9 members incl. `6.5-item-7-feature-provenance`, `6.5-item-8-magnitude-without-computed-effect`, `6.5-item-9-subgroup-harm-declaration` | STILL-VALID |
| 10 | `_SECTION_65_BACKLOG_CODES` = {PAR-020/021/022/030} | `tests/test_known_bad_corpus.py:819` | Exactly those four | STILL-VALID |
| 11 | Live catalogue = 276 (re-measure, do NOT assume) | `scripts/gen-finding-catalogue.py --check`; `references/finding-codes.md` | "finding catalogue is current"; 276 `DSX-` rows counted directly. 3 pre-existing "declared twice" warnings (SPEC-070, VAL-021, VAL-060) — informational, non-fatal | STILL-VALID |
| 12 | Corpus spec count = 42 | `tests/test_dsx.py:579` | `assertEqual(len(paths), 42)` ("count updated from 23 to 42") | STILL-VALID |
| 13 | `node install.mjs --check` passes at open | (deferred to S1/S2 phase gates per brief §5) | not re-run this firing — installer sync is each touching phase's own gate | DEFERRED (not an S0 gate) |

## 2. Per-requirement verdict

Legend: **still-valid** = scope premise holds, requirement is executable as written;
**already-satisfied** = live tree already delivers it (would be dropped); **contradicted**
= a premise is false and the requirement needs re-scoping.

| Requirement | Verdict | Basis |
|---|---|---|
| REQ-P25-01 | still-valid | Premise 1: none of the additive keys exist yet |
| REQ-P25-02 | still-valid | No reference-value fixtures / cited quantile type today; Hyndman & Fan type is an open plan-time fact |
| REQ-P25-03 | still-valid | Premises 2, 3, 11: dq.py + vocabulary frozen, 276 baseline live, ripple targets exist |
| REQ-P26-01 | still-valid | Premise 4: all five skills lack a read step |
| REQ-P26-02 | still-valid | No repo-integrity test parses skill read steps today |
| REQ-P26-03 | still-valid | Skill-only boundary intact |
| REQ-P27-01 | still-valid | No feature-origin-only known-bad case in the corpus (39 specs enumerated) |
| REQ-P27-02 | still-valid (conditional) | Premise 5: `LEAKAGE_PATTERNS` name-only, so a pre-joined innocuous column is a plausible live miss; D-13 measures first |
| REQ-P27-03 | still-valid | Harness slots (`_SECTION_65_*`, spec count 42) present and countable |
| REQ-P28-01 | still-valid | No magnitude-without-test case in corpus; CLM-070/STA-012 present to clear |
| REQ-P28-02 | still-valid (conditional) | Premise 7: REP-061 precedent exists; check is additive |
| REQ-P28-03 | still-valid | Harness slots present |
| REQ-P29-01 | still-valid | Premise 6: 1-of-4 opposing fires neither MET-030 nor 031 → structural gap is real |
| REQ-P29-02 | still-valid | D-05 source (Gail & Simon) + documented case are open research/human-read items, not yet satisfied |
| REQ-P29-03 | still-valid (conditional) | `decision.subgroup_harm[]` absent today; conditional on P29-01 miss + source confirmed |
| REQ-P30-01 | still-valid | Terminal re-baseline; no prior v2.6 measurement exists |
| REQ-P30-02 | still-valid | Prereqs (catalogue check exit 0, installer check, suite) are the phase gate |
| REQ-P30-03 | still-valid | Set-identity check is the gate |

**Tally: 18/18 still-valid (5 conditional on the D-13 live-miss measurement, exactly as
the scope frames them). 0 already-satisfied. 0 contradicted.**

## 3. Discrepancies found

- **A — `time_column` is not a literal DQ assertion key (minor, immaterial).**
  Scope §2 lists the DQ assertion vocabulary as `row_count, primary_key,
  max_null_rate, time_column, max_gap_days, banned_sentinels`. In `dsx/checks/dq.py`
  the time check reads `max_gap_days` from `data[].assertions` and the `time` block
  from the profile; there is **no** assertion key literally named `time_column`
  (`grep time_column dsx/checks/dq.py` → no hit). The scope's `time_column` conflates
  the profile's `time.column` field with an assertion key. This does not touch any
  requirement: the material premise — the assertion vocabulary is FROZEN and no gate
  reads a new profile key this milestone (D-01/D-02, REQ-P25-03) — is intact either
  way. No action; noted for accuracy.
- **B — `source_path` omitted from the scope §2 profiler-keys list (cosmetic).**
  `profile_csv` also returns `source_path`; §2's enumeration left it out. It is an
  existing key and thus falls under the byte-stability guard like the rest. No action.

## 4. Conclusion

The scope is executable as written. No premise is contradicted; no requirement is
already satisfied; the two discrepancies are cosmetic and touch no gate. Planning may
proceed on `.planning/research/V2.6-SCOPE.md` and `.planning/REQUIREMENTS.md` without a
re-scoping round. The one open dependency is the D-05 citation pack (S0-3), which gates
only Phases 27–29 row-by-row and is non-blocking for Phases 25–26.
