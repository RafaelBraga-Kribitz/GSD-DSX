# v2.2 Analytic Surface — scope recheck against the shipped v2.0.0 tree

**Ledger unit:** S0-2. **Date:** 2026-08-28. **HEAD at recheck:** `4ae156f`
(branch `gsd/v2.2.0-analytic-surface`, up to date with origin).

**Why this exists.** The 23 v2.2 requirements (REQ-P13-01 … REQ-P16-04) and every
phase's success criteria were written at v2.0.0 planning time (2026-08-26), before
v2.0.0 actually shipped. Before any planning is done on top of them, each requirement
is re-read against the **live shipped tree** to record whether v2.0.0 has already
**satisfied**, **contradicted**, or **made obsolete** it. Per LOOP-BRIEF §4, any
amendment would be loud and recorded (and a scope change escalated to HUMAN-QUEUE);
this recheck found **none**.

**Method.** Each requirement carries at least one falsifiable premise about a file,
symbol, config key, or fixture. Each premise below was checked directly against the
tree (`grep`/`ls`/`Read`, and `gen-finding-catalogue.py --check` run for real), not
inferred. Evidence locators are inline. The leftover `.claude/worktrees/…` scratch
checkout was excluded from all scans — it is an untracked stale worktree, not source.

**Sources rechecked:** `.planning/REQUIREMENTS.md` (lines 317–402) and
`.planning/ROADMAP.md` (lines 52–165).

---

## Verdict summary — 23/23 still-valid, 0 amendments

| Requirement | Verdict | One-line basis |
|---|---|---|
| REQ-P13-01 | still-valid | 4 target skills **absent**; `capability.json` registers the other 15 dsx skills/agents, ready to extend |
| REQ-P13-02 | still-valid | `dsx-explore-data` ships; spec has `assumptions:` (fixture L281) + `results.tests` (L221/234) to map into; no hypothesis register yet |
| REQ-P13-03 | still-valid | `dsx-narrate` ships; What/So-What/Now-What shape not yet required |
| REQ-P13-04 | still-valid | `dsx-scope-analysis` ships; `docs/gsd-tiers.md` has Tier 0/1/2; engagement-mode routing not yet wired |
| REQ-P13-05 | still-valid | `reproducibility.entrypoint` field exists (fixture L202–203, an `analysis/*.py` script); executor "prefer script over notebook" rule not yet asserted |
| REQ-P13-06 | still-valid | `references/finding-codes.md` + `gen-finding-catalogue.py --check` (exit 0) are the Phase 12 baseline to diff against |
| REQ-P14-01 | still-valid | `docs/dsx/learnings/` **absent**; plan-pre search not yet wired |
| REQ-P14-02 | still-valid | `DATA-PROFILE.yaml` is produced (referenced in brief + executor fragment + fixtures); no `DATA-DICTIONARY.md` beside it yet |
| REQ-P14-03 | still-valid | `dsx.domain` enum includes `research` (capability.json L112–116); disclosure block not yet offered |
| REQ-P14-04 | still-valid | no `data_storage/` folder (good); slash aliases not yet added |
| REQ-P14-05 | still-valid | conditional — GSD-Core overlay-hook question deferred to S2-1 discuss per ordering constraint; neither branch taken yet |
| REQ-P14-06 | still-valid | negative scope bound; current catalogue is the baseline |
| REQ-P15-01 | still-valid | `VARIANCE_ADJUSTMENTS` = exactly the 4 named members, **no `cuped`** (`dsx/spec.py:264`); `dsx vocab` command exists (`cli.py:499,1057`) |
| REQ-P15-02 | still-valid | no CUPED covariate check / citation present; `dsx gate plan` path exists to extend |
| REQ-P15-03 | still-valid | no cohort-grain / funnel-step spec fields yet (`funnel` appears only as a chart type, `spec.py:312`); good/bad D-08 fixtures present |
| REQ-P15-04 | still-valid | no survivorship/changing-denominator code exists; `brief.md §6.5` parking lot exists (L364) |
| REQ-P15-05 | still-valid | `research` + `marketing_science` domains both in enum; no APA template yet |
| REQ-P15-06 | still-valid | **no `shapiro` reference anywhere in `skills/` or `dsx/`**; `references/test-selection.md` already encodes the required order (independence→variance→normality, L56–59) and warns against the auto-switch (L62) |
| REQ-P15-07 | still-valid | `gen-finding-catalogue.py --check` runs and exits 0; canonical fixtures present |
| REQ-P16-01 | still-valid | `dsx-reproduce` skill **absent**; `reproducibility.entrypoint` + `results.tests` exist to compare against |
| REQ-P16-02 | still-valid | **gate path is currently pure** — `dsx/frame` and `dsx/checks` import no pandas/scipy/numpy; must stay so |
| REQ-P16-03 | still-valid | no `protocol_adherence` field in `dsx`/`examples`/`tests`; Phase 12 corpus present (`examples/good-corpus`, `examples/known-bad`) |
| REQ-P16-04 | still-valid | the asserting "no module executes the entrypoint" test does not yet exist |

**Legend:** *still-valid* = the requirement describes real work not yet done, on
premises that still hold. *already-satisfied* / *needs-amendment* — none found.

---

## Premise evidence by phase

### Phase 13 (skill-only, 6 reqs)
- The four new skills `dsx-cohort`, `dsx-funnel`, `dsx-root-cause`, `dsx-segment`
  are **absent** from `skills/` (present: build-model, chart-audit, define-metrics,
  design-experiment, explore-data, narrate, review-analysis, scope-analysis,
  visualize). `capabilities/dsx/capability.json` registers 15 dsx skills/agents — the
  new four are unregistered, exactly as REQ-P13-01 requires them to become.
- Spec fields the P13 skills fill already exist: `assumptions:` (fixture L281),
  `results.tests` (L221/234), `reproducibility.entrypoint` (L202–203).
- `docs/gsd-tiers.md` defines Tier 0 (exploratory) / Tier 1 (published artifact) /
  Tier 2 (code others run). REQ-P13-04's lookup/ad-hoc/full-pipeline routing maps
  engagement modes *onto* these three tiers — coherent, not contradicted.
- Baseline for the REQ-P13-06 no-new-codes diff is `references/finding-codes.md`
  driven by `scripts/gen-finding-catalogue.py`.

### Phase 14 (compounding + onboarding, 6 reqs)
- `docs/dsx/learnings/` does not exist yet (REQ-P14-01 to create).
- `DATA-PROFILE.yaml` is a real produced artifact; no sibling `DATA-DICTIONARY.md`.
- `dsx.domain` is an enum (`auto`, `experimentation`, `machine_learning`,
  `business_intelligence`, `marketing_science`, `research`) — the `research` and
  `marketing_science` branches REQ-P14-03/-15-05 need both exist.
- No `data_storage/` special folder (REQ-P14-04 forbids one).
- REQ-P14-05 stays conditional; its GSD-Core overlay-hook determination is an
  ordering-constraint item for the S2-1 discuss round, not something the shipped
  tree decides. Recorded here as still-open, not amended.

### Phase 15 (new codes + D-05, 7 reqs)
- `VARIANCE_ADJUSTMENTS = {"cluster_robust", "delta_method", "bootstrap_cluster",
  "mixed_effects"}` at `dsx/spec.py:264` — exactly the four members REQ-P15-01 names
  as the pre-existing set, and **no `cuped`**. The requirement's premise holds intact.
- `dsx vocab` command exists (`dsx/cli.py:499`, parser `:1057`).
- No `cuped` / `survivorship` / `changing-denominator` finding code exists in the
  catalogue or `dsx/checks` — the P15 codes are genuinely new (D-06 minting is real
  future work, correctly deferred to S4-1).
- `brief.md §6.5` "Gated backlog (D-13)" (L364) is the parking lot REQ-P15-04 sends
  an uncited code back to.
- **REQ-P15-06 (no Shapiro–Wilk auto-switch):** grep for `shapiro` across `skills/`
  and `dsx/` returns nothing — the codebase currently commits no violation, so the
  requirement is a *negative assertion still to be pinned by a test*, not something
  already-satisfied-and-provable. `references/test-selection.md` already documents the
  correct precedence and explicitly names the Shapiro-Wilk anti-pattern.

### Phase 16 (re-run verification, off the gate path, 4 reqs)
- `dsx-reproduce` skill is **absent**.
- **Gate purity holds now:** neither `dsx/frame` nor `dsx/checks` imports pandas,
  scipy, or numpy. REQ-P16-02/-04's hard constraint (gate never executes analysis
  code) is currently true and the phase's job is to keep it true under test.
- No `protocol_adherence` field yet; the Phase 12 corpus survives the ship
  (`examples/good-corpus`, `examples/known-bad`) so REQ-P16-03 has cases to tag.

---

## Observations recorded for later firings (context, NOT scope amendments)

1. **`DSX-REP-*` already exists (11 codes).** The catalogue's `DSX-REP` family is
   live (Phase 4 provenance). This is directly material to the **S3-1 open question**
   — whether REQ-P16-02's `REPRO-REPORT.md` gate check extends the existing
   `DSX-REP-*` family or mints a new code. It does not change scope; it just means the
   "extend existing family" option S3-1 weighs is concretely available. Decide it in
   the S3-1 persona round, loudly, per the ledger.
2. **Pre-existing catalogue warnings.** `gen-finding-catalogue.py --check` exits 0
   ("finding catalogue is current") but prints non-fatal warnings that `DSX-SPEC-070`,
   `DSX-VAL-021`, and `DSX-VAL-060` are each declared twice with different text. These
   are in the **shipped v2.0.0 tree**, not introduced by v2.2. Recorded so the S4-5 /
   REQ-P15-07 `--check` gate is not misread as a Phase 15 regression when these same
   warnings reappear — the gate contract is exit code 0, which is met.
3. **`DSX-COH` family ≠ cohort analysis.** The existing `DSX-COH` codes are coherence
   (Phase 1), not the Phase 13/15 cohort-analysis work — no naming collision, but
   note it before a P15 cohort code reaches for a `COH` prefix.

## Conclusion

All 23 v2.2 requirements are **still-valid** against the shipped v2.0.0 tree. None was
already-satisfied, contradicted, or made obsolete. **Zero amendments**, therefore no
§4 loud amendment and no HUMAN-QUEUE scope escalation. The milestone needs execution,
not re-scoping — planning may proceed on the inherited requirements as written.
