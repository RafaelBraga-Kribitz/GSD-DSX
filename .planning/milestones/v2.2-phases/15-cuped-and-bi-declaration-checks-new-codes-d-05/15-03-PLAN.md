---
phase: 15-cuped-and-bi-declaration-checks-new-codes-d-05
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/APA-TABLE-research.md
  - tests/test_apa_template.py
  - tests/test_no_shapiro_autoswitch.py
autonomous: true
requirements: [REQ-P15-05, REQ-P15-06]
tags: [dsx, template, apa, research-domain, no-mint, shapiro-negative-assertion, test-selection, D-07]

must_haves:
  truths:
    - "`templates/APA-TABLE-research.md` exists as an OPTIONAL research-domain APA-style statistical results table template (M ± SD / test statistic / df / p / effect size / CI columns and APA notes), mirroring the house style of the existing domain-suffixed `templates/DISCLOSURE-research.md`; it mints no finding code and the marketing-domain ship path still requires narrative + sealed figure + claim evidence (existing NAR/FIG/CLM codes, unchanged) (REQ-P15-05)."
    - "A negative-assertion test pins that no skill or gate auto-switches a test on Shapiro-Wilk: it asserts `references/test-selection.md`'s assumption order is fixed independence → equal variance → normality (lines 54-63), that the continuous 2-group recommendation is Welch UNCONDITIONALLY with no branch on a computed variance test, and that normality is a DECLARED shape+n property, never a tool-run test output (REQ-P15-06 / D-07)."
    - "The gate + skill decision surface (dsx/ and skills/) contains no normality-test CALL: a CRLF-safe grep for `scipy.stats` / `shapiro(` / `normaltest(` / `anderson(` / `kstest(` over dsx/ and skills/ returns zero matches — belt-and-suspenders over the anti-feature at brief.md:463-464 (D-07)."
    - "This plan mints NO finding code and edits NO gate code — it adds one template and two read-only tests; the catalogue count is untouched (REQ-P15-05/06)."
  artifacts:
    - templates/APA-TABLE-research.md
    - tests/test_apa_template.py
    - tests/test_no_shapiro_autoswitch.py
  key_links:
    - "The normality-test-CALL grep is scoped to `dsx/` and `skills/` ONLY — never `tests/` (which legitimately contains the token denylist in this plan's own test) and never `references/The AI Data Scientist.md` (an untracked academic paper that quotes scipy.stats in example code, not a dsx decision surface). Scoping to the gate + skill decision path is exactly D-07 clause (4)."
    - "`references/test-selection.md:54-63` is the SSOT the order assertion reads: 1 Independence, 2 Equal variance (→ Welch, 'stop worrying about it'), 3 Normality (small-n only). The test reads that order; it does NOT grep test-selection.md for the token `shapiro` (the doc names Shapiro-Wilk at line 62 as the anti-pattern it warns against — a token grep there would false-positive)."
  prohibitions:
    - statement: "No finding code is minted; no dsx/ gate module is edited. This plan adds a template under templates/ and two read-only tests under tests/. The marketing-domain NAR/FIG/CLM codes are unchanged (REQ-P15-05)."
      status: enforced
      verification: "`git status --porcelain -- dsx/ references/finding-codes.md` is empty for this plan; the diff touches only templates/APA-TABLE-research.md and the two new test files."
    - statement: "The normality-test-CALL grep excludes tests/ and the untracked references/The AI Data Scientist.md; it scopes to dsx/ and skills/ (the gate + skill decision surface). The assertion is about a normality-test CALL on a decision path, not a prose mention of Shapiro-Wilk."
      status: enforced
      verification: "the test's grep helper builds its file list from dsx/ and skills/ only; the order assertion reads references/test-selection.md by structure, not by a `shapiro` token grep."
---

<objective>
Deliver two no-mint requirements. REQ-P15-05: add `templates/APA-TABLE-research.md`, an optional
research-domain APA-style statistical results table template, without weakening the marketing-domain
ship contract (narrative + sealed figure + claim evidence stay required via the unchanged NAR/FIG/CLM
codes). REQ-P15-06: add a negative-assertion test proving no skill or gate auto-switches a test on
Shapiro-Wilk — asserted against `references/test-selection.md`'s fixed assumption order (independence,
then variance, then normality) and by a CRLF-safe grep of the gate + skill decision surface for
normality-test calls.

Purpose: this is the phase's declarative surface that mints nothing and touches no gate code. The APA
template serves the research domain (mirroring the existing `templates/DISCLOSURE-research.md`
domain-suffixed template) as an optional aid, while the marketing default is unchanged. The Shapiro
negative assertion is belt-and-suspenders over the anti-feature at brief.md:463-464: normality must
enter only as a DECLARED shape+n property, never as a tool-run test output that flips the recommended
test — the classic assumption-checking-as-theatre failure test-selection.md:61-63 names.

Output: `templates/APA-TABLE-research.md` + `tests/test_apa_template.py` + `tests/test_no_shapiro_autoswitch.py`.
</objective>

<artifacts_this_phase_produces>
This plan (15-03) runs in wave 1 and is the sole writer of templates/APA-TABLE-research.md and its two
test files. Its `files_modified` are disjoint from the other wave-1 plans (15-01 owns dsx/spec.py;
15-02 owns dsx/checks/metrics.py + tests/test_cohort_denominator.py). It mints zero finding codes and
edits no gate code. See 15-01 for the full phase wave structure and single-writer justification.

## Phase-level requirement coverage (union across all six Phase-15 plans)
- REQ-P15-01 — plan 15-01; REQ-P15-02 — plan 15-04; REQ-P15-03 — plan 15-05; REQ-P15-04 — plan 15-02;
  REQ-P15-05 — **this plan (15-03)**; REQ-P15-06 — **this plan (15-03)**; REQ-P15-07 — plan 15-06.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cuped-and-bi-declaration-checks-new-codes-d-05/15-CONTEXT.md
@references/test-selection.md
@templates/DISCLOSURE-research.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add templates/APA-TABLE-research.md and prove it exists with the APA column contract</name>
  <files>templates/APA-TABLE-research.md, tests/test_apa_template.py</files>
  <read_first>
    - templates/DISCLOSURE-research.md (the existing domain-suffixed research template: heading structure, house tone, the "optional, research-domain" framing to mirror; `-research` suffix convention)
    - templates/CHART-REVIEW.md (an existing report-style template with a machine-readable block and column tables — the table/section shape to reuse)
    - references/test-selection.md:41-63 (the statistical-reporting vocabulary the APA table columns must reflect: test statistic, df, p, effect size + CI, and the declared-normality framing)
  </read_first>
  <action>
    Create templates/APA-TABLE-research.md, an OPTIONAL research-domain APA-style statistical results
    table template. Give it a short prose header stating it is an optional aid for the research domain
    (dsx.domain = research), mirroring templates/DISCLOSURE-research.md, and that it does NOT replace or
    relax the marketing-domain ship requirements (narrative + sealed figure + claim evidence remain
    required, enforced by the unchanged NAR/FIG/CLM codes). Then provide an APA-style results table with
    a clear column header row covering, at minimum: group/condition, n, M (mean), SD, the test statistic
    with its symbol, degrees of freedom (df), p value, the effect size (with its kind, e.g. d or h) and
    its confidence interval. Add an APA-style note row beneath the table (e.g. a `Note.` line stating
    what the effect size and CI describe) and a one-line instruction that normality is reported as a
    DECLARED shape+n property, never as the output of a normality test run to pick the test — pointing
    the reader at references/test-selection.md's assumption order. Keep the file authored plain text and
    CRLF-tolerant.

    Then create tests/test_apa_template.py, a stdlib-only unittest (import unittest, pathlib) with ROOT
    via `Path(__file__).resolve().parent.parent`. Write test methods asserting: (1) the template file
    exists; (2) its text contains the APA column vocabulary — case-insensitively, tokens for the test
    statistic, `df`, `p`, effect size and CI (assert several distinct column tokens are present so an
    empty or non-APA file fails); (3) the header states the template is research-domain and optional and
    that the marketing ship path is unchanged. Keep assertions CRLF-agnostic (collapse whitespace or use
    case-insensitive substring checks, never a line-anchored regex).
  </action>
  <acceptance_criteria>
    - templates/APA-TABLE-research.md exists with an APA results table whose header names at least the test statistic, df, p, effect size and CI columns, plus an APA `Note.` line.
    - The header frames the template as optional and research-domain, and states the marketing-domain ship path (narrative + sealed figure + claim evidence) is unchanged.
    - tests/test_apa_template.py exists, is stdlib-only, and asserts the file exists and carries the APA column vocabulary and the domain framing.
    - `python -m unittest tests.test_apa_template` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f templates/APA-TABLE-research.md && python -m unittest tests.test_apa_template >/dev/null 2>&1 && python -c "t=open('templates/APA-TABLE-research.md',encoding='utf-8').read().lower(); assert 'df' in t and (' p ' in t or 'p value' in t or 'p-value' in t or '| p' in t); assert 'ci' in t or 'confidence interval' in t; assert 'effect size' in t or 'effect' in t; assert 'research' in t and 'optional' in t"</automated>
  </verify>
  <done>templates/APA-TABLE-research.md is an optional research-domain APA results table (test statistic, df, p, effect size, CI columns + Note line) that leaves the marketing NAR/FIG/CLM ship path unchanged, proven by a stdlib-only existence-and-columns test.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests/test_no_shapiro_autoswitch.py — the D-07 negative assertion</name>
  <files>tests/test_no_shapiro_autoswitch.py</files>
  <read_first>
    - references/test-selection.md:54-63 (the fixed assumption order — 1 Independence, 2 Equal variance → Welch 'stop worrying about it', 3 Normality small-n only — and the line 61-63 warning that reversing the order is assumption-checking theatre; this is the SSOT the test asserts by structure)
    - tests/test_gate_path_hermetic.py (the stdlib-only ast/pathlib/unittest house style, ROOT resolution, and the non-empty-scan-set sanity anchor to mirror)
    - dsx/mathx.py:1-11 (the module docstring stating the stats kernel is stdlib-only, no scipy required — the reason the gate decision surface has no normality-test call)
  </read_first>
  <action>
    Create tests/test_no_shapiro_autoswitch.py, a stdlib-only unittest (import unittest, re, pathlib;
    import no third-party package) with ROOT via `Path(__file__).resolve().parent.parent`. Add a
    top-of-file comment noting this test mints nothing (no `# D-05:` marker — nothing to mint).

    Write these test methods:

    1. test_test_selection_assumption_order_is_fixed: read references/test-selection.md as UTF-8,
       collapse to a CRLF-agnostic form, and assert the assumption order is independence → equal
       variance → normality by asserting the FIRST occurrence index of an independence token precedes
       that of an equal-variance token, which precedes that of a normality token (locate the "in order
       of how much they matter" section). This pins the order lines 54-63 declare.
    2. test_continuous_two_group_recommendation_is_welch_unconditional: assert test-selection.md states
       the equal-variance response is Welch unconditionally ("use Welch") with no branch on a computed
       variance test — assert the Welch guidance is present and sits under the equal-variance item, and
       that there is no "if variance test ... then" style conditional switching the recommended test.
    3. test_normality_is_declared_not_tool_run: assert test-selection.md frames normality as a declared
       shape+n property (matters at small n only, irrelevant above ~200 per group) rather than the
       output of a normality test the tool ran.
    4. test_no_normality_test_call_on_the_decision_surface: build a file list from `ROOT/"dsx"` and
       `ROOT/"skills"` (recursively; include *.py and *.md), read each UTF-8, and assert NONE contains a
       normality-test CALL token — a CRLF-safe search for `scipy.stats`, `shapiro(`, `normaltest(`,
       `anderson(`, or `kstest(`. EXPLICITLY exclude the tests/ tree (this file's own denylist lives
       there) and do not scan references/ (the untracked academic paper quotes scipy.stats in example
       code, and test-selection.md names Shapiro-Wilk only as the anti-pattern it warns against). Assert
       the scanned file set is non-empty (anti-vacuity) and includes at least one dsx/ module and one
       skills/ file by name, so an empty walk cannot pass green. Aggregate any offender into a message
       naming the file and the token.

    Keep the module stdlib-only and every search CRLF-safe (`\r?\n`, never bare `\n`).
  </action>
  <acceptance_criteria>
    - tests/test_no_shapiro_autoswitch.py exists, is stdlib-only, and defines the four methods above.
    - The order test asserts independence precedes equal-variance precedes normality in test-selection.md; the Welch test asserts an unconditional Welch recommendation with no computed-variance-test branch; the normality test asserts normality is a declared small-n property.
    - The decision-surface test scans dsx/ and skills/ (not tests/, not references/), asserts a non-empty named scan set, and finds zero normality-test-call tokens.
    - `python -m unittest tests.test_no_shapiro_autoswitch` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_no_shapiro_autoswitch >/dev/null 2>&1 && python -c "import ast; t=ast.parse(open('tests/test_no_shapiro_autoswitch.py',encoding='utf-8').read()); ms={f.name for f in ast.walk(t) if isinstance(f,ast.FunctionDef)}; need={'test_test_selection_assumption_order_is_fixed','test_continuous_two_group_recommendation_is_welch_unconditional','test_normality_is_declared_not_tool_run','test_no_normality_test_call_on_the_decision_surface'}; assert need<=ms, need-ms" && python -c "import re,glob; hit=[]; [hit.append(p) for p in glob.glob('dsx/**/*.py',recursive=True)+glob.glob('skills/**/*.md',recursive=True)+glob.glob('skills/**/*.py',recursive=True) if re.search(r'scipy\.stats|shapiro\(|normaltest\(|anderson\(|kstest\(', open(p,encoding='utf-8').read())]; assert not hit, hit"</automated>
  </verify>
  <done>tests/test_no_shapiro_autoswitch.py is a stdlib-only unittest pinning test-selection.md's independence→variance→normality order, the unconditional Welch recommendation, normality as a declared property, and zero normality-test calls across dsx/ and skills/ — scoped away from tests/ and the untracked references paper, and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds an authored template and two read-only tests. It introduces no runtime gate surface, no
network/auth/input, and executes nothing (the tests read source and doc text via pathlib/re). No
npm/pip/cargo install occurs, so T-15-SC is N/A this plan.

| Boundary | Description |
|----------|-------------|
| template → research-domain deliverable | templates/APA-TABLE-research.md is authored text a research-domain user optionally fills; it changes no gate behaviour and relaxes no marketing-domain requirement. |
| test → dsx/ + skills/ source | The negative-assertion test reads gate + skill source to detect a normality-test call; it mutates nothing and runs nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-20 | Tampering | The APA template being read as relaxing the marketing-domain ship contract (dropping narrative/figure/claim evidence) | medium | mitigate | REQ-P15-05: the header states the template is optional and research-domain and that NAR/FIG/CLM stay required; the test asserts the domain + optional framing; no gate code is edited. |
| T-15-21 | Tampering | A future skill or gate quietly adding a Shapiro-Wilk auto-switch that flips the recommended test | high | mitigate | D-07: test_no_normality_test_call_on_the_decision_surface greps dsx/ + skills/ for normality-test calls and the order tests pin test-selection.md's fixed independence→variance→normality order and unconditional Welch. |
| T-15-22 | Tampering | A vacuous decision-surface scan (empty or mis-scoped walk) passing green while proving nothing | medium | mitigate | anti-vacuity: the scan asserts a non-empty named file set including a dsx/ module and a skills/ file; scoping excludes tests/ and references/ so it cannot false-positive or self-match. |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs occur; the deliverables are a template and two stdlib tests. |
</threat_model>

<verification>
This plan delivers REQ-P15-05 (research-domain optional APA table template; marketing-domain ship
unchanged) and REQ-P15-06 (no skill or gate auto-switches a test on Shapiro-Wilk, asserted against
test-selection.md's order).

For this plan specifically:
- `python -m unittest tests.test_apa_template` passes: the template exists with APA columns and the domain framing.
- `python -m unittest tests.test_no_shapiro_autoswitch` passes: the fixed assumption order, unconditional Welch, declared normality, and zero normality-test calls on dsx/ + skills/.
- `git status --porcelain -- dsx/ references/finding-codes.md` is empty (no gate edit, no mint).
- Do NOT run `gen-finding-catalogue.py --check` here — this plan mints nothing; the catalogue is regenerated once in 15-06.
</verification>

<success_criteria>
- templates/APA-TABLE-research.md exists as an optional research-domain APA results table; the marketing-domain NAR/FIG/CLM ship path is unchanged (REQ-P15-05).
- A negative-assertion test pins test-selection.md's independence→variance→normality order, unconditional Welch, and declared normality; no normality-test call exists on the dsx/ + skills/ decision surface (REQ-P15-06 / D-07).
- Zero finding codes minted; no gate module edited; catalogue untouched.
</success_criteria>

<output>
Create `.planning/phases/15-cuped-and-bi-declaration-checks-new-codes-d-05/15-03-SUMMARY.md` when done.
</output>
