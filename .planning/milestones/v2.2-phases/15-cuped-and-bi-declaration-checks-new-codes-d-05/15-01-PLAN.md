---
phase: 15-cuped-and-bi-declaration-checks-new-codes-d-05
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dsx/spec.py
  - tests/test_cuped_vocab.py
autonomous: true
requirements: [REQ-P15-01]
tags: [dsx, spec, vocabulary, cuped, VARIANCE_ADJUSTMENTS, CUPED_COVARIATE_TIMINGS, dsx-vocab, no-mint, trap-12]

must_haves:
  truths:
    - "`VARIANCE_ADJUSTMENTS` in dsx/spec.py contains `cuped` alongside the four pre-existing members (cluster_robust, delta_method, bootstrap_cluster, mixed_effects), so a spec declaring `design.variance_adjustment: cuped` no longer draws a stray DSX-SPEC-044 (MEDIUM) at spec.py:1058 — the trap-12 precondition for the CUPED PASS fixture (REQ-P15-01 / trap #12)."
    - "`dsx vocab` dumps `cuped` under `variance_adjustments`: `describe_vocabulary()['variance_adjustments']` (the exact object cmd_vocab serialises) is a sorted list containing `cuped` and all four legacy members — the four round-trip unchanged (REQ-P15-01)."
    - "A closed two-member vocabulary `CUPED_COVARIATE_TIMINGS = {\"pre_experiment\", \"post_treatment\"}` exists at module level in dsx/spec.py and is registered in `_VOCABULARIES` so `dsx vocab` dumps `cuped_covariate_timings` too; it is the single source of truth the 15-04 CUPED check imports (D-02)."
    - "This plan mints NO finding code and adds NO GATE_THRESHOLDS/GATE_PROFILES entry — it is vocabulary only; the catalogue count is untouched by this plan (D-01 / trap #8)."
  artifacts:
    - dsx/spec.py
    - tests/test_cuped_vocab.py
  key_links:
    - "`VARIANCE_ADJUSTMENTS` is shared by TWO consumers by design (M-09): DSX-SPEC-044 membership (spec.py:1058) and `dependence.method_family_required` membership (spec.py:1159). Adding `cuped` intentionally lets both accept it — this is the reuse the milestone chose over a parallel set; it is NOT a defect to be 'fixed' by splitting the vocabulary."
    - "The 15-04 CUPED check (`dsx/checks/design.py::_check_cuped`) imports `CUPED_COVARIATE_TIMINGS` from `..spec`, exactly as `dsx/checks/design.py` already imports `IDENTIFICATION_STRATEGIES` — so the check's recognised set and the dumped vocabulary cannot drift. This plan MUST land in an earlier wave than 15-04 (trap #12)."
  prohibitions:
    - statement: "No finding code is minted and no gate threshold/profile is edited. This plan changes closed vocabularies only; `references/finding-codes.md`, GATE_THRESHOLDS and GATE_PROFILES are untouched."
      status: enforced
      verification: "`git status --porcelain -- references/finding-codes.md dsx/cli.py` is empty for this plan; the diff touches only dsx/spec.py and tests/test_cuped_vocab.py."
    - statement: "`CUPED_COVARIATE_TIMINGS` is strictly two-valued — exactly {pre_experiment, post_treatment}. No fuzzy third state is added, so a valid covariate is never false-flagged by the 15-04 check (D-02)."
      status: enforced
      verification: "tests/test_cuped_vocab.py asserts `CUPED_COVARIATE_TIMINGS == {'pre_experiment','post_treatment'}` by equality (not subset)."
---

<objective>
Land REQ-P15-01: extend the closed vocabulary `VARIANCE_ADJUSTMENTS` in dsx/spec.py with `cuped`
(making five members), add the new closed two-member vocabulary
`CUPED_COVARIATE_TIMINGS = {"pre_experiment", "post_treatment"}`, and register the latter in the
single `_VOCABULARIES` registry so `dsx vocab` dumps both. Prove `cuped` is dumped, the four legacy
variance adjustments round-trip, and the timing vocabulary is exactly two-valued.

Purpose: this is the trap-12 keystone. Until `cuped ∈ VARIANCE_ADJUSTMENTS`, any
`design.variance_adjustment: cuped` spec draws a stray DSX-SPEC-044 (MEDIUM, spec.py:1058-1065) and
the pre-experiment PASS fixture (15-04) is not clean. It must therefore land in wave 1, strictly
before the 15-04 CUPED check that keys on `cuped` and imports `CUPED_COVARIATE_TIMINGS`. This plan is
vocabulary-only: it mints nothing and touches no gate threshold.

Output: the extended `VARIANCE_ADJUSTMENTS`, the new `CUPED_COVARIATE_TIMINGS`, its `_VOCABULARIES`
registration, and a stdlib-only test module pinning all four properties.
</objective>

<artifacts_this_phase_produces>
This plan (15-01) is a wave-1 plan and the sole writer of dsx/spec.py in Phase 15. It edits one file
and creates one:
- `dsx/spec.py` (edit — add `cuped` to `VARIANCE_ADJUSTMENTS`; add `CUPED_COVARIATE_TIMINGS`; register `cuped_covariate_timings` in `_VOCABULARIES`)
- `tests/test_cuped_vocab.py` (new — round-trip + dump + membership assertions)

## Phase-15 wave structure and single-writer justification (whole phase)
- **Wave 1** (parallel, pairwise file-disjoint): **15-01** (dsx/spec.py vocabulary — this plan),
  **15-02** (dsx/checks/metrics.py, DSX-MET-021), **15-03** (templates/ APA table + tests/, no code).
  spec.py, metrics.py and templates/ are disjoint; each plan owns its own new test file.
- **Wave 2**: **15-04** (dsx/mathx.py CUPED arithmetic + dsx/checks/design.py DSX-EXP-070 + fixtures),
  `depends_on: [15-01]` — the sole wave-2 plan, so single-writer holds trivially. It depends on this
  plan because `_check_cuped` imports `CUPED_COVARIATE_TIMINGS` and keys on `cuped` (trap #12).
- **Wave 3** (parallel, file-disjoint): **15-05** (examples/good-ANALYSIS-SPEC.yaml D-08 silence),
  `depends_on: [15-01, 15-02, 15-04]`; **15-06** (catalogue regen + invariant + D-09 allowlist),
  `depends_on: [15-02, 15-04]`. Good-fixture+its test vs catalogue-md+invariant-test+generator-script
  are disjoint.
- **Single-writer per file across waves**: dsx/spec.py is written ONLY here (both the
  `VARIANCE_ADJUSTMENTS` add and the `CUPED_COVARIATE_TIMINGS` vocab live in this one plan, so no wave
  double-writes spec.py); dsx/checks/metrics.py only by 15-02; dsx/mathx.py and dsx/checks/design.py
  only by 15-04; examples/good-ANALYSIS-SPEC.yaml only by 15-05; references/finding-codes.md,
  tests/test_finding_catalogue_invariant.py and scripts/gen-finding-catalogue.py only by 15-06.

## Phase-level requirement coverage (union across all six Phase-15 plans)
- REQ-P15-01 — **this plan (15-01)**: `cuped ∈ VARIANCE_ADJUSTMENTS`, dumped by `dsx vocab`, four members round-trip; no mint.
- REQ-P15-02 — plan 15-04: DSX-EXP-070 CRITICAL CUPED post-treatment check + CUPED reference arithmetic + worked-value test.
- REQ-P15-03 — plan 15-05: thin cohort-grain/funnel-step fields; extended good fixture silent at every threshold (D-08).
- REQ-P15-04 (PARTIAL, loud) — plan 15-02: DSX-MET-021 HIGH changing-denominator check; survivorship NOT minted (D-05/§6.5).
- REQ-P15-05 — plan 15-03: research-domain APA table template; marketing-domain NAR/FIG/CLM unchanged; no mint.
- REQ-P15-06 — plan 15-03: Shapiro-Wilk negative assertion vs test-selection.md order; no mint.
- REQ-P15-07 — plan 15-06: `gen-finding-catalogue.py --check` exit 0 at 260; additive invariant rebaseline; D-09 exact-code allowlist.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cuped-and-bi-declaration-checks-new-codes-d-05/15-CONTEXT.md
@dsx/spec.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `cuped` to VARIANCE_ADJUSTMENTS and register CUPED_COVARIATE_TIMINGS in dsx/spec.py</name>
  <files>dsx/spec.py</files>
  <read_first>
    - dsx/spec.py:264 (`VARIANCE_ADJUSTMENTS = {"cluster_robust", "delta_method", "bootstrap_cluster", "mixed_effects"}` — the set to extend with `cuped`; keep it a plain set literal so describe_vocabulary sorts it)
    - dsx/spec.py:266 (METRIC_TYPES — the module-constant house style/placement for adding CUPED_COVARIATE_TIMINGS as a sibling set nearby)
    - dsx/spec.py:548-574 (`_VOCABULARIES` list — the single registry describe_vocabulary() dumps; add a `("cuped_covariate_timings", CUPED_COVARIATE_TIMINGS)` tuple beside the `("variance_adjustments", VARIANCE_ADJUSTMENTS)` entry at line 557)
    - dsx/spec.py:1057-1066 (DSX-SPEC-044 fires MEDIUM when `design.variance_adjustment` is not in VARIANCE_ADJUSTMENTS — this is exactly why `cuped` must join the set: trap #12)
    - dsx/spec.py:1148-1165 (`_VALIDITY_FRAME_MEMBERSHIP` — line 1159 reuses VARIANCE_ADJUSTMENTS for `dependence.method_family_required`; adding `cuped` intentionally lets this consumer accept it too, the M-09 shared-vocabulary reuse; do NOT split the set)
  </read_first>
  <action>
    Add the string `"cuped"` to the `VARIANCE_ADJUSTMENTS` set literal at dsx/spec.py:264, leaving the
    four existing members unchanged (result: five members). Define a new module-level constant near the
    other closed vocabularies, `CUPED_COVARIATE_TIMINGS = {"pre_experiment", "post_treatment"}` —
    strictly two-valued, no third state — with a one-line comment stating it is the closed vocabulary
    for `design.cuped.covariate_timing` that the always-run CUPED design check (dsx/checks/design.py)
    keys on, and that `pre_experiment` is the only passing value.

    Register the new vocabulary for the `dsx vocab` dump by appending
    `("cuped_covariate_timings", CUPED_COVARIATE_TIMINGS)` to the `_VOCABULARIES` list (place it beside
    the existing `("variance_adjustments", VARIANCE_ADJUSTMENTS)` entry). Do not touch
    describe_vocabulary()'s body — appending the tuple is sufficient; the registry is the single place a
    vocabulary is added.

    Add NO finding code, NO GATE_THRESHOLDS/GATE_PROFILES entry, and NO strict-key block. Leave the
    shared reuse of VARIANCE_ADJUSTMENTS at line 1159 (`dependence.method_family_required`) intact — the
    fact that `method_family_required: cuped` now also passes membership is the intended M-09 reuse, not
    a defect; add a short comment there noting the shared vocabulary now includes the CUPED member so a
    future reader does not "fix" it by forking the set.
  </action>
  <acceptance_criteria>
    - `VARIANCE_ADJUSTMENTS` contains exactly {cluster_robust, delta_method, bootstrap_cluster, mixed_effects, cuped}.
    - `CUPED_COVARIATE_TIMINGS` exists at module level and equals {pre_experiment, post_treatment}.
    - `_VOCABULARIES` includes a `("cuped_covariate_timings", CUPED_COVARIATE_TIMINGS)` entry; `describe_vocabulary()` returns a `cuped_covariate_timings` key and a `variance_adjustments` list containing `cuped`.
    - No finding code, GATE_THRESHOLDS or GATE_PROFILES entry is added; references/finding-codes.md is untouched by this plan.
  </acceptance_criteria>
  <verify>
    <automated>python -c "from dsx.spec import VARIANCE_ADJUSTMENTS, CUPED_COVARIATE_TIMINGS, describe_vocabulary; assert VARIANCE_ADJUSTMENTS == {'cluster_robust','delta_method','bootstrap_cluster','mixed_effects','cuped'}, VARIANCE_ADJUSTMENTS; assert CUPED_COVARIATE_TIMINGS == {'pre_experiment','post_treatment'}, CUPED_COVARIATE_TIMINGS; d=describe_vocabulary(); assert 'cuped' in d['variance_adjustments'], d['variance_adjustments']; assert set(d['cuped_covariate_timings']) == {'pre_experiment','post_treatment'}, d['cuped_covariate_timings']" && [ -z "$(git status --porcelain -- references/finding-codes.md dsx/cli.py)" ]</automated>
  </verify>
  <done>dsx/spec.py declares five variance adjustments including cuped, a two-member CUPED_COVARIATE_TIMINGS, and a _VOCABULARIES registration so `dsx vocab` dumps both; no finding code or gate threshold is added and the catalogue is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the vocabulary contract in tests/test_cuped_vocab.py</name>
  <files>tests/test_cuped_vocab.py</files>
  <read_first>
    - dsx/spec.py:264 and the new CUPED_COVARIATE_TIMINGS constant from Task 1 (the objects under test)
    - dsx/cli.py:499-500 (cmd_vocab prints `json.dumps(describe_vocabulary(), indent=2)` — so asserting against describe_vocabulary() tests the exact `dsx vocab` payload)
    - tests/test_finding_catalogue_invariant.py:17-23 (the stdlib-only unittest + ROOT-via-`Path(__file__).resolve().parent.parent` house style to mirror)
  </read_first>
  <action>
    Create tests/test_cuped_vocab.py, a stdlib-only unittest module (import unittest and, from dsx.spec,
    `VARIANCE_ADJUSTMENTS`, `CUPED_COVARIATE_TIMINGS`, `describe_vocabulary`; import no third-party
    package). Write these test methods:

    1. test_cuped_is_a_variance_adjustment: assert `"cuped" in VARIANCE_ADJUSTMENTS`.
    2. test_four_legacy_variance_adjustments_round_trip: assert the four pre-existing members
       `{"cluster_robust", "delta_method", "bootstrap_cluster", "mixed_effects"}` are all still present
       (subset), and that the set has exactly five members — proving the add was additive, not a replace.
    3. test_vocab_dump_lists_cuped: call `describe_vocabulary()`, assert its `variance_adjustments` value
       contains `cuped` and every legacy member (this is the exact object `dsx vocab` serialises).
    4. test_cuped_covariate_timings_is_exactly_two_valued: assert
       `CUPED_COVARIATE_TIMINGS == {"pre_experiment", "post_treatment"}` by equality, and that
       `describe_vocabulary()["cuped_covariate_timings"]` dumps the same two members.

    Keep the module stdlib-only and CRLF-agnostic (no line-anchored parsing). This plan mints nothing,
    so add NO `# D-05:` marker here.
  </action>
  <acceptance_criteria>
    - tests/test_cuped_vocab.py exists, is stdlib-only, and defines the four methods above.
    - The legacy-round-trip test asserts the four prior members remain and the set is exactly five members.
    - The dump test asserts `describe_vocabulary()['variance_adjustments']` contains `cuped`.
    - `python -m unittest tests.test_cuped_vocab` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_cuped_vocab >/dev/null 2>&1 && python -c "import ast; t=ast.parse(open('tests/test_cuped_vocab.py',encoding='utf-8').read()); ms={f.name for f in ast.walk(t) if isinstance(f,ast.FunctionDef)}; need={'test_cuped_is_a_variance_adjustment','test_four_legacy_variance_adjustments_round_trip','test_vocab_dump_lists_cuped','test_cuped_covariate_timings_is_exactly_two_valued'}; assert need<=ms, need-ms"</automated>
  </verify>
  <done>tests/test_cuped_vocab.py is a stdlib-only unittest proving cuped joined VARIANCE_ADJUSTMENTS additively, the four legacy members round-trip, `dsx vocab` dumps cuped, and CUPED_COVARIATE_TIMINGS is exactly two-valued — and it passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan edits closed-vocabulary constants in dsx/spec.py and adds a stdlib-only test. No runtime
gate behaviour changes beyond membership acceptance of one new string, no network/auth/input surface
is introduced, and nothing is executed. No npm/pip/cargo install occurs, so T-15-SC is N/A this plan.

| Boundary | Description |
|----------|-------------|
| spec vocabulary → membership checks | Adding `cuped` to VARIANCE_ADJUSTMENTS changes only which declared string passes DSX-SPEC-044 (spec.py:1058) and `method_family_required` membership (spec.py:1159); no computation, no data read. |
| _VOCABULARIES → `dsx vocab` dump | Appending the timing tuple changes only the JSON `dsx vocab` prints; it is a readout, never a gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Tampering | A parallel CUPED-timing set forked from VARIANCE_ADJUSTMENTS, letting the dumped vocabulary and the 15-04 check drift | medium | mitigate | M-09 single-source reuse: CUPED_COVARIATE_TIMINGS is one module constant, registered once in `_VOCABULARIES`, and imported by the 15-04 check; the test asserts equality on both the constant and the dump. |
| T-15-02 | Tampering | A silent finding-code mint or gate-threshold edit riding along with the vocabulary change | high | mitigate | trap #8/D-01: the verify asserts `git status` on references/finding-codes.md and dsx/cli.py is empty; this plan adds no report.add and no GATE_THRESHOLDS/GATE_PROFILES entry. |
| T-15-03 | Tampering | Replacing (not extending) VARIANCE_ADJUSTMENTS, dropping a legacy member and silently loosening an existing gate | high | mitigate | REQ-P15-01: the round-trip test asserts the four legacy members remain and the set is exactly five — a replace or drop fails it. |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs occur; the change is two constants and a stdlib test. |
</threat_model>

<verification>
This plan delivers REQ-P15-01: `cuped ∈ VARIANCE_ADJUSTMENTS`, dumped by `dsx vocab`, with the four
existing members round-tripping and the D-08 fixtures extended not replaced downstream.

For this plan specifically:
- `python -m unittest tests.test_cuped_vocab` passes all four properties.
- `describe_vocabulary()['variance_adjustments']` contains `cuped` and every legacy member; `cuped_covariate_timings` dumps exactly two members.
- `git status --porcelain -- references/finding-codes.md dsx/cli.py` is empty (no mint, no gate-threshold edit).
- Do NOT run `gen-finding-catalogue.py --check` here — no code is minted by this plan; the catalogue is regenerated once in 15-06.
</verification>

<success_criteria>
- `VARIANCE_ADJUSTMENTS` gains `cuped` additively (five members); the four legacy members round-trip (REQ-P15-01).
- `CUPED_COVARIATE_TIMINGS` is exactly {pre_experiment, post_treatment} and registered so `dsx vocab` dumps it (D-02).
- No finding code minted; no GATE_THRESHOLDS/GATE_PROFILES edit; catalogue untouched (D-01 / trap #8).
- This plan lands in wave 1, strictly before the 15-04 CUPED check that keys on `cuped` (trap #12).
</success_criteria>

<output>
Create `.planning/phases/15-cuped-and-bi-declaration-checks-new-codes-d-05/15-01-SUMMARY.md` when done.
</output>
