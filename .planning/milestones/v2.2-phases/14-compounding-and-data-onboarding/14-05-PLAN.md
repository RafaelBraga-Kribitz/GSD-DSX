---
phase: 14-compounding-and-data-onboarding
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_gate_path_hermetic.py
autonomous: true
requirements: [REQ-P14-06]
tags: [dsx, test, finding-catalogue, invariant, gate-path-hermetic, zero-mint, scope-bound]

must_haves:
  truths:
    - "The finding catalogue stays at exactly 256 codes after this phase: `python scripts/gen-finding-catalogue.py --check` exits 0 and `python -m unittest tests.test_finding_catalogue_invariant` passes both legs (count==256 AND set-identity vs tests/fixtures/finding-codes-phase12.md), catching a mint-one/drop-one swap a count alone would pass (REQ-P14-06 / D-07)."
    - "The gate path stays stdlib-pure and profiler-free: a new tests/test_gate_path_hermetic.py asserts no top-level import of pandas/scipy/numpy/csv reaches the import closure of any GATE_PROFILES module, and that dsx/profiler.py is not in dsx/checks/dq.py's import closure — a standing guard stronger than the current implicit purity (D-07)."
    - "The phase touches nothing on the deterministic gate path: `git status --porcelain -- dsx/` is empty for this plan and `grep -c report.add dsx/cli.py` stays 0 (REQ-P14-06 / D-01 / D-07)."
  artifacts:
    - tests/test_gate_path_hermetic.py
  key_links:
    - "The set-identity leg (not the count alone) is the REQ-P14-06 proof that no cardinality-preserving swap slipped through; it is re-run by the orchestrator after each Phase-14 plan and any added=[…]/removed=[…] is a hard stop (D-07)."
    - "The hermeticity test walks the import closure of the actual GATE_PROFILES modules resolved from dsx/cli.py, so it stays correct if the gate profile membership changes; it is tests/-only and adds no report.add (D-07)."
  prohibitions:
    - statement: "Zero new DSX-* finding codes are minted; the catalogue stays exactly 256, set-identical to the Phase-12 snapshot."
      status: enforced
      verification: "The only new file is a test under tests/; the catalogue generator only walks dsx/**/*.py (gen-finding-catalogue.py:226-243). The invariant test and gen-finding-catalogue --check are this plan's gate."
    - statement: "No edit to the deterministic gate path — dsx/cli.py CHECKS/GATE_PROFILES, dsx/checks/*, dsx/frame/* stay read-only; the hermeticity test only READS dsx/ source via ast, it imports no third-party package and adds no check."
      status: enforced
      verification: "files_modified is tests/test_gate_path_hermetic.py only; `git status --porcelain -- dsx/` empty; the test is stdlib-only (ast/pathlib/unittest)."
    - statement: "No gate check is added for any Phase-14 artifact (DATA-DICTIONARY, learnings, disclosure) — the single mint trap D-07 names; all three are written and ungated."
      status: enforced
      verification: "the set-identity diff would name any minted code; this plan adds none and asserts the set is unchanged."
---

<objective>
Prove REQ-P14-06 the way Phase 13 did (D-07): the finding catalogue mints ZERO codes across
Phase 14, asserted by a catalogue diff, not by review. This plan reuses the Phase-13 machinery
unchanged — `gen-finding-catalogue.py --check` plus `tests/test_finding_catalogue_invariant.py`
(count==256 AND set-identity vs `tests/fixtures/finding-codes-phase12.md`) — as its gate, and ships
one optional hardening file, `tests/test_gate_path_hermetic.py`, a standing guard that the gate
path stays stdlib-pure and profiler-free.

Purpose: the single mint trap in a doc/skill/template phase is adding a gate check for the new
artifacts (a DATA-DICTIONARY existence check, a learnings check, or a disclosure heading-scanner) —
each adds a `report.add` under `dsx/checks/*` and mints a code. The set-identity diff catches a
mint-one/drop-one swap the count alone would pass. The hermeticity test additionally pins the
D-01/D-03 gate-path bound structurally: the deterministic gate path never pulls a data library or the
CSV-opening profiler.

Output: `tests/test_gate_path_hermetic.py` (new). The catalogue proof edits nothing — it asserts the
tree is unchanged.
</objective>

<artifacts_this_phase_produces>
This plan creates one file:
- `tests/test_gate_path_hermetic.py` (new — the gate-path-hermeticity standing guard, D-07)

It edits no other file: the catalogue proof is a set of read-only assertions the orchestrator re-runs
after each plan. It produces NO new files under `dsx/`, NO new symbols, NO new `DSX-*` finding codes,
and adds NO gate check. Zero file overlap with plans 14-01, 14-02, 14-03 (docs/, templates/, skills/,
capabilities/fragments/) — same wave (wave 1) — and with 14-04 (docs/operating-guide.md, skill
descriptions, .claude/commands/) in wave 2.

## Phase-level goal-backward coverage (union across all five Phase-14 plans)
- Success Criterion 1 (REQ-P14-01) — plan 14-01: docs/dsx/learnings/ + dated exemplar + dsx-scope-analysis search-before-framing + researcher.md pointer.
- Success Criterion 2 (REQ-P14-02) — plan 14-02: templates/DATA-DICTIONARY.md + dsx-explore-data authoring step next to DATA-PROFILE.yaml.
- Success Criterion 3 (REQ-P14-03) — plan 14-03: templates/DISCLOSURE-research.md + dsx-narrate opt-in disclosure guarded on dsx.domain==research; marketing/auto byte-unchanged.
- Success Criterion 4 (REQ-P14-04) — plan 14-04: docs/operating-guide.md alias table + CSV-first description triggers on all DSX skills (+ optional .claude/commands/ shims); no data_storage/ folder.
- Success Criterion 5 (REQ-P14-05) — plan 14-04: docs/operating-guide.md "why no file-drop hook" honesty subsection (all four D-06 claims + DSX-DQ-001 named); capability.json hooks stays [].
- Success Criterion 6 (REQ-P14-06) — this plan (14-05): catalogue stays 256, set-identical to Phase-12; gate path hermetic.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-compounding-and-data-onboarding/14-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests/test_gate_path_hermetic.py — the gate-path-hermeticity standing guard</name>
  <files>tests/test_gate_path_hermetic.py</files>
  <read_first>
    - tests/test_finding_catalogue_invariant.py:1-124 (the machinery this plan reuses as its gate, and the CRLF/stdlib-only house style the new test mirrors — ROOT via Path(__file__).resolve().parent.parent, unittest, no third-party import)
    - dsx/cli.py:63-131 (CHECKS maps check-name -> callable; GATE_PROFILES maps plan/execute/verify/ship -> tuple of check names — the test reads these to find the gate modules; note `repro` is a GATE_PROFILES member resolving to dsx/checks/repro.py even though it is dispatched specially)
    - dsx/checks/dq.py:1-16 (its imports: `from ..findings import Report`, `from ..loader import ...`, `from ..spec import ...` — relative, internal; it never imports profiler or csv — the isolation assertion the test pins)
    - .planning/phases/14-compounding-and-data-onboarding/14-CONTEXT.md (D-07: the optional hardening — no pandas/scipy/numpy/csv in any GATE_PROFILES module's import closure; dsx.profiler not in dsx.checks.dq's closure; off every gate module's import surface)
  </read_first>
  <action>
    Create `tests/test_gate_path_hermetic.py`, a stdlib-only unittest module (ast, pathlib, unittest —
    import NO third-party package). Define ROOT as `Path(__file__).resolve().parent.parent`, DSX as
    `ROOT / "dsx"`, and FORBIDDEN as the set of top-level import names {`pandas`, `scipy`, `numpy`,
    `csv`}.

    Resolve the gate modules: import `dsx.cli`, read `GATE_PROFILES`, and take the union of all check
    names across its four profiles. Map each name to a source file by trying `DSX/"checks"/name+".py"`
    then `DSX/"frame"/name+".py"`; collect the files that exist as the gate roots. Sanity-anchor the
    test against a vacuous pass: assert the gate-root set is non-empty and includes
    `dsx/checks/dq.py`.

    Implement a closure walk over a start file: parse it with `ast.parse`, and for every `ast.Import`
    record each alias's top-level name (`name.split('.')[0]`); for every `ast.ImportFrom` with
    `level == 0` record `module.split('.')[0]` as a top-level name and, when that top-level name is
    `dsx`, resolve the target to a file under ROOT and add it to the frontier; for every
    `ast.ImportFrom` with `level > 0` (a relative, internal import) resolve the target against the
    current file's package by ascending `level - 1` directories then appending the module parts (try
    both `<parts>.py` and `<parts>/__init__.py`), and add any existing target file to the frontier.
    Walk to a fixpoint, accumulating the set of visited files and the union of top-level import names
    seen across every visited file.

    Assertion A (gate-path purity): compute the union closure of ALL gate roots and assert
    `FORBIDDEN & top_level_names == set()`; on failure the message names the offending import and the
    module it was reached through. Assertion B (dq isolation): compute the closure of just
    `dsx/checks/dq.py` and assert `(DSX / "profiler.py").resolve()` is NOT in its visited set (and, as
    a belt-and-braces check, that no visited module's stem is `profiler`); the message names the path
    if the closure reaches it. Give each assertion its own test method so a failure localises. Add a
    module docstring stating this pins the D-01/D-03/D-07 gate-path bound and that it reads dsx/ source
    via ast only — it imports no third-party package and adds no `report.add`, so it mints no code.

    The test must be CRLF-safe (ast.parse tolerates `\r\n`) and must not write any file. Do NOT edit
    any path under `dsx/`; this is a tests/-only addition.
  </action>
  <acceptance_criteria>
    - `tests/test_gate_path_hermetic.py` exists, is stdlib-only (imports ast, pathlib, unittest; imports no pandas/scipy/numpy), and defines at least two test methods (gate-path purity, dq isolation).
    - It resolves gate modules from `dsx.cli.GATE_PROFILES`, asserts the gate-root set is non-empty and includes dsx/checks/dq.py, and walks each module's import closure.
    - `python -m unittest tests.test_gate_path_hermetic` exits 0 against the current tree.
    - The test writes no file and edits no path under `dsx/`.
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/test_gate_path_hermetic.py && python -c "import ast,sys; t=ast.parse(open('tests/test_gate_path_hermetic.py',encoding='utf-8').read()); mods={a.name.split('.')[0] for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names} | {n.module.split('.')[0] for n in ast.walk(t) if isinstance(n,ast.ImportFrom) and n.level==0 and n.module}; sys.exit(1 if ({'pandas','scipy','numpy'} & mods) else 0)" && python -m unittest tests.test_gate_path_hermetic >/dev/null 2>&1</automated>
  </verify>
  <done>tests/test_gate_path_hermetic.py is a stdlib-only unittest that resolves the GATE_PROFILES modules from dsx.cli, asserts no pandas/scipy/numpy/csv reaches any gate module's import closure and that profiler is not in dq's closure, passes against the current tree, writes nothing, and edits no dsx/ path.</done>
</task>

<task type="auto">
  <name>Task 2: Assert the zero-mint proof and the gate-path-untouched guards</name>
  <files>tests/test_gate_path_hermetic.py</files>
  <read_first>
    - scripts/gen-finding-catalogue.py:226-243 (collect() walks only dsx/**/*.py for report.add first-args — the structural reason a doc/skill/template phase cannot mint a code; `--check` compares the generated catalogue to references/finding-codes.md)
    - tests/test_finding_catalogue_invariant.py:43-119 (the two legs: count==256 and set-identity vs tests/fixtures/finding-codes-phase12.md — re-run here as the phase proof)
    - tests/fixtures/finding-codes-phase12.md (the frozen Phase-12 snapshot the set-identity leg compares against)
    - .planning/phases/14-compounding-and-data-onboarding/14-CONTEXT.md (D-07: gen-finding-catalogue --check exit 0 + the two-leg invariant, re-run after each plan; git diff --stat on dsx/ empty; grep -c report.add dsx/cli.py == 0)
  </read_first>
  <action>
    This task adds no new file — it is the zero-mint proof, run as a gate over the whole tree after the
    Phase-14 edits. Run `python scripts/gen-finding-catalogue.py --check` (must exit 0 —
    "finding catalogue is current"; the pre-existing "declared twice" warnings for
    DSX-VAL-021/VAL-060 and the others are shipped-tree noise that does not change `--check`'s exit or
    the code count) and `python -m unittest tests.test_finding_catalogue_invariant` (both legs must
    pass: count==256 and set-identity vs tests/fixtures/finding-codes-phase12.md). Assert the gate path
    was not touched by the phase: `git status --porcelain -- dsx/` is empty and
    `grep -c "report.add" dsx/cli.py` is 0. Any `added=[…]`/`removed=[…]` from the set-identity leg,
    or any non-empty `dsx/` status, is a hard stop — a Phase-14 artifact tried to mint a code or edit
    the gate path, exactly the D-07 mint trap. Do not edit any tracking file and do not edit any path
    under `dsx/`; this task only runs the existing proofs.
  </action>
  <acceptance_criteria>
    - `python scripts/gen-finding-catalogue.py --check` exits 0.
    - `python -m unittest tests.test_finding_catalogue_invariant` passes both legs (count==256 AND set-identity vs the Phase-12 snapshot).
    - `git status --porcelain -- dsx/` is empty (the phase touched no gate-path file).
    - `grep -c "report.add" dsx/cli.py` is 0.
  </acceptance_criteria>
  <verify>
    <automated>python scripts/gen-finding-catalogue.py --check && python -m unittest tests.test_finding_catalogue_invariant >/dev/null 2>&1 && [ -z "$(git status --porcelain -- dsx/)" ] && [ "$(grep -c 'report.add' dsx/cli.py)" -eq 0 ]</automated>
  </verify>
  <done>gen-finding-catalogue.py --check exits 0, the two-leg catalogue invariant passes (256 + set-identity vs the Phase-12 snapshot), git status on dsx/ is empty, and grep -c report.add dsx/cli.py is 0 — the phase minted no code and touched no gate-path file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds one stdlib-only test file and runs read-only proofs over the tree. It introduces no
executable surface on any runtime gate path, no network or file input, no auth, and no user-supplied
input. The hermeticity test reads `dsx/` source via `ast` only — it imports no third-party package
and writes nothing. ASVS Level 1 injection / auth / session vectors are N/A here — there is no
exploitable surface to assess. No npm / pip / cargo install occurs, so the supply-chain vector
(T-14-SC) is N/A this plan.

| Boundary | Description |
|----------|-------------|
| test -> dsx/ source | The test READS dsx/ source via ast to walk import closures; it mutates nothing and imports no third-party package. No new trust boundary is created (D-07). |
| catalogue proof -> tree state | The proof asserts the generated catalogue and the dsx/ tree are unchanged; it writes nothing and edits no tracking file (D-07). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-10 | Tampering | A Phase-14 artifact silently minting a code via a gate check (DATA-DICTIONARY existence, learnings, disclosure heading-scanner) | high | mitigate | D-07: gen-finding-catalogue --check + the two-leg invariant (count==256 AND set-identity) re-run after each plan name any added/removed code; a mint is a hard stop. |
| T-14-11 | Tampering | A data library (pandas/scipy/numpy) or the CSV-opening profiler creeping into a GATE_PROFILES module's import closure | medium | mitigate | tests/test_gate_path_hermetic.py walks the closure of every gate module and asserts FORBIDDEN imports and dsx.profiler are absent — a standing guard stronger than the current implicit purity (D-01/D-03/D-07). |
| T-14-12 | Tampering | An accidental edit to dsx/cli.py CHECKS/GATE_PROFILES or dsx/checks/* during the phase | high | mitigate | `git status --porcelain -- dsx/` empty and `grep -c report.add dsx/cli.py == 0` asserted here; files_modified is tests/-only. |
| T-14-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs occur in this phase; the test is stdlib-only. |
</threat_model>

<verification>
This plan delivers Success Criterion 6 (no new blocking finding codes ship in this phase — asserted
by a catalogue diff, not by review — REQ-P14-06) and holds the D-01 gate-path bound for the whole
phase.

For this plan specifically:
- The catalogue stays at 256, set-identical to the Phase-12 snapshot (both legs of the invariant).
- `gen-finding-catalogue.py --check` exits 0.
- `tests/test_gate_path_hermetic.py` passes: no pandas/scipy/numpy/csv in any gate module's closure; profiler absent from dq's closure.
- `git status --porcelain -- dsx/` is empty and `grep -c report.add dsx/cli.py` is 0.
</verification>

<success_criteria>
- The finding catalogue is unchanged: 256 codes, set-identical to tests/fixtures/finding-codes-phase12.md; gen-finding-catalogue.py --check exits 0 (REQ-P14-06 / D-07).
- tests/test_gate_path_hermetic.py pins the gate-path purity (no data library / no profiler in any gate module's import closure) and passes against the current tree (D-07).
- No path under dsx/ is modified; grep -c report.add dsx/cli.py is 0; no new finding code is minted.
</success_criteria>

<output>
Create `.planning/phases/14-compounding-and-data-onboarding/14-05-SUMMARY.md` when done.
</output>
</content>
