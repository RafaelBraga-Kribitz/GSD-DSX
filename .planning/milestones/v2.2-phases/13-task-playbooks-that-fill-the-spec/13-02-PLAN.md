---
phase: 13-task-playbooks-that-fill-the-spec
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/dsx-root-cause/SKILL.md
  - skills/dsx-segment/SKILL.md
autonomous: true
requirements: [REQ-P13-01]
tags: [dsx, skill, playbook, root-cause, segment, routing]

must_haves:
  truths:
    - "A skill `dsx-root-cause` exists at skills/dsx-root-cause/SKILL.md with house frontmatter and routes question_type: diagnostic and results.segments {name,effect,n} at DSX-MET-030/031 and the causal guard DSX-CAU-*/DSX-COH-001/010, mirroring the EDA 5B decomposition (Success Criterion 1)."
    - "A skill `dsx-segment` exists at skills/dsx-segment/SKILL.md with house frontmatter and routes design.multiplicity.family[]+correction, results.segments and results.comparisons_looked_at at DSX-SPEC-043, DSX-EXP-050..053 and DSX-MET-030/031 (Success Criterion 1)."
    - "D-02 anti-parallel-advice: no line in either new SKILL.md states a numeric statistical threshold or correction method that is not on the same line as a DSX-* code citation."
  artifacts:
    - skills/dsx-root-cause/SKILL.md
    - skills/dsx-segment/SKILL.md
  key_links:
    - "dsx-segment must name the `correction` FIELD but route the method choice to DSX-SPEC-043 / DSX-EXP-050..053, or it becomes parallel advice."
    - "dsx-root-cause must keep the diagnostic label under DSX-CAU-*/DSX-COH-001/010, or the causal guard has nothing to hold the wording to."
  prohibitions:
    - statement: "Zero new DSX-* finding codes are minted; the catalogue stays exactly 256."
      status: enforced
      verification: "These files are markdown; the catalogue generator only walks dsx/**/*.py (gen-finding-catalogue.py:226-229). Certified phase-wide by plan 13-05 (set-identity diff)."
    - statement: "No edits to the deterministic dsx gate path — dsx/cli.py CHECKS/GATE_PROFILES (:64-131), dsx/checks/*, dsx/frame/* stay read-only."
      status: enforced
      verification: "files_modified touches no path under dsx/."
    - statement: "No Python added under skills/; the playbooks are pure prompt-guidance and are NOT loop-wired."
      status: enforced
      verification: "the two new SKILL.md are markdown; registration is handled name-only by plan 13-01 (D-01) and this plan adds no capability.json entry."
---

<objective>
Create the second two task-router playbooks — `dsx-root-cause` and `dsx-segment`. Each is a ROUTER,
not an author: it names which existing `ANALYSIS-SPEC.yaml` fields the analyst fills and which
already-shipped deterministic gate reads each field, and it states no threshold or correction method
of its own (D-02).

Purpose: give diagnostic ("why did X move") and multi-cut segmentation work a skill that fills the
spec at the existing Simpson/mixture, multiplicity and causal-guard gates, so the rigour comes from
the deterministic checks in `dsx/`, not from a second copy of the advice.

Output: `skills/dsx-root-cause/SKILL.md` and `skills/dsx-segment/SKILL.md`. Registration of these two
names is handled by plan 13-01's capability.json append (the phase's single shared-file write); this
plan does not touch capability.json.
</objective>

<artifacts_this_phase_produces>
This plan creates:
- `skills/dsx-root-cause/SKILL.md` (new markdown playbook)
- `skills/dsx-segment/SKILL.md` (new markdown playbook)

It produces NO new symbols under `dsx/` and NO new `DSX-*` finding codes. It edits no JSON and no
existing skill. The registration of these two names in `capabilities/dsx/capability.json` is done by
plan 13-01 (single writer), so this plan has zero file overlap with 13-01 and runs in the same wave.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-task-playbooks-that-fill-the-spec/13-CONTEXT.md

# Frontmatter house shape every new SKILL.md must mirror
@skills/dsx-define-metrics/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skills/dsx-root-cause/SKILL.md as a router to the diagnostic + Simpson gates</name>
  <files>skills/dsx-root-cause/SKILL.md</files>
  <read_first>
    - skills/dsx-define-metrics/SKILL.md:1-13 (house frontmatter shape)
    - .planning/phases/13-task-playbooks-that-fill-the-spec/13-CONTEXT.md (D-02 contract row for dsx-root-cause)
    - dsx/spec.py:22-28 (question_type, including `diagnostic`)
    - skills/dsx-explore-data/SKILL.md:356-386 (EDA branch 5B decomposition — the body dsx-root-cause mirrors, not rewrites)
    - dsx/checks/design.py:41 and :550-593 (the causal guard DSX-CAU-* that keeps a label honest)
  </read_first>
  <action>
    Create `skills/dsx-root-cause/SKILL.md`. Frontmatter mirrors the house shape at
    `skills/dsx-define-metrics/SKILL.md:1-13`: `name: dsx-root-cause`, a one-sentence `description`
    (a "why did this metric move" diagnostic question), an `argument-hint`, and `allowed-tools`
    (Read, Write, Edit, Bash, Grep, Glob, Agent). Router body: (1) set `question_type: diagnostic`
    (`dsx/spec.py:22-28`). (2) Fill `results.segments` with `{name, effect, n}` rows; a Simpson /
    mixture reversal in those rows is adjudicated by `DSX-MET-030` (CRITICAL) and `DSX-MET-031`
    (HIGH); the skill names the field and defers the reversal ruling to those checks. (3) The causal
    guard keeps the diagnostic label honest under `DSX-CAU-*` and `DSX-COH-001` / `DSX-COH-010` — the
    skill routes diagnostic wording to those and must NOT assert causation the identification strategy
    does not support. (4) Mirror (do not rewrite) the EDA branch 5B decomposition body at
    `skills/dsx-explore-data/SKILL.md:356-386` — point at it rather than restating it. Tell the analyst
    to run `dsx gate plan` and `dsx check` to adjudicate. State no numeric threshold of its own; every
    number, if present, sits on the same line as its owning `DSX-*` code.
  </action>
  <acceptance_criteria>
    - `skills/dsx-root-cause/SKILL.md` exists; frontmatter has `name: dsx-root-cause`, `description`, `argument-hint`, `allowed-tools` (grep confirms each).
    - The body cites `question_type: diagnostic`, `DSX-MET-030`, `DSX-MET-031`, and at least one of `DSX-CAU-`/`DSX-COH-001`/`DSX-COH-010`, and points at `dsx-explore-data/SKILL.md` for the 5B decomposition (grep each returns a hit).
    - Anti-parallel-advice: `grep -hEi '(p ?[<>=]|\balpha\b|α|[0-9]+ ?%|[<>]=? ?0?\.[0-9]|\bbonferroni\b|\bholm\b|\bbenjamini\b|\bfdr\b|\bsidak\b)' skills/dsx-root-cause/SKILL.md | grep -v 'DSX-'` returns no lines.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "^name: dsx-root-cause" skills/dsx-root-cause/SKILL.md && grep -q "diagnostic" skills/dsx-root-cause/SKILL.md && grep -q "DSX-MET-030" skills/dsx-root-cause/SKILL.md && grep -q "dsx-explore-data" skills/dsx-root-cause/SKILL.md && ! (grep -hEi '(p ?[<>=]|\balpha\b|α|[0-9]+ ?%|[<>]=? ?0?\.[0-9]|\bbonferroni\b|\bholm\b|\bbenjamini\b|\bfdr\b|\bsidak\b)' skills/dsx-root-cause/SKILL.md | grep -q 'DSX-\|.')</automated>
  </verify>
  <done>skills/dsx-root-cause/SKILL.md exists, mirrors the house frontmatter, routes the diagnostic label and results.segments to DSX-MET-030/031 and the causal guard, points at the EDA 5B body, and carries no unattributed threshold.</done>
</task>

<task type="auto">
  <name>Task 2: Author skills/dsx-segment/SKILL.md as a router to the multiplicity + Simpson gates</name>
  <files>skills/dsx-segment/SKILL.md</files>
  <read_first>
    - skills/dsx-define-metrics/SKILL.md:1-13 (house frontmatter shape)
    - .planning/phases/13-task-playbooks-that-fill-the-spec/13-CONTEXT.md (D-02 contract row for dsx-segment)
    - dsx/spec.py:1046-1055 (DSX-SPEC-043 multiplicity) and dsx/checks/design.py:362-412 (DSX-EXP-050/051/052/053)
    - skills/dsx-explore-data/SKILL.md:586-599 (the candidate->confirmatory promotion handshake to reuse: promotion is a spec amendment adding the test to design.multiplicity.family; never promotes into decision.replay)
  </read_first>
  <action>
    Create `skills/dsx-segment/SKILL.md`. Frontmatter mirrors the house shape: `name: dsx-segment`, a
    one-sentence `description` (a multi-cut / who-differs segmentation question), an `argument-hint`,
    and `allowed-tools` (Read, Write, Edit, Bash, Grep, Glob, Agent). Router body: (1) declare the
    confirmatory cuts in `design.multiplicity.family[]` plus the `correction` field — adjudicated by
    `DSX-SPEC-043` (`dsx/spec.py:1046-1055`) and `DSX-EXP-050`/`051`/`052`/`053`
    (`dsx/checks/design.py:362-412`); the skill names the `correction` FIELD but must NOT prescribe a
    correction method of its own — that ruling is the gate's. (2) Fill `results.segments` and
    `results.comparisons_looked_at`. (3) A Simpson reversal across cuts is adjudicated by `DSX-MET-030`
    / `DSX-MET-031`. Reuse the candidate→confirmatory promotion handshake at
    `skills/dsx-explore-data/SKILL.md:586-599` (promotion is a spec amendment through
    `dsx-scope-analysis` that adds the intended test to `design.multiplicity.family`; a candidate is
    never promoted into `decision.replay`). Tell the analyst to run `dsx gate plan` and `dsx check` to
    adjudicate. State no threshold or correction method of its own; every number, if present, sits on
    the same line as its owning `DSX-*` code.
  </action>
  <acceptance_criteria>
    - `skills/dsx-segment/SKILL.md` exists; frontmatter has `name: dsx-segment`, `description`, `argument-hint`, `allowed-tools` (grep confirms each).
    - The body cites `design.multiplicity.family`, `DSX-SPEC-043`, `DSX-EXP-050`, `DSX-MET-030`, and points at `dsx-explore-data/SKILL.md` for the candidate→confirmatory handshake (grep each returns a hit).
    - The `correction` field is named as a field to fill, but no correction-method name (bonferroni/holm/benjamini/fdr/sidak) appears on a line without a `DSX-` citation (the anti-parallel-advice grep below covers this).
    - Anti-parallel-advice: `grep -hEi '(p ?[<>=]|\balpha\b|α|[0-9]+ ?%|[<>]=? ?0?\.[0-9]|\bbonferroni\b|\bholm\b|\bbenjamini\b|\bfdr\b|\bsidak\b)' skills/dsx-segment/SKILL.md | grep -v 'DSX-'` returns no lines.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "^name: dsx-segment" skills/dsx-segment/SKILL.md && grep -q "DSX-SPEC-043" skills/dsx-segment/SKILL.md && grep -q "DSX-EXP-050" skills/dsx-segment/SKILL.md && grep -q "design.multiplicity.family" skills/dsx-segment/SKILL.md && ! (grep -hEi '(p ?[<>=]|\balpha\b|α|[0-9]+ ?%|[<>]=? ?0?\.[0-9]|\bbonferroni\b|\bholm\b|\bbenjamini\b|\bfdr\b|\bsidak\b)' skills/dsx-segment/SKILL.md | grep -q 'DSX-\|.')</automated>
  </verify>
  <done>skills/dsx-segment/SKILL.md exists, mirrors the house frontmatter, routes multiplicity + correction and results.segments to DSX-SPEC-043/DSX-EXP-050..053 and DSX-MET-030/031, reuses the candidate→confirmatory handshake, and carries no unattributed threshold or correction method.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds two markdown SKILL.md files. It introduces no executable surface, no network or file
input on any runtime path, no auth, and no user-supplied input on any deterministic gate path. ASVS
Level 1 injection / auth / session vectors are N/A — there is no exploitable surface to assess. No
npm / pip / cargo install occurs, so the supply-chain vector (T-13-SC) is N/A this plan.

| Boundary | Description |
|----------|-------------|
| analyst → ANALYSIS-SPEC.yaml | The playbook is prose read by an analyst; the spec it produces is adjudicated only by the existing deterministic `dsx` gates. No new trust boundary is created. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | The two SKILL.md files stating a threshold/correction method of their own (parallel-advice drift), especially dsx-segment naming a correction method | medium | mitigate | D-02 route-and-cite rule: name the `correction` field and route the method to DSX-SPEC-043 / DSX-EXP-050..053; enforced by the per-task anti-parallel-advice grep (S1-4). |
| T-13-02 | Tampering | Accidental edit to the deterministic gate path | high | mitigate | D-01/D-08 scope fence: markdown only; files_modified touches no path under `dsx/`; certified phase-wide by the plan 13-05 catalogue set-identity diff (REQ-P13-06). |
| T-13-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs occur in this phase. |
</threat_model>

<verification>
Phase-level goal-backward coverage is documented in plan 13-01. This plan completes Success
Criterion 1's coverage (the four skills exist + route to existing gates) with root-cause and segment,
and completes the D-02 anti-parallel-advice check for the second two playbooks — the union of
13-01 and 13-02 covers all four SKILL.md files.

For this plan specifically:
- Both new SKILL.md files exist with house frontmatter and route their fields to the named existing codes.
- The anti-parallel-advice grep returns no unattributed threshold or correction-method lines for either file.
</verification>

<success_criteria>
- skills/dsx-root-cause/SKILL.md and skills/dsx-segment/SKILL.md exist, mirror the house frontmatter, and are pure routers citing existing DSX-* codes.
- No path under dsx/ is modified; no new finding code is minted; no capability.json edit is made here.
</success_criteria>

<output>
Create `.planning/phases/13-task-playbooks-that-fill-the-spec/13-02-SUMMARY.md` when done.
</output>
