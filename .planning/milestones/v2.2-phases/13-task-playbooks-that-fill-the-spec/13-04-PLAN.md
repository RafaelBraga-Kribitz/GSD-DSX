---
phase: 13-task-playbooks-that-fill-the-spec
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/dsx-scope-analysis/SKILL.md
  - capabilities/dsx/fragments/executor.md
autonomous: true
requirements: [REQ-P13-04, REQ-P13-05]
tags: [dsx, skill, tier-routing, executor, entrypoint]

must_haves:
  truths:
    - "skills/dsx-scope-analysis/SKILL.md classifies the engagement, recommends a ceremony tier, and emits the exact command `pwsh scripts/gsd-tier.ps1 -Tier N` for lookup -> Tier 0 exploratory (dsx.enforce=false), ad-hoc -> Tier 1 published artifact (dsx.enforce=true), and full pipeline -> Tier 2 code others run (full ceremony, mode=interactive), matching docs/gsd-tiers.md:39-56; it does NOT itself mutate global configuration (Success Criterion 4 / REQ-P13-04 / D-05)."
    - "capabilities/dsx/fragments/executor.md carries one prose bullet preferring a scripts/*.py entrypoint over a notebook as reproducibility.entrypoint, citing DSX-REP-040 (dsx/checks/repro.py:176-197) and the DSX-CODE fit-order scan (dsx/checks/code.py), framed as ordering fidelity and explicitly NOT a leakage claim; the deterministic gate stays suffix-neutral (Success Criterion 5 / REQ-P13-05 / D-06)."
  artifacts:
    - skills/dsx-scope-analysis/SKILL.md
    - capabilities/dsx/fragments/executor.md
  key_links:
    - "The tier block must emit `pwsh scripts/gsd-tier.ps1 -Tier N` and must NOT carry a raw configuration-mutation command, or D-05's advisory boundary (no silent global mutation from a scope skill) is broken — the skill recommends, the operator applies."
    - "The executor bullet must cite DSX-REP-040 and the DSX-CODE scan by name (route-and-cite, mirroring the existing DSX-MET-040 citation at executor.md:34-35) rather than restating either rule, and must sell the benefit as ordering fidelity, not leakage prevention."
  prohibitions:
    - statement: "Zero new DSX-* finding codes are minted; the catalogue stays exactly 256."
      status: enforced
      verification: "Both edits are markdown; the catalogue generator only walks dsx/**/*.py (gen-finding-catalogue.py:226-229), so it is structurally incapable of minting a code. Certified phase-wide by plan 13-05 (set-identity diff)."
    - statement: "No edits to the deterministic dsx gate path — dsx/cli.py CHECKS/GATE_PROFILES (:64-131), dsx/checks/*, dsx/frame/* stay read-only; the entrypoint preference adds NO notebook-blocking gate and changes NO check behaviour (the DSX-CODE scan stays suffix-neutral)."
      status: enforced
      verification: "files_modified touches no path under dsx/; the executor bullet is prose guidance, not a check."
    - statement: "Tier routing is advisory only (D-05): the scope skill emits the tier command but performs NO configuration mutation of its own; any auto-apply is deferred behind an explicit operator opt-in flag, never an ungated side effect of writing a spec."
      status: enforced
      verification: "the tier block routes through the `gsd-tier.ps1` helper and carries no raw config-mutation command; the anti-mutation grep in Task 1 asserts this."
---

<objective>
Two edits that fill the last two spec-shaping requirements without touching the gate path: (1) add
an advisory ceremony-tier routing table to `dsx-scope-analysis` (REQ-P13-04 / D-05) and (2) add one
executor-fragment bullet preferring a `scripts/*.py` entrypoint over a notebook (REQ-P13-05 / D-06).
Both are prompt-guidance edits that route to existing behaviour and cite existing codes; neither
mints a code, adds a check, or mutates configuration.

Purpose: the scope skill should tell the operator which ceremony tier fits the engagement and print
the exact `gsd-tier.ps1` command to apply it — but never flip the whole project's global
`workflow.*`/`dsx.enforce`/`mode` keys silently mid-loop (D-05). And the executor should prefer a
linear `.py` entrypoint so `reproducibility.entrypoint` is a faithful record of execution order,
riding two checks that already make a script preferable (D-06).

Output: an edit to `skills/dsx-scope-analysis/SKILL.md` (the tier-routing table) and an edit to
`capabilities/dsx/fragments/executor.md` (the entrypoint-preference bullet).

Judgment on the "possibly scope-analysis.md" note (13-CONTEXT integration points): the tier table
lives in `skills/dsx-scope-analysis/SKILL.md`, NOT the `capabilities/dsx/fragments/scope-analysis.md`
fragment. The fragment is a terse planner-facing "produce ANALYSIS-SPEC.yaml before planning; fill it
with the dsx-scope-analysis skill" instruction; tier routing is analyst-facing scope guidance and
belongs in the skill the analyst actually reaches for. Keeping it out of the fragment also keeps
files_modified minimal and avoids editing a planner-injected file for analyst-facing content.
</objective>

<artifacts_this_phase_produces>
This plan edits two existing files:
- `skills/dsx-scope-analysis/SKILL.md` (adds the advisory tier-routing table, D-05)
- `capabilities/dsx/fragments/executor.md` (adds one entrypoint-preference bullet, D-06)

It produces NO new files, NO new symbols under `dsx/`, NO new `DSX-*` finding codes, and mutates NO
configuration. Zero file overlap with plans 13-01, 13-02, 13-03 (skills/dsx-{cohort,funnel,root-cause,segment},
capability.json, dsx-explore-data, dsx-narrate) and 13-05 (tests/) — same wave (wave 1).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-task-playbooks-that-fill-the-spec/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the advisory ceremony-tier routing table to dsx-scope-analysis</name>
  <files>skills/dsx-scope-analysis/SKILL.md</files>
  <read_first>
    - skills/dsx-scope-analysis/SKILL.md (the existing process/gates shape the table is added into; note the pre-existing `dsx power --alpha 0.05 --power 0.8` tool-command line at :42 — a legitimate command example, NOT parallel advice, and out of scope for the anti-parallel-advice rule)
    - .planning/phases/13-task-playbooks-that-fill-the-spec/13-CONTEXT.md (D-05: advisory, not a config mutation; the fixed lookup/ad-hoc/full-pipeline -> Tier 0/1/2 mapping; auto-apply deferred behind an explicit operator opt-in flag)
    - docs/gsd-tiers.md:39-56 (the Tier 0/1/2 table + the `pwsh scripts/gsd-tier.ps1 -Tier N` helper the skill emits; note the helper is the thing that flips the global keys, not the skill)
  </read_first>
  <action>
    Edit `skills/dsx-scope-analysis/SKILL.md` to add an advisory ceremony-tier routing step (D-05).
    Classify the engagement and recommend a tier by a fixed mapping, cross-checked to
    `docs/gsd-tiers.md:39-56`: a **lookup** (throwaway, no audience) routes to **Tier 0 exploratory**
    (`dsx.enforce=false`); an **ad-hoc** published-but-not-executed artifact routes to **Tier 1
    published artifact** (`dsx.enforce=true`, the DS gates on); a **full pipeline** — code other people
    run — routes to **Tier 2 code others run** (full ceremony, `mode=interactive`). For the recommended
    tier, the skill EMITS the exact command `pwsh scripts/gsd-tier.ps1 -Tier N` (N in 0/1/2) for the
    operator to run, and cites `docs/gsd-tiers.md` as the authority for what each tier flips. The skill
    RECOMMENDS and PRINTS the command; it must never mutate global GSD configuration itself — the
    `gsd-tier.ps1` helper is what applies the change, and only when the operator chooses to run it. State
    explicitly that tier routing is advisory: any auto-apply is deferred behind an explicit operator
    opt-in flag and is never an ungated side effect of writing a spec (D-05 / Deferred Ideas). The tier
    table routes ceremony configuration, not statistics — it introduces no statistical threshold. Add no
    Python and no new field.
  </action>
  <acceptance_criteria>
    - `skills/dsx-scope-analysis/SKILL.md` names all three tiers (Tier 0, Tier 1, Tier 2) and the fixed lookup/ad-hoc/full-pipeline mapping, references `dsx.enforce`, and cites `docs/gsd-tiers.md` (grep each returns a hit).
    - The tier block emits the helper command `pwsh scripts/gsd-tier.ps1 -Tier N` (grep `gsd-tier.ps1` returns a hit).
    - The skill carries NO raw configuration-mutation command of its own — it routes through the `gsd-tier.ps1` helper (grep for the raw config mutator returns no hit; the Task verify asserts this).
    - Anti-parallel-advice scoping note: the whole-file statistical-threshold grep from plans 13-01/13-02 is intentionally NOT run here because the file already carries a legitimate `dsx power --alpha 0.05` tool-command example at :42 unrelated to tier routing; the tier block itself carries no statistical threshold (manual read confirms it routes config tiers only).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "gsd-tier.ps1" skills/dsx-scope-analysis/SKILL.md && grep -Eiq "Tier[ -]?0" skills/dsx-scope-analysis/SKILL.md && grep -Eiq "Tier[ -]?1" skills/dsx-scope-analysis/SKILL.md && grep -Eiq "Tier[ -]?2" skills/dsx-scope-analysis/SKILL.md && grep -q "dsx.enforce" skills/dsx-scope-analysis/SKILL.md && grep -q "gsd-tiers" skills/dsx-scope-analysis/SKILL.md && ! grep -q "config-set" skills/dsx-scope-analysis/SKILL.md</automated>
  </verify>
  <done>skills/dsx-scope-analysis/SKILL.md carries the advisory tier table mapping lookup/ad-hoc/full-pipeline to Tier 0/1/2, emits `pwsh scripts/gsd-tier.ps1 -Tier N`, cites docs/gsd-tiers.md, mutates no configuration itself, and adds no statistical threshold or new field.</done>
</task>

<task type="auto">
  <name>Task 2: Add the scripts/*.py entrypoint-preference bullet to the executor fragment</name>
  <files>capabilities/dsx/fragments/executor.md</files>
  <read_first>
    - capabilities/dsx/fragments/executor.md (the "Rules that hold for every task" bullet list the new bullet joins; note the house route-and-cite style at :34-35 citing DSX-MET-040 inline rather than restating the SQL rule — mirror it)
    - .planning/phases/13-task-playbooks-that-fill-the-spec/13-CONTEXT.md (D-06: one bullet riding two shipped checks; ordering-fidelity rationale, NOT a leakage claim; gate stays suffix-neutral)
    - dsx/checks/repro.py:176-197 (DSX-REP-040 fires HIGH on an `.ipynb` entrypoint unless runs_clean_top_to_bottom; its remedy is "move the logic into a module the notebook imports")
    - dsx/checks/code.py:1-24 (the DSX-CODE fit-order scan reads `.py` source directly; for an `.ipynb` it reports reconstructed-cell offsets a reader cannot usefully open at that offset; the scan is suffix-neutral — reads `.py` and `.ipynb` identically)
  </read_first>
  <action>
    Edit `capabilities/dsx/fragments/executor.md` to add ONE prose bullet to the "Rules that hold for
    every task" list preferring a `scripts/*.py` entrypoint over a notebook as
    `reproducibility.entrypoint` (D-06). Route-and-cite in the house style (mirror the DSX-MET-040
    citation at :34-35 — name the code, do not restate the rule): a notebook entrypoint fires
    `DSX-REP-040` HIGH unless it is confirmed to run clean top-to-bottom, whose own remedy is to move
    the logic into a module the notebook imports (`dsx/checks/repro.py:176-197`); and the `DSX-CODE`
    fit-order scan reads a `.py` source directly, where a saved `.ipynb` only offers reconstructed-cell
    offsets a reader cannot usefully open (`dsx/checks/code.py`). State the benefit as ordering
    fidelity: a linear `.py` makes `reproducibility.entrypoint` a faithful record of execution ORDER,
    removing the out-of-order cell-counter ambiguity a saved notebook can encode. State explicitly that
    this is ordering fidelity and NOT a leakage claim, and that the deterministic `dsx check code` gate
    stays suffix-neutral (it reads `.py` and `.ipynb` identically) — this bullet changes no gate
    behaviour and blocks no notebook. Add no threshold of its own; keep it one bullet.
  </action>
  <acceptance_criteria>
    - `capabilities/dsx/fragments/executor.md` gains one bullet citing `DSX-REP-040`, mentioning a `scripts/` `.py` entrypoint and a notebook/`.ipynb`, and framing the benefit as execution ordering (grep `DSX-REP-040`, `scripts/`, `\.ipynb`, and an ordering term each return a hit).
    - The bullet frames the benefit as ordering fidelity, NOT leakage prevention, and notes the gate stays suffix-neutral (manual read confirms no leakage claim is made).
    - Anti-parallel-advice: `grep -hEi '(p ?[<>=]|\balpha\b|α|[0-9]+ ?%|[<>]=? ?0?\.[0-9]|\bbonferroni\b|\bholm\b|\bbenjamini\b|\bfdr\b|\bsidak\b)' capabilities/dsx/fragments/executor.md | grep -v 'DSX-'` returns no lines (the bullet introduces no unattributed statistical threshold).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "DSX-REP-040" capabilities/dsx/fragments/executor.md && grep -q "scripts/" capabilities/dsx/fragments/executor.md && grep -q "\.ipynb" capabilities/dsx/fragments/executor.md && grep -Eiq "ordering|execution order" capabilities/dsx/fragments/executor.md && ! (grep -hEi '(p ?[<>=]|\balpha\b|α|[0-9]+ ?%|[<>]=? ?0?\.[0-9]|\bbonferroni\b|\bholm\b|\bbenjamini\b|\bfdr\b|\bsidak\b)' capabilities/dsx/fragments/executor.md | grep -q 'DSX-\|.')</automated>
  </verify>
  <done>capabilities/dsx/fragments/executor.md carries one bullet preferring a scripts/*.py entrypoint over a notebook, citing DSX-REP-040 and the DSX-CODE scan, framed as ordering fidelity (not leakage) with the gate staying suffix-neutral, and no unattributed threshold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan edits two markdown files — one skill and one executor fragment. It introduces no
executable surface, no network or file input on any runtime path, no auth, and no user-supplied
input on any deterministic gate path. ASVS Level 1 injection / auth / session vectors are N/A here —
there is no exploitable surface to assess. No npm / pip / cargo install occurs, so the supply-chain
vector (T-13-SC) is N/A this plan.

| Boundary | Description |
|----------|-------------|
| scope skill -> global GSD configuration | The tier routing must stay advisory: the skill prints the `gsd-tier.ps1` command but never mutates `workflow.*`/`dsx.enforce`/`mode` itself. The only mutation is the operator running the helper. No new automated trust boundary is created (D-05). |
| executor fragment -> ANALYSIS-SPEC.yaml | The bullet is prose read by the executor; the spec it shapes is adjudicated only by the existing deterministic `dsx` gates. No new trust boundary is created. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | The scope skill silently mutating global `workflow.*`/`dsx.enforce`/`mode` as an ungated side effect of scoping (the exact hazard D-05 fences off) | high | mitigate | D-05 advisory boundary: the skill classifies, recommends, and EMITS `pwsh scripts/gsd-tier.ps1 -Tier N` for the operator to run; it performs no configuration mutation itself; enforced by the Task 1 anti-mutation grep and the deferral of auto-apply behind an explicit operator opt-in flag. |
| T-13-02 | Tampering | The executor bullet overstating the `.py` preference as leakage prevention, restating a check rule (parallel advice), or being read as a gate that blocks notebooks | medium | mitigate | D-06 route-and-cite: name `DSX-REP-040` + the `DSX-CODE` scan (mirroring the DSX-MET-040 citation at :34-35), frame the benefit as ordering fidelity NOT leakage, and state the gate stays suffix-neutral so no behaviour changes and no notebook is blocked; enforced by the per-task greps and the anti-parallel-advice grep. |
| T-13-03 | Tampering | Accidental edit to the deterministic gate path (`dsx/cli.py` CHECKS/GATE_PROFILES, `dsx/checks/*`) or minting a code | high | mitigate | D-08 scope fence: markdown edits only; files_modified touches no path under `dsx/`; certified phase-wide by the plan 13-05 catalogue set-identity diff (REQ-P13-06). |
| T-13-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs occur in this phase. |
</threat_model>

<verification>
Phase-level goal-backward coverage (union across all five plans) is documented in plan 13-01. This
plan delivers Success Criterion 4 (tier routing -> Tier 0/1/2 matching docs/gsd-tiers.md, REQ-P13-04)
and Success Criterion 5 (executor prefers scripts/*.py over a notebook, REQ-P13-05).

For this plan specifically:
- `skills/dsx-scope-analysis/SKILL.md` carries the advisory tier table, emits `gsd-tier.ps1`, cites docs/gsd-tiers.md, and holds no configuration-mutation command of its own.
- `capabilities/dsx/fragments/executor.md` carries one entrypoint-preference bullet citing DSX-REP-040 and the DSX-CODE scan, framed as ordering fidelity, with the gate staying suffix-neutral.
- The anti-parallel-advice grep returns no unattributed threshold lines for the executor fragment.
</verification>

<success_criteria>
- skills/dsx-scope-analysis/SKILL.md recommends lookup->Tier 0 / ad-hoc->Tier 1 / full-pipeline->Tier 2, emits `pwsh scripts/gsd-tier.ps1 -Tier N`, and mutates no configuration itself (D-05).
- capabilities/dsx/fragments/executor.md prefers a scripts/*.py entrypoint over a notebook, citing DSX-REP-040 and the DSX-CODE scan, framed as ordering fidelity not leakage, with the gate suffix-neutral (D-06).
- No path under dsx/ is modified; no new finding code is minted; no configuration is mutated.
</success_criteria>

<output>
Create `.planning/phases/13-task-playbooks-that-fill-the-spec/13-04-SUMMARY.md` when done.
</output>
