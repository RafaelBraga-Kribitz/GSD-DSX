---
phase: 16-re-run-verification-off-the-gate-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dsx/checks/repro.py
  - references/finding-codes.md
  - tests/test_finding_catalogue_invariant.py
  - tests/test_reproduce_report.py
autonomous: true
requirements: [REQ-P16-02]
tags: [dsx, repro, gate-path-pure, finding-catalogue, mint, DSX-REP-060, DSX-REP-061, strict-only]

must_haves:
  truths:
    - "At verify/ship, `dsx gate` emits DSX-REP-060 (HIGH) when `reproducibility.reproduce_report` is declared but the named `REPRO-REPORT.md` is missing, and DSX-REP-061 (HIGH) when the report is present but its declared lead-metric number does not overlap `results.tests[0]` — proven by tests/test_reproduce_report.py against tmp-dir fixtures (REQ-P16-02 / D-02 / D-04 / D-05)."
    - "The gate emits NEITHER code when `reproducibility.reproduce_report` is absent (silent opt-out, D-02) NOR when the report carries an explicit SKIPPED/UNABLE status (honest skip, D-11); a report whose status/verdict claims success but whose numbers do not overlap STILL emits DSX-REP-061 (the gate recomputes nothing and trusts no verdict line, D-04)."
    - "The check is declaration-only and gate-path-pure: dsx/checks/repro.py imports only pathlib/re/math + relative `from ..findings`/`from ..spec`, never pandas/scipy/numpy/csv, never subprocess/runpy/os.system/exec — asserted by tests/test_gate_path_hermetic.py (14-05) still passing and by the phase-16 no-execution scan (16-04) (D-01)."
    - "The catalogue rebaseline is additive and non-silent: `python scripts/gen-finding-catalogue.py --check` exits 0 at 258, tests/test_finding_catalogue_invariant.py passes with `_EXPECTED_TOTAL`=258 and set-identity against `snapshot ∪ {DSX-REP-060, DSX-REP-061}`, while the byte-frozen `tests/fixtures/finding-codes-phase12.md` anchor stays at 256 and untouched (D-08)."
  artifacts:
    - dsx/checks/repro.py
    - references/finding-codes.md
    - tests/test_finding_catalogue_invariant.py
    - tests/test_reproduce_report.py
  key_links:
    - "`_check_reproduce_report(spec, repro, report, phase_dir)` is called ONLY from the existing `if strict:` branch of `repro.check` (beside `_check_repro_lock`), so it runs at verify/ship only and early-returns on empty `results.tests` — the two properties that keep execute-time silent and mirror _check_repro_lock (D-03)."
    - "The DSX-REP-060/061 `report.add` MESSAGE (3rd positional arg) is a fixed plain string, so the generator renders the exact catalogue row text; every dynamic value (metric name, declared vs report number, tolerance) goes in `detail=` only — an f-string message would render `<…>` and break the row text (gen-finding-catalogue.py:177-193)."
    - "The invariant's snapshot-length leg is re-anchored to a SEPARATE literal 256 (`_SNAPSHOT_TOTAL`), NOT `_EXPECTED_TOTAL` (now 258) — because the phase-12 snapshot file stays byte-frozen at 256; conflating them would make the len-check fail after the bump (D-08 trap #3)."
  prohibitions:
    - statement: "The committed good fixture (examples/good-ANALYSIS-SPEC.yaml) is NOT given a `reproducibility.reproduce_report` field; it declares an entrypoint but no reproduce_report, so it stays SILENT under this check — the D-02 back-compat pin. Positive/negative overlap paths are proven with tmp-dir fixtures in tests/test_reproduce_report.py, not by committing a REPRO-REPORT.md beside the good fixture."
      status: enforced
      verification: "`git status --porcelain -- examples/` is empty for this plan; tests/test_reproduce_report.py asserts the good fixture emits neither DSX-REP-060 nor DSX-REP-061."
    - statement: "The tabular/execution families never enter the check: no import of pandas/scipy/numpy/csv and no subprocess/runpy/os.system/exec/eval/compile in dsx/checks/repro.py; the entrypoint is never executed on the gate path."
      status: enforced
      verification: "tests/test_gate_path_hermetic.py (FORBIDDEN={pandas,scipy,numpy,csv}) still passes; the 16-04 static AST scan flags no execution primitive in dsx/checks/repro.py; imports added are re + math only (stdlib)."
    - statement: "The phase-12 snapshot anchor tests/fixtures/finding-codes-phase12.md is NEVER mutated; the mint is expressed only as an explicit `∪ {DSX-REP-060, DSX-REP-061}` delta over the frozen 256-code set."
      status: enforced
      verification: "`git status --porcelain -- tests/fixtures/finding-codes-phase12.md` is empty; the invariant's set-identity leg compares current against `snapshot_set | {060,061}` and any code beyond that delta still trips the test."
---

<objective>
Deliver REQ-P16-02's enforcement half and the phase's only catalogue mint. Add a
declaration-only helper `_check_reproduce_report` to `dsx/checks/repro.py` that, at verify/ship
only, emits DSX-REP-060 when `reproducibility.reproduce_report` is declared but the named
`REPRO-REPORT.md` file is missing, and DSX-REP-061 when the report is present but its declared
lead-metric number does not overlap `results.tests` — while importing no pandas/scipy and executing
no entrypoint. Then regenerate `references/finding-codes.md` to 258 codes and rebaseline the
catalogue invariant ADDITIVELY (D-08), leaving the phase-12 snapshot anchor byte-frozen.

Purpose: this is the DAAF "reproduced" verdict stolen onto the gate as a pure declaration check —
the gate asserts a produced report EXISTS and its named numbers MATCH what the human wrote, without
re-running anything. It is the ONE plan in Phase 16 that mints (DSX-REP-060, DSX-REP-061, both HIGH
so verify/ship — threshold HIGH — actually block), so it carries the D-08 additive-rebaseline
hazard the whole phase turns on: three artifacts (regen catalogue, bump `_EXPECTED_TOTAL`, extend the
expected-set) must move in lockstep, and the frozen snapshot must not.

Output: the two new codes wired into the existing `if strict:` branch, the regenerated 258-code
catalogue, the additively-rebaselined invariant, and a behavioural test file proving all four
firing/silence conditions including the D-11 SKIPPED short-circuit.
</objective>

<artifacts_this_phase_produces>
This plan (16-01) is the sole wave-1 plan and the sole minting plan of Phase 16. It creates:
- `tests/test_reproduce_report.py` (new — behavioural proof of DSX-REP-060/061 firing, silence, and the D-11 SKIPPED short-circuit)

and edits:
- `dsx/checks/repro.py` (add `_check_reproduce_report`; call it from the existing `if strict:` branch)
- `references/finding-codes.md` (REGENERATE via the generator — 256 → 258)
- `tests/test_finding_catalogue_invariant.py` (D-08 additive rebaseline: `_EXPECTED_TOTAL` 256→258; set-identity vs `snapshot ∪ {060,061}`; snapshot-len anchored to a separate literal 256)

It does NOT touch `examples/` (the good fixture stays silent, D-02), `dsx/spec.py` (the
`reproducibility` block is an open dict — `reproduce_report` needs no schema key), `dsx/cli.py`
(`repro` is already registered and dispatched `strict=strict`), or the phase-12 snapshot fixture.

## Wave structure and disjointness justification (whole phase)
Wave 1: **16-01 alone.** It establishes the 258-code baseline and the modified `repro.py` before any
other plan runs. Wave 2: **16-02, 16-03, 16-04**, each `depends_on: [16-01]`. This ordering (option b
from the plan brief) is chosen over "all four in wave 1" for two correctness reasons, not speed:
(1) 16-04's static no-execution scan asserts over `dsx/checks/repro.py` AFTER this plan adds
`_check_reproduce_report`, so its teeth bite on the merged file, not a stale copy; (2) any catalogue
re-check in 16-02/03/04 then runs against a HEAD already at 258, so none can assert the wrong (256)
baseline. Within wave 2 the three plans are pairwise `files_modified`-disjoint
(skills/+templates/+capabilities/ vs examples/known-bad/*-ATTRIBUTION.yaml + tests/test_known_bad_corpus.py
vs tests/test_no_entrypoint_execution.py), satisfying single-writer-per-wave.

## Phase-level goal-backward coverage (union across all four Phase-16 plans)
- Success Criterion 1 (REQ-P16-01) — plan 16-02: skill `dsx-reproduce` + `templates/REPRO-REPORT.md` + capability registration.
- Success Criterion 2 (REQ-P16-02) — **this plan (16-01)**: DSX-REP-060/061 declaration-only gate check + the 258 catalogue mint + additive invariant rebaseline.
- Success Criterion 3 (REQ-P16-03) — plan 16-03: `protocol_adherence` on the three ATTRIBUTION sidecars + accepted-but-ignored corpus assertion (calibration pinned).
- Success Criterion 4 (REQ-P16-04) — plan 16-04: static AST no-entrypoint-execution guard with a positive control.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-re-run-verification-off-the-gate-path/16-CONTEXT.md
@dsx/checks/repro.py
@dsx/cli.py
@tests/test_finding_catalogue_invariant.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _check_reproduce_report to dsx/checks/repro.py (DSX-REP-060/061, strict-only, gate-path-pure)</name>
  <files>dsx/checks/repro.py</files>
  <read_first>
    - dsx/checks/repro.py:24-40 (check(): the `if strict:` branch where `_check_repro_lock` is called — the new helper is called immediately after it, same branch, same signature style)
    - dsx/checks/repro.py:200-279 (_check_repro_lock: the closest analog — strict-only, early-return on empty `results.tests`, distinguishes key-omitted vs explicit-null, emits via report.add and report.ok)
    - dsx/checks/repro.py:147-173 (_check_code_pointer: the phase_dir file-existence pattern — `candidate = Path(phase_dir) / value`, `alt = Path(value)`, `.exists()` on both — reuse this exact shape for locating REPRO-REPORT.md)
    - dsx/checks/repro.py:11-14 (current imports: only `from pathlib import Path`, `from ..findings import Report`, `from ..spec import as_number, is_blank, items, normalize, section` — this task ADDS top-level `import re` and `import math`, both stdlib; add NO dsx-dotted import, keep every dsx import relative `from ..x`)
    - dsx/cli.py:115-140 (GATE_PROFILES has `repro` in execute/verify/ship; GATE_THRESHOLDS verify/ship = HIGH — this is why both new codes must be HIGH to flip exit 1)
    - dsx/cli.py:184-192 (repro dispatched `repro.check(spec, phase_dir, strict=strict)` with `strict = gate_point in {"verify","ship"}` — no cli.py edit needed)
    - examples/good-ANALYSIS-SPEC.yaml:202-258 (the reproducibility block declares an entrypoint but NO reproduce_report — must stay silent; results.tests[0] is metric `activation_rate`, effect 0.024 — the lead metric and lead value the overlap check reads)
    - scripts/gen-finding-catalogue.py:177-193 (extract(): the catalogue row Finding-column is the VERBATIM 3rd positional report.add arg; an f-string message renders `<…>`, so the 060/061 message strings must be plain constants — dynamic values go in detail= only)
  </read_first>
  <action>
    Add `import re` and `import math` to the top-level stdlib imports of dsx/checks/repro.py (do not
    import yaml, pandas, scipy, numpy, or csv; do not add any dsx-dotted import). Define a new module
    function `_check_reproduce_report(spec, repro, report, phase_dir)` and call it from the existing
    `if strict:` branch of `check()`, on the line immediately after `_check_repro_lock(spec, repro,
    report)`.

    Trigger and early-returns (D-02, D-03): read `declared = repro.get("reproduce_report")`. If it
    `is_blank`, return immediately (field absent → gate silent; this is the opt-in, NEVER keyed on
    entrypoint-presence). Then read `tests = items(section(spec, "results"), "tests")`; if empty,
    return (early-return on empty results.tests, mirroring _check_repro_lock — nothing to reproduce).

    Locate the report file exactly as _check_code_pointer locates the entrypoint: when `phase_dir` is
    set, form `candidate = Path(phase_dir) / str(declared)` and `alt = Path(str(declared))`, pick
    whichever exists; when `phase_dir` is None, use `Path(str(declared))`. If neither exists, emit
    DSX-REP-060 at severity HIGH with the FIXED plain-string message (3rd positional arg, no
    f-string) exactly: Reproduce report declared (`reproducibility.reproduce_report`) but
    `REPRO-REPORT.md` is missing — the reproduced verdict is unsubstantiated. Put the declared path in
    a `detail=` f-string and `where="spec.reproducibility.reproduce_report"`, then return.

    Parse the report (D-04, CRLF-safe): read the file with `Path.read_text(encoding="utf-8")`. Extract
    the FIRST fenced machine-readable block — the block opened by a line whose info string is the
    three-backtick `yaml` marker and closed by the next three-backtick line — using an `re` pattern
    built on `\r?\n` (never a bare `\n`-anchored pattern; this checkout is CRLF). Within the block,
    parse flat mapping lines with a `\r?\n`-tolerant split and a per-line match of the form
    optional-space, key (word/dot/hyphen chars), optional-space, colon, value; collect a
    `dict[str, str]`.

    SKIPPED short-circuit (D-11): read the block's `status` value. If it is one of `skipped` or
    `unable` (compare case-insensitively, trimmed), the reproduce was an honest opt-out with no fresh
    numbers — call `report.ok(...)` noting the skipped status and return WITHOUT emitting DSX-REP-061.
    This is the analog of DSX-REP-051's null-repro_lock honest opt-out and is what makes "a missing
    interpreter on a reproduce skill run is not a gate exit 1" true.

    Overlap check (D-04, verdict-agnostic): the lead metric is `tests[0]`. Read `lead_metric =
    str(tests[0].get("metric"))` and the lead value as `as_number(tests[0].get("effect"))`, falling
    back to `as_number(tests[0].get("p_value"))` when `effect` is absent. Read the report's declared
    value as `as_number(block.get(lead_metric))`. Emit DSX-REP-061 at severity HIGH when the report
    value is missing/non-numeric OR is not within tolerance of the spec value, using
    `math.isclose(report_value, spec_value, rel_tol=1e-2, abs_tol=1e-9)` — tolerance deliberately
    WEAKER than equality (a rounding-level difference overlaps; a gross disagreement such as a 10×
    error does not). The message (3rd positional arg) is the FIXED plain string exactly:
    `REPRO-REPORT.md` present but its declared re-run numbers do not overlap `results.tests` — the
    analysis does not reproduce. Put the metric name, the spec value, and the report value in a
    `detail=` f-string and `where="spec.reproducibility.reproduce_report"`. Otherwise call
    `report.ok(...)`. The check reads NO PASS/FAIL/verdict line from the report — only the `status`
    field (for the SKIPPED short-circuit) and the numeric block — so a report whose verdict claims
    success but whose lead number disagrees STILL emits DSX-REP-061.
  </action>
  <acceptance_criteria>
    - `_check_reproduce_report(spec, repro, report, phase_dir)` exists in dsx/checks/repro.py and is called from the `if strict:` branch of check(), immediately after `_check_repro_lock`.
    - dsx/checks/repro.py imports only pathlib/re/math plus relative `from ..findings`/`from ..spec`; it imports no pandas/scipy/numpy/csv/yaml and contains no subprocess/runpy/os.system/exec/eval/compile call.
    - DSX-REP-060 and DSX-REP-061 are both emitted at severity "HIGH"; their 3rd-positional messages are the two fixed plain strings above (no f-string interpolation in the message).
    - The check returns silently when `reproduce_report` is blank, when `results.tests` is empty, and when the report status is skipped/unable; it never executes the declared entrypoint.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast; t=ast.parse(open('dsx/checks/repro.py',encoding='utf-8').read()); imp={a.name.split('.')[0] for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names} | {n.module.split('.')[0] for n in ast.walk(t) if isinstance(n,ast.ImportFrom) and n.level==0 and n.module}; assert not ({'pandas','scipy','numpy','csv','yaml','subprocess','runpy','os'} & imp), imp; assert '_check_reproduce_report' in {f.name for f in ast.walk(t) if isinstance(f,ast.FunctionDef)}" && python -m unittest tests.test_gate_path_hermetic >/dev/null 2>&1</automated>
  </verify>
  <done>dsx/checks/repro.py defines _check_reproduce_report (called strict-only from check()), stays stdlib-pure (no pandas/scipy/numpy/csv/yaml, no subprocess/runpy/os/exec), emits DSX-REP-060/061 at HIGH with the two fixed messages, honours the D-11 SKIPPED short-circuit, and the gate-path hermetic test still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Prove firing, silence, and the SKIPPED short-circuit in tests/test_reproduce_report.py</name>
  <files>tests/test_reproduce_report.py</files>
  <read_first>
    - tests/test_dsx.py:2246-2280 (the repro unit-test section: the `codes(...)` idiom over a Report, and the DSX-REP-031 tmp-phase_dir existence test using `repro.check(spec, phase_dir=tmp)`)
    - tests/test_dsx.py:3534-3551 (test_repro_lock_missing_and_null: the strict-only assertion shape `codes(repro_mod.check(spec, strict=True))` — the exact analog for a strict-only code)
    - dsx/checks/repro.py:24-40 (check signature `check(spec, phase_dir=None, *, strict=False)` — how the new tests invoke it)
    - examples/good-ANALYSIS-SPEC.yaml:202-258 (the good spec's reproducibility + results.tests — load it to assert it stays silent, and to build overlap fixtures whose lead metric is activation_rate / effect 0.024)
  </read_first>
  <action>
    Create tests/test_reproduce_report.py, a stdlib-only unittest module (import unittest, tempfile,
    pathlib; import `from dsx.checks import repro`; do not import third-party packages). Define a small
    local helper that returns the set of finding codes from a Report (mirror test_dsx.py's `codes`
    idiom: `{f.code for f in report.findings}`). Add ROOT via `Path(__file__).resolve().parent.parent`
    so the good fixture can be loaded with `dsx.loader.load`.

    Write these test methods, each building a minimal spec dict with a non-empty `results: {tests:
    [{metric: "activation_rate", effect: 0.024, p_value: 0.0011}]}` and a `reproducibility` block, and
    calling `repro.check(spec, phase_dir=tmp, strict=True)` inside a `tempfile.TemporaryDirectory()`
    unless noted:

    1. test_060_fires_when_declared_but_report_missing: `reproducibility.reproduce_report:
       "REPRO-REPORT.md"`, tmp dir with NO such file → DSX-REP-060 in the codes.
    2. test_060_is_strict_only: same spec, `strict=False` → DSX-REP-060 NOT in the codes (execute-time
       stays silent, D-03).
    3. test_silent_when_reproduce_report_absent: spec with an entrypoint but no `reproduce_report` key
       → neither DSX-REP-060 nor DSX-REP-061; AND load examples/good-ANALYSIS-SPEC.yaml with
       dsx.loader.load and assert `repro.check(good, phase_dir=<repo phase>, strict=True)` emits
       neither code (the D-02 back-compat pin — the good fixture declares an entrypoint but no
       reproduce_report).
    4. test_061_fires_when_numbers_disagree: write a REPRO-REPORT.md into tmp containing a
       three-backtick `yaml` fenced block with `status: reproduced` and `activation_rate: 0.24` (a 10×
       error) → DSX-REP-061 in the codes.
    5. test_silent_when_numbers_overlap: same but the block reads `activation_rate: 0.024` → neither
       DSX-REP-060 nor DSX-REP-061 (overlap within rel_tol).
    6. test_061_short_circuits_on_skipped_status: report present, block `status: skipped` with no
       usable number → neither code fires (D-11 — the honest missing-interpreter opt-out; this is the
       load-bearing "not a gate exit 1" test).
    7. test_verdict_pass_does_not_suppress_061: report present, block carries a success-claiming
       verdict line/status such as `status: reproduced` (NOT skipped/unable) yet `activation_rate:
       0.24` → DSX-REP-061 still fires (D-04 — the gate trusts numbers, not a verdict).

    Write every REPRO-REPORT.md with UTF-8 and content that exercises the `\r?\n` parse (the check
    must tolerate CRLF; the test may write "\n" — the check's parser is CRLF-safe regardless). Do not
    edit any file under examples/ or dsx/.
  </action>
  <acceptance_criteria>
    - tests/test_reproduce_report.py exists, is stdlib-only, and defines at least the seven methods above.
    - The missing-report, disagreeing-numbers, and verdict-pass-with-bad-numbers cases assert the expected code fires; the absent-field, overlapping-numbers, and skipped-status cases assert neither code fires; the strict=False case asserts DSX-REP-060 does not fire.
    - The good-fixture leg loads examples/good-ANALYSIS-SPEC.yaml and asserts it emits neither DSX-REP-060 nor DSX-REP-061.
    - `python -m unittest tests.test_reproduce_report` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_reproduce_report >/dev/null 2>&1 && python -c "import ast; t=ast.parse(open('tests/test_reproduce_report.py',encoding='utf-8').read()); ms={f.name for f in ast.walk(t) if isinstance(f,ast.FunctionDef)}; need={'test_060_fires_when_declared_but_report_missing','test_061_fires_when_numbers_disagree','test_061_short_circuits_on_skipped_status','test_verdict_pass_does_not_suppress_061'}; assert need<=ms, need-ms"</automated>
  </verify>
  <done>tests/test_reproduce_report.py is a stdlib-only unittest proving DSX-REP-060 fires on a declared-but-missing report (strict-only), DSX-REP-061 fires on non-overlapping numbers even under a success verdict, and neither fires when the field is absent, the numbers overlap, or the status is SKIPPED — and it passes.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate the catalogue to 258 and rebaseline tests/test_finding_catalogue_invariant.py additively (D-08)</name>
  <files>references/finding-codes.md, tests/test_finding_catalogue_invariant.py</files>
  <read_first>
    - scripts/gen-finding-catalogue.py:226-265 (collect() + render(): dedupes by code, sorts, writes `**Total: N codes.**` and the REP group rows — running the generator without --check rewrites references/finding-codes.md)
    - references/finding-codes.md:295-307 (the REP table: DSX-REP-030..053 today; 060/061 render after 053 by string sort)
    - references/finding-codes.md:16 (`**Total: 256 codes.**` — becomes 258 after regen)
    - tests/test_finding_catalogue_invariant.py:30-31 (`_EXPECTED_TOTAL = 256` — bump to 258)
    - tests/test_finding_catalogue_invariant.py:44-86 (count leg: reads Total line and rows, both must equal _EXPECTED_TOTAL — name/docstring/messages still say 256/"Phase 12 mints no code")
    - tests/test_finding_catalogue_invariant.py:89-119 (set-identity leg: `assertEqual(len(snapshot_set), _EXPECTED_TOTAL, ...)` then `assertEqual(current_set, snapshot_set, ...)` — BOTH lines change; the snapshot-len must anchor to a NEW literal 256, the set-identity must compare against `snapshot_set | {060,061}`)
    - .planning/phases/16-re-run-verification-off-the-gate-path/16-CONTEXT.md (D-08: three artifacts move in lockstep, snapshot file byte-frozen, any code beyond phase12 ∪ {060,061} still trips the test)
  </read_first>
  <action>
    First regenerate the catalogue: run `python scripts/gen-finding-catalogue.py` (no --check) so it
    rewrites references/finding-codes.md from the real report.add call sites. After Task 1, this adds
    the DSX-REP-060 and DSX-REP-061 rows in the REP group and updates the total line to
    `**Total: 258 codes.**`. Confirm the two new rows read exactly, both HIGH, with the fixed message
    text from Task 1. The pre-existing "declared twice" warnings on stderr
    (DSX-CLM-020/021, COH-030, PAR-002, SPEC-070, VAL-021/060) are shipped-tree noise and do not
    change the exit code or the count — do not attempt to fix them.

    Then rebaseline tests/test_finding_catalogue_invariant.py ADDITIVELY:
    (a) change `_EXPECTED_TOTAL = 256` to `_EXPECTED_TOTAL = 258`;
    (b) add two module constants near it: `_SNAPSHOT_TOTAL = 256` (the byte-frozen phase-12 anchor
        size) and `_MINTED_CODES = {"DSX-REP-060", "DSX-REP-061"}` (the explicit Phase-16 delta);
    (c) in the set-identity test, change the snapshot-length assertion so it compares
        `len(snapshot_set)` against `_SNAPSHOT_TOTAL` (256), NOT `_EXPECTED_TOTAL` — the snapshot file
        stays 256 while the live catalogue is 258, so this MUST be a separate literal or the leg fails
        after the bump;
    (d) in the same test, change the set-identity assertion from `current_set == snapshot_set` to
        `current_set == snapshot_set | _MINTED_CODES`, and update the `added`/`removed` diff and the
        failure message to describe the expected `∪ {DSX-REP-060, DSX-REP-061}` delta so any code
        beyond `phase12 ∪ {060,061}` (or a missing minted code) still names itself and fails;
    (e) update the count test's method name, docstring, and assertion messages that assert "256" /
        "Phase 12 mints no finding code" so they read honestly for the additive Phase-16 mint at 258
        (e.g. rename to `test_finding_catalogue_stays_at_258_codes`).

    Do NOT edit tests/fixtures/finding-codes-phase12.md — it is the byte-frozen anchor; leave it at
    256. Do not touch any tracking file.
  </action>
  <acceptance_criteria>
    - references/finding-codes.md declares `**Total: 258 codes.**` and contains a DSX-REP-060 row and a DSX-REP-061 row, both HIGH, with the two fixed messages.
    - `python scripts/gen-finding-catalogue.py --check` exits 0 (catalogue current at 258).
    - tests/test_finding_catalogue_invariant.py has `_EXPECTED_TOTAL = 258`, a separate `_SNAPSHOT_TOTAL = 256`, a `_MINTED_CODES` set, the snapshot-length leg anchored to 256, and the set-identity leg comparing against `snapshot_set | _MINTED_CODES`; both legs pass.
    - tests/fixtures/finding-codes-phase12.md is unchanged (still 256, byte-frozen).
  </acceptance_criteria>
  <verify>
    <automated>python scripts/gen-finding-catalogue.py --check && python -m unittest tests.test_finding_catalogue_invariant >/dev/null 2>&1 && [ "$(grep -c 'DSX-REP-060' references/finding-codes.md)" -ge 1 ] && [ "$(grep -c 'DSX-REP-061' references/finding-codes.md)" -ge 1 ] && [ "$(grep -c 'Total: 258 codes' references/finding-codes.md)" -ge 1 ] && [ -z "$(git status --porcelain -- tests/fixtures/finding-codes-phase12.md)" ]</automated>
  </verify>
  <done>references/finding-codes.md is regenerated to 258 with the two HIGH DSX-REP-06x rows, gen-finding-catalogue.py --check exits 0, the invariant passes with _EXPECTED_TOTAL=258 and a separate 256 snapshot anchor comparing against snapshot ∪ {060,061}, and the phase-12 snapshot fixture is byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds a declaration-only check to the deterministic gate path and mints two finding codes.
The check reads the spec dict (already loaded, no new parser) and a phase-local `REPRO-REPORT.md`
via `Path.read_text` + stdlib `re` — it opens no data file, imports no tabular library, and never
executes the declared entrypoint. There is no network, no auth, no user-supplied input beyond the
already-trusted spec and a report file the same operator wrote. ASVS Level 1 injection/auth/session
vectors are N/A. No npm/pip/cargo install occurs (stdlib only), so T-16-SC is N/A this plan.

| Boundary | Description |
|----------|-------------|
| spec + REPRO-REPORT.md → check | The check reads the spec's `reproducibility.reproduce_report` string and the named report's text; it parses a fenced number block with stdlib re and compares one number with math.isclose. It writes nothing and runs nothing (D-01). |
| report.add → catalogue → invariant | The two new report.add sites define DSX-REP-060/061; the generator reads them; the invariant pins the additive 258 delta. A silent mint/drop is caught by the two-leg invariant (D-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | A future "simplification" shelling/runpy-ing the entrypoint from the check to "check it runs" | critical | mitigate | D-01: the check is Path.exists + re + math only; tests/test_gate_path_hermetic.py and the 16-04 static AST scan both cover dsx/checks/repro.py; no subprocess/runpy/os/exec is imported or called. |
| T-16-02 | Tampering | Parsing the report with pandas/scipy/csv, pulling a data library onto the gate | high | mitigate | D-01/D-04: the fenced block is parsed with stdlib re on `\r?\n`; the import-closure hermetic test asserts FORBIDDEN={pandas,scipy,numpy,csv} absent; imports added are re+math only. |
| T-16-03 | Tampering | Trusting a skill-authored PASS verdict so a non-reproducing analysis ships | high | mitigate | D-04: the check reads only the numeric block and the SKIPPED status; test_verdict_pass_does_not_suppress_061 proves a success verdict with disagreeing numbers still emits DSX-REP-061. |
| T-16-04 | Tampering | A silent or cardinality-preserving catalogue mint/drop hiding the two new codes | high | mitigate | D-08: gen-finding-catalogue.py --check at 258 + the two-leg invariant (count 258 AND set-identity vs snapshot ∪ {060,061}); the byte-frozen 256 snapshot anchor makes any drift name itself. |
| T-16-05 | Denial of Service | A legitimately skipped reproduce (interpreter absent) firing DSX-REP-061 and forcing exit 1 the ROADMAP forbids | high | mitigate | D-11: the SKIPPED/UNABLE status short-circuits DSX-REP-061; test_061_short_circuits_on_skipped_status pins it — an honest opt-out analogous to DSX-REP-051. |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs occur; the check and tests are stdlib-only. |
</threat_model>

<verification>
This plan delivers Success Criterion 2 (REQ-P16-02): `dsx gate verify`/`ship` emits DSX-REP-060 when
`reproducibility.reproduce_report` is declared but `REPRO-REPORT.md` is missing, DSX-REP-061 when the
report is present but its lead number does not overlap `results.tests`, and NEITHER when the field is
absent or the report is honestly SKIPPED — importing no pandas/scipy and executing no entrypoint.

For this plan specifically:
- `python -m unittest tests.test_reproduce_report` passes all seven firing/silence/short-circuit cases.
- `python scripts/gen-finding-catalogue.py --check` exits 0 at 258; the two DSX-REP-06x rows are present and HIGH.
- `python -m unittest tests.test_finding_catalogue_invariant` passes both legs (258 count AND set-identity vs snapshot ∪ {060,061}); the phase-12 snapshot fixture is byte-unchanged at 256.
- `python -m unittest tests.test_gate_path_hermetic` still passes (repro.py stays stdlib-pure).
</verification>

<success_criteria>
- DSX-REP-060 and DSX-REP-061 exist as HIGH codes, wired strict-only into repro.check, with the two fixed catalogue-row messages (REQ-P16-02 / D-05 / D-06).
- The check is declaration-only and gate-path-pure: no pandas/scipy/numpy/csv, no subprocess/runpy/os/exec, no entrypoint execution (D-01); the good fixture stays silent (D-02).
- DSX-REP-061 short-circuits on an explicit SKIPPED/UNABLE report status and never trusts a verdict line (D-11 / D-04).
- The catalogue is regenerated to 258, the invariant is rebaselined additively with a separate 256 snapshot anchor, and the phase-12 snapshot file is untouched (D-08).
</success_criteria>

<output>
Create `.planning/phases/16-re-run-verification-off-the-gate-path/16-01-SUMMARY.md` when done.
</output>
