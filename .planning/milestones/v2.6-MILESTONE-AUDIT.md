---
milestone: v2.6
milestone_name: Exploration Depth and Backlog Evidence
audited: 2026-09-10T18:33Z
head_at_audit: 352ab13
auditor: ceremony orchestrator (opus/high, LOOP-BRIEF §3) + gsd-integration-checker (haiku, adaptive profile)
method: 3-source requirements cross-reference (phase VERIFICATION.md `— MET` sections + SUMMARY `requirements_completed` frontmatter + REQUIREMENTS.md traceability), orchestrator re-run of every load-bearing gate on a clean tree on the real interpreter (LOOP-BRIEF §5), cross-phase integration check
status: passed
scores:
  requirements: 18/18
  phases: 6/6
  integration: 4/4
  flows: 2/2 (profile→EDA-keys→5 skills; ANALYSIS-SPEC→gate→finding-codes on the 42+15 corpus — both pass at their thresholds)
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: milestone-wide
    items:
      - "SUMMARY frontmatter `requirements_completed` is empty for 26-01, 26-02, 26-04, 27-01, 27-02, 28-01, 28-02, 29-02, 30-01, 30-02 (populated only for 25-01..04 and 26-03). The mechanical 3-source matrix would flag those REQ-IDs 'partial (verify manually)' on that basis alone. Hand-verified this audit against the authoritative source — each phase VERIFICATION.md carries a dedicated `### REQ-Pxx-yy — MET` section for all three of its requirements (25:01/02/03, 26:01/02/03, 27:01/02/03, 28:01/02/03, 29:01/02/03, 30:01/02/03), and REQUIREMENTS.md marks all 18 `[x]` + Met. Non-blocking: this is the single-writer discipline's known bookkeeping cost (executors run WRITE-ONLY and never touch REQUIREMENTS.md/frontmatter; the orchestrator populates coverage serially in VERIFICATION.md + REQUIREMENTS.md), not missing data. Same class of CLI/frontmatter blind spot flagged at v2.4's close."
      - "Phase 29 has a 29-02-SUMMARY.md but no 29-01-SUMMARY.md (the 29-01 mint wave was executed WRITE-ONLY across a crashed-then-recovered firing per LOOP-LEDGER S5-3/S5-4; the orphan was adopted and every gate independently re-run, but the 29-01 SUMMARY was never authored). Non-blocking: 29-VERIFICATION.md carries the full 3/3 REQ-P29-01/02/03 coverage with evidence; the missing summary is a documentation-completeness residual, not a coverage gap."
      - "12 pre-existing 'declared twice with different text' catalogue warnings surface on `gen-finding-catalogue.py --check` (DSX-CLM-020/021, DSX-COH-030, DSX-PAR-002, DSX-SPEC-070 ×3, DSX-VAL-021, DSX-VAL-060, plus the by-design two-severity dedup rows DSX-COH-041 ×2 and DSX-ML-034). All are the documented two-severity-dedup / legacy render pattern; catalogue reports 'current' @279 with exit 0 regardless. Non-failing, out of milestone scope."
      - "REQUIREMENTS.md traceability lists all 18 REQ-IDs already `[x]`. Per the audit status matrix this is 'satisfied'; the S7-5 /gsd-complete-milestone transition archives the file — standing note: hand-verify the generated accomplishments and archived checkboxes (wrong at every close so far)."
      - "LOOP-BRIEF §5 mislocates `_SECTION_65_BACKLOG_CODES` as `scripts/gen-finding-catalogue.py`; it actually lives in `tests/test_known_bad_corpus.py:968` (already documented in 27-RESEARCH.md:242). Doc-only inaccuracy in the brief; the disjointness invariant is intact (all three minted codes are OUT of the backlog set, assertion at test_known_bad_corpus.py:1763). Not a wiring break."
consented_scope: []
nyquist:
  compliant_phases: [25, 26, 27, 28, 29, 30]
  partial_phases: []
  not_validated_phases: []
  missing_phases: []
  overall: compliant
open_human_items:  # all tracked in HUMAN-QUEUE.md; drained at S7-2 (operator-gated), NOT audit gaps
  d05_citation: [HQ-40]                                        # brief §4 cat.1 — the 5-row v2.6 citation pack; ANSWERED 2026-09-07 (40a/40c/40e primary; 40b secondary-corroborated, flagged; 40d dropped)
  security_signoff_and_uat: [HQ-41, HQ-42, HQ-43, HQ-44, HQ-45, HQ-46]  # brief §4 cat.4 — the six per-phase end-of-phase sign-offs; batched to S7-2, unsigned by design (loop verifies mitigations, does not sign)
remaining_closeout:  # NOT audit gaps — sequenced human-gated units in LOOP-LEDGER S7
  s7_2_drain_human_queue: "operator-gated — the six SECURITY.md approval lines + UAT acceptances (HQ-41..46); the loop re-gated all threats technically (25:8/8, 26:8/8, 27:12/12, 28:14/14, 29:15/15, 30:11/11 CLOSED) but §4 cat.4 forbids self-signing"
  s7_5_complete_milestone: "interactive-only (NOT headless-safe: interactive prompts + git rm REQUIREMENTS.md) — runs in an interactive session per operator approval"
  s7_6_ship: "outward-facing (merge to main by EXPLICIT branch name `gsd/v2.6.0-exploration-depth` + tag v2.6.0) — brief §4 cat.5; loop may not self-approve"
---

# Milestone v2.6 "Exploration Depth and Backlog Evidence" — Audit

**Status: PASSED (not gaps-accepted).** 18/18 requirements satisfied; 6/6 phases verified
PASSED; 4/4 cross-phase integration seams wired; Nyquist compliant across all six phases.
Full suite green on the real interpreter (**Ran 1629 tests → OK, no skips, 68.9s**, Python
3.12.10), `scripts/check.sh` "all checks passed" (unit tests + catalogue current + capability
conformant + gate contract + determinism), `node install.mjs --check` self-test passed (6/6
agents, 14/14 skills, 5 gates), catalogue current @279 with zero-mint set-identity 279→279, on a
clean tree at HEAD `352ab13`. No unsatisfied requirements, no orphaned requirements, no critical
gaps.

This milestone deepens the exploration surface and pays down three gated backlog cases. Phase 25
grows `dsx profile` to emit the trust-core statistics hermetically (stdlib-only, additive, every
existing key byte-stable, cited Hyndman & Fan type-7 quantiles with reference-value fixtures).
Phase 26 gives five analysis skills an explicit named-input read contract over the Phase-25 profile
keys, enforced by an off-gate-path repo-integrity test. Phases 27–29 build and *measure-first* three
corpus evidence cases under LOOP-BRIEF D-13 — a feature-origin-only leak (minted `DSX-ML-034`), a
magnitude no reported test computed (minted `DSX-CLM-034`), and subgroup harm under a prescriptive
recommendation (minted `DSX-COH-041`, the corpus's first `kind: target` fixture) — each a documented
live miss before any check was designed, each code carrying a D-05 citation. Phase 30 re-baselines
calibration terminally against the grown corpus (42 known-bad + 15 good-control) and mints zero
codes. The `dsx` tool is an internal CLI + library — **no HTTP routes, no UI** — so "integration"
means cross-phase code wiring plus two genuine end-to-end surfaces: the profile→skill read path and
the ANALYSIS-SPEC→gate→finding-code path over the corpus.

## Requirements coverage (3-source cross-reference)

All 18 REQ-IDs are satisfied against three independent sources: REQUIREMENTS.md traceability
(`[x]` + "Met"), each phase VERIFICATION.md's dedicated `### REQ-Pxx-yy — MET` section, and (where
present) the per-plan SUMMARY `requirements_completed` frontmatter. Where the SUMMARY frontmatter is
empty (see tech_debt), the VERIFICATION.md `— MET` section is the authoritative confirmation.

| REQ-ID | REQUIREMENTS.md | VERIFICATION.md | Completing plan(s) | Status |
|--------|-----------------|-----------------|--------------------|--------|
| REQ-P25-01 | `[x]` Met | MET (P25 passed) | 25-01/02/03 | **satisfied** |
| REQ-P25-02 | `[x]` Met | MET | 25-01/02/03 | **satisfied** |
| REQ-P25-03 | `[x]` Met | MET | 25-03/04 | **satisfied** |
| REQ-P26-01 | `[x]` Met | MET (P26 passed) | 26-01/02 | **satisfied** |
| REQ-P26-02 | `[x]` Met | MET | 26-03 | **satisfied** |
| REQ-P26-03 | `[x]` Met | MET | 26-04 | **satisfied** |
| REQ-P27-01 | `[x]` Met | MET (P27 passed) | 27-01/02 | **satisfied** |
| REQ-P27-02 | `[x]` Met | MET | 27-01 (DSX-ML-034) | **satisfied** |
| REQ-P27-03 | `[x]` Met | MET | 27-02 | **satisfied** |
| REQ-P28-01 | `[x]` Met | MET (P28 passed) | 28-01/02 | **satisfied** |
| REQ-P28-02 | `[x]` Met | MET | 28-01 (DSX-CLM-034) | **satisfied** |
| REQ-P28-03 | `[x]` Met | MET | 28-02 | **satisfied** |
| REQ-P29-01 | `[x]` Met | MET (P29 passed) | 29-01/02 | **satisfied** |
| REQ-P29-02 | `[x]` Met | MET | 29-01 (Gail & Simon 1985) | **satisfied** |
| REQ-P29-03 | `[x]` Met | MET | 29-01 (DSX-COH-041) / 29-02 (TARGET) | **satisfied** |
| REQ-P30-01 | `[x]` Met | MET (P30 passed) | 30-01 | **satisfied** |
| REQ-P30-02 | `[x]` Met | MET | 30-01/02 | **satisfied** |
| REQ-P30-03 | `[x]` Met | MET | 30-02 (zero-mint) | **satisfied** |

**Orphan detection:** none. Every REQ-ID in the REQUIREMENTS.md traceability table appears as a
`### REQ-Pxx-yy — MET` section in exactly its assigned phase's VERIFICATION.md. No requirement was
assigned but left unverified, and no requirement is verified outside its phase.

**FAIL gate:** not triggered — zero `unsatisfied` requirements. (The SUMMARY-frontmatter empties
resolve to **satisfied** on manual verification against the authoritative VERIFICATION.md sections,
not to `unsatisfied`.)

## Phase verifications (6/6 PASSED)

| Phase | VERIFICATION verdict | status | gaps | Score |
|-------|----------------------|--------|------|-------|
| 25 — hermetic profile depth | PASSED | passed | [] | 3/3 MET |
| 26 — per-skill read contracts | PASSED | passed | [] | 3/3 MET |
| 27 — feature-origin-only leak (DSX-ML-034) | PASSED | passed | [] | 3/3 MET |
| 28 — magnitude no test computed (DSX-CLM-034) | PASSED | passed | [] | 3/3 MET |
| 29 — subgroup harm prescriptive (DSX-COH-041, first `kind: target`) | PASSED | passed | [] | 3/3 MET |
| 30 — calibration re-baseline (zero-mint 279→279) | PASSED | passed | [] | 3/3 MET |

No phase is missing its VERIFICATION.md. No phase reports `gaps_found`.

## Cross-phase integration (4/4 WIRED)

Verified by gsd-integration-checker (haiku, read-only) and reconciled by the orchestrator against
independently re-run gates:

1. **P25 → P26 (hard dependency)** — every profile/EDA key the five skill read-steps name resolves
   against the live `templates/DATA-PROFILE.yaml` (incl. the `#`-commented example blocks the guard's
   uncomment pre-processor recovers) and `templates/EDA.md` front-matter. No orphaned read-step key.
   Guard `tests/test_skill_read_contracts.py` asserts `named == expected` and membership, with a live
   negative control that rejects a renamed key. *(REQ-P26-01/02, REQ-P25-01.)*
2. **P27/28/29 → shared finding catalogue (monotonic 276→279)** — `references/finding-codes.md:16`
   reads "Total: 279 codes"; exactly one row each for `DSX-ML-034` (CRITICAL, :163), `DSX-CLM-034`
   (HIGH, :236), `DSX-COH-041` (HIGH, :357); all three in `_D05_ALLOWLIST_CODES` by exact code
   (gen-finding-catalogue.py :209/:218/:228) and OUT of `_SECTION_65_BACKLOG_CODES`
   (disjointness asserted at test_known_bad_corpus.py:1763). *(REQ-P27-02, P28-02, P29-03, P30-03.)*
3. **P27/28/29 → corpus harness** — the three fixture families exist under `examples/known-bad/`
   (spec + entrypoint + postmortem + ATTRIBUTION sidecar each); 27 & 28 are permanent `kind: miss`
   (`_EXPECTED_CAUGHT_DEFECTS[...] = frozenset()`), 29 is the first `kind: target`
   (`_TARGET_DEFECT_CODES["subgroup-harm-without-disposition"]` names DSX-COH-041 at plan/verify/ship).
   Live known-bad spec count = 42, good-corpus = 15. *(REQ-P27-01/03, P28-01/03, P29-01/02/03.)*
4. **P30 re-baseline (terminal)** — `docs/literature/the-ai-data-scientist.md` row 15 reads "42
   known-bad specs, 15 good-control specs" (no "39" remains) and names all three v2.6 codes; the
   doc/code agreement test `tests/test_literature_corpus_count_agreement.py` binds the doc counts to
   live globs (42==42, 15==15) with no hardcoded literal. *(REQ-P30-01/02/03.)*

**Broken wiring / E2E gaps:** none. All four seams resolve WIRED end-to-end.

## Nyquist coverage (compliant)

| Phase | VALIDATION.md | status | nyquist_compliant | wave_0_complete |
|-------|---------------|--------|-------------------|-----------------|
| 25 | exists | validated | true | true |
| 26 | exists | validated | true | true |
| 27 | exists | validated | true | true |
| 28 | exists | validated | true | true |
| 29 | exists | validated | true | true |
| 30 | exists | validated | true | true |

Overall: **compliant** — all six phases `status: validated` with `nyquist_compliant: true`.

## Orchestrator-re-run gate evidence (clean tree, real interpreter, HEAD 352ab13)

- **Full suite:** `scripts/check.sh` with `python3` shimmed to the real 3.12 interpreter →
  **Ran 1629 tests in 68.890s → OK** (no skips, no failures; Python 3.12.10). `check.sh` also
  ran, in-sequence and green: catalogue currency, capability conformance
  ("capability 'dsx' v2.0.0 is conformant: 2 steps, 5 contributions, 5 gates, 14 skills, 6 agents"),
  the gate contract (good spec passes / bad spec blocks / missing spec exits 2 at plan→execute→
  verify→ship), and determinism (identical input → identical output). Final line: **"all checks
  passed"** (exit 0). *(The bare `python3` on this machine is a 3.14.6 package-less stub that would
  silently SKIP the matplotlib determinism test; the shim forces the real 3.12.10 so the run is
  authoritative — LOOP-BRIEF §5 standing rule.)*
- **Zero-mint catalogue (REQ-P30-03):** `python scripts/gen-finding-catalogue.py --check` → exit 0,
  "finding catalogue is current"; unique DSX-code count = **279** (set-identity 279→279 against the
  post-Phase-29 catalogue). The three v2.6 mints (DSX-ML-034, DSX-CLM-034, DSX-COH-041) are the only
  additions across the milestone (276→277→278→279); Phases 25, 26 and 30 mint zero.
- **Installer sync (REQ-P30-02):** `node install.mjs --check` → self-test passed, 6/6 agents,
  14/14 skills, 5 gates.
- **Corpus growth:** `examples/known-bad/*-ANALYSIS-SPEC.yaml` = 42 (was 39 pre-v2.6; +3 evidence
  cases); `examples/good-corpus/*-ANALYSIS-SPEC.yaml` = 15 (the FPR denominator).
- **Spec-count reconciliation:** `tests/test_dsx.py:593` asserts `len(paths) == 45` where `paths`
  enumerates `examples/*-ANALYSIS-SPEC.yaml` + `examples/known-bad/*` (42) + `templates/ANALYSIS-SPEC.yaml`
  (1) — a broader ANALYSIS-SPEC enumeration than the 42-known-bad corpus count, and green inside the
  passing suite. The "45" and the "42" are two independent, correctly-distinct counts (not an
  inconsistency; the integration checker's flag is resolved).

## Verdict

**PASSED (not gaps-accepted).** All 18 requirements met, all 6 phases verified PASSED, all 4
integration seams wired, Nyquist compliant across the milestone, no critical blockers. The
tech-debt items above are pre-existing (legacy catalogue warnings), documentation-completeness
residuals (empty SUMMARY frontmatter, the absent 29-01 SUMMARY, the brief's file mislocation), or
the standing REQUIREMENTS.md `[x]` transition handled at S7-5 — none introduced as a defect by
v2.6, none blocking. The remaining close-out units are human-gated by design and tracked in
LOOP-LEDGER S7 + HUMAN-QUEUE: S7-2 drain HUMAN-QUEUE (the six HQ-41..46 security sign-offs + UAT
acceptances, unsigned because the loop verifies mitigations but does not self-sign, §4 cat.4);
S7-5 /gsd-complete-milestone (interactive-only); S7-6 ship to main by explicit branch name + tag
`v2.6.0` (outward-facing, §4 cat.5). They are not audit gaps.
