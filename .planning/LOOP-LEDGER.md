# LOOP-LEDGER — v2.2 Analytic Surface

Source of truth for the autonomous loop. One checkbox = one unit.
Check a box ONLY with its gate evidence pasted in the Log.

**Scope source:** `.planning/ROADMAP.md` "Queued milestone — v2.2 Analytic Surface
(Phases 13–16)" and `.planning/REQUIREMENTS.md` "Queued — Milestone v2.2"
(23 requirements, REQ-P13-01 … REQ-P16-04). Both were written at v2.0.0 planning
time and carry full goals, success criteria, dependencies and ordering constraints —
**this milestone does not need a fresh scoping round; it needs execution.**

**Research backing:** `.planning/research/SURFACE.md` (2026-08-26). Per that file
and `brief.md` §6.5, comparison-repo READMEs are **not** admissible D-05 sources.

**Predecessor:** v2.0.0 DSX Validity Frame — shipped 2026-08-28, tag `v2.1.0`,
merged to `main`. Its loop artifacts are archived at
`.planning/milestones/v2.0.0-LOOP-{LEDGER,LEDGER-ARCHIVE}.md` and
`v2.0.0-HUMAN-QUEUE{,-ARCHIVE}.md`.

## Ordering rationale (read before reordering anything)

Phases are executed **13 → 14 → 16 → 15**, not in numeric order. This is deliberate
and satisfies every declared dependency:

- Phase 13 soft-blocks 14 (slash aliases must cover the new task skills) and
  soft-blocks 15 (the new spec fields want a skill that fills them) — so 13 is first.
- Phase 16 hard-depends only on Phase 12, which shipped — so it is free to run early.
- **Phase 15 is the only phase that mints new finding codes and therefore the only
  one carrying a D-05 human-read gate and a D-06 irreversible-numbering veto.**
  Running it last maximises the work the loop can complete without the operator,
  and its citation evidence pack is prepared up front (S0-3) so the operator can
  answer asynchronously while 13/14/16 build.

## S0 — Milestone bootstrap

- [x] S0-1 Point GSD state at the new milestone: `STATE.md` frontmatter (`milestone: v2.2`, `milestone_name: Analytic Surface`, `status: executing`, `current_phase: 13`, progress reset to 0/4 phases), body reconciled. Gate: `gsd-tools query init.milestone-op` resolves the new milestone without error; `.planning/phases/` exists and is empty (v2.0.0 dirs are archived, not deleted). — **DONE 2026-08-28T15:02Z:** frontmatter already at v2.2 (from milestone-transition `33dc7de`); body reconciled to a 79-line GSD-template digest (v2.0.0 history collapsed to archive pointers — full record in `MILESTONES.md` + `.planning/milestones/v2.0.0-*`). Gate green: `init.milestone-op` exit 0 (v2.2 / Analytic Surface / phase_count 4 / completed_phases 0 / phases_dir_exists true); `.planning/phases/` = `.gitkeep` only. See Log.
- [x] S0-2 Verify the inherited v2.2 scope is still true against the shipped v2.0.0 tree before planning anything on it. — **DONE 2026-08-28:** all 23 requirements re-read against live code; verdict = **23/23 still-valid, 0 amendments** (none already-satisfied/contradicted/obsolete), so no §4 loud amendment and no HUMAN-QUEUE escalation. Gate met: per-requirement verdict table + premise evidence in `.planning/v2.2-SCOPE-RECHECK.md`. Key confirmations: 5 new skills absent; `VARIANCE_ADJUSTMENTS` = exactly 4 named members (`dsx/spec.py:264`); gate path pure (no pandas/scipy in `dsx/frame`,`dsx/checks`); no `shapiro` in `skills/`|`dsx/`; `brief.md §6.5` + fixtures present; `gen-finding-catalogue.py --check` exit 0. See Log.
- [x] S0-3 Prepare the Phase 15 D-05 citation evidence pack EARLY and file it as HQ-8, so the operator can answer while 13/14/16 build. Covers: Deng, Xu, Kohavi & Walker (2013) WSDM CUPED — exact formulation + a published worked value (REQ-P15-02); and the survivorship-bias / changing-denominator candidate citations (REQ-P15-04). Gate: `HUMAN-QUEUE.md` HQ-8 contains a per-citation evidence table with locators and a clear statement of what is confirmed vs unverified. **Do not sign it — D-05 requires the human read.** — **DONE 2026-08-28:** HQ-8 assembled with 3 per-citation evidence tables (CUPED HIGH / survivorship MEDIUM / changing-denominator MEDIUM), each carrying locator (DOI+pages), the formulation/defect to confirm, and an explicit Confirmed-by-loop vs Unverified/Open-D05 split. **Not signed** (D-05 = human read). Flagged the changing-denominator↔Deng&Hu-2015 out-of-scope boundary (brief line 450) so S4 doesn't re-mint. D-06 numbering left to S4-1. Gate met by construction. See Log.

## S1 — Phase 13: Task playbooks that fill the spec (skill-only, 6 requirements)

- [x] S1-1 Discuss (assumptions mode + persona round); `13-CONTEXT.md` written. — **DONE 2026-08-28T15:51Z:** Architect-led + Auditor 2-persona round (opus/high, §4) settled D-01..D-08. 256-code baseline re-verified by orchestrator (`--check` exit 0, distinct-code grep = 256). Locks: 4 pure prompt-guidance skills + `capability.json` append, no gate-path touch (D-01); field→gate contract table, route-and-cite, no parallel advice (D-02); hypothesis register → `assumptions[]`/`multiplicity.family`→`results.tests[]` (D-03); What/So What/Now What = template-only, no `DSX-NAR` mint (D-04); advisory tier routing lookup/ad-hoc/full→Tier 0/1/2 (D-05); executor `scripts/*.py` bullet citing `DSX-REP-040` (D-06); **REQ-P13-06 asserted by set-identity diff vs a frozen Phase-12 snapshot, not count alone** (D-07, tie-break rigour>reliability); zero mint, `DSX-COH-*`=Coherence-not-Cohort caveat (D-08). **Pre-req for S1-2:** ROADMAP active-milestone repoint (v2.2 still under "## Queued milestone"; `init phase-op --phase 13`→`phase_found:false`) — structural, not a scope change. Gate met: `.planning/phases/13-task-playbooks-that-fill-the-spec/13-CONTEXT.md` (21.8 KB, 6 sections, D-01..D-08). See Log.
- [x] S1-2 Plan (plan-checker must pass). — **DONE 2026-08-28T17:55Z:** 5-plan set complete (13-01..05), requirements union = REQ-P13-01…06, single-writer safe. `gsd-plan-checker` (opus, independent) verdict **REVISE → 1 repair (B1: 13-01 registered only 2 of 4 skills) → PASS**; orchestrator re-verified the fix (all four names appended+asserted in 13-01 Task 3). Reconcile also corrected the S1-1 "ROADMAP repoint prerequisite" claim (false — plan-phase uses `init.plan-phase` which resolves phase 13). (full evidence + 4 non-blocking execute-time nits: LOOP-LEDGER-ARCHIVE.md#S1-2)
- [x] S1-3 Execute all plans. — **DONE 2026-08-28:** all 5 plans executed, orchestrator-verified (every plan's own verify block re-run green), pushed. Wave 1: 13-01 (dsx-cohort+dsx-funnel+capability.json), 13-02 (dsx-root-cause+dsx-segment), 13-03 (dsx-explore-data hypothesis register + dsx-narrate What/So What/Now What), 13-04 (dsx-scope-analysis advisory tier table + executor.md scripts/*.py bullet). Wave 2: 13-05 (byte-copy Phase-12 snapshot fixture + set-identity diff test). Catalogue held at 256 (`unittest tests.test_finding_catalogue_invariant -v` = 2 tests OK: D-18 count + D-07 set-identity; `gen-finding-catalogue.py --check` exit 0); zero `dsx/`|`scripts/` edits phase-wide. This firing did 13-03/04/05 directly as orchestrator (loud op-decision, see Log). Commits: 13-03 `ac5bae9`, 13-04 `74c62da`, 13-05 `affe761`. Full-suite + code review is S1-4. See Log.
- [x] S1-4 Code review + auto-fix; verification `passed`. — **DONE 2026-08-28:** review PASS (0 blocking, 0 fixes needed) + verification PASSED (6/6 REQ-P13-01..06 goal-backward). Orchestrator-direct (opus/high, §3), every gate re-run here not subagent-trusted. Key checks: 21/21 cited `DSX-*` codes real (0 dangling); 3/3 source locators accurate; `docs/gsd-tiers.md`+`gsd-tier.ps1` exist and the tier table matches; D-01 gate-path pure (0 `dsx/`|`scripts/*.py` edits); D-07 zero-mint by set-identity diff (`added=[] removed=[]`, 256); D-02 router discipline + D-08 Coherence caveat present; 4 carried S1-2 nits all resolved. Gate: `sh scripts/check.sh` = all checks passed (1222 OK). Artifacts: `13-.../REVIEW.md`, `13-.../VERIFICATION.md`. See Log.
- [x] S1-5 `/gsd-secure-phase 13` verified + `/gsd-validate-phase 13` compliant. **Scope bound (REQ-P13-06): the catalogue must contain no new codes vs the Phase 12 catalogue — assert by diff, not by review. A skill that "needs" a new code is a Phase 15 item, not a Phase 13 exception.** — **DONE 2026-08-28:** secure = **SECURED** `threats_open: 0` (14/14 closed, L1 short-circuit, orchestrator re-gate — not auditor-trusted); validate = **nyquist_compliant: true** 0 gaps (6/6 REQ COVERED). Loud op-decision: crystallised S1-4's hand-greps into a standing test `tests/test_phase13_playbooks.py` (8 tests) so REQ-P13-01..05 are automated-COVERED. Re-gate: zero `dsx/`|`scripts/` edits; anti-parallel-advice grep 0 lines ×5 files; catalogue set-identity + 256 + `--check` exit 0; `sh scripts/check.sh` all passed (1230 OK). D-05 §4-cat-4 human security sign-off queued to HUMAN-QUEUE (non-blocking to S5-2). Artifacts: `13-SECURITY.md`, `13-VALIDATION.md`.

## S2 — Phase 14: Compounding and data onboarding (6 requirements)

- [x] S2-1 Discuss; `14-CONTEXT.md` written. **Open item to settle here, do not decide silently:** REQ-P14-05 is conditional — if GSD Core exposes no overlay hooks, the requirement is satisfied by a documented skip in the operating guide, NOT by inventing a hook channel. Verify against the installed GSD Core and record which branch was taken. — **DONE 2026-08-28T19:55Z:** Architect+Auditor 2-persona round (opus/high, §4, converged no ties), D-01..D-07 in `14-CONTEXT.md`. **REQ-P14-05 branch = DOCUMENTED SKIP** (D-06), verified against installed GSD Core: only file-change hook is `FileChanged` = Claude-Code-family-only (`capability-validator.cjs:728,759`), runtime-descriptor-gated, filename-matched, used solely for `config.json` hot-reload (`runtime-hooks-surface.cjs:1656-1693`); binding it breaks `supported:["*"]` (silent no-op on non-CC runtimes, uncaught) — skip weakens no control (`DSX-DQ-001` already forces the profile). Loud op-decision, not HUMAN-QUEUE (requirement's disjunction pre-authorises skip; mints nothing). Orchestrator re-verified baseline: catalogue `--check` exit 0 / 256 codes / invariant 2 tests OK / no pandas·scipy in `dsx/` / `import csv` only in `profiler.py`. Gate met: `14-CONTEXT.md` (375 lines, 6 sections, D-01..D-07). See Log.
- [x] S2-2 Plan (plan-checker must pass). — **DONE 2026-08-28:** 5-plan set (14-01..05) authored by `gsd-planner` (opus); independent `gsd-plan-checker` (opus) verdict **PASS, 0 blocking** (3 non-blocking nits carried to S2-3/S2-4). Requirements union = exactly {REQ-P14-01..06}; single-writer-per-wave holds (wave-1 01/02/03/05 file-disjoint; 14-04 sole wave-2, `depends_on:[14-01,14-02,14-03]`, re-edits only the 3 shared skills' `description`); gate-path pure (no `dsx/**.py`/`capability.json` in any files_modified; every `report.add` mention is a prohibition; no `data_storage`, no `capability.json aliases` key); REQ-P14-05 documented-skip = 4 D-06 claims in force; 14-05 gate re-runs catalogue `--check` + invariant + `report.add`==0. Orchestrator re-verified all at frontmatter+body level (brief §5), not subagent-trusted. (full evidence + producer decision + 3 nits: LOOP-LEDGER-ARCHIVE.md#S2-2)
- [x] S2-3 Execute all plans. — **DONE 2026-08-28:** all 5 plans (14-01..05) executed directly as orchestrator (S1-3 precedent — surgical md/template/test edits, single-writer, every gate re-run here per brief §5), each atomically committed: 14-01 dated-learnings loop `38801a0`, 14-02 DATA-DICTIONARY `6ddee4c`, 14-03 opt-in research disclosure `5e54297`, 14-04 alias table + documented-skip + 13 Triggers descriptions + 2 shims `4d418b9`, 14-05 zero-mint proof + hermeticity guard `720ba10`. Every per-plan verify block PASS. Phase-wide proof: `--check` exit 0, two-leg invariant OK (256 + set-identity vs Phase-12), `test_gate_path_hermetic` 2 OK, `git status -- dsx/` empty, `report.add` cli.py==0, capability.json untouched (hooks []); 13 skill frontmatters parse clean. Per-plan evidence in `14-0{1..5}-SUMMARY.md`. See Log.
- [x] S2-4 Code review + auto-fix; verification `passed`. — **DONE 2026-08-28:** review **PASS** (0 blocking, 0 fixes) + verification **PASSED** 6/6 REQ-P14-01..06. Orchestrator-direct (opus/high, §3), every gate re-run here not subagent-trusted. Key: gate-path pure (empty `dsx/` + `capability.json` diff over the 5 feature commits, `hooks:[]`); zero-mint (invariant 2 OK = 256 + set-identity `added=[] removed=[]`; `--check` exit 0; `report.add` cli.py=0); new hermeticity guard `tests/test_gate_path_hermetic.py` 2 OK; only cited code `DSX-DQ-001` authentic; Triggers on 13/13 skills; no `data_storage`. 1 non-blocking nit (N1: hermeticity closure skips dotted `import dsx.x` — off-req, left as-is). Gate: `sh scripts/check.sh` all passed (**Ran 1232 tests OK**). Artifacts: `14-.../REVIEW.md`, `14-.../VERIFICATION.md`.
- [x] S2-5 `/gsd-secure-phase 14` verified + `/gsd-validate-phase 14` compliant (REQ-P14-06: no new blocking codes). — **DONE 2026-08-29:** secure = **SECURED** `threats_open: 0` (16/16 register entries closed = 15 threats + 1 SC-accept, 7 high; L1 short-circuit, orchestrator re-gate — not auditor-trusted); validate = **nyquist_compliant: true** 0 gaps (6/6 REQ-P14-01..06 COVERED). Loud op-decision (§4, no scope/mint → no persona round): crystallised S2-4's hand-greps into a standing test `tests/test_phase14_onboarding.py` (11 tests); one naive assertion corrected during authoring (operating guide is exempt from the `data_storage` grep — it documents the folder's *absence*; rescoped to skills+shims). Re-gate: empty `dsx/`+`scripts/`+`capability.json`-manifest diff over the 5 feature commits; `report.add` cli.py=0; catalogue set-identity + 256 + `--check` exit 0; `test_gate_path_hermetic` 2 OK; `hooks:[]`/no-aliases/`supported:["*"]`; Triggers 13/13; `data_storage` 0 in skills/shims; disclosure guarded on literal `research`. `sh scripts/check.sh` all passed (**Ran 1243 tests OK**, 1232→+11). D-05/§4-cat-4 human security sign-off + UAT queued to HUMAN-QUEUE HQ-10 (non-blocking to S5-2). Artifacts: `14-SECURITY.md`, `14-VALIDATION.md`, `tests/test_phase14_onboarding.py`. **S2 (Phase 14) is now fully complete (S2-1..S2-5).**

## S3 — Phase 16: Re-run verification, off the gate path (4 requirements)

- [x] S3-1 Discuss; `16-CONTEXT.md` written. Open item RESOLVED loudly (not silently): REQ-P16-02's reproduce-report gate check **does** need a new code (none of the 11 `DSX-REP-*` covers "report missing" / "numbers don't overlap"). Architect+Auditor 2-persona round (opus/high, §4) **unanimous Option A**: mint **`DSX-REP-060`+`DSX-REP-061`** (both HIGH — verify/ship blocks only at HIGH), **in Phase 16** (not moved to 15, not reused). D-06 numbering decided by loop (§4, next-free-06x), recorded loudly + filed HQ-11 veto window (non-blocking). D-07: ROADMAP "only Phase 15 extends the catalogue" amended (line 119) — no requirement dropped/reworded. No D-05 owed (engineering-hygiene codes, no citation). Catalogue moves 256→258 additively; phase-12 anchor untouched (D-08). Gate met: `.planning/phases/16-re-run-verification-off-the-gate-path/16-CONTEXT.md` (D-01..D-11, 6 sections). See Log.
- [x] S3-2 Plan (plan-checker must pass). — **DONE 2026-08-29:** 4-plan set (16-01..04) authored by `gsd-planner` (opus) + independent `gsd-plan-checker` (opus, §3-override of profile haiku) verdict **PASS, 0 blocking**; orchestrator re-verified all load-bearing claims against the plan files (§5): coverage union == {REQ-P16-01..04}; single-writer waves (wave-1 = 16-01 alone / wave-2 = 02·03·04 `depends_on:[16-01]`, file-disjoint); **D-08 additive rebaseline exact** (`_EXPECTED_TOTAL`→258 + SEPARATE `_SNAPSHOT_TOTAL=256` for the byte-frozen anchor + set-identity vs `snapshot∪{060,061}`); both codes HIGH w/ fixed plain-string msgs; D-02 opt-in (good fixture silent); D-04 `math.isclose`+verdict-agnostic 061; D-09 positive-control AST scan (named-set incl code.py+repro.py); D-10 `_headline` pinned (0.25,0.3)+co_varnames guard+anchors unedited; D-11 SKIPPED short-circuit (template+check). 3 non-blocking nits (N1 POSIX verify blocks need git-bash executor; N2 glob pathspec; N3 single-string-literal msg) carried to S3-3. Plans: `16-0{1..4}-PLAN.md`. See Log.
- [x] S3-3 Execute all plans. — **DONE 2026-08-29 (recovered):** a prior firing executed all 4 plans (16-01..04) but crashed before commit/log/gate; this firing reconciled the uncommitted tree against the 4 plans (§0.4 — repo is the fact) and re-ran EVERY gate itself (brief §5, not summary-trusted). All green: `--check` exit 0 (only S0-2 double-declare noise); invariant 2 OK (**258** + set-identity vs snapshot∪{060,061}); `test_reproduce_report` 7 OK; `test_no_entrypoint_execution` 3 OK; `test_known_bad_corpus` 45 OK; `test_gate_path_hermetic` 2 OK. Gate-path pure: only `dsx/checks/repro.py` under `dsx/`, `scripts/` clean, no forbidden imports, Phase-12 snapshot byte-unchanged. Mint = `DSX-REP-060`/`061` (HIGH), 256→258 additive. Committed ceremony artifacts ONLY (operator-local `.claude/`·`.vscode/`·`graphify-out/`·etc. left untracked) as 4 atomic commits in wave order: 16-01 `1195d97`, 16-02 `71454f6`, 16-03 `e32f9e6`, 16-04 `0bd6a75`. Full suite + code review is S3-4. See Log.
- [ ] S3-4 Code review + auto-fix; verification `passed`. **Hard constraint (REQ-P16-02/-04, D-01/D-02): the gate path must never import pandas/scipy or execute the analysis entrypoint. A test must assert this. If a future change "simplifies" by calling the entrypoint from `dsx gate`, the phase is failed, not done.**
- [ ] S3-5 `/gsd-secure-phase 16` verified + `/gsd-validate-phase 16` compliant.

## S4 — Phase 15: CUPED and BI declaration checks (7 requirements, new codes + D-05)

- [ ] S4-1 Discuss; `15-CONTEXT.md` written. Persona round assigns the new numeric codes (D-06 irreversible) and records them loudly for the veto window. Ordering constraint: REQ-P15-01 before REQ-P15-02 (`cuped` must be a legal vocabulary member before a check can require it).
- [ ] S4-2 Plan (plan-checker must pass).
- [ ] S4-3 Execute all plans. **D-05 bar: a code whose citation is not in hand at implement time does NOT ship — it stays in `brief.md` §6.5 rather than being invented.**
- [ ] S4-4 Code review + auto-fix; verification `passed`; queue the phase's D-05 reads + D-06 veto window to HUMAN-QUEUE (evidence pack from S0-3 already filed as HQ-8; extend it with anything minted during execution).
- [ ] S4-5 `/gsd-secure-phase 15` verified + `/gsd-validate-phase 15` compliant; `gen-finding-catalogue.py --check` exits 0 on the new codes; both canonical fixtures still satisfy D-08 (REQ-P15-07).

## S5 — Close-out

- [ ] S5-1 `/gsd-audit-uat` cross-phase sweep. **Do NOT accept the CLI's "All Clear" as evidence** — it under-reports (see Standing framework notes in `HUMAN-QUEUE.md`); cross-check each phase's VERIFICATION.md by hand.
- [ ] S5-2 Drain HUMAN-QUEUE (the only permitted blocking wait).
- [ ] S5-3 `/gsd-extract-learnings`.
- [ ] S5-4 `/gsd-audit-milestone` — must reach `passed`.
- [ ] S5-5 `/gsd-complete-milestone`.
- [ ] S5-6 Ship: merge to `main` and tag. **Precedent from v2.0.0 (do not repeat its cost):** `/gsd-pr-branch`'s per-commit cherry-pick chain does NOT survive a long ceremony branch — it hit recurring modify/delete and structural-file conflicts and was abandoned. Ship by direct 3-way merge into `main` (`git merge --no-ff`), verified on a throwaway branch first with the full suite + `scripts/check.sh` green BEFORE touching `main`. Tag the next free version (`v2.2.0`); **never force-move a published tag.** Merge and tag are outward-facing — queue to HUMAN-QUEUE for the operator, do not self-approve.

## Log

(append one line per firing: `YYYY-MM-DDTHH:MMZ | unit | outcome | evidence`. Keep
the most recent ~20; archive older entries to `LOOP-LEDGER-ARCHIVE.md` per brief §5.)

2026-08-28T14:40Z | milestone-transition | v2.0.0 SHIPPED (merged to main `c81fc6b`, tag `v2.1.0` pushed, both verified on origin); v2.2 Analytic Surface opened on branch `gsd/v2.2.0-analytic-surface`; loop repointed | v2.0.0 loop artifacts archived to `.planning/milestones/v2.0.0-LOOP-*`; suite 1221 OK + `scripts/check.sh` all checks passed on the merged tree before push
2026-08-28T15:02Z | S0-1 | DONE — GSD state reconciled onto v2.2. Reconcile OK first: HEAD `33dc7de`, branch up-to-date with origin, ledger==repo (no correction needed). Frontmatter already at v2.2/Analytic Surface/executing/current_phase 13/0-of-4 from the transition commit; body was still 198 lines of v2.0.0 detail contradicting it, so reconciled to a 79-line GSD-template digest — v2.0.0 history preserved in `MILESTONES.md` + `.planning/milestones/v2.0.0-*`, not deleted. | Gate `node ~/.claude/gsd-core/bin/gsd-tools.cjs query init.milestone-op` exit 0: milestone_version v2.2, milestone_name "Analytic Surface", phase_count 4, completed_phases 0, phases_dir_exists true; `.planning/phases/` = `.gitkeep` only; STATE.md 79 lines (< 100-line template cap). Next unblocked = S0-2.
2026-08-28T16:20Z | S0-3 | DONE — Phase 15 D-05 citation evidence pack assembled into HUMAN-QUEUE.md HQ-8 (prepared, NOT signed — D-05 is the human read). Reconcile OK first: HEAD `541c5b9`, branch up-to-date with origin, ledger==repo (no correction). Pack = 3 per-citation tables with locators + confirmed/unverified split: (1) CUPED — Deng/Xu/Kohavi/Walker 2013 WSDM pp.123–132 DOI 10.1145/2433396.2433413; formulation Ŷ_cv=Ȳ−θ(X̄−E[X]), θ=Cov/Var, Var-reduction 1−ρ²; worked value = 1−ρ² identity (fixture/docstring, not gate-path per D-02) + paper's ≈50 % Bing headline; HIGH, only exact eq#/page left for the read. (2) Survivorship — Brown/Goetzmann/Ibbotson/Ross 1992 RFS 5(4):553–580 DOI 10.1093/rfs/5.4.553 (+ Elton/Gruber/Blake 1996); MEDIUM, open question = does the finance criterion transfer to a cohort/funnel declaration check (else stays §6.5). (3) Changing-denominator — Crook/Frasca/Kohavi/Longbotham 2009 KDD pp.1105–1114 DOI 10.1145/1557019.1557139 (+ Kohavi/Tang/Xu 2020 book); MEDIUM, flagged the out-of-scope boundary vs Deng&Hu-2015 dilution (brief line 450) so S4 won't re-mint. D-06 numbering left to S4-1; no code assigned. No §4 persona round needed (evidence-gathering, not a decision). Sources corroborated across independent web results 2026-08-28; not a D-05 sign-off. Gate met: HQ-8 has per-citation table + locators + confirmed-vs-unverified. Next unblocked = S1-1 (Phase 13 discuss). | evidence: `.planning/HUMAN-QUEUE.md` HQ-8
2026-08-28T15:51Z | S1-1 | DONE — Phase 13 discuss complete; `13-CONTEXT.md` written. Reconcile OK first: HEAD `bdf7393`, branch up-to-date with origin, ledger==repo (S0-1/2/3 all landed, no correction). Ran an **Architect-led (`dsx-analysis-architect`) + Auditor (`dsx-ml-integrity-auditor`) 2-persona round** (both opus/high, concurrent, §4), tie-break rigour>reliability>flexibility — the relevant 2 for a skill-only spec-shape phase. Orchestrator independently re-verified the load-bearing baseline before trusting it: `references/finding-codes.md` Total = **256**, distinct-`DSX-*` grep = **256**, `gen-finding-catalogue.py --check` exit **0** (DSX-VAL-060 double-declare = known shipped-tree noise); `test_finding_catalogue_invariant.py` `_EXPECTED_TOTAL=256` present; narrate `SKILL.md` has 0 "so what", executor fragment has 0 script-preference → both REQ-P13-03/-05 confirmed additive. 8 loud/vetoable decisions D-01..D-08 recorded in CONTEXT (full rationale + citations there). Genuine tie-break made: REQ-P13-06 "assert by diff" → **set-identity diff vs a frozen Phase-12 code-set snapshot**, not count-only (count passes a mint-one/drop-one swap; the ledger says "by diff, not by review"). No §4 escalation (mints nothing, no scope change, non-destructive). No D-05/D-06 owed by Phase 13. **Flagged for S1-2:** ROADMAP still has v2.2 phases under "## Queued milestone" (`:52`) while active `## Milestones`/`## Phases` (`:15`/`:20`) show v2.0.0, so `init phase-op --phase 13`→`phase_found:false`; S1-2 must promote the section to active before `/gsd-plan-phase 13` resolves (structural repoint, requirements unchanged). Gate met: `.planning/phases/13-task-playbooks-that-fill-the-spec/13-CONTEXT.md`. Next unblocked = S1-2 (plan Phase 13; do the ROADMAP repoint first). | evidence: `13-CONTEXT.md`
2026-08-28T17:55Z | S1-2 | DONE — Phase 13 planned; 5-plan set (13-01..05) passes the plan-checker gate. Reconcile-first at HEAD `2ad38c4` found TWO ledger/repo disagreements, both corrected (repo is the fact): (1) S1-1's "ROADMAP repoint is a prerequisite for S1-2" is FALSE — plan-phase uses `init.plan-phase` (→`phase_found:true`, plan_count 3), not `init.phase-op` (still false); no repoint done, none needed for planning. Open for S1-3: check execute-phase's init verb before assuming a repoint. (2) The prior S1-2 firing was interrupted mid-generation — left 3 untracked drafts (13-01 forward-refs phantom 13-04/05) with **13-03 truncated on disk** (1 of 2 tasks, no closing sections); REQ-P13-03/-04/-05/-06 uncovered. Resolution: kept the 3 good drafts; `gsd-planner` (opus) authored the missing 13-04 (tier D-05 + executor D-06) & 13-05 (catalogue set-diff D-07) and repaired 13-03 (added dsx-narrate What/So What/Now What, D-04, no `DSX-NAR` mint). Gate: independent `gsd-plan-checker` (opus) → **REVISE** (1 blocker B1: 13-01 Task 3 registered only cohort+funnel, not root-cause+segment → SC1 fail) → **1 repair** → orchestrator re-verified (13-01 Task 3 now appends+asserts all four names; sole manifest writer; 3 tasks whole). Checker also PASSED coverage SC2–6, gate-path purity D-01/D-02, D-04/D-08, waves, single-writer. 4 non-blocking nits (verify-block robustness) logged for S1-3/S1-4. Next unblocked = S1-3 (execute all Phase 13 plans). | evidence: `.planning/LOOP-LEDGER-ARCHIVE.md#S1-2`; plans `13-0{1..5}-PLAN.md`
2026-08-28T15:40Z | S0-2 | DONE — v2.2 scope rechecked against shipped v2.0.0 tree. Reconcile OK first: HEAD `4ae156f`, branch up-to-date with origin, ledger==repo (no correction). All 23 reqs (REQ-P13-01…P16-04) re-read against live code via direct grep/ls/Read + a real `gen-finding-catalogue.py --check` (exit 0). Verdict **23/23 still-valid, 0 amendments** → no §4 loud amendment, no HUMAN-QUEUE escalation; milestone needs execution, not re-scoping. Evidence: 5 target skills (`dsx-{cohort,funnel,root-cause,segment,reproduce}`) absent; `VARIANCE_ADJUSTMENTS` = exactly the 4 named members no `cuped` (`dsx/spec.py:264`); `dsx vocab` present; gate path pure (no pandas/scipy in `dsx/frame`|`dsx/checks`); no `shapiro` in `skills/`|`dsx/`; `test-selection.md` order matches REQ-P15-06; `brief.md §6.5` + D-08 fixtures present. Recorded (non-amendments): `DSX-REP-*` already exists (11 codes) → material to S3-1; pre-existing double-declare warnings (DSX-SPEC-070/VAL-021/VAL-060) are shipped-tree noise, --check still exit 0 → don't misread at S4-5. Full verdict table: `.planning/v2.2-SCOPE-RECHECK.md`. Next unblocked = S0-3 (Phase 15 D-05 evidence pack → HQ-8).
2026-08-28T18:33Z | S1-3 (partial, 2/5) | Phase 13 execution started; 2 of 5 plans executed, orchestrator-verified, pushed. **Reconcile-first** at HEAD `d99891d` (branch up-to-date w/ origin): ledger==repo. `d99891d` = interactive HQ-8 verdict commit (cite1 CUPED + cite3 changing-denominator confirmed at locator; cite2 survivorship does-not-transfer → unshipped) — already in HUMAN-QUEUE "Answered", no firing Log owed. **CLOSED the S1-1/S1-2 "ROADMAP repoint" open item as a query-syntax artifact, not a real blocker:** `init execute-phase --phase 13` passes the phase as `args[2]='--phase'` → `phase_found:false`; the POSITIONAL form `init execute-phase 13` → `phase_found:true`, 5 plans, all 6 REQ ids. Root-caused in gsd-tools source (`init-command-router.cjs:34`) + a direct `cmdInitExecutePhase` call. **No ROADMAP edit made** — single-writer file protected from a speculative restructure. **Ceremony execution decisions (loud, operational — non-scope, no persona round):** (a) STAY on ceremony branch, SKIP GSD `handle_branching` (it would fork `gsd/v2.2-analytic-surface` off `origin/main`, orphaning S0/S1); (b) sequential no-worktree executors (`worktree.baseRef=head` so worktrees would be brief-compliant, but sequential direct-commit is simpler+safer headless, no merge-back); (c) executors forbidden from touching STATE/ROADMAP/REQUIREMENTS/LOOP/HUMAN → orchestrator single-writer preserved. **Plans done:** 13-01 (skills `dsx-cohort`+`dsx-funnel` + `capability.json` registers all 4 playbooks; `6c17955`,`716151c`,`b1bc21e`) + 13-02 (skills `dsx-root-cause`+`dsx-segment`; `e5e9a98`,`7c27aef`,`81eb538`). Both **orchestrator-verified**: only `skills/*` + `capability.json` touched, valid JSON w/ 4 skills, zero `dsx/` or tracking-file changes, no new `DSX-*` codes (catalogue file untouched), verify blocks green. Stopped at a clean plan boundary per §1 pacing cap (2 executors + heavy diagnosis). **For S1-4:** confirm every `DSX-*` code the new skills CITE actually exists in `references/finding-codes.md` (skills cite codes in prose; authenticity is an S1-4 / 13-05-catalogue concern, not an execution gate). Next = resume S1-3 at **13-03**. git: branch `gsd/v2.2.0-analytic-surface`, pushed to origin (0 ahead / 0 unpushed after this sync commit).
2026-08-28T19:30Z | S1-4 | DONE — Phase 13 code review + verification, both PASS. Reconcile-first OK at HEAD `bf5748e` (branch up-to-date w/ origin; git log == ledger, last entry was the S1-3 sync commit — no correction). **Loud op-decision (non-scope, consistent w/ S1-3):** ran the review DIRECTLY as orchestrator (opus/high, §3) rather than spawning `gsd-code-reviewer` — the diff is small/bounded (skill md + 1 py test + 1 json + 1 fixture) and I re-ran every gate myself (brief §5, not subagent-trusted). **Review = PASS, 0 blocking, 0 auto-fix (nothing to fix).** Load-bearing checks for a skill-only phase: (1) citation authenticity — all **21/21** cited `DSX-*` codes exist in `references/finding-codes.md`, zero dangling; (2) locator fidelity — spot-checked `dsx/spec.py:22-28`, `spec.py:1046-1055`, `dsx/checks/design.py:362-412` all accurate; (3) file refs — `docs/gsd-tiers.md`+`scripts/gsd-tier.ps1` exist, tier table matches the doc's Tier0/1/2 (`enforce` off/on/on + `mode=interactive`@T2); (4) D-01 gate-path pure — 0 `dsx/`|`scripts/*.py` edits (git diff --stat); (5) D-07/REQ-P13-06 zero-mint by set-identity diff (`added=[] removed=[]`, 256); (6) D-02 router discipline + D-08 Coherence-not-Cohort caveat present; the 4 carried S1-2 nits all resolved (nit1 errs-safe & moot; nit2 narrate cites exactly 1 DSX-NAR; nit3 tier block prints not mutates; nit4 chart-admissibility hand-read = clean). **Verification = PASSED, 6/6** REQ-P13-01..06 goal-backward (each mapped to a re-run gate or read locator, incl. REQ-P13-04 "matching docs/gsd-tiers.md" cross-checked). **Gate:** `sh scripts/check.sh` = *all checks passed* (Ran **1222** tests OK; catalogue current; capability conformant 13 skills; gate contract; determinism); `unittest tests.test_finding_catalogue_invariant -v` = 2 OK. Artifacts written: `REVIEW.md` + `VERIFICATION.md` in the phase dir. Stopped at S1-4 boundary per §1 pacing (heavy opus/high review unit). Next unblocked = S1-5 (`/gsd-secure-phase 13` + `/gsd-validate-phase 13`). git: branch `gsd/v2.2.0-analytic-surface`, pushed to origin (0 ahead / 0 unpushed after this commit). | evidence: `13-.../REVIEW.md`, `13-.../VERIFICATION.md`
2026-08-28T19:22Z | S1-5 | DONE — Phase 13 secure + validate, both PASS; Phase 13 ceremony (S1) complete. Reconcile-first OK at HEAD `f4d5e9b` (branch up-to-date w/ origin; git log == ledger — S1-4 was the last landed commit, no correction). **Load-bearing gate verification (all real commands, brief §5 — never trusted from a report):** confirmed `init.phase-op 13` (POSITIONAL) resolves `phase_found:true`/phase_dir/5 plans — the `--phase 13` flag-form false-negative (S1-1) is a query-syntax artifact, not a ROADMAP-repoint blocker; no repoint made. Confirmed both step hooks ACTIVE via `render-hooks verify:post` `activeHooks[]` (my first parser looked for the wrong key `hooks` → false "disabled"; corrected): `secure-phase`(when security_enforcement=true) + `validate-phase`(when nyquist_validation=true); config asvs_level 1, block_on high, text_mode true. **SECURE = SECURED, threats_open: 0** — State B, 14-threat register consolidated from 5 plans' `<threat_model>` blocks (`register_authored_at_plan_time:true`); asvs1 L1 short-circuit (no auditor); 14/14 CLOSED by orchestrator re-gate: zero `dsx/`|`scripts/` edits (git diff --stat empty → T-13-02/04/07/10/08-code); anti-parallel-advice grep 0 lines on cohort/funnel/root-cause/segment/executor (T-13-01/03/09); catalogue set-identity `added=[] removed=[]` + 256 + `--check` exit 0 (T-13-05/11/12/13, D-04/D-07); scope-analysis emits gsd-tier.ps1 & no config-set (T-13-08 D-05); 0 dangling of 28 cited codes. **VALIDATE = nyquist_compliant: true, 0 gaps** — State B; 6/6 REQ COVERED. **Loud op-decision (§4 rigour>flexibility, no scope/mint → no persona round, no escalation):** rather than mark 5 of 6 requirements manual-only, crystallised the phase's deterministic invariants into a standing test `tests/test_phase13_playbooks.py` (8 tests: skills-exist+registered, citation authenticity, router discipline, hypothesis register, What/So What/Now What, tier routing + advisory-boundary, .py entrypoint) — REQ-P13-01..05 now automated-COVERED; REQ-P13-06 stays on `test_finding_catalogue_invariant`. Gate: `sh scripts/check.sh` = all checks passed (**Ran 1230 tests OK**, 1222→+8; catalogue current 256; capability conformant 13 skills; gate contract; determinism). Two manual-only items (semantic/prose fidelity) do NOT reduce compliance (underlying behaviours automated). **Queued to HUMAN-QUEUE:** batched Phase 13 end-of-phase security sign-off (D-05/§4-cat-4) + UAT round — non-blocking until S5-2. Artifacts: `13-SECURITY.md`, `13-VALIDATION.md`, `tests/test_phase13_playbooks.py`. **S1 (Phase 13) is now fully complete (S1-1..S1-5).** Stopped after this one unit per firing instruction. Next unblocked = S2-1 (Phase 14 discuss). git: branch `gsd/v2.2.0-analytic-surface`, pushed to origin (0 ahead / 0 unpushed after this commit). | evidence: `13-SECURITY.md`, `13-VALIDATION.md`
2026-08-28T18:48Z | S1-3 (complete) | DONE — Phase 13 execution finished; all 5 plans executed, orchestrator-verified, pushed. Reconcile-first OK at HEAD `52fcf49` (branch up-to-date w/ origin; git log confirmed exactly the 13-01/13-02 commits the ledger claimed — no correction). **Loud op-decision (non-scope):** executed the remaining 3 plans DIRECTLY as orchestrator rather than spawning executors — they are surgical markdown/test edits with exact line anchors + exact grep/test verify blocks I re-ran myself; faster within the §1 pacing cap, keeps me single-writer, and I gate every plan. 13-03 `ac5bae9`: dsx-explore-data hypothesis-register mapping (untested belief→assumptions[] via DSX-COH-030/031; promoted test→design.multiplicity.family[]→results.tests[] via DSX-EXP-050..053, rides EDA.md ledgers, no new field) + dsx-narrate What/So What/Now What over the 5-part structure (Now What cites DSX-COH-040 + DSX-CLM-080; distinct DSX-NAR=1, template-only). 13-04 `74c62da`: dsx-scope-analysis <ceremony_tier> advisory table lookup→T0(dsx.enforce=false)/ad-hoc→T1(true)/full→T2(true,interactive), emits `pwsh scripts/gsd-tier.ps1 -Tier N`, cites docs/gsd-tiers.md, no config-set (D-05) + executor.md scripts/*.py-over-notebook bullet citing DSX-REP-040 + DSX-CODE, ordering-fidelity-not-leakage, suffix-neutral (D-06); anti-parallel-advice grep clean. 13-05 `affe761`: byte-copied references/finding-codes.md→tests/fixtures/finding-codes-phase12.md (cmp IDENTICAL, 256 codes) + CRLF-safe set-identity diff in test_finding_catalogue_invariant.py (current_set==phase12_set, added/removed reporting, reuses _ROW_RE, _EXPECTED_TOTAL untouched). **Gate evidence:** every plan verify block re-run green; `unittest tests.test_finding_catalogue_invariant -v`=2 tests OK; `gen-finding-catalogue.py --check` exit 0; catalogue 256 phase-wide; zero `dsx/`|`scripts/` edits (scope fence green each plan). Stopped at S1-3 boundary per §1 pacing — did NOT start S1-4 (heavier: full-suite + gsd-code-review, opus/high). Next = S1-4. git: branch `gsd/v2.2.0-analytic-surface`, pushed to origin (0 ahead / 0 unpushed after the sync commit). | evidence: 13-0{3,4,5}-SUMMARY.md; commits ac5bae9,74c62da,affe761
2026-08-28T21:40Z | S2-2 | DONE — Phase 14 planned; 5-plan set (14-01..05) passes the plan-checker gate. Reconcile-first OK at HEAD `ba6a218` (branch up-to-date w/ origin; git log == ledger — S2-1 was the last landed commit, no correction). Spawned `gsd-planner` (opus/adaptive, §3) to author; independent `gsd-plan-checker` (opus) gate = **PASS, 0 blocking, 3 non-blocking nits** (carried to S2-3/S2-4). **Orchestrator re-verified every load-bearing claim (brief §5, not subagent-trusted), at frontmatter AND plan-body level:** (1) requirements union == exactly {REQ-P14-01..06} — 14-01{01}/14-02{02}/14-03{03}/14-04{04,05}/14-05{06}; (2) single-writer-per-wave — wave-1 plans (01/02/03/05) pairwise file-disjoint (dsx-scope-analysis / dsx-explore-data / dsx-narrate / tests), 14-04 sole wave-2 with `depends_on:[14-01,14-02,14-03]` re-editing only the 3 shared skills' `description` frontmatter; (3) gate-path purity — 0 `dsx/**.py` and 0 `capability.json` in any files_modified (`fragments/researcher.md` is a prompt fragment, same class 13-04 edited), every `report.add` mention in the plans is a prohibition/explanation not an ADD, `data_storage` only in a negative guardrail grep, no `capability.json aliases` key; (4) REQ-P14-05 documented-skip (D-06) — 14-04 carries the 4 honesty claims in force (FileChanged×5, config.json×3, supported:["*"]×8, DSX-DQ-001×13, hooks-stays-[]×12), no FileChanged binding; (5) 14-05 zero-mint gate is real (`gen-finding-catalogue.py --check && unittest ...invariant && git status --porcelain -- dsx/ empty && grep -c report.add dsx/cli.py -eq 0`). **Planner discretion (loud, §4 — no mint/scope/destructive, vetoable via daily summary):** REQ-P14-01 producer = existing `gsd-extract-learnings` (referenced, not edited); shipped 2 optional `.claude/commands/*.md` shims + `tests/test_gate_path_hermetic.py`. No tracking file edited by subagents (orchestrator single-writer). STATE.md updated: total_plans 0→5, resume at S2-3. Stopped after this one unit per firing instruction. Next unblocked = S2-3 (execute all Phase 14 plans; wave 1 = 14-01/02/03/05, wave 2 = 14-04). git: branch `gsd/v2.2.0-analytic-surface`, pushed to origin (0 ahead / 0 unpushed after this commit). | evidence: `.planning/LOOP-LEDGER-ARCHIVE.md#S2-2`; plans `14-0{1..5}-PLAN.md`
2026-08-28T23:06Z | S2-3 | DONE — Phase 14 executed; all 5 plans (14-01..05) executed, orchestrator-verified, committed + pushed. **Reconcile-first OK** at HEAD `2236bb4` (branch up-to-date w/ origin; git log == ledger — S2-2 was the last landed commit, no correction). **Loud op-decision (non-scope, S1-3 precedent):** executed all 5 plans DIRECTLY as orchestrator, not via spawned executors — every plan is a surgical markdown/template/test edit with exact anchors + greppable verify blocks I re-ran myself (brief §5, not subagent-trusted), keeping me single-writer of the tracking files. Wave-1 (file-disjoint): 14-01 `38801a0` (docs/dsx/learnings/ README+dated exemplar, dsx-scope-analysis step-0 search-before-framing, researcher.md pointer — producer=gsd-extract-learnings); 14-02 `6ddee4c` (templates/DATA-DICTIONARY.md + dsx-explore-data authoring step, roster copied verbatim); 14-03 `5e54297` (templates/DISCLOSURE-research.md + dsx-narrate opt-in disclosure guarded on literal dsx.domain==research). Wave-2: 14-04 `4d418b9` (operating-guide §9 alias table + "why no file-drop hook" 4-claim subsection naming DSX-DQ-001; Triggers: clause on all 13 skill descriptions; 2 optional .claude/commands shims). 14-05 `720ba10` (tests/test_gate_path_hermetic.py + phase-wide zero-mint proof). **Caught+fixed one self-inflicted slip before gating:** an early dsx-narrate disclosure draft contained the literal `dsx/checks/` string that would trip the plan's `! grep dsx/checks/` — reworded. **Gate evidence (all real commands):** every per-plan verify block PASS; `python scripts/gen-finding-catalogue.py --check` exit 0 (DSX-CLM/COH/PAR/SPEC/VAL double-declare = known shipped-tree noise, S0-2); `unittest tests.test_finding_catalogue_invariant` both legs OK (count==256 AND set-identity vs Phase-12 snapshot); `unittest tests.test_gate_path_hermetic` 2 OK; `git status --porcelain -- dsx/` empty; `grep -c report.add dsx/cli.py`==0; capability.json untouched (hooks stays []); 13 skill YAML frontmatters parse clean (pyyaml) with Triggers present. REQ-P14-01..06 all delivered. Stopped after this one unit per firing instruction (did NOT start S2-4 — heavier: full-suite scripts/check.sh + gsd-code-review, opus/high). Next = S2-4 (code review + verification). git: branch `gsd/v2.2.0-analytic-surface`, pushed to origin (0 ahead / 0 unpushed after this commit). | evidence: `14-0{1..5}-SUMMARY.md`; commits 38801a0,6ddee4c,5e54297,4d418b9,720ba10
2026-08-28T19:55Z | S2-1 | DONE — Phase 14 discuss complete; `14-CONTEXT.md` written (375 lines, 6 sections, D-01..D-07). **Reconcile-first OK** at HEAD `a88d9d0` (branch up-to-date w/ origin; git log == ledger — S1-5 was the last landed commit, S1 fully complete, no correction). STATE.md confirmed resume at S2-1. Ran an **Architect-led (`dsx-analysis-architect`) + Auditor (`dsx-ml-integrity-auditor`) 2-persona round** (both opus/high, concurrent, §4), tie-break rigour>reliability>flexibility — **they converged with no genuine ties**. **The S2-1 flagged open item is settled loudly against the installed GSD Core, not silently:** REQ-P14-05 branch = **DOCUMENTED SKIP** (D-06). Evidence read directly from GSD Core source: the portable hook floor `{SessionStart,PreToolUse,PostToolUse,Stop,SessionEnd}` (`hook-bus.cjs:28`) has no "file appeared" event; the only file-change surface `FileChanged` is Claude-Code-family-only (`capability-validator.cjs:728,759`), runtime-descriptor-gated, filename-matched, and used solely for `config.json` hot-reload (`runtime-hooks-surface.cjs:1656-1693`); the capability `hooks[]` validator does NOT check `event` against runtime support, so a `FileChanged` binding would be written for every runtime and **silently no-op on every non-Claude-Code runtime**, breaching the DSX capability's `supported:["*"]` contract (the exact "config that silently no-ops" the repo's Tool Version Grounding rule forbids). Secondary: firing on a NEW arbitrary CSV is unverified host behaviour; a folder-watch auto-profile is the `data_storage/` watcher REQ-P14-04 rejects, in disguise. **The skip weakens no control** — `dsx/checks/dq.py`'s `DSX-DQ-001` CRITICAL already forces the analyst to produce DATA-PROFILE.yaml; automation was convenience. Operating guide must state 4 honesty claims (D-06); `capability.json hooks` stays `[]`. Loud op-decision, vetoable via daily summary — **NOT** a HUMAN-QUEUE escalation (the requirement's own disjunction pre-authorises the skip; mints no code, rewords nothing, non-destructive). Other decisions: D-02 learnings = prompt-guidance search in `dsx-scope-analysis` + fixed frontmatter schema in `docs/dsx/learnings/`, no CLI; D-03 `DATA-DICTIONARY.md` = `templates/` skeleton authored by `dsx-explore-data`, roster copied from DATA-PROFILE.yaml, written+ungated; D-04 disclosure = additive opt-in block gated on literal `dsx.domain==research` (default `auto` never triggers → marketing byte-unchanged), template-only; D-05 slash aliases = documented convention + `description` triggers primary (host shims optional sugar, no `capability.json aliases` key, no `data_storage/`); D-07 zero-mint (REQ-P14-06) rides Phase-13 set-identity diff + `--check` exit 0, the one mint trap being a *gate check* for the new artifacts. **Orchestrator re-verified baseline (real commands, §5):** `gen-finding-catalogue.py --check` exit 0 / 256 distinct codes / `test_finding_catalogue_invariant` 2 tests OK / zero pandas·scipy·numpy in `dsx/` / `import csv` only in `dsx/profiler.py:9` / `capability.json` `hooks:[]`+`supported:["*"]`. Gate met: `14-CONTEXT.md` present. Stopped after this one unit per firing instruction. Next unblocked = S2-2 (Phase 14 plan; plan-checker must pass). git: branch `gsd/v2.2.0-analytic-surface`, pushed to origin (0 ahead / 0 unpushed after this commit). | evidence: `.planning/phases/14-compounding-and-data-onboarding/14-CONTEXT.md`
2026-08-29T00:10Z | S2-5 | DONE — Phase 14 secure + validate, both PASS; Phase 14 ceremony (S2) complete. **Reconcile-first OK** at HEAD `d5fba32` (branch up-to-date w/ origin; git log == ledger — S2-4 `d5fba32` was the last landed commit, no correction). Config confirmed live: `security_enforcement:true`, `security_block_on:"high"`, `nyquist_validation:true`; `asvs_level` not set in config → secure-phase default floor L1 (consistent w/ S1-5). **Loud op-decision (non-scope, S1-5 precedent):** ran secure+validate DIRECTLY as orchestrator (opus/high, §3), every gate re-run here (brief §5, not subagent-trusted). **SECURE = SECURED, threats_open: 0** — State B, 16-entry register consolidated from the 5 plans' `<threat_model>` blocks (`register_authored_at_plan_time:true`); 15 threats + T-14-SC accept; 7 at high severity; asvs1 L1 short-circuit (no auditor); all closed by orchestrator re-gate: gate-path purity (`git diff --stat 2236bb4..720ba10 -- dsx/ scripts/` **empty** AND `-- capabilities/dsx/capability.json` **empty** — only the prompt fragment `researcher.md` changed, same class as 13-04's executor.md; `report.add` cli.py=0) → T-14-01/04/07/10/12; zero-mint set-identity (invariant 2 OK = 256 + `added=[] removed=[]`, `--check` exit 0) → T-14-10; hermeticity (`test_gate_path_hermetic` 2 OK) → T-14-06/11; documented-skip honesty (`hooks:[]`, no `aliases` key, `runtimeCompat.supported:["*"]`, guide names DSX-DQ-001) → T-14-13/14; alias discipline (Triggers 13/13, `data_storage` 0 in 13 skills + 2 shims — guide exempt, documents the folder's *absence*) → T-14-04/15; disclosure guarded on literal `research` via `config-get dsx.domain` (`dsx-narrate` :47-59) → T-14-08/09; only cited code DSX-DQ-001 authentic (0 dangling). **VALIDATE = nyquist_compliant: true, 0 gaps** — State B; 6/6 REQ COVERED. **Loud op-decision (§4 rigour>flexibility, no scope/mint → no persona round, no escalation):** crystallised the phase's deterministic invariants into a standing test `tests/test_phase14_onboarding.py` (11 tests: learnings loop, DATA-DICTIONARY copy-verbatim, disclosure guard, CSV-first aliases + Triggers, documented-skip DSX-DQ-001, zero-mint capability guard) — REQ-P14-01..06 now automated-COVERED. **Caught+fixed one self-inflicted test slip before gating:** an initial naive `assertNotIn("data_storage/", guide)` contradicted the 14-04 contract (Task 1 verify *requires* the "without a data_storage" absence phrase; the guide is exempt) — rescoped the check to skills + shims (git-confirmed the string was present at execution commit `4d418b9` and the plan gate passed there). Gate: `sh scripts/check.sh` = all checks passed (**Ran 1243 tests OK**, 1232→+11; catalogue current 256; capability conformant 13 skills; gate contract good/bad/missing; determinism identical). Two manual-only items (prose/semantic fidelity) do NOT reduce compliance (underlying behaviours automated). **Queued to HUMAN-QUEUE:** batched Phase 14 end-of-phase security sign-off (D-05/§4-cat-4) + UAT as HQ-10 — non-blocking until S5-2. Artifacts: `14-SECURITY.md`, `14-VALIDATION.md`, `tests/test_phase14_onboarding.py`. **S2 (Phase 14) is now fully complete (S2-1..S2-5).** Stopped after this one unit per firing instruction. Next unblocked = S3-1 (Phase 16 discuss — settle the REQ-P16-02 finding-code question loudly, D-06 veto window if it mints). git: branch `gsd/v2.2.0-analytic-surface`, pushed to origin (0 ahead / 0 unpushed after this commit). | evidence: `14-SECURITY.md`, `14-VALIDATION.md`
2026-08-28T23:40Z | S2-4 | DONE — Phase 14 code review + verification, both PASS. **Reconcile-first OK** at HEAD `9f2ff47` (branch up-to-date w/ origin; git log == ledger — S2-3 sync `9f2ff47` + the 5 feature commits were the last landed, no correction). **Loud op-decision (non-scope, S1-4 precedent):** ran review + verification DIRECTLY as orchestrator (opus/high, §3), not via `gsd-code-reviewer` — the diff is a bounded doc/skill/template phase (`git diff 2236bb4..720ba10` = 27 files, +756/−15, 1 Python test, **no `dsx/**.py`**), and I re-ran every gate myself (brief §5, not subagent-trusted). **Review = PASS, 0 blocking, 0 fixes.** Load-bearing re-runs: gate-path pure (empty `dsx/` diff AND empty `capability.json` diff over the 5 feature commits; `hooks:[]`/`supported:["*"]` intact); zero-mint (`test_finding_catalogue_invariant` 2 OK = 256 count + set-identity `added=[] removed=[]`; `--check` exit 0 w/ only S0-2 known double-declare noise; `report.add` cli.py=0); new standing hermeticity guard `tests/test_gate_path_hermetic.py` 2 OK (no pandas/scipy/numpy/csv in any `GATE_PROFILES` closure; profiler absent from `dq.py` closure, resolved from live GATE_PROFILES + anti-vacuous anchors); citation authenticity — sole cited code `DSX-DQ-001` present in `references/finding-codes.md`; REQ-P14-04 `Triggers:` clause on **13/13** DSX skills (grep) + **no** `data_storage` in commands/skills + no pandas/scipy in the alias/fragment files; REQ-P14-03 disclosure guarded on the literal `research` value (marketing/`auto` byte-unchanged by construction); REQ-P14-05 documented-skip with all 4 honesty claims + DSX-DQ-001 compensating control in operating-guide §9. **1 non-blocking nit (N1):** hermeticity closure walk doesn't traverse dotted `import dsx.x` (`ast.Import`) statements — negligible (all third-party forbidden imports still captured directly; gate modules use `from`-imports which it does follow); optional D-07 hardening, passes, left as-is (widening risks a bug for no req gain). **Verification = PASSED, 6/6** REQ-P14-01..06 goal-backward (each mapped to a re-run gate/read locator). No D-05 owed (mints no code; DSX-DQ-001 pre-existing). **Gate:** `sh scripts/check.sh` = all checks passed (**Ran 1232 tests OK**, 1230→+2 hermeticity; catalogue current 256; capability conformant 13 skills; gate contract; determinism). Artifacts: `14-.../REVIEW.md`, `14-.../VERIFICATION.md`. Stopped after this one unit per firing instruction. Next unblocked = S2-5 (`/gsd-secure-phase 14` + `/gsd-validate-phase 14`; REQ-P14-06 no new blocking codes). git: branch `gsd/v2.2.0-analytic-surface`, pushed to origin (0 ahead / 0 unpushed after this commit). | evidence: `14-.../REVIEW.md`, `14-.../VERIFICATION.md`
2026-08-28T23:56Z | S3-1 | DONE — Phase 16 discuss complete; `16-CONTEXT.md` written (D-01..D-11, 6 sections). **Reconcile-first OK** at HEAD `34c97ab` (branch up-to-date w/ origin; git log == ledger — S2-5 `34c97ab` was the last landed commit, S2 fully complete, no correction). STATE.md frontmatter was stale (`current_phase: 14`) though its body/stopped_at pointed to S3-1 — advanced to phase 16. **Settled the S3-1 open item LOUDLY, not silently:** REQ-P16-02's reproduce-report gate check **does** need a new finding code — orchestrator re-verified (brief §5) that none of the 11 `DSX-REP-*` codes (max `053`) covers "REPRO-REPORT.md missing" or "declared numbers don't overlap `results.tests`"; `dsx/checks/repro.py` is a pure declaration check already `strict`-gated at verify/ship with `phase_dir` passed (`cli.py:186,190-191`); verify/ship block threshold = **HIGH** (`cli.py:138-139`). Ran an **Architect (`dsx-analysis-architect`) + Auditor (`dsx-ml-integrity-auditor`) 2-persona round, both opus/high (§4), concurrent — UNANIMOUS Option A**, tie-break (rigour>reliability>flexibility) not needed. **D-06 (irreversible numbering, §4 — next-free-in-family, decided by the loop, NOT escalated):** mint **`DSX-REP-060`** (report declared via `reproducibility.reproduce_report` but `REPRO-REPORT.md` missing) + **`DSX-REP-061`** (report present but numbers don't overlap), **both HIGH**, **in Phase 16** — rejected Option B (move to 15: fragments produce/enforce across a phase boundary, contaminates Phase 15's D-05 regime, inverts the P16-only-on-P12 dependency) and Option C (reuse/advisory: no code fits → false text, catalogue dedupes by code hiding the drift, sub-HIGH can't exit 1). **D-07:** ROADMAP "only Phase 15 extends the catalogue" **amended** with a recorded note (`ROADMAP.md:119`) — Phase 15 AND 16 extend it; no requirement dropped/reworded (NOT a §4-cat-3 escalation). **D-08:** catalogue rebaseline is additive (256→**258**); the byte-frozen `tests/fixtures/finding-codes-phase12.md` anchor (which Phase 13/14 zero-mint tests reference) is **never mutated** — expected-set becomes `phase12 ∪ {060,061}`. **No D-05 owed** by Phase 16 (engineering-hygiene codes, brief.md:389 assigns no citation). Also recorded loudly for S3-2/S3-3: D-02 opt-in trigger `reproducibility.reproduce_report` not entrypoint-presence; D-04 overlap = set-membership over a machine-readable YAML block (CRLF-safe, `math.isclose`), gate recomputes nothing + doesn't trust a skill verdict line; D-09 REQ-P16-04 test must be a non-vacuous AST scan w/ positive control (subprocess/runpy are stdlib → NOT caught by the import-based hermetic test); D-10 `protocol_adherence` additive on `*-ATTRIBUTION.yaml` sidecars, catch-rate/FPR pinned; D-11 the "missing-interpreter ≠ exit 1" ↔ `061` short-circuit on an honest SKIPPED status. **Filed HQ-11** (D-06 veto window, non-blocking to S3-2..S3-5; drains at S5-2). Gate met: `16-CONTEXT.md` present (canonical dir `.planning/phases/16-re-run-verification-off-the-gate-path`, GSD `init.plan-phase 16` → `phase_found:true`). Stopped after this one unit per firing instruction (discuss = a full heavy unit; did NOT start S3-2). Next unblocked = S3-2 (Phase 16 plan; plan-checker must pass). git: branch `gsd/v2.2.0-analytic-surface`, will push after commit. | evidence: `16-CONTEXT.md`; `ROADMAP.md:119`; HUMAN-QUEUE HQ-11
2026-08-29T00:35Z | S3-2 | DONE — Phase 16 planned; 4-plan set (16-01..04) passes the plan-checker gate. **Reconcile-first OK** at HEAD `fb30a75` (branch up-to-date w/ origin; git log == ledger — S3-1 `fb30a75` was the last landed commit, no correction). Spawned `gsd-planner` (opus, adaptive-routed) to author; independent `gsd-plan-checker` (opus — overriding the profile's haiku `checker_model` per brief §3 plan-check→opus) verdict **PASS, 0 blocking, 3 non-blocking nits**. **Orchestrator re-verified every load-bearing claim (brief §5, not subagent-trusted) against the actual plan files:** (1) requirements union == exactly {REQ-P16-01,02,03,04} — 16-01{02}/16-02{01}/16-03{03}/16-04{04}; (2) single-writer-per-wave — wave-1 = 16-01 ALONE (the sole catalogue-extending plan), wave-2 = 16-02/03/04 all `depends_on:[16-01]`, `files_modified` pairwise disjoint (skills·templates·capabilities / 3×`*-ATTRIBUTION.yaml`·`test_known_bad_corpus.py` / new `test_no_entrypoint_execution.py`); (3) **D-08 additive rebaseline handled EXACTLY** — 16-01 Task 3 bumps `_EXPECTED_TOTAL` 256→258, adds a SEPARATE `_SNAPSHOT_TOTAL=256` for the byte-frozen anchor's len-leg (the trap: reusing `_EXPECTED_TOTAL` would demand the frozen snapshot enumerate 258), set-identity compares `current==snapshot∪{DSX-REP-060,061}`, phase-12 snapshot never in `files_modified` (git-status-empty asserted); (4) both `DSX-REP-060`/`061` HIGH (verify/ship threshold HIGH — MEDIUM couldn't exit 1) w/ FIXED plain-string messages (f-string would render `<…>` per `gen-finding-catalogue.py:177-193`); (5) D-02 opt-in on `reproduce_report` not entrypoint-presence, good D-08 fixture stays silent (16-01 touches no `examples/`); (6) D-04 overlap = `math.isclose(rel_tol=1e-2)` lead-metric set-membership over a CRLF-safe fenced YAML block, verdict-agnostic (`test_verdict_pass_does_not_suppress_061`); (7) D-09 REQ-P16-04 = static AST scan (NOT grep) over `dsx/checks/`+`dsx/frame/`, positive control (`subprocess.run`/`runpy.run_path`/`os.system`/`exec` asserted FLAGGED) + negative control (`ast.*`/`re.compile` NOT flagged) + named non-empty scan set incl `code.py` & `repro.py`; (8) D-10 `protocol_adherence` on sidecars only, `_headline` pinned (0.25,0.3) + asserted not in `co_varnames`, two anchor tests unedited; (9) D-11 SKIPPED/UNABLE short-circuit present in BOTH the 16-02 template AND the 16-01 061 check; (10) no plan writes any tracking file. **Planner refinements (both sound):** added `tests/test_reproduce_report.py` to 16-01 (behavioural proof of 060/061 firing/silence + D-11); chose wave-(b) over all-wave-1 for a clean 258-baseline-before-consumers ordering. **3 non-blocking nits carried to S3-3:** N1 POSIX verify blocks require the git-bash executor (as S1-3/S2-3 did); N2 16-03 uses a shell-glob git pathspec; N3 16-01's `report.add` message must be a single string literal or the catalogue row drops (`--check` self-catches). No mint performed this unit — the plans DESCRIBE the mint; S3-3 executes it. STATE.md advanced to S3-3 (total_plans 5→4 for Phase 16, resume at execute). Stopped after this one unit per firing instruction (S3-3 execute is the heavier next unit). Next = S3-3 (execute all Phase 16 plans; wave 1 = 16-01, wave 2 = 16-02/03/04). git: branch `gsd/v2.2.0-analytic-surface`, will push after commit. | evidence: `16-0{1..4}-PLAN.md`; plan-checker PASS
2026-08-29T03:27Z | S3-3 (recovered) | DONE — Phase 16 all 4 plans executed + verified. **Reconcile-first (§0.4) found a ledger/repo disagreement and trusted the repo:** ledger showed S3-3 `[ ]` but HEAD `ec216b2` (S3-2) had the FULL S3-3 output sitting UNCOMMITTED + UNLOGGED in the working tree — 4 `16-0{1..4}-SUMMARY.md` (all `status: complete`), `skills/dsx-reproduce/SKILL.md`, `templates/REPRO-REPORT.md`, `dsx/checks/repro.py` (+109, `_check_reproduce_report`), the `DSX-REP-060/061` mint in `references/finding-codes.md`, the D-08 rebaseline in `test_finding_catalogue_invariant.py`, D-10 sidecars+`test_known_bad_corpus.py`, D-09 `test_no_entrypoint_execution.py`, `test_reproduce_report.py`. A prior firing executed all 4 plans then crashed before commit/gate/log (no S3-3 Log line existed). Per §0.4 I completed S3-3 (verify + gate + commit) rather than restarting it. **Re-ran EVERY gate myself (brief §5, NOT summary-trusted), all green:** `gen-finding-catalogue.py --check` exit 0 (only S0-2 shipped-tree double-declare noise); `test_finding_catalogue_invariant` 2 OK (258 count + set-identity vs `snapshot∪{060,061}`); `test_reproduce_report` 7 OK; `test_no_entrypoint_execution` 3 OK; `test_known_bad_corpus` 45 OK; `test_gate_path_hermetic` 2 OK. **Gate-path purity (D-01):** `git status -- dsx/` = only `dsx/checks/repro.py`; `-- scripts/` empty; no pandas/scipy/numpy/subprocess/runpy/os/csv import in repro.py (only stdlib `math`,`re`); `tests/fixtures/finding-codes-phase12.md` byte-unchanged. **Mint confirmed on disk:** 256→258 additive, both codes HIGH, exact HQ-11 text; `capability.json` appended `dsx-reproduce` (14th), `hooks:[]` untouched. **Committed the ceremony artifacts ONLY** — the operator-local untracked files (`.claude/`, `.vscode/`, `graphify-out/`, `EDA_enhancement_BRIEF.md`, `references/The AI Data Scientist.md`) were left untracked, staged by explicit pathspec never `-A` — as 4 atomic commits in wave order (16-01 mint first, then 02/03/04, matching S1-3/S2-3 precedent): 16-01 `1195d97`, 16-02 `71454f6`, 16-03 `e32f9e6`, 16-04 `0bd6a75`, + this ledger sync. HQ-11 D-06 veto window stays open (non-blocking). Stopped after this one unit per firing instruction (S3-4 = full-suite `scripts/check.sh` + code review is the heavier next unit). Next = S3-4. git: branch `gsd/v2.2.0-analytic-surface`, pushed to origin after this commit. | evidence: commits 1195d97,71454f6,e32f9e6,0bd6a75; `16-0{1..4}-SUMMARY.md`
