# LOOP-LEDGER — v2.3 Test Catalog

Source of truth for the autonomous loop. One checkbox = one unit.
Check a box ONLY with its gate evidence pasted in the Log (one line; long
evidence to `LOOP-LEDGER-ARCHIVE.md`).

**Scope source:** `.planning/research/V2.3-V2.4-SCOPE.md` (2026-08-29) and
`.planning/REQUIREMENTS.md` (22 requirements, REQ-P17-01 … REQ-P20-04). Both
carry full goals, citations, doctrine dispositions and ordering constraints —
**this milestone needs execution, not a fresh scoping round.**

**Predecessors:** v2.2 Analytic Surface shipped 2026-08-29 (tag `v2.2.0`);
v2.0.0 shipped 2026-08-28 (tag `v2.1.0`). Loop artifacts archived at
`.planning/milestones/v2.2-LOOP-*`, `v2.2-HUMAN-QUEUE.md`, and `v2.0.0-*`.

## Ordering rationale

Phases run in numeric order **17 → 18 → 19 → 20**: Phase 17 is the foundation
every later unit reads (`estimand_kind` vocabulary, Boschloo repair, D-12a
dispositions, D-06 pre-allocated ranges) and hard-blocks 18/19; Phase 20 is
terminal calibration and consumes every new code. The D-05 human reads are the
long pole (~15–20), so their evidence packs are filed at S0 — before Phase 17
even plans — exactly the v2.2 HQ-8 pattern that worked.

**Standing v2.3 rules (from the brief §5, repeated because they bind every unit):**
routing keys read DECLARED fields only; doc table and `recommend_test` change in
the same commit; new codes only from the pre-allocated ranges; effect-size bands
are conventions, never thresholds.

## S0 — Milestone bootstrap

- [x] S0-1 Verify GSD state points at v2.3 (STATE.md frontmatter `milestone: v2.3`,
  `current_phase: 17`, progress 0/4) and `gsd-tools query init.milestone-op`
  resolves it (4 phases, 0 complete); `.planning/phases/` is empty (v2.2 dirs
  archived). Gate: command output pasted. → evidence in LOOP-LEDGER-ARCHIVE.md `## S0-1`
- [x] S0-2 Re-verify the inherited scope against the live tree before planning on
  it: per-requirement verdict table (still-valid / already-satisfied /
  contradicted) written to `.planning/v2.3-SCOPE-RECHECK.md`. Key premises to
  re-confirm: `fisher_exact` still at `dsx/checks/stats.py:65` and `boschloo_exact`
  absent from `NONPARAMETRIC_TESTS`; `QUESTION_TYPES` still the closed 5-key list;
  the `time_to_event` fallthrough still unconditional; live catalogue count
  re-measured (do NOT assume 260). Gate: the recheck file with evidence.
  → `.planning/v2.3-SCOPE-RECHECK.md`: all 5 premises CONFIRMED at locators, count re-measured = 260, all 22 reqs still-valid.
- [x] S0-3 File the **Phase 18 D-05 citation evidence pack** to HUMAN-QUEUE
  (correlation + agreement codes and effect-size band sources: Cohen 1960/1968,
  Fleiss 1971, Hayes & Krippendorff 2007 worked value, Shrout & Fleiss 1979,
  McGraw & Wong 1996, Koo & Li 2016, Feinstein & Cicchetti 1990, Landis & Koch
  1977, Bland & Altman 1986, Fisher 1921). Per-citation table: locator, the exact
  formulation/value the check will cite, confirmed-by-loop vs
  UNVERIFIED-for-human split. **Do not sign — D-05 is a human read.**
  → filed as HQ-16 (HUMAN-QUEUE.md): 11 citations, 6 groups keyed to the two Phase 18
  gate codes + band sources; all 11 locators corroborated across multiple independent
  sources; two load-bearing reads flagged (Krippendorff α=0.743 worked value w/ the
  0.734 book-typo trap; Landis-Koch/Koo-Li band boundaries). NOT signed — human read owed.
- [x] S0-4 File the **Phase 19 D-05 citation evidence pack** to HUMAN-QUEUE
  (Greenhouse & Geisser 1959 + Maxwell & Delaney 2004 ch.11-12; Hamed & Rao 1998;
  Armitage 1955 / Cochran 1954; Campbell 2007; Davidson & MacKinnon 2000; Efron
  1987; Games & Howell 1976; Hayter 1986; Zimmerman 2004; Hoenig & Heisey 2001;
  Lakens 2022; Brown, Cai & DasGupta 2001; Newcombe 1998; McCullagh & Nelder 1989
  §6.2; Wilson 2015). Same format. **Do not sign.**
  → filed as HQ-17 (HUMAN-QUEUE.md): 16 citations in 7 groups keyed to the REQ-P19-01…07
  gates; every locator corroborated across ≥2 independent sources; 7 load-bearing reads
  flagged (esp. McCullagh-Nelder §6.2 UNCONFIRMED → ships chapter-granular; Hayter 1986
  JASA vs the 1984 Annals decoy; Newcombe Paper B 873-890/SIM779). NOT signed — human read owed.

## S1 — Phase 17: Foundation — repairs and spec vocabulary (5 requirements)

- [x] S1-1 Discuss (assumptions mode + persona round); `17-CONTEXT.md` written.
  Must settle: final `estimand_kind` vocabulary name/members; the D-12a
  disposition table (REQ-P17-03) for every Phase 18/19 gate check; the D-06 range
  pre-allocation plan keyed to the live count.
  → `.planning/phases/17-foundation-repairs-and-spec-vocabulary/17-CONTEXT.md`: Architect+Statistician
  round (opus/high, concurrent). D-01 `estimand_kind` = closed 6-member set on `analysis:`
  (5 required + `nominal_association` added, unanimous); D-02 disposition table for all 9 gates
  (8 neutral/self-scoping, observed-power ban ships + Bayesian BF-sibling D-13-deferred);
  D-03 D-06 ranges DSX-STA 050–129 (decade/theme, keyed to count 260), 130s reserve; D-04
  Boschloo reconcile-to-doc for S1-3. Veto window = HQ-18 (non-blocking).
- [x] S1-2 Plan (plan-checker must pass).
  → 3 plans across 2 waves (17-01 Boschloo reconcile; 17-02 fallthrough pin + D-12a/D-06 verify; 17-03 estimand_kind vocab + DSX-STA-040 widen + zero-new-codes). gsd-plan-checker (haiku) VERIFICATION PASSED — all 12 dims green, REQ-P17-01..05 covered, single-writer waves (17-03 depends_on 17-01, both touch stats.py). dsx plan:post BLOCKING gate re-run by orchestrator = clean spec-less (exit 0, "no ANALYSIS-SPEC found — skipping"). See Log 2026-08-30T00:21Z.
- [x] S1-3 Execute all plans (Boschloo reconciliation + pinned regression test;
  `estimand_kind` vocabulary additive with D-08 fixture extension; fallthrough
  regression test; range pre-allocation note committed).
  → Wave 1 done prior firing (17-01/17-02). Wave 2 (17-03) executed inline this firing:
  `ESTIMAND_KINDS` 6-member vocab in spec.py + DSX-STA-040 widened to a single-call-site
  `(field,vocab)` loop over outcome_type+estimand_kind (Pitfall-2: runs independent of the
  declared-test early return); fixtures/template extended (D-08); catalogue regenerated.
  Gates re-run by orchestrator: `test_estimand_kind_vocab` 6/6, catalogue `--check` exit 0
  = 260 by set identity (zero new codes, REQ-P17-05), golden+good-fixture 11/11, **full
  suite 1323 OK from clean tree**. See Log 2026-09-01T21:49Z + 17-03-SUMMARY.md.
- [x] S1-4 Code review + fixes; verification `passed` (goal-backward vs
  REQ-P17-01..05; REQ-P17-05 zero-new-codes asserted by set-identity diff).
  → `17-REVIEW.md` (1 LOW finding — missing `Any` import in stats.py — FIXED) +
  `17-VERIFICATION.md` verdict **PASSED** all 5 reqs goal-backward. Gate re-run by
  orchestrator from a clean tree: `scripts/check.sh` all checks passed (1323 OK; catalogue
  current @260 set-identity = REQ-P17-05; gate contract good/bad/missing; determinism).
  Targeted 12/12 ok. See Log 2026-09-01T22:00Z.
- [x] S1-5 `/gsd-secure-phase 17` verified + `/gsd-validate-phase 17` compliant.
  End-of-phase security sign-off + UAT batched to HUMAN-QUEUE (non-blocking
  until S5-2).
  → **secure:** State B, `17-SECURITY.md` written — 11-threat register (8 mitigate, 3 accept),
  all CLOSED, **threats_open:0**, `status: verified`; every non-accepted mitigation re-run green
  (13 targeted tests + catalogue `--check` exit 0 + D-06/D-12a grep oracle). **validate:** State A,
  `17-VALIDATION.md` — all 5 reqs COVERED, **nyquist_compliant:true**, zero gaps (no auditor spawn).
  Operator security sign-off + Phase-17 UAT batched to HUMAN-QUEUE **HQ-19** (non-blocking until S5-2).
  **Phase 17 CLOSED** (S1-1…S1-5 all done). See Log 2026-09-01T22:16Z. commits 1ab7f8a, c45f666.

## S2 — Phase 18: Correlation, association and agreement (6 requirements)

- [x] S2-1 Discuss + persona round; `18-CONTEXT.md`. D-06 numbering from the
  pre-allocated ranges (veto window filed to HUMAN-QUEUE, non-blocking). Blocked
  until S1-5 AND the S0-3 evidence pack is human-answered for every code that
  ships a citation (codes whose citations are not confirmed are NOT in hand).
  → `18-CONTEXT.md`: Architect+Statistician round (opus/high, concurrent, unanimous). 8 decisions
  (D-01…D-08). Five HIGH codes minted: DSX-STA-050/051 (correlation scale/kind, range 050–059) +
  060/061/062 (agreement completeness, range 060–069); dataless `recommend_association` = anti-two-stage
  proof (P18-06); p_pos/p_neg companion correction (HQ-16); report-only effect-size KIND registry keeps
  `EFFECT_SIZE_KINDS={d,h,r}` blocking-domain intact; α=0.7598@ordinal pinned, ICC/Kendall-W bands +
  dCor/partial catalog-only. Two riders DEFERRED (ICC coherence → candidate 063; coeff-typo guard).
  Veto window = HQ-20 (non-blocking). S1-5 done + HQ-16 answered = unblocked. See Log 2026-09-01T22:55Z.
- [x] S2-2 Plan (plan-checker must pass). Single-writer waves: `dsx/checks/stats.py`
  and `references/test-selection.md` change in the same plan/commit, always.
  → 2 plans, file-disjoint single-writer Wave 1: **18-A** routing+gates+doc/catalogue lockstep
  [REQ-P18-01/02/03/04/06] ∥ **18-B** effect-size convention bands [REQ-P18-05]. gsd-plan-checker
  (adaptive→haiku) → **VERIFICATION PASSED**: all 6 reqs covered (each with a checkable oracle),
  D-01…D-08 honored (Dim-7), 18-A/18-B files_modified disjoint (DSX-STA-012 seam via getattr default,
  not a shared write). 2 non-blocking warnings BOTH FIXED inline: W-1 (18-A Task-3 verify was a no-op
  import → replaced with a real fixture audit that runs `run_checks` on both canonical specs and asserts
  the five codes absent) + W-2 (18-RESEARCH.md OQ-1/2/3 marked RESOLVED-in-18-A). Orchestrator re-ran
  gates: dsx plan:post clean spec-less (exit 0); the injected W-1 command validated runnable vs the
  current tree (five codes can't fire yet → passes, proves the command works); gap-analysis + decision-
  coverage CLI non-blocking (known format-parse under-report, HUMAN-QUEUE note). See Log 2026-09-02T01:36Z.
- [x] S2-3 Execute all plans (rows + gates + mathx bands + APA wiring).
  → 18-B (fdd8617/9647387/aafeecc) ∥ 18-A (03c0327/a8ac933/0381a2e/28f3f2c) executed inline on the
  ceremony branch by two sequential gsd-executors (18-B first → DSX-STA-012 seam validated live, not skipped).
  **Wave-1 merge gate re-run by orchestrator from a clean tree:** full suite **1366 OK**; catalogue `--check`
  exit 0 at **265** (256 snapshot + 4 prior mints + 5 new = set-identity, no drift); DSX-STA-050/051/060/061/062
  all present; `recommend_association` signature == ['estimand_kind'] (REQ-P18-06 anti-two-stage);
  EFFECT_SIZE_KINDS firewall {d,h,r} intact + Krippendorff ordinal 0.7598 pinned; both canonical fixtures
  silent on the five new codes (no fixture edit); seam oracle RUNS+PASSES post-merge. See Log 2026-09-02T02:19Z.
- [x] S2-4 Code review + fixes; verification `passed` (REQ-P18-01..06; the
  no-autoswitch extension test green).
  → `18-REVIEW.md` (1 LOW finding — `CORRELATION_FAMILY` drift risk vs `_ASSOCIATION_ROUTES`
  union, claimed "cannot drift" but unenforced — FIXED with an invariant test, the codebase's
  own idiom) + `18-VERIFICATION.md` verdict **PASSED** all 6 reqs goal-backward. Three suspected
  defects adversarially probed and cleared (is_blank(0.0)=False so p_pos=0.0 no false-fire;
  normalize() str()-first tolerates non-string icc; DSX-STA-012 seam branch correct + inert-safe).
  Gate re-run by orchestrator from a clean tree: **full suite Ran 1367 OK** (1366 + 1 invariant
  test), catalogue `--check` exit 0 @265 (5 codes each present once), no-autoswitch test green,
  seam oracle RUNS (not skipped) + passes. See Log 2026-09-02T02:31Z.
- [x] S2-5 `/gsd-secure-phase 18` + `/gsd-validate-phase 18`; sign-off batched.
  → **secure:** State B, `18-SECURITY.md` written — combined **10-threat** register (8 mitigate, 2 accept =
  supply-chain), all CLOSED, **threats_open:0**, `status: verified`; every non-accepted mitigation re-run green
  from a clean tree (52 targeted Phase-18 tests + catalogue `--check` exit 0 @265 + `EFFECT_SIZE_KINDS={d,h,r}`
  firewall + DSX-STA-012 seam oracle, all inside full suite **1367 OK**). **validate:** State A, `18-VALIDATION.md`
  — all 6 reqs (REQ-P18-01…06) COVERED, **nyquist_compliant:true**, zero gaps (no auditor spawn). Operator security
  sign-off + Phase-18 UAT batched to HUMAN-QUEUE **HQ-21** (non-blocking until S5-2). **Phase 18 CLOSED**
  (S2-1…S2-5 all done). See Log 2026-09-02T02:44Z. commit this firing.

## S3 — Phase 19: RM, trend, categorical, resampling, post-hoc (7 requirements)

- [ ] S3-1 Discuss + persona round; `19-CONTEXT.md`. D-06 numbering from ranges
  (veto window). Blocked until S1-5 AND the S0-4 pack is answered for shipping
  codes. This is the largest phase — the discuss should confirm the wave split
  keeps every file single-writer.
- [ ] S3-2 Plan (plan-checker must pass).
- [ ] S3-3 Execute all plans (rows incl. DEPRECATED routing-off entries; negative
  gates; doc/code lockstep per commit).
- [ ] S3-4 Code review + fixes; verification `passed` (REQ-P19-01..07).
- [ ] S3-5 `/gsd-secure-phase 19` + `/gsd-validate-phase 19`; sign-off batched.

## S4 — Phase 20: Calibration and reporting close (4 requirements)

- [ ] S4-1 Discuss (light — calibration shape is Phase-12-precedented);
  `20-CONTEXT.md`.
- [ ] S4-2 Plan (plan-checker must pass).
- [ ] S4-3 Execute (known-bad fixtures per new code; catch-rate/FPR re-baseline;
  D-08 silence; no-autoswitch full coverage; the REQ-P20-04 doc/code agreement
  test).
- [ ] S4-4 Code review + fixes; verification `passed` (REQ-P20-01..04).
- [ ] S4-5 `/gsd-secure-phase 20` + `/gsd-validate-phase 20`; sign-off batched.

## S5 — Close-out

- [ ] S5-1 `/gsd-audit-uat` cross-phase sweep. **Do NOT accept the CLI's "All
  Clear"** — it under-reports via TWO known defects (heading-level mismatch AND
  the `-VERIFICATION` filename filter); hand-check every phase's VERIFICATION.md.
- [ ] S5-2 Drain HUMAN-QUEUE (the only permitted blocking wait).
- [ ] S5-3 `/gsd-extract-learnings`.
- [ ] S5-4 `/gsd-audit-milestone` — must reach `passed`.
- [ ] S5-5 `/gsd-complete-milestone` — **NOT headless-safe** (interactive
  prompts + `git rm REQUIREMENTS.md`); runs in an interactive session per
  operator approval. Hand-verify the CLI's generated accomplishments and the
  archived REQUIREMENTS checkboxes (both were wrong at v2.2 close and needed
  correction).
- [ ] S5-6 Ship: merge to `main` **by explicit branch name**
  (`git merge --no-ff gsd/v2.3.0-test-catalog`, verified on a throwaway branch
  first, full suite + `scripts/check.sh` green BEFORE touching `main`) and tag
  `v2.3.0`. **Never** the framework's alphabetical `gsd/*` auto-detect (stale
  branches would be picked); **never** `/gsd-pr-branch` (cherry-pick chain fails
  on long branches); **never** force-move a published tag. Outward-facing —
  queue to HUMAN-QUEUE for the operator, do not self-approve.

## Log

(append one line per firing: `YYYY-MM-DDTHH:MMZ | unit | outcome | evidence`.
Keep the most recent ~15–20; archive older entries to `LOOP-LEDGER-ARCHIVE.md`.)

2026-09-02T02:44Z | S2-5 | DONE — Phase 18 secure + validate, run inline by the orchestrator on `gsd/v2.3.0-test-catalog` (branch confirmed; framework `handle_branching` NOT run — secure/validate don't switch branches). **PHASE 18 NOW CLOSED** (S2-1…S2-5 all checked). **Reconciled ledger vs repo (brief §0.4):** HEAD `69c51e6` == ledger's S2-4-DONE claim, branch up-to-date with origin, catalogue 265 — agree, no correction. **`/gsd-secure-phase 18` (State B, `18-SECURITY.md`):** both PLAN threat registers (18-A, 18-B) carry a parseable `<threat_model>` → `register_authored_at_plan_time:true`, ASVS L1, `block_on:high`. Built the combined **10-threat** register (8 mitigate + 2 accept = supply-chain, zero packages) — T-18-A-01…05/SC + T-18-B-01…03/SC; classified **all 10 CLOSED, threats_open:0**. Did NOT trust the L1 short-circuit blind (brief §5): re-ran every non-accepted mitigation from a clean tree (3 stray gitignored `DECISIONS.jsonl` cleared per HQ note) → **full suite `Ran 1367 OK`** (35.996s); 52 targeted Phase-18 tests OK (`test_correlation_scale_kind_gate`+`test_agreement_completeness_gate`+`test_declared_association_routing`+`test_effect_size_kind`+`test_finding_catalogue_invariant`); `gen-finding-catalogue.py --check` **exit 0 @265** ("finding catalogue is current"; the 3 `declared twice` warnings are pre-existing legacy, none Phase-18); `EFFECT_SIZE_KINDS={d,h,r}` firewall (`test_effect_size_kinds_is_exactly_d_h_r`+`test_interpret_effect_still_rejects_a_report_only_kind`) green; DSX-STA-012 seam oracle `test_report_only_kappa_fires_neither_011_nor_012_and_reports_ok` RUNS (not skipped)+passes; T-18-A-05 presence-only/catalog-only discipline confirmed in `test-selection.md` (dCor/partial/ICC-Koo-Li/Kendall's-W all catalog-only, no invented locator). `status:verified`. Operator security **Approval** line NOT self-signed → batched HQ-21 (brief §4 item 4). **`/gsd-validate-phase 18` (State A, `18-VALIDATION.md`):** all 6 reqs (REQ-P18-01…06) COVERED — 10 automated oracles green (the 52 targeted + catalogue `--check` + firewall/seam, inside the 1367-OK suite) + 1 manual-only D-05 citation item (HQ-16-answered for the five gate codes). **Zero gaps → no nyquist-auditor spawn (workflow §3).** `status:validated` + `nyquist_compliant:true` + `wave_0_complete:true`. Phase-18 UAT folded into HQ-21 (internal routing/gate/catalogue phase, no user-facing surface → acceptance = the green automated gates). Stopped at the ~12-min pacing cap on the S2-5 / Phase-18-close boundary (S3-1 is a full opus/high discuss+persona unit — starting it risks mid-unit compaction on a decision artifact, brief §1). **Next unblocked = S3-1** (Phase 19 discuss): UNBLOCKED — S1-5 done AND HQ-17 (Phase 19 D-05 pack) answered 2026-09-01. This is the largest phase (7 reqs); the discuss must confirm the wave split keeps every file single-writer. | 18-SECURITY.md; 18-VALIDATION.md; HUMAN-QUEUE.md HQ-21; commit this firing
2026-09-02T02:31Z | S2-4 | DONE — Phase 18 code review + fixes + verification `passed`, run inline by the orchestrator (opus/high, brief §3) on `gsd/v2.3.0-test-catalog` (branch confirmed; framework `handle_branching` NOT run — code review/verify don't switch branches, confirmed anyway per HQ branch-safety rule). **Reconciled ledger vs repo (brief §0.4):** HEAD `0a14a8a` == ledger's S2-3-DONE claim, branch up-to-date with origin, catalogue 265 — ledger and repo agree, no correction. **Review (`18-REVIEW.md`):** read the full `a266a9b..HEAD` source diff (1003 insertions / 12 files: stats.py routing+gates, mathx.py convention bands, spec.py vocabularies, doc mirror + catalogue regen, APA template, 5 test files). **One LOW finding — `CORRELATION_FAMILY` drift risk:** stats.py defines the DSX-STA-051 key set as a standalone literal AND `_ASSOCIATION_ROUTES` whose three coefficient sets union to the same six; the comment claims they "cannot drift" but nothing enforced it under permanent D-06 numbering (verified currently equal). **Fix applied:** `CorrelationFamilyInvariantTest` asserting `CORRELATION_FAMILY == union(route test-sets)` — the codebase's own invariant-test idiom (cf. the EFFECT_SIZE_KINDS firewall test); test-only, mints no code, catalogue unaffected. **Three suspected defects adversarially probed + cleared:** `is_blank(0.0)`→`False` (spec.py:688 only None/empty-str/empty-container are blank) so a legitimate `p_pos/p_neg: 0.0` does NOT false-fire DSX-STA-062; `normalize()` does `str(value)` first (spec.py:728) so a non-string `icc.model` yields a string (out-of-vocab→fires) never an AttributeError; the DSX-STA-012 report-only `elif` sits correctly inside `if p<alpha and standardized is not None` between the 011 and 012 branches (inert-safe via getattr, recognises only on a significant standardized result). `fleiss_kappa` in the 062 predicate is a recorded D-02 decision (tested), not drift; non-dict-icc-without-test=icc is within the stated presence contract. Security clean (declaration-only string/isinstance/dict-lookup, no data path, no user regex). **Verification (`18-VERIFICATION.md`) = PASSED** all 6 reqs goal-backward: P18-01 `recommend_association` 3-kind routing + dCor/partial catalog-only + doc mirror; P18-02 kappa/ICC-triple/Krippendorff-pin/Bland-Altman/Cronbach→omega; P18-03 DSX-STA-050 (Pearson@ordinal, D-03 whitelist) + 051 (correlation-family@agreement/method_comparison); P18-04 DSX-STA-060 (ICC triple presence+membership) + 061 (weights isinstance) + 062 (p_pos AND p_neg, HQ-16-corrected); P18-05 mathx report-only bands + EFFECT_SIZE_KINDS={d,h,r} firewall + ungated APA template mints no code; P18-06 dataless `recommend_association` signature (stronger per-function proof) + decision-surface grep covers new stats.py. **Gate re-run by orchestrator from a clean tree** (stray DECISIONS.jsonl cleared per HQ note): targeted Phase-18 suite 53 OK + new invariant 1 OK; **full suite `Ran 1367 tests ... OK`** (1366 + the 1 invariant test); `gen-finding-catalogue.py --check` exit 0, **Total 265** (5 codes each present once; the 3 `declared twice` warnings are pre-existing legacy DSX-SPEC-070/DSX-VAL-021/DSX-VAL-060, none Phase-18); seam oracle `test_report_only_kappa_fires_neither_011_nor_012_and_reports_ok` RUNS (not skipped) + passes. **S2-4 CHECKED.** Stopped at the S2-4 boundary (~12-min pacing cap; S2-5 is `/gsd-secure-phase 18` + `/gsd-validate-phase 18`, a distinct multi-step unit). **Next unblocked = S2-5** (Phase 18 secure + validate; sign-off batched to HUMAN-QUEUE). | 18-REVIEW.md; 18-VERIFICATION.md; CORRELATION_FAMILY invariant test (this commit)
2026-09-02T02:19Z | S2-3 | DONE — Phase 18 plans EXECUTED inline on `gsd/v2.3.0-test-catalog` (branch confirmed before/after by both executors AND me; framework `handle_branching` NOT run — HQ branch-safety rule). **Two file-disjoint Wave-1 plans, run by two SEQUENTIAL gsd-executors (adaptive routing), 18-B before 18-A** so 18-A's DSX-STA-012 report-only seam validated LIVE against a present `mathx.REPORT_ONLY_EFFECT_KINDS` rather than skipping. **18-B** (commits fdd8617/9647387/aafeecc, REQ-P18-05): `REPORT_ONLY_EFFECT_KINDS` + `KAPPA_BANDS` (Landis-Koch, convention-labelled) + `KRIPPENDORFF_REFERENCE` (0.7598@ordinal pinned, level-carrying; nominal/interval/ratio companions) + catalog-only ICC/Koo-Li, Kendall's-W ("no band citation exists"), dCor, partial, Cronbach→omega (NO numeric boundary) + `label_convention_band` in mathx.py; bands wired ONLY into the ungated APA template (mints no code); EFFECT_SIZE_KINDS firewall stays exactly {d,h,r}. One honesty-only deviation: template prose reworded off the literal `DSX-STA-011` to satisfy "mints no finding code". **18-A** (commits 03c0327/a8ac933/0381a2e/28f3f2c, REQ-P18-01/02/03/04/06): dataless `recommend_association(estimand_kind)` (signature == ['estimand_kind'] = the anti-two-stage proof) + `_check_declared_association` wired at BOTH check() call sites → five HIGH codes DSX-STA-050 (Pearson@ordinal, D-03 point-biserial/dichotomous whitelist) / 051 (correlation-family for agreement|method_comparison) / 060 (ICC triple presence+membership) / 061 (weighted-kappa weights, Pitfall-5 isinstance branch — nested-list matrix accepted) / 062 (kappa p_pos AND p_neg, HQ-16-corrected D-04); new spec vocabularies (OPERAND_SCALES/ICC_MODELS/ICC_TYPES/ICC_DEFINITIONS/KAPPA_WEIGHT_TOKENS) registered in _VOCABULARIES; operand_scale joined _MEMBERSHIP_FIELDS (mis-slot loud via DSX-STA-040 for free); five codes added BY EXACT NAME to `_D05_ALLOWLIST_CODES` (DSX-STA- prefix deliberately NOT allowlisted); Association/agreement doc mirror in test-selection.md; finding-codes.md REGEN in-commit with the report.add sites (D-08 lockstep). One honesty-only deviation inside the permitted invariant test: method-name/message "260"→"265" prose alongside the _EXPECTED_TOTAL bump; _SNAPSHOT_TOTAL 256 + phase12 fixture byte-frozen. **GATE RE-RUN BY ORCHESTRATOR from a clean tree (stray DECISIONS.jsonl cleared per HQ note):** full suite **`Ran 1366 tests ... OK`**; `gen-finding-catalogue.py --check` exit 0, **Total 265** (set-identity: 256 frozen + 4 prior + 5 new, `--write` produced no diff = no drift; the 3 `declared twice` warnings are pre-existing legacy, none are Phase-18 codes); `inspect.signature(recommend_association)==['estimand_kind']`; `EFFECT_SIZE_KINDS=={d,h,r}` + Krippendorff ordinal 0.7598; BOTH canonical fixtures fire NONE of the five new codes with neither fixture edited. **S2-3 CHECKED — both plans executed, merge gate green.** Stopped at the S2-3 boundary: S2-4 (code review + verification) is a full opus/high unit that would risk mid-unit compaction on a decision artifact past the ~12-min pacing cap. **Next unblocked = S2-4** (Phase 18 code review + fixes; verification `passed` REQ-P18-01..06; the no-autoswitch extension test green). | 18-A-SUMMARY.md; 18-B-SUMMARY.md; commits fdd8617,9647387,aafeecc,03c0327,a8ac933,0381a2e,28f3f2c
2026-09-02T01:36Z | S2-2 | DONE — Phase 18 plan gate PASSED, run inline by the orchestrator on `gsd/v2.3.0-test-catalog` (branch confirmed before/after; framework `handle_branching` NOT run — plan-phase doesn't switch branches, confirmed anyway per HQ branch-safety rule). **Reconciled ledger vs repo (brief §0.4):** a prior firing's gsd-planner had already written BOTH plans (18-A/18-B, uncommitted, `phase_status: Planned`, `has_plans:true`) then stopped before the checker — ledger+STATE still said "run planner next"; trusted the repo. Ran **gsd-plan-checker** (adaptive→haiku) on the two existing plans → **VERIFICATION PASSED**: REQ-P18-01…06 all covered (18-A: 01/02/03/04/06 ∥ 18-B: 05), D-01…D-08 honored (Dim-7 substantive), 18-A/18-B `files_modified` disjoint single-writer (DSX-STA-012 report-only seam via `getattr(mathx,...,frozenset())` default — inert in isolation, NOT a shared write). **2 non-blocking warnings BOTH FIXED inline before commit:** W-1 — 18-A Task-3 verify was a no-op (imported+printed, never audited) → replaced with a real audit running `run_checks` on good+bad canonical specs at the strictest 'ship' gate asserting the five new codes absent (validated runnable vs current tree: passes, codes don't exist yet); W-2 — 18-RESEARCH.md OQ-1/2/3 left "Planner decision" → marked RESOLVED-in-18-A (same class as S1-2 Dim-11). **Orchestrator re-ran gates (brief §5):** dsx plan:post clean spec-less (exit 0, "no ANALYSIS-SPEC — skipping"); gap-analysis-plan-post + decision-coverage-plan non-blocking (`passed:true`, "nothing to check" = the known format-parse under-report). Stopped at the S2-2 boundary — S2-3 (execute BOTH plans + full-suite Wave-1 merge gate) is a large multi-plan unit that risks mid-unit compaction on a gated artifact (brief §1) and the ~12-min cap. **Next unblocked = S2-3** (execute 18-A ∥ 18-B inline on the ceremony branch; Wave-1 merge gate validates the DSX-STA-012 seam; catalogue 260→265). | 18-A-PLAN.md; 18-B-PLAN.md; 18-RESEARCH.md (W-2); LOOP-LEDGER.md; STATE.md
2026-09-01T23:26Z | S2-2 | IN PROGRESS (plan preflight) — Phase 18 plan preflight done inline on `gsd/v2.3.0-test-catalog` (branch confirmed before/after; framework `handle_branching` NOT run — HQ branch-safety rule). **gsd-phase-researcher (sonnet, init routing) → `18-RESEARCH.md` (commit 4e531df, pushed):** every live locator verified against the tree (stats.py `recommend_test`/`_check_declared_test` shape + `check()` dispatch idiom; spec.py `_VOCABULARIES` registry; mathx.py `EFFECT_SIZE_KINDS`+`interpret_effect`; `gen-finding-catalogue.py`; `test_finding_catalogue_invariant.py` `_EXPECTED_TOTAL`/`_SNAPSHOT_TOTAL`/`_MINTED_CODES`; both canonical fixtures; ungated APA template). **Surfaced a real HOW gap the CONTEXT locator list missed:** the five new codes must be added to `_D05_ALLOWLIST_CODES` **by exact name** (family prefix `DSX-STA-` is NOT in `_D05_ALLOWLIST_PREFIXES`) or the D-05 citation build gate silently skips them; recommends splitting the gate into `_check_correlation_scale_kind` (050/051) + `_check_agreement_completeness` (060/061/062) for per-function citation docstrings; weighted-kappa `weights` is string-OR-matrix → needs an `isinstance` branch (breaks the pure-string normalize idiom); 3 field shapes genuinely undecided (operand-scale field D-03, ICC triple nesting, kappa companion nesting) → left as planner Open Questions with concrete recommendations; 5 other Open Questions marked (RESOLVED — CONTEXT D-NN). **`18-VALIDATION.md` seeded** from RESEARCH §Validation Architecture (stdlib unittest; full suite `python3 -m unittest discover -s tests -q`; 6 Wave-0 items incl. the `_D05_ALLOWLIST_CODES` addition; REQ-P18-01…06 oracle map; catalogue 260→265; D-07 pinned α=0.7598@ordinal + Landis-Koch bands via numeric fixture vs catalog-only via presence-only). dsx plan:pre step-hook self-skips (Phase 18 declaration-only, no ANALYSIS-SPEC — same as Phase 17). Stopped at the ~12-min pacing cap on the GSD-native resumable boundary (init now reports has_research:true, has_plans:false). **S2-2 NOT checked — its gate is plan-checker pass.** NEXT firing: gsd-planner (opus) → gsd-plan-checker (haiku) revision loop → check S2-2 (single-writer waves per 18-CONTEXT.md D-08: 18-A routing+gates+doc/catalogue lockstep ∥ 18-B effect-size convention bands; §13a decision-coverage could-not-parse/total:0 is the known parser mismatch, confirm via plan-checker Dim-7). | 18-RESEARCH.md (4e531df); 18-VALIDATION.md
2026-09-01T22:55Z | S2-1 | DONE — Phase 18 discuss + persona round, run inline by the orchestrator on `gsd/v2.3.0-test-catalog` (branch confirmed; framework `handle_branching` NOT run). **UNBLOCKED this firing:** S1-5 done (Phase 17 CLOSED) AND HQ-16 (Phase 18 D-05 pack) operator-answered 2026-09-01. §4 persona round: Architect (`dsx-analysis-architect`) + Statistician (`dsx-statistician`), opus/high, concurrent, fed S0/S1-verified ground truth (live `recommend_test`/gate structure, Phase-17 D-01/D-06 bindings, HQ-16 answers) — adjudicated, not blind-explored. Auditor lens NOT engaged (declaration-only, no data/security surface). Tie-break rigour>reliability>flexibility. **Converged unanimously**; `18-CONTEXT.md` written with 8 decisions. **D-01** routing = a dataless `recommend_association(estimand_kind)` pure fn (returns acceptable-coefficient SET) + new `_check_declared_association` gate beside an untouched `recommend_test`; the dataless signature IS the anti-two-stage proof (REQ-P18-06). **D-02** five HIGH codes minted from the pre-allocated ranges: **DSX-STA-050** (Pearson/linear vs declared-ordinal operand) + **051** (correlation-for-agreement/method_comparison estimand) in 050–059; **060** (ICC missing model/type/definition triple) + **061** (weighted kappa missing weights) + **062** (kappa missing p_pos/p_neg companions) in 060–069 — split not merged (distinct remedy+citation+predicate each; permanent under D-06); catalogue 260→265. **D-03** 050 whitelists point-biserial + dichotomous (point-biserial IS Pearson-on-{0,1}; false-block guard). **D-04** 062 companions = **p_pos AND p_neg** per the HQ-16 correction (Cicchetti-Feinstein Part II), NOT the requirement's stale "raw agreement + prevalence" paraphrase — cite both parts; requirement-parenthetical alignment offered non-blocking (REQUIREMENTS.md NOT edited — §4 escalation). **D-05** ICC gate = presence+membership only (enumerated model/type/definition vocab); combination-coherence DEFERRED to D-13 (candidate DSX-STA-063). **D-06** report-only effect-size KIND registry — new kinds (kappa/ICC/W/phi/Cramér's V/tau-b/rho) recognised but the blocking DSX-STA-011 band domain stays `EFFECT_SIZE_KINDS={d,h,r}` (flat abs() band is wrong for df-dependent Cramér's V; conventions never block — structural). **D-07** pin α=0.7598@ordinal + Landis-Koch kappa bands (convention-labelled); ICC (Koo-Li values unconfirmed) + Kendall's W (no citation exists) + dCor/partial/Cronbach→omega = catalog-only; P18-03 doctrinal scale citation not-in-hand (block rests on Phase-17 internal defs). **D-08** two file-disjoint single-writer plans preview: 18-A (stats.py+spec.py+test-selection.md+finding-codes.md REGEN+fixtures+gate/no-autoswitch tests, doc/code/catalogue lockstep in one commit) ∥ 18-B (mathx.py bands + APA template + 011/012 tests). Two riders DEFERRED with named triggers (ICC coherence; coeff-typo vocab guard) — smaller provable claim over unrequested surface. Veto window filed = **HQ-20** (non-blocking, silence=accept). STATE.md body refreshed (was stale — still described Phase-17 S1-3). Stopped at the S2-1 boundary (unit complete; ~12-min pacing cap — S2-2 plan is the next opus/high unit). **Next unblocked = S2-2** (Phase 18 plan; plan-checker must pass; single-writer waves per D-08). | 18-CONTEXT.md; HUMAN-QUEUE.md HQ-20; STATE.md
2026-09-01T22:16Z | S1-5 | DONE — Phase 17 secure + validate, run inline by the orchestrator on `gsd/v2.3.0-test-catalog` (branch confirmed; framework `handle_branching` NOT run). **PHASE 17 NOW CLOSED** (S1-1…S1-5 all checked). **`/gsd-secure-phase 17` (State B, `17-SECURITY.md`):** verify:post secure step hook active; all 3 PLANs carry parseable `<threat_model>` → `register_authored_at_plan_time:true`, ASVS L1, `block_on:high`. Built the 11-threat register (8 mitigate + 3 accept=supply-chain/zero-packages); classified **all 11 CLOSED, threats_open:0**. Did NOT trust the L1 short-circuit blind (brief §5): re-ran every non-accepted mitigation → 13 targeted tests green (`test_boschloo_reconciliation` 3/3, `test_time_to_event_fallthrough` 2/2, `test_estimand_kind_vocab` 6/6, `test_finding_catalogue_invariant` 2/2) + `gen-finding-catalogue.py --check` exit 0 ("finding catalogue is current"; DSX-STA-040 NOT among the pre-existing dup-text warnings = single call site) + grep oracle re-confirming the 9 D-12a dispositions & D-06 ranges (050–129, 130s reserve) in committed `17-CONTEXT.md`. `status:verified`. Operator security **Approval** line NOT self-signed → batched HQ-19 (brief §4 item 4). commit 1ab7f8a. **`/gsd-validate-phase 17` (State A, `17-VALIDATION.md`):** nyquist step hook active; all 5 reqs COVERED — 7 automated oracles green (the 13 targeted + 9 golden/good-fixture inc. `test_every_fixture_ship_finding_set_equals_its_golden_baseline` proving the D-08-extended good fixture holds + catalogue `--check`) + 2 manual-only recorded-decision items re-confirmed. **Zero gaps → no nyquist-auditor spawn (workflow §3).** Per-Task Map all green, `status:validated` + `nyquist_compliant:true` + `wave_0_complete:true`, audit trail appended. commit c45f666. **Both commits pushed.** Phase-17 UAT folded into HQ-19 (internal spec/routing phase, no user-facing surface → acceptance = the green automated gates). Stopped at the ~12-min pacing cap on the S1-5 / Phase-17-close boundary (S2-1 is a full opus/high discuss+persona unit — starting it risks mid-unit compaction on a decision artifact, brief §1). **Next unblocked = S2-1** (Phase 18 discuss): now UNBLOCKED — S1-5 done AND HQ-16 (Phase 18 D-05 pack) answered 2026-09-01. | 17-SECURITY.md; 17-VALIDATION.md; HUMAN-QUEUE.md HQ-19; commits 1ab7f8a,c45f666

2026-09-01T22:00Z | S1-4 | DONE — Phase 17 code review + fixes + verification `passed`, run inline by the orchestrator (opus/high, brief §3) on `gsd/v2.3.0-test-catalog` (branch confirmed; framework `handle_branching` NOT run). **Review (`17-REVIEW.md`):** read the full `6baa7ce..HEAD` source diff (2 source files ~88 lines, 4 test files, 3 fixtures, 1 doc row). One LOW finding — `dsx/checks/stats.py:22` annotated `_MEMBERSHIP_FIELDS: "tuple[tuple[str, Any], ...]"` but never imported `Any` (spec.py imports it for the identical `_VOCABULARIES` pattern; no runtime impact under `from __future__ import annotations`, but a latent type-checker / `get_type_hints` defect). **Fix applied:** added `from typing import Any` to stats.py, re-gated green. Cleared with no finding: no DSX-STA-040 double-report (old site reduced to bare `return`), intended Pitfall-2 tightening not a regression, single call site preserved, fisher retained, doc/code lockstep. **Verification (`17-VERIFICATION.md`) = PASSED** all 5 reqs goal-backward: P17-01 Boschloo (test 3/3 + baseline 1/1), P17-02 estimand_kind 6-member (test 6/6, D-08 additive), P17-03 D-12a table recorded pre-implementation (17-CONTEXT.md D-02, 9 gates + Bayesian sibling D-13-deferred), P17-04 count re-measured 260 + D-06 ranges 050–129 committed + fallthrough pin (test 2/2), P17-05 zero-new-codes by set-identity. **Gate re-run by me from a clean tree** (stray DECISIONS.jsonl cleared per HQ note): `scripts/check.sh` → **all checks passed** (Ran 1323 OK; finding catalogue current; capability dsx v2.0.0 conformant; gate contract good-passes/bad-blocks/missing-exits-2; determinism identical); targeted 12/12 ok; catalogue count grep = 260. STATE.md corrected (was stale: completed_plans 2→3, S1-3 done). Stopped at the ~12-min pacing cap on the S1-4 boundary; next unblocked = S1-5 (`/gsd-secure-phase 17` + `/gsd-validate-phase 17`, sign-off batched to HQ; hard-blocks S2/S3). | 17-REVIEW.md; 17-VERIFICATION.md; stats.py import fix (uncommitted→this commit)
2026-09-01T21:49Z | S1-3 | DONE — Wave 2 (17-03) executed inline on `gsd/v2.3.0-test-catalog` as orchestrator (branch confirmed before AND after; framework `handle_branching` NOT run — HQ branch-safety anti-pattern). **17-03 (REQ-P17-02/05):** RED `f684e8e` (`ESTIMAND_KINDS` ImportError + guard/Pitfall-2 asserts fail) → GREEN `d7ca5ce` (spec.py `ESTIMAND_KINDS` 6-member dict registered under singular dump key `estimand_kind`; stats.py DSX-STA-040 widened to a single-call-site `(field,vocab)` membership loop over outcome_type+estimand_kind, run independently of the declared-test early return — Pitfall-2 tightening; D-03a import boundary held) → Task 3 `929a364` (fixtures+template `estimand_kind` extended D-08; `references/finding-codes.md` regen). Gates re-run by me: `test_estimand_kind_vocab` 6/6; `gen-finding-catalogue.py --check` exit 0 = **260 by set identity, zero new codes** (REQ-P17-05), DSX-STA-040 NOT in the pre-existing duplicate-text warnings (single call site); `test_finding_catalogue_invariant`+`test_good_fixture_phase15`+`test_causal_verb_golden` 11/11; **full suite 1323 OK from clean tree**. **Deviation (one repair, brief §5):** the merge gate caught a Wave-1 lockstep gap — REQ-P11-05 pinned snapshot `test_dsx.py::_BASELINE_TWO_PROPORTION_NO_SPEC` still asserted `fisher_exact` + a now-false "stats.py byte-identical to v1.4.0" comment after 17-01's Boschloo change; fixed value+comment+docstring to the reconciled reality (commit `06d4cf6`, REQ-P17-01; `git log v1.4.0..HEAD -- stats.py` confirms the change). **S1-3 CHECKED — all 3 plans executed.** Stopped at the ~12-min pacing cap on the S1-3 boundary; next unblocked = S1-4 (code review + verification, opus/high). | 17-03-SUMMARY.md; commits f684e8e,d7ca5ce,06d4cf6,929a364
2026-09-01T21:35Z | S1-3 | IN PROGRESS (Wave 1 of 2) — executed 17-01 + 17-02 **inline on the ceremony branch, deliberately NOT via `/gsd-execute-phase`'s subagent-wave path**: `init.execute-phase` computes `branch_name: gsd/v2.3-test-catalog` (milestone template `gsd/{milestone}-{slug}`) which MISMATCHES the ceremony branch `gsd/v2.3.0-test-catalog`; its `handle_branching` forks off origin/main and would have ORPHANED every S0/S1 commit (brief §5 / HUMAN-QUEUE branch-safety rule). Ran inline as orchestrator, every gate re-run by me. **17-01 Boschloo (REQ-P17-01):** RED (`c2c91cd` — 2 divergence asserts fail, doc-pin ok) → GREEN (`99622fe`): `boschloo_exact` added to NONPARAMETRIC_TESTS (fisher_exact retained), two-proportion small-cell alt swapped fisher→boschloo, line-69 3-plus-group fisher_exact untouched; `test_boschloo_reconciliation` 3/3, `test_finding_catalogue_invariant` = 260 by set-identity (zero mint), `references/test-selection.md` byte-unedited. **17-02 fallthrough pin (REQ-P17-03/04):** `test_time_to_event_fallthrough` 2/2 (behavioural + source-scan GREEN pin, `048d203`); grep oracle over 17-CONTEXT.md confirms all 9 D-12a gate dispositions + D-13 sibling + D-06 050/130 range anchors. Baseline full suite GREEN (1312) before edits. **Stopped at the ~12-min pacing cap on the clean Wave-1 boundary; 17-03 (estimand_kind vocab + DSX-STA-040 widen + fixtures + catalogue regen, zero-new-codes) is the next unblocked plan.** Handoff: phase `.continue-here.md` + STATE. S1-3 NOT checked (atomic: all plans must execute). Branch-name mismatch filed as a HUMAN-QUEUE standing note. | 17-01/02-SUMMARY.md; commits c2c91cd,99622fe,b10f328,048d203,c09b0b9
2026-08-30T00:21Z | S1-2 | PASS — Phase 17 planned via `/gsd-plan-phase 17 --auto` (resumed at planner→checker per the 23:39Z handoff; research/context/validation preflight already committed). gsd-planner (opus) wrote 3 plans / 2 waves → `## PLANNING COMPLETE`: **17-01** Boschloo reconcile-to-doc (add `boschloo_exact` to NONPARAMETRIC_TESTS + emit it for two-proportion small-expected-cell; pinned regression test) [Wave 1, REQ-P17-01]; **17-02** `time_to_event` fallthrough regression pin + review-verify D-12a nine-gate disposition table & D-06 range pre-alloc discharged in committed 17-CONTEXT.md [Wave 1, REQ-P17-03/04]; **17-03** `estimand_kind` closed 6-member vocab in `dsx/spec.py` (D-01/import-boundary) + loud membership guard by WIDENING DSX-STA-040 in stats.py (no new code) + fixtures/template (D-08) + catalogue regen + zero-new-codes set-identity @260 [Wave 2 depends_on 17-01, REQ-P17-02/05]. gsd-plan-checker (haiku) iter-1 → 1 BLOCKER, all 12 plan dims green EXCEPT Dim-11 research-resolution (17-RESEARCH.md `## Open Questions` lacked (RESOLVED) markers); orchestrator fixed inline (header + 2 inline RESOLVED markers reflecting the planner's actual choices) → iter-2 → `## VERIFICATION PASSED`, all dims green, REQ-P17-01..05 covered. **Gates re-run by orchestrator:** §13 requirements 5/5; dsx plan:post BLOCKING gate `dsx gate plan --allow-missing --verbose` = exit 0 clean spec-less (non-analytical, no ANALYSIS-SPEC minted). **§13a decision-coverage CLI = could-not-parse/total:0** — deterministic parser (`decisions.cjs` regexes expect `- **D-NN:** …`/em-dash-in-bold) can't read 17-CONTEXT.md's `- **D-12a …** prose` bullet style; NOT a real gap (plan-checker Dim-7 substantively confirmed D-01..D-04 honored) — filed as standing note (recurs at P18/19 plan gates). Next unblocked = S1-3 (execute all plans; `/gsd-execute-phase 17`). | 17-01/02/03-PLAN.md; STATE.md; 17-RESEARCH.md
2026-08-29T23:39Z | S1-2 | IN PROGRESS (plan preflight) — `/gsd-plan-phase 17`: Nyquist-required planner inputs produced + committed. gsd-phase-researcher (sonnet) → `17-RESEARCH.md` (c0d7df3; all locators live-tree-verified: `estimand_kind` must live in `dsx/spec.py` not `stats.py` [enforced import boundary]; zero-new-codes achieved by WIDENING existing `DSX-STA-040` at stats.py:440-447 — mirroring `_VALIDITY_FRAME_MEMBERSHIP` spec.py:1165-1176/1279-1298 — not minting; the REQ-P17-05 set-identity gate ALREADY EXISTS = `tests/test_finding_catalogue_invariant.py`; fixtures to extend = `examples/good|bad-ANALYSIS-SPEC.yaml`; Boschloo = stats.py:65 string + add to NONPARAMETRIC_TESTS:23-26, doc side already correct). `17-VALIDATION.md` seeded from RESEARCH §Validation Architecture (stdlib `unittest`; full suite `python3 -m unittest discover -s tests -q`; 3 Wave-0 test files). **dsx plan:pre step-hook correctly SKIPPED** — Phase 17 is non-analytical (no number/model/chart), so no ANALYSIS-SPEC.yaml and `dsx gate plan` passes clean on a spec-less phase (its own step-hook rule + CONTEXT.md boundary). Stopped at the ~12-min pacing cap on a GSD-native resumable boundary (`init.plan-phase` now reports has_research:true). **NEXT firing:** gsd-planner (opus) → gsd-plan-checker (haiku) revision loop → then check S1-2 [pattern-mapper is optional/low-value — analogs already surfaced in RESEARCH]. Verify `dsx gate plan` clean at plan:post. | 17-RESEARCH.md; 17-VALIDATION.md
2026-08-29T21:48Z | S1-1 | PASS — Phase 17 discuss complete; `17-CONTEXT.md` written (gate = the three decisions settled). §4 persona round: Architect (`dsx-analysis-architect`) + Statistician (`dsx-statistician`), opus/high, concurrent, run inline by the orchestrator (single-artifact unit that must complete without mid-unit compaction, brief §1) with S0-verified ground truth fed in so they adjudicated rather than blind-explored. Tie-break rigour>reliability>flexibility. **D-01 `estimand_kind`**: kept the name (rejected Architect's rename-off-"estimand" — `relationship_kind` misnames the agreement/method_comparison members + churns inherited scope; mis-slot answered structurally: different block from `estimand_type`, disjoint closed-vocab membership guards → swapped value is a loud decidable error). Closed **6-member** set on `analysis:` = linear/monotone/**nominal**/agreement/method_comparison/ordered_trend; +nominal_association was unanimous (Cramér's V/phi = unsigned χ²-dependence, not signed Pearson r — folding it into linear mis-carves the estimand space). Absence non-blocking (D-10). **D-02** disposition table for all 9 Phase 18/19 gates: 8 paradigm-neutral/self-scoping; the observed-power ban SHIPS (rationale = power≡monotone f(p), Hoenig-Heisey) with its Bayesian sibling (post-hoc Bayes-factor / posterior "prob. of replication" as design adequacy) **named + D-13-deferred** (no live Bayesian post-hoc surface → no asymmetric ban). Two rigour riders adopted: P19-06a Zimmerman-2004 is two-group (binds HQ-17 F1 for Phase 19); P19-01 mixed-model/GEE pointer rows are the un-blocked route. **D-03** D-06 pre-alloc: all new codes DSX-STA-*, one decade/theme 050–129 (keyed to re-measured count 260, S0-2), 130s reserve; Phase 17 assigns none (REQ-P17-05). **D-04** Boschloo → reconcile-to-doc + pinned regression test (binds S1-3). Veto window filed = HQ-18 (non-blocking, silence=accept). Next unblocked = S1-2 (plan; plan-checker must pass). | 17-CONTEXT.md; HUMAN-QUEUE.md `### HQ-18`
2026-08-29T21:35Z | S0-4 | PASS — Phase 19 D-05 citation evidence pack filed to HUMAN-QUEUE as HQ-17 (non-blocking; NOT signed — D-05 is a human read). 16 citations in 7 groups keyed to the REQ-P19-01…07 gates; every bibliographic locator corroborated across ≥2 independent sources (Crossref / publisher / PubMed / DOI-resolver / citation-index) via 4 parallel research agents. 7 load-bearing reads flagged: McCullagh-Nelder "§6.2" UNCONFIRMED (secondary cites give p.206 / §6.3.2 → ships CHAPTER-granular per brief §5); Hayter 1986 = JASA 81(396):1000-1004, NOT the 1984 Annals Tukey-Kramer decoy; Newcombe = Paper B (SiM 17(8):873-890, DOI …SIM779, PMID 9595617), not the single-proportion Paper A (857-872/SIM777); Davidson-MacKinnon B = 399/1499 recommended vs 19/99 exactness-floor (don't conflate); Brown-Cai-DasGupta n≤40 cutoff is secondary-source-only; Campbell 2007 = revalidated N-1 (Egon Pearson originated it), cite "argued for" not "invented"; Greenhouse-Geisser = 1959 Psychometrika, not the reversed-author 1958 Annals paper. TIMESTAMP CORRECTION: real UTC via `date -u` = 21:35Z; prior S0-1..S0-3 Log times (22:00-22:40Z) ran ~2.5h ahead of the actual git commit times (S0-3 committed 20:14Z) — those were estimated, this one is measured, hence the Log appears to step backward here. S0 bootstrap now COMPLETE (S0-1..S0-4 all done); next unblocked unit = S1-1 (Phase 17 discuss). | HUMAN-QUEUE.md `### HQ-17`
2026-08-29T22:40Z | S0-3 | PASS — Phase 18 D-05 citation evidence pack filed to HUMAN-QUEUE as HQ-16 (non-blocking; NOT signed — D-05 is a human read). 11 citations grouped into 6 sets keyed to the granularity ruling (read-per-CODE): A Fisher 1921 (Pearson Fisher-z CI); B Cohen 1960/1968 + Fleiss 1971 + Hayes-Krippendorff 2007; C Shrout-Fleiss 1979 + McGraw-Wong 1996 (ICC triple); D Bland-Altman 1986; E Feinstein-Cicchetti 1990 (kappa companion gate); F Landis-Koch 1977 + Koo-Li 2016 (bands=conventions). All 11 bibliographic locators corroborated across ≥2 independent sources (venue/vol/pages/DOI-or-PMID). Loop caught: Krippendorff worked value = α 0.743 (0.734 in the textbook is a known typo) — the one load-bearing numeric-fixture read; Landis-Koch has TWO 1977 Biometrics papers, bands are in 33(1):159-174 not 33(2):363-374; McGraw-Wong 1996 has a published erratum; Koo-Li band VALUES not confirmed by search (candidate-only). Per brief §5, any fixture/band unconfirmed at source ships catalog-only/convention-labelled. Next = S0-4 (Phase 19 pack → HQ-17). | HUMAN-QUEUE.md `### HQ-16`
2026-08-29T22:10Z | S0-2 | PASS — inherited scope re-verified against live tree; `.planning/v2.3-SCOPE-RECHECK.md` written. All 5 load-bearing premises CONFIRMED at locators: fisher_exact fallback at stats.py:65; boschloo_exact absent from NONPARAMETRIC_TESTS (stats.py:23-26); doc side still names Boschloo (test-selection.md:10 + [^1] Lydersen-Fagerland-Laake 2009) — divergence genuinely live; QUESTION_TYPES closed 5-key (spec.py:22-28) + estimand_kind absent everywhere; time_to_event fallthrough unconditional (stats.py:128-129). Catalogue RE-MEASURED = 260 codes (matches checked-in finding-codes.md; = REQ-P17-05 baseline). All 22 reqs still-valid, none satisfied/contradicted — execution, not re-scoping. Next = S0-3. | .planning/v2.3-SCOPE-RECHECK.md
2026-08-29T22:00Z | S0-1 | PASS — GSD state points at v2.3: STATE.md frontmatter milestone v2.3 / current_phase 17 / progress 0/4; `node gsd-tools.cjs query init.milestone-op` resolves (phase_count 4, completed_phases 0, all_phases_complete false, exit 0); .planning/phases/ empty (only .gitkeep, v2.2 dirs archived). Note: gsd-tools not on Bash PATH — invoke via `node ~/.claude/gsd-core/bin/gsd-tools.cjs`. Next = S0-2. | LOOP-LEDGER-ARCHIVE.md `## S0-1`
2026-08-29T21:30Z | milestone-open | v2.3 opened by operator direction in an interactive session: scope researched (6-agent workflow: 2 repo mappers, 3 domain researchers, 1 adversarial critic — journal preserved), .planning/research/V2.3-V2.4-SCOPE.md + REQUIREMENTS.md (REQ-P17-01..P20-04) + ROADMAP.md (Phases 17-20 active, 21-24 queued) written; STATE.md repointed (v2.3, phase 17, 0/4); v2.2 loop artifacts archived to .planning/milestones/v2.2-LOOP-*, v2.2-HUMAN-QUEUE.md; this ledger + LOOP-BRIEF + HUMAN-QUEUE rewritten for v2.3; branch gsd/v2.3.0-test-catalog created from main; firing script repointed + usage-limit backoff added (weekly reset Wednesday 13:00 UTC = 10:00 São Paulo; 5h-window = 60-min hold). Operator directive of record: token limits WILL hit this week — firings log one line and stop on limit errors, the wrapper owns pacing and self-resumes. Next = S0-1. | .planning/research/V2.3-V2.4-SCOPE.md; scripts/run-ceremony-firing.ps1
