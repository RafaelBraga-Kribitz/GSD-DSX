# Catch-attribution sidecar (Phase 12 D-06/D-07, REQ-P12-02).
#
# Machine-countable catch-attribution for the paired
# retracted-fabricated-field-experiment fixture. Lives OUTSIDE the ANALYSIS-SPEC
# (D-06) so it cannot perturb frame_digest (placement precedent 11.2 D-08 /
# dsx/decisions.py:99-121 — NOT 11.3 D-08). No DSX-* code is minted (D-18):
# absent_code REFERENCES an existing shipped catalogue code.
#
# Schema (D-07): absent_code (required), promotes_backlog_item (required),
# rationale? (optional), kind? (optional, default "miss").

absent_code: DSX-REP-020
promotes_backlog_item: "6.5-item-7-feature-provenance"
kind: miss
rationale: >
  The reported effect rests on fabricated data. A plausible warehouse source and
  period are declared, so the nearest shipped provenance code DSX-REP-020 (source
  cannot be pinned to a specific extract) does not fire — the fabrication is
  downstream of what the declaration exposes. Attributing it needs §6.5 item 7's
  provenance/origin work (verifying a declared extract corresponds to real
  collected data), which a declaration-only gate structurally cannot do.
