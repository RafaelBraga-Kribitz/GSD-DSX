# Catch-attribution sidecar (Phase 28 D-28-04/D-06/D-07, REQ-P28-01/03).
#
# Machine-countable catch-attribution for the paired
# magnitude-without-computed-effect fixture. Lives OUTSIDE the ANALYSIS-SPEC (D-06)
# so it cannot perturb frame_digest (placement precedent 11.2 D-08 /
# dsx/decisions.py:99-121). No DSX-* code is minted here: absent_code REFERENCES the
# code shipped in plan 28-01 (DSX-CLM-034, catalogue Total 278).
#
# Schema (D-07): absent_code (required), promotes_backlog_item (required),
# rationale? (optional), kind? (optional, default "miss"),
# protocol_adherence? (optional, additive — Phase 16 REQ-P16-03/D-10), reported
# beside catch rate / FPR, never inside them.

absent_code: DSX-CLM-034
promotes_backlog_item: "6.5-item-8-magnitude-without-computed-effect"
kind: miss
# Whether the dsx-reproduce protocol was followed for this hand-authored fixture.
# Vocabulary: adhered | skipped | not_applicable. This fixture never ran the
# reproduce skill, so 'skipped' is the honest value — accepted-but-ignored by
# calibration (REQ-P16-03 / D-10).
protocol_adherence: skipped
rationale: >
  The headline churn magnitude (27% vs 18%) is quoted for a metric no results.tests
  entry computes; the two computed tests are on other metrics whose reported effects
  coincide with 27 and 18 via the x100 proportion bridge, so DSX-CLM-033 clears on
  its full union-membership logic and nothing verifies that any test measured churn
  (the measured LIVE MISS, 28-MEASUREMENT.md). DSX-CLM-034 reads a declared
  claim-to-cited-test traceability pointer and checks the magnitude traces to the
  specific cited test; this honest spec declares no such pointer, so the code
  correctly stays silent — attribution, not detection. Attributing the miss needs
  §6.5 item 8's claim->test metric-binding work, which a declaration-only gate reads
  only when the pointer is present. The blocking DSX-COH-001 residual is a
  swap-invariant, defect-orthogonal incidental (claim strength vs question framing),
  not this fixture's target defect.
