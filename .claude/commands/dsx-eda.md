---
description: "CSV-first entry — profile and explore an extract (routes to dsx-explore-data)."
argument-hint: "explore <extract.csv>"
---

> **Optional, Claude-Code-only, non-load-bearing sugar.** The portable path is the
> alias table in `docs/operating-guide.md`; this shim is not the sole path.

Invoke the **dsx-explore-data** skill, taking the CSV as an argument
(`explore <extract.csv>`) — the CSV is passed as an argument, not dropped into a
watched folder.

$ARGUMENTS
